/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12pipelinelibrary.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12PIPELINELIBRARY_H
#define NPT_PROTOCOL_GUEST_ID3D12PIPELINELIBRARY_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12PipelineLibrary::StorePipeline
 */

struct npt_command_ID3D12PipelineLibrary_StorePipeline {
    void *_self;
    const WCHAR * pName;
    ID3D12PipelineState * pPipeline;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_StorePipeline(const WCHAR * pName,const ID3D12PipelineState * pPipeline)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(pName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12PipelineLibrary_StorePipeline(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const WCHAR * pName,
                                               ID3D12PipelineState * pPipeline)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12PipelineLibrary_StorePipeline(pName,pPipeline);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                 NPT_METHOD_ID3D12PipelineLibrary_StorePipeline),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, pName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_com_handle(enc, npt_object_get_id(pPipeline));
}

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_StorePipeline_reply(const WCHAR * pName,const ID3D12PipelineState * pPipeline)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pName (input only) */
/* skip pPipeline (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12PipelineLibrary_StorePipeline_reply(struct npt_cs_decoder *dec,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                                  NPT_METHOD_ID3D12PipelineLibrary_StorePipeline))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12PipelineLibrary_StorePipeline reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                       NPT_METHOD_ID3D12PipelineLibrary_StorePipeline));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12PipelineLibrary_StorePipeline(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const WCHAR * pName,
                                               ID3D12PipelineState * pPipeline,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12PipelineLibrary_StorePipeline(pName,pPipeline);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12PipelineLibrary_StorePipeline_reply(pName,pPipeline)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12PipelineLibrary_StorePipeline(enc, cmd_flags, object_id, pName, pPipeline);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12PipelineLibrary_StorePipeline(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const WCHAR * pName,
                                              ID3D12PipelineState * pPipeline)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_StorePipeline(ring, 0, object_id, pName, pPipeline, &submit);
}

static inline HRESULT
npt_call_ID3D12PipelineLibrary_StorePipeline(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const WCHAR * pName,
                                             ID3D12PipelineState * pPipeline)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_StorePipeline(ring, NPT_CMD_FLAG_REPLY, object_id, pName, pPipeline, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12PipelineLibrary_StorePipeline_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12PipelineLibrary::LoadGraphicsPipeline
 */

struct npt_command_ID3D12PipelineLibrary_LoadGraphicsPipeline {
    void *_self;
    const WCHAR * pName;
    const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc;
    const IID * riid;
    void ** ppPipelineState;
    /* Shadow: the guest-allocated id for ppPipelineState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineState -> *ppPipelineState} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_LoadGraphicsPipeline(const WCHAR * pName,const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(pName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_GRAPHICS_PIPELINE_STATE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppPipelineState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12PipelineLibrary_LoadGraphicsPipeline(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      const WCHAR * pName,
                                                      const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,
                                                      const IID * riid,
                                                      void ** ppPipelineState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12PipelineLibrary_LoadGraphicsPipeline(pName,pDesc,riid,ppPipelineState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                 NPT_METHOD_ID3D12PipelineLibrary_LoadGraphicsPipeline),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, pName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_GRAPHICS_PIPELINE_STATE_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppPipelineState = 0;
    if (ppPipelineState) {
        _gid_ppPipelineState = npt_com_allocate_next_id();
        *ppPipelineState = (void *)(uintptr_t)_gid_ppPipelineState;
    }
    npt_encode_uint64_t(enc, &_gid_ppPipelineState);
}
}

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_LoadGraphicsPipeline_reply(const WCHAR * pName,const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pName (input only) */
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppPipelineState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12PipelineLibrary_LoadGraphicsPipeline_reply(struct npt_cs_decoder *dec,
                                                            void ** ppPipelineState,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                                  NPT_METHOD_ID3D12PipelineLibrary_LoadGraphicsPipeline))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12PipelineLibrary_LoadGraphicsPipeline reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                       NPT_METHOD_ID3D12PipelineLibrary_LoadGraphicsPipeline));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppPipelineState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12PipelineLibrary_LoadGraphicsPipeline(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      const WCHAR * pName,
                                                      const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,
                                                      const IID * riid,
                                                      void ** ppPipelineState,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12PipelineLibrary_LoadGraphicsPipeline(pName,pDesc,riid,ppPipelineState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12PipelineLibrary_LoadGraphicsPipeline_reply(pName,pDesc,riid,ppPipelineState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12PipelineLibrary_LoadGraphicsPipeline(enc, cmd_flags, object_id, pName, pDesc, riid, ppPipelineState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12PipelineLibrary_LoadGraphicsPipeline(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     const WCHAR * pName,
                                                     const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,
                                                     const IID * riid,
                                                     void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_LoadGraphicsPipeline(ring, 0, object_id, pName, pDesc, riid, ppPipelineState, &submit);
}

static inline HRESULT
npt_call_ID3D12PipelineLibrary_LoadGraphicsPipeline(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    const WCHAR * pName,
                                                    const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,
                                                    const IID * riid,
                                                    void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_LoadGraphicsPipeline(ring, NPT_CMD_FLAG_REPLY, object_id, pName, pDesc, riid, ppPipelineState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12PipelineLibrary_LoadGraphicsPipeline_reply(dec, ppPipelineState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12PipelineLibrary::LoadComputePipeline
 */

struct npt_command_ID3D12PipelineLibrary_LoadComputePipeline {
    void *_self;
    const WCHAR * pName;
    const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc;
    const IID * riid;
    void ** ppPipelineState;
    /* Shadow: the guest-allocated id for ppPipelineState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineState -> *ppPipelineState} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_LoadComputePipeline(const WCHAR * pName,const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(pName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_COMPUTE_PIPELINE_STATE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppPipelineState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12PipelineLibrary_LoadComputePipeline(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const WCHAR * pName,
                                                     const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,
                                                     const IID * riid,
                                                     void ** ppPipelineState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12PipelineLibrary_LoadComputePipeline(pName,pDesc,riid,ppPipelineState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                 NPT_METHOD_ID3D12PipelineLibrary_LoadComputePipeline),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, pName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_COMPUTE_PIPELINE_STATE_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppPipelineState = 0;
    if (ppPipelineState) {
        _gid_ppPipelineState = npt_com_allocate_next_id();
        *ppPipelineState = (void *)(uintptr_t)_gid_ppPipelineState;
    }
    npt_encode_uint64_t(enc, &_gid_ppPipelineState);
}
}

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_LoadComputePipeline_reply(const WCHAR * pName,const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pName (input only) */
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppPipelineState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12PipelineLibrary_LoadComputePipeline_reply(struct npt_cs_decoder *dec,
                                                           void ** ppPipelineState,
                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                                  NPT_METHOD_ID3D12PipelineLibrary_LoadComputePipeline))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12PipelineLibrary_LoadComputePipeline reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                       NPT_METHOD_ID3D12PipelineLibrary_LoadComputePipeline));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppPipelineState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12PipelineLibrary_LoadComputePipeline(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const WCHAR * pName,
                                                     const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,
                                                     const IID * riid,
                                                     void ** ppPipelineState,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12PipelineLibrary_LoadComputePipeline(pName,pDesc,riid,ppPipelineState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12PipelineLibrary_LoadComputePipeline_reply(pName,pDesc,riid,ppPipelineState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12PipelineLibrary_LoadComputePipeline(enc, cmd_flags, object_id, pName, pDesc, riid, ppPipelineState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12PipelineLibrary_LoadComputePipeline(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    const WCHAR * pName,
                                                    const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,
                                                    const IID * riid,
                                                    void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_LoadComputePipeline(ring, 0, object_id, pName, pDesc, riid, ppPipelineState, &submit);
}

static inline HRESULT
npt_call_ID3D12PipelineLibrary_LoadComputePipeline(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   const WCHAR * pName,
                                                   const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,
                                                   const IID * riid,
                                                   void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_LoadComputePipeline(ring, NPT_CMD_FLAG_REPLY, object_id, pName, pDesc, riid, ppPipelineState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12PipelineLibrary_LoadComputePipeline_reply(dec, ppPipelineState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12PipelineLibrary::GetSerializedSize
 */

struct npt_command_ID3D12PipelineLibrary_GetSerializedSize {
    void *_self;
    SIZE_T ret;
};

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_GetSerializedSize(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12PipelineLibrary_GetSerializedSize(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12PipelineLibrary_GetSerializedSize();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                 NPT_METHOD_ID3D12PipelineLibrary_GetSerializedSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_GetSerializedSize_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_SIZE_T(&(const SIZE_T){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12PipelineLibrary_GetSerializedSize_reply(struct npt_cs_decoder *dec,
                                                         SIZE_T *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                                  NPT_METHOD_ID3D12PipelineLibrary_GetSerializedSize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12PipelineLibrary_GetSerializedSize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                       NPT_METHOD_ID3D12PipelineLibrary_GetSerializedSize));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_SIZE_T(dec, ret);
}

static inline void
npt_submit_ID3D12PipelineLibrary_GetSerializedSize(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12PipelineLibrary_GetSerializedSize();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12PipelineLibrary_GetSerializedSize_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12PipelineLibrary_GetSerializedSize(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12PipelineLibrary_GetSerializedSize(struct npt_ring *ring,
                                                  npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_GetSerializedSize(ring, 0, object_id, &submit);
}

static inline SIZE_T
npt_call_ID3D12PipelineLibrary_GetSerializedSize(struct npt_ring *ring,
                                                 npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_GetSerializedSize(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        SIZE_T _ret;
        npt_decode_ID3D12PipelineLibrary_GetSerializedSize_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (SIZE_T){0};
    }
}

/*
 * ID3D12PipelineLibrary::Serialize
 */

struct npt_command_ID3D12PipelineLibrary_Serialize {
    void *_self;
    void * pData;
    SIZE_T DataSizeInBytes;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_Serialize(const void * pData,const SIZE_T DataSizeInBytes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pData (output only) */
    cmd_size += npt_sizeof_SIZE_T(&DataSizeInBytes, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12PipelineLibrary_Serialize(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           void * pData,
                                           SIZE_T DataSizeInBytes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12PipelineLibrary_Serialize(pData,DataSizeInBytes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                 NPT_METHOD_ID3D12PipelineLibrary_Serialize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pData (output only) */
    npt_encode_SIZE_T(enc, &DataSizeInBytes);
}

static inline size_t
npt_sizeof_ID3D12PipelineLibrary_Serialize_reply(const void * pData,const SIZE_T DataSizeInBytes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSizeInBytes);
        cmd_size += npt_sizeof_blob_array(pData, DataSizeInBytes);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip DataSizeInBytes (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12PipelineLibrary_Serialize_reply(struct npt_cs_decoder *dec,
                                                 void * pData,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                                  NPT_METHOD_ID3D12PipelineLibrary_Serialize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12PipelineLibrary_Serialize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary,
                                       NPT_METHOD_ID3D12PipelineLibrary_Serialize));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pData)
            npt_decode_blob_array(dec, (void *)pData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D12PipelineLibrary_Serialize(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           void * pData,
                                           SIZE_T DataSizeInBytes,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12PipelineLibrary_Serialize(pData,DataSizeInBytes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12PipelineLibrary_Serialize_reply(pData,DataSizeInBytes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12PipelineLibrary_Serialize(enc, cmd_flags, object_id, pData, DataSizeInBytes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12PipelineLibrary_Serialize(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          void * pData,
                                          SIZE_T DataSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_Serialize(ring, 0, object_id, pData, DataSizeInBytes, &submit);
}

static inline HRESULT
npt_call_ID3D12PipelineLibrary_Serialize(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         void * pData,
                                         SIZE_T DataSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary_Serialize(ring, NPT_CMD_FLAG_REPLY, object_id, pData, DataSizeInBytes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12PipelineLibrary_Serialize_reply(dec, pData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12PipelineLibrary1::LoadPipeline
 */

struct npt_command_ID3D12PipelineLibrary1_LoadPipeline {
    void *_self;
    const WCHAR * pName;
    const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc;
    const IID * riid;
    void ** ppPipelineState;
    /* Shadow: the guest-allocated id for ppPipelineState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineState -> *ppPipelineState} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12PipelineLibrary1_LoadPipeline(const WCHAR * pName,const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(pName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_PIPELINE_STATE_STREAM_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppPipelineState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12PipelineLibrary1_LoadPipeline(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const WCHAR * pName,
                                               const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,
                                               const IID * riid,
                                               void ** ppPipelineState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12PipelineLibrary1_LoadPipeline(pName,pDesc,riid,ppPipelineState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary1,
                                 NPT_METHOD_ID3D12PipelineLibrary1_LoadPipeline),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, pName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_PIPELINE_STATE_STREAM_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppPipelineState = 0;
    if (ppPipelineState) {
        _gid_ppPipelineState = npt_com_allocate_next_id();
        *ppPipelineState = (void *)(uintptr_t)_gid_ppPipelineState;
    }
    npt_encode_uint64_t(enc, &_gid_ppPipelineState);
}
}

static inline size_t
npt_sizeof_ID3D12PipelineLibrary1_LoadPipeline_reply(const WCHAR * pName,const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pName (input only) */
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppPipelineState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12PipelineLibrary1_LoadPipeline_reply(struct npt_cs_decoder *dec,
                                                     void ** ppPipelineState,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary1,
                                                  NPT_METHOD_ID3D12PipelineLibrary1_LoadPipeline))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12PipelineLibrary1_LoadPipeline reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12PipelineLibrary1,
                                       NPT_METHOD_ID3D12PipelineLibrary1_LoadPipeline));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppPipelineState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12PipelineLibrary1_LoadPipeline(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const WCHAR * pName,
                                               const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,
                                               const IID * riid,
                                               void ** ppPipelineState,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12PipelineLibrary1_LoadPipeline(pName,pDesc,riid,ppPipelineState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12PipelineLibrary1_LoadPipeline_reply(pName,pDesc,riid,ppPipelineState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12PipelineLibrary1_LoadPipeline(enc, cmd_flags, object_id, pName, pDesc, riid, ppPipelineState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12PipelineLibrary1_LoadPipeline(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const WCHAR * pName,
                                              const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,
                                              const IID * riid,
                                              void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary1_LoadPipeline(ring, 0, object_id, pName, pDesc, riid, ppPipelineState, &submit);
}

static inline HRESULT
npt_call_ID3D12PipelineLibrary1_LoadPipeline(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const WCHAR * pName,
                                             const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,
                                             const IID * riid,
                                             void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12PipelineLibrary1_LoadPipeline(ring, NPT_CMD_FLAG_REPLY, object_id, pName, pDesc, riid, ppPipelineState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12PipelineLibrary1_LoadPipeline_reply(dec, ppPipelineState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12PIPELINELIBRARY_H */
