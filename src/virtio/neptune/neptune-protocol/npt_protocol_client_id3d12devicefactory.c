/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for id3d12devicefactory: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_id3d12devicefactory.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* ID3D12DeviceFactory                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12devicefactory_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12DeviceFactory,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12devicefactory_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_InitializeFromGlobalState(void *self)
{
    npt_async_ID3D12DeviceFactory_InitializeFromGlobalState(npt_com_self_ring(self),npt_com_self_id(self));
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_ApplyToGlobalState(void *self)
{
    npt_async_ID3D12DeviceFactory_ApplyToGlobalState(npt_com_self_ring(self),npt_com_self_id(self));
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_SetFlags(void *self, D3D12_DEVICE_FACTORY_FLAGS flags)
{
    npt_async_ID3D12DeviceFactory_SetFlags(npt_com_self_ring(self),npt_com_self_id(self),flags);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

D3D12_DEVICE_FACTORY_FLAGS NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_GetFlags(void *self)
{
    D3D12_DEVICE_FACTORY_FLAGS _ret = npt_call_ID3D12DeviceFactory_GetFlags(npt_com_self_ring(self),npt_com_self_id(self));
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_GetConfigurationInterface(void *self, const CLSID * clsid, const IID * iid, void ** ppv)
{
    void *_raw_ppv = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppv = ppv;
    if (ppv)
        ppv = (void **)&_raw_ppv;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D12DeviceFactory_GetConfigurationInterface(npt_com_self_ring(self),npt_com_self_id(self),clsid,iid,ppv);
    } else {
        npt_async_ID3D12DeviceFactory_GetConfigurationInterface(npt_com_self_ring(self),npt_com_self_id(self),clsid,iid,ppv);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppv) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppv)
            *_orig_ppv = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                iid,
                (uint64_t)(uintptr_t)_raw_ppv,
                (struct npt_com_base *)self);
        else
            *_orig_ppv = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_EnableExperimentalFeatures(void *self, UINT NumFeatures, const IID * pIIDs, void * pConfigurationStructs, UINT * pConfigurationStructSizes)
{
    npt_async_ID3D12DeviceFactory_EnableExperimentalFeatures(npt_com_self_ring(self),npt_com_self_id(self),NumFeatures,pIIDs,pConfigurationStructs,pConfigurationStructSizes);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12devicefactory_default_CreateDevice(void *self, IUnknown * adapter, D3D_FEATURE_LEVEL FeatureLevel, const IID * riid, void ** ppvDevice)
{
    void *_raw_ppvDevice = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvDevice = ppvDevice;
    if (ppvDevice)
        ppvDevice = (void **)&_raw_ppvDevice;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D12DeviceFactory_CreateDevice(npt_com_self_ring(self),npt_com_self_id(self),adapter,FeatureLevel,riid,ppvDevice);
    } else {
        npt_async_ID3D12DeviceFactory_CreateDevice(npt_com_self_ring(self),npt_com_self_id(self),adapter,FeatureLevel,riid,ppvDevice);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppvDevice) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppvDevice)
            *_orig_ppvDevice = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvDevice,
                (struct npt_com_base *)self);
        else
            *_orig_ppvDevice = NULL;
    }
    return _ret;
}


void
npt_id3d12devicefactory_fill_default_vtbl(struct npt_id3d12devicefactory_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12devicefactory_default_QueryInterface;
    dst->AddRef         = npt_id3d12devicefactory_default_AddRef;
    dst->Release        = npt_id3d12devicefactory_default_Release;
    dst->InitializeFromGlobalState = npt_id3d12devicefactory_default_InitializeFromGlobalState;
    dst->ApplyToGlobalState = npt_id3d12devicefactory_default_ApplyToGlobalState;
    dst->SetFlags = npt_id3d12devicefactory_default_SetFlags;
    dst->GetFlags = npt_id3d12devicefactory_default_GetFlags;
    dst->GetConfigurationInterface = npt_id3d12devicefactory_default_GetConfigurationInterface;
    dst->EnableExperimentalFeatures = npt_id3d12devicefactory_default_EnableExperimentalFeatures;
    dst->CreateDevice = npt_id3d12devicefactory_default_CreateDevice;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12devicefactory_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12devicefactory_client_vtbl npt_id3d12devicefactory_default_vtbl_storage;

void
npt_id3d12devicefactory_install_default_vtbl(void)
{
    npt_id3d12devicefactory_fill_default_vtbl(&npt_id3d12devicefactory_default_vtbl_storage);
}

void *
npt_id3d12devicefactory_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12devicefactory_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
