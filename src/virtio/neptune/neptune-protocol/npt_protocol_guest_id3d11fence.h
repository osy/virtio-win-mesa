/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11fence.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11FENCE_H
#define NPT_PROTOCOL_GUEST_ID3D11FENCE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11Fence::CreateSharedHandle
 */

struct npt_command_ID3D11Fence_CreateSharedHandle {
    void *_self;
    const SECURITY_ATTRIBUTES * pAttributes;
    DWORD dwAccess;
    const WCHAR * lpName;
    HANDLE * pHandle;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Fence_CreateSharedHandle(const SECURITY_ATTRIBUTES * pAttributes,const DWORD dwAccess,const WCHAR * lpName,const HANDLE * pHandle)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    /* pAttributes: non-serializable, skipped */
    cmd_size += npt_sizeof_DWORD(&dwAccess, max_mode);
    if (lpName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)lpName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(lpName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip pHandle (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Fence_CreateSharedHandle(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const SECURITY_ATTRIBUTES * pAttributes,
                                          DWORD dwAccess,
                                          const WCHAR * lpName,
                                          HANDLE * pHandle)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Fence_CreateSharedHandle(pAttributes,dwAccess,lpName,pHandle);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                 NPT_METHOD_ID3D11Fence_CreateSharedHandle),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    /* pAttributes: non-serializable, skipped */
    npt_encode_DWORD(enc, &dwAccess);
    if (lpName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)lpName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, lpName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pHandle (output only) */
}

static inline size_t
npt_sizeof_ID3D11Fence_CreateSharedHandle_reply(const SECURITY_ATTRIBUTES * pAttributes,const DWORD dwAccess,const WCHAR * lpName,const HANDLE * pHandle)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAttributes (input only) */
/* skip dwAccess (input only) */
/* skip lpName (input only) */
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Fence_CreateSharedHandle_reply(struct npt_cs_decoder *dec,
                                                HANDLE * pHandle,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                                  NPT_METHOD_ID3D11Fence_CreateSharedHandle))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Fence_CreateSharedHandle reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                       NPT_METHOD_ID3D11Fence_CreateSharedHandle));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        if (pHandle)
            *pHandle = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
}

static inline void
npt_submit_ID3D11Fence_CreateSharedHandle(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const SECURITY_ATTRIBUTES * pAttributes,
                                          DWORD dwAccess,
                                          const WCHAR * lpName,
                                          HANDLE * pHandle,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Fence_CreateSharedHandle(pAttributes,dwAccess,lpName,pHandle);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Fence_CreateSharedHandle_reply(pAttributes,dwAccess,lpName,pHandle)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Fence_CreateSharedHandle(enc, cmd_flags, object_id, pAttributes, dwAccess, lpName, pHandle);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Fence_CreateSharedHandle(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const SECURITY_ATTRIBUTES * pAttributes,
                                         DWORD dwAccess,
                                         const WCHAR * lpName,
                                         HANDLE * pHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Fence_CreateSharedHandle(ring, 0, object_id, pAttributes, dwAccess, lpName, pHandle, &submit);
}

static inline HRESULT
npt_call_ID3D11Fence_CreateSharedHandle(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const SECURITY_ATTRIBUTES * pAttributes,
                                        DWORD dwAccess,
                                        const WCHAR * lpName,
                                        HANDLE * pHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Fence_CreateSharedHandle(ring, NPT_CMD_FLAG_REPLY, object_id, pAttributes, dwAccess, lpName, pHandle, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Fence_CreateSharedHandle_reply(dec, pHandle, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Fence::GetCompletedValue
 */

struct npt_command_ID3D11Fence_GetCompletedValue {
    void *_self;
    UINT64 ret;
};

static inline size_t
npt_sizeof_ID3D11Fence_GetCompletedValue(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Fence_GetCompletedValue(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Fence_GetCompletedValue();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                 NPT_METHOD_ID3D11Fence_GetCompletedValue),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11Fence_GetCompletedValue_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_UINT64(&(const UINT64){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Fence_GetCompletedValue_reply(struct npt_cs_decoder *dec,
                                               UINT64 *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                                  NPT_METHOD_ID3D11Fence_GetCompletedValue))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Fence_GetCompletedValue reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                       NPT_METHOD_ID3D11Fence_GetCompletedValue));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_UINT64(dec, ret);
}

static inline void
npt_submit_ID3D11Fence_GetCompletedValue(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Fence_GetCompletedValue();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Fence_GetCompletedValue_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Fence_GetCompletedValue(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Fence_GetCompletedValue(struct npt_ring *ring,
                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Fence_GetCompletedValue(ring, 0, object_id, &submit);
}

static inline UINT64
npt_call_ID3D11Fence_GetCompletedValue(struct npt_ring *ring,
                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Fence_GetCompletedValue(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT64 _ret;
        npt_decode_ID3D11Fence_GetCompletedValue_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT64){0};
    }
}

/*
 * ID3D11Fence::SetEventOnCompletion
 */

struct npt_command_ID3D11Fence_SetEventOnCompletion {
    void *_self;
    UINT64 Value;
    HANDLE hEvent;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Fence_SetEventOnCompletion(const UINT64 Value,const HANDLE hEvent)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT64(&Value, max_mode);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Fence_SetEventOnCompletion(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT64 Value,
                                            HANDLE hEvent)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Fence_SetEventOnCompletion(Value,hEvent);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                 NPT_METHOD_ID3D11Fence_SetEventOnCompletion),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT64(enc, &Value);
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
}

static inline size_t
npt_sizeof_ID3D11Fence_SetEventOnCompletion_reply(const UINT64 Value,const HANDLE hEvent)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Value (input only) */
/* skip hEvent (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Fence_SetEventOnCompletion_reply(struct npt_cs_decoder *dec,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                                  NPT_METHOD_ID3D11Fence_SetEventOnCompletion))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Fence_SetEventOnCompletion reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                       NPT_METHOD_ID3D11Fence_SetEventOnCompletion));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Fence_SetEventOnCompletion(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT64 Value,
                                            HANDLE hEvent,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Fence_SetEventOnCompletion(Value,hEvent);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Fence_SetEventOnCompletion_reply(Value,hEvent)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Fence_SetEventOnCompletion(enc, cmd_flags, object_id, Value, hEvent);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Fence_SetEventOnCompletion(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT64 Value,
                                           HANDLE hEvent)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Fence_SetEventOnCompletion(ring, 0, object_id, Value, hEvent, &submit);
}

static inline HRESULT
npt_call_ID3D11Fence_SetEventOnCompletion(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          UINT64 Value,
                                          HANDLE hEvent)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Fence_SetEventOnCompletion(ring, NPT_CMD_FLAG_REPLY, object_id, Value, hEvent, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Fence_SetEventOnCompletion_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11FENCE_H */
