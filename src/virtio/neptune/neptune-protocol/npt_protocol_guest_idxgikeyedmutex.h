/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgikeyedmutex.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIKEYEDMUTEX_H
#define NPT_PROTOCOL_GUEST_IDXGIKEYEDMUTEX_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIKeyedMutex::AcquireSync
 */

struct npt_command_IDXGIKeyedMutex_AcquireSync {
    void *_self;
    UINT64 Key;
    DWORD dwMilliseconds;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIKeyedMutex_AcquireSync(const UINT64 Key,const DWORD dwMilliseconds)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT64(&Key, max_mode);
    cmd_size += npt_sizeof_DWORD(&dwMilliseconds, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIKeyedMutex_AcquireSync(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       UINT64 Key,
                                       DWORD dwMilliseconds)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIKeyedMutex_AcquireSync(Key,dwMilliseconds);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIKeyedMutex,
                                 NPT_METHOD_IDXGIKeyedMutex_AcquireSync),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT64(enc, &Key);
    npt_encode_DWORD(enc, &dwMilliseconds);
}

static inline size_t
npt_sizeof_IDXGIKeyedMutex_AcquireSync_reply(const UINT64 Key,const DWORD dwMilliseconds)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Key (input only) */
/* skip dwMilliseconds (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIKeyedMutex_AcquireSync_reply(struct npt_cs_decoder *dec,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIKeyedMutex,
                                                  NPT_METHOD_IDXGIKeyedMutex_AcquireSync))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIKeyedMutex_AcquireSync reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIKeyedMutex,
                                       NPT_METHOD_IDXGIKeyedMutex_AcquireSync));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIKeyedMutex_AcquireSync(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       UINT64 Key,
                                       DWORD dwMilliseconds,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIKeyedMutex_AcquireSync(Key,dwMilliseconds);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIKeyedMutex_AcquireSync_reply(Key,dwMilliseconds)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIKeyedMutex_AcquireSync(enc, cmd_flags, object_id, Key, dwMilliseconds);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIKeyedMutex_AcquireSync(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      UINT64 Key,
                                      DWORD dwMilliseconds)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIKeyedMutex_AcquireSync(ring, 0, object_id, Key, dwMilliseconds, &submit);
}

static inline HRESULT
npt_call_IDXGIKeyedMutex_AcquireSync(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     UINT64 Key,
                                     DWORD dwMilliseconds)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIKeyedMutex_AcquireSync(ring, NPT_CMD_FLAG_REPLY, object_id, Key, dwMilliseconds, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIKeyedMutex_AcquireSync_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIKeyedMutex::ReleaseSync
 */

struct npt_command_IDXGIKeyedMutex_ReleaseSync {
    void *_self;
    UINT64 Key;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIKeyedMutex_ReleaseSync(const UINT64 Key)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT64(&Key, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIKeyedMutex_ReleaseSync(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       UINT64 Key)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIKeyedMutex_ReleaseSync(Key);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIKeyedMutex,
                                 NPT_METHOD_IDXGIKeyedMutex_ReleaseSync),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT64(enc, &Key);
}

static inline size_t
npt_sizeof_IDXGIKeyedMutex_ReleaseSync_reply(const UINT64 Key)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Key (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIKeyedMutex_ReleaseSync_reply(struct npt_cs_decoder *dec,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIKeyedMutex,
                                                  NPT_METHOD_IDXGIKeyedMutex_ReleaseSync))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIKeyedMutex_ReleaseSync reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIKeyedMutex,
                                       NPT_METHOD_IDXGIKeyedMutex_ReleaseSync));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIKeyedMutex_ReleaseSync(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       UINT64 Key,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIKeyedMutex_ReleaseSync(Key);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIKeyedMutex_ReleaseSync_reply(Key)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIKeyedMutex_ReleaseSync(enc, cmd_flags, object_id, Key);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIKeyedMutex_ReleaseSync(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      UINT64 Key)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIKeyedMutex_ReleaseSync(ring, 0, object_id, Key, &submit);
}

static inline HRESULT
npt_call_IDXGIKeyedMutex_ReleaseSync(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     UINT64 Key)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIKeyedMutex_ReleaseSync(ring, NPT_CMD_FLAG_REPLY, object_id, Key, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIKeyedMutex_ReleaseSync_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIKEYEDMUTEX_H */
