/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM declarations for id3d11on12device: per-interface vtable
 * structs, default thunk prototypes, default vtable populators, and default
 * constructors.
 *
 * The thunk bodies, populators, and constructors live in the matching .c
 * file.  This split keeps the headers small and prevents N copies of the
 * thunk code from landing in every TU that pulls in this family.
 */

#ifndef NPT_PROTOCOL_CLIENT_ID3D11ON12DEVICE_H
#define NPT_PROTOCOL_CLIENT_ID3D11ON12DEVICE_H

#include "npt_protocol_guest_id3d11on12device.h"

/* Forward-declare struct npt_device so the client_create prototypes don't
 * trigger the "declared inside parameter list" warning.  The full
 * definition lives in the consumer's npt_device.h, which the .c file pulls
 * in via npt_com.h. */
struct npt_device;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"



/* ================================================================== */
/* ID3D11On12Device                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D11On12DeviceVtbl, by construction.
 */
struct npt_id3d11on12device_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D11On12Device::CreateWrappedResource */
    HRESULT (NPT_STDMETHODCALLTYPE *CreateWrappedResource)(void *self, IUnknown * pResource12, const D3D11_RESOURCE_FLAGS * pFlags11, D3D12_RESOURCE_STATES InState, D3D12_RESOURCE_STATES OutState, const IID * riid, void ** ppResource11);
    /* slot 4: ID3D11On12Device::ReleaseWrappedResources */
    void (NPT_STDMETHODCALLTYPE *ReleaseWrappedResources)(void *self, ID3D11Resource ** ppResources, UINT NumResources);
    /* slot 5: ID3D11On12Device::AcquireWrappedResources */
    void (NPT_STDMETHODCALLTYPE *AcquireWrappedResources)(void *self, ID3D11Resource ** ppResources, UINT NumResources);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11on12device_default_CreateWrappedResource(void *self, IUnknown * pResource12, const D3D11_RESOURCE_FLAGS * pFlags11, D3D12_RESOURCE_STATES InState, D3D12_RESOURCE_STATES OutState, const IID * riid, void ** ppResource11);
extern void NPT_STDMETHODCALLTYPE
npt_id3d11on12device_default_ReleaseWrappedResources(void *self, ID3D11Resource ** ppResources, UINT NumResources);
extern void NPT_STDMETHODCALLTYPE
npt_id3d11on12device_default_AcquireWrappedResources(void *self, ID3D11Resource ** ppResources, UINT NumResources);

/* Default IUnknown thunks for ID3D11On12Device.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11on12device_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d11on12device_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d11on12device_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d11on12device_fill_default_vtbl(struct npt_id3d11on12device_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d11on12device_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d11on12device_client_vtbl npt_id3d11on12device_default_vtbl_storage;
extern void
npt_id3d11on12device_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d11on12device_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* ID3D11On12Device1                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D11On12Device1Vtbl, by construction.
 */
struct npt_id3d11on12device1_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D11On12Device::CreateWrappedResource */
    HRESULT (NPT_STDMETHODCALLTYPE *CreateWrappedResource)(void *self, IUnknown * pResource12, const D3D11_RESOURCE_FLAGS * pFlags11, D3D12_RESOURCE_STATES InState, D3D12_RESOURCE_STATES OutState, const IID * riid, void ** ppResource11);
    /* slot 4: ID3D11On12Device::ReleaseWrappedResources */
    void (NPT_STDMETHODCALLTYPE *ReleaseWrappedResources)(void *self, ID3D11Resource ** ppResources, UINT NumResources);
    /* slot 5: ID3D11On12Device::AcquireWrappedResources */
    void (NPT_STDMETHODCALLTYPE *AcquireWrappedResources)(void *self, ID3D11Resource ** ppResources, UINT NumResources);
    /* slot 6: ID3D11On12Device1::GetD3D12Device */
    HRESULT (NPT_STDMETHODCALLTYPE *GetD3D12Device)(void *self, const IID * riid, void ** ppvDevice);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11on12device1_default_GetD3D12Device(void *self, const IID * riid, void ** ppvDevice);

/* Default IUnknown thunks for ID3D11On12Device1.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11on12device1_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d11on12device1_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d11on12device1_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d11on12device1_fill_default_vtbl(struct npt_id3d11on12device1_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d11on12device1_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d11on12device1_client_vtbl npt_id3d11on12device1_default_vtbl_storage;
extern void
npt_id3d11on12device1_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d11on12device1_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* ID3D11On12Device2                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D11On12Device2Vtbl, by construction.
 */
struct npt_id3d11on12device2_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D11On12Device::CreateWrappedResource */
    HRESULT (NPT_STDMETHODCALLTYPE *CreateWrappedResource)(void *self, IUnknown * pResource12, const D3D11_RESOURCE_FLAGS * pFlags11, D3D12_RESOURCE_STATES InState, D3D12_RESOURCE_STATES OutState, const IID * riid, void ** ppResource11);
    /* slot 4: ID3D11On12Device::ReleaseWrappedResources */
    void (NPT_STDMETHODCALLTYPE *ReleaseWrappedResources)(void *self, ID3D11Resource ** ppResources, UINT NumResources);
    /* slot 5: ID3D11On12Device::AcquireWrappedResources */
    void (NPT_STDMETHODCALLTYPE *AcquireWrappedResources)(void *self, ID3D11Resource ** ppResources, UINT NumResources);
    /* slot 6: ID3D11On12Device1::GetD3D12Device */
    HRESULT (NPT_STDMETHODCALLTYPE *GetD3D12Device)(void *self, const IID * riid, void ** ppvDevice);
    /* slot 7: ID3D11On12Device2::UnwrapUnderlyingResource */
    HRESULT (NPT_STDMETHODCALLTYPE *UnwrapUnderlyingResource)(void *self, ID3D11Resource * pResource11, ID3D12CommandQueue * pCommandQueue, const IID * riid, void ** ppvResource12);
    /* slot 8: ID3D11On12Device2::ReturnUnderlyingResource */
    HRESULT (NPT_STDMETHODCALLTYPE *ReturnUnderlyingResource)(void *self, ID3D11Resource * pResource11, UINT NumSync, UINT64 * pSignalValues, ID3D12Fence ** ppFences);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11on12device2_default_UnwrapUnderlyingResource(void *self, ID3D11Resource * pResource11, ID3D12CommandQueue * pCommandQueue, const IID * riid, void ** ppvResource12);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11on12device2_default_ReturnUnderlyingResource(void *self, ID3D11Resource * pResource11, UINT NumSync, UINT64 * pSignalValues, ID3D12Fence ** ppFences);

/* Default IUnknown thunks for ID3D11On12Device2.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11on12device2_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d11on12device2_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d11on12device2_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d11on12device2_fill_default_vtbl(struct npt_id3d11on12device2_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d11on12device2_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d11on12device2_client_vtbl npt_id3d11on12device2_default_vtbl_storage;
extern void
npt_id3d11on12device2_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d11on12device2_client_create(struct npt_device *dev, uint64_t host_id);


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_CLIENT_ID3D11ON12DEVICE_H */
