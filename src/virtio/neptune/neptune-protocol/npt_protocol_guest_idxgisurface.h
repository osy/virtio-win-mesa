/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgisurface.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGISURFACE_H
#define NPT_PROTOCOL_GUEST_IDXGISURFACE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGISurface::GetDesc
 */

struct npt_command_IDXGISurface_GetDesc {
    void *_self;
    DXGI_SURFACE_DESC * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISurface_GetDesc(const DXGI_SURFACE_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISurface_GetDesc(struct npt_cs_encoder *enc,
                                uint32_t cmd_flags,
                                npt_object_id object_id,
                                DXGI_SURFACE_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISurface_GetDesc(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                 NPT_METHOD_IDXGISurface_GetDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGISurface_GetDesc_reply(const DXGI_SURFACE_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_SURFACE_DESC(&(const DXGI_SURFACE_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISurface_GetDesc_reply(struct npt_cs_decoder *dec,
                                      DXGI_SURFACE_DESC * pDesc,
                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                                  NPT_METHOD_IDXGISurface_GetDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISurface_GetDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                       NPT_METHOD_IDXGISurface_GetDesc));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_SURFACE_DESC(dec, (DXGI_SURFACE_DESC *)pDesc);
    }
}

static inline void
npt_submit_IDXGISurface_GetDesc(struct npt_ring *ring,
                                uint32_t cmd_flags,
                                npt_object_id object_id,
                                DXGI_SURFACE_DESC * pDesc,
                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISurface_GetDesc(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISurface_GetDesc_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISurface_GetDesc(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISurface_GetDesc(struct npt_ring *ring,
                               npt_object_id object_id,
                               DXGI_SURFACE_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface_GetDesc(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGISurface_GetDesc(struct npt_ring *ring,
                              npt_object_id object_id,
                              DXGI_SURFACE_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface_GetDesc(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISurface_GetDesc_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISurface::Map
 */

struct npt_command_IDXGISurface_Map {
    void *_self;
    DXGI_MAPPED_RECT * pLockedRect;
    UINT MapFlags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISurface_Map(const DXGI_MAPPED_RECT * pLockedRect,const UINT MapFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pLockedRect (output only) */
    cmd_size += npt_sizeof_UINT(&MapFlags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISurface_Map(struct npt_cs_encoder *enc,
                            uint32_t cmd_flags,
                            npt_object_id object_id,
                            DXGI_MAPPED_RECT * pLockedRect,
                            UINT MapFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISurface_Map(pLockedRect,MapFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                 NPT_METHOD_IDXGISurface_Map),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pLockedRect (output only) */
    npt_encode_UINT(enc, &MapFlags);
}

static inline size_t
npt_sizeof_IDXGISurface_Map_reply(const DXGI_MAPPED_RECT * pLockedRect,const UINT MapFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_MAPPED_RECT(&(const DXGI_MAPPED_RECT){0}, 1);
/* skip MapFlags (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISurface_Map_reply(struct npt_cs_decoder *dec,
                                  DXGI_MAPPED_RECT * pLockedRect,
                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                                  NPT_METHOD_IDXGISurface_Map))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISurface_Map reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                       NPT_METHOD_IDXGISurface_Map));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pLockedRect)
            npt_decode_DXGI_MAPPED_RECT(dec, (DXGI_MAPPED_RECT *)pLockedRect);
    }
}

static inline void
npt_submit_IDXGISurface_Map(struct npt_ring *ring,
                            uint32_t cmd_flags,
                            npt_object_id object_id,
                            DXGI_MAPPED_RECT * pLockedRect,
                            UINT MapFlags,
                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISurface_Map(pLockedRect,MapFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISurface_Map_reply(pLockedRect,MapFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISurface_Map(enc, cmd_flags, object_id, pLockedRect, MapFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISurface_Map(struct npt_ring *ring,
                           npt_object_id object_id,
                           DXGI_MAPPED_RECT * pLockedRect,
                           UINT MapFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface_Map(ring, 0, object_id, pLockedRect, MapFlags, &submit);
}

static inline HRESULT
npt_call_IDXGISurface_Map(struct npt_ring *ring,
                          npt_object_id object_id,
                          DXGI_MAPPED_RECT * pLockedRect,
                          UINT MapFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface_Map(ring, NPT_CMD_FLAG_REPLY, object_id, pLockedRect, MapFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISurface_Map_reply(dec, pLockedRect, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISurface::Unmap
 */

struct npt_command_IDXGISurface_Unmap {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISurface_Unmap(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGISurface_Unmap(struct npt_cs_encoder *enc,
                              uint32_t cmd_flags,
                              npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISurface_Unmap();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                 NPT_METHOD_IDXGISurface_Unmap),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGISurface_Unmap_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGISurface_Unmap_reply(struct npt_cs_decoder *dec,
                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                                  NPT_METHOD_IDXGISurface_Unmap))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISurface_Unmap reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                       NPT_METHOD_IDXGISurface_Unmap));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISurface_Unmap(struct npt_ring *ring,
                              uint32_t cmd_flags,
                              npt_object_id object_id,
                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISurface_Unmap();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISurface_Unmap_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISurface_Unmap(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISurface_Unmap(struct npt_ring *ring,
                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface_Unmap(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_IDXGISurface_Unmap(struct npt_ring *ring,
                            npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface_Unmap(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISurface_Unmap_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISurface1::GetDC
 */

struct npt_command_IDXGISurface1_GetDC {
    void *_self;
    BOOL Discard;
    HDC * phdc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISurface1_GetDC(const BOOL Discard,const HDC * phdc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_BOOL(&Discard, max_mode);
/* skip phdc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISurface1_GetDC(struct npt_cs_encoder *enc,
                               uint32_t cmd_flags,
                               npt_object_id object_id,
                               BOOL Discard,
                               HDC * phdc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISurface1_GetDC(Discard,phdc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface1,
                                 NPT_METHOD_IDXGISurface1_GetDC),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_BOOL(enc, &Discard);
/* skip phdc (output only) */
}

static inline size_t
npt_sizeof_IDXGISurface1_GetDC_reply(const BOOL Discard,const HDC * phdc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Discard (input only) */
    /* ERROR: phdc (HDC) is not serializable */
    return 0;
    return cmd_size;
}

static inline void
npt_decode_IDXGISurface1_GetDC_reply(struct npt_cs_decoder *dec,
                                     HDC * phdc,
                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface1,
                                                  NPT_METHOD_IDXGISurface1_GetDC))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISurface1_GetDC reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface1,
                                       NPT_METHOD_IDXGISurface1_GetDC));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    /* ERROR: phdc (HDC) is not serializable */
    return;
}

static inline void
npt_submit_IDXGISurface1_GetDC(struct npt_ring *ring,
                               uint32_t cmd_flags,
                               npt_object_id object_id,
                               BOOL Discard,
                               HDC * phdc,
                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISurface1_GetDC(Discard,phdc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISurface1_GetDC_reply(Discard,phdc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISurface1_GetDC(enc, cmd_flags, object_id, Discard, phdc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISurface1_GetDC(struct npt_ring *ring,
                              npt_object_id object_id,
                              BOOL Discard,
                              HDC * phdc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface1_GetDC(ring, 0, object_id, Discard, phdc, &submit);
}

static inline HRESULT
npt_call_IDXGISurface1_GetDC(struct npt_ring *ring,
                             npt_object_id object_id,
                             BOOL Discard,
                             HDC * phdc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface1_GetDC(ring, NPT_CMD_FLAG_REPLY, object_id, Discard, phdc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISurface1_GetDC_reply(dec, phdc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISurface1::ReleaseDC
 */

struct npt_command_IDXGISurface1_ReleaseDC {
    void *_self;
    RECT * pDirtyRect;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISurface1_ReleaseDC(const RECT * pDirtyRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDirtyRect);
    if (pDirtyRect)
        cmd_size += npt_sizeof_RECT(pDirtyRect, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISurface1_ReleaseDC(struct npt_cs_encoder *enc,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   RECT * pDirtyRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISurface1_ReleaseDC(pDirtyRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface1,
                                 NPT_METHOD_IDXGISurface1_ReleaseDC),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDirtyRect))
        npt_encode_RECT(enc, pDirtyRect);
}

static inline size_t
npt_sizeof_IDXGISurface1_ReleaseDC_reply(const RECT * pDirtyRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDirtyRect (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISurface1_ReleaseDC_reply(struct npt_cs_decoder *dec,
                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface1,
                                                  NPT_METHOD_IDXGISurface1_ReleaseDC))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISurface1_ReleaseDC reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface1,
                                       NPT_METHOD_IDXGISurface1_ReleaseDC));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISurface1_ReleaseDC(struct npt_ring *ring,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   RECT * pDirtyRect,
                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISurface1_ReleaseDC(pDirtyRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISurface1_ReleaseDC_reply(pDirtyRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISurface1_ReleaseDC(enc, cmd_flags, object_id, pDirtyRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISurface1_ReleaseDC(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  RECT * pDirtyRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface1_ReleaseDC(ring, 0, object_id, pDirtyRect, &submit);
}

static inline HRESULT
npt_call_IDXGISurface1_ReleaseDC(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 RECT * pDirtyRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface1_ReleaseDC(ring, NPT_CMD_FLAG_REPLY, object_id, pDirtyRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISurface1_ReleaseDC_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISurface2::GetResource
 */

struct npt_command_IDXGISurface2_GetResource {
    void *_self;
    const IID * riid;
    void ** ppParentResource;
    UINT * pSubresourceIndex;
    /* Shadow: the guest-allocated id for ppParentResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppParentResource -> *ppParentResource} in the
     * context object table. */
    uint64_t _guest_id_ppParentResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISurface2_GetResource(const IID * riid,void ** ppParentResource,const UINT * pSubresourceIndex)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppParentResource: guest id */
/* skip pSubresourceIndex (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISurface2_GetResource(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const IID * riid,
                                     void ** ppParentResource,
                                     UINT * pSubresourceIndex)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISurface2_GetResource(riid,ppParentResource,pSubresourceIndex);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface2,
                                 NPT_METHOD_IDXGISurface2_GetResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppParentResource = 0;
    if (ppParentResource) {
        _gid_ppParentResource = npt_com_allocate_next_id();
        *ppParentResource = (void *)(uintptr_t)_gid_ppParentResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppParentResource);
}
/* skip pSubresourceIndex (output only) */
}

static inline size_t
npt_sizeof_IDXGISurface2_GetResource_reply(const IID * riid,void ** ppParentResource,const UINT * pSubresourceIndex)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip riid (input only) */
/* skip ppParentResource (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISurface2_GetResource_reply(struct npt_cs_decoder *dec,
                                           void ** ppParentResource,
                                           UINT * pSubresourceIndex,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface2,
                                                  NPT_METHOD_IDXGISurface2_GetResource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISurface2_GetResource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface2,
                                       NPT_METHOD_IDXGISurface2_GetResource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppParentResource (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pSubresourceIndex)
            npt_decode_UINT(dec, (UINT *)pSubresourceIndex);
    }
}

static inline void
npt_submit_IDXGISurface2_GetResource(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const IID * riid,
                                     void ** ppParentResource,
                                     UINT * pSubresourceIndex,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISurface2_GetResource(riid,ppParentResource,pSubresourceIndex);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISurface2_GetResource_reply(riid,ppParentResource,pSubresourceIndex)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISurface2_GetResource(enc, cmd_flags, object_id, riid, ppParentResource, pSubresourceIndex);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISurface2_GetResource(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const IID * riid,
                                    void ** ppParentResource,
                                    UINT * pSubresourceIndex)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface2_GetResource(ring, 0, object_id, riid, ppParentResource, pSubresourceIndex, &submit);
}

static inline HRESULT
npt_call_IDXGISurface2_GetResource(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   const IID * riid,
                                   void ** ppParentResource,
                                   UINT * pSubresourceIndex)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISurface2_GetResource(ring, NPT_CMD_FLAG_REPLY, object_id, riid, ppParentResource, pSubresourceIndex, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISurface2_GetResource_reply(dec, ppParentResource, pSubresourceIndex, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGISURFACE_H */
