/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgiswapchainmedia.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGISWAPCHAINMEDIA_H
#define NPT_PROTOCOL_GUEST_IDXGISWAPCHAINMEDIA_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGISwapChainMedia::GetFrameStatisticsMedia
 */

struct npt_command_IDXGISwapChainMedia_GetFrameStatisticsMedia {
    void *_self;
    DXGI_FRAME_STATISTICS_MEDIA * pStats;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChainMedia_GetFrameStatisticsMedia(const DXGI_FRAME_STATISTICS_MEDIA * pStats)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pStats (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChainMedia_GetFrameStatisticsMedia(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       DXGI_FRAME_STATISTICS_MEDIA * pStats)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChainMedia_GetFrameStatisticsMedia(pStats);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                 NPT_METHOD_IDXGISwapChainMedia_GetFrameStatisticsMedia),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pStats (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChainMedia_GetFrameStatisticsMedia_reply(const DXGI_FRAME_STATISTICS_MEDIA * pStats)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_FRAME_STATISTICS_MEDIA(&(const DXGI_FRAME_STATISTICS_MEDIA){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChainMedia_GetFrameStatisticsMedia_reply(struct npt_cs_decoder *dec,
                                                             DXGI_FRAME_STATISTICS_MEDIA * pStats,
                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                                  NPT_METHOD_IDXGISwapChainMedia_GetFrameStatisticsMedia))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChainMedia_GetFrameStatisticsMedia reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                       NPT_METHOD_IDXGISwapChainMedia_GetFrameStatisticsMedia));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pStats)
            npt_decode_DXGI_FRAME_STATISTICS_MEDIA(dec, (DXGI_FRAME_STATISTICS_MEDIA *)pStats);
    }
}

static inline void
npt_submit_IDXGISwapChainMedia_GetFrameStatisticsMedia(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       DXGI_FRAME_STATISTICS_MEDIA * pStats,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChainMedia_GetFrameStatisticsMedia(pStats);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChainMedia_GetFrameStatisticsMedia_reply(pStats)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChainMedia_GetFrameStatisticsMedia(enc, cmd_flags, object_id, pStats);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChainMedia_GetFrameStatisticsMedia(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      DXGI_FRAME_STATISTICS_MEDIA * pStats)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChainMedia_GetFrameStatisticsMedia(ring, 0, object_id, pStats, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChainMedia_GetFrameStatisticsMedia(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     DXGI_FRAME_STATISTICS_MEDIA * pStats)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChainMedia_GetFrameStatisticsMedia(ring, NPT_CMD_FLAG_REPLY, object_id, pStats, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChainMedia_GetFrameStatisticsMedia_reply(dec, pStats, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChainMedia::SetPresentDuration
 */

struct npt_command_IDXGISwapChainMedia_SetPresentDuration {
    void *_self;
    UINT Duration;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChainMedia_SetPresentDuration(const UINT Duration)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Duration, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChainMedia_SetPresentDuration(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT Duration)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChainMedia_SetPresentDuration(Duration);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                 NPT_METHOD_IDXGISwapChainMedia_SetPresentDuration),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Duration);
}

static inline size_t
npt_sizeof_IDXGISwapChainMedia_SetPresentDuration_reply(const UINT Duration)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Duration (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChainMedia_SetPresentDuration_reply(struct npt_cs_decoder *dec,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                                  NPT_METHOD_IDXGISwapChainMedia_SetPresentDuration))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChainMedia_SetPresentDuration reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                       NPT_METHOD_IDXGISwapChainMedia_SetPresentDuration));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChainMedia_SetPresentDuration(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT Duration,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChainMedia_SetPresentDuration(Duration);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChainMedia_SetPresentDuration_reply(Duration)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChainMedia_SetPresentDuration(enc, cmd_flags, object_id, Duration);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChainMedia_SetPresentDuration(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT Duration)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChainMedia_SetPresentDuration(ring, 0, object_id, Duration, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChainMedia_SetPresentDuration(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT Duration)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChainMedia_SetPresentDuration(ring, NPT_CMD_FLAG_REPLY, object_id, Duration, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChainMedia_SetPresentDuration_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChainMedia::CheckPresentDurationSupport
 */

struct npt_command_IDXGISwapChainMedia_CheckPresentDurationSupport {
    void *_self;
    UINT DesiredPresentDuration;
    UINT * pClosestSmallerPresentDuration;
    UINT * pClosestLargerPresentDuration;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChainMedia_CheckPresentDurationSupport(const UINT DesiredPresentDuration,const UINT * pClosestSmallerPresentDuration,const UINT * pClosestLargerPresentDuration)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&DesiredPresentDuration, max_mode);
/* skip pClosestSmallerPresentDuration (output only) */
/* skip pClosestLargerPresentDuration (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChainMedia_CheckPresentDurationSupport(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           UINT DesiredPresentDuration,
                                                           UINT * pClosestSmallerPresentDuration,
                                                           UINT * pClosestLargerPresentDuration)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChainMedia_CheckPresentDurationSupport(DesiredPresentDuration,pClosestSmallerPresentDuration,pClosestLargerPresentDuration);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                 NPT_METHOD_IDXGISwapChainMedia_CheckPresentDurationSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &DesiredPresentDuration);
/* skip pClosestSmallerPresentDuration (output only) */
/* skip pClosestLargerPresentDuration (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChainMedia_CheckPresentDurationSupport_reply(const UINT DesiredPresentDuration,const UINT * pClosestSmallerPresentDuration,const UINT * pClosestLargerPresentDuration)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip DesiredPresentDuration (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChainMedia_CheckPresentDurationSupport_reply(struct npt_cs_decoder *dec,
                                                                 UINT * pClosestSmallerPresentDuration,
                                                                 UINT * pClosestLargerPresentDuration,
                                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                                  NPT_METHOD_IDXGISwapChainMedia_CheckPresentDurationSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChainMedia_CheckPresentDurationSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                       NPT_METHOD_IDXGISwapChainMedia_CheckPresentDurationSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pClosestSmallerPresentDuration)
            npt_decode_UINT(dec, (UINT *)pClosestSmallerPresentDuration);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pClosestLargerPresentDuration)
            npt_decode_UINT(dec, (UINT *)pClosestLargerPresentDuration);
    }
}

static inline void
npt_submit_IDXGISwapChainMedia_CheckPresentDurationSupport(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           UINT DesiredPresentDuration,
                                                           UINT * pClosestSmallerPresentDuration,
                                                           UINT * pClosestLargerPresentDuration,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChainMedia_CheckPresentDurationSupport(DesiredPresentDuration,pClosestSmallerPresentDuration,pClosestLargerPresentDuration);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChainMedia_CheckPresentDurationSupport_reply(DesiredPresentDuration,pClosestSmallerPresentDuration,pClosestLargerPresentDuration)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChainMedia_CheckPresentDurationSupport(enc, cmd_flags, object_id, DesiredPresentDuration, pClosestSmallerPresentDuration, pClosestLargerPresentDuration);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChainMedia_CheckPresentDurationSupport(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          UINT DesiredPresentDuration,
                                                          UINT * pClosestSmallerPresentDuration,
                                                          UINT * pClosestLargerPresentDuration)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChainMedia_CheckPresentDurationSupport(ring, 0, object_id, DesiredPresentDuration, pClosestSmallerPresentDuration, pClosestLargerPresentDuration, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChainMedia_CheckPresentDurationSupport(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         UINT DesiredPresentDuration,
                                                         UINT * pClosestSmallerPresentDuration,
                                                         UINT * pClosestLargerPresentDuration)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChainMedia_CheckPresentDurationSupport(ring, NPT_CMD_FLAG_REPLY, object_id, DesiredPresentDuration, pClosestSmallerPresentDuration, pClosestLargerPresentDuration, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChainMedia_CheckPresentDurationSupport_reply(dec, pClosestSmallerPresentDuration, pClosestLargerPresentDuration, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGISWAPCHAINMEDIA_H */
