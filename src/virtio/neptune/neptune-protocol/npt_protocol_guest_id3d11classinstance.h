/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11classinstance.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11CLASSINSTANCE_H
#define NPT_PROTOCOL_GUEST_ID3D11CLASSINSTANCE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11ClassInstance::GetClassLinkage
 */

struct npt_command_ID3D11ClassInstance_GetClassLinkage {
    void *_self;
    ID3D11ClassLinkage ** ppLinkage;
    /* Shadow: the guest-allocated id for ppLinkage arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppLinkage -> *ppLinkage} in the
     * context object table. */
    uint64_t _guest_id_ppLinkage;
};

static inline size_t
npt_sizeof_ID3D11ClassInstance_GetClassLinkage(ID3D11ClassLinkage ** ppLinkage)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppLinkage: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11ClassInstance_GetClassLinkage(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11ClassLinkage ** ppLinkage)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11ClassInstance_GetClassLinkage(ppLinkage);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                 NPT_METHOD_ID3D11ClassInstance_GetClassLinkage),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppLinkage = 0;
    if (ppLinkage) {
        _gid_ppLinkage = npt_com_allocate_next_id();
        *ppLinkage = (ID3D11ClassLinkage *)(uintptr_t)_gid_ppLinkage;
    }
    npt_encode_uint64_t(enc, &_gid_ppLinkage);
}
}

static inline size_t
npt_sizeof_ID3D11ClassInstance_GetClassLinkage_reply(ID3D11ClassLinkage ** ppLinkage)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppLinkage (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11ClassInstance_GetClassLinkage_reply(struct npt_cs_decoder *dec,
                                                     ID3D11ClassLinkage ** ppLinkage)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                                  NPT_METHOD_ID3D11ClassInstance_GetClassLinkage))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11ClassInstance_GetClassLinkage reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                       NPT_METHOD_ID3D11ClassInstance_GetClassLinkage));
        return;
    }
/* skip ppLinkage (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11ClassInstance_GetClassLinkage(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11ClassLinkage ** ppLinkage,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11ClassInstance_GetClassLinkage(ppLinkage);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11ClassInstance_GetClassLinkage_reply(ppLinkage)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11ClassInstance_GetClassLinkage(enc, cmd_flags, object_id, ppLinkage);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11ClassInstance_GetClassLinkage(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11ClassLinkage ** ppLinkage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassInstance_GetClassLinkage(ring, 0, object_id, ppLinkage, &submit);
}

static inline void
npt_call_ID3D11ClassInstance_GetClassLinkage(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11ClassLinkage ** ppLinkage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassInstance_GetClassLinkage(ring, NPT_CMD_FLAG_REPLY, object_id, ppLinkage, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11ClassInstance_GetClassLinkage_reply(dec, ppLinkage);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11ClassInstance::GetDesc
 */

struct npt_command_ID3D11ClassInstance_GetDesc {
    void *_self;
    D3D11_CLASS_INSTANCE_DESC * pDesc;
};

static inline size_t
npt_sizeof_ID3D11ClassInstance_GetDesc(const D3D11_CLASS_INSTANCE_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11ClassInstance_GetDesc(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       D3D11_CLASS_INSTANCE_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11ClassInstance_GetDesc(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                 NPT_METHOD_ID3D11ClassInstance_GetDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_ID3D11ClassInstance_GetDesc_reply(const D3D11_CLASS_INSTANCE_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_CLASS_INSTANCE_DESC(&(const D3D11_CLASS_INSTANCE_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11ClassInstance_GetDesc_reply(struct npt_cs_decoder *dec,
                                             D3D11_CLASS_INSTANCE_DESC * pDesc)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                                  NPT_METHOD_ID3D11ClassInstance_GetDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11ClassInstance_GetDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                       NPT_METHOD_ID3D11ClassInstance_GetDesc));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_D3D11_CLASS_INSTANCE_DESC(dec, (D3D11_CLASS_INSTANCE_DESC *)pDesc);
    }
}

static inline void
npt_submit_ID3D11ClassInstance_GetDesc(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       D3D11_CLASS_INSTANCE_DESC * pDesc,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11ClassInstance_GetDesc(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11ClassInstance_GetDesc_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11ClassInstance_GetDesc(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11ClassInstance_GetDesc(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      D3D11_CLASS_INSTANCE_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassInstance_GetDesc(ring, 0, object_id, pDesc, &submit);
}

static inline void
npt_call_ID3D11ClassInstance_GetDesc(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     D3D11_CLASS_INSTANCE_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassInstance_GetDesc(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11ClassInstance_GetDesc_reply(dec, pDesc);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11ClassInstance::GetInstanceName
 */

struct npt_command_ID3D11ClassInstance_GetInstanceName {
    void *_self;
    CHAR * pInstanceName;
    SIZE_T * pBufferLength;
};

static inline size_t
npt_sizeof_ID3D11ClassInstance_GetInstanceName(const CHAR * pInstanceName,const SIZE_T * pBufferLength)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pInstanceName (output only) */
    cmd_size += npt_sizeof_simple_pointer(pBufferLength);
    if (pBufferLength)
        cmd_size += npt_sizeof_SIZE_T(pBufferLength, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11ClassInstance_GetInstanceName(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               CHAR * pInstanceName,
                                               SIZE_T * pBufferLength)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11ClassInstance_GetInstanceName(pInstanceName,pBufferLength);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                 NPT_METHOD_ID3D11ClassInstance_GetInstanceName),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pInstanceName (output only) */
    if (npt_encode_simple_pointer(enc, pBufferLength))
        npt_encode_SIZE_T(enc, pBufferLength);
}

static inline size_t
npt_sizeof_ID3D11ClassInstance_GetInstanceName_reply(const CHAR * pInstanceName,const SIZE_T * pBufferLength)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_array_count(pInstanceName ? *pBufferLength : 0);
    if (pInstanceName)
        cmd_size += npt_sizeof_CHAR_array(pInstanceName, *pBufferLength);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_SIZE_T(&(const SIZE_T){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11ClassInstance_GetInstanceName_reply(struct npt_cs_decoder *dec,
                                                     CHAR * pInstanceName,
                                                     SIZE_T * pBufferLength)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                                  NPT_METHOD_ID3D11ClassInstance_GetInstanceName))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11ClassInstance_GetInstanceName reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                       NPT_METHOD_ID3D11ClassInstance_GetInstanceName));
        return;
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pInstanceName)
            npt_decode_CHAR_array(dec, (CHAR *)pInstanceName, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pBufferLength)
            npt_decode_SIZE_T(dec, (SIZE_T *)pBufferLength);
    }
}

static inline void
npt_submit_ID3D11ClassInstance_GetInstanceName(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               CHAR * pInstanceName,
                                               SIZE_T * pBufferLength,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11ClassInstance_GetInstanceName(pInstanceName,pBufferLength);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11ClassInstance_GetInstanceName_reply(pInstanceName,pBufferLength)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11ClassInstance_GetInstanceName(enc, cmd_flags, object_id, pInstanceName, pBufferLength);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11ClassInstance_GetInstanceName(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              CHAR * pInstanceName,
                                              SIZE_T * pBufferLength)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassInstance_GetInstanceName(ring, 0, object_id, pInstanceName, pBufferLength, &submit);
}

static inline void
npt_call_ID3D11ClassInstance_GetInstanceName(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             CHAR * pInstanceName,
                                             SIZE_T * pBufferLength)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassInstance_GetInstanceName(ring, NPT_CMD_FLAG_REPLY, object_id, pInstanceName, pBufferLength, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11ClassInstance_GetInstanceName_reply(dec, pInstanceName, pBufferLength);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11ClassInstance::GetTypeName
 */

struct npt_command_ID3D11ClassInstance_GetTypeName {
    void *_self;
    CHAR * pTypeName;
    SIZE_T * pBufferLength;
};

static inline size_t
npt_sizeof_ID3D11ClassInstance_GetTypeName(const CHAR * pTypeName,const SIZE_T * pBufferLength)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pTypeName (output only) */
    cmd_size += npt_sizeof_simple_pointer(pBufferLength);
    if (pBufferLength)
        cmd_size += npt_sizeof_SIZE_T(pBufferLength, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11ClassInstance_GetTypeName(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           CHAR * pTypeName,
                                           SIZE_T * pBufferLength)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11ClassInstance_GetTypeName(pTypeName,pBufferLength);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                 NPT_METHOD_ID3D11ClassInstance_GetTypeName),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pTypeName (output only) */
    if (npt_encode_simple_pointer(enc, pBufferLength))
        npt_encode_SIZE_T(enc, pBufferLength);
}

static inline size_t
npt_sizeof_ID3D11ClassInstance_GetTypeName_reply(const CHAR * pTypeName,const SIZE_T * pBufferLength)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_array_count(pTypeName ? *pBufferLength : 0);
    if (pTypeName)
        cmd_size += npt_sizeof_CHAR_array(pTypeName, *pBufferLength);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_SIZE_T(&(const SIZE_T){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11ClassInstance_GetTypeName_reply(struct npt_cs_decoder *dec,
                                                 CHAR * pTypeName,
                                                 SIZE_T * pBufferLength)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                                  NPT_METHOD_ID3D11ClassInstance_GetTypeName))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11ClassInstance_GetTypeName reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassInstance,
                                       NPT_METHOD_ID3D11ClassInstance_GetTypeName));
        return;
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pTypeName)
            npt_decode_CHAR_array(dec, (CHAR *)pTypeName, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pBufferLength)
            npt_decode_SIZE_T(dec, (SIZE_T *)pBufferLength);
    }
}

static inline void
npt_submit_ID3D11ClassInstance_GetTypeName(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           CHAR * pTypeName,
                                           SIZE_T * pBufferLength,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11ClassInstance_GetTypeName(pTypeName,pBufferLength);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11ClassInstance_GetTypeName_reply(pTypeName,pBufferLength)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11ClassInstance_GetTypeName(enc, cmd_flags, object_id, pTypeName, pBufferLength);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11ClassInstance_GetTypeName(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          CHAR * pTypeName,
                                          SIZE_T * pBufferLength)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassInstance_GetTypeName(ring, 0, object_id, pTypeName, pBufferLength, &submit);
}

static inline void
npt_call_ID3D11ClassInstance_GetTypeName(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         CHAR * pTypeName,
                                         SIZE_T * pBufferLength)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassInstance_GetTypeName(ring, NPT_CMD_FLAG_REPLY, object_id, pTypeName, pBufferLength, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11ClassInstance_GetTypeName_reply(dec, pTypeName, pBufferLength);
        npt_ring_free_command_reply(ring, &submit);
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11CLASSINSTANCE_H */
