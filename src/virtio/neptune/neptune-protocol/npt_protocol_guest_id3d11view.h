/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11view.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11VIEW_H
#define NPT_PROTOCOL_GUEST_ID3D11VIEW_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11View::GetResource
 */

struct npt_command_ID3D11View_GetResource {
    void *_self;
    ID3D11Resource ** ppResource;
    /* Shadow: the guest-allocated id for ppResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResource -> *ppResource} in the
     * context object table. */
    uint64_t _guest_id_ppResource;
};

static inline size_t
npt_sizeof_ID3D11View_GetResource(ID3D11Resource ** ppResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11View_GetResource(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  ID3D11Resource ** ppResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11View_GetResource(ppResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11View,
                                 NPT_METHOD_ID3D11View_GetResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppResource = 0;
    if (ppResource) {
        _gid_ppResource = npt_com_allocate_next_id();
        *ppResource = (ID3D11Resource *)(uintptr_t)_gid_ppResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppResource);
}
}

static inline size_t
npt_sizeof_ID3D11View_GetResource_reply(ID3D11Resource ** ppResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11View_GetResource_reply(struct npt_cs_decoder *dec,
                                        ID3D11Resource ** ppResource)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11View,
                                                  NPT_METHOD_ID3D11View_GetResource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11View_GetResource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11View,
                                       NPT_METHOD_ID3D11View_GetResource));
        return;
    }
/* skip ppResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11View_GetResource(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  ID3D11Resource ** ppResource,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11View_GetResource(ppResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11View_GetResource_reply(ppResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11View_GetResource(enc, cmd_flags, object_id, ppResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11View_GetResource(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 ID3D11Resource ** ppResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11View_GetResource(ring, 0, object_id, ppResource, &submit);
}

static inline void
npt_call_ID3D11View_GetResource(struct npt_ring *ring,
                                npt_object_id object_id,
                                ID3D11Resource ** ppResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11View_GetResource(ring, NPT_CMD_FLAG_REPLY, object_id, ppResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11View_GetResource_reply(dec, ppResource);
        npt_ring_free_command_reply(ring, &submit);
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11VIEW_H */
