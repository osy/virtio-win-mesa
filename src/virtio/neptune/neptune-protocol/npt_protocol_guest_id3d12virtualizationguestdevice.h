/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12virtualizationguestdevice.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12VIRTUALIZATIONGUESTDEVICE_H
#define NPT_PROTOCOL_GUEST_ID3D12VIRTUALIZATIONGUESTDEVICE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12VirtualizationGuestDevice::ShareWithHost
 */

struct npt_command_ID3D12VirtualizationGuestDevice_ShareWithHost {
    void *_self;
    ID3D12DeviceChild * pObject;
    HANDLE * pHandle;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12VirtualizationGuestDevice_ShareWithHost(const ID3D12DeviceChild * pObject,const HANDLE * pHandle)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pHandle (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12VirtualizationGuestDevice_ShareWithHost(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D12DeviceChild * pObject,
                                                         HANDLE * pHandle)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12VirtualizationGuestDevice_ShareWithHost(pObject,pHandle);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VirtualizationGuestDevice,
                                 NPT_METHOD_ID3D12VirtualizationGuestDevice_ShareWithHost),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pObject));
/* skip pHandle (output only) */
}

static inline size_t
npt_sizeof_ID3D12VirtualizationGuestDevice_ShareWithHost_reply(const ID3D12DeviceChild * pObject,const HANDLE * pHandle)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pObject (input only) */
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_decode_ID3D12VirtualizationGuestDevice_ShareWithHost_reply(struct npt_cs_decoder *dec,
                                                               HANDLE * pHandle,
                                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VirtualizationGuestDevice,
                                                  NPT_METHOD_ID3D12VirtualizationGuestDevice_ShareWithHost))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12VirtualizationGuestDevice_ShareWithHost reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VirtualizationGuestDevice,
                                       NPT_METHOD_ID3D12VirtualizationGuestDevice_ShareWithHost));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        if (pHandle)
            *pHandle = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
}

static inline void
npt_submit_ID3D12VirtualizationGuestDevice_ShareWithHost(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D12DeviceChild * pObject,
                                                         HANDLE * pHandle,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12VirtualizationGuestDevice_ShareWithHost(pObject,pHandle);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12VirtualizationGuestDevice_ShareWithHost_reply(pObject,pHandle)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12VirtualizationGuestDevice_ShareWithHost(enc, cmd_flags, object_id, pObject, pHandle);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12VirtualizationGuestDevice_ShareWithHost(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        ID3D12DeviceChild * pObject,
                                                        HANDLE * pHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12VirtualizationGuestDevice_ShareWithHost(ring, 0, object_id, pObject, pHandle, &submit);
}

static inline HRESULT
npt_call_ID3D12VirtualizationGuestDevice_ShareWithHost(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D12DeviceChild * pObject,
                                                       HANDLE * pHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12VirtualizationGuestDevice_ShareWithHost(ring, NPT_CMD_FLAG_REPLY, object_id, pObject, pHandle, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12VirtualizationGuestDevice_ShareWithHost_reply(dec, pHandle, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12VirtualizationGuestDevice::CreateFenceFd
 */

struct npt_command_ID3D12VirtualizationGuestDevice_CreateFenceFd {
    void *_self;
    ID3D12Fence * pFence;
    UINT64 FenceValue;
    int * pFenceFd;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12VirtualizationGuestDevice_CreateFenceFd(const ID3D12Fence * pFence,const UINT64 FenceValue,const int * pFenceFd)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&FenceValue, max_mode);
/* skip pFenceFd (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12VirtualizationGuestDevice_CreateFenceFd(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D12Fence * pFence,
                                                         UINT64 FenceValue,
                                                         int * pFenceFd)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12VirtualizationGuestDevice_CreateFenceFd(pFence,FenceValue,pFenceFd);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VirtualizationGuestDevice,
                                 NPT_METHOD_ID3D12VirtualizationGuestDevice_CreateFenceFd),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pFence));
    npt_encode_UINT64(enc, &FenceValue);
/* skip pFenceFd (output only) */
}

static inline size_t
npt_sizeof_ID3D12VirtualizationGuestDevice_CreateFenceFd_reply(const ID3D12Fence * pFence,const UINT64 FenceValue,const int * pFenceFd)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pFence (input only) */
/* skip FenceValue (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_int(&(const int){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12VirtualizationGuestDevice_CreateFenceFd_reply(struct npt_cs_decoder *dec,
                                                               int * pFenceFd,
                                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VirtualizationGuestDevice,
                                                  NPT_METHOD_ID3D12VirtualizationGuestDevice_CreateFenceFd))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12VirtualizationGuestDevice_CreateFenceFd reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VirtualizationGuestDevice,
                                       NPT_METHOD_ID3D12VirtualizationGuestDevice_CreateFenceFd));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pFenceFd)
            npt_decode_int(dec, (int *)pFenceFd);
    }
}

static inline void
npt_submit_ID3D12VirtualizationGuestDevice_CreateFenceFd(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D12Fence * pFence,
                                                         UINT64 FenceValue,
                                                         int * pFenceFd,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12VirtualizationGuestDevice_CreateFenceFd(pFence,FenceValue,pFenceFd);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12VirtualizationGuestDevice_CreateFenceFd_reply(pFence,FenceValue,pFenceFd)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12VirtualizationGuestDevice_CreateFenceFd(enc, cmd_flags, object_id, pFence, FenceValue, pFenceFd);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12VirtualizationGuestDevice_CreateFenceFd(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        ID3D12Fence * pFence,
                                                        UINT64 FenceValue,
                                                        int * pFenceFd)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12VirtualizationGuestDevice_CreateFenceFd(ring, 0, object_id, pFence, FenceValue, pFenceFd, &submit);
}

static inline HRESULT
npt_call_ID3D12VirtualizationGuestDevice_CreateFenceFd(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D12Fence * pFence,
                                                       UINT64 FenceValue,
                                                       int * pFenceFd)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12VirtualizationGuestDevice_CreateFenceFd(ring, NPT_CMD_FLAG_REPLY, object_id, pFence, FenceValue, pFenceFd, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12VirtualizationGuestDevice_CreateFenceFd_reply(dec, pFenceFd, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12VIRTUALIZATIONGUESTDEVICE_H */
