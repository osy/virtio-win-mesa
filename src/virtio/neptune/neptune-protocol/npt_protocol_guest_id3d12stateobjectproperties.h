/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12stateobjectproperties.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12STATEOBJECTPROPERTIES_H
#define NPT_PROTOCOL_GUEST_ID3D12STATEOBJECTPROPERTIES_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12StateObjectProperties::GetShaderIdentifier
 */

struct npt_command_ID3D12StateObjectProperties_GetShaderIdentifier {
    void *_self;
    const WCHAR * pExportName;
};

static inline size_t
npt_sizeof_ID3D12StateObjectProperties_GetShaderIdentifier(const WCHAR * pExportName)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pExportName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pExportName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(pExportName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D12StateObjectProperties_GetShaderIdentifier(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           const WCHAR * pExportName)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12StateObjectProperties_GetShaderIdentifier(pExportName);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties,
                                 NPT_METHOD_ID3D12StateObjectProperties_GetShaderIdentifier),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pExportName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pExportName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, pExportName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12StateObjectProperties_GetShaderIdentifier_reply(const WCHAR * pExportName)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pExportName (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12StateObjectProperties_GetShaderIdentifier(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           const WCHAR * pExportName,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12StateObjectProperties_GetShaderIdentifier(pExportName);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12StateObjectProperties_GetShaderIdentifier_reply(pExportName)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12StateObjectProperties_GetShaderIdentifier(enc, cmd_flags, object_id, pExportName);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12StateObjectProperties_GetShaderIdentifier(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          const WCHAR * pExportName)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties_GetShaderIdentifier(ring, 0, object_id, pExportName, &submit);
}

static inline void
npt_call_ID3D12StateObjectProperties_GetShaderIdentifier(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         const WCHAR * pExportName)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties_GetShaderIdentifier(ring, NPT_CMD_FLAG_REPLY, object_id, pExportName, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12StateObjectProperties::GetShaderStackSize
 */

struct npt_command_ID3D12StateObjectProperties_GetShaderStackSize {
    void *_self;
    const WCHAR * pExportName;
    UINT64 ret;
};

static inline size_t
npt_sizeof_ID3D12StateObjectProperties_GetShaderStackSize(const WCHAR * pExportName)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pExportName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pExportName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(pExportName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D12StateObjectProperties_GetShaderStackSize(struct npt_cs_encoder *enc,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          const WCHAR * pExportName)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12StateObjectProperties_GetShaderStackSize(pExportName);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties,
                                 NPT_METHOD_ID3D12StateObjectProperties_GetShaderStackSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pExportName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pExportName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, pExportName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12StateObjectProperties_GetShaderStackSize_reply(const WCHAR * pExportName)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_UINT64(&(const UINT64){0}, 1);
/* skip pExportName (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12StateObjectProperties_GetShaderStackSize_reply(struct npt_cs_decoder *dec,
                                                                UINT64 *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties,
                                                  NPT_METHOD_ID3D12StateObjectProperties_GetShaderStackSize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12StateObjectProperties_GetShaderStackSize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties,
                                       NPT_METHOD_ID3D12StateObjectProperties_GetShaderStackSize));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_UINT64(dec, ret);
}

static inline void
npt_submit_ID3D12StateObjectProperties_GetShaderStackSize(struct npt_ring *ring,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          const WCHAR * pExportName,
                                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12StateObjectProperties_GetShaderStackSize(pExportName);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12StateObjectProperties_GetShaderStackSize_reply(pExportName)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12StateObjectProperties_GetShaderStackSize(enc, cmd_flags, object_id, pExportName);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12StateObjectProperties_GetShaderStackSize(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         const WCHAR * pExportName)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties_GetShaderStackSize(ring, 0, object_id, pExportName, &submit);
}

static inline UINT64
npt_call_ID3D12StateObjectProperties_GetShaderStackSize(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        const WCHAR * pExportName)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties_GetShaderStackSize(ring, NPT_CMD_FLAG_REPLY, object_id, pExportName, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT64 _ret;
        npt_decode_ID3D12StateObjectProperties_GetShaderStackSize_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT64){0};
    }
}

/*
 * ID3D12StateObjectProperties::GetPipelineStackSize
 */

struct npt_command_ID3D12StateObjectProperties_GetPipelineStackSize {
    void *_self;
    UINT64 ret;
};

static inline size_t
npt_sizeof_ID3D12StateObjectProperties_GetPipelineStackSize(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12StateObjectProperties_GetPipelineStackSize(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12StateObjectProperties_GetPipelineStackSize();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties,
                                 NPT_METHOD_ID3D12StateObjectProperties_GetPipelineStackSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12StateObjectProperties_GetPipelineStackSize_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_UINT64(&(const UINT64){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12StateObjectProperties_GetPipelineStackSize_reply(struct npt_cs_decoder *dec,
                                                                  UINT64 *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties,
                                                  NPT_METHOD_ID3D12StateObjectProperties_GetPipelineStackSize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12StateObjectProperties_GetPipelineStackSize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties,
                                       NPT_METHOD_ID3D12StateObjectProperties_GetPipelineStackSize));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_UINT64(dec, ret);
}

static inline void
npt_submit_ID3D12StateObjectProperties_GetPipelineStackSize(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12StateObjectProperties_GetPipelineStackSize();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12StateObjectProperties_GetPipelineStackSize_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12StateObjectProperties_GetPipelineStackSize(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12StateObjectProperties_GetPipelineStackSize(struct npt_ring *ring,
                                                           npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties_GetPipelineStackSize(ring, 0, object_id, &submit);
}

static inline UINT64
npt_call_ID3D12StateObjectProperties_GetPipelineStackSize(struct npt_ring *ring,
                                                          npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties_GetPipelineStackSize(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT64 _ret;
        npt_decode_ID3D12StateObjectProperties_GetPipelineStackSize_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT64){0};
    }
}

/*
 * ID3D12StateObjectProperties::SetPipelineStackSize
 */

struct npt_command_ID3D12StateObjectProperties_SetPipelineStackSize {
    void *_self;
    UINT64 PipelineStackSizeInBytes;
};

static inline size_t
npt_sizeof_ID3D12StateObjectProperties_SetPipelineStackSize(const UINT64 PipelineStackSizeInBytes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT64(&PipelineStackSizeInBytes, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12StateObjectProperties_SetPipelineStackSize(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            UINT64 PipelineStackSizeInBytes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12StateObjectProperties_SetPipelineStackSize(PipelineStackSizeInBytes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties,
                                 NPT_METHOD_ID3D12StateObjectProperties_SetPipelineStackSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT64(enc, &PipelineStackSizeInBytes);
}

static inline size_t
npt_sizeof_ID3D12StateObjectProperties_SetPipelineStackSize_reply(const UINT64 PipelineStackSizeInBytes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip PipelineStackSizeInBytes (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12StateObjectProperties_SetPipelineStackSize(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            UINT64 PipelineStackSizeInBytes,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12StateObjectProperties_SetPipelineStackSize(PipelineStackSizeInBytes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12StateObjectProperties_SetPipelineStackSize_reply(PipelineStackSizeInBytes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12StateObjectProperties_SetPipelineStackSize(enc, cmd_flags, object_id, PipelineStackSizeInBytes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12StateObjectProperties_SetPipelineStackSize(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           UINT64 PipelineStackSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties_SetPipelineStackSize(ring, 0, object_id, PipelineStackSizeInBytes, &submit);
}

static inline void
npt_call_ID3D12StateObjectProperties_SetPipelineStackSize(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          UINT64 PipelineStackSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties_SetPipelineStackSize(ring, NPT_CMD_FLAG_REPLY, object_id, PipelineStackSizeInBytes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12StateObjectProperties1::GetProgramIdentifier
 */

struct npt_command_ID3D12StateObjectProperties1_GetProgramIdentifier {
    void *_self;
    const WCHAR * pProgramName;
    D3D12_PROGRAM_IDENTIFIER ret;
};

static inline size_t
npt_sizeof_ID3D12StateObjectProperties1_GetProgramIdentifier(const WCHAR * pProgramName)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pProgramName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pProgramName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(pProgramName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D12StateObjectProperties1_GetProgramIdentifier(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             const WCHAR * pProgramName)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12StateObjectProperties1_GetProgramIdentifier(pProgramName);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties1,
                                 NPT_METHOD_ID3D12StateObjectProperties1_GetProgramIdentifier),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pProgramName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pProgramName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, pProgramName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12StateObjectProperties1_GetProgramIdentifier_reply(const WCHAR * pProgramName)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_PROGRAM_IDENTIFIER(&(const D3D12_PROGRAM_IDENTIFIER){0}, 1);
/* skip pProgramName (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12StateObjectProperties1_GetProgramIdentifier_reply(struct npt_cs_decoder *dec,
                                                                   D3D12_PROGRAM_IDENTIFIER *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties1,
                                                  NPT_METHOD_ID3D12StateObjectProperties1_GetProgramIdentifier))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12StateObjectProperties1_GetProgramIdentifier reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12StateObjectProperties1,
                                       NPT_METHOD_ID3D12StateObjectProperties1_GetProgramIdentifier));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_PROGRAM_IDENTIFIER(dec, ret);
}

static inline void
npt_submit_ID3D12StateObjectProperties1_GetProgramIdentifier(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             const WCHAR * pProgramName,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12StateObjectProperties1_GetProgramIdentifier(pProgramName);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12StateObjectProperties1_GetProgramIdentifier_reply(pProgramName)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12StateObjectProperties1_GetProgramIdentifier(enc, cmd_flags, object_id, pProgramName);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12StateObjectProperties1_GetProgramIdentifier(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            const WCHAR * pProgramName)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties1_GetProgramIdentifier(ring, 0, object_id, pProgramName, &submit);
}

static inline D3D12_PROGRAM_IDENTIFIER
npt_call_ID3D12StateObjectProperties1_GetProgramIdentifier(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           const WCHAR * pProgramName)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12StateObjectProperties1_GetProgramIdentifier(ring, NPT_CMD_FLAG_REPLY, object_id, pProgramName, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_PROGRAM_IDENTIFIER _ret;
        npt_decode_ID3D12StateObjectProperties1_GetProgramIdentifier_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_PROGRAM_IDENTIFIER){0};
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12STATEOBJECTPROPERTIES_H */
