/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for id3d12commandqueue: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_id3d12commandqueue.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* ID3D12CommandQueue                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12commandqueue_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12DeviceChild,
    &NPT_IID_ID3D12Pageable,
    &NPT_IID_ID3D12CommandQueue,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12commandqueue_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Pre-allocate the guest id and speculatively build the wrapper
     * BEFORE the host round-trip.  On E_NOINTERFACE the wrapper is
     * never used (the host rejected the QI, so its id is unregistered)
     * and we must Release the speculative wrapper to keep the refcount
     * straight.  The host round-trip carries just HRESULT. */
    uint64_t _guest_id = npt_com_allocate_next_id();
    /* Pass `self` as parent so the new wrapper inherits its instance_ring
     * (DC/SC shared ring pinning).  QI typically returns the same host
     * pointer as `self`, so the cache hit short-circuits before any new
     * wrapper is created -- but on the cache-miss path the new wrapper
     * must inherit the same ring or generated methods on the QI'd
     * interface would scatter onto a different host dispatch thread
     * than every other method on the same underlying object. */
    void *wrapper = npt_com_get_or_wrap(
        npt_com_self_device(self), riid, _guest_id,
        (struct npt_com_base *)self);
    if (!wrapper) {
        if (ppvObject) *ppvObject = NULL;
        return NPT_E_OUTOFMEMORY;
    }
    HRESULT host_hr = npt_com_send_query_interface(
        npt_com_self_device(self),
        npt_com_self_id(self),
        riid, _guest_id);
    if (host_hr == NPT_S_OK) {
        if (ppvObject)
            *ppvObject = wrapper;
        return NPT_S_OK;
    }
    /* QI failed host-side: the guest_id stays unregistered.  Release the
     * speculative wrapper so the guest-side refcount drains cleanly.
     * (npt_com_default_release still issues COM_RELEASE to the host,
     * which is a no-op there because the id was never registered.) */
    struct npt_com_base *com = wrapper;
    typedef ULONG (NPT_STDMETHODCALLTYPE *release_fn_t)(void *);
    ((release_fn_t)com->lpVtbl[2])(com);
    if (ppvObject) *ppvObject = NULL;
    return host_hr;
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_Release(void *self)
{
    return npt_com_default_release(self);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_UpdateTileMappings(void *self, ID3D12Resource * pResource, UINT NumResourceRegions, const D3D12_TILED_RESOURCE_COORDINATE * pResourceRegionStartCoordinates, const D3D12_TILE_REGION_SIZE * pResourceRegionSizes, ID3D12Heap * pHeap, UINT NumRanges, const D3D12_TILE_RANGE_FLAGS * pRangeFlags, const UINT * pHeapRangeStartOffsets, const UINT * pRangeTileCounts, D3D12_TILE_MAPPING_FLAGS Flags)
{
    npt_async_ID3D12CommandQueue_UpdateTileMappings(npt_com_self_ring(self),npt_com_self_id(self),pResource,NumResourceRegions,pResourceRegionStartCoordinates,pResourceRegionSizes,pHeap,NumRanges,pRangeFlags,pHeapRangeStartOffsets,pRangeTileCounts,Flags);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_CopyTileMappings(void *self, ID3D12Resource * pDstResource, const D3D12_TILED_RESOURCE_COORDINATE * pDstRegionStartCoordinate, ID3D12Resource * pSrcResource, const D3D12_TILED_RESOURCE_COORDINATE * pSrcRegionStartCoordinate, const D3D12_TILE_REGION_SIZE * pRegionSize, D3D12_TILE_MAPPING_FLAGS Flags)
{
    npt_async_ID3D12CommandQueue_CopyTileMappings(npt_com_self_ring(self),npt_com_self_id(self),pDstResource,pDstRegionStartCoordinate,pSrcResource,pSrcRegionStartCoordinate,pRegionSize,Flags);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_ExecuteCommandLists(void *self, UINT NumCommandLists, ID3D12CommandList ** ppCommandLists)
{
    npt_async_ID3D12CommandQueue_ExecuteCommandLists(npt_com_self_ring(self),npt_com_self_id(self),NumCommandLists,ppCommandLists);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_SetMarker(void *self, UINT Metadata, const void * pData, UINT Size)
{
    npt_async_ID3D12CommandQueue_SetMarker(npt_com_self_ring(self),npt_com_self_id(self),Metadata,pData,Size);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_BeginEvent(void *self, UINT Metadata, const void * pData, UINT Size)
{
    npt_async_ID3D12CommandQueue_BeginEvent(npt_com_self_ring(self),npt_com_self_id(self),Metadata,pData,Size);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_EndEvent(void *self)
{
    npt_async_ID3D12CommandQueue_EndEvent(npt_com_self_ring(self),npt_com_self_id(self));
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_Signal(void *self, ID3D12Fence * pFence, UINT64 Value)
{
    npt_async_ID3D12CommandQueue_Signal(npt_com_self_ring(self),npt_com_self_id(self),pFence,Value);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_Wait(void *self, ID3D12Fence * pFence, UINT64 Value)
{
    npt_async_ID3D12CommandQueue_Wait(npt_com_self_ring(self),npt_com_self_id(self),pFence,Value);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_GetTimestampFrequency(void *self, UINT64 * pFrequency)
{
    HRESULT _ret = npt_call_ID3D12CommandQueue_GetTimestampFrequency(npt_com_self_ring(self),npt_com_self_id(self),pFrequency);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_GetClockCalibration(void *self, UINT64 * pGpuTimestamp, UINT64 * pCpuTimestamp)
{
    HRESULT _ret = npt_call_ID3D12CommandQueue_GetClockCalibration(npt_com_self_ring(self),npt_com_self_id(self),pGpuTimestamp,pCpuTimestamp);
    return _ret;
}

D3D12_COMMAND_QUEUE_DESC NPT_STDMETHODCALLTYPE
npt_id3d12commandqueue_default_GetDesc(void *self)
{
    D3D12_COMMAND_QUEUE_DESC _ret = npt_call_ID3D12CommandQueue_GetDesc(npt_com_self_ring(self),npt_com_self_id(self));
    return _ret;
}


void
npt_id3d12commandqueue_fill_default_vtbl(struct npt_id3d12commandqueue_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12commandqueue_default_QueryInterface;
    dst->AddRef         = npt_id3d12commandqueue_default_AddRef;
    dst->Release        = npt_id3d12commandqueue_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetDevice = npt_id3d12devicechild_default_GetDevice;
    dst->UpdateTileMappings = npt_id3d12commandqueue_default_UpdateTileMappings;
    dst->CopyTileMappings = npt_id3d12commandqueue_default_CopyTileMappings;
    dst->ExecuteCommandLists = npt_id3d12commandqueue_default_ExecuteCommandLists;
    dst->SetMarker = npt_id3d12commandqueue_default_SetMarker;
    dst->BeginEvent = npt_id3d12commandqueue_default_BeginEvent;
    dst->EndEvent = npt_id3d12commandqueue_default_EndEvent;
    dst->Signal = npt_id3d12commandqueue_default_Signal;
    dst->Wait = npt_id3d12commandqueue_default_Wait;
    dst->GetTimestampFrequency = npt_id3d12commandqueue_default_GetTimestampFrequency;
    dst->GetClockCalibration = npt_id3d12commandqueue_default_GetClockCalibration;
    dst->GetDesc = npt_id3d12commandqueue_default_GetDesc;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12commandqueue_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12commandqueue_client_vtbl npt_id3d12commandqueue_default_vtbl_storage;

void
npt_id3d12commandqueue_install_default_vtbl(void)
{
    npt_id3d12commandqueue_fill_default_vtbl(&npt_id3d12commandqueue_default_vtbl_storage);
}

void *
npt_id3d12commandqueue_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12commandqueue_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
