/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgiobject.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIOBJECT_H
#define NPT_PROTOCOL_GUEST_IDXGIOBJECT_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIObject::SetPrivateData
 */

struct npt_command_IDXGIObject_SetPrivateData {
    void *_self;
    const GUID * Name;
    UINT DataSize;
    const void * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIObject_SetPrivateData(const GUID * Name,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(Name);
    if (Name)
        cmd_size += npt_sizeof_GUID(Name, max_mode);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_IDXGIObject_SetPrivateData(struct npt_cs_encoder *enc,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const GUID * Name,
                                      UINT DataSize,
                                      const void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIObject_SetPrivateData(Name,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                 NPT_METHOD_IDXGIObject_SetPrivateData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, Name))
        npt_encode_GUID(enc, Name);
    npt_encode_UINT(enc, &DataSize);
    if (pData) {
        npt_encode_array_count(enc, DataSize);
        npt_encode_blob_array(enc, pData, DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_IDXGIObject_SetPrivateData_reply(const GUID * Name,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Name (input only) */
/* skip DataSize (input only) */
/* skip pData (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIObject_SetPrivateData_reply(struct npt_cs_decoder *dec,
                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                                  NPT_METHOD_IDXGIObject_SetPrivateData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIObject_SetPrivateData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                       NPT_METHOD_IDXGIObject_SetPrivateData));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIObject_SetPrivateData(struct npt_ring *ring,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const GUID * Name,
                                      UINT DataSize,
                                      const void * pData,
                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIObject_SetPrivateData(Name,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIObject_SetPrivateData_reply(Name,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIObject_SetPrivateData(enc, cmd_flags, object_id, Name, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIObject_SetPrivateData(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     const GUID * Name,
                                     UINT DataSize,
                                     const void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIObject_SetPrivateData(ring, 0, object_id, Name, DataSize, pData, &submit);
}

static inline HRESULT
npt_call_IDXGIObject_SetPrivateData(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const GUID * Name,
                                    UINT DataSize,
                                    const void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIObject_SetPrivateData(ring, NPT_CMD_FLAG_REPLY, object_id, Name, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIObject_SetPrivateData_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIObject::SetPrivateDataInterface
 */

struct npt_command_IDXGIObject_SetPrivateDataInterface {
    void *_self;
    const GUID * Name;
    const IUnknown * pUnknown;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIObject_SetPrivateDataInterface(const GUID * Name,const IUnknown * pUnknown)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(Name);
    if (Name)
        cmd_size += npt_sizeof_GUID(Name, max_mode);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_IDXGIObject_SetPrivateDataInterface(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const GUID * Name,
                                               const IUnknown * pUnknown)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIObject_SetPrivateDataInterface(Name,pUnknown);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                 NPT_METHOD_IDXGIObject_SetPrivateDataInterface),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, Name))
        npt_encode_GUID(enc, Name);
    npt_encode_com_handle(enc, npt_object_get_id(pUnknown));
}

static inline size_t
npt_sizeof_IDXGIObject_SetPrivateDataInterface_reply(const GUID * Name,const IUnknown * pUnknown)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Name (input only) */
/* skip pUnknown (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIObject_SetPrivateDataInterface_reply(struct npt_cs_decoder *dec,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                                  NPT_METHOD_IDXGIObject_SetPrivateDataInterface))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIObject_SetPrivateDataInterface reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                       NPT_METHOD_IDXGIObject_SetPrivateDataInterface));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIObject_SetPrivateDataInterface(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const GUID * Name,
                                               const IUnknown * pUnknown,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIObject_SetPrivateDataInterface(Name,pUnknown);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIObject_SetPrivateDataInterface_reply(Name,pUnknown)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIObject_SetPrivateDataInterface(enc, cmd_flags, object_id, Name, pUnknown);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIObject_SetPrivateDataInterface(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const GUID * Name,
                                              const IUnknown * pUnknown)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIObject_SetPrivateDataInterface(ring, 0, object_id, Name, pUnknown, &submit);
}

static inline HRESULT
npt_call_IDXGIObject_SetPrivateDataInterface(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const GUID * Name,
                                             const IUnknown * pUnknown)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIObject_SetPrivateDataInterface(ring, NPT_CMD_FLAG_REPLY, object_id, Name, pUnknown, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIObject_SetPrivateDataInterface_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIObject::GetPrivateData
 */

struct npt_command_IDXGIObject_GetPrivateData {
    void *_self;
    const GUID * Name;
    UINT * pDataSize;
    void * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIObject_GetPrivateData(const GUID * Name,const UINT * pDataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(Name);
    if (Name)
        cmd_size += npt_sizeof_GUID(Name, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDataSize);
    if (pDataSize)
        cmd_size += npt_sizeof_UINT(pDataSize, max_mode);
/* skip pData (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIObject_GetPrivateData(struct npt_cs_encoder *enc,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const GUID * Name,
                                      UINT * pDataSize,
                                      void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIObject_GetPrivateData(Name,pDataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                 NPT_METHOD_IDXGIObject_GetPrivateData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, Name))
        npt_encode_GUID(enc, Name);
    if (npt_encode_simple_pointer(enc, pDataSize))
        npt_encode_UINT(enc, pDataSize);
/* skip pData (output only) */
}

static inline size_t
npt_sizeof_IDXGIObject_GetPrivateData_reply(const GUID * Name,const UINT * pDataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Name (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    if (pData) {
        cmd_size += npt_sizeof_array_count(*pDataSize);
        cmd_size += npt_sizeof_blob_array(pData, *pDataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_IDXGIObject_GetPrivateData_reply(struct npt_cs_decoder *dec,
                                            UINT * pDataSize,
                                            void * pData,
                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                                  NPT_METHOD_IDXGIObject_GetPrivateData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIObject_GetPrivateData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                       NPT_METHOD_IDXGIObject_GetPrivateData));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDataSize)
            npt_decode_UINT(dec, (UINT *)pDataSize);
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pData)
            npt_decode_blob_array(dec, (void *)pData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_IDXGIObject_GetPrivateData(struct npt_ring *ring,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const GUID * Name,
                                      UINT * pDataSize,
                                      void * pData,
                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIObject_GetPrivateData(Name,pDataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIObject_GetPrivateData_reply(Name,pDataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIObject_GetPrivateData(enc, cmd_flags, object_id, Name, pDataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIObject_GetPrivateData(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     const GUID * Name,
                                     UINT * pDataSize,
                                     void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIObject_GetPrivateData(ring, 0, object_id, Name, pDataSize, pData, &submit);
}

static inline HRESULT
npt_call_IDXGIObject_GetPrivateData(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const GUID * Name,
                                    UINT * pDataSize,
                                    void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIObject_GetPrivateData(ring, NPT_CMD_FLAG_REPLY, object_id, Name, pDataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIObject_GetPrivateData_reply(dec, pDataSize, pData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIObject::GetParent
 */

struct npt_command_IDXGIObject_GetParent {
    void *_self;
    const IID * riid;
    void ** ppParent;
    /* Shadow: the guest-allocated id for ppParent arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppParent -> *ppParent} in the
     * context object table. */
    uint64_t _guest_id_ppParent;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIObject_GetParent(const IID * riid,void ** ppParent)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppParent: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIObject_GetParent(struct npt_cs_encoder *enc,
                                 uint32_t cmd_flags,
                                 npt_object_id object_id,
                                 const IID * riid,
                                 void ** ppParent)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIObject_GetParent(riid,ppParent);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                 NPT_METHOD_IDXGIObject_GetParent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppParent = 0;
    if (ppParent) {
        _gid_ppParent = npt_com_allocate_next_id();
        *ppParent = (void *)(uintptr_t)_gid_ppParent;
    }
    npt_encode_uint64_t(enc, &_gid_ppParent);
}
}

static inline size_t
npt_sizeof_IDXGIObject_GetParent_reply(const IID * riid,void ** ppParent)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip riid (input only) */
/* skip ppParent (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIObject_GetParent_reply(struct npt_cs_decoder *dec,
                                       void ** ppParent,
                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                                  NPT_METHOD_IDXGIObject_GetParent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIObject_GetParent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                       NPT_METHOD_IDXGIObject_GetParent));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppParent (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIObject_GetParent(struct npt_ring *ring,
                                 uint32_t cmd_flags,
                                 npt_object_id object_id,
                                 const IID * riid,
                                 void ** ppParent,
                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIObject_GetParent(riid,ppParent);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIObject_GetParent_reply(riid,ppParent)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIObject_GetParent(enc, cmd_flags, object_id, riid, ppParent);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIObject_GetParent(struct npt_ring *ring,
                                npt_object_id object_id,
                                const IID * riid,
                                void ** ppParent)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIObject_GetParent(ring, 0, object_id, riid, ppParent, &submit);
}

static inline HRESULT
npt_call_IDXGIObject_GetParent(struct npt_ring *ring,
                               npt_object_id object_id,
                               const IID * riid,
                               void ** ppParent)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIObject_GetParent(ring, NPT_CMD_FLAG_REPLY, object_id, riid, ppParent, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIObject_GetParent_reply(dec, ppParent, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIOBJECT_H */
