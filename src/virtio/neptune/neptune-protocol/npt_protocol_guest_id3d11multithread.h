/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11multithread.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11MULTITHREAD_H
#define NPT_PROTOCOL_GUEST_ID3D11MULTITHREAD_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11Multithread::Enter
 */

struct npt_command_ID3D11Multithread_Enter {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D11Multithread_Enter(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Multithread_Enter(struct npt_cs_encoder *enc,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Multithread_Enter();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Multithread,
                                 NPT_METHOD_ID3D11Multithread_Enter),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11Multithread_Enter_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D11Multithread_Enter(struct npt_ring *ring,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Multithread_Enter();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Multithread_Enter_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Multithread_Enter(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Multithread_Enter(struct npt_ring *ring,
                                  npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Multithread_Enter(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D11Multithread_Enter(struct npt_ring *ring,
                                 npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Multithread_Enter(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Multithread::Leave
 */

struct npt_command_ID3D11Multithread_Leave {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D11Multithread_Leave(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Multithread_Leave(struct npt_cs_encoder *enc,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Multithread_Leave();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Multithread,
                                 NPT_METHOD_ID3D11Multithread_Leave),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11Multithread_Leave_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D11Multithread_Leave(struct npt_ring *ring,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Multithread_Leave();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Multithread_Leave_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Multithread_Leave(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Multithread_Leave(struct npt_ring *ring,
                                  npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Multithread_Leave(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D11Multithread_Leave(struct npt_ring *ring,
                                 npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Multithread_Leave(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Multithread::SetMultithreadProtected
 */

struct npt_command_ID3D11Multithread_SetMultithreadProtected {
    void *_self;
    BOOL bMTProtect;
    BOOL ret;
};

static inline size_t
npt_sizeof_ID3D11Multithread_SetMultithreadProtected(const BOOL bMTProtect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_BOOL(&bMTProtect, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Multithread_SetMultithreadProtected(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     BOOL bMTProtect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Multithread_SetMultithreadProtected(bMTProtect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Multithread,
                                 NPT_METHOD_ID3D11Multithread_SetMultithreadProtected),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_BOOL(enc, &bMTProtect);
}

static inline size_t
npt_sizeof_ID3D11Multithread_SetMultithreadProtected_reply(const BOOL bMTProtect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip bMTProtect (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Multithread_SetMultithreadProtected_reply(struct npt_cs_decoder *dec,
                                                           BOOL *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Multithread,
                                                  NPT_METHOD_ID3D11Multithread_SetMultithreadProtected))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Multithread_SetMultithreadProtected reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Multithread,
                                       NPT_METHOD_ID3D11Multithread_SetMultithreadProtected));
        if (ret) *ret = (BOOL)0;
        return;
    }
    if (ret) *ret = (BOOL)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Multithread_SetMultithreadProtected(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     BOOL bMTProtect,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Multithread_SetMultithreadProtected(bMTProtect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Multithread_SetMultithreadProtected_reply(bMTProtect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Multithread_SetMultithreadProtected(enc, cmd_flags, object_id, bMTProtect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Multithread_SetMultithreadProtected(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    BOOL bMTProtect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Multithread_SetMultithreadProtected(ring, 0, object_id, bMTProtect, &submit);
}

static inline BOOL
npt_call_ID3D11Multithread_SetMultithreadProtected(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   BOOL bMTProtect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Multithread_SetMultithreadProtected(ring, NPT_CMD_FLAG_REPLY, object_id, bMTProtect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        BOOL _ret;
        npt_decode_ID3D11Multithread_SetMultithreadProtected_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (BOOL)0;
    }
}

/*
 * ID3D11Multithread::GetMultithreadProtected
 */

struct npt_command_ID3D11Multithread_GetMultithreadProtected {
    void *_self;
    BOOL ret;
};

static inline size_t
npt_sizeof_ID3D11Multithread_GetMultithreadProtected(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Multithread_GetMultithreadProtected(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Multithread_GetMultithreadProtected();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Multithread,
                                 NPT_METHOD_ID3D11Multithread_GetMultithreadProtected),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11Multithread_GetMultithreadProtected_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Multithread_GetMultithreadProtected_reply(struct npt_cs_decoder *dec,
                                                           BOOL *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Multithread,
                                                  NPT_METHOD_ID3D11Multithread_GetMultithreadProtected))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Multithread_GetMultithreadProtected reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Multithread,
                                       NPT_METHOD_ID3D11Multithread_GetMultithreadProtected));
        if (ret) *ret = (BOOL)0;
        return;
    }
    if (ret) *ret = (BOOL)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Multithread_GetMultithreadProtected(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Multithread_GetMultithreadProtected();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Multithread_GetMultithreadProtected_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Multithread_GetMultithreadProtected(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Multithread_GetMultithreadProtected(struct npt_ring *ring,
                                                    npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Multithread_GetMultithreadProtected(ring, 0, object_id, &submit);
}

static inline BOOL
npt_call_ID3D11Multithread_GetMultithreadProtected(struct npt_ring *ring,
                                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Multithread_GetMultithreadProtected(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        BOOL _ret;
        npt_decode_ID3D11Multithread_GetMultithreadProtected_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (BOOL)0;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11MULTITHREAD_H */
