/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for idxgifactorymedia: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_idxgifactorymedia.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* IDXGIFactoryMedia                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_idxgifactorymedia_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_IDXGIFactoryMedia,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgifactorymedia_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_idxgifactorymedia_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_idxgifactorymedia_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_idxgifactorymedia_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgifactorymedia_default_CreateSwapChainForCompositionSurfaceHandle(void *self, IUnknown * pDevice, HANDLE hSurface, const DXGI_SWAP_CHAIN_DESC1 * pDesc, IDXGIOutput * pRestrictToOutput, IDXGISwapChain1 ** ppSwapChain)
{
    IDXGISwapChain1 *_raw_ppSwapChain = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    IDXGISwapChain1 **_orig_ppSwapChain = ppSwapChain;
    if (ppSwapChain)
        ppSwapChain = (IDXGISwapChain1 **)&_raw_ppSwapChain;
    npt_async_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(npt_com_self_ring(self),npt_com_self_id(self),pDevice,hSurface,pDesc,pRestrictToOutput,ppSwapChain);
    if (_orig_ppSwapChain) {
        if (_raw_ppSwapChain)
            *_orig_ppSwapChain = (IDXGISwapChain1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_IDXGISwapChain1,
                (uint64_t)(uintptr_t)_raw_ppSwapChain,
                (struct npt_com_base *)self);
        else
            *_orig_ppSwapChain = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgifactorymedia_default_CreateDecodeSwapChainForCompositionSurfaceHandle(void *self, IUnknown * pDevice, HANDLE hSurface, DXGI_DECODE_SWAP_CHAIN_DESC * pDesc, IDXGIResource * pYuvDecodeBuffers, IDXGIOutput * pRestrictToOutput, IDXGIDecodeSwapChain ** ppSwapChain)
{
    IDXGIDecodeSwapChain *_raw_ppSwapChain = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    IDXGIDecodeSwapChain **_orig_ppSwapChain = ppSwapChain;
    if (ppSwapChain)
        ppSwapChain = (IDXGIDecodeSwapChain **)&_raw_ppSwapChain;
    npt_async_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(npt_com_self_ring(self),npt_com_self_id(self),pDevice,hSurface,pDesc,pYuvDecodeBuffers,pRestrictToOutput,ppSwapChain);
    if (_orig_ppSwapChain) {
        if (_raw_ppSwapChain)
            *_orig_ppSwapChain = (IDXGIDecodeSwapChain *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_IDXGIDecodeSwapChain,
                (uint64_t)(uintptr_t)_raw_ppSwapChain,
                (struct npt_com_base *)self);
        else
            *_orig_ppSwapChain = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_idxgifactorymedia_fill_default_vtbl(struct npt_idxgifactorymedia_client_vtbl *dst)
{
    dst->QueryInterface = npt_idxgifactorymedia_default_QueryInterface;
    dst->AddRef         = npt_idxgifactorymedia_default_AddRef;
    dst->Release        = npt_idxgifactorymedia_default_Release;
    dst->CreateSwapChainForCompositionSurfaceHandle = npt_idxgifactorymedia_default_CreateSwapChainForCompositionSurfaceHandle;
    dst->CreateDecodeSwapChainForCompositionSurfaceHandle = npt_idxgifactorymedia_default_CreateDecodeSwapChainForCompositionSurfaceHandle;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_idxgifactorymedia_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_idxgifactorymedia_client_vtbl npt_idxgifactorymedia_default_vtbl_storage;

void
npt_idxgifactorymedia_install_default_vtbl(void)
{
    npt_idxgifactorymedia_fill_default_vtbl(&npt_idxgifactorymedia_default_vtbl_storage);
}

void *
npt_idxgifactorymedia_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_idxgifactorymedia_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
