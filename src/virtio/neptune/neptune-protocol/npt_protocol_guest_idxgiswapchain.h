/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgiswapchain.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGISWAPCHAIN_H
#define NPT_PROTOCOL_GUEST_IDXGISWAPCHAIN_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGISwapChain::Present
 */

struct npt_command_IDXGISwapChain_Present {
    void *_self;
    UINT SyncInterval;
    UINT Flags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_Present(const UINT SyncInterval,const UINT Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&SyncInterval, max_mode);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_Present(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  UINT SyncInterval,
                                  UINT Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_Present(SyncInterval,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_Present),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &SyncInterval);
    npt_encode_UINT(enc, &Flags);
}

static inline size_t
npt_sizeof_IDXGISwapChain_Present_reply(const UINT SyncInterval,const UINT Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip SyncInterval (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_Present_reply(struct npt_cs_decoder *dec,
                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_Present))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_Present reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_Present));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain_Present(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  UINT SyncInterval,
                                  UINT Flags,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_Present(SyncInterval,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_Present_reply(SyncInterval,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_Present(enc, cmd_flags, object_id, SyncInterval, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_Present(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 UINT SyncInterval,
                                 UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_Present(ring, 0, object_id, SyncInterval, Flags, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_Present(struct npt_ring *ring,
                                npt_object_id object_id,
                                UINT SyncInterval,
                                UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_Present(ring, NPT_CMD_FLAG_REPLY, object_id, SyncInterval, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_Present_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain::GetBuffer
 */

struct npt_command_IDXGISwapChain_GetBuffer {
    void *_self;
    UINT Buffer;
    const IID * riid;
    void ** ppSurface;
    /* Shadow: the guest-allocated id for ppSurface arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSurface -> *ppSurface} in the
     * context object table. */
    uint64_t _guest_id_ppSurface;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_GetBuffer(const UINT Buffer,const IID * riid,void ** ppSurface)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Buffer, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppSurface: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_GetBuffer(struct npt_cs_encoder *enc,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT Buffer,
                                    const IID * riid,
                                    void ** ppSurface)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_GetBuffer(Buffer,riid,ppSurface);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetBuffer),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Buffer);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppSurface = 0;
    if (ppSurface) {
        _gid_ppSurface = npt_com_allocate_next_id();
        *ppSurface = (void *)(uintptr_t)_gid_ppSurface;
    }
    npt_encode_uint64_t(enc, &_gid_ppSurface);
}
}

static inline size_t
npt_sizeof_IDXGISwapChain_GetBuffer_reply(const UINT Buffer,const IID * riid,void ** ppSurface)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Buffer (input only) */
/* skip riid (input only) */
/* skip ppSurface (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_GetBuffer_reply(struct npt_cs_decoder *dec,
                                          void ** ppSurface,
                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_GetBuffer))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_GetBuffer reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_GetBuffer));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSurface (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGISwapChain_GetBuffer(struct npt_ring *ring,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT Buffer,
                                    const IID * riid,
                                    void ** ppSurface,
                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_GetBuffer(Buffer,riid,ppSurface);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_GetBuffer_reply(Buffer,riid,ppSurface)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_GetBuffer(enc, cmd_flags, object_id, Buffer, riid, ppSurface);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_GetBuffer(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   UINT Buffer,
                                   const IID * riid,
                                   void ** ppSurface)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetBuffer(ring, 0, object_id, Buffer, riid, ppSurface, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_GetBuffer(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  UINT Buffer,
                                  const IID * riid,
                                  void ** ppSurface)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetBuffer(ring, NPT_CMD_FLAG_REPLY, object_id, Buffer, riid, ppSurface, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_GetBuffer_reply(dec, ppSurface, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain::SetFullscreenState
 */

struct npt_command_IDXGISwapChain_SetFullscreenState {
    void *_self;
    BOOL Fullscreen;
    IDXGIOutput * pTarget;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_SetFullscreenState(const BOOL Fullscreen,const IDXGIOutput * pTarget)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_BOOL(&Fullscreen, max_mode);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_SetFullscreenState(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             BOOL Fullscreen,
                                             IDXGIOutput * pTarget)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_SetFullscreenState(Fullscreen,pTarget);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_SetFullscreenState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_BOOL(enc, &Fullscreen);
    npt_encode_com_handle(enc, npt_object_get_id(pTarget));
}

static inline size_t
npt_sizeof_IDXGISwapChain_SetFullscreenState_reply(const BOOL Fullscreen,const IDXGIOutput * pTarget)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Fullscreen (input only) */
/* skip pTarget (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_SetFullscreenState_reply(struct npt_cs_decoder *dec,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_SetFullscreenState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_SetFullscreenState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_SetFullscreenState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain_SetFullscreenState(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             BOOL Fullscreen,
                                             IDXGIOutput * pTarget,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_SetFullscreenState(Fullscreen,pTarget);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_SetFullscreenState_reply(Fullscreen,pTarget)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_SetFullscreenState(enc, cmd_flags, object_id, Fullscreen, pTarget);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_SetFullscreenState(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            BOOL Fullscreen,
                                            IDXGIOutput * pTarget)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_SetFullscreenState(ring, 0, object_id, Fullscreen, pTarget, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_SetFullscreenState(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           BOOL Fullscreen,
                                           IDXGIOutput * pTarget)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_SetFullscreenState(ring, NPT_CMD_FLAG_REPLY, object_id, Fullscreen, pTarget, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_SetFullscreenState_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain::GetFullscreenState
 */

struct npt_command_IDXGISwapChain_GetFullscreenState {
    void *_self;
    BOOL * pFullscreen;
    IDXGIOutput ** ppTarget;
    /* Shadow: the guest-allocated id for ppTarget arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTarget -> *ppTarget} in the
     * context object table. */
    uint64_t _guest_id_ppTarget;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_GetFullscreenState(const BOOL * pFullscreen,IDXGIOutput ** ppTarget)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pFullscreen (output only) */
cmd_size += sizeof(uint64_t);  /* ppTarget: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_GetFullscreenState(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             BOOL * pFullscreen,
                                             IDXGIOutput ** ppTarget)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_GetFullscreenState(pFullscreen,ppTarget);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetFullscreenState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pFullscreen (output only) */
{
    uint64_t _gid_ppTarget = 0;
    if (ppTarget) {
        _gid_ppTarget = npt_com_allocate_next_id();
        *ppTarget = (IDXGIOutput *)(uintptr_t)_gid_ppTarget;
    }
    npt_encode_uint64_t(enc, &_gid_ppTarget);
}
}

static inline size_t
npt_sizeof_IDXGISwapChain_GetFullscreenState_reply(const BOOL * pFullscreen,IDXGIOutput ** ppTarget)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
/* skip ppTarget (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_GetFullscreenState_reply(struct npt_cs_decoder *dec,
                                                   BOOL * pFullscreen,
                                                   IDXGIOutput ** ppTarget,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_GetFullscreenState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_GetFullscreenState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_GetFullscreenState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pFullscreen)
            npt_decode_BOOL(dec, (BOOL *)pFullscreen);
    }
/* skip ppTarget (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGISwapChain_GetFullscreenState(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             BOOL * pFullscreen,
                                             IDXGIOutput ** ppTarget,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_GetFullscreenState(pFullscreen,ppTarget);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_GetFullscreenState_reply(pFullscreen,ppTarget)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_GetFullscreenState(enc, cmd_flags, object_id, pFullscreen, ppTarget);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_GetFullscreenState(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            BOOL * pFullscreen,
                                            IDXGIOutput ** ppTarget)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetFullscreenState(ring, 0, object_id, pFullscreen, ppTarget, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_GetFullscreenState(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           BOOL * pFullscreen,
                                           IDXGIOutput ** ppTarget)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetFullscreenState(ring, NPT_CMD_FLAG_REPLY, object_id, pFullscreen, ppTarget, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_GetFullscreenState_reply(dec, pFullscreen, ppTarget, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain::GetDesc
 */

struct npt_command_IDXGISwapChain_GetDesc {
    void *_self;
    DXGI_SWAP_CHAIN_DESC * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_GetDesc(const DXGI_SWAP_CHAIN_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_GetDesc(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_SWAP_CHAIN_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_GetDesc(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain_GetDesc_reply(const DXGI_SWAP_CHAIN_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_DESC(&(const DXGI_SWAP_CHAIN_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_GetDesc_reply(struct npt_cs_decoder *dec,
                                        DXGI_SWAP_CHAIN_DESC * pDesc,
                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_GetDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_GetDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_GetDesc));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_SWAP_CHAIN_DESC(dec, (DXGI_SWAP_CHAIN_DESC *)pDesc);
    }
}

static inline void
npt_submit_IDXGISwapChain_GetDesc(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_SWAP_CHAIN_DESC * pDesc,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_GetDesc(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_GetDesc_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_GetDesc(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_GetDesc(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 DXGI_SWAP_CHAIN_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetDesc(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_GetDesc(struct npt_ring *ring,
                                npt_object_id object_id,
                                DXGI_SWAP_CHAIN_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetDesc(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_GetDesc_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain::ResizeBuffers
 */

struct npt_command_IDXGISwapChain_ResizeBuffers {
    void *_self;
    UINT BufferCount;
    UINT Width;
    UINT Height;
    DXGI_FORMAT NewFormat;
    UINT SwapChainFlags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_ResizeBuffers(const UINT BufferCount,const UINT Width,const UINT Height,const DXGI_FORMAT NewFormat,const UINT SwapChainFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&BufferCount, max_mode);
    cmd_size += npt_sizeof_UINT(&Width, max_mode);
    cmd_size += npt_sizeof_UINT(&Height, max_mode);
    cmd_size += npt_sizeof_DXGI_FORMAT(&NewFormat, max_mode);
    cmd_size += npt_sizeof_UINT(&SwapChainFlags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_ResizeBuffers(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        UINT BufferCount,
                                        UINT Width,
                                        UINT Height,
                                        DXGI_FORMAT NewFormat,
                                        UINT SwapChainFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_ResizeBuffers(BufferCount,Width,Height,NewFormat,SwapChainFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_ResizeBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &BufferCount);
    npt_encode_UINT(enc, &Width);
    npt_encode_UINT(enc, &Height);
    npt_encode_DXGI_FORMAT(enc, &NewFormat);
    npt_encode_UINT(enc, &SwapChainFlags);
}

static inline size_t
npt_sizeof_IDXGISwapChain_ResizeBuffers_reply(const UINT BufferCount,const UINT Width,const UINT Height,const DXGI_FORMAT NewFormat,const UINT SwapChainFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip BufferCount (input only) */
/* skip Width (input only) */
/* skip Height (input only) */
/* skip NewFormat (input only) */
/* skip SwapChainFlags (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_ResizeBuffers_reply(struct npt_cs_decoder *dec,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_ResizeBuffers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_ResizeBuffers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_ResizeBuffers));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain_ResizeBuffers(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        UINT BufferCount,
                                        UINT Width,
                                        UINT Height,
                                        DXGI_FORMAT NewFormat,
                                        UINT SwapChainFlags,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_ResizeBuffers(BufferCount,Width,Height,NewFormat,SwapChainFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_ResizeBuffers_reply(BufferCount,Width,Height,NewFormat,SwapChainFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_ResizeBuffers(enc, cmd_flags, object_id, BufferCount, Width, Height, NewFormat, SwapChainFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_ResizeBuffers(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       UINT BufferCount,
                                       UINT Width,
                                       UINT Height,
                                       DXGI_FORMAT NewFormat,
                                       UINT SwapChainFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_ResizeBuffers(ring, 0, object_id, BufferCount, Width, Height, NewFormat, SwapChainFlags, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_ResizeBuffers(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      UINT BufferCount,
                                      UINT Width,
                                      UINT Height,
                                      DXGI_FORMAT NewFormat,
                                      UINT SwapChainFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_ResizeBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, BufferCount, Width, Height, NewFormat, SwapChainFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_ResizeBuffers_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain::ResizeTarget
 */

struct npt_command_IDXGISwapChain_ResizeTarget {
    void *_self;
    const DXGI_MODE_DESC * pNewTargetParameters;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_ResizeTarget(const DXGI_MODE_DESC * pNewTargetParameters)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pNewTargetParameters);
    if (pNewTargetParameters)
        cmd_size += npt_sizeof_DXGI_MODE_DESC(pNewTargetParameters, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_ResizeTarget(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       const DXGI_MODE_DESC * pNewTargetParameters)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_ResizeTarget(pNewTargetParameters);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_ResizeTarget),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pNewTargetParameters))
        npt_encode_DXGI_MODE_DESC(enc, pNewTargetParameters);
}

static inline size_t
npt_sizeof_IDXGISwapChain_ResizeTarget_reply(const DXGI_MODE_DESC * pNewTargetParameters)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pNewTargetParameters (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_ResizeTarget_reply(struct npt_cs_decoder *dec,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_ResizeTarget))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_ResizeTarget reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_ResizeTarget));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain_ResizeTarget(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       const DXGI_MODE_DESC * pNewTargetParameters,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_ResizeTarget(pNewTargetParameters);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_ResizeTarget_reply(pNewTargetParameters)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_ResizeTarget(enc, cmd_flags, object_id, pNewTargetParameters);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_ResizeTarget(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      const DXGI_MODE_DESC * pNewTargetParameters)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_ResizeTarget(ring, 0, object_id, pNewTargetParameters, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_ResizeTarget(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     const DXGI_MODE_DESC * pNewTargetParameters)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_ResizeTarget(ring, NPT_CMD_FLAG_REPLY, object_id, pNewTargetParameters, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_ResizeTarget_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain::GetContainingOutput
 */

struct npt_command_IDXGISwapChain_GetContainingOutput {
    void *_self;
    IDXGIOutput ** ppOutput;
    /* Shadow: the guest-allocated id for ppOutput arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppOutput -> *ppOutput} in the
     * context object table. */
    uint64_t _guest_id_ppOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_GetContainingOutput(IDXGIOutput ** ppOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppOutput: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_GetContainingOutput(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              IDXGIOutput ** ppOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_GetContainingOutput(ppOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetContainingOutput),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppOutput = 0;
    if (ppOutput) {
        _gid_ppOutput = npt_com_allocate_next_id();
        *ppOutput = (IDXGIOutput *)(uintptr_t)_gid_ppOutput;
    }
    npt_encode_uint64_t(enc, &_gid_ppOutput);
}
}

static inline size_t
npt_sizeof_IDXGISwapChain_GetContainingOutput_reply(IDXGIOutput ** ppOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppOutput (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_GetContainingOutput_reply(struct npt_cs_decoder *dec,
                                                    IDXGIOutput ** ppOutput,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_GetContainingOutput))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_GetContainingOutput reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_GetContainingOutput));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppOutput (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGISwapChain_GetContainingOutput(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              IDXGIOutput ** ppOutput,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_GetContainingOutput(ppOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_GetContainingOutput_reply(ppOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_GetContainingOutput(enc, cmd_flags, object_id, ppOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_GetContainingOutput(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             IDXGIOutput ** ppOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetContainingOutput(ring, 0, object_id, ppOutput, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_GetContainingOutput(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            IDXGIOutput ** ppOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetContainingOutput(ring, NPT_CMD_FLAG_REPLY, object_id, ppOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_GetContainingOutput_reply(dec, ppOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain::GetFrameStatistics
 */

struct npt_command_IDXGISwapChain_GetFrameStatistics {
    void *_self;
    DXGI_FRAME_STATISTICS * pStats;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_GetFrameStatistics(const DXGI_FRAME_STATISTICS * pStats)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pStats (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_GetFrameStatistics(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             DXGI_FRAME_STATISTICS * pStats)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_GetFrameStatistics(pStats);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetFrameStatistics),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pStats (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain_GetFrameStatistics_reply(const DXGI_FRAME_STATISTICS * pStats)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_FRAME_STATISTICS(&(const DXGI_FRAME_STATISTICS){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_GetFrameStatistics_reply(struct npt_cs_decoder *dec,
                                                   DXGI_FRAME_STATISTICS * pStats,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_GetFrameStatistics))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_GetFrameStatistics reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_GetFrameStatistics));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pStats)
            npt_decode_DXGI_FRAME_STATISTICS(dec, (DXGI_FRAME_STATISTICS *)pStats);
    }
}

static inline void
npt_submit_IDXGISwapChain_GetFrameStatistics(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             DXGI_FRAME_STATISTICS * pStats,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_GetFrameStatistics(pStats);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_GetFrameStatistics_reply(pStats)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_GetFrameStatistics(enc, cmd_flags, object_id, pStats);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_GetFrameStatistics(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            DXGI_FRAME_STATISTICS * pStats)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetFrameStatistics(ring, 0, object_id, pStats, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_GetFrameStatistics(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           DXGI_FRAME_STATISTICS * pStats)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetFrameStatistics(ring, NPT_CMD_FLAG_REPLY, object_id, pStats, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_GetFrameStatistics_reply(dec, pStats, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain::GetLastPresentCount
 */

struct npt_command_IDXGISwapChain_GetLastPresentCount {
    void *_self;
    UINT * pLastPresentCount;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain_GetLastPresentCount(const UINT * pLastPresentCount)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pLastPresentCount (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain_GetLastPresentCount(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT * pLastPresentCount)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain_GetLastPresentCount(pLastPresentCount);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetLastPresentCount),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pLastPresentCount (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain_GetLastPresentCount_reply(const UINT * pLastPresentCount)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain_GetLastPresentCount_reply(struct npt_cs_decoder *dec,
                                                    UINT * pLastPresentCount,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                                  NPT_METHOD_IDXGISwapChain_GetLastPresentCount))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain_GetLastPresentCount reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                       NPT_METHOD_IDXGISwapChain_GetLastPresentCount));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pLastPresentCount)
            npt_decode_UINT(dec, (UINT *)pLastPresentCount);
    }
}

static inline void
npt_submit_IDXGISwapChain_GetLastPresentCount(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT * pLastPresentCount,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain_GetLastPresentCount(pLastPresentCount);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain_GetLastPresentCount_reply(pLastPresentCount)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain_GetLastPresentCount(enc, cmd_flags, object_id, pLastPresentCount);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain_GetLastPresentCount(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT * pLastPresentCount)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetLastPresentCount(ring, 0, object_id, pLastPresentCount, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain_GetLastPresentCount(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT * pLastPresentCount)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain_GetLastPresentCount(ring, NPT_CMD_FLAG_REPLY, object_id, pLastPresentCount, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain_GetLastPresentCount_reply(dec, pLastPresentCount, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::GetDesc1
 */

struct npt_command_IDXGISwapChain1_GetDesc1 {
    void *_self;
    DXGI_SWAP_CHAIN_DESC1 * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_GetDesc1(const DXGI_SWAP_CHAIN_DESC1 * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_GetDesc1(struct npt_cs_encoder *enc,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    DXGI_SWAP_CHAIN_DESC1 * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_GetDesc1(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetDesc1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain1_GetDesc1_reply(const DXGI_SWAP_CHAIN_DESC1 * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_DESC1(&(const DXGI_SWAP_CHAIN_DESC1){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_GetDesc1_reply(struct npt_cs_decoder *dec,
                                          DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_GetDesc1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_GetDesc1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_GetDesc1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_SWAP_CHAIN_DESC1(dec, (DXGI_SWAP_CHAIN_DESC1 *)pDesc);
    }
}

static inline void
npt_submit_IDXGISwapChain1_GetDesc1(struct npt_ring *ring,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_GetDesc1(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_GetDesc1_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_GetDesc1(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_GetDesc1(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   DXGI_SWAP_CHAIN_DESC1 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetDesc1(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_GetDesc1(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  DXGI_SWAP_CHAIN_DESC1 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetDesc1(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_GetDesc1_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::GetFullscreenDesc
 */

struct npt_command_IDXGISwapChain1_GetFullscreenDesc {
    void *_self;
    DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_GetFullscreenDesc(const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_GetFullscreenDesc(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_GetFullscreenDesc(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetFullscreenDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain1_GetFullscreenDesc_reply(const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_FULLSCREEN_DESC(&(const DXGI_SWAP_CHAIN_FULLSCREEN_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_GetFullscreenDesc_reply(struct npt_cs_decoder *dec,
                                                   DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pDesc,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_GetFullscreenDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_GetFullscreenDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_GetFullscreenDesc));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_SWAP_CHAIN_FULLSCREEN_DESC(dec, (DXGI_SWAP_CHAIN_FULLSCREEN_DESC *)pDesc);
    }
}

static inline void
npt_submit_IDXGISwapChain1_GetFullscreenDesc(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pDesc,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_GetFullscreenDesc(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_GetFullscreenDesc_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_GetFullscreenDesc(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_GetFullscreenDesc(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetFullscreenDesc(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_GetFullscreenDesc(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetFullscreenDesc(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_GetFullscreenDesc_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::GetHwnd
 */

struct npt_command_IDXGISwapChain1_GetHwnd {
    void *_self;
    HWND * pHwnd;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_GetHwnd(const HWND * pHwnd)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pHwnd (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_GetHwnd(struct npt_cs_encoder *enc,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   HWND * pHwnd)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_GetHwnd(pHwnd);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetHwnd),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pHwnd (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain1_GetHwnd_reply(const HWND * pHwnd)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_GetHwnd_reply(struct npt_cs_decoder *dec,
                                         HWND * pHwnd,
                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_GetHwnd))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_GetHwnd reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_GetHwnd));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        if (pHwnd)
            *pHwnd = (HWND)(uintptr_t)npt_win32_handle_from_id(_id);
    }
}

static inline void
npt_submit_IDXGISwapChain1_GetHwnd(struct npt_ring *ring,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   HWND * pHwnd,
                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_GetHwnd(pHwnd);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_GetHwnd_reply(pHwnd)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_GetHwnd(enc, cmd_flags, object_id, pHwnd);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_GetHwnd(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  HWND * pHwnd)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetHwnd(ring, 0, object_id, pHwnd, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_GetHwnd(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 HWND * pHwnd)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetHwnd(ring, NPT_CMD_FLAG_REPLY, object_id, pHwnd, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_GetHwnd_reply(dec, pHwnd, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::GetCoreWindow
 */

struct npt_command_IDXGISwapChain1_GetCoreWindow {
    void *_self;
    const IID * refiid;
    void ** ppUnk;
    /* Shadow: the guest-allocated id for ppUnk arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppUnk -> *ppUnk} in the
     * context object table. */
    uint64_t _guest_id_ppUnk;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_GetCoreWindow(const IID * refiid,void ** ppUnk)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(refiid);
    if (refiid)
        cmd_size += npt_sizeof_IID(refiid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppUnk: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_GetCoreWindow(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const IID * refiid,
                                         void ** ppUnk)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_GetCoreWindow(refiid,ppUnk);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetCoreWindow),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, refiid))
        npt_encode_IID(enc, refiid);
{
    uint64_t _gid_ppUnk = 0;
    if (ppUnk) {
        _gid_ppUnk = npt_com_allocate_next_id();
        *ppUnk = (void *)(uintptr_t)_gid_ppUnk;
    }
    npt_encode_uint64_t(enc, &_gid_ppUnk);
}
}

static inline size_t
npt_sizeof_IDXGISwapChain1_GetCoreWindow_reply(const IID * refiid,void ** ppUnk)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip refiid (input only) */
/* skip ppUnk (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_GetCoreWindow_reply(struct npt_cs_decoder *dec,
                                               void ** ppUnk,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_GetCoreWindow))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_GetCoreWindow reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_GetCoreWindow));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppUnk (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGISwapChain1_GetCoreWindow(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const IID * refiid,
                                         void ** ppUnk,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_GetCoreWindow(refiid,ppUnk);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_GetCoreWindow_reply(refiid,ppUnk)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_GetCoreWindow(enc, cmd_flags, object_id, refiid, ppUnk);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_GetCoreWindow(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const IID * refiid,
                                        void ** ppUnk)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetCoreWindow(ring, 0, object_id, refiid, ppUnk, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_GetCoreWindow(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const IID * refiid,
                                       void ** ppUnk)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetCoreWindow(ring, NPT_CMD_FLAG_REPLY, object_id, refiid, ppUnk, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_GetCoreWindow_reply(dec, ppUnk, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::Present1
 */

struct npt_command_IDXGISwapChain1_Present1 {
    void *_self;
    UINT SyncInterval;
    UINT PresentFlags;
    const DXGI_PRESENT_PARAMETERS * pPresentParameters;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_Present1(const UINT SyncInterval,const UINT PresentFlags,const DXGI_PRESENT_PARAMETERS * pPresentParameters)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&SyncInterval, max_mode);
    cmd_size += npt_sizeof_UINT(&PresentFlags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pPresentParameters);
    if (pPresentParameters)
        cmd_size += npt_sizeof_DXGI_PRESENT_PARAMETERS(pPresentParameters, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_Present1(struct npt_cs_encoder *enc,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT SyncInterval,
                                    UINT PresentFlags,
                                    const DXGI_PRESENT_PARAMETERS * pPresentParameters)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_Present1(SyncInterval,PresentFlags,pPresentParameters);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_Present1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &SyncInterval);
    npt_encode_UINT(enc, &PresentFlags);
    if (npt_encode_simple_pointer(enc, pPresentParameters))
        npt_encode_DXGI_PRESENT_PARAMETERS(enc, pPresentParameters);
}

static inline size_t
npt_sizeof_IDXGISwapChain1_Present1_reply(const UINT SyncInterval,const UINT PresentFlags,const DXGI_PRESENT_PARAMETERS * pPresentParameters)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip SyncInterval (input only) */
/* skip PresentFlags (input only) */
/* skip pPresentParameters (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_Present1_reply(struct npt_cs_decoder *dec,
                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_Present1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_Present1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_Present1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain1_Present1(struct npt_ring *ring,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT SyncInterval,
                                    UINT PresentFlags,
                                    const DXGI_PRESENT_PARAMETERS * pPresentParameters,
                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_Present1(SyncInterval,PresentFlags,pPresentParameters);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_Present1_reply(SyncInterval,PresentFlags,pPresentParameters)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_Present1(enc, cmd_flags, object_id, SyncInterval, PresentFlags, pPresentParameters);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_Present1(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   UINT SyncInterval,
                                   UINT PresentFlags,
                                   const DXGI_PRESENT_PARAMETERS * pPresentParameters)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_Present1(ring, 0, object_id, SyncInterval, PresentFlags, pPresentParameters, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_Present1(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  UINT SyncInterval,
                                  UINT PresentFlags,
                                  const DXGI_PRESENT_PARAMETERS * pPresentParameters)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_Present1(ring, NPT_CMD_FLAG_REPLY, object_id, SyncInterval, PresentFlags, pPresentParameters, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_Present1_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::IsTemporaryMonoSupported
 */

struct npt_command_IDXGISwapChain1_IsTemporaryMonoSupported {
    void *_self;
    BOOL ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_IsTemporaryMonoSupported(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_IsTemporaryMonoSupported(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_IsTemporaryMonoSupported();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_IsTemporaryMonoSupported),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGISwapChain1_IsTemporaryMonoSupported_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_IsTemporaryMonoSupported_reply(struct npt_cs_decoder *dec,
                                                          BOOL *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_IsTemporaryMonoSupported))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_IsTemporaryMonoSupported reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_IsTemporaryMonoSupported));
        if (ret) *ret = (BOOL)0;
        return;
    }
    if (ret) *ret = (BOOL)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain1_IsTemporaryMonoSupported(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_IsTemporaryMonoSupported();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_IsTemporaryMonoSupported_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_IsTemporaryMonoSupported(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_IsTemporaryMonoSupported(struct npt_ring *ring,
                                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_IsTemporaryMonoSupported(ring, 0, object_id, &submit);
}

static inline BOOL
npt_call_IDXGISwapChain1_IsTemporaryMonoSupported(struct npt_ring *ring,
                                                  npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_IsTemporaryMonoSupported(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        BOOL _ret;
        npt_decode_IDXGISwapChain1_IsTemporaryMonoSupported_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (BOOL)0;
    }
}

/*
 * IDXGISwapChain1::GetRestrictToOutput
 */

struct npt_command_IDXGISwapChain1_GetRestrictToOutput {
    void *_self;
    IDXGIOutput ** ppRestrictToOutput;
    /* Shadow: the guest-allocated id for ppRestrictToOutput arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRestrictToOutput -> *ppRestrictToOutput} in the
     * context object table. */
    uint64_t _guest_id_ppRestrictToOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_GetRestrictToOutput(IDXGIOutput ** ppRestrictToOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppRestrictToOutput: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_GetRestrictToOutput(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               IDXGIOutput ** ppRestrictToOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_GetRestrictToOutput(ppRestrictToOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetRestrictToOutput),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppRestrictToOutput = 0;
    if (ppRestrictToOutput) {
        _gid_ppRestrictToOutput = npt_com_allocate_next_id();
        *ppRestrictToOutput = (IDXGIOutput *)(uintptr_t)_gid_ppRestrictToOutput;
    }
    npt_encode_uint64_t(enc, &_gid_ppRestrictToOutput);
}
}

static inline size_t
npt_sizeof_IDXGISwapChain1_GetRestrictToOutput_reply(IDXGIOutput ** ppRestrictToOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppRestrictToOutput (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_GetRestrictToOutput_reply(struct npt_cs_decoder *dec,
                                                     IDXGIOutput ** ppRestrictToOutput,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_GetRestrictToOutput))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_GetRestrictToOutput reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_GetRestrictToOutput));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppRestrictToOutput (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGISwapChain1_GetRestrictToOutput(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               IDXGIOutput ** ppRestrictToOutput,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_GetRestrictToOutput(ppRestrictToOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_GetRestrictToOutput_reply(ppRestrictToOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_GetRestrictToOutput(enc, cmd_flags, object_id, ppRestrictToOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_GetRestrictToOutput(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              IDXGIOutput ** ppRestrictToOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetRestrictToOutput(ring, 0, object_id, ppRestrictToOutput, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_GetRestrictToOutput(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             IDXGIOutput ** ppRestrictToOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetRestrictToOutput(ring, NPT_CMD_FLAG_REPLY, object_id, ppRestrictToOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_GetRestrictToOutput_reply(dec, ppRestrictToOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::SetBackgroundColor
 */

struct npt_command_IDXGISwapChain1_SetBackgroundColor {
    void *_self;
    const DXGI_RGBA * pColor;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_SetBackgroundColor(const DXGI_RGBA * pColor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pColor);
    if (pColor)
        cmd_size += npt_sizeof_DXGI_RGBA(pColor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_SetBackgroundColor(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const DXGI_RGBA * pColor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_SetBackgroundColor(pColor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_SetBackgroundColor),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pColor))
        npt_encode_DXGI_RGBA(enc, pColor);
}

static inline size_t
npt_sizeof_IDXGISwapChain1_SetBackgroundColor_reply(const DXGI_RGBA * pColor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pColor (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_SetBackgroundColor_reply(struct npt_cs_decoder *dec,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_SetBackgroundColor))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_SetBackgroundColor reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_SetBackgroundColor));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain1_SetBackgroundColor(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const DXGI_RGBA * pColor,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_SetBackgroundColor(pColor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_SetBackgroundColor_reply(pColor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_SetBackgroundColor(enc, cmd_flags, object_id, pColor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_SetBackgroundColor(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const DXGI_RGBA * pColor)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_SetBackgroundColor(ring, 0, object_id, pColor, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_SetBackgroundColor(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const DXGI_RGBA * pColor)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_SetBackgroundColor(ring, NPT_CMD_FLAG_REPLY, object_id, pColor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_SetBackgroundColor_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::GetBackgroundColor
 */

struct npt_command_IDXGISwapChain1_GetBackgroundColor {
    void *_self;
    DXGI_RGBA * pColor;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_GetBackgroundColor(const DXGI_RGBA * pColor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pColor (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_GetBackgroundColor(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              DXGI_RGBA * pColor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_GetBackgroundColor(pColor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetBackgroundColor),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pColor (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain1_GetBackgroundColor_reply(const DXGI_RGBA * pColor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_RGBA(&(const DXGI_RGBA){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_GetBackgroundColor_reply(struct npt_cs_decoder *dec,
                                                    DXGI_RGBA * pColor,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_GetBackgroundColor))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_GetBackgroundColor reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_GetBackgroundColor));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pColor)
            npt_decode_DXGI_RGBA(dec, (DXGI_RGBA *)pColor);
    }
}

static inline void
npt_submit_IDXGISwapChain1_GetBackgroundColor(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              DXGI_RGBA * pColor,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_GetBackgroundColor(pColor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_GetBackgroundColor_reply(pColor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_GetBackgroundColor(enc, cmd_flags, object_id, pColor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_GetBackgroundColor(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             DXGI_RGBA * pColor)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetBackgroundColor(ring, 0, object_id, pColor, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_GetBackgroundColor(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            DXGI_RGBA * pColor)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetBackgroundColor(ring, NPT_CMD_FLAG_REPLY, object_id, pColor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_GetBackgroundColor_reply(dec, pColor, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::SetRotation
 */

struct npt_command_IDXGISwapChain1_SetRotation {
    void *_self;
    DXGI_MODE_ROTATION Rotation;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_SetRotation(const DXGI_MODE_ROTATION Rotation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_MODE_ROTATION(&Rotation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_SetRotation(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       DXGI_MODE_ROTATION Rotation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_SetRotation(Rotation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_SetRotation),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_MODE_ROTATION(enc, &Rotation);
}

static inline size_t
npt_sizeof_IDXGISwapChain1_SetRotation_reply(const DXGI_MODE_ROTATION Rotation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Rotation (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_SetRotation_reply(struct npt_cs_decoder *dec,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_SetRotation))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_SetRotation reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_SetRotation));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain1_SetRotation(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       DXGI_MODE_ROTATION Rotation,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_SetRotation(Rotation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_SetRotation_reply(Rotation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_SetRotation(enc, cmd_flags, object_id, Rotation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_SetRotation(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      DXGI_MODE_ROTATION Rotation)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_SetRotation(ring, 0, object_id, Rotation, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_SetRotation(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     DXGI_MODE_ROTATION Rotation)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_SetRotation(ring, NPT_CMD_FLAG_REPLY, object_id, Rotation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_SetRotation_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain1::GetRotation
 */

struct npt_command_IDXGISwapChain1_GetRotation {
    void *_self;
    DXGI_MODE_ROTATION * pRotation;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain1_GetRotation(const DXGI_MODE_ROTATION * pRotation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pRotation (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain1_GetRotation(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       DXGI_MODE_ROTATION * pRotation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain1_GetRotation(pRotation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetRotation),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pRotation (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain1_GetRotation_reply(const DXGI_MODE_ROTATION * pRotation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_MODE_ROTATION(&(const DXGI_MODE_ROTATION){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain1_GetRotation_reply(struct npt_cs_decoder *dec,
                                             DXGI_MODE_ROTATION * pRotation,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                                  NPT_METHOD_IDXGISwapChain1_GetRotation))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain1_GetRotation reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                       NPT_METHOD_IDXGISwapChain1_GetRotation));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pRotation)
            npt_decode_DXGI_MODE_ROTATION(dec, (DXGI_MODE_ROTATION *)pRotation);
    }
}

static inline void
npt_submit_IDXGISwapChain1_GetRotation(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       DXGI_MODE_ROTATION * pRotation,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain1_GetRotation(pRotation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain1_GetRotation_reply(pRotation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain1_GetRotation(enc, cmd_flags, object_id, pRotation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain1_GetRotation(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      DXGI_MODE_ROTATION * pRotation)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetRotation(ring, 0, object_id, pRotation, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain1_GetRotation(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     DXGI_MODE_ROTATION * pRotation)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain1_GetRotation(ring, NPT_CMD_FLAG_REPLY, object_id, pRotation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain1_GetRotation_reply(dec, pRotation, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain2::SetSourceSize
 */

struct npt_command_IDXGISwapChain2_SetSourceSize {
    void *_self;
    UINT Width;
    UINT Height;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain2_SetSourceSize(const UINT Width,const UINT Height)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Width, max_mode);
    cmd_size += npt_sizeof_UINT(&Height, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain2_SetSourceSize(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         UINT Width,
                                         UINT Height)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain2_SetSourceSize(Width,Height);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_SetSourceSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Width);
    npt_encode_UINT(enc, &Height);
}

static inline size_t
npt_sizeof_IDXGISwapChain2_SetSourceSize_reply(const UINT Width,const UINT Height)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Width (input only) */
/* skip Height (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain2_SetSourceSize_reply(struct npt_cs_decoder *dec,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                                  NPT_METHOD_IDXGISwapChain2_SetSourceSize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain2_SetSourceSize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                       NPT_METHOD_IDXGISwapChain2_SetSourceSize));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain2_SetSourceSize(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         UINT Width,
                                         UINT Height,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain2_SetSourceSize(Width,Height);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain2_SetSourceSize_reply(Width,Height)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain2_SetSourceSize(enc, cmd_flags, object_id, Width, Height);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain2_SetSourceSize(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        UINT Width,
                                        UINT Height)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_SetSourceSize(ring, 0, object_id, Width, Height, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain2_SetSourceSize(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       UINT Width,
                                       UINT Height)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_SetSourceSize(ring, NPT_CMD_FLAG_REPLY, object_id, Width, Height, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain2_SetSourceSize_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain2::GetSourceSize
 */

struct npt_command_IDXGISwapChain2_GetSourceSize {
    void *_self;
    UINT * pWidth;
    UINT * pHeight;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain2_GetSourceSize(const UINT * pWidth,const UINT * pHeight)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pWidth (output only) */
/* skip pHeight (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain2_GetSourceSize(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         UINT * pWidth,
                                         UINT * pHeight)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain2_GetSourceSize(pWidth,pHeight);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_GetSourceSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pWidth (output only) */
/* skip pHeight (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain2_GetSourceSize_reply(const UINT * pWidth,const UINT * pHeight)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain2_GetSourceSize_reply(struct npt_cs_decoder *dec,
                                               UINT * pWidth,
                                               UINT * pHeight,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                                  NPT_METHOD_IDXGISwapChain2_GetSourceSize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain2_GetSourceSize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                       NPT_METHOD_IDXGISwapChain2_GetSourceSize));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pWidth)
            npt_decode_UINT(dec, (UINT *)pWidth);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pHeight)
            npt_decode_UINT(dec, (UINT *)pHeight);
    }
}

static inline void
npt_submit_IDXGISwapChain2_GetSourceSize(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         UINT * pWidth,
                                         UINT * pHeight,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain2_GetSourceSize(pWidth,pHeight);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain2_GetSourceSize_reply(pWidth,pHeight)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain2_GetSourceSize(enc, cmd_flags, object_id, pWidth, pHeight);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain2_GetSourceSize(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        UINT * pWidth,
                                        UINT * pHeight)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_GetSourceSize(ring, 0, object_id, pWidth, pHeight, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain2_GetSourceSize(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       UINT * pWidth,
                                       UINT * pHeight)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_GetSourceSize(ring, NPT_CMD_FLAG_REPLY, object_id, pWidth, pHeight, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain2_GetSourceSize_reply(dec, pWidth, pHeight, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain2::SetMaximumFrameLatency
 */

struct npt_command_IDXGISwapChain2_SetMaximumFrameLatency {
    void *_self;
    UINT MaxLatency;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain2_SetMaximumFrameLatency(const UINT MaxLatency)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&MaxLatency, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain2_SetMaximumFrameLatency(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT MaxLatency)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain2_SetMaximumFrameLatency(MaxLatency);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_SetMaximumFrameLatency),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &MaxLatency);
}

static inline size_t
npt_sizeof_IDXGISwapChain2_SetMaximumFrameLatency_reply(const UINT MaxLatency)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip MaxLatency (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain2_SetMaximumFrameLatency_reply(struct npt_cs_decoder *dec,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                                  NPT_METHOD_IDXGISwapChain2_SetMaximumFrameLatency))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain2_SetMaximumFrameLatency reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                       NPT_METHOD_IDXGISwapChain2_SetMaximumFrameLatency));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain2_SetMaximumFrameLatency(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT MaxLatency,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain2_SetMaximumFrameLatency(MaxLatency);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain2_SetMaximumFrameLatency_reply(MaxLatency)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain2_SetMaximumFrameLatency(enc, cmd_flags, object_id, MaxLatency);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain2_SetMaximumFrameLatency(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT MaxLatency)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_SetMaximumFrameLatency(ring, 0, object_id, MaxLatency, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain2_SetMaximumFrameLatency(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT MaxLatency)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_SetMaximumFrameLatency(ring, NPT_CMD_FLAG_REPLY, object_id, MaxLatency, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain2_SetMaximumFrameLatency_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain2::GetMaximumFrameLatency
 */

struct npt_command_IDXGISwapChain2_GetMaximumFrameLatency {
    void *_self;
    UINT * pMaxLatency;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain2_GetMaximumFrameLatency(const UINT * pMaxLatency)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pMaxLatency (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain2_GetMaximumFrameLatency(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT * pMaxLatency)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain2_GetMaximumFrameLatency(pMaxLatency);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_GetMaximumFrameLatency),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pMaxLatency (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain2_GetMaximumFrameLatency_reply(const UINT * pMaxLatency)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain2_GetMaximumFrameLatency_reply(struct npt_cs_decoder *dec,
                                                        UINT * pMaxLatency,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                                  NPT_METHOD_IDXGISwapChain2_GetMaximumFrameLatency))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain2_GetMaximumFrameLatency reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                       NPT_METHOD_IDXGISwapChain2_GetMaximumFrameLatency));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pMaxLatency)
            npt_decode_UINT(dec, (UINT *)pMaxLatency);
    }
}

static inline void
npt_submit_IDXGISwapChain2_GetMaximumFrameLatency(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT * pMaxLatency,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain2_GetMaximumFrameLatency(pMaxLatency);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain2_GetMaximumFrameLatency_reply(pMaxLatency)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain2_GetMaximumFrameLatency(enc, cmd_flags, object_id, pMaxLatency);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain2_GetMaximumFrameLatency(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT * pMaxLatency)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_GetMaximumFrameLatency(ring, 0, object_id, pMaxLatency, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain2_GetMaximumFrameLatency(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT * pMaxLatency)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_GetMaximumFrameLatency(ring, NPT_CMD_FLAG_REPLY, object_id, pMaxLatency, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain2_GetMaximumFrameLatency_reply(dec, pMaxLatency, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain2::GetFrameLatencyWaitableObject
 */

struct npt_command_IDXGISwapChain2_GetFrameLatencyWaitableObject {
    void *_self;
    HANDLE ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain2_GetFrameLatencyWaitableObject(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain2_GetFrameLatencyWaitableObject(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain2_GetFrameLatencyWaitableObject();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_GetFrameLatencyWaitableObject),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGISwapChain2_GetFrameLatencyWaitableObject_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_HANDLE(&(const HANDLE){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain2_GetFrameLatencyWaitableObject_reply(struct npt_cs_decoder *dec,
                                                               HANDLE *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                                  NPT_METHOD_IDXGISwapChain2_GetFrameLatencyWaitableObject))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain2_GetFrameLatencyWaitableObject reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                       NPT_METHOD_IDXGISwapChain2_GetFrameLatencyWaitableObject));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_HANDLE(dec, ret);
}

static inline void
npt_submit_IDXGISwapChain2_GetFrameLatencyWaitableObject(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain2_GetFrameLatencyWaitableObject();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain2_GetFrameLatencyWaitableObject_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain2_GetFrameLatencyWaitableObject(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain2_GetFrameLatencyWaitableObject(struct npt_ring *ring,
                                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_GetFrameLatencyWaitableObject(ring, 0, object_id, &submit);
}

static inline HANDLE
npt_call_IDXGISwapChain2_GetFrameLatencyWaitableObject(struct npt_ring *ring,
                                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_GetFrameLatencyWaitableObject(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HANDLE _ret;
        npt_decode_IDXGISwapChain2_GetFrameLatencyWaitableObject_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HANDLE){0};
    }
}

/*
 * IDXGISwapChain2::SetMatrixTransform
 */

struct npt_command_IDXGISwapChain2_SetMatrixTransform {
    void *_self;
    const DXGI_MATRIX_3X2_F * pMatrix;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain2_SetMatrixTransform(const DXGI_MATRIX_3X2_F * pMatrix)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pMatrix);
    if (pMatrix)
        cmd_size += npt_sizeof_DXGI_MATRIX_3X2_F(pMatrix, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain2_SetMatrixTransform(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const DXGI_MATRIX_3X2_F * pMatrix)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain2_SetMatrixTransform(pMatrix);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_SetMatrixTransform),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pMatrix))
        npt_encode_DXGI_MATRIX_3X2_F(enc, pMatrix);
}

static inline size_t
npt_sizeof_IDXGISwapChain2_SetMatrixTransform_reply(const DXGI_MATRIX_3X2_F * pMatrix)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pMatrix (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain2_SetMatrixTransform_reply(struct npt_cs_decoder *dec,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                                  NPT_METHOD_IDXGISwapChain2_SetMatrixTransform))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain2_SetMatrixTransform reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                       NPT_METHOD_IDXGISwapChain2_SetMatrixTransform));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain2_SetMatrixTransform(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const DXGI_MATRIX_3X2_F * pMatrix,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain2_SetMatrixTransform(pMatrix);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain2_SetMatrixTransform_reply(pMatrix)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain2_SetMatrixTransform(enc, cmd_flags, object_id, pMatrix);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain2_SetMatrixTransform(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const DXGI_MATRIX_3X2_F * pMatrix)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_SetMatrixTransform(ring, 0, object_id, pMatrix, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain2_SetMatrixTransform(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const DXGI_MATRIX_3X2_F * pMatrix)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_SetMatrixTransform(ring, NPT_CMD_FLAG_REPLY, object_id, pMatrix, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain2_SetMatrixTransform_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain2::GetMatrixTransform
 */

struct npt_command_IDXGISwapChain2_GetMatrixTransform {
    void *_self;
    DXGI_MATRIX_3X2_F * pMatrix;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain2_GetMatrixTransform(const DXGI_MATRIX_3X2_F * pMatrix)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pMatrix (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain2_GetMatrixTransform(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              DXGI_MATRIX_3X2_F * pMatrix)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain2_GetMatrixTransform(pMatrix);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_GetMatrixTransform),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pMatrix (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain2_GetMatrixTransform_reply(const DXGI_MATRIX_3X2_F * pMatrix)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_MATRIX_3X2_F(&(const DXGI_MATRIX_3X2_F){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain2_GetMatrixTransform_reply(struct npt_cs_decoder *dec,
                                                    DXGI_MATRIX_3X2_F * pMatrix,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                                  NPT_METHOD_IDXGISwapChain2_GetMatrixTransform))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain2_GetMatrixTransform reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                       NPT_METHOD_IDXGISwapChain2_GetMatrixTransform));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pMatrix)
            npt_decode_DXGI_MATRIX_3X2_F(dec, (DXGI_MATRIX_3X2_F *)pMatrix);
    }
}

static inline void
npt_submit_IDXGISwapChain2_GetMatrixTransform(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              DXGI_MATRIX_3X2_F * pMatrix,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain2_GetMatrixTransform(pMatrix);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain2_GetMatrixTransform_reply(pMatrix)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain2_GetMatrixTransform(enc, cmd_flags, object_id, pMatrix);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain2_GetMatrixTransform(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             DXGI_MATRIX_3X2_F * pMatrix)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_GetMatrixTransform(ring, 0, object_id, pMatrix, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain2_GetMatrixTransform(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            DXGI_MATRIX_3X2_F * pMatrix)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain2_GetMatrixTransform(ring, NPT_CMD_FLAG_REPLY, object_id, pMatrix, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain2_GetMatrixTransform_reply(dec, pMatrix, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain3::GetCurrentBackBufferIndex
 */

struct npt_command_IDXGISwapChain3_GetCurrentBackBufferIndex {
    void *_self;
    UINT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain3_GetCurrentBackBufferIndex(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain3_GetCurrentBackBufferIndex(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain3_GetCurrentBackBufferIndex();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                 NPT_METHOD_IDXGISwapChain3_GetCurrentBackBufferIndex),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGISwapChain3_GetCurrentBackBufferIndex_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain3_GetCurrentBackBufferIndex_reply(struct npt_cs_decoder *dec,
                                                           UINT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                                  NPT_METHOD_IDXGISwapChain3_GetCurrentBackBufferIndex))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain3_GetCurrentBackBufferIndex reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                       NPT_METHOD_IDXGISwapChain3_GetCurrentBackBufferIndex));
        if (ret) *ret = (UINT)0;
        return;
    }
    if (ret) *ret = (UINT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain3_GetCurrentBackBufferIndex(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain3_GetCurrentBackBufferIndex();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain3_GetCurrentBackBufferIndex_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain3_GetCurrentBackBufferIndex(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain3_GetCurrentBackBufferIndex(struct npt_ring *ring,
                                                    npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain3_GetCurrentBackBufferIndex(ring, 0, object_id, &submit);
}

static inline UINT
npt_call_IDXGISwapChain3_GetCurrentBackBufferIndex(struct npt_ring *ring,
                                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain3_GetCurrentBackBufferIndex(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT _ret;
        npt_decode_IDXGISwapChain3_GetCurrentBackBufferIndex_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT)0;
    }
}

/*
 * IDXGISwapChain3::CheckColorSpaceSupport
 */

struct npt_command_IDXGISwapChain3_CheckColorSpaceSupport {
    void *_self;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
    UINT * pColorSpaceSupport;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain3_CheckColorSpaceSupport(const DXGI_COLOR_SPACE_TYPE ColorSpace,const UINT * pColorSpaceSupport)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&ColorSpace, max_mode);
/* skip pColorSpaceSupport (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain3_CheckColorSpaceSupport(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                  UINT * pColorSpaceSupport)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain3_CheckColorSpaceSupport(ColorSpace,pColorSpaceSupport);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                 NPT_METHOD_IDXGISwapChain3_CheckColorSpaceSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &ColorSpace);
/* skip pColorSpaceSupport (output only) */
}

static inline size_t
npt_sizeof_IDXGISwapChain3_CheckColorSpaceSupport_reply(const DXGI_COLOR_SPACE_TYPE ColorSpace,const UINT * pColorSpaceSupport)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ColorSpace (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain3_CheckColorSpaceSupport_reply(struct npt_cs_decoder *dec,
                                                        UINT * pColorSpaceSupport,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                                  NPT_METHOD_IDXGISwapChain3_CheckColorSpaceSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain3_CheckColorSpaceSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                       NPT_METHOD_IDXGISwapChain3_CheckColorSpaceSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pColorSpaceSupport)
            npt_decode_UINT(dec, (UINT *)pColorSpaceSupport);
    }
}

static inline void
npt_submit_IDXGISwapChain3_CheckColorSpaceSupport(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                  UINT * pColorSpaceSupport,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain3_CheckColorSpaceSupport(ColorSpace,pColorSpaceSupport);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain3_CheckColorSpaceSupport_reply(ColorSpace,pColorSpaceSupport)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain3_CheckColorSpaceSupport(enc, cmd_flags, object_id, ColorSpace, pColorSpaceSupport);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain3_CheckColorSpaceSupport(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                 UINT * pColorSpaceSupport)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain3_CheckColorSpaceSupport(ring, 0, object_id, ColorSpace, pColorSpaceSupport, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain3_CheckColorSpaceSupport(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                UINT * pColorSpaceSupport)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain3_CheckColorSpaceSupport(ring, NPT_CMD_FLAG_REPLY, object_id, ColorSpace, pColorSpaceSupport, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain3_CheckColorSpaceSupport_reply(dec, pColorSpaceSupport, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain3::SetColorSpace1
 */

struct npt_command_IDXGISwapChain3_SetColorSpace1 {
    void *_self;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain3_SetColorSpace1(const DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&ColorSpace, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain3_SetColorSpace1(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain3_SetColorSpace1(ColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                 NPT_METHOD_IDXGISwapChain3_SetColorSpace1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &ColorSpace);
}

static inline size_t
npt_sizeof_IDXGISwapChain3_SetColorSpace1_reply(const DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ColorSpace (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain3_SetColorSpace1_reply(struct npt_cs_decoder *dec,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                                  NPT_METHOD_IDXGISwapChain3_SetColorSpace1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain3_SetColorSpace1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                       NPT_METHOD_IDXGISwapChain3_SetColorSpace1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain3_SetColorSpace1(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_COLOR_SPACE_TYPE ColorSpace,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain3_SetColorSpace1(ColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain3_SetColorSpace1_reply(ColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain3_SetColorSpace1(enc, cmd_flags, object_id, ColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain3_SetColorSpace1(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain3_SetColorSpace1(ring, 0, object_id, ColorSpace, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain3_SetColorSpace1(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain3_SetColorSpace1(ring, NPT_CMD_FLAG_REPLY, object_id, ColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain3_SetColorSpace1_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain3::ResizeBuffers1
 */

struct npt_command_IDXGISwapChain3_ResizeBuffers1 {
    void *_self;
    UINT BufferCount;
    UINT Width;
    UINT Height;
    DXGI_FORMAT Format;
    UINT SwapChainFlags;
    const UINT * pCreationNodeMask;
    IUnknown ** ppPresentQueue;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain3_ResizeBuffers1(const UINT BufferCount,const UINT Width,const UINT Height,const DXGI_FORMAT Format,const UINT SwapChainFlags,const UINT * pCreationNodeMask,IUnknown ** ppPresentQueue)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&BufferCount, max_mode);
    cmd_size += npt_sizeof_UINT(&Width, max_mode);
    cmd_size += npt_sizeof_UINT(&Height, max_mode);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
    cmd_size += npt_sizeof_UINT(&SwapChainFlags, max_mode);
    cmd_size += npt_sizeof_array_count(pCreationNodeMask ? BufferCount : 0);
    if (pCreationNodeMask)
        cmd_size += npt_sizeof_UINT_array(pCreationNodeMask, BufferCount);
    cmd_size += npt_sizeof_array_count(BufferCount);
    cmd_size += sizeof(uint64_t) * BufferCount;
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain3_ResizeBuffers1(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          UINT BufferCount,
                                          UINT Width,
                                          UINT Height,
                                          DXGI_FORMAT Format,
                                          UINT SwapChainFlags,
                                          const UINT * pCreationNodeMask,
                                          IUnknown ** ppPresentQueue)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain3_ResizeBuffers1(BufferCount,Width,Height,Format,SwapChainFlags,pCreationNodeMask,ppPresentQueue);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                 NPT_METHOD_IDXGISwapChain3_ResizeBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &BufferCount);
    npt_encode_UINT(enc, &Width);
    npt_encode_UINT(enc, &Height);
    npt_encode_DXGI_FORMAT(enc, &Format);
    npt_encode_UINT(enc, &SwapChainFlags);
    if (pCreationNodeMask) {
        npt_encode_array_count(enc, BufferCount);
        npt_encode_UINT_array(enc, pCreationNodeMask, BufferCount);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (ppPresentQueue) {
        npt_encode_array_count(enc, BufferCount);
        for (uint32_t _i = 0; _i < (uint32_t)BufferCount; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppPresentQueue[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_IDXGISwapChain3_ResizeBuffers1_reply(const UINT BufferCount,const UINT Width,const UINT Height,const DXGI_FORMAT Format,const UINT SwapChainFlags,const UINT * pCreationNodeMask,IUnknown ** ppPresentQueue)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip BufferCount (input only) */
/* skip Width (input only) */
/* skip Height (input only) */
/* skip Format (input only) */
/* skip SwapChainFlags (input only) */
/* skip pCreationNodeMask (input only) */
/* skip ppPresentQueue (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain3_ResizeBuffers1_reply(struct npt_cs_decoder *dec,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                                  NPT_METHOD_IDXGISwapChain3_ResizeBuffers1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain3_ResizeBuffers1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                       NPT_METHOD_IDXGISwapChain3_ResizeBuffers1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain3_ResizeBuffers1(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          UINT BufferCount,
                                          UINT Width,
                                          UINT Height,
                                          DXGI_FORMAT Format,
                                          UINT SwapChainFlags,
                                          const UINT * pCreationNodeMask,
                                          IUnknown ** ppPresentQueue,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain3_ResizeBuffers1(BufferCount,Width,Height,Format,SwapChainFlags,pCreationNodeMask,ppPresentQueue);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain3_ResizeBuffers1_reply(BufferCount,Width,Height,Format,SwapChainFlags,pCreationNodeMask,ppPresentQueue)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain3_ResizeBuffers1(enc, cmd_flags, object_id, BufferCount, Width, Height, Format, SwapChainFlags, pCreationNodeMask, ppPresentQueue);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain3_ResizeBuffers1(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         UINT BufferCount,
                                         UINT Width,
                                         UINT Height,
                                         DXGI_FORMAT Format,
                                         UINT SwapChainFlags,
                                         const UINT * pCreationNodeMask,
                                         IUnknown ** ppPresentQueue)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain3_ResizeBuffers1(ring, 0, object_id, BufferCount, Width, Height, Format, SwapChainFlags, pCreationNodeMask, ppPresentQueue, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain3_ResizeBuffers1(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        UINT BufferCount,
                                        UINT Width,
                                        UINT Height,
                                        DXGI_FORMAT Format,
                                        UINT SwapChainFlags,
                                        const UINT * pCreationNodeMask,
                                        IUnknown ** ppPresentQueue)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain3_ResizeBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, BufferCount, Width, Height, Format, SwapChainFlags, pCreationNodeMask, ppPresentQueue, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain3_ResizeBuffers1_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGISwapChain4::SetHDRMetaData
 */

struct npt_command_IDXGISwapChain4_SetHDRMetaData {
    void *_self;
    DXGI_HDR_METADATA_TYPE Type;
    UINT Size;
    void * pMetaData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGISwapChain4_SetHDRMetaData(const DXGI_HDR_METADATA_TYPE Type,const UINT Size,const void * pMetaData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_HDR_METADATA_TYPE(&Type, max_mode);
    cmd_size += npt_sizeof_UINT(&Size, max_mode);
    if (pMetaData) {
        cmd_size += npt_sizeof_array_count(Size);
        cmd_size += npt_sizeof_blob_array(pMetaData, Size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_IDXGISwapChain4_SetHDRMetaData(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_HDR_METADATA_TYPE Type,
                                          UINT Size,
                                          void * pMetaData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGISwapChain4_SetHDRMetaData(Type,Size,pMetaData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain4,
                                 NPT_METHOD_IDXGISwapChain4_SetHDRMetaData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_HDR_METADATA_TYPE(enc, &Type);
    npt_encode_UINT(enc, &Size);
    if (pMetaData) {
        npt_encode_array_count(enc, Size);
        npt_encode_blob_array(enc, pMetaData, Size);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_IDXGISwapChain4_SetHDRMetaData_reply(const DXGI_HDR_METADATA_TYPE Type,const UINT Size,const void * pMetaData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Type (input only) */
/* skip Size (input only) */
/* skip pMetaData (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGISwapChain4_SetHDRMetaData_reply(struct npt_cs_decoder *dec,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain4,
                                                  NPT_METHOD_IDXGISwapChain4_SetHDRMetaData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGISwapChain4_SetHDRMetaData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain4,
                                       NPT_METHOD_IDXGISwapChain4_SetHDRMetaData));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGISwapChain4_SetHDRMetaData(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_HDR_METADATA_TYPE Type,
                                          UINT Size,
                                          void * pMetaData,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGISwapChain4_SetHDRMetaData(Type,Size,pMetaData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGISwapChain4_SetHDRMetaData_reply(Type,Size,pMetaData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGISwapChain4_SetHDRMetaData(enc, cmd_flags, object_id, Type, Size, pMetaData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGISwapChain4_SetHDRMetaData(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         DXGI_HDR_METADATA_TYPE Type,
                                         UINT Size,
                                         void * pMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain4_SetHDRMetaData(ring, 0, object_id, Type, Size, pMetaData, &submit);
}

static inline HRESULT
npt_call_IDXGISwapChain4_SetHDRMetaData(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        DXGI_HDR_METADATA_TYPE Type,
                                        UINT Size,
                                        void * pMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGISwapChain4_SetHDRMetaData(ring, NPT_CMD_FLAG_REPLY, object_id, Type, Size, pMetaData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGISwapChain4_SetHDRMetaData_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGISWAPCHAIN_H */
