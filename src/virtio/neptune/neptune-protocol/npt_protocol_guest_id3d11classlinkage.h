/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11classlinkage.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11CLASSLINKAGE_H
#define NPT_PROTOCOL_GUEST_ID3D11CLASSLINKAGE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11ClassLinkage::GetClassInstance
 */

struct npt_command_ID3D11ClassLinkage_GetClassInstance {
    void *_self;
    const CHAR * pClassInstanceName;
    UINT InstanceIndex;
    ID3D11ClassInstance ** ppInstance;
    /* Shadow: the guest-allocated id for ppInstance arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppInstance -> *ppInstance} in the
     * context object table. */
    uint64_t _guest_id_ppInstance;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11ClassLinkage_GetClassInstance(const CHAR * pClassInstanceName,const UINT InstanceIndex,ID3D11ClassInstance ** ppInstance)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pClassInstanceName) {
        const size_t _str_size = strlen((const char *)pClassInstanceName) + 1;
        cmd_size += npt_sizeof_array_count(_str_size);
        cmd_size += npt_sizeof_blob_array(pClassInstanceName, _str_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&InstanceIndex, max_mode);
cmd_size += sizeof(uint64_t);  /* ppInstance: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11ClassLinkage_GetClassInstance(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const CHAR * pClassInstanceName,
                                               UINT InstanceIndex,
                                               ID3D11ClassInstance ** ppInstance)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11ClassLinkage_GetClassInstance(pClassInstanceName,InstanceIndex,ppInstance);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassLinkage,
                                 NPT_METHOD_ID3D11ClassLinkage_GetClassInstance),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pClassInstanceName) {
        const size_t _str_size = strlen((const char *)pClassInstanceName) + 1;
        npt_encode_array_count(enc, _str_size);
        npt_encode_blob_array(enc, pClassInstanceName, _str_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &InstanceIndex);
{
    uint64_t _gid_ppInstance = 0;
    if (ppInstance) {
        _gid_ppInstance = npt_com_allocate_next_id();
        *ppInstance = (ID3D11ClassInstance *)(uintptr_t)_gid_ppInstance;
    }
    npt_encode_uint64_t(enc, &_gid_ppInstance);
}
}

static inline size_t
npt_sizeof_ID3D11ClassLinkage_GetClassInstance_reply(const CHAR * pClassInstanceName,const UINT InstanceIndex,ID3D11ClassInstance ** ppInstance)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pClassInstanceName (input only) */
/* skip InstanceIndex (input only) */
/* skip ppInstance (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11ClassLinkage_GetClassInstance_reply(struct npt_cs_decoder *dec,
                                                     ID3D11ClassInstance ** ppInstance,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassLinkage,
                                                  NPT_METHOD_ID3D11ClassLinkage_GetClassInstance))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11ClassLinkage_GetClassInstance reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassLinkage,
                                       NPT_METHOD_ID3D11ClassLinkage_GetClassInstance));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppInstance (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11ClassLinkage_GetClassInstance(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const CHAR * pClassInstanceName,
                                               UINT InstanceIndex,
                                               ID3D11ClassInstance ** ppInstance,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11ClassLinkage_GetClassInstance(pClassInstanceName,InstanceIndex,ppInstance);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11ClassLinkage_GetClassInstance_reply(pClassInstanceName,InstanceIndex,ppInstance)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11ClassLinkage_GetClassInstance(enc, cmd_flags, object_id, pClassInstanceName, InstanceIndex, ppInstance);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11ClassLinkage_GetClassInstance(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const CHAR * pClassInstanceName,
                                              UINT InstanceIndex,
                                              ID3D11ClassInstance ** ppInstance)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassLinkage_GetClassInstance(ring, 0, object_id, pClassInstanceName, InstanceIndex, ppInstance, &submit);
}

static inline HRESULT
npt_call_ID3D11ClassLinkage_GetClassInstance(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const CHAR * pClassInstanceName,
                                             UINT InstanceIndex,
                                             ID3D11ClassInstance ** ppInstance)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassLinkage_GetClassInstance(ring, NPT_CMD_FLAG_REPLY, object_id, pClassInstanceName, InstanceIndex, ppInstance, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11ClassLinkage_GetClassInstance_reply(dec, ppInstance, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11ClassLinkage::CreateClassInstance
 */

struct npt_command_ID3D11ClassLinkage_CreateClassInstance {
    void *_self;
    const CHAR * pClassTypeName;
    UINT ConstantBufferOffset;
    UINT ConstantVectorOffset;
    UINT TextureOffset;
    UINT SamplerOffset;
    ID3D11ClassInstance ** ppInstance;
    /* Shadow: the guest-allocated id for ppInstance arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppInstance -> *ppInstance} in the
     * context object table. */
    uint64_t _guest_id_ppInstance;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11ClassLinkage_CreateClassInstance(const CHAR * pClassTypeName,const UINT ConstantBufferOffset,const UINT ConstantVectorOffset,const UINT TextureOffset,const UINT SamplerOffset,ID3D11ClassInstance ** ppInstance)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pClassTypeName) {
        const size_t _str_size = strlen((const char *)pClassTypeName) + 1;
        cmd_size += npt_sizeof_array_count(_str_size);
        cmd_size += npt_sizeof_blob_array(pClassTypeName, _str_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&ConstantBufferOffset, max_mode);
    cmd_size += npt_sizeof_UINT(&ConstantVectorOffset, max_mode);
    cmd_size += npt_sizeof_UINT(&TextureOffset, max_mode);
    cmd_size += npt_sizeof_UINT(&SamplerOffset, max_mode);
cmd_size += sizeof(uint64_t);  /* ppInstance: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11ClassLinkage_CreateClassInstance(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const CHAR * pClassTypeName,
                                                  UINT ConstantBufferOffset,
                                                  UINT ConstantVectorOffset,
                                                  UINT TextureOffset,
                                                  UINT SamplerOffset,
                                                  ID3D11ClassInstance ** ppInstance)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11ClassLinkage_CreateClassInstance(pClassTypeName,ConstantBufferOffset,ConstantVectorOffset,TextureOffset,SamplerOffset,ppInstance);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassLinkage,
                                 NPT_METHOD_ID3D11ClassLinkage_CreateClassInstance),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pClassTypeName) {
        const size_t _str_size = strlen((const char *)pClassTypeName) + 1;
        npt_encode_array_count(enc, _str_size);
        npt_encode_blob_array(enc, pClassTypeName, _str_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &ConstantBufferOffset);
    npt_encode_UINT(enc, &ConstantVectorOffset);
    npt_encode_UINT(enc, &TextureOffset);
    npt_encode_UINT(enc, &SamplerOffset);
{
    uint64_t _gid_ppInstance = 0;
    if (ppInstance) {
        _gid_ppInstance = npt_com_allocate_next_id();
        *ppInstance = (ID3D11ClassInstance *)(uintptr_t)_gid_ppInstance;
    }
    npt_encode_uint64_t(enc, &_gid_ppInstance);
}
}

static inline size_t
npt_sizeof_ID3D11ClassLinkage_CreateClassInstance_reply(const CHAR * pClassTypeName,const UINT ConstantBufferOffset,const UINT ConstantVectorOffset,const UINT TextureOffset,const UINT SamplerOffset,ID3D11ClassInstance ** ppInstance)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pClassTypeName (input only) */
/* skip ConstantBufferOffset (input only) */
/* skip ConstantVectorOffset (input only) */
/* skip TextureOffset (input only) */
/* skip SamplerOffset (input only) */
/* skip ppInstance (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11ClassLinkage_CreateClassInstance_reply(struct npt_cs_decoder *dec,
                                                        ID3D11ClassInstance ** ppInstance,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassLinkage,
                                                  NPT_METHOD_ID3D11ClassLinkage_CreateClassInstance))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11ClassLinkage_CreateClassInstance reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassLinkage,
                                       NPT_METHOD_ID3D11ClassLinkage_CreateClassInstance));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppInstance (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11ClassLinkage_CreateClassInstance(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const CHAR * pClassTypeName,
                                                  UINT ConstantBufferOffset,
                                                  UINT ConstantVectorOffset,
                                                  UINT TextureOffset,
                                                  UINT SamplerOffset,
                                                  ID3D11ClassInstance ** ppInstance,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11ClassLinkage_CreateClassInstance(pClassTypeName,ConstantBufferOffset,ConstantVectorOffset,TextureOffset,SamplerOffset,ppInstance);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11ClassLinkage_CreateClassInstance_reply(pClassTypeName,ConstantBufferOffset,ConstantVectorOffset,TextureOffset,SamplerOffset,ppInstance)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11ClassLinkage_CreateClassInstance(enc, cmd_flags, object_id, pClassTypeName, ConstantBufferOffset, ConstantVectorOffset, TextureOffset, SamplerOffset, ppInstance);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11ClassLinkage_CreateClassInstance(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const CHAR * pClassTypeName,
                                                 UINT ConstantBufferOffset,
                                                 UINT ConstantVectorOffset,
                                                 UINT TextureOffset,
                                                 UINT SamplerOffset,
                                                 ID3D11ClassInstance ** ppInstance)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassLinkage_CreateClassInstance(ring, 0, object_id, pClassTypeName, ConstantBufferOffset, ConstantVectorOffset, TextureOffset, SamplerOffset, ppInstance, &submit);
}

static inline HRESULT
npt_call_ID3D11ClassLinkage_CreateClassInstance(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const CHAR * pClassTypeName,
                                                UINT ConstantBufferOffset,
                                                UINT ConstantVectorOffset,
                                                UINT TextureOffset,
                                                UINT SamplerOffset,
                                                ID3D11ClassInstance ** ppInstance)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11ClassLinkage_CreateClassInstance(ring, NPT_CMD_FLAG_REPLY, object_id, pClassTypeName, ConstantBufferOffset, ConstantVectorOffset, TextureOffset, SamplerOffset, ppInstance, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11ClassLinkage_CreateClassInstance_reply(dec, ppInstance, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11CLASSLINKAGE_H */
