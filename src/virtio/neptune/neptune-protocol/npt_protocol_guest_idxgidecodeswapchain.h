/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgidecodeswapchain.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIDECODESWAPCHAIN_H
#define NPT_PROTOCOL_GUEST_IDXGIDECODESWAPCHAIN_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIDecodeSwapChain::PresentBuffer
 */

struct npt_command_IDXGIDecodeSwapChain_PresentBuffer {
    void *_self;
    UINT BufferToPresent;
    UINT SyncInterval;
    UINT Flags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_PresentBuffer(const UINT BufferToPresent,const UINT SyncInterval,const UINT Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&BufferToPresent, max_mode);
    cmd_size += npt_sizeof_UINT(&SyncInterval, max_mode);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDecodeSwapChain_PresentBuffer(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT BufferToPresent,
                                              UINT SyncInterval,
                                              UINT Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDecodeSwapChain_PresentBuffer(BufferToPresent,SyncInterval,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_PresentBuffer),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &BufferToPresent);
    npt_encode_UINT(enc, &SyncInterval);
    npt_encode_UINT(enc, &Flags);
}

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_PresentBuffer_reply(const UINT BufferToPresent,const UINT SyncInterval,const UINT Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip BufferToPresent (input only) */
/* skip SyncInterval (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDecodeSwapChain_PresentBuffer_reply(struct npt_cs_decoder *dec,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                                  NPT_METHOD_IDXGIDecodeSwapChain_PresentBuffer))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDecodeSwapChain_PresentBuffer reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                       NPT_METHOD_IDXGIDecodeSwapChain_PresentBuffer));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDecodeSwapChain_PresentBuffer(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT BufferToPresent,
                                              UINT SyncInterval,
                                              UINT Flags,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDecodeSwapChain_PresentBuffer(BufferToPresent,SyncInterval,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDecodeSwapChain_PresentBuffer_reply(BufferToPresent,SyncInterval,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDecodeSwapChain_PresentBuffer(enc, cmd_flags, object_id, BufferToPresent, SyncInterval, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDecodeSwapChain_PresentBuffer(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT BufferToPresent,
                                             UINT SyncInterval,
                                             UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_PresentBuffer(ring, 0, object_id, BufferToPresent, SyncInterval, Flags, &submit);
}

static inline HRESULT
npt_call_IDXGIDecodeSwapChain_PresentBuffer(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT BufferToPresent,
                                            UINT SyncInterval,
                                            UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_PresentBuffer(ring, NPT_CMD_FLAG_REPLY, object_id, BufferToPresent, SyncInterval, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDecodeSwapChain_PresentBuffer_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDecodeSwapChain::SetSourceRect
 */

struct npt_command_IDXGIDecodeSwapChain_SetSourceRect {
    void *_self;
    const RECT * pRect;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_SetSourceRect(const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pRect);
    if (pRect)
        cmd_size += npt_sizeof_RECT(pRect, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDecodeSwapChain_SetSourceRect(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDecodeSwapChain_SetSourceRect(pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_SetSourceRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pRect))
        npt_encode_RECT(enc, pRect);
}

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_SetSourceRect_reply(const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRect (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDecodeSwapChain_SetSourceRect_reply(struct npt_cs_decoder *dec,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                                  NPT_METHOD_IDXGIDecodeSwapChain_SetSourceRect))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDecodeSwapChain_SetSourceRect reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                       NPT_METHOD_IDXGIDecodeSwapChain_SetSourceRect));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDecodeSwapChain_SetSourceRect(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const RECT * pRect,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDecodeSwapChain_SetSourceRect(pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDecodeSwapChain_SetSourceRect_reply(pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDecodeSwapChain_SetSourceRect(enc, cmd_flags, object_id, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDecodeSwapChain_SetSourceRect(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_SetSourceRect(ring, 0, object_id, pRect, &submit);
}

static inline HRESULT
npt_call_IDXGIDecodeSwapChain_SetSourceRect(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_SetSourceRect(ring, NPT_CMD_FLAG_REPLY, object_id, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDecodeSwapChain_SetSourceRect_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDecodeSwapChain::SetTargetRect
 */

struct npt_command_IDXGIDecodeSwapChain_SetTargetRect {
    void *_self;
    const RECT * pRect;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_SetTargetRect(const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pRect);
    if (pRect)
        cmd_size += npt_sizeof_RECT(pRect, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDecodeSwapChain_SetTargetRect(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDecodeSwapChain_SetTargetRect(pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_SetTargetRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pRect))
        npt_encode_RECT(enc, pRect);
}

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_SetTargetRect_reply(const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRect (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDecodeSwapChain_SetTargetRect_reply(struct npt_cs_decoder *dec,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                                  NPT_METHOD_IDXGIDecodeSwapChain_SetTargetRect))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDecodeSwapChain_SetTargetRect reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                       NPT_METHOD_IDXGIDecodeSwapChain_SetTargetRect));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDecodeSwapChain_SetTargetRect(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const RECT * pRect,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDecodeSwapChain_SetTargetRect(pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDecodeSwapChain_SetTargetRect_reply(pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDecodeSwapChain_SetTargetRect(enc, cmd_flags, object_id, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDecodeSwapChain_SetTargetRect(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_SetTargetRect(ring, 0, object_id, pRect, &submit);
}

static inline HRESULT
npt_call_IDXGIDecodeSwapChain_SetTargetRect(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_SetTargetRect(ring, NPT_CMD_FLAG_REPLY, object_id, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDecodeSwapChain_SetTargetRect_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDecodeSwapChain::SetDestSize
 */

struct npt_command_IDXGIDecodeSwapChain_SetDestSize {
    void *_self;
    UINT Width;
    UINT Height;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_SetDestSize(const UINT Width,const UINT Height)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Width, max_mode);
    cmd_size += npt_sizeof_UINT(&Height, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDecodeSwapChain_SetDestSize(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT Width,
                                            UINT Height)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDecodeSwapChain_SetDestSize(Width,Height);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_SetDestSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Width);
    npt_encode_UINT(enc, &Height);
}

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_SetDestSize_reply(const UINT Width,const UINT Height)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Width (input only) */
/* skip Height (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDecodeSwapChain_SetDestSize_reply(struct npt_cs_decoder *dec,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                                  NPT_METHOD_IDXGIDecodeSwapChain_SetDestSize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDecodeSwapChain_SetDestSize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                       NPT_METHOD_IDXGIDecodeSwapChain_SetDestSize));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDecodeSwapChain_SetDestSize(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT Width,
                                            UINT Height,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDecodeSwapChain_SetDestSize(Width,Height);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDecodeSwapChain_SetDestSize_reply(Width,Height)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDecodeSwapChain_SetDestSize(enc, cmd_flags, object_id, Width, Height);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDecodeSwapChain_SetDestSize(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT Width,
                                           UINT Height)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_SetDestSize(ring, 0, object_id, Width, Height, &submit);
}

static inline HRESULT
npt_call_IDXGIDecodeSwapChain_SetDestSize(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          UINT Width,
                                          UINT Height)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_SetDestSize(ring, NPT_CMD_FLAG_REPLY, object_id, Width, Height, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDecodeSwapChain_SetDestSize_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDecodeSwapChain::GetSourceRect
 */

struct npt_command_IDXGIDecodeSwapChain_GetSourceRect {
    void *_self;
    RECT * pRect;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_GetSourceRect(const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pRect (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIDecodeSwapChain_GetSourceRect(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDecodeSwapChain_GetSourceRect(pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_GetSourceRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pRect (output only) */
}

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_GetSourceRect_reply(const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_RECT(&(const RECT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIDecodeSwapChain_GetSourceRect_reply(struct npt_cs_decoder *dec,
                                                    RECT * pRect,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                                  NPT_METHOD_IDXGIDecodeSwapChain_GetSourceRect))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDecodeSwapChain_GetSourceRect reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                       NPT_METHOD_IDXGIDecodeSwapChain_GetSourceRect));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pRect)
            npt_decode_RECT(dec, (RECT *)pRect);
    }
}

static inline void
npt_submit_IDXGIDecodeSwapChain_GetSourceRect(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              RECT * pRect,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDecodeSwapChain_GetSourceRect(pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDecodeSwapChain_GetSourceRect_reply(pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDecodeSwapChain_GetSourceRect(enc, cmd_flags, object_id, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDecodeSwapChain_GetSourceRect(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_GetSourceRect(ring, 0, object_id, pRect, &submit);
}

static inline HRESULT
npt_call_IDXGIDecodeSwapChain_GetSourceRect(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_GetSourceRect(ring, NPT_CMD_FLAG_REPLY, object_id, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDecodeSwapChain_GetSourceRect_reply(dec, pRect, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDecodeSwapChain::GetTargetRect
 */

struct npt_command_IDXGIDecodeSwapChain_GetTargetRect {
    void *_self;
    RECT * pRect;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_GetTargetRect(const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pRect (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIDecodeSwapChain_GetTargetRect(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDecodeSwapChain_GetTargetRect(pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_GetTargetRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pRect (output only) */
}

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_GetTargetRect_reply(const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_RECT(&(const RECT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIDecodeSwapChain_GetTargetRect_reply(struct npt_cs_decoder *dec,
                                                    RECT * pRect,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                                  NPT_METHOD_IDXGIDecodeSwapChain_GetTargetRect))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDecodeSwapChain_GetTargetRect reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                       NPT_METHOD_IDXGIDecodeSwapChain_GetTargetRect));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pRect)
            npt_decode_RECT(dec, (RECT *)pRect);
    }
}

static inline void
npt_submit_IDXGIDecodeSwapChain_GetTargetRect(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              RECT * pRect,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDecodeSwapChain_GetTargetRect(pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDecodeSwapChain_GetTargetRect_reply(pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDecodeSwapChain_GetTargetRect(enc, cmd_flags, object_id, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDecodeSwapChain_GetTargetRect(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_GetTargetRect(ring, 0, object_id, pRect, &submit);
}

static inline HRESULT
npt_call_IDXGIDecodeSwapChain_GetTargetRect(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_GetTargetRect(ring, NPT_CMD_FLAG_REPLY, object_id, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDecodeSwapChain_GetTargetRect_reply(dec, pRect, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDecodeSwapChain::GetDestSize
 */

struct npt_command_IDXGIDecodeSwapChain_GetDestSize {
    void *_self;
    UINT * pWidth;
    UINT * pHeight;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_GetDestSize(const UINT * pWidth,const UINT * pHeight)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pWidth (output only) */
/* skip pHeight (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIDecodeSwapChain_GetDestSize(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT * pWidth,
                                            UINT * pHeight)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDecodeSwapChain_GetDestSize(pWidth,pHeight);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_GetDestSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pWidth (output only) */
/* skip pHeight (output only) */
}

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_GetDestSize_reply(const UINT * pWidth,const UINT * pHeight)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIDecodeSwapChain_GetDestSize_reply(struct npt_cs_decoder *dec,
                                                  UINT * pWidth,
                                                  UINT * pHeight,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                                  NPT_METHOD_IDXGIDecodeSwapChain_GetDestSize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDecodeSwapChain_GetDestSize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                       NPT_METHOD_IDXGIDecodeSwapChain_GetDestSize));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pWidth)
            npt_decode_UINT(dec, (UINT *)pWidth);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pHeight)
            npt_decode_UINT(dec, (UINT *)pHeight);
    }
}

static inline void
npt_submit_IDXGIDecodeSwapChain_GetDestSize(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT * pWidth,
                                            UINT * pHeight,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDecodeSwapChain_GetDestSize(pWidth,pHeight);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDecodeSwapChain_GetDestSize_reply(pWidth,pHeight)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDecodeSwapChain_GetDestSize(enc, cmd_flags, object_id, pWidth, pHeight);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDecodeSwapChain_GetDestSize(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT * pWidth,
                                           UINT * pHeight)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_GetDestSize(ring, 0, object_id, pWidth, pHeight, &submit);
}

static inline HRESULT
npt_call_IDXGIDecodeSwapChain_GetDestSize(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          UINT * pWidth,
                                          UINT * pHeight)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_GetDestSize(ring, NPT_CMD_FLAG_REPLY, object_id, pWidth, pHeight, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDecodeSwapChain_GetDestSize_reply(dec, pWidth, pHeight, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDecodeSwapChain::SetColorSpace
 */

struct npt_command_IDXGIDecodeSwapChain_SetColorSpace {
    void *_self;
    DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ColorSpace;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_SetColorSpace(const DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS(&ColorSpace, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDecodeSwapChain_SetColorSpace(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDecodeSwapChain_SetColorSpace(ColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_SetColorSpace),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS(enc, &ColorSpace);
}

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_SetColorSpace_reply(const DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ColorSpace (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDecodeSwapChain_SetColorSpace_reply(struct npt_cs_decoder *dec,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                                  NPT_METHOD_IDXGIDecodeSwapChain_SetColorSpace))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDecodeSwapChain_SetColorSpace reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                       NPT_METHOD_IDXGIDecodeSwapChain_SetColorSpace));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDecodeSwapChain_SetColorSpace(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ColorSpace,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDecodeSwapChain_SetColorSpace(ColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDecodeSwapChain_SetColorSpace_reply(ColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDecodeSwapChain_SetColorSpace(enc, cmd_flags, object_id, ColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDecodeSwapChain_SetColorSpace(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_SetColorSpace(ring, 0, object_id, ColorSpace, &submit);
}

static inline HRESULT
npt_call_IDXGIDecodeSwapChain_SetColorSpace(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_SetColorSpace(ring, NPT_CMD_FLAG_REPLY, object_id, ColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDecodeSwapChain_SetColorSpace_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDecodeSwapChain::GetColorSpace
 */

struct npt_command_IDXGIDecodeSwapChain_GetColorSpace {
    void *_self;
    DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ret;
};

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_GetColorSpace(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDecodeSwapChain_GetColorSpace(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDecodeSwapChain_GetColorSpace();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_GetColorSpace),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIDecodeSwapChain_GetColorSpace_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGIDecodeSwapChain_GetColorSpace_reply(struct npt_cs_decoder *dec,
                                                    DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                                  NPT_METHOD_IDXGIDecodeSwapChain_GetColorSpace))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDecodeSwapChain_GetColorSpace reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                       NPT_METHOD_IDXGIDecodeSwapChain_GetColorSpace));
        if (ret) *ret = (DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS)0;
        return;
    }
    if (ret) *ret = (DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDecodeSwapChain_GetColorSpace(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDecodeSwapChain_GetColorSpace();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDecodeSwapChain_GetColorSpace_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDecodeSwapChain_GetColorSpace(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDecodeSwapChain_GetColorSpace(struct npt_ring *ring,
                                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_GetColorSpace(ring, 0, object_id, &submit);
}

static inline DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS
npt_call_IDXGIDecodeSwapChain_GetColorSpace(struct npt_ring *ring,
                                            npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDecodeSwapChain_GetColorSpace(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS _ret;
        npt_decode_IDXGIDecodeSwapChain_GetColorSpace_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS)0;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIDECODESWAPCHAIN_H */
