/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgiresource.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIRESOURCE_H
#define NPT_PROTOCOL_GUEST_IDXGIRESOURCE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIResource::GetSharedHandle
 */

struct npt_command_IDXGIResource_GetSharedHandle {
    void *_self;
    HANDLE * pSharedHandle;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIResource_GetSharedHandle(const HANDLE * pSharedHandle)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pSharedHandle (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIResource_GetSharedHandle(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         HANDLE * pSharedHandle)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIResource_GetSharedHandle(pSharedHandle);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                 NPT_METHOD_IDXGIResource_GetSharedHandle),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pSharedHandle (output only) */
}

static inline size_t
npt_sizeof_IDXGIResource_GetSharedHandle_reply(const HANDLE * pSharedHandle)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_decode_IDXGIResource_GetSharedHandle_reply(struct npt_cs_decoder *dec,
                                               HANDLE * pSharedHandle,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                                  NPT_METHOD_IDXGIResource_GetSharedHandle))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIResource_GetSharedHandle reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                       NPT_METHOD_IDXGIResource_GetSharedHandle));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        if (pSharedHandle)
            *pSharedHandle = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
}

static inline void
npt_submit_IDXGIResource_GetSharedHandle(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         HANDLE * pSharedHandle,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIResource_GetSharedHandle(pSharedHandle);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIResource_GetSharedHandle_reply(pSharedHandle)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIResource_GetSharedHandle(enc, cmd_flags, object_id, pSharedHandle);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIResource_GetSharedHandle(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        HANDLE * pSharedHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource_GetSharedHandle(ring, 0, object_id, pSharedHandle, &submit);
}

static inline HRESULT
npt_call_IDXGIResource_GetSharedHandle(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       HANDLE * pSharedHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource_GetSharedHandle(ring, NPT_CMD_FLAG_REPLY, object_id, pSharedHandle, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIResource_GetSharedHandle_reply(dec, pSharedHandle, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIResource::GetUsage
 */

struct npt_command_IDXGIResource_GetUsage {
    void *_self;
    DXGI_USAGE * pUsage;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIResource_GetUsage(const DXGI_USAGE * pUsage)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pUsage (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIResource_GetUsage(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_USAGE * pUsage)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIResource_GetUsage(pUsage);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                 NPT_METHOD_IDXGIResource_GetUsage),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pUsage (output only) */
}

static inline size_t
npt_sizeof_IDXGIResource_GetUsage_reply(const DXGI_USAGE * pUsage)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_USAGE(&(const DXGI_USAGE){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIResource_GetUsage_reply(struct npt_cs_decoder *dec,
                                        DXGI_USAGE * pUsage,
                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                                  NPT_METHOD_IDXGIResource_GetUsage))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIResource_GetUsage reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                       NPT_METHOD_IDXGIResource_GetUsage));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pUsage)
            npt_decode_DXGI_USAGE(dec, (DXGI_USAGE *)pUsage);
    }
}

static inline void
npt_submit_IDXGIResource_GetUsage(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_USAGE * pUsage,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIResource_GetUsage(pUsage);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIResource_GetUsage_reply(pUsage)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIResource_GetUsage(enc, cmd_flags, object_id, pUsage);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIResource_GetUsage(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 DXGI_USAGE * pUsage)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource_GetUsage(ring, 0, object_id, pUsage, &submit);
}

static inline HRESULT
npt_call_IDXGIResource_GetUsage(struct npt_ring *ring,
                                npt_object_id object_id,
                                DXGI_USAGE * pUsage)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource_GetUsage(ring, NPT_CMD_FLAG_REPLY, object_id, pUsage, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIResource_GetUsage_reply(dec, pUsage, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIResource::SetEvictionPriority
 */

struct npt_command_IDXGIResource_SetEvictionPriority {
    void *_self;
    UINT EvictionPriority;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIResource_SetEvictionPriority(const UINT EvictionPriority)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&EvictionPriority, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIResource_SetEvictionPriority(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT EvictionPriority)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIResource_SetEvictionPriority(EvictionPriority);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                 NPT_METHOD_IDXGIResource_SetEvictionPriority),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &EvictionPriority);
}

static inline size_t
npt_sizeof_IDXGIResource_SetEvictionPriority_reply(const UINT EvictionPriority)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip EvictionPriority (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIResource_SetEvictionPriority_reply(struct npt_cs_decoder *dec,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                                  NPT_METHOD_IDXGIResource_SetEvictionPriority))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIResource_SetEvictionPriority reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                       NPT_METHOD_IDXGIResource_SetEvictionPriority));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIResource_SetEvictionPriority(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT EvictionPriority,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIResource_SetEvictionPriority(EvictionPriority);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIResource_SetEvictionPriority_reply(EvictionPriority)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIResource_SetEvictionPriority(enc, cmd_flags, object_id, EvictionPriority);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIResource_SetEvictionPriority(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT EvictionPriority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource_SetEvictionPriority(ring, 0, object_id, EvictionPriority, &submit);
}

static inline HRESULT
npt_call_IDXGIResource_SetEvictionPriority(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT EvictionPriority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource_SetEvictionPriority(ring, NPT_CMD_FLAG_REPLY, object_id, EvictionPriority, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIResource_SetEvictionPriority_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIResource::GetEvictionPriority
 */

struct npt_command_IDXGIResource_GetEvictionPriority {
    void *_self;
    UINT * pEvictionPriority;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIResource_GetEvictionPriority(const UINT * pEvictionPriority)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pEvictionPriority (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIResource_GetEvictionPriority(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT * pEvictionPriority)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIResource_GetEvictionPriority(pEvictionPriority);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                 NPT_METHOD_IDXGIResource_GetEvictionPriority),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pEvictionPriority (output only) */
}

static inline size_t
npt_sizeof_IDXGIResource_GetEvictionPriority_reply(const UINT * pEvictionPriority)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIResource_GetEvictionPriority_reply(struct npt_cs_decoder *dec,
                                                   UINT * pEvictionPriority,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                                  NPT_METHOD_IDXGIResource_GetEvictionPriority))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIResource_GetEvictionPriority reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource,
                                       NPT_METHOD_IDXGIResource_GetEvictionPriority));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pEvictionPriority)
            npt_decode_UINT(dec, (UINT *)pEvictionPriority);
    }
}

static inline void
npt_submit_IDXGIResource_GetEvictionPriority(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT * pEvictionPriority,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIResource_GetEvictionPriority(pEvictionPriority);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIResource_GetEvictionPriority_reply(pEvictionPriority)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIResource_GetEvictionPriority(enc, cmd_flags, object_id, pEvictionPriority);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIResource_GetEvictionPriority(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT * pEvictionPriority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource_GetEvictionPriority(ring, 0, object_id, pEvictionPriority, &submit);
}

static inline HRESULT
npt_call_IDXGIResource_GetEvictionPriority(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT * pEvictionPriority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource_GetEvictionPriority(ring, NPT_CMD_FLAG_REPLY, object_id, pEvictionPriority, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIResource_GetEvictionPriority_reply(dec, pEvictionPriority, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIResource1::CreateSubresourceSurface
 */

struct npt_command_IDXGIResource1_CreateSubresourceSurface {
    void *_self;
    UINT index;
    IDXGISurface2 ** ppSurface;
    /* Shadow: the guest-allocated id for ppSurface arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSurface -> *ppSurface} in the
     * context object table. */
    uint64_t _guest_id_ppSurface;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIResource1_CreateSubresourceSurface(const UINT index,IDXGISurface2 ** ppSurface)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&index, max_mode);
cmd_size += sizeof(uint64_t);  /* ppSurface: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIResource1_CreateSubresourceSurface(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT index,
                                                   IDXGISurface2 ** ppSurface)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIResource1_CreateSubresourceSurface(index,ppSurface);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource1,
                                 NPT_METHOD_IDXGIResource1_CreateSubresourceSurface),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &index);
{
    uint64_t _gid_ppSurface = 0;
    if (ppSurface) {
        _gid_ppSurface = npt_com_allocate_next_id();
        *ppSurface = (IDXGISurface2 *)(uintptr_t)_gid_ppSurface;
    }
    npt_encode_uint64_t(enc, &_gid_ppSurface);
}
}

static inline size_t
npt_sizeof_IDXGIResource1_CreateSubresourceSurface_reply(const UINT index,IDXGISurface2 ** ppSurface)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip index (input only) */
/* skip ppSurface (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIResource1_CreateSubresourceSurface_reply(struct npt_cs_decoder *dec,
                                                         IDXGISurface2 ** ppSurface,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource1,
                                                  NPT_METHOD_IDXGIResource1_CreateSubresourceSurface))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIResource1_CreateSubresourceSurface reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource1,
                                       NPT_METHOD_IDXGIResource1_CreateSubresourceSurface));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSurface (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIResource1_CreateSubresourceSurface(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT index,
                                                   IDXGISurface2 ** ppSurface,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIResource1_CreateSubresourceSurface(index,ppSurface);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIResource1_CreateSubresourceSurface_reply(index,ppSurface)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIResource1_CreateSubresourceSurface(enc, cmd_flags, object_id, index, ppSurface);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIResource1_CreateSubresourceSurface(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT index,
                                                  IDXGISurface2 ** ppSurface)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource1_CreateSubresourceSurface(ring, 0, object_id, index, ppSurface, &submit);
}

static inline HRESULT
npt_call_IDXGIResource1_CreateSubresourceSurface(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT index,
                                                 IDXGISurface2 ** ppSurface)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource1_CreateSubresourceSurface(ring, NPT_CMD_FLAG_REPLY, object_id, index, ppSurface, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIResource1_CreateSubresourceSurface_reply(dec, ppSurface, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIResource1::CreateSharedHandle
 */

struct npt_command_IDXGIResource1_CreateSharedHandle {
    void *_self;
    const SECURITY_ATTRIBUTES * pAttributes;
    DWORD dwAccess;
    const WCHAR * lpName;
    HANDLE * pHandle;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIResource1_CreateSharedHandle(const SECURITY_ATTRIBUTES * pAttributes,const DWORD dwAccess,const WCHAR * lpName,const HANDLE * pHandle)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    /* pAttributes: non-serializable, skipped */
    cmd_size += npt_sizeof_DWORD(&dwAccess, max_mode);
    if (lpName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)lpName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(lpName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip pHandle (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIResource1_CreateSharedHandle(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const SECURITY_ATTRIBUTES * pAttributes,
                                             DWORD dwAccess,
                                             const WCHAR * lpName,
                                             HANDLE * pHandle)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIResource1_CreateSharedHandle(pAttributes,dwAccess,lpName,pHandle);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource1,
                                 NPT_METHOD_IDXGIResource1_CreateSharedHandle),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    /* pAttributes: non-serializable, skipped */
    npt_encode_DWORD(enc, &dwAccess);
    if (lpName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)lpName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, lpName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pHandle (output only) */
}

static inline size_t
npt_sizeof_IDXGIResource1_CreateSharedHandle_reply(const SECURITY_ATTRIBUTES * pAttributes,const DWORD dwAccess,const WCHAR * lpName,const HANDLE * pHandle)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAttributes (input only) */
/* skip dwAccess (input only) */
/* skip lpName (input only) */
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_decode_IDXGIResource1_CreateSharedHandle_reply(struct npt_cs_decoder *dec,
                                                   HANDLE * pHandle,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource1,
                                                  NPT_METHOD_IDXGIResource1_CreateSharedHandle))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIResource1_CreateSharedHandle reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIResource1,
                                       NPT_METHOD_IDXGIResource1_CreateSharedHandle));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        if (pHandle)
            *pHandle = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
}

static inline void
npt_submit_IDXGIResource1_CreateSharedHandle(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const SECURITY_ATTRIBUTES * pAttributes,
                                             DWORD dwAccess,
                                             const WCHAR * lpName,
                                             HANDLE * pHandle,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIResource1_CreateSharedHandle(pAttributes,dwAccess,lpName,pHandle);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIResource1_CreateSharedHandle_reply(pAttributes,dwAccess,lpName,pHandle)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIResource1_CreateSharedHandle(enc, cmd_flags, object_id, pAttributes, dwAccess, lpName, pHandle);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIResource1_CreateSharedHandle(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const SECURITY_ATTRIBUTES * pAttributes,
                                            DWORD dwAccess,
                                            const WCHAR * lpName,
                                            HANDLE * pHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource1_CreateSharedHandle(ring, 0, object_id, pAttributes, dwAccess, lpName, pHandle, &submit);
}

static inline HRESULT
npt_call_IDXGIResource1_CreateSharedHandle(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           const SECURITY_ATTRIBUTES * pAttributes,
                                           DWORD dwAccess,
                                           const WCHAR * lpName,
                                           HANDLE * pHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIResource1_CreateSharedHandle(ring, NPT_CMD_FLAG_REPLY, object_id, pAttributes, dwAccess, lpName, pHandle, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIResource1_CreateSharedHandle_reply(dec, pHandle, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIRESOURCE_H */
