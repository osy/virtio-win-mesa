/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgioutputduplication.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIOUTPUTDUPLICATION_H
#define NPT_PROTOCOL_GUEST_IDXGIOUTPUTDUPLICATION_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIOutputDuplication::GetDesc
 */

struct npt_command_IDXGIOutputDuplication_GetDesc {
    void *_self;
    DXGI_OUTDUPL_DESC * pDesc;
};

static inline size_t
npt_sizeof_IDXGIOutputDuplication_GetDesc(const DXGI_OUTDUPL_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutputDuplication_GetDesc(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_OUTDUPL_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutputDuplication_GetDesc(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                 NPT_METHOD_IDXGIOutputDuplication_GetDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutputDuplication_GetDesc_reply(const DXGI_OUTDUPL_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_OUTDUPL_DESC(&(const DXGI_OUTDUPL_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutputDuplication_GetDesc_reply(struct npt_cs_decoder *dec,
                                                DXGI_OUTDUPL_DESC * pDesc)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                                  NPT_METHOD_IDXGIOutputDuplication_GetDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutputDuplication_GetDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                       NPT_METHOD_IDXGIOutputDuplication_GetDesc));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_OUTDUPL_DESC(dec, (DXGI_OUTDUPL_DESC *)pDesc);
    }
}

static inline void
npt_submit_IDXGIOutputDuplication_GetDesc(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_OUTDUPL_DESC * pDesc,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutputDuplication_GetDesc(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutputDuplication_GetDesc_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutputDuplication_GetDesc(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutputDuplication_GetDesc(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         DXGI_OUTDUPL_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_GetDesc(ring, 0, object_id, pDesc, &submit);
}

static inline void
npt_call_IDXGIOutputDuplication_GetDesc(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        DXGI_OUTDUPL_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_GetDesc(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_IDXGIOutputDuplication_GetDesc_reply(dec, pDesc);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * IDXGIOutputDuplication::AcquireNextFrame
 */

struct npt_command_IDXGIOutputDuplication_AcquireNextFrame {
    void *_self;
    UINT TimeoutInMilliseconds;
    DXGI_OUTDUPL_FRAME_INFO * pFrameInfo;
    IDXGIResource ** ppDesktopResource;
    /* Shadow: the guest-allocated id for ppDesktopResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDesktopResource -> *ppDesktopResource} in the
     * context object table. */
    uint64_t _guest_id_ppDesktopResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutputDuplication_AcquireNextFrame(const UINT TimeoutInMilliseconds,const DXGI_OUTDUPL_FRAME_INFO * pFrameInfo,IDXGIResource ** ppDesktopResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&TimeoutInMilliseconds, max_mode);
/* skip pFrameInfo (output only) */
cmd_size += sizeof(uint64_t);  /* ppDesktopResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutputDuplication_AcquireNextFrame(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT TimeoutInMilliseconds,
                                                   DXGI_OUTDUPL_FRAME_INFO * pFrameInfo,
                                                   IDXGIResource ** ppDesktopResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutputDuplication_AcquireNextFrame(TimeoutInMilliseconds,pFrameInfo,ppDesktopResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                 NPT_METHOD_IDXGIOutputDuplication_AcquireNextFrame),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &TimeoutInMilliseconds);
/* skip pFrameInfo (output only) */
{
    uint64_t _gid_ppDesktopResource = 0;
    if (ppDesktopResource) {
        _gid_ppDesktopResource = npt_com_allocate_next_id();
        *ppDesktopResource = (IDXGIResource *)(uintptr_t)_gid_ppDesktopResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppDesktopResource);
}
}

static inline size_t
npt_sizeof_IDXGIOutputDuplication_AcquireNextFrame_reply(const UINT TimeoutInMilliseconds,const DXGI_OUTDUPL_FRAME_INFO * pFrameInfo,IDXGIResource ** ppDesktopResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip TimeoutInMilliseconds (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_OUTDUPL_FRAME_INFO(&(const DXGI_OUTDUPL_FRAME_INFO){0}, 1);
/* skip ppDesktopResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutputDuplication_AcquireNextFrame_reply(struct npt_cs_decoder *dec,
                                                         DXGI_OUTDUPL_FRAME_INFO * pFrameInfo,
                                                         IDXGIResource ** ppDesktopResource,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                                  NPT_METHOD_IDXGIOutputDuplication_AcquireNextFrame))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutputDuplication_AcquireNextFrame reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                       NPT_METHOD_IDXGIOutputDuplication_AcquireNextFrame));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pFrameInfo)
            npt_decode_DXGI_OUTDUPL_FRAME_INFO(dec, (DXGI_OUTDUPL_FRAME_INFO *)pFrameInfo);
    }
/* skip ppDesktopResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIOutputDuplication_AcquireNextFrame(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT TimeoutInMilliseconds,
                                                   DXGI_OUTDUPL_FRAME_INFO * pFrameInfo,
                                                   IDXGIResource ** ppDesktopResource,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutputDuplication_AcquireNextFrame(TimeoutInMilliseconds,pFrameInfo,ppDesktopResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutputDuplication_AcquireNextFrame_reply(TimeoutInMilliseconds,pFrameInfo,ppDesktopResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutputDuplication_AcquireNextFrame(enc, cmd_flags, object_id, TimeoutInMilliseconds, pFrameInfo, ppDesktopResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutputDuplication_AcquireNextFrame(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT TimeoutInMilliseconds,
                                                  DXGI_OUTDUPL_FRAME_INFO * pFrameInfo,
                                                  IDXGIResource ** ppDesktopResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_AcquireNextFrame(ring, 0, object_id, TimeoutInMilliseconds, pFrameInfo, ppDesktopResource, &submit);
}

static inline HRESULT
npt_call_IDXGIOutputDuplication_AcquireNextFrame(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT TimeoutInMilliseconds,
                                                 DXGI_OUTDUPL_FRAME_INFO * pFrameInfo,
                                                 IDXGIResource ** ppDesktopResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_AcquireNextFrame(ring, NPT_CMD_FLAG_REPLY, object_id, TimeoutInMilliseconds, pFrameInfo, ppDesktopResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutputDuplication_AcquireNextFrame_reply(dec, pFrameInfo, ppDesktopResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutputDuplication::GetFrameDirtyRects
 */

struct npt_command_IDXGIOutputDuplication_GetFrameDirtyRects {
    void *_self;
    UINT DirtyRectsBufferSize;
    void * pDirtyRectsBuffer;
    UINT * pDirtyRectsBufferSizeRequired;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutputDuplication_GetFrameDirtyRects(const UINT DirtyRectsBufferSize,const void * pDirtyRectsBuffer,const UINT * pDirtyRectsBufferSizeRequired)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&DirtyRectsBufferSize, max_mode);
/* skip pDirtyRectsBuffer (output only) */
/* skip pDirtyRectsBufferSizeRequired (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutputDuplication_GetFrameDirtyRects(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     UINT DirtyRectsBufferSize,
                                                     void * pDirtyRectsBuffer,
                                                     UINT * pDirtyRectsBufferSizeRequired)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutputDuplication_GetFrameDirtyRects(DirtyRectsBufferSize,pDirtyRectsBuffer,pDirtyRectsBufferSizeRequired);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                 NPT_METHOD_IDXGIOutputDuplication_GetFrameDirtyRects),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &DirtyRectsBufferSize);
/* skip pDirtyRectsBuffer (output only) */
/* skip pDirtyRectsBufferSizeRequired (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutputDuplication_GetFrameDirtyRects_reply(const UINT DirtyRectsBufferSize,const void * pDirtyRectsBuffer,const UINT * pDirtyRectsBufferSizeRequired)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip DirtyRectsBufferSize (input only) */
    if (pDirtyRectsBuffer) {
        cmd_size += npt_sizeof_array_count(DirtyRectsBufferSize);
        cmd_size += npt_sizeof_blob_array(pDirtyRectsBuffer, DirtyRectsBufferSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutputDuplication_GetFrameDirtyRects_reply(struct npt_cs_decoder *dec,
                                                           void * pDirtyRectsBuffer,
                                                           UINT * pDirtyRectsBufferSizeRequired,
                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                                  NPT_METHOD_IDXGIOutputDuplication_GetFrameDirtyRects))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutputDuplication_GetFrameDirtyRects reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                       NPT_METHOD_IDXGIOutputDuplication_GetFrameDirtyRects));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pDirtyRectsBuffer)
            npt_decode_blob_array(dec, (void *)pDirtyRectsBuffer, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pDirtyRectsBufferSizeRequired)
            npt_decode_UINT(dec, (UINT *)pDirtyRectsBufferSizeRequired);
    }
}

static inline void
npt_submit_IDXGIOutputDuplication_GetFrameDirtyRects(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     UINT DirtyRectsBufferSize,
                                                     void * pDirtyRectsBuffer,
                                                     UINT * pDirtyRectsBufferSizeRequired,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutputDuplication_GetFrameDirtyRects(DirtyRectsBufferSize,pDirtyRectsBuffer,pDirtyRectsBufferSizeRequired);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutputDuplication_GetFrameDirtyRects_reply(DirtyRectsBufferSize,pDirtyRectsBuffer,pDirtyRectsBufferSizeRequired)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutputDuplication_GetFrameDirtyRects(enc, cmd_flags, object_id, DirtyRectsBufferSize, pDirtyRectsBuffer, pDirtyRectsBufferSizeRequired);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutputDuplication_GetFrameDirtyRects(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT DirtyRectsBufferSize,
                                                    void * pDirtyRectsBuffer,
                                                    UINT * pDirtyRectsBufferSizeRequired)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_GetFrameDirtyRects(ring, 0, object_id, DirtyRectsBufferSize, pDirtyRectsBuffer, pDirtyRectsBufferSizeRequired, &submit);
}

static inline HRESULT
npt_call_IDXGIOutputDuplication_GetFrameDirtyRects(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT DirtyRectsBufferSize,
                                                   void * pDirtyRectsBuffer,
                                                   UINT * pDirtyRectsBufferSizeRequired)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_GetFrameDirtyRects(ring, NPT_CMD_FLAG_REPLY, object_id, DirtyRectsBufferSize, pDirtyRectsBuffer, pDirtyRectsBufferSizeRequired, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutputDuplication_GetFrameDirtyRects_reply(dec, pDirtyRectsBuffer, pDirtyRectsBufferSizeRequired, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutputDuplication::GetFrameMoveRects
 */

struct npt_command_IDXGIOutputDuplication_GetFrameMoveRects {
    void *_self;
    UINT MoveRectsBufferSize;
    void * pMoveRectBuffer;
    UINT * pMoveRectsBufferSizeRequired;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutputDuplication_GetFrameMoveRects(const UINT MoveRectsBufferSize,const void * pMoveRectBuffer,const UINT * pMoveRectsBufferSizeRequired)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&MoveRectsBufferSize, max_mode);
/* skip pMoveRectBuffer (output only) */
/* skip pMoveRectsBufferSizeRequired (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutputDuplication_GetFrameMoveRects(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT MoveRectsBufferSize,
                                                    void * pMoveRectBuffer,
                                                    UINT * pMoveRectsBufferSizeRequired)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutputDuplication_GetFrameMoveRects(MoveRectsBufferSize,pMoveRectBuffer,pMoveRectsBufferSizeRequired);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                 NPT_METHOD_IDXGIOutputDuplication_GetFrameMoveRects),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &MoveRectsBufferSize);
/* skip pMoveRectBuffer (output only) */
/* skip pMoveRectsBufferSizeRequired (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutputDuplication_GetFrameMoveRects_reply(const UINT MoveRectsBufferSize,const void * pMoveRectBuffer,const UINT * pMoveRectsBufferSizeRequired)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip MoveRectsBufferSize (input only) */
    if (pMoveRectBuffer) {
        cmd_size += npt_sizeof_array_count(MoveRectsBufferSize);
        cmd_size += npt_sizeof_blob_array(pMoveRectBuffer, MoveRectsBufferSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutputDuplication_GetFrameMoveRects_reply(struct npt_cs_decoder *dec,
                                                          void * pMoveRectBuffer,
                                                          UINT * pMoveRectsBufferSizeRequired,
                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                                  NPT_METHOD_IDXGIOutputDuplication_GetFrameMoveRects))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutputDuplication_GetFrameMoveRects reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                       NPT_METHOD_IDXGIOutputDuplication_GetFrameMoveRects));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pMoveRectBuffer)
            npt_decode_blob_array(dec, (void *)pMoveRectBuffer, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pMoveRectsBufferSizeRequired)
            npt_decode_UINT(dec, (UINT *)pMoveRectsBufferSizeRequired);
    }
}

static inline void
npt_submit_IDXGIOutputDuplication_GetFrameMoveRects(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT MoveRectsBufferSize,
                                                    void * pMoveRectBuffer,
                                                    UINT * pMoveRectsBufferSizeRequired,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutputDuplication_GetFrameMoveRects(MoveRectsBufferSize,pMoveRectBuffer,pMoveRectsBufferSizeRequired);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutputDuplication_GetFrameMoveRects_reply(MoveRectsBufferSize,pMoveRectBuffer,pMoveRectsBufferSizeRequired)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutputDuplication_GetFrameMoveRects(enc, cmd_flags, object_id, MoveRectsBufferSize, pMoveRectBuffer, pMoveRectsBufferSizeRequired);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutputDuplication_GetFrameMoveRects(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT MoveRectsBufferSize,
                                                   void * pMoveRectBuffer,
                                                   UINT * pMoveRectsBufferSizeRequired)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_GetFrameMoveRects(ring, 0, object_id, MoveRectsBufferSize, pMoveRectBuffer, pMoveRectsBufferSizeRequired, &submit);
}

static inline HRESULT
npt_call_IDXGIOutputDuplication_GetFrameMoveRects(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT MoveRectsBufferSize,
                                                  void * pMoveRectBuffer,
                                                  UINT * pMoveRectsBufferSizeRequired)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_GetFrameMoveRects(ring, NPT_CMD_FLAG_REPLY, object_id, MoveRectsBufferSize, pMoveRectBuffer, pMoveRectsBufferSizeRequired, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutputDuplication_GetFrameMoveRects_reply(dec, pMoveRectBuffer, pMoveRectsBufferSizeRequired, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutputDuplication::GetFramePointerShape
 */

struct npt_command_IDXGIOutputDuplication_GetFramePointerShape {
    void *_self;
    UINT PointerShapeBufferSize;
    void * pPointerShapeBuffer;
    UINT * pPointerShapeBufferSizeRequired;
    DXGI_OUTDUPL_POINTER_SHAPE_INFO * pPointerShapeInfo;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutputDuplication_GetFramePointerShape(const UINT PointerShapeBufferSize,const void * pPointerShapeBuffer,const UINT * pPointerShapeBufferSizeRequired,const DXGI_OUTDUPL_POINTER_SHAPE_INFO * pPointerShapeInfo)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&PointerShapeBufferSize, max_mode);
/* skip pPointerShapeBuffer (output only) */
/* skip pPointerShapeBufferSizeRequired (output only) */
/* skip pPointerShapeInfo (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutputDuplication_GetFramePointerShape(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       UINT PointerShapeBufferSize,
                                                       void * pPointerShapeBuffer,
                                                       UINT * pPointerShapeBufferSizeRequired,
                                                       DXGI_OUTDUPL_POINTER_SHAPE_INFO * pPointerShapeInfo)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutputDuplication_GetFramePointerShape(PointerShapeBufferSize,pPointerShapeBuffer,pPointerShapeBufferSizeRequired,pPointerShapeInfo);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                 NPT_METHOD_IDXGIOutputDuplication_GetFramePointerShape),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &PointerShapeBufferSize);
/* skip pPointerShapeBuffer (output only) */
/* skip pPointerShapeBufferSizeRequired (output only) */
/* skip pPointerShapeInfo (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutputDuplication_GetFramePointerShape_reply(const UINT PointerShapeBufferSize,const void * pPointerShapeBuffer,const UINT * pPointerShapeBufferSizeRequired,const DXGI_OUTDUPL_POINTER_SHAPE_INFO * pPointerShapeInfo)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip PointerShapeBufferSize (input only) */
    if (pPointerShapeBuffer) {
        cmd_size += npt_sizeof_array_count(PointerShapeBufferSize);
        cmd_size += npt_sizeof_blob_array(pPointerShapeBuffer, PointerShapeBufferSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_OUTDUPL_POINTER_SHAPE_INFO(&(const DXGI_OUTDUPL_POINTER_SHAPE_INFO){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutputDuplication_GetFramePointerShape_reply(struct npt_cs_decoder *dec,
                                                             void * pPointerShapeBuffer,
                                                             UINT * pPointerShapeBufferSizeRequired,
                                                             DXGI_OUTDUPL_POINTER_SHAPE_INFO * pPointerShapeInfo,
                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                                  NPT_METHOD_IDXGIOutputDuplication_GetFramePointerShape))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutputDuplication_GetFramePointerShape reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                       NPT_METHOD_IDXGIOutputDuplication_GetFramePointerShape));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pPointerShapeBuffer)
            npt_decode_blob_array(dec, (void *)pPointerShapeBuffer, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pPointerShapeBufferSizeRequired)
            npt_decode_UINT(dec, (UINT *)pPointerShapeBufferSizeRequired);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pPointerShapeInfo)
            npt_decode_DXGI_OUTDUPL_POINTER_SHAPE_INFO(dec, (DXGI_OUTDUPL_POINTER_SHAPE_INFO *)pPointerShapeInfo);
    }
}

static inline void
npt_submit_IDXGIOutputDuplication_GetFramePointerShape(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       UINT PointerShapeBufferSize,
                                                       void * pPointerShapeBuffer,
                                                       UINT * pPointerShapeBufferSizeRequired,
                                                       DXGI_OUTDUPL_POINTER_SHAPE_INFO * pPointerShapeInfo,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutputDuplication_GetFramePointerShape(PointerShapeBufferSize,pPointerShapeBuffer,pPointerShapeBufferSizeRequired,pPointerShapeInfo);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutputDuplication_GetFramePointerShape_reply(PointerShapeBufferSize,pPointerShapeBuffer,pPointerShapeBufferSizeRequired,pPointerShapeInfo)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutputDuplication_GetFramePointerShape(enc, cmd_flags, object_id, PointerShapeBufferSize, pPointerShapeBuffer, pPointerShapeBufferSizeRequired, pPointerShapeInfo);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutputDuplication_GetFramePointerShape(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      UINT PointerShapeBufferSize,
                                                      void * pPointerShapeBuffer,
                                                      UINT * pPointerShapeBufferSizeRequired,
                                                      DXGI_OUTDUPL_POINTER_SHAPE_INFO * pPointerShapeInfo)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_GetFramePointerShape(ring, 0, object_id, PointerShapeBufferSize, pPointerShapeBuffer, pPointerShapeBufferSizeRequired, pPointerShapeInfo, &submit);
}

static inline HRESULT
npt_call_IDXGIOutputDuplication_GetFramePointerShape(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT PointerShapeBufferSize,
                                                     void * pPointerShapeBuffer,
                                                     UINT * pPointerShapeBufferSizeRequired,
                                                     DXGI_OUTDUPL_POINTER_SHAPE_INFO * pPointerShapeInfo)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_GetFramePointerShape(ring, NPT_CMD_FLAG_REPLY, object_id, PointerShapeBufferSize, pPointerShapeBuffer, pPointerShapeBufferSizeRequired, pPointerShapeInfo, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutputDuplication_GetFramePointerShape_reply(dec, pPointerShapeBuffer, pPointerShapeBufferSizeRequired, pPointerShapeInfo, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutputDuplication::MapDesktopSurface
 */

struct npt_command_IDXGIOutputDuplication_MapDesktopSurface {
    void *_self;
    DXGI_MAPPED_RECT * pLockedRect;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutputDuplication_MapDesktopSurface(const DXGI_MAPPED_RECT * pLockedRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pLockedRect (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutputDuplication_MapDesktopSurface(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    DXGI_MAPPED_RECT * pLockedRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutputDuplication_MapDesktopSurface(pLockedRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                 NPT_METHOD_IDXGIOutputDuplication_MapDesktopSurface),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pLockedRect (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutputDuplication_MapDesktopSurface_reply(const DXGI_MAPPED_RECT * pLockedRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_MAPPED_RECT(&(const DXGI_MAPPED_RECT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutputDuplication_MapDesktopSurface_reply(struct npt_cs_decoder *dec,
                                                          DXGI_MAPPED_RECT * pLockedRect,
                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                                  NPT_METHOD_IDXGIOutputDuplication_MapDesktopSurface))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutputDuplication_MapDesktopSurface reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                       NPT_METHOD_IDXGIOutputDuplication_MapDesktopSurface));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pLockedRect)
            npt_decode_DXGI_MAPPED_RECT(dec, (DXGI_MAPPED_RECT *)pLockedRect);
    }
}

static inline void
npt_submit_IDXGIOutputDuplication_MapDesktopSurface(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    DXGI_MAPPED_RECT * pLockedRect,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutputDuplication_MapDesktopSurface(pLockedRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutputDuplication_MapDesktopSurface_reply(pLockedRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutputDuplication_MapDesktopSurface(enc, cmd_flags, object_id, pLockedRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutputDuplication_MapDesktopSurface(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   DXGI_MAPPED_RECT * pLockedRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_MapDesktopSurface(ring, 0, object_id, pLockedRect, &submit);
}

static inline HRESULT
npt_call_IDXGIOutputDuplication_MapDesktopSurface(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  DXGI_MAPPED_RECT * pLockedRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_MapDesktopSurface(ring, NPT_CMD_FLAG_REPLY, object_id, pLockedRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutputDuplication_MapDesktopSurface_reply(dec, pLockedRect, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutputDuplication::UnMapDesktopSurface
 */

struct npt_command_IDXGIOutputDuplication_UnMapDesktopSurface {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutputDuplication_UnMapDesktopSurface(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutputDuplication_UnMapDesktopSurface(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutputDuplication_UnMapDesktopSurface();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                 NPT_METHOD_IDXGIOutputDuplication_UnMapDesktopSurface),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIOutputDuplication_UnMapDesktopSurface_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutputDuplication_UnMapDesktopSurface_reply(struct npt_cs_decoder *dec,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                                  NPT_METHOD_IDXGIOutputDuplication_UnMapDesktopSurface))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutputDuplication_UnMapDesktopSurface reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                       NPT_METHOD_IDXGIOutputDuplication_UnMapDesktopSurface));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIOutputDuplication_UnMapDesktopSurface(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutputDuplication_UnMapDesktopSurface();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutputDuplication_UnMapDesktopSurface_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutputDuplication_UnMapDesktopSurface(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutputDuplication_UnMapDesktopSurface(struct npt_ring *ring,
                                                     npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_UnMapDesktopSurface(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_IDXGIOutputDuplication_UnMapDesktopSurface(struct npt_ring *ring,
                                                    npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_UnMapDesktopSurface(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutputDuplication_UnMapDesktopSurface_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutputDuplication::ReleaseFrame
 */

struct npt_command_IDXGIOutputDuplication_ReleaseFrame {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutputDuplication_ReleaseFrame(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutputDuplication_ReleaseFrame(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutputDuplication_ReleaseFrame();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                 NPT_METHOD_IDXGIOutputDuplication_ReleaseFrame),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIOutputDuplication_ReleaseFrame_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutputDuplication_ReleaseFrame_reply(struct npt_cs_decoder *dec,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                                  NPT_METHOD_IDXGIOutputDuplication_ReleaseFrame))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutputDuplication_ReleaseFrame reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutputDuplication,
                                       NPT_METHOD_IDXGIOutputDuplication_ReleaseFrame));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIOutputDuplication_ReleaseFrame(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutputDuplication_ReleaseFrame();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutputDuplication_ReleaseFrame_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutputDuplication_ReleaseFrame(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutputDuplication_ReleaseFrame(struct npt_ring *ring,
                                              npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_ReleaseFrame(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_IDXGIOutputDuplication_ReleaseFrame(struct npt_ring *ring,
                                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutputDuplication_ReleaseFrame(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutputDuplication_ReleaseFrame_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIOUTPUTDUPLICATION_H */
