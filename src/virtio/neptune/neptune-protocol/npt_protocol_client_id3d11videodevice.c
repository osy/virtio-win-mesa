/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for id3d11videodevice: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_id3d11videodevice.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* ID3D11VideoDevice                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d11videodevice_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D11VideoDevice,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d11videodevice_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CreateVideoDecoder(void *self, const D3D11_VIDEO_DECODER_DESC * pVideoDesc, const D3D11_VIDEO_DECODER_CONFIG * pConfig, ID3D11VideoDecoder ** ppDecoder)
{
    ID3D11VideoDecoder *_raw_ppDecoder = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11VideoDecoder **_orig_ppDecoder = ppDecoder;
    if (ppDecoder)
        ppDecoder = (ID3D11VideoDecoder **)&_raw_ppDecoder;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11VideoDevice_CreateVideoDecoder(npt_com_self_ring(self),npt_com_self_id(self),pVideoDesc,pConfig,ppDecoder);
    } else {
        npt_async_ID3D11VideoDevice_CreateVideoDecoder(npt_com_self_ring(self),npt_com_self_id(self),pVideoDesc,pConfig,ppDecoder);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppDecoder) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppDecoder)
            *_orig_ppDecoder = (ID3D11VideoDecoder *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11VideoDecoder,
                (uint64_t)(uintptr_t)_raw_ppDecoder,
                (struct npt_com_base *)self);
        else
            *_orig_ppDecoder = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CreateVideoProcessor(void *self, ID3D11VideoProcessorEnumerator * pEnum, UINT RateConversionIndex, ID3D11VideoProcessor ** ppVideoProcessor)
{
    ID3D11VideoProcessor *_raw_ppVideoProcessor = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11VideoProcessor **_orig_ppVideoProcessor = ppVideoProcessor;
    if (ppVideoProcessor)
        ppVideoProcessor = (ID3D11VideoProcessor **)&_raw_ppVideoProcessor;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11VideoDevice_CreateVideoProcessor(npt_com_self_ring(self),npt_com_self_id(self),pEnum,RateConversionIndex,ppVideoProcessor);
    } else {
        npt_async_ID3D11VideoDevice_CreateVideoProcessor(npt_com_self_ring(self),npt_com_self_id(self),pEnum,RateConversionIndex,ppVideoProcessor);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppVideoProcessor) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppVideoProcessor)
            *_orig_ppVideoProcessor = (ID3D11VideoProcessor *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11VideoProcessor,
                (uint64_t)(uintptr_t)_raw_ppVideoProcessor,
                (struct npt_com_base *)self);
        else
            *_orig_ppVideoProcessor = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CreateAuthenticatedChannel(void *self, D3D11_AUTHENTICATED_CHANNEL_TYPE ChannelType, ID3D11AuthenticatedChannel ** ppAuthenticatedChannel)
{
    ID3D11AuthenticatedChannel *_raw_ppAuthenticatedChannel = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11AuthenticatedChannel **_orig_ppAuthenticatedChannel = ppAuthenticatedChannel;
    if (ppAuthenticatedChannel)
        ppAuthenticatedChannel = (ID3D11AuthenticatedChannel **)&_raw_ppAuthenticatedChannel;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11VideoDevice_CreateAuthenticatedChannel(npt_com_self_ring(self),npt_com_self_id(self),ChannelType,ppAuthenticatedChannel);
    } else {
        npt_async_ID3D11VideoDevice_CreateAuthenticatedChannel(npt_com_self_ring(self),npt_com_self_id(self),ChannelType,ppAuthenticatedChannel);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppAuthenticatedChannel) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppAuthenticatedChannel)
            *_orig_ppAuthenticatedChannel = (ID3D11AuthenticatedChannel *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11AuthenticatedChannel,
                (uint64_t)(uintptr_t)_raw_ppAuthenticatedChannel,
                (struct npt_com_base *)self);
        else
            *_orig_ppAuthenticatedChannel = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CreateCryptoSession(void *self, const GUID * pCryptoType, const GUID * pDecoderProfile, const GUID * pKeyExchangeType, ID3D11CryptoSession ** ppCryptoSession)
{
    ID3D11CryptoSession *_raw_ppCryptoSession = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11CryptoSession **_orig_ppCryptoSession = ppCryptoSession;
    if (ppCryptoSession)
        ppCryptoSession = (ID3D11CryptoSession **)&_raw_ppCryptoSession;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11VideoDevice_CreateCryptoSession(npt_com_self_ring(self),npt_com_self_id(self),pCryptoType,pDecoderProfile,pKeyExchangeType,ppCryptoSession);
    } else {
        npt_async_ID3D11VideoDevice_CreateCryptoSession(npt_com_self_ring(self),npt_com_self_id(self),pCryptoType,pDecoderProfile,pKeyExchangeType,ppCryptoSession);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppCryptoSession) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppCryptoSession)
            *_orig_ppCryptoSession = (ID3D11CryptoSession *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11CryptoSession,
                (uint64_t)(uintptr_t)_raw_ppCryptoSession,
                (struct npt_com_base *)self);
        else
            *_orig_ppCryptoSession = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CreateVideoDecoderOutputView(void *self, ID3D11Resource * pResource, const D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC * pDesc, ID3D11VideoDecoderOutputView ** ppVDOVView)
{
    ID3D11VideoDecoderOutputView *_raw_ppVDOVView = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11VideoDecoderOutputView **_orig_ppVDOVView = ppVDOVView;
    if (ppVDOVView)
        ppVDOVView = (ID3D11VideoDecoderOutputView **)&_raw_ppVDOVView;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11VideoDevice_CreateVideoDecoderOutputView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppVDOVView);
    } else {
        npt_async_ID3D11VideoDevice_CreateVideoDecoderOutputView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppVDOVView);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppVDOVView) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppVDOVView)
            *_orig_ppVDOVView = (ID3D11VideoDecoderOutputView *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11VideoDecoderOutputView,
                (uint64_t)(uintptr_t)_raw_ppVDOVView,
                (struct npt_com_base *)self);
        else
            *_orig_ppVDOVView = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CreateVideoProcessorInputView(void *self, ID3D11Resource * pResource, ID3D11VideoProcessorEnumerator * pEnum, const D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC * pDesc, ID3D11VideoProcessorInputView ** ppVPIView)
{
    ID3D11VideoProcessorInputView *_raw_ppVPIView = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11VideoProcessorInputView **_orig_ppVPIView = ppVPIView;
    if (ppVPIView)
        ppVPIView = (ID3D11VideoProcessorInputView **)&_raw_ppVPIView;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11VideoDevice_CreateVideoProcessorInputView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pEnum,pDesc,ppVPIView);
    } else {
        npt_async_ID3D11VideoDevice_CreateVideoProcessorInputView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pEnum,pDesc,ppVPIView);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppVPIView) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppVPIView)
            *_orig_ppVPIView = (ID3D11VideoProcessorInputView *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11VideoProcessorInputView,
                (uint64_t)(uintptr_t)_raw_ppVPIView,
                (struct npt_com_base *)self);
        else
            *_orig_ppVPIView = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CreateVideoProcessorOutputView(void *self, ID3D11Resource * pResource, ID3D11VideoProcessorEnumerator * pEnum, const D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC * pDesc, ID3D11VideoProcessorOutputView ** ppVPOView)
{
    ID3D11VideoProcessorOutputView *_raw_ppVPOView = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11VideoProcessorOutputView **_orig_ppVPOView = ppVPOView;
    if (ppVPOView)
        ppVPOView = (ID3D11VideoProcessorOutputView **)&_raw_ppVPOView;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11VideoDevice_CreateVideoProcessorOutputView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pEnum,pDesc,ppVPOView);
    } else {
        npt_async_ID3D11VideoDevice_CreateVideoProcessorOutputView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pEnum,pDesc,ppVPOView);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppVPOView) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppVPOView)
            *_orig_ppVPOView = (ID3D11VideoProcessorOutputView *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11VideoProcessorOutputView,
                (uint64_t)(uintptr_t)_raw_ppVPOView,
                (struct npt_com_base *)self);
        else
            *_orig_ppVPOView = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CreateVideoProcessorEnumerator(void *self, const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pDesc, ID3D11VideoProcessorEnumerator ** ppEnum)
{
    ID3D11VideoProcessorEnumerator *_raw_ppEnum = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11VideoProcessorEnumerator **_orig_ppEnum = ppEnum;
    if (ppEnum)
        ppEnum = (ID3D11VideoProcessorEnumerator **)&_raw_ppEnum;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11VideoDevice_CreateVideoProcessorEnumerator(npt_com_self_ring(self),npt_com_self_id(self),pDesc,ppEnum);
    } else {
        npt_async_ID3D11VideoDevice_CreateVideoProcessorEnumerator(npt_com_self_ring(self),npt_com_self_id(self),pDesc,ppEnum);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppEnum) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppEnum)
            *_orig_ppEnum = (ID3D11VideoProcessorEnumerator *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11VideoProcessorEnumerator,
                (uint64_t)(uintptr_t)_raw_ppEnum,
                (struct npt_com_base *)self);
        else
            *_orig_ppEnum = NULL;
    }
    return _ret;
}

UINT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_GetVideoDecoderProfileCount(void *self)
{
    UINT _ret = npt_call_ID3D11VideoDevice_GetVideoDecoderProfileCount(npt_com_self_ring(self),npt_com_self_id(self));
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_GetVideoDecoderProfile(void *self, UINT Index, GUID * pDecoderProfile)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice_GetVideoDecoderProfile(npt_com_self_ring(self),npt_com_self_id(self),Index,pDecoderProfile);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CheckVideoDecoderFormat(void *self, const GUID * pDecoderProfile, DXGI_FORMAT Format, BOOL * pSupported)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice_CheckVideoDecoderFormat(npt_com_self_ring(self),npt_com_self_id(self),pDecoderProfile,Format,pSupported);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_GetVideoDecoderConfigCount(void *self, const D3D11_VIDEO_DECODER_DESC * pDesc, UINT * pCount)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice_GetVideoDecoderConfigCount(npt_com_self_ring(self),npt_com_self_id(self),pDesc,pCount);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_GetVideoDecoderConfig(void *self, const D3D11_VIDEO_DECODER_DESC * pDesc, UINT Index, D3D11_VIDEO_DECODER_CONFIG * pConfig)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice_GetVideoDecoderConfig(npt_com_self_ring(self),npt_com_self_id(self),pDesc,Index,pConfig);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_GetContentProtectionCaps(void *self, const GUID * pCryptoType, const GUID * pDecoderProfile, D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice_GetContentProtectionCaps(npt_com_self_ring(self),npt_com_self_id(self),pCryptoType,pDecoderProfile,pCaps);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_CheckCryptoKeyExchange(void *self, const GUID * pCryptoType, const GUID * pDecoderProfile, UINT Index, GUID * pKeyExchangeType)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice_CheckCryptoKeyExchange(npt_com_self_ring(self),npt_com_self_id(self),pCryptoType,pDecoderProfile,Index,pKeyExchangeType);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_SetPrivateData(void *self, const GUID * guid, UINT DataSize, const void * pData)
{
    npt_async_ID3D11VideoDevice_SetPrivateData(npt_com_self_ring(self),npt_com_self_id(self),guid,DataSize,pData);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice_default_SetPrivateDataInterface(void *self, const GUID * guid, const IUnknown * pData)
{
    npt_async_ID3D11VideoDevice_SetPrivateDataInterface(npt_com_self_ring(self),npt_com_self_id(self),guid,pData);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d11videodevice_fill_default_vtbl(struct npt_id3d11videodevice_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d11videodevice_default_QueryInterface;
    dst->AddRef         = npt_id3d11videodevice_default_AddRef;
    dst->Release        = npt_id3d11videodevice_default_Release;
    dst->CreateVideoDecoder = npt_id3d11videodevice_default_CreateVideoDecoder;
    dst->CreateVideoProcessor = npt_id3d11videodevice_default_CreateVideoProcessor;
    dst->CreateAuthenticatedChannel = npt_id3d11videodevice_default_CreateAuthenticatedChannel;
    dst->CreateCryptoSession = npt_id3d11videodevice_default_CreateCryptoSession;
    dst->CreateVideoDecoderOutputView = npt_id3d11videodevice_default_CreateVideoDecoderOutputView;
    dst->CreateVideoProcessorInputView = npt_id3d11videodevice_default_CreateVideoProcessorInputView;
    dst->CreateVideoProcessorOutputView = npt_id3d11videodevice_default_CreateVideoProcessorOutputView;
    dst->CreateVideoProcessorEnumerator = npt_id3d11videodevice_default_CreateVideoProcessorEnumerator;
    dst->GetVideoDecoderProfileCount = npt_id3d11videodevice_default_GetVideoDecoderProfileCount;
    dst->GetVideoDecoderProfile = npt_id3d11videodevice_default_GetVideoDecoderProfile;
    dst->CheckVideoDecoderFormat = npt_id3d11videodevice_default_CheckVideoDecoderFormat;
    dst->GetVideoDecoderConfigCount = npt_id3d11videodevice_default_GetVideoDecoderConfigCount;
    dst->GetVideoDecoderConfig = npt_id3d11videodevice_default_GetVideoDecoderConfig;
    dst->GetContentProtectionCaps = npt_id3d11videodevice_default_GetContentProtectionCaps;
    dst->CheckCryptoKeyExchange = npt_id3d11videodevice_default_CheckCryptoKeyExchange;
    dst->SetPrivateData = npt_id3d11videodevice_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d11videodevice_default_SetPrivateDataInterface;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d11videodevice_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d11videodevice_client_vtbl npt_id3d11videodevice_default_vtbl_storage;

void
npt_id3d11videodevice_install_default_vtbl(void)
{
    npt_id3d11videodevice_fill_default_vtbl(&npt_id3d11videodevice_default_vtbl_storage);
}

void *
npt_id3d11videodevice_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d11videodevice_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D11VideoDevice1                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d11videodevice1_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D11VideoDevice,
    &NPT_IID_ID3D11VideoDevice1,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice1_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d11videodevice1_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11videodevice1_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11videodevice1_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice1_default_GetCryptoSessionPrivateDataSize(void *self, const GUID * pCryptoType, const GUID * pDecoderProfile, const GUID * pKeyExchangeType, UINT * pPrivateInputSize, UINT * pPrivateOutputSize)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(npt_com_self_ring(self),npt_com_self_id(self),pCryptoType,pDecoderProfile,pKeyExchangeType,pPrivateInputSize,pPrivateOutputSize);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice1_default_GetVideoDecoderCaps(void *self, const GUID * pDecoderProfile, UINT SampleWidth, UINT SampleHeight, const DXGI_RATIONAL * pFrameRate, UINT BitRate, const GUID * pCryptoType, UINT * pDecoderCaps)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice1_GetVideoDecoderCaps(npt_com_self_ring(self),npt_com_self_id(self),pDecoderProfile,SampleWidth,SampleHeight,pFrameRate,BitRate,pCryptoType,pDecoderCaps);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice1_default_CheckVideoDecoderDownsampling(void *self, const D3D11_VIDEO_DECODER_DESC * pInputDesc, DXGI_COLOR_SPACE_TYPE InputColorSpace, const D3D11_VIDEO_DECODER_CONFIG * pInputConfig, const DXGI_RATIONAL * pFrameRate, const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc, BOOL * pSupported, BOOL * pRealTimeHint)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(npt_com_self_ring(self),npt_com_self_id(self),pInputDesc,InputColorSpace,pInputConfig,pFrameRate,pOutputDesc,pSupported,pRealTimeHint);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice1_default_RecommendVideoDecoderDownsampleParameters(void *self, const D3D11_VIDEO_DECODER_DESC * pInputDesc, DXGI_COLOR_SPACE_TYPE InputColorSpace, const D3D11_VIDEO_DECODER_CONFIG * pInputConfig, const DXGI_RATIONAL * pFrameRate, D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(npt_com_self_ring(self),npt_com_self_id(self),pInputDesc,InputColorSpace,pInputConfig,pFrameRate,pRecommendedOutputDesc);
    return _ret;
}


void
npt_id3d11videodevice1_fill_default_vtbl(struct npt_id3d11videodevice1_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d11videodevice1_default_QueryInterface;
    dst->AddRef         = npt_id3d11videodevice1_default_AddRef;
    dst->Release        = npt_id3d11videodevice1_default_Release;
    dst->CreateVideoDecoder = npt_id3d11videodevice_default_CreateVideoDecoder;
    dst->CreateVideoProcessor = npt_id3d11videodevice_default_CreateVideoProcessor;
    dst->CreateAuthenticatedChannel = npt_id3d11videodevice_default_CreateAuthenticatedChannel;
    dst->CreateCryptoSession = npt_id3d11videodevice_default_CreateCryptoSession;
    dst->CreateVideoDecoderOutputView = npt_id3d11videodevice_default_CreateVideoDecoderOutputView;
    dst->CreateVideoProcessorInputView = npt_id3d11videodevice_default_CreateVideoProcessorInputView;
    dst->CreateVideoProcessorOutputView = npt_id3d11videodevice_default_CreateVideoProcessorOutputView;
    dst->CreateVideoProcessorEnumerator = npt_id3d11videodevice_default_CreateVideoProcessorEnumerator;
    dst->GetVideoDecoderProfileCount = npt_id3d11videodevice_default_GetVideoDecoderProfileCount;
    dst->GetVideoDecoderProfile = npt_id3d11videodevice_default_GetVideoDecoderProfile;
    dst->CheckVideoDecoderFormat = npt_id3d11videodevice_default_CheckVideoDecoderFormat;
    dst->GetVideoDecoderConfigCount = npt_id3d11videodevice_default_GetVideoDecoderConfigCount;
    dst->GetVideoDecoderConfig = npt_id3d11videodevice_default_GetVideoDecoderConfig;
    dst->GetContentProtectionCaps = npt_id3d11videodevice_default_GetContentProtectionCaps;
    dst->CheckCryptoKeyExchange = npt_id3d11videodevice_default_CheckCryptoKeyExchange;
    dst->SetPrivateData = npt_id3d11videodevice_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d11videodevice_default_SetPrivateDataInterface;
    dst->GetCryptoSessionPrivateDataSize = npt_id3d11videodevice1_default_GetCryptoSessionPrivateDataSize;
    dst->GetVideoDecoderCaps = npt_id3d11videodevice1_default_GetVideoDecoderCaps;
    dst->CheckVideoDecoderDownsampling = npt_id3d11videodevice1_default_CheckVideoDecoderDownsampling;
    dst->RecommendVideoDecoderDownsampleParameters = npt_id3d11videodevice1_default_RecommendVideoDecoderDownsampleParameters;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d11videodevice1_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d11videodevice1_client_vtbl npt_id3d11videodevice1_default_vtbl_storage;

void
npt_id3d11videodevice1_install_default_vtbl(void)
{
    npt_id3d11videodevice1_fill_default_vtbl(&npt_id3d11videodevice1_default_vtbl_storage);
}

void *
npt_id3d11videodevice1_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d11videodevice1_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D11VideoDevice2                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d11videodevice2_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D11VideoDevice,
    &NPT_IID_ID3D11VideoDevice1,
    &NPT_IID_ID3D11VideoDevice2,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice2_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d11videodevice2_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11videodevice2_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11videodevice2_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice2_default_CheckFeatureSupport(void *self, D3D11_FEATURE_VIDEO Feature, void * pFeatureSupportData, UINT FeatureSupportDataSize)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice2_CheckFeatureSupport(npt_com_self_ring(self),npt_com_self_id(self),Feature,pFeatureSupportData,FeatureSupportDataSize);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11videodevice2_default_NegotiateCryptoSessionKeyExchangeMT(void *self, ID3D11CryptoSession * pCryptoSession, D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS flags, UINT DataSize, void * pData)
{
    HRESULT _ret = npt_call_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(npt_com_self_ring(self),npt_com_self_id(self),pCryptoSession,flags,DataSize,pData);
    return _ret;
}


void
npt_id3d11videodevice2_fill_default_vtbl(struct npt_id3d11videodevice2_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d11videodevice2_default_QueryInterface;
    dst->AddRef         = npt_id3d11videodevice2_default_AddRef;
    dst->Release        = npt_id3d11videodevice2_default_Release;
    dst->CreateVideoDecoder = npt_id3d11videodevice_default_CreateVideoDecoder;
    dst->CreateVideoProcessor = npt_id3d11videodevice_default_CreateVideoProcessor;
    dst->CreateAuthenticatedChannel = npt_id3d11videodevice_default_CreateAuthenticatedChannel;
    dst->CreateCryptoSession = npt_id3d11videodevice_default_CreateCryptoSession;
    dst->CreateVideoDecoderOutputView = npt_id3d11videodevice_default_CreateVideoDecoderOutputView;
    dst->CreateVideoProcessorInputView = npt_id3d11videodevice_default_CreateVideoProcessorInputView;
    dst->CreateVideoProcessorOutputView = npt_id3d11videodevice_default_CreateVideoProcessorOutputView;
    dst->CreateVideoProcessorEnumerator = npt_id3d11videodevice_default_CreateVideoProcessorEnumerator;
    dst->GetVideoDecoderProfileCount = npt_id3d11videodevice_default_GetVideoDecoderProfileCount;
    dst->GetVideoDecoderProfile = npt_id3d11videodevice_default_GetVideoDecoderProfile;
    dst->CheckVideoDecoderFormat = npt_id3d11videodevice_default_CheckVideoDecoderFormat;
    dst->GetVideoDecoderConfigCount = npt_id3d11videodevice_default_GetVideoDecoderConfigCount;
    dst->GetVideoDecoderConfig = npt_id3d11videodevice_default_GetVideoDecoderConfig;
    dst->GetContentProtectionCaps = npt_id3d11videodevice_default_GetContentProtectionCaps;
    dst->CheckCryptoKeyExchange = npt_id3d11videodevice_default_CheckCryptoKeyExchange;
    dst->SetPrivateData = npt_id3d11videodevice_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d11videodevice_default_SetPrivateDataInterface;
    dst->GetCryptoSessionPrivateDataSize = npt_id3d11videodevice1_default_GetCryptoSessionPrivateDataSize;
    dst->GetVideoDecoderCaps = npt_id3d11videodevice1_default_GetVideoDecoderCaps;
    dst->CheckVideoDecoderDownsampling = npt_id3d11videodevice1_default_CheckVideoDecoderDownsampling;
    dst->RecommendVideoDecoderDownsampleParameters = npt_id3d11videodevice1_default_RecommendVideoDecoderDownsampleParameters;
    dst->CheckFeatureSupport = npt_id3d11videodevice2_default_CheckFeatureSupport;
    dst->NegotiateCryptoSessionKeyExchangeMT = npt_id3d11videodevice2_default_NegotiateCryptoSessionKeyExchangeMT;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d11videodevice2_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d11videodevice2_client_vtbl npt_id3d11videodevice2_default_vtbl_storage;

void
npt_id3d11videodevice2_install_default_vtbl(void)
{
    npt_id3d11videodevice2_fill_default_vtbl(&npt_id3d11videodevice2_default_vtbl_storage);
}

void *
npt_id3d11videodevice2_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d11videodevice2_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
