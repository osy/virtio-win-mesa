/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgidevice.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIDEVICE_H
#define NPT_PROTOCOL_GUEST_IDXGIDEVICE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIDevice::GetAdapter
 */

struct npt_command_IDXGIDevice_GetAdapter {
    void *_self;
    IDXGIAdapter ** pAdapter;
    /* Shadow: the guest-allocated id for pAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_pAdapter -> *pAdapter} in the
     * context object table. */
    uint64_t _guest_id_pAdapter;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice_GetAdapter(IDXGIAdapter ** pAdapter)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* pAdapter: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice_GetAdapter(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  IDXGIAdapter ** pAdapter)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice_GetAdapter(pAdapter);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_GetAdapter),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_pAdapter = 0;
    if (pAdapter) {
        _gid_pAdapter = npt_com_allocate_next_id();
        *pAdapter = (IDXGIAdapter *)(uintptr_t)_gid_pAdapter;
    }
    npt_encode_uint64_t(enc, &_gid_pAdapter);
}
}

static inline size_t
npt_sizeof_IDXGIDevice_GetAdapter_reply(IDXGIAdapter ** pAdapter)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAdapter (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice_GetAdapter_reply(struct npt_cs_decoder *dec,
                                        IDXGIAdapter ** pAdapter,
                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                                  NPT_METHOD_IDXGIDevice_GetAdapter))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice_GetAdapter reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                       NPT_METHOD_IDXGIDevice_GetAdapter));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip pAdapter (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIDevice_GetAdapter(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  IDXGIAdapter ** pAdapter,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice_GetAdapter(pAdapter);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice_GetAdapter_reply(pAdapter)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice_GetAdapter(enc, cmd_flags, object_id, pAdapter);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice_GetAdapter(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 IDXGIAdapter ** pAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_GetAdapter(ring, 0, object_id, pAdapter, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice_GetAdapter(struct npt_ring *ring,
                                npt_object_id object_id,
                                IDXGIAdapter ** pAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_GetAdapter(ring, NPT_CMD_FLAG_REPLY, object_id, pAdapter, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice_GetAdapter_reply(dec, pAdapter, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice::CreateSurface
 */

struct npt_command_IDXGIDevice_CreateSurface {
    void *_self;
    const DXGI_SURFACE_DESC * pDesc;
    UINT NumSurfaces;
    DXGI_USAGE Usage;
    const DXGI_SHARED_RESOURCE * pSharedResource;
    IDXGISurface ** ppSurface;
    /* Shadow: per-element guest-allocated ids for ppSurface.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSurface;
    uint32_t _guest_id_count_ppSurface;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice_CreateSurface(const DXGI_SURFACE_DESC * pDesc,const UINT NumSurfaces,const DXGI_USAGE Usage,const DXGI_SHARED_RESOURCE * pSharedResource,IDXGISurface ** ppSurface)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_DXGI_SURFACE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSurfaces, max_mode);
    cmd_size += npt_sizeof_DXGI_USAGE(&Usage, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSharedResource);
    if (pSharedResource)
        cmd_size += npt_sizeof_DXGI_SHARED_RESOURCE(pSharedResource, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumSurfaces) * sizeof(uint64_t);  /* ppSurface: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice_CreateSurface(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const DXGI_SURFACE_DESC * pDesc,
                                     UINT NumSurfaces,
                                     DXGI_USAGE Usage,
                                     const DXGI_SHARED_RESOURCE * pSharedResource,
                                     IDXGISurface ** ppSurface)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice_CreateSurface(pDesc,NumSurfaces,Usage,pSharedResource,ppSurface);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_CreateSurface),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_DXGI_SURFACE_DESC(enc, pDesc);
    npt_encode_UINT(enc, &NumSurfaces);
    npt_encode_DXGI_USAGE(enc, &Usage);
    if (npt_encode_simple_pointer(enc, pSharedResource))
        npt_encode_DXGI_SHARED_RESOURCE(enc, pSharedResource);
if (ppSurface) {
    const uint64_t _cnt_ppSurface = (uint64_t)(NumSurfaces);
    npt_encode_array_count(enc, _cnt_ppSurface);
    for (uint64_t _i = 0; _i < _cnt_ppSurface; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppSurface[_i] = (IDXGISurface *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_IDXGIDevice_CreateSurface_reply(const DXGI_SURFACE_DESC * pDesc,const UINT NumSurfaces,const DXGI_USAGE Usage,const DXGI_SHARED_RESOURCE * pSharedResource,IDXGISurface ** ppSurface)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip NumSurfaces (input only) */
/* skip Usage (input only) */
/* skip pSharedResource (input only) */
/* skip ppSurface (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice_CreateSurface_reply(struct npt_cs_decoder *dec,
                                           IDXGISurface ** ppSurface,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                                  NPT_METHOD_IDXGIDevice_CreateSurface))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice_CreateSurface reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                       NPT_METHOD_IDXGIDevice_CreateSurface));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSurface (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIDevice_CreateSurface(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const DXGI_SURFACE_DESC * pDesc,
                                     UINT NumSurfaces,
                                     DXGI_USAGE Usage,
                                     const DXGI_SHARED_RESOURCE * pSharedResource,
                                     IDXGISurface ** ppSurface,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice_CreateSurface(pDesc,NumSurfaces,Usage,pSharedResource,ppSurface);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice_CreateSurface_reply(pDesc,NumSurfaces,Usage,pSharedResource,ppSurface)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice_CreateSurface(enc, cmd_flags, object_id, pDesc, NumSurfaces, Usage, pSharedResource, ppSurface);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice_CreateSurface(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const DXGI_SURFACE_DESC * pDesc,
                                    UINT NumSurfaces,
                                    DXGI_USAGE Usage,
                                    const DXGI_SHARED_RESOURCE * pSharedResource,
                                    IDXGISurface ** ppSurface)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_CreateSurface(ring, 0, object_id, pDesc, NumSurfaces, Usage, pSharedResource, ppSurface, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice_CreateSurface(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   const DXGI_SURFACE_DESC * pDesc,
                                   UINT NumSurfaces,
                                   DXGI_USAGE Usage,
                                   const DXGI_SHARED_RESOURCE * pSharedResource,
                                   IDXGISurface ** ppSurface)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_CreateSurface(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, NumSurfaces, Usage, pSharedResource, ppSurface, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice_CreateSurface_reply(dec, ppSurface, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice::QueryResourceResidency
 */

struct npt_command_IDXGIDevice_QueryResourceResidency {
    void *_self;
    IUnknown ** ppResources;
    DXGI_RESIDENCY * pResidencyStatus;
    UINT NumResources;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice_QueryResourceResidency(IUnknown ** ppResources,const DXGI_RESIDENCY * pResidencyStatus,const UINT NumResources)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_array_count(NumResources);
    cmd_size += sizeof(uint64_t) * NumResources;
/* skip pResidencyStatus (output only) */
    cmd_size += npt_sizeof_UINT(&NumResources, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice_QueryResourceResidency(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              IUnknown ** ppResources,
                                              DXGI_RESIDENCY * pResidencyStatus,
                                              UINT NumResources)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice_QueryResourceResidency(ppResources,pResidencyStatus,NumResources);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_QueryResourceResidency),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (ppResources) {
        npt_encode_array_count(enc, NumResources);
        for (uint32_t _i = 0; _i < (uint32_t)NumResources; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppResources[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pResidencyStatus (output only) */
    npt_encode_UINT(enc, &NumResources);
}

static inline size_t
npt_sizeof_IDXGIDevice_QueryResourceResidency_reply(IUnknown ** ppResources,const DXGI_RESIDENCY * pResidencyStatus,const UINT NumResources)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppResources (input only) */
    cmd_size += npt_sizeof_array_count(pResidencyStatus ? NumResources : 0);
    if (pResidencyStatus)
        cmd_size += npt_sizeof_DXGI_RESIDENCY_array(pResidencyStatus, NumResources);
/* skip NumResources (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice_QueryResourceResidency_reply(struct npt_cs_decoder *dec,
                                                    DXGI_RESIDENCY * pResidencyStatus,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                                  NPT_METHOD_IDXGIDevice_QueryResourceResidency))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice_QueryResourceResidency reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                       NPT_METHOD_IDXGIDevice_QueryResourceResidency));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pResidencyStatus)
            npt_decode_DXGI_RESIDENCY_array(dec, (DXGI_RESIDENCY *)pResidencyStatus, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_IDXGIDevice_QueryResourceResidency(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              IUnknown ** ppResources,
                                              DXGI_RESIDENCY * pResidencyStatus,
                                              UINT NumResources,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice_QueryResourceResidency(ppResources,pResidencyStatus,NumResources);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice_QueryResourceResidency_reply(ppResources,pResidencyStatus,NumResources)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice_QueryResourceResidency(enc, cmd_flags, object_id, ppResources, pResidencyStatus, NumResources);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice_QueryResourceResidency(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             IUnknown ** ppResources,
                                             DXGI_RESIDENCY * pResidencyStatus,
                                             UINT NumResources)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_QueryResourceResidency(ring, 0, object_id, ppResources, pResidencyStatus, NumResources, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice_QueryResourceResidency(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            IUnknown ** ppResources,
                                            DXGI_RESIDENCY * pResidencyStatus,
                                            UINT NumResources)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_QueryResourceResidency(ring, NPT_CMD_FLAG_REPLY, object_id, ppResources, pResidencyStatus, NumResources, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice_QueryResourceResidency_reply(dec, pResidencyStatus, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice::SetGPUThreadPriority
 */

struct npt_command_IDXGIDevice_SetGPUThreadPriority {
    void *_self;
    INT Priority;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice_SetGPUThreadPriority(const INT Priority)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_INT(&Priority, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice_SetGPUThreadPriority(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            INT Priority)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice_SetGPUThreadPriority(Priority);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_SetGPUThreadPriority),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_INT(enc, &Priority);
}

static inline size_t
npt_sizeof_IDXGIDevice_SetGPUThreadPriority_reply(const INT Priority)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Priority (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice_SetGPUThreadPriority_reply(struct npt_cs_decoder *dec,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                                  NPT_METHOD_IDXGIDevice_SetGPUThreadPriority))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice_SetGPUThreadPriority reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                       NPT_METHOD_IDXGIDevice_SetGPUThreadPriority));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDevice_SetGPUThreadPriority(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            INT Priority,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice_SetGPUThreadPriority(Priority);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice_SetGPUThreadPriority_reply(Priority)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice_SetGPUThreadPriority(enc, cmd_flags, object_id, Priority);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice_SetGPUThreadPriority(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           INT Priority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_SetGPUThreadPriority(ring, 0, object_id, Priority, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice_SetGPUThreadPriority(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          INT Priority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_SetGPUThreadPriority(ring, NPT_CMD_FLAG_REPLY, object_id, Priority, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice_SetGPUThreadPriority_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice::GetGPUThreadPriority
 */

struct npt_command_IDXGIDevice_GetGPUThreadPriority {
    void *_self;
    INT * pPriority;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice_GetGPUThreadPriority(const INT * pPriority)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pPriority (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice_GetGPUThreadPriority(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            INT * pPriority)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice_GetGPUThreadPriority(pPriority);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_GetGPUThreadPriority),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pPriority (output only) */
}

static inline size_t
npt_sizeof_IDXGIDevice_GetGPUThreadPriority_reply(const INT * pPriority)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_INT(&(const INT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice_GetGPUThreadPriority_reply(struct npt_cs_decoder *dec,
                                                  INT * pPriority,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                                  NPT_METHOD_IDXGIDevice_GetGPUThreadPriority))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice_GetGPUThreadPriority reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                       NPT_METHOD_IDXGIDevice_GetGPUThreadPriority));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pPriority)
            npt_decode_INT(dec, (INT *)pPriority);
    }
}

static inline void
npt_submit_IDXGIDevice_GetGPUThreadPriority(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            INT * pPriority,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice_GetGPUThreadPriority(pPriority);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice_GetGPUThreadPriority_reply(pPriority)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice_GetGPUThreadPriority(enc, cmd_flags, object_id, pPriority);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice_GetGPUThreadPriority(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           INT * pPriority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_GetGPUThreadPriority(ring, 0, object_id, pPriority, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice_GetGPUThreadPriority(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          INT * pPriority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice_GetGPUThreadPriority(ring, NPT_CMD_FLAG_REPLY, object_id, pPriority, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice_GetGPUThreadPriority_reply(dec, pPriority, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice1::SetMaximumFrameLatency
 */

struct npt_command_IDXGIDevice1_SetMaximumFrameLatency {
    void *_self;
    UINT MaxLatency;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice1_SetMaximumFrameLatency(const UINT MaxLatency)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&MaxLatency, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice1_SetMaximumFrameLatency(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               UINT MaxLatency)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice1_SetMaximumFrameLatency(MaxLatency);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice1,
                                 NPT_METHOD_IDXGIDevice1_SetMaximumFrameLatency),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &MaxLatency);
}

static inline size_t
npt_sizeof_IDXGIDevice1_SetMaximumFrameLatency_reply(const UINT MaxLatency)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip MaxLatency (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice1_SetMaximumFrameLatency_reply(struct npt_cs_decoder *dec,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice1,
                                                  NPT_METHOD_IDXGIDevice1_SetMaximumFrameLatency))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice1_SetMaximumFrameLatency reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice1,
                                       NPT_METHOD_IDXGIDevice1_SetMaximumFrameLatency));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDevice1_SetMaximumFrameLatency(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               UINT MaxLatency,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice1_SetMaximumFrameLatency(MaxLatency);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice1_SetMaximumFrameLatency_reply(MaxLatency)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice1_SetMaximumFrameLatency(enc, cmd_flags, object_id, MaxLatency);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice1_SetMaximumFrameLatency(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT MaxLatency)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice1_SetMaximumFrameLatency(ring, 0, object_id, MaxLatency, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice1_SetMaximumFrameLatency(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT MaxLatency)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice1_SetMaximumFrameLatency(ring, NPT_CMD_FLAG_REPLY, object_id, MaxLatency, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice1_SetMaximumFrameLatency_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice1::GetMaximumFrameLatency
 */

struct npt_command_IDXGIDevice1_GetMaximumFrameLatency {
    void *_self;
    UINT * pMaxLatency;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice1_GetMaximumFrameLatency(const UINT * pMaxLatency)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pMaxLatency (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice1_GetMaximumFrameLatency(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               UINT * pMaxLatency)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice1_GetMaximumFrameLatency(pMaxLatency);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice1,
                                 NPT_METHOD_IDXGIDevice1_GetMaximumFrameLatency),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pMaxLatency (output only) */
}

static inline size_t
npt_sizeof_IDXGIDevice1_GetMaximumFrameLatency_reply(const UINT * pMaxLatency)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice1_GetMaximumFrameLatency_reply(struct npt_cs_decoder *dec,
                                                     UINT * pMaxLatency,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice1,
                                                  NPT_METHOD_IDXGIDevice1_GetMaximumFrameLatency))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice1_GetMaximumFrameLatency reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice1,
                                       NPT_METHOD_IDXGIDevice1_GetMaximumFrameLatency));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pMaxLatency)
            npt_decode_UINT(dec, (UINT *)pMaxLatency);
    }
}

static inline void
npt_submit_IDXGIDevice1_GetMaximumFrameLatency(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               UINT * pMaxLatency,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice1_GetMaximumFrameLatency(pMaxLatency);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice1_GetMaximumFrameLatency_reply(pMaxLatency)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice1_GetMaximumFrameLatency(enc, cmd_flags, object_id, pMaxLatency);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice1_GetMaximumFrameLatency(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT * pMaxLatency)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice1_GetMaximumFrameLatency(ring, 0, object_id, pMaxLatency, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice1_GetMaximumFrameLatency(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT * pMaxLatency)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice1_GetMaximumFrameLatency(ring, NPT_CMD_FLAG_REPLY, object_id, pMaxLatency, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice1_GetMaximumFrameLatency_reply(dec, pMaxLatency, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice2::OfferResources
 */

struct npt_command_IDXGIDevice2_OfferResources {
    void *_self;
    UINT NumResources;
    IDXGIResource ** ppResources;
    DXGI_OFFER_RESOURCE_PRIORITY Priority;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice2_OfferResources(const UINT NumResources,IDXGIResource ** ppResources,const DXGI_OFFER_RESOURCE_PRIORITY Priority)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumResources, max_mode);
    cmd_size += npt_sizeof_array_count(NumResources);
    cmd_size += sizeof(uint64_t) * NumResources;
    cmd_size += npt_sizeof_DXGI_OFFER_RESOURCE_PRIORITY(&Priority, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice2_OfferResources(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       UINT NumResources,
                                       IDXGIResource ** ppResources,
                                       DXGI_OFFER_RESOURCE_PRIORITY Priority)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice2_OfferResources(NumResources,ppResources,Priority);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                 NPT_METHOD_IDXGIDevice2_OfferResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumResources);
    if (ppResources) {
        npt_encode_array_count(enc, NumResources);
        for (uint32_t _i = 0; _i < (uint32_t)NumResources; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppResources[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_DXGI_OFFER_RESOURCE_PRIORITY(enc, &Priority);
}

static inline size_t
npt_sizeof_IDXGIDevice2_OfferResources_reply(const UINT NumResources,IDXGIResource ** ppResources,const DXGI_OFFER_RESOURCE_PRIORITY Priority)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumResources (input only) */
/* skip ppResources (input only) */
/* skip Priority (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice2_OfferResources_reply(struct npt_cs_decoder *dec,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                                  NPT_METHOD_IDXGIDevice2_OfferResources))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice2_OfferResources reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                       NPT_METHOD_IDXGIDevice2_OfferResources));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDevice2_OfferResources(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       UINT NumResources,
                                       IDXGIResource ** ppResources,
                                       DXGI_OFFER_RESOURCE_PRIORITY Priority,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice2_OfferResources(NumResources,ppResources,Priority);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice2_OfferResources_reply(NumResources,ppResources,Priority)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice2_OfferResources(enc, cmd_flags, object_id, NumResources, ppResources, Priority);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice2_OfferResources(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      UINT NumResources,
                                      IDXGIResource ** ppResources,
                                      DXGI_OFFER_RESOURCE_PRIORITY Priority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice2_OfferResources(ring, 0, object_id, NumResources, ppResources, Priority, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice2_OfferResources(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     UINT NumResources,
                                     IDXGIResource ** ppResources,
                                     DXGI_OFFER_RESOURCE_PRIORITY Priority)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice2_OfferResources(ring, NPT_CMD_FLAG_REPLY, object_id, NumResources, ppResources, Priority, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice2_OfferResources_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice2::ReclaimResources
 */

struct npt_command_IDXGIDevice2_ReclaimResources {
    void *_self;
    UINT NumResources;
    IDXGIResource ** ppResources;
    BOOL * pDiscarded;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice2_ReclaimResources(const UINT NumResources,IDXGIResource ** ppResources,const BOOL * pDiscarded)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumResources, max_mode);
    cmd_size += npt_sizeof_array_count(NumResources);
    cmd_size += sizeof(uint64_t) * NumResources;
/* skip pDiscarded (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice2_ReclaimResources(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         UINT NumResources,
                                         IDXGIResource ** ppResources,
                                         BOOL * pDiscarded)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice2_ReclaimResources(NumResources,ppResources,pDiscarded);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                 NPT_METHOD_IDXGIDevice2_ReclaimResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumResources);
    if (ppResources) {
        npt_encode_array_count(enc, NumResources);
        for (uint32_t _i = 0; _i < (uint32_t)NumResources; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppResources[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pDiscarded (output only) */
}

static inline size_t
npt_sizeof_IDXGIDevice2_ReclaimResources_reply(const UINT NumResources,IDXGIResource ** ppResources,const BOOL * pDiscarded)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumResources (input only) */
/* skip ppResources (input only) */
    cmd_size += npt_sizeof_array_count(pDiscarded ? NumResources : 0);
    if (pDiscarded)
        cmd_size += npt_sizeof_BOOL_array(pDiscarded, NumResources);
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice2_ReclaimResources_reply(struct npt_cs_decoder *dec,
                                               BOOL * pDiscarded,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                                  NPT_METHOD_IDXGIDevice2_ReclaimResources))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice2_ReclaimResources reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                       NPT_METHOD_IDXGIDevice2_ReclaimResources));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pDiscarded)
            npt_decode_BOOL_array(dec, (BOOL *)pDiscarded, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_IDXGIDevice2_ReclaimResources(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         UINT NumResources,
                                         IDXGIResource ** ppResources,
                                         BOOL * pDiscarded,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice2_ReclaimResources(NumResources,ppResources,pDiscarded);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice2_ReclaimResources_reply(NumResources,ppResources,pDiscarded)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice2_ReclaimResources(enc, cmd_flags, object_id, NumResources, ppResources, pDiscarded);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice2_ReclaimResources(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        UINT NumResources,
                                        IDXGIResource ** ppResources,
                                        BOOL * pDiscarded)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice2_ReclaimResources(ring, 0, object_id, NumResources, ppResources, pDiscarded, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice2_ReclaimResources(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       UINT NumResources,
                                       IDXGIResource ** ppResources,
                                       BOOL * pDiscarded)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice2_ReclaimResources(ring, NPT_CMD_FLAG_REPLY, object_id, NumResources, ppResources, pDiscarded, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice2_ReclaimResources_reply(dec, pDiscarded, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice2::EnqueueSetEvent
 */

struct npt_command_IDXGIDevice2_EnqueueSetEvent {
    void *_self;
    HANDLE hEvent;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice2_EnqueueSetEvent(const HANDLE hEvent)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice2_EnqueueSetEvent(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        HANDLE hEvent)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice2_EnqueueSetEvent(hEvent);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                 NPT_METHOD_IDXGIDevice2_EnqueueSetEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
}

static inline size_t
npt_sizeof_IDXGIDevice2_EnqueueSetEvent_reply(const HANDLE hEvent)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hEvent (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice2_EnqueueSetEvent_reply(struct npt_cs_decoder *dec,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                                  NPT_METHOD_IDXGIDevice2_EnqueueSetEvent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice2_EnqueueSetEvent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                       NPT_METHOD_IDXGIDevice2_EnqueueSetEvent));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDevice2_EnqueueSetEvent(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        HANDLE hEvent,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice2_EnqueueSetEvent(hEvent);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice2_EnqueueSetEvent_reply(hEvent)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice2_EnqueueSetEvent(enc, cmd_flags, object_id, hEvent);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice2_EnqueueSetEvent(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       HANDLE hEvent)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice2_EnqueueSetEvent(ring, 0, object_id, hEvent, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice2_EnqueueSetEvent(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      HANDLE hEvent)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice2_EnqueueSetEvent(ring, NPT_CMD_FLAG_REPLY, object_id, hEvent, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice2_EnqueueSetEvent_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice3::Trim
 */

struct npt_command_IDXGIDevice3_Trim {
    void *_self;
};

static inline size_t
npt_sizeof_IDXGIDevice3_Trim(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice3_Trim(struct npt_cs_encoder *enc,
                             uint32_t cmd_flags,
                             npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice3_Trim();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice3,
                                 NPT_METHOD_IDXGIDevice3_Trim),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIDevice3_Trim_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_IDXGIDevice3_Trim(struct npt_ring *ring,
                             uint32_t cmd_flags,
                             npt_object_id object_id,
                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice3_Trim();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice3_Trim_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice3_Trim(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice3_Trim(struct npt_ring *ring,
                            npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice3_Trim(ring, 0, object_id, &submit);
}

static inline void
npt_call_IDXGIDevice3_Trim(struct npt_ring *ring,
                           npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice3_Trim(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * IDXGIDevice4::OfferResources1
 */

struct npt_command_IDXGIDevice4_OfferResources1 {
    void *_self;
    UINT NumResources;
    IDXGIResource ** ppResources;
    DXGI_OFFER_RESOURCE_PRIORITY Priority;
    UINT Flags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice4_OfferResources1(const UINT NumResources,IDXGIResource ** ppResources,const DXGI_OFFER_RESOURCE_PRIORITY Priority,const UINT Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumResources, max_mode);
    cmd_size += npt_sizeof_array_count(NumResources);
    cmd_size += sizeof(uint64_t) * NumResources;
    cmd_size += npt_sizeof_DXGI_OFFER_RESOURCE_PRIORITY(&Priority, max_mode);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice4_OfferResources1(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        UINT NumResources,
                                        IDXGIResource ** ppResources,
                                        DXGI_OFFER_RESOURCE_PRIORITY Priority,
                                        UINT Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice4_OfferResources1(NumResources,ppResources,Priority,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice4,
                                 NPT_METHOD_IDXGIDevice4_OfferResources1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumResources);
    if (ppResources) {
        npt_encode_array_count(enc, NumResources);
        for (uint32_t _i = 0; _i < (uint32_t)NumResources; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppResources[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_DXGI_OFFER_RESOURCE_PRIORITY(enc, &Priority);
    npt_encode_UINT(enc, &Flags);
}

static inline size_t
npt_sizeof_IDXGIDevice4_OfferResources1_reply(const UINT NumResources,IDXGIResource ** ppResources,const DXGI_OFFER_RESOURCE_PRIORITY Priority,const UINT Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumResources (input only) */
/* skip ppResources (input only) */
/* skip Priority (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice4_OfferResources1_reply(struct npt_cs_decoder *dec,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice4,
                                                  NPT_METHOD_IDXGIDevice4_OfferResources1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice4_OfferResources1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice4,
                                       NPT_METHOD_IDXGIDevice4_OfferResources1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDevice4_OfferResources1(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        UINT NumResources,
                                        IDXGIResource ** ppResources,
                                        DXGI_OFFER_RESOURCE_PRIORITY Priority,
                                        UINT Flags,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice4_OfferResources1(NumResources,ppResources,Priority,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice4_OfferResources1_reply(NumResources,ppResources,Priority,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice4_OfferResources1(enc, cmd_flags, object_id, NumResources, ppResources, Priority, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice4_OfferResources1(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       UINT NumResources,
                                       IDXGIResource ** ppResources,
                                       DXGI_OFFER_RESOURCE_PRIORITY Priority,
                                       UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice4_OfferResources1(ring, 0, object_id, NumResources, ppResources, Priority, Flags, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice4_OfferResources1(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      UINT NumResources,
                                      IDXGIResource ** ppResources,
                                      DXGI_OFFER_RESOURCE_PRIORITY Priority,
                                      UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice4_OfferResources1(ring, NPT_CMD_FLAG_REPLY, object_id, NumResources, ppResources, Priority, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice4_OfferResources1_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIDevice4::ReclaimResources1
 */

struct npt_command_IDXGIDevice4_ReclaimResources1 {
    void *_self;
    UINT NumResources;
    IDXGIResource ** ppResources;
    DXGI_RECLAIM_RESOURCE_RESULTS * pResults;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIDevice4_ReclaimResources1(const UINT NumResources,IDXGIResource ** ppResources,const DXGI_RECLAIM_RESOURCE_RESULTS * pResults)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumResources, max_mode);
    cmd_size += npt_sizeof_array_count(NumResources);
    cmd_size += sizeof(uint64_t) * NumResources;
/* skip pResults (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIDevice4_ReclaimResources1(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          UINT NumResources,
                                          IDXGIResource ** ppResources,
                                          DXGI_RECLAIM_RESOURCE_RESULTS * pResults)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDevice4_ReclaimResources1(NumResources,ppResources,pResults);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice4,
                                 NPT_METHOD_IDXGIDevice4_ReclaimResources1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumResources);
    if (ppResources) {
        npt_encode_array_count(enc, NumResources);
        for (uint32_t _i = 0; _i < (uint32_t)NumResources; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppResources[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pResults (output only) */
}

static inline size_t
npt_sizeof_IDXGIDevice4_ReclaimResources1_reply(const UINT NumResources,IDXGIResource ** ppResources,const DXGI_RECLAIM_RESOURCE_RESULTS * pResults)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumResources (input only) */
/* skip ppResources (input only) */
    cmd_size += npt_sizeof_array_count(pResults ? NumResources : 0);
    if (pResults)
        cmd_size += npt_sizeof_DXGI_RECLAIM_RESOURCE_RESULTS_array(pResults, NumResources);
    return cmd_size;
}

static inline void
npt_decode_IDXGIDevice4_ReclaimResources1_reply(struct npt_cs_decoder *dec,
                                                DXGI_RECLAIM_RESOURCE_RESULTS * pResults,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice4,
                                                  NPT_METHOD_IDXGIDevice4_ReclaimResources1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDevice4_ReclaimResources1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice4,
                                       NPT_METHOD_IDXGIDevice4_ReclaimResources1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pResults)
            npt_decode_DXGI_RECLAIM_RESOURCE_RESULTS_array(dec, (DXGI_RECLAIM_RESOURCE_RESULTS *)pResults, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_IDXGIDevice4_ReclaimResources1(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          UINT NumResources,
                                          IDXGIResource ** ppResources,
                                          DXGI_RECLAIM_RESOURCE_RESULTS * pResults,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDevice4_ReclaimResources1(NumResources,ppResources,pResults);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDevice4_ReclaimResources1_reply(NumResources,ppResources,pResults)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDevice4_ReclaimResources1(enc, cmd_flags, object_id, NumResources, ppResources, pResults);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDevice4_ReclaimResources1(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         UINT NumResources,
                                         IDXGIResource ** ppResources,
                                         DXGI_RECLAIM_RESOURCE_RESULTS * pResults)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice4_ReclaimResources1(ring, 0, object_id, NumResources, ppResources, pResults, &submit);
}

static inline HRESULT
npt_call_IDXGIDevice4_ReclaimResources1(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        UINT NumResources,
                                        IDXGIResource ** ppResources,
                                        DXGI_RECLAIM_RESOURCE_RESULTS * pResults)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDevice4_ReclaimResources1(ring, NPT_CMD_FLAG_REPLY, object_id, NumResources, ppResources, pResults, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIDevice4_ReclaimResources1_reply(dec, pResults, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIDEVICE_H */
