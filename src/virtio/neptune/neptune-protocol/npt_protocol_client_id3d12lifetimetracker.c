/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for id3d12lifetimetracker: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_id3d12lifetimetracker.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* ID3D12LifetimeTracker                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12lifetimetracker_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12DeviceChild,
    &NPT_IID_ID3D12LifetimeTracker,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12lifetimetracker_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12lifetimetracker_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12lifetimetracker_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12lifetimetracker_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12lifetimetracker_default_DestroyOwnedObject(void *self, ID3D12DeviceChild * pObject)
{
    npt_async_ID3D12LifetimeTracker_DestroyOwnedObject(npt_com_self_ring(self),npt_com_self_id(self),pObject);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d12lifetimetracker_fill_default_vtbl(struct npt_id3d12lifetimetracker_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12lifetimetracker_default_QueryInterface;
    dst->AddRef         = npt_id3d12lifetimetracker_default_AddRef;
    dst->Release        = npt_id3d12lifetimetracker_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetDevice = npt_id3d12devicechild_default_GetDevice;
    dst->DestroyOwnedObject = npt_id3d12lifetimetracker_default_DestroyOwnedObject;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12lifetimetracker_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12lifetimetracker_client_vtbl npt_id3d12lifetimetracker_default_vtbl_storage;

void
npt_id3d12lifetimetracker_install_default_vtbl(void)
{
    npt_id3d12lifetimetracker_fill_default_vtbl(&npt_id3d12lifetimetracker_default_vtbl_storage);
}

void *
npt_id3d12lifetimetracker_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12lifetimetracker_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
