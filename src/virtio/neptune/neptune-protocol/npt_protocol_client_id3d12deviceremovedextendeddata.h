/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM declarations for id3d12deviceremovedextendeddata: per-interface vtable
 * structs, default thunk prototypes, default vtable populators, and default
 * constructors.
 *
 * The thunk bodies, populators, and constructors live in the matching .c
 * file.  This split keeps the headers small and prevents N copies of the
 * thunk code from landing in every TU that pulls in this family.
 */

#ifndef NPT_PROTOCOL_CLIENT_ID3D12DEVICEREMOVEDEXTENDEDDATA_H
#define NPT_PROTOCOL_CLIENT_ID3D12DEVICEREMOVEDEXTENDEDDATA_H

#include "npt_protocol_guest_id3d12deviceremovedextendeddata.h"

/* Forward-declare struct npt_device so the client_create prototypes don't
 * trigger the "declared inside parameter list" warning.  The full
 * definition lives in the consumer's npt_device.h, which the .c file pulls
 * in via npt_com.h. */
struct npt_device;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"



/* ================================================================== */
/* ID3D12DeviceRemovedExtendedData                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D12DeviceRemovedExtendedDataVtbl, by construction.
 */
struct npt_id3d12deviceremovedextendeddata_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D12DeviceRemovedExtendedData::GetAutoBreadcrumbsOutput */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAutoBreadcrumbsOutput)(void *self, D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput);
    /* slot 4: ID3D12DeviceRemovedExtendedData::GetPageFaultAllocationOutput */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPageFaultAllocationOutput)(void *self, D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata_default_GetAutoBreadcrumbsOutput(void *self, D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata_default_GetPageFaultAllocationOutput(void *self, D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput);

/* Default IUnknown thunks for ID3D12DeviceRemovedExtendedData.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d12deviceremovedextendeddata_fill_default_vtbl(struct npt_id3d12deviceremovedextendeddata_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d12deviceremovedextendeddata_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d12deviceremovedextendeddata_client_vtbl npt_id3d12deviceremovedextendeddata_default_vtbl_storage;
extern void
npt_id3d12deviceremovedextendeddata_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d12deviceremovedextendeddata_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* ID3D12DeviceRemovedExtendedData1                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D12DeviceRemovedExtendedData1Vtbl, by construction.
 */
struct npt_id3d12deviceremovedextendeddata1_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D12DeviceRemovedExtendedData::GetAutoBreadcrumbsOutput */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAutoBreadcrumbsOutput)(void *self, D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput);
    /* slot 4: ID3D12DeviceRemovedExtendedData::GetPageFaultAllocationOutput */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPageFaultAllocationOutput)(void *self, D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput);
    /* slot 5: ID3D12DeviceRemovedExtendedData1::GetAutoBreadcrumbsOutput1 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAutoBreadcrumbsOutput1)(void *self, D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput);
    /* slot 6: ID3D12DeviceRemovedExtendedData1::GetPageFaultAllocationOutput1 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPageFaultAllocationOutput1)(void *self, D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata1_default_GetAutoBreadcrumbsOutput1(void *self, D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata1_default_GetPageFaultAllocationOutput1(void *self, D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput);

/* Default IUnknown thunks for ID3D12DeviceRemovedExtendedData1.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata1_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata1_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata1_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d12deviceremovedextendeddata1_fill_default_vtbl(struct npt_id3d12deviceremovedextendeddata1_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d12deviceremovedextendeddata1_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d12deviceremovedextendeddata1_client_vtbl npt_id3d12deviceremovedextendeddata1_default_vtbl_storage;
extern void
npt_id3d12deviceremovedextendeddata1_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d12deviceremovedextendeddata1_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* ID3D12DeviceRemovedExtendedData2                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D12DeviceRemovedExtendedData2Vtbl, by construction.
 */
struct npt_id3d12deviceremovedextendeddata2_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D12DeviceRemovedExtendedData::GetAutoBreadcrumbsOutput */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAutoBreadcrumbsOutput)(void *self, D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput);
    /* slot 4: ID3D12DeviceRemovedExtendedData::GetPageFaultAllocationOutput */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPageFaultAllocationOutput)(void *self, D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput);
    /* slot 5: ID3D12DeviceRemovedExtendedData1::GetAutoBreadcrumbsOutput1 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAutoBreadcrumbsOutput1)(void *self, D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput);
    /* slot 6: ID3D12DeviceRemovedExtendedData1::GetPageFaultAllocationOutput1 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPageFaultAllocationOutput1)(void *self, D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput);
    /* slot 7: ID3D12DeviceRemovedExtendedData2::GetPageFaultAllocationOutput2 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPageFaultAllocationOutput2)(void *self, D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput);
    /* slot 8: ID3D12DeviceRemovedExtendedData2::GetDeviceState */
    D3D12_DRED_DEVICE_STATE (NPT_STDMETHODCALLTYPE *GetDeviceState)(void *self);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata2_default_GetPageFaultAllocationOutput2(void *self, D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput);
extern D3D12_DRED_DEVICE_STATE NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata2_default_GetDeviceState(void *self);

/* Default IUnknown thunks for ID3D12DeviceRemovedExtendedData2.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata2_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata2_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceremovedextendeddata2_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d12deviceremovedextendeddata2_fill_default_vtbl(struct npt_id3d12deviceremovedextendeddata2_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d12deviceremovedextendeddata2_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d12deviceremovedextendeddata2_client_vtbl npt_id3d12deviceremovedextendeddata2_default_vtbl_storage;
extern void
npt_id3d12deviceremovedextendeddata2_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d12deviceremovedextendeddata2_client_create(struct npt_device *dev, uint64_t host_id);


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_CLIENT_ID3D12DEVICEREMOVEDEXTENDEDDATA_H */
