/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12device.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12DEVICE_H
#define NPT_PROTOCOL_GUEST_ID3D12DEVICE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12Device::GetNodeCount
 */

struct npt_command_ID3D12Device_GetNodeCount {
    void *_self;
    UINT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_GetNodeCount(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_GetNodeCount(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_GetNodeCount();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetNodeCount),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12Device_GetNodeCount_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_GetNodeCount_reply(struct npt_cs_decoder *dec,
                                           UINT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_GetNodeCount))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_GetNodeCount reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_GetNodeCount));
        if (ret) *ret = (UINT)0;
        return;
    }
    if (ret) *ret = (UINT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device_GetNodeCount(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_GetNodeCount();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_GetNodeCount_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_GetNodeCount(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_GetNodeCount(struct npt_ring *ring,
                                    npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetNodeCount(ring, 0, object_id, &submit);
}

static inline UINT
npt_call_ID3D12Device_GetNodeCount(struct npt_ring *ring,
                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetNodeCount(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT _ret;
        npt_decode_ID3D12Device_GetNodeCount_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT)0;
    }
}

/*
 * ID3D12Device::CreateCommandQueue
 */

struct npt_command_ID3D12Device_CreateCommandQueue {
    void *_self;
    const D3D12_COMMAND_QUEUE_DESC * pDesc;
    const IID * riid;
    void ** ppCommandQueue;
    /* Shadow: the guest-allocated id for ppCommandQueue arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandQueue -> *ppCommandQueue} in the
     * context object table. */
    uint64_t _guest_id_ppCommandQueue;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateCommandQueue(const D3D12_COMMAND_QUEUE_DESC * pDesc,const IID * riid,void ** ppCommandQueue)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_COMMAND_QUEUE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppCommandQueue: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateCommandQueue(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const D3D12_COMMAND_QUEUE_DESC * pDesc,
                                           const IID * riid,
                                           void ** ppCommandQueue)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateCommandQueue(pDesc,riid,ppCommandQueue);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommandQueue),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_COMMAND_QUEUE_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppCommandQueue = 0;
    if (ppCommandQueue) {
        _gid_ppCommandQueue = npt_com_allocate_next_id();
        *ppCommandQueue = (void *)(uintptr_t)_gid_ppCommandQueue;
    }
    npt_encode_uint64_t(enc, &_gid_ppCommandQueue);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateCommandQueue_reply(const D3D12_COMMAND_QUEUE_DESC * pDesc,const IID * riid,void ** ppCommandQueue)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppCommandQueue (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateCommandQueue_reply(struct npt_cs_decoder *dec,
                                                 void ** ppCommandQueue,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateCommandQueue))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateCommandQueue reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateCommandQueue));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppCommandQueue (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateCommandQueue(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const D3D12_COMMAND_QUEUE_DESC * pDesc,
                                           const IID * riid,
                                           void ** ppCommandQueue,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateCommandQueue(pDesc,riid,ppCommandQueue);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateCommandQueue_reply(pDesc,riid,ppCommandQueue)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateCommandQueue(enc, cmd_flags, object_id, pDesc, riid, ppCommandQueue);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateCommandQueue(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const D3D12_COMMAND_QUEUE_DESC * pDesc,
                                          const IID * riid,
                                          void ** ppCommandQueue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommandQueue(ring, 0, object_id, pDesc, riid, ppCommandQueue, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateCommandQueue(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const D3D12_COMMAND_QUEUE_DESC * pDesc,
                                         const IID * riid,
                                         void ** ppCommandQueue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommandQueue(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppCommandQueue, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateCommandQueue_reply(dec, ppCommandQueue, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateCommandAllocator
 */

struct npt_command_ID3D12Device_CreateCommandAllocator {
    void *_self;
    D3D12_COMMAND_LIST_TYPE type;
    const IID * riid;
    void ** ppCommandAllocator;
    /* Shadow: the guest-allocated id for ppCommandAllocator arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandAllocator -> *ppCommandAllocator} in the
     * context object table. */
    uint64_t _guest_id_ppCommandAllocator;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateCommandAllocator(const D3D12_COMMAND_LIST_TYPE type,const IID * riid,void ** ppCommandAllocator)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_COMMAND_LIST_TYPE(&type, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppCommandAllocator: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateCommandAllocator(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               D3D12_COMMAND_LIST_TYPE type,
                                               const IID * riid,
                                               void ** ppCommandAllocator)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateCommandAllocator(type,riid,ppCommandAllocator);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommandAllocator),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_COMMAND_LIST_TYPE(enc, &type);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppCommandAllocator = 0;
    if (ppCommandAllocator) {
        _gid_ppCommandAllocator = npt_com_allocate_next_id();
        *ppCommandAllocator = (void *)(uintptr_t)_gid_ppCommandAllocator;
    }
    npt_encode_uint64_t(enc, &_gid_ppCommandAllocator);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateCommandAllocator_reply(const D3D12_COMMAND_LIST_TYPE type,const IID * riid,void ** ppCommandAllocator)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip type (input only) */
/* skip riid (input only) */
/* skip ppCommandAllocator (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateCommandAllocator_reply(struct npt_cs_decoder *dec,
                                                     void ** ppCommandAllocator,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateCommandAllocator))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateCommandAllocator reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateCommandAllocator));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppCommandAllocator (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateCommandAllocator(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               D3D12_COMMAND_LIST_TYPE type,
                                               const IID * riid,
                                               void ** ppCommandAllocator,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateCommandAllocator(type,riid,ppCommandAllocator);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateCommandAllocator_reply(type,riid,ppCommandAllocator)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateCommandAllocator(enc, cmd_flags, object_id, type, riid, ppCommandAllocator);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateCommandAllocator(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              D3D12_COMMAND_LIST_TYPE type,
                                              const IID * riid,
                                              void ** ppCommandAllocator)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommandAllocator(ring, 0, object_id, type, riid, ppCommandAllocator, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateCommandAllocator(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             D3D12_COMMAND_LIST_TYPE type,
                                             const IID * riid,
                                             void ** ppCommandAllocator)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommandAllocator(ring, NPT_CMD_FLAG_REPLY, object_id, type, riid, ppCommandAllocator, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateCommandAllocator_reply(dec, ppCommandAllocator, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateGraphicsPipelineState
 */

struct npt_command_ID3D12Device_CreateGraphicsPipelineState {
    void *_self;
    const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc;
    const IID * riid;
    void ** ppPipelineState;
    /* Shadow: the guest-allocated id for ppPipelineState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineState -> *ppPipelineState} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateGraphicsPipelineState(const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_GRAPHICS_PIPELINE_STATE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppPipelineState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateGraphicsPipelineState(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,
                                                    const IID * riid,
                                                    void ** ppPipelineState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateGraphicsPipelineState(pDesc,riid,ppPipelineState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateGraphicsPipelineState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_GRAPHICS_PIPELINE_STATE_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppPipelineState = 0;
    if (ppPipelineState) {
        _gid_ppPipelineState = npt_com_allocate_next_id();
        *ppPipelineState = (void *)(uintptr_t)_gid_ppPipelineState;
    }
    npt_encode_uint64_t(enc, &_gid_ppPipelineState);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateGraphicsPipelineState_reply(const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppPipelineState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateGraphicsPipelineState_reply(struct npt_cs_decoder *dec,
                                                          void ** ppPipelineState,
                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateGraphicsPipelineState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateGraphicsPipelineState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateGraphicsPipelineState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppPipelineState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateGraphicsPipelineState(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,
                                                    const IID * riid,
                                                    void ** ppPipelineState,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateGraphicsPipelineState(pDesc,riid,ppPipelineState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateGraphicsPipelineState_reply(pDesc,riid,ppPipelineState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateGraphicsPipelineState(enc, cmd_flags, object_id, pDesc, riid, ppPipelineState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateGraphicsPipelineState(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,
                                                   const IID * riid,
                                                   void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateGraphicsPipelineState(ring, 0, object_id, pDesc, riid, ppPipelineState, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateGraphicsPipelineState(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc,
                                                  const IID * riid,
                                                  void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateGraphicsPipelineState(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppPipelineState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateGraphicsPipelineState_reply(dec, ppPipelineState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateComputePipelineState
 */

struct npt_command_ID3D12Device_CreateComputePipelineState {
    void *_self;
    const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc;
    const IID * riid;
    void ** ppPipelineState;
    /* Shadow: the guest-allocated id for ppPipelineState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineState -> *ppPipelineState} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateComputePipelineState(const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_COMPUTE_PIPELINE_STATE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppPipelineState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateComputePipelineState(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,
                                                   const IID * riid,
                                                   void ** ppPipelineState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateComputePipelineState(pDesc,riid,ppPipelineState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateComputePipelineState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_COMPUTE_PIPELINE_STATE_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppPipelineState = 0;
    if (ppPipelineState) {
        _gid_ppPipelineState = npt_com_allocate_next_id();
        *ppPipelineState = (void *)(uintptr_t)_gid_ppPipelineState;
    }
    npt_encode_uint64_t(enc, &_gid_ppPipelineState);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateComputePipelineState_reply(const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppPipelineState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateComputePipelineState_reply(struct npt_cs_decoder *dec,
                                                         void ** ppPipelineState,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateComputePipelineState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateComputePipelineState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateComputePipelineState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppPipelineState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateComputePipelineState(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,
                                                   const IID * riid,
                                                   void ** ppPipelineState,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateComputePipelineState(pDesc,riid,ppPipelineState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateComputePipelineState_reply(pDesc,riid,ppPipelineState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateComputePipelineState(enc, cmd_flags, object_id, pDesc, riid, ppPipelineState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateComputePipelineState(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,
                                                  const IID * riid,
                                                  void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateComputePipelineState(ring, 0, object_id, pDesc, riid, ppPipelineState, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateComputePipelineState(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc,
                                                 const IID * riid,
                                                 void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateComputePipelineState(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppPipelineState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateComputePipelineState_reply(dec, ppPipelineState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateCommandList
 */

struct npt_command_ID3D12Device_CreateCommandList {
    void *_self;
    UINT nodeMask;
    D3D12_COMMAND_LIST_TYPE type;
    ID3D12CommandAllocator * pCommandAllocator;
    ID3D12PipelineState * pInitialState;
    const IID * riid;
    void ** ppCommandList;
    /* Shadow: the guest-allocated id for ppCommandList arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandList -> *ppCommandList} in the
     * context object table. */
    uint64_t _guest_id_ppCommandList;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateCommandList(const UINT nodeMask,const D3D12_COMMAND_LIST_TYPE type,const ID3D12CommandAllocator * pCommandAllocator,const ID3D12PipelineState * pInitialState,const IID * riid,void ** ppCommandList)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&nodeMask, max_mode);
    cmd_size += npt_sizeof_D3D12_COMMAND_LIST_TYPE(&type, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppCommandList: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateCommandList(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          UINT nodeMask,
                                          D3D12_COMMAND_LIST_TYPE type,
                                          ID3D12CommandAllocator * pCommandAllocator,
                                          ID3D12PipelineState * pInitialState,
                                          const IID * riid,
                                          void ** ppCommandList)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateCommandList(nodeMask,type,pCommandAllocator,pInitialState,riid,ppCommandList);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommandList),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &nodeMask);
    npt_encode_D3D12_COMMAND_LIST_TYPE(enc, &type);
    npt_encode_com_handle(enc, npt_object_get_id(pCommandAllocator));
    npt_encode_com_handle(enc, npt_object_get_id(pInitialState));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppCommandList = 0;
    if (ppCommandList) {
        _gid_ppCommandList = npt_com_allocate_next_id();
        *ppCommandList = (void *)(uintptr_t)_gid_ppCommandList;
    }
    npt_encode_uint64_t(enc, &_gid_ppCommandList);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateCommandList_reply(const UINT nodeMask,const D3D12_COMMAND_LIST_TYPE type,const ID3D12CommandAllocator * pCommandAllocator,const ID3D12PipelineState * pInitialState,const IID * riid,void ** ppCommandList)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip nodeMask (input only) */
/* skip type (input only) */
/* skip pCommandAllocator (input only) */
/* skip pInitialState (input only) */
/* skip riid (input only) */
/* skip ppCommandList (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateCommandList_reply(struct npt_cs_decoder *dec,
                                                void ** ppCommandList,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateCommandList))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateCommandList reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateCommandList));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppCommandList (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateCommandList(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          UINT nodeMask,
                                          D3D12_COMMAND_LIST_TYPE type,
                                          ID3D12CommandAllocator * pCommandAllocator,
                                          ID3D12PipelineState * pInitialState,
                                          const IID * riid,
                                          void ** ppCommandList,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateCommandList(nodeMask,type,pCommandAllocator,pInitialState,riid,ppCommandList);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateCommandList_reply(nodeMask,type,pCommandAllocator,pInitialState,riid,ppCommandList)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateCommandList(enc, cmd_flags, object_id, nodeMask, type, pCommandAllocator, pInitialState, riid, ppCommandList);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateCommandList(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         UINT nodeMask,
                                         D3D12_COMMAND_LIST_TYPE type,
                                         ID3D12CommandAllocator * pCommandAllocator,
                                         ID3D12PipelineState * pInitialState,
                                         const IID * riid,
                                         void ** ppCommandList)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommandList(ring, 0, object_id, nodeMask, type, pCommandAllocator, pInitialState, riid, ppCommandList, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateCommandList(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        UINT nodeMask,
                                        D3D12_COMMAND_LIST_TYPE type,
                                        ID3D12CommandAllocator * pCommandAllocator,
                                        ID3D12PipelineState * pInitialState,
                                        const IID * riid,
                                        void ** ppCommandList)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommandList(ring, NPT_CMD_FLAG_REPLY, object_id, nodeMask, type, pCommandAllocator, pInitialState, riid, ppCommandList, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateCommandList_reply(dec, ppCommandList, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CheckFeatureSupport
 */

struct npt_command_ID3D12Device_CheckFeatureSupport {
    void *_self;
    D3D12_FEATURE Feature;
    void * pFeatureSupportData;
    UINT FeatureSupportDataSize;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CheckFeatureSupport(const D3D12_FEATURE Feature,const void * pFeatureSupportData,const UINT FeatureSupportDataSize)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_FEATURE(&Feature, max_mode);
    if (pFeatureSupportData) {
        cmd_size += npt_sizeof_array_count(FeatureSupportDataSize);
        cmd_size += npt_sizeof_blob_array(pFeatureSupportData, FeatureSupportDataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&FeatureSupportDataSize, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CheckFeatureSupport(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            D3D12_FEATURE Feature,
                                            void * pFeatureSupportData,
                                            UINT FeatureSupportDataSize)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CheckFeatureSupport(Feature,pFeatureSupportData,FeatureSupportDataSize);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CheckFeatureSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_FEATURE(enc, &Feature);
    if (pFeatureSupportData) {
        npt_encode_array_count(enc, FeatureSupportDataSize);
        npt_encode_blob_array(enc, pFeatureSupportData, FeatureSupportDataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &FeatureSupportDataSize);
}

static inline size_t
npt_sizeof_ID3D12Device_CheckFeatureSupport_reply(const D3D12_FEATURE Feature,const void * pFeatureSupportData,const UINT FeatureSupportDataSize)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Feature (input only) */
    if (pFeatureSupportData) {
        cmd_size += npt_sizeof_array_count(FeatureSupportDataSize);
        cmd_size += npt_sizeof_blob_array(pFeatureSupportData, FeatureSupportDataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip FeatureSupportDataSize (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CheckFeatureSupport_reply(struct npt_cs_decoder *dec,
                                                  void * pFeatureSupportData,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CheckFeatureSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CheckFeatureSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CheckFeatureSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pFeatureSupportData)
            npt_decode_blob_array(dec, (void *)pFeatureSupportData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D12Device_CheckFeatureSupport(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            D3D12_FEATURE Feature,
                                            void * pFeatureSupportData,
                                            UINT FeatureSupportDataSize,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CheckFeatureSupport(Feature,pFeatureSupportData,FeatureSupportDataSize);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CheckFeatureSupport_reply(Feature,pFeatureSupportData,FeatureSupportDataSize)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CheckFeatureSupport(enc, cmd_flags, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CheckFeatureSupport(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           D3D12_FEATURE Feature,
                                           void * pFeatureSupportData,
                                           UINT FeatureSupportDataSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CheckFeatureSupport(ring, 0, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CheckFeatureSupport(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          D3D12_FEATURE Feature,
                                          void * pFeatureSupportData,
                                          UINT FeatureSupportDataSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CheckFeatureSupport(ring, NPT_CMD_FLAG_REPLY, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CheckFeatureSupport_reply(dec, pFeatureSupportData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateDescriptorHeap
 */

struct npt_command_ID3D12Device_CreateDescriptorHeap {
    void *_self;
    const D3D12_DESCRIPTOR_HEAP_DESC * pDescriptorHeapDesc;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateDescriptorHeap(const D3D12_DESCRIPTOR_HEAP_DESC * pDescriptorHeapDesc,const IID * riid,void ** ppvHeap)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDescriptorHeapDesc);
    if (pDescriptorHeapDesc)
        cmd_size += npt_sizeof_D3D12_DESCRIPTOR_HEAP_DESC(pDescriptorHeapDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvHeap: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateDescriptorHeap(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const D3D12_DESCRIPTOR_HEAP_DESC * pDescriptorHeapDesc,
                                             const IID * riid,
                                             void ** ppvHeap)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateDescriptorHeap(pDescriptorHeapDesc,riid,ppvHeap);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateDescriptorHeap),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDescriptorHeapDesc))
        npt_encode_D3D12_DESCRIPTOR_HEAP_DESC(enc, pDescriptorHeapDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvHeap = 0;
    if (ppvHeap) {
        _gid_ppvHeap = npt_com_allocate_next_id();
        *ppvHeap = (void *)(uintptr_t)_gid_ppvHeap;
    }
    npt_encode_uint64_t(enc, &_gid_ppvHeap);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateDescriptorHeap_reply(const D3D12_DESCRIPTOR_HEAP_DESC * pDescriptorHeapDesc,const IID * riid,void ** ppvHeap)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDescriptorHeapDesc (input only) */
/* skip riid (input only) */
/* skip ppvHeap (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateDescriptorHeap_reply(struct npt_cs_decoder *dec,
                                                   void ** ppvHeap,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateDescriptorHeap))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateDescriptorHeap reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateDescriptorHeap));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvHeap (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateDescriptorHeap(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const D3D12_DESCRIPTOR_HEAP_DESC * pDescriptorHeapDesc,
                                             const IID * riid,
                                             void ** ppvHeap,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateDescriptorHeap(pDescriptorHeapDesc,riid,ppvHeap);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateDescriptorHeap_reply(pDescriptorHeapDesc,riid,ppvHeap)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateDescriptorHeap(enc, cmd_flags, object_id, pDescriptorHeapDesc, riid, ppvHeap);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateDescriptorHeap(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const D3D12_DESCRIPTOR_HEAP_DESC * pDescriptorHeapDesc,
                                            const IID * riid,
                                            void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateDescriptorHeap(ring, 0, object_id, pDescriptorHeapDesc, riid, ppvHeap, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateDescriptorHeap(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           const D3D12_DESCRIPTOR_HEAP_DESC * pDescriptorHeapDesc,
                                           const IID * riid,
                                           void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateDescriptorHeap(ring, NPT_CMD_FLAG_REPLY, object_id, pDescriptorHeapDesc, riid, ppvHeap, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateDescriptorHeap_reply(dec, ppvHeap, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::GetDescriptorHandleIncrementSize
 */

struct npt_command_ID3D12Device_GetDescriptorHandleIncrementSize {
    void *_self;
    D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapType;
    UINT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_GetDescriptorHandleIncrementSize(const D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapType)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_DESCRIPTOR_HEAP_TYPE(&DescriptorHeapType, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_GetDescriptorHandleIncrementSize(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapType)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_GetDescriptorHandleIncrementSize(DescriptorHeapType);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetDescriptorHandleIncrementSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_DESCRIPTOR_HEAP_TYPE(enc, &DescriptorHeapType);
}

static inline size_t
npt_sizeof_ID3D12Device_GetDescriptorHandleIncrementSize_reply(const D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapType)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip DescriptorHeapType (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_GetDescriptorHandleIncrementSize_reply(struct npt_cs_decoder *dec,
                                                               UINT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_GetDescriptorHandleIncrementSize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_GetDescriptorHandleIncrementSize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_GetDescriptorHandleIncrementSize));
        if (ret) *ret = (UINT)0;
        return;
    }
    if (ret) *ret = (UINT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device_GetDescriptorHandleIncrementSize(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapType,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_GetDescriptorHandleIncrementSize(DescriptorHeapType);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_GetDescriptorHandleIncrementSize_reply(DescriptorHeapType)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_GetDescriptorHandleIncrementSize(enc, cmd_flags, object_id, DescriptorHeapType);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_GetDescriptorHandleIncrementSize(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetDescriptorHandleIncrementSize(ring, 0, object_id, DescriptorHeapType, &submit);
}

static inline UINT
npt_call_ID3D12Device_GetDescriptorHandleIncrementSize(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetDescriptorHandleIncrementSize(ring, NPT_CMD_FLAG_REPLY, object_id, DescriptorHeapType, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT _ret;
        npt_decode_ID3D12Device_GetDescriptorHandleIncrementSize_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT)0;
    }
}

/*
 * ID3D12Device::CreateRootSignature
 */

struct npt_command_ID3D12Device_CreateRootSignature {
    void *_self;
    UINT nodeMask;
    const void * pBlobWithRootSignature;
    SIZE_T blobLengthInBytes;
    const IID * riid;
    void ** ppvRootSignature;
    /* Shadow: the guest-allocated id for ppvRootSignature arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvRootSignature -> *ppvRootSignature} in the
     * context object table. */
    uint64_t _guest_id_ppvRootSignature;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateRootSignature(const UINT nodeMask,const void * pBlobWithRootSignature,const SIZE_T blobLengthInBytes,const IID * riid,void ** ppvRootSignature)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&nodeMask, max_mode);
    if (pBlobWithRootSignature) {
        cmd_size += npt_sizeof_array_count(blobLengthInBytes);
        cmd_size += npt_sizeof_blob_array(pBlobWithRootSignature, blobLengthInBytes);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&blobLengthInBytes, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvRootSignature: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateRootSignature(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT nodeMask,
                                            const void * pBlobWithRootSignature,
                                            SIZE_T blobLengthInBytes,
                                            const IID * riid,
                                            void ** ppvRootSignature)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateRootSignature(nodeMask,pBlobWithRootSignature,blobLengthInBytes,riid,ppvRootSignature);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateRootSignature),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &nodeMask);
    if (pBlobWithRootSignature) {
        npt_encode_array_count(enc, blobLengthInBytes);
        npt_encode_blob_array(enc, pBlobWithRootSignature, blobLengthInBytes);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &blobLengthInBytes);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvRootSignature = 0;
    if (ppvRootSignature) {
        _gid_ppvRootSignature = npt_com_allocate_next_id();
        *ppvRootSignature = (void *)(uintptr_t)_gid_ppvRootSignature;
    }
    npt_encode_uint64_t(enc, &_gid_ppvRootSignature);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateRootSignature_reply(const UINT nodeMask,const void * pBlobWithRootSignature,const SIZE_T blobLengthInBytes,const IID * riid,void ** ppvRootSignature)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip nodeMask (input only) */
/* skip pBlobWithRootSignature (input only) */
/* skip blobLengthInBytes (input only) */
/* skip riid (input only) */
/* skip ppvRootSignature (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateRootSignature_reply(struct npt_cs_decoder *dec,
                                                  void ** ppvRootSignature,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateRootSignature))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateRootSignature reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateRootSignature));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvRootSignature (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateRootSignature(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT nodeMask,
                                            const void * pBlobWithRootSignature,
                                            SIZE_T blobLengthInBytes,
                                            const IID * riid,
                                            void ** ppvRootSignature,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateRootSignature(nodeMask,pBlobWithRootSignature,blobLengthInBytes,riid,ppvRootSignature);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateRootSignature_reply(nodeMask,pBlobWithRootSignature,blobLengthInBytes,riid,ppvRootSignature)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateRootSignature(enc, cmd_flags, object_id, nodeMask, pBlobWithRootSignature, blobLengthInBytes, riid, ppvRootSignature);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateRootSignature(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT nodeMask,
                                           const void * pBlobWithRootSignature,
                                           SIZE_T blobLengthInBytes,
                                           const IID * riid,
                                           void ** ppvRootSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateRootSignature(ring, 0, object_id, nodeMask, pBlobWithRootSignature, blobLengthInBytes, riid, ppvRootSignature, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateRootSignature(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          UINT nodeMask,
                                          const void * pBlobWithRootSignature,
                                          SIZE_T blobLengthInBytes,
                                          const IID * riid,
                                          void ** ppvRootSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateRootSignature(ring, NPT_CMD_FLAG_REPLY, object_id, nodeMask, pBlobWithRootSignature, blobLengthInBytes, riid, ppvRootSignature, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateRootSignature_reply(dec, ppvRootSignature, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateConstantBufferView
 */

struct npt_command_ID3D12Device_CreateConstantBufferView {
    void *_self;
    const D3D12_CONSTANT_BUFFER_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateConstantBufferView(const D3D12_CONSTANT_BUFFER_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_CONSTANT_BUFFER_VIEW_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DestDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateConstantBufferView(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 const D3D12_CONSTANT_BUFFER_VIEW_DESC * pDesc,
                                                 D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateConstantBufferView(pDesc,DestDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateConstantBufferView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_CONSTANT_BUFFER_VIEW_DESC(enc, pDesc);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DestDescriptor);
}

static inline size_t
npt_sizeof_ID3D12Device_CreateConstantBufferView_reply(const D3D12_CONSTANT_BUFFER_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip DestDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device_CreateConstantBufferView(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 const D3D12_CONSTANT_BUFFER_VIEW_DESC * pDesc,
                                                 D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateConstantBufferView(pDesc,DestDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateConstantBufferView_reply(pDesc,DestDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateConstantBufferView(enc, cmd_flags, object_id, pDesc, DestDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateConstantBufferView(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const D3D12_CONSTANT_BUFFER_VIEW_DESC * pDesc,
                                                D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateConstantBufferView(ring, 0, object_id, pDesc, DestDescriptor, &submit);
}

static inline void
npt_call_ID3D12Device_CreateConstantBufferView(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const D3D12_CONSTANT_BUFFER_VIEW_DESC * pDesc,
                                               D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateConstantBufferView(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, DestDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::CreateShaderResourceView
 */

struct npt_command_ID3D12Device_CreateShaderResourceView {
    void *_self;
    ID3D12Resource * pResource;
    const D3D12_SHADER_RESOURCE_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateShaderResourceView(const ID3D12Resource * pResource,const D3D12_SHADER_RESOURCE_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_SHADER_RESOURCE_VIEW_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DestDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateShaderResourceView(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D12Resource * pResource,
                                                 const D3D12_SHADER_RESOURCE_VIEW_DESC * pDesc,
                                                 D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateShaderResourceView(pResource,pDesc,DestDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateShaderResourceView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_SHADER_RESOURCE_VIEW_DESC(enc, pDesc);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DestDescriptor);
}

static inline size_t
npt_sizeof_ID3D12Device_CreateShaderResourceView_reply(const ID3D12Resource * pResource,const D3D12_SHADER_RESOURCE_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc (input only) */
/* skip DestDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device_CreateShaderResourceView(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D12Resource * pResource,
                                                 const D3D12_SHADER_RESOURCE_VIEW_DESC * pDesc,
                                                 D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateShaderResourceView(pResource,pDesc,DestDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateShaderResourceView_reply(pResource,pDesc,DestDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateShaderResourceView(enc, cmd_flags, object_id, pResource, pDesc, DestDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateShaderResourceView(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D12Resource * pResource,
                                                const D3D12_SHADER_RESOURCE_VIEW_DESC * pDesc,
                                                D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateShaderResourceView(ring, 0, object_id, pResource, pDesc, DestDescriptor, &submit);
}

static inline void
npt_call_ID3D12Device_CreateShaderResourceView(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D12Resource * pResource,
                                               const D3D12_SHADER_RESOURCE_VIEW_DESC * pDesc,
                                               D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateShaderResourceView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc, DestDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::CreateUnorderedAccessView
 */

struct npt_command_ID3D12Device_CreateUnorderedAccessView {
    void *_self;
    ID3D12Resource * pResource;
    ID3D12Resource * pCounterResource;
    const D3D12_UNORDERED_ACCESS_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateUnorderedAccessView(const ID3D12Resource * pResource,const ID3D12Resource * pCounterResource,const D3D12_UNORDERED_ACCESS_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_UNORDERED_ACCESS_VIEW_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DestDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateUnorderedAccessView(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D12Resource * pResource,
                                                  ID3D12Resource * pCounterResource,
                                                  const D3D12_UNORDERED_ACCESS_VIEW_DESC * pDesc,
                                                  D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateUnorderedAccessView(pResource,pCounterResource,pDesc,DestDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateUnorderedAccessView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    npt_encode_com_handle(enc, npt_object_get_id(pCounterResource));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_UNORDERED_ACCESS_VIEW_DESC(enc, pDesc);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DestDescriptor);
}

static inline size_t
npt_sizeof_ID3D12Device_CreateUnorderedAccessView_reply(const ID3D12Resource * pResource,const ID3D12Resource * pCounterResource,const D3D12_UNORDERED_ACCESS_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pCounterResource (input only) */
/* skip pDesc (input only) */
/* skip DestDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device_CreateUnorderedAccessView(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D12Resource * pResource,
                                                  ID3D12Resource * pCounterResource,
                                                  const D3D12_UNORDERED_ACCESS_VIEW_DESC * pDesc,
                                                  D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateUnorderedAccessView(pResource,pCounterResource,pDesc,DestDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateUnorderedAccessView_reply(pResource,pCounterResource,pDesc,DestDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateUnorderedAccessView(enc, cmd_flags, object_id, pResource, pCounterResource, pDesc, DestDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateUnorderedAccessView(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D12Resource * pResource,
                                                 ID3D12Resource * pCounterResource,
                                                 const D3D12_UNORDERED_ACCESS_VIEW_DESC * pDesc,
                                                 D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateUnorderedAccessView(ring, 0, object_id, pResource, pCounterResource, pDesc, DestDescriptor, &submit);
}

static inline void
npt_call_ID3D12Device_CreateUnorderedAccessView(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D12Resource * pResource,
                                                ID3D12Resource * pCounterResource,
                                                const D3D12_UNORDERED_ACCESS_VIEW_DESC * pDesc,
                                                D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateUnorderedAccessView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pCounterResource, pDesc, DestDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::CreateRenderTargetView
 */

struct npt_command_ID3D12Device_CreateRenderTargetView {
    void *_self;
    ID3D12Resource * pResource;
    const D3D12_RENDER_TARGET_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateRenderTargetView(const ID3D12Resource * pResource,const D3D12_RENDER_TARGET_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RENDER_TARGET_VIEW_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DestDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateRenderTargetView(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12Resource * pResource,
                                               const D3D12_RENDER_TARGET_VIEW_DESC * pDesc,
                                               D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateRenderTargetView(pResource,pDesc,DestDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateRenderTargetView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RENDER_TARGET_VIEW_DESC(enc, pDesc);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DestDescriptor);
}

static inline size_t
npt_sizeof_ID3D12Device_CreateRenderTargetView_reply(const ID3D12Resource * pResource,const D3D12_RENDER_TARGET_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc (input only) */
/* skip DestDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device_CreateRenderTargetView(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12Resource * pResource,
                                               const D3D12_RENDER_TARGET_VIEW_DESC * pDesc,
                                               D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateRenderTargetView(pResource,pDesc,DestDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateRenderTargetView_reply(pResource,pDesc,DestDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateRenderTargetView(enc, cmd_flags, object_id, pResource, pDesc, DestDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateRenderTargetView(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D12Resource * pResource,
                                              const D3D12_RENDER_TARGET_VIEW_DESC * pDesc,
                                              D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateRenderTargetView(ring, 0, object_id, pResource, pDesc, DestDescriptor, &submit);
}

static inline void
npt_call_ID3D12Device_CreateRenderTargetView(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D12Resource * pResource,
                                             const D3D12_RENDER_TARGET_VIEW_DESC * pDesc,
                                             D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateRenderTargetView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc, DestDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::CreateDepthStencilView
 */

struct npt_command_ID3D12Device_CreateDepthStencilView {
    void *_self;
    ID3D12Resource * pResource;
    const D3D12_DEPTH_STENCIL_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateDepthStencilView(const ID3D12Resource * pResource,const D3D12_DEPTH_STENCIL_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_DEPTH_STENCIL_VIEW_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DestDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateDepthStencilView(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12Resource * pResource,
                                               const D3D12_DEPTH_STENCIL_VIEW_DESC * pDesc,
                                               D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateDepthStencilView(pResource,pDesc,DestDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateDepthStencilView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_DEPTH_STENCIL_VIEW_DESC(enc, pDesc);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DestDescriptor);
}

static inline size_t
npt_sizeof_ID3D12Device_CreateDepthStencilView_reply(const ID3D12Resource * pResource,const D3D12_DEPTH_STENCIL_VIEW_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc (input only) */
/* skip DestDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device_CreateDepthStencilView(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12Resource * pResource,
                                               const D3D12_DEPTH_STENCIL_VIEW_DESC * pDesc,
                                               D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateDepthStencilView(pResource,pDesc,DestDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateDepthStencilView_reply(pResource,pDesc,DestDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateDepthStencilView(enc, cmd_flags, object_id, pResource, pDesc, DestDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateDepthStencilView(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D12Resource * pResource,
                                              const D3D12_DEPTH_STENCIL_VIEW_DESC * pDesc,
                                              D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateDepthStencilView(ring, 0, object_id, pResource, pDesc, DestDescriptor, &submit);
}

static inline void
npt_call_ID3D12Device_CreateDepthStencilView(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D12Resource * pResource,
                                             const D3D12_DEPTH_STENCIL_VIEW_DESC * pDesc,
                                             D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateDepthStencilView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc, DestDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::CreateSampler
 */

struct npt_command_ID3D12Device_CreateSampler {
    void *_self;
    const D3D12_SAMPLER_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateSampler(const D3D12_SAMPLER_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_SAMPLER_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DestDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateSampler(struct npt_cs_encoder *enc,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const D3D12_SAMPLER_DESC * pDesc,
                                      D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateSampler(pDesc,DestDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateSampler),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_SAMPLER_DESC(enc, pDesc);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DestDescriptor);
}

static inline size_t
npt_sizeof_ID3D12Device_CreateSampler_reply(const D3D12_SAMPLER_DESC * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip DestDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device_CreateSampler(struct npt_ring *ring,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const D3D12_SAMPLER_DESC * pDesc,
                                      D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor,
                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateSampler(pDesc,DestDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateSampler_reply(pDesc,DestDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateSampler(enc, cmd_flags, object_id, pDesc, DestDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateSampler(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     const D3D12_SAMPLER_DESC * pDesc,
                                     D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateSampler(ring, 0, object_id, pDesc, DestDescriptor, &submit);
}

static inline void
npt_call_ID3D12Device_CreateSampler(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const D3D12_SAMPLER_DESC * pDesc,
                                    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateSampler(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, DestDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::CopyDescriptors
 */

struct npt_command_ID3D12Device_CopyDescriptors {
    void *_self;
    UINT NumDestDescriptorRanges;
    const D3D12_CPU_DESCRIPTOR_HANDLE * pDestDescriptorRangeStarts;
    const UINT * pDestDescriptorRangeSizes;
    UINT NumSrcDescriptorRanges;
    const D3D12_CPU_DESCRIPTOR_HANDLE * pSrcDescriptorRangeStarts;
    const UINT * pSrcDescriptorRangeSizes;
    D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType;
};

static inline size_t
npt_sizeof_ID3D12Device_CopyDescriptors(const UINT NumDestDescriptorRanges,const D3D12_CPU_DESCRIPTOR_HANDLE * pDestDescriptorRangeStarts,const UINT * pDestDescriptorRangeSizes,const UINT NumSrcDescriptorRanges,const D3D12_CPU_DESCRIPTOR_HANDLE * pSrcDescriptorRangeStarts,const UINT * pSrcDescriptorRangeSizes,const D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumDestDescriptorRanges, max_mode);
    cmd_size += npt_sizeof_array_count(pDestDescriptorRangeStarts ? NumDestDescriptorRanges : 0);
    for (uint32_t _i = 0; _i < (pDestDescriptorRangeStarts ? NumDestDescriptorRanges : 0); _i++)
        cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&pDestDescriptorRangeStarts[_i], max_mode);
    cmd_size += npt_sizeof_array_count(pDestDescriptorRangeSizes ? NumDestDescriptorRanges : 0);
    if (pDestDescriptorRangeSizes)
        cmd_size += npt_sizeof_UINT_array(pDestDescriptorRangeSizes, NumDestDescriptorRanges);
    cmd_size += npt_sizeof_UINT(&NumSrcDescriptorRanges, max_mode);
    cmd_size += npt_sizeof_array_count(pSrcDescriptorRangeStarts ? NumSrcDescriptorRanges : 0);
    for (uint32_t _i = 0; _i < (pSrcDescriptorRangeStarts ? NumSrcDescriptorRanges : 0); _i++)
        cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&pSrcDescriptorRangeStarts[_i], max_mode);
    cmd_size += npt_sizeof_array_count(pSrcDescriptorRangeSizes ? NumSrcDescriptorRanges : 0);
    if (pSrcDescriptorRangeSizes)
        cmd_size += npt_sizeof_UINT_array(pSrcDescriptorRangeSizes, NumSrcDescriptorRanges);
    cmd_size += npt_sizeof_D3D12_DESCRIPTOR_HEAP_TYPE(&DescriptorHeapsType, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CopyDescriptors(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        UINT NumDestDescriptorRanges,
                                        const D3D12_CPU_DESCRIPTOR_HANDLE * pDestDescriptorRangeStarts,
                                        const UINT * pDestDescriptorRangeSizes,
                                        UINT NumSrcDescriptorRanges,
                                        const D3D12_CPU_DESCRIPTOR_HANDLE * pSrcDescriptorRangeStarts,
                                        const UINT * pSrcDescriptorRangeSizes,
                                        D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CopyDescriptors(NumDestDescriptorRanges,pDestDescriptorRangeStarts,pDestDescriptorRangeSizes,NumSrcDescriptorRanges,pSrcDescriptorRangeStarts,pSrcDescriptorRangeSizes,DescriptorHeapsType);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CopyDescriptors),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumDestDescriptorRanges);
    if (pDestDescriptorRangeStarts) {
        npt_encode_array_count(enc, NumDestDescriptorRanges);
        for (uint32_t _i = 0; _i < (uint32_t)(NumDestDescriptorRanges); _i++)
            npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &pDestDescriptorRangeStarts[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pDestDescriptorRangeSizes) {
        npt_encode_array_count(enc, NumDestDescriptorRanges);
        npt_encode_UINT_array(enc, pDestDescriptorRangeSizes, NumDestDescriptorRanges);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumSrcDescriptorRanges);
    if (pSrcDescriptorRangeStarts) {
        npt_encode_array_count(enc, NumSrcDescriptorRanges);
        for (uint32_t _i = 0; _i < (uint32_t)(NumSrcDescriptorRanges); _i++)
            npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &pSrcDescriptorRangeStarts[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pSrcDescriptorRangeSizes) {
        npt_encode_array_count(enc, NumSrcDescriptorRanges);
        npt_encode_UINT_array(enc, pSrcDescriptorRangeSizes, NumSrcDescriptorRanges);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_D3D12_DESCRIPTOR_HEAP_TYPE(enc, &DescriptorHeapsType);
}

static inline size_t
npt_sizeof_ID3D12Device_CopyDescriptors_reply(const UINT NumDestDescriptorRanges,const D3D12_CPU_DESCRIPTOR_HANDLE * pDestDescriptorRangeStarts,const UINT * pDestDescriptorRangeSizes,const UINT NumSrcDescriptorRanges,const D3D12_CPU_DESCRIPTOR_HANDLE * pSrcDescriptorRangeStarts,const UINT * pSrcDescriptorRangeSizes,const D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumDestDescriptorRanges (input only) */
/* skip pDestDescriptorRangeStarts (input only) */
/* skip pDestDescriptorRangeSizes (input only) */
/* skip NumSrcDescriptorRanges (input only) */
/* skip pSrcDescriptorRangeStarts (input only) */
/* skip pSrcDescriptorRangeSizes (input only) */
/* skip DescriptorHeapsType (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device_CopyDescriptors(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        UINT NumDestDescriptorRanges,
                                        const D3D12_CPU_DESCRIPTOR_HANDLE * pDestDescriptorRangeStarts,
                                        const UINT * pDestDescriptorRangeSizes,
                                        UINT NumSrcDescriptorRanges,
                                        const D3D12_CPU_DESCRIPTOR_HANDLE * pSrcDescriptorRangeStarts,
                                        const UINT * pSrcDescriptorRangeSizes,
                                        D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CopyDescriptors(NumDestDescriptorRanges,pDestDescriptorRangeStarts,pDestDescriptorRangeSizes,NumSrcDescriptorRanges,pSrcDescriptorRangeStarts,pSrcDescriptorRangeSizes,DescriptorHeapsType);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CopyDescriptors_reply(NumDestDescriptorRanges,pDestDescriptorRangeStarts,pDestDescriptorRangeSizes,NumSrcDescriptorRanges,pSrcDescriptorRangeStarts,pSrcDescriptorRangeSizes,DescriptorHeapsType)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CopyDescriptors(enc, cmd_flags, object_id, NumDestDescriptorRanges, pDestDescriptorRangeStarts, pDestDescriptorRangeSizes, NumSrcDescriptorRanges, pSrcDescriptorRangeStarts, pSrcDescriptorRangeSizes, DescriptorHeapsType);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CopyDescriptors(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       UINT NumDestDescriptorRanges,
                                       const D3D12_CPU_DESCRIPTOR_HANDLE * pDestDescriptorRangeStarts,
                                       const UINT * pDestDescriptorRangeSizes,
                                       UINT NumSrcDescriptorRanges,
                                       const D3D12_CPU_DESCRIPTOR_HANDLE * pSrcDescriptorRangeStarts,
                                       const UINT * pSrcDescriptorRangeSizes,
                                       D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CopyDescriptors(ring, 0, object_id, NumDestDescriptorRanges, pDestDescriptorRangeStarts, pDestDescriptorRangeSizes, NumSrcDescriptorRanges, pSrcDescriptorRangeStarts, pSrcDescriptorRangeSizes, DescriptorHeapsType, &submit);
}

static inline void
npt_call_ID3D12Device_CopyDescriptors(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      UINT NumDestDescriptorRanges,
                                      const D3D12_CPU_DESCRIPTOR_HANDLE * pDestDescriptorRangeStarts,
                                      const UINT * pDestDescriptorRangeSizes,
                                      UINT NumSrcDescriptorRanges,
                                      const D3D12_CPU_DESCRIPTOR_HANDLE * pSrcDescriptorRangeStarts,
                                      const UINT * pSrcDescriptorRangeSizes,
                                      D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CopyDescriptors(ring, NPT_CMD_FLAG_REPLY, object_id, NumDestDescriptorRanges, pDestDescriptorRangeStarts, pDestDescriptorRangeSizes, NumSrcDescriptorRanges, pSrcDescriptorRangeStarts, pSrcDescriptorRangeSizes, DescriptorHeapsType, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::CopyDescriptorsSimple
 */

struct npt_command_ID3D12Device_CopyDescriptorsSimple {
    void *_self;
    UINT NumDescriptors;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptorRangeStart;
    D3D12_CPU_DESCRIPTOR_HANDLE SrcDescriptorRangeStart;
    D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType;
};

static inline size_t
npt_sizeof_ID3D12Device_CopyDescriptorsSimple(const UINT NumDescriptors,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptorRangeStart,const D3D12_CPU_DESCRIPTOR_HANDLE SrcDescriptorRangeStart,const D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumDescriptors, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DestDescriptorRangeStart, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&SrcDescriptorRangeStart, max_mode);
    cmd_size += npt_sizeof_D3D12_DESCRIPTOR_HEAP_TYPE(&DescriptorHeapsType, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CopyDescriptorsSimple(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT NumDescriptors,
                                              D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptorRangeStart,
                                              D3D12_CPU_DESCRIPTOR_HANDLE SrcDescriptorRangeStart,
                                              D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CopyDescriptorsSimple(NumDescriptors,DestDescriptorRangeStart,SrcDescriptorRangeStart,DescriptorHeapsType);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CopyDescriptorsSimple),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumDescriptors);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DestDescriptorRangeStart);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &SrcDescriptorRangeStart);
    npt_encode_D3D12_DESCRIPTOR_HEAP_TYPE(enc, &DescriptorHeapsType);
}

static inline size_t
npt_sizeof_ID3D12Device_CopyDescriptorsSimple_reply(const UINT NumDescriptors,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptorRangeStart,const D3D12_CPU_DESCRIPTOR_HANDLE SrcDescriptorRangeStart,const D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumDescriptors (input only) */
/* skip DestDescriptorRangeStart (input only) */
/* skip SrcDescriptorRangeStart (input only) */
/* skip DescriptorHeapsType (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device_CopyDescriptorsSimple(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT NumDescriptors,
                                              D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptorRangeStart,
                                              D3D12_CPU_DESCRIPTOR_HANDLE SrcDescriptorRangeStart,
                                              D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CopyDescriptorsSimple(NumDescriptors,DestDescriptorRangeStart,SrcDescriptorRangeStart,DescriptorHeapsType);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CopyDescriptorsSimple_reply(NumDescriptors,DestDescriptorRangeStart,SrcDescriptorRangeStart,DescriptorHeapsType)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CopyDescriptorsSimple(enc, cmd_flags, object_id, NumDescriptors, DestDescriptorRangeStart, SrcDescriptorRangeStart, DescriptorHeapsType);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CopyDescriptorsSimple(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT NumDescriptors,
                                             D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptorRangeStart,
                                             D3D12_CPU_DESCRIPTOR_HANDLE SrcDescriptorRangeStart,
                                             D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CopyDescriptorsSimple(ring, 0, object_id, NumDescriptors, DestDescriptorRangeStart, SrcDescriptorRangeStart, DescriptorHeapsType, &submit);
}

static inline void
npt_call_ID3D12Device_CopyDescriptorsSimple(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT NumDescriptors,
                                            D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptorRangeStart,
                                            D3D12_CPU_DESCRIPTOR_HANDLE SrcDescriptorRangeStart,
                                            D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CopyDescriptorsSimple(ring, NPT_CMD_FLAG_REPLY, object_id, NumDescriptors, DestDescriptorRangeStart, SrcDescriptorRangeStart, DescriptorHeapsType, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::GetResourceAllocationInfo
 */

struct npt_command_ID3D12Device_GetResourceAllocationInfo {
    void *_self;
    UINT visibleMask;
    UINT numResourceDescs;
    const D3D12_RESOURCE_DESC * pResourceDescs;
    D3D12_RESOURCE_ALLOCATION_INFO ret;
};

static inline size_t
npt_sizeof_ID3D12Device_GetResourceAllocationInfo(const UINT visibleMask,const UINT numResourceDescs,const D3D12_RESOURCE_DESC * pResourceDescs)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&visibleMask, max_mode);
    cmd_size += npt_sizeof_UINT(&numResourceDescs, max_mode);
    cmd_size += npt_sizeof_array_count(pResourceDescs ? numResourceDescs : 0);
    for (uint32_t _i = 0; _i < (pResourceDescs ? numResourceDescs : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(&pResourceDescs[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_GetResourceAllocationInfo(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT visibleMask,
                                                  UINT numResourceDescs,
                                                  const D3D12_RESOURCE_DESC * pResourceDescs)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_GetResourceAllocationInfo(visibleMask,numResourceDescs,pResourceDescs);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetResourceAllocationInfo),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &visibleMask);
    npt_encode_UINT(enc, &numResourceDescs);
    if (pResourceDescs) {
        npt_encode_array_count(enc, numResourceDescs);
        for (uint32_t _i = 0; _i < (uint32_t)(numResourceDescs); _i++)
            npt_encode_D3D12_RESOURCE_DESC(enc, &pResourceDescs[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12Device_GetResourceAllocationInfo_reply(const UINT visibleMask,const UINT numResourceDescs,const D3D12_RESOURCE_DESC * pResourceDescs)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_RESOURCE_ALLOCATION_INFO(&(const D3D12_RESOURCE_ALLOCATION_INFO){0}, 1);
/* skip visibleMask (input only) */
/* skip numResourceDescs (input only) */
/* skip pResourceDescs (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_GetResourceAllocationInfo_reply(struct npt_cs_decoder *dec,
                                                        D3D12_RESOURCE_ALLOCATION_INFO *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_GetResourceAllocationInfo))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_GetResourceAllocationInfo reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_GetResourceAllocationInfo));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_RESOURCE_ALLOCATION_INFO(dec, ret);
}

static inline void
npt_submit_ID3D12Device_GetResourceAllocationInfo(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT visibleMask,
                                                  UINT numResourceDescs,
                                                  const D3D12_RESOURCE_DESC * pResourceDescs,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_GetResourceAllocationInfo(visibleMask,numResourceDescs,pResourceDescs);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_GetResourceAllocationInfo_reply(visibleMask,numResourceDescs,pResourceDescs)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_GetResourceAllocationInfo(enc, cmd_flags, object_id, visibleMask, numResourceDescs, pResourceDescs);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_GetResourceAllocationInfo(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT visibleMask,
                                                 UINT numResourceDescs,
                                                 const D3D12_RESOURCE_DESC * pResourceDescs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetResourceAllocationInfo(ring, 0, object_id, visibleMask, numResourceDescs, pResourceDescs, &submit);
}

static inline D3D12_RESOURCE_ALLOCATION_INFO
npt_call_ID3D12Device_GetResourceAllocationInfo(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT visibleMask,
                                                UINT numResourceDescs,
                                                const D3D12_RESOURCE_DESC * pResourceDescs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetResourceAllocationInfo(ring, NPT_CMD_FLAG_REPLY, object_id, visibleMask, numResourceDescs, pResourceDescs, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_RESOURCE_ALLOCATION_INFO _ret;
        npt_decode_ID3D12Device_GetResourceAllocationInfo_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_RESOURCE_ALLOCATION_INFO){0};
    }
}

/*
 * ID3D12Device::GetCustomHeapProperties
 */

struct npt_command_ID3D12Device_GetCustomHeapProperties {
    void *_self;
    UINT nodeMask;
    D3D12_HEAP_TYPE heapType;
    D3D12_HEAP_PROPERTIES ret;
};

static inline size_t
npt_sizeof_ID3D12Device_GetCustomHeapProperties(const UINT nodeMask,const D3D12_HEAP_TYPE heapType)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&nodeMask, max_mode);
    cmd_size += npt_sizeof_D3D12_HEAP_TYPE(&heapType, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_GetCustomHeapProperties(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT nodeMask,
                                                D3D12_HEAP_TYPE heapType)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_GetCustomHeapProperties(nodeMask,heapType);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetCustomHeapProperties),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &nodeMask);
    npt_encode_D3D12_HEAP_TYPE(enc, &heapType);
}

static inline size_t
npt_sizeof_ID3D12Device_GetCustomHeapProperties_reply(const UINT nodeMask,const D3D12_HEAP_TYPE heapType)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_HEAP_PROPERTIES(&(const D3D12_HEAP_PROPERTIES){0}, 1);
/* skip nodeMask (input only) */
/* skip heapType (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_GetCustomHeapProperties_reply(struct npt_cs_decoder *dec,
                                                      D3D12_HEAP_PROPERTIES *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_GetCustomHeapProperties))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_GetCustomHeapProperties reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_GetCustomHeapProperties));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_HEAP_PROPERTIES(dec, ret);
}

static inline void
npt_submit_ID3D12Device_GetCustomHeapProperties(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT nodeMask,
                                                D3D12_HEAP_TYPE heapType,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_GetCustomHeapProperties(nodeMask,heapType);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_GetCustomHeapProperties_reply(nodeMask,heapType)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_GetCustomHeapProperties(enc, cmd_flags, object_id, nodeMask, heapType);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_GetCustomHeapProperties(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               UINT nodeMask,
                                               D3D12_HEAP_TYPE heapType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetCustomHeapProperties(ring, 0, object_id, nodeMask, heapType, &submit);
}

static inline D3D12_HEAP_PROPERTIES
npt_call_ID3D12Device_GetCustomHeapProperties(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT nodeMask,
                                              D3D12_HEAP_TYPE heapType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetCustomHeapProperties(ring, NPT_CMD_FLAG_REPLY, object_id, nodeMask, heapType, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_HEAP_PROPERTIES _ret;
        npt_decode_ID3D12Device_GetCustomHeapProperties_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_HEAP_PROPERTIES){0};
    }
}

/*
 * ID3D12Device::CreateCommittedResource
 */

struct npt_command_ID3D12Device_CreateCommittedResource {
    void *_self;
    const D3D12_HEAP_PROPERTIES * pHeapProperties;
    D3D12_HEAP_FLAGS HeapFlags;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialResourceState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    const IID * riidResource;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateCommittedResource(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS HeapFlags,const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialResourceState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const IID * riidResource,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pHeapProperties);
    if (pHeapProperties)
        cmd_size += npt_sizeof_D3D12_HEAP_PROPERTIES(pHeapProperties, max_mode);
    cmd_size += npt_sizeof_D3D12_HEAP_FLAGS(&HeapFlags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOURCE_STATES(&InitialResourceState, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riidResource);
    if (riidResource)
        cmd_size += npt_sizeof_IID(riidResource, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateCommittedResource(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                D3D12_HEAP_FLAGS HeapFlags,
                                                const D3D12_RESOURCE_DESC * pDesc,
                                                D3D12_RESOURCE_STATES InitialResourceState,
                                                const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                const IID * riidResource,
                                                void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateCommittedResource(pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,riidResource,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommittedResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pHeapProperties))
        npt_encode_D3D12_HEAP_PROPERTIES(enc, pHeapProperties);
    npt_encode_D3D12_HEAP_FLAGS(enc, &HeapFlags);
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC(enc, pDesc);
    npt_encode_D3D12_RESOURCE_STATES(enc, &InitialResourceState);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    if (npt_encode_simple_pointer(enc, riidResource))
        npt_encode_IID(enc, riidResource);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateCommittedResource_reply(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS HeapFlags,const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialResourceState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const IID * riidResource,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pHeapProperties (input only) */
/* skip HeapFlags (input only) */
/* skip pDesc (input only) */
/* skip InitialResourceState (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip riidResource (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateCommittedResource_reply(struct npt_cs_decoder *dec,
                                                      void ** ppvResource,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateCommittedResource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateCommittedResource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateCommittedResource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateCommittedResource(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                D3D12_HEAP_FLAGS HeapFlags,
                                                const D3D12_RESOURCE_DESC * pDesc,
                                                D3D12_RESOURCE_STATES InitialResourceState,
                                                const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                const IID * riidResource,
                                                void ** ppvResource,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateCommittedResource(pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,riidResource,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateCommittedResource_reply(pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,riidResource,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateCommittedResource(enc, cmd_flags, object_id, pHeapProperties, HeapFlags, pDesc, InitialResourceState, pOptimizedClearValue, riidResource, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateCommittedResource(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                               D3D12_HEAP_FLAGS HeapFlags,
                                               const D3D12_RESOURCE_DESC * pDesc,
                                               D3D12_RESOURCE_STATES InitialResourceState,
                                               const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                               const IID * riidResource,
                                               void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommittedResource(ring, 0, object_id, pHeapProperties, HeapFlags, pDesc, InitialResourceState, pOptimizedClearValue, riidResource, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateCommittedResource(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                              D3D12_HEAP_FLAGS HeapFlags,
                                              const D3D12_RESOURCE_DESC * pDesc,
                                              D3D12_RESOURCE_STATES InitialResourceState,
                                              const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                              const IID * riidResource,
                                              void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommittedResource(ring, NPT_CMD_FLAG_REPLY, object_id, pHeapProperties, HeapFlags, pDesc, InitialResourceState, pOptimizedClearValue, riidResource, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateCommittedResource_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateHeap
 */

struct npt_command_ID3D12Device_CreateHeap {
    void *_self;
    const D3D12_HEAP_DESC * pDesc;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateHeap(const D3D12_HEAP_DESC * pDesc,const IID * riid,void ** ppvHeap)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_HEAP_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvHeap: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateHeap(struct npt_cs_encoder *enc,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   const D3D12_HEAP_DESC * pDesc,
                                   const IID * riid,
                                   void ** ppvHeap)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateHeap(pDesc,riid,ppvHeap);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateHeap),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_HEAP_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvHeap = 0;
    if (ppvHeap) {
        _gid_ppvHeap = npt_com_allocate_next_id();
        *ppvHeap = (void *)(uintptr_t)_gid_ppvHeap;
    }
    npt_encode_uint64_t(enc, &_gid_ppvHeap);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateHeap_reply(const D3D12_HEAP_DESC * pDesc,const IID * riid,void ** ppvHeap)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppvHeap (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateHeap_reply(struct npt_cs_decoder *dec,
                                         void ** ppvHeap,
                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateHeap))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateHeap reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateHeap));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvHeap (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateHeap(struct npt_ring *ring,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   const D3D12_HEAP_DESC * pDesc,
                                   const IID * riid,
                                   void ** ppvHeap,
                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateHeap(pDesc,riid,ppvHeap);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateHeap_reply(pDesc,riid,ppvHeap)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateHeap(enc, cmd_flags, object_id, pDesc, riid, ppvHeap);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateHeap(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  const D3D12_HEAP_DESC * pDesc,
                                  const IID * riid,
                                  void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateHeap(ring, 0, object_id, pDesc, riid, ppvHeap, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateHeap(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 const D3D12_HEAP_DESC * pDesc,
                                 const IID * riid,
                                 void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateHeap(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppvHeap, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateHeap_reply(dec, ppvHeap, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreatePlacedResource
 */

struct npt_command_ID3D12Device_CreatePlacedResource {
    void *_self;
    ID3D12Heap * pHeap;
    UINT64 HeapOffset;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreatePlacedResource(const ID3D12Heap * pHeap,const UINT64 HeapOffset,const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const IID * riid,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&HeapOffset, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOURCE_STATES(&InitialState, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreatePlacedResource(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             ID3D12Heap * pHeap,
                                             UINT64 HeapOffset,
                                             const D3D12_RESOURCE_DESC * pDesc,
                                             D3D12_RESOURCE_STATES InitialState,
                                             const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                             const IID * riid,
                                             void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreatePlacedResource(pHeap,HeapOffset,pDesc,InitialState,pOptimizedClearValue,riid,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreatePlacedResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pHeap));
    npt_encode_UINT64(enc, &HeapOffset);
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC(enc, pDesc);
    npt_encode_D3D12_RESOURCE_STATES(enc, &InitialState);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreatePlacedResource_reply(const ID3D12Heap * pHeap,const UINT64 HeapOffset,const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const IID * riid,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pHeap (input only) */
/* skip HeapOffset (input only) */
/* skip pDesc (input only) */
/* skip InitialState (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip riid (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreatePlacedResource_reply(struct npt_cs_decoder *dec,
                                                   void ** ppvResource,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreatePlacedResource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreatePlacedResource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreatePlacedResource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreatePlacedResource(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             ID3D12Heap * pHeap,
                                             UINT64 HeapOffset,
                                             const D3D12_RESOURCE_DESC * pDesc,
                                             D3D12_RESOURCE_STATES InitialState,
                                             const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                             const IID * riid,
                                             void ** ppvResource,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreatePlacedResource(pHeap,HeapOffset,pDesc,InitialState,pOptimizedClearValue,riid,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreatePlacedResource_reply(pHeap,HeapOffset,pDesc,InitialState,pOptimizedClearValue,riid,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreatePlacedResource(enc, cmd_flags, object_id, pHeap, HeapOffset, pDesc, InitialState, pOptimizedClearValue, riid, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreatePlacedResource(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            ID3D12Heap * pHeap,
                                            UINT64 HeapOffset,
                                            const D3D12_RESOURCE_DESC * pDesc,
                                            D3D12_RESOURCE_STATES InitialState,
                                            const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                            const IID * riid,
                                            void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreatePlacedResource(ring, 0, object_id, pHeap, HeapOffset, pDesc, InitialState, pOptimizedClearValue, riid, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreatePlacedResource(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D12Heap * pHeap,
                                           UINT64 HeapOffset,
                                           const D3D12_RESOURCE_DESC * pDesc,
                                           D3D12_RESOURCE_STATES InitialState,
                                           const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                           const IID * riid,
                                           void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreatePlacedResource(ring, NPT_CMD_FLAG_REPLY, object_id, pHeap, HeapOffset, pDesc, InitialState, pOptimizedClearValue, riid, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreatePlacedResource_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateReservedResource
 */

struct npt_command_ID3D12Device_CreateReservedResource {
    void *_self;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateReservedResource(const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const IID * riid,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOURCE_STATES(&InitialState, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateReservedResource(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const D3D12_RESOURCE_DESC * pDesc,
                                               D3D12_RESOURCE_STATES InitialState,
                                               const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                               const IID * riid,
                                               void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateReservedResource(pDesc,InitialState,pOptimizedClearValue,riid,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateReservedResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC(enc, pDesc);
    npt_encode_D3D12_RESOURCE_STATES(enc, &InitialState);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateReservedResource_reply(const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const IID * riid,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip InitialState (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip riid (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateReservedResource_reply(struct npt_cs_decoder *dec,
                                                     void ** ppvResource,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateReservedResource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateReservedResource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateReservedResource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateReservedResource(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const D3D12_RESOURCE_DESC * pDesc,
                                               D3D12_RESOURCE_STATES InitialState,
                                               const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                               const IID * riid,
                                               void ** ppvResource,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateReservedResource(pDesc,InitialState,pOptimizedClearValue,riid,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateReservedResource_reply(pDesc,InitialState,pOptimizedClearValue,riid,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateReservedResource(enc, cmd_flags, object_id, pDesc, InitialState, pOptimizedClearValue, riid, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateReservedResource(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const D3D12_RESOURCE_DESC * pDesc,
                                              D3D12_RESOURCE_STATES InitialState,
                                              const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                              const IID * riid,
                                              void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateReservedResource(ring, 0, object_id, pDesc, InitialState, pOptimizedClearValue, riid, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateReservedResource(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const D3D12_RESOURCE_DESC * pDesc,
                                             D3D12_RESOURCE_STATES InitialState,
                                             const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                             const IID * riid,
                                             void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateReservedResource(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, InitialState, pOptimizedClearValue, riid, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateReservedResource_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateSharedHandle
 */

struct npt_command_ID3D12Device_CreateSharedHandle {
    void *_self;
    ID3D12DeviceChild * pObject;
    const SECURITY_ATTRIBUTES * pAttributes;
    DWORD Access;
    const WCHAR * Name;
    HANDLE * pHandle;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateSharedHandle(const ID3D12DeviceChild * pObject,const SECURITY_ATTRIBUTES * pAttributes,const DWORD Access,const WCHAR * Name,const HANDLE * pHandle)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    /* pAttributes: non-serializable, skipped */
    cmd_size += npt_sizeof_DWORD(&Access, max_mode);
    if (Name) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)Name) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(Name, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip pHandle (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateSharedHandle(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D12DeviceChild * pObject,
                                           const SECURITY_ATTRIBUTES * pAttributes,
                                           DWORD Access,
                                           const WCHAR * Name,
                                           HANDLE * pHandle)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateSharedHandle(pObject,pAttributes,Access,Name,pHandle);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateSharedHandle),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pObject));
    /* pAttributes: non-serializable, skipped */
    npt_encode_DWORD(enc, &Access);
    if (Name) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)Name) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, Name, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pHandle (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device_CreateSharedHandle_reply(const ID3D12DeviceChild * pObject,const SECURITY_ATTRIBUTES * pAttributes,const DWORD Access,const WCHAR * Name,const HANDLE * pHandle)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pObject (input only) */
/* skip pAttributes (input only) */
/* skip Access (input only) */
/* skip Name (input only) */
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateSharedHandle_reply(struct npt_cs_decoder *dec,
                                                 HANDLE * pHandle,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateSharedHandle))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateSharedHandle reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateSharedHandle));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        if (pHandle)
            *pHandle = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
}

static inline void
npt_submit_ID3D12Device_CreateSharedHandle(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D12DeviceChild * pObject,
                                           const SECURITY_ATTRIBUTES * pAttributes,
                                           DWORD Access,
                                           const WCHAR * Name,
                                           HANDLE * pHandle,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateSharedHandle(pObject,pAttributes,Access,Name,pHandle);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateSharedHandle_reply(pObject,pAttributes,Access,Name,pHandle)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateSharedHandle(enc, cmd_flags, object_id, pObject, pAttributes, Access, Name, pHandle);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateSharedHandle(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D12DeviceChild * pObject,
                                          const SECURITY_ATTRIBUTES * pAttributes,
                                          DWORD Access,
                                          const WCHAR * Name,
                                          HANDLE * pHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateSharedHandle(ring, 0, object_id, pObject, pAttributes, Access, Name, pHandle, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateSharedHandle(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D12DeviceChild * pObject,
                                         const SECURITY_ATTRIBUTES * pAttributes,
                                         DWORD Access,
                                         const WCHAR * Name,
                                         HANDLE * pHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateSharedHandle(ring, NPT_CMD_FLAG_REPLY, object_id, pObject, pAttributes, Access, Name, pHandle, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateSharedHandle_reply(dec, pHandle, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::OpenSharedHandle
 */

struct npt_command_ID3D12Device_OpenSharedHandle {
    void *_self;
    HANDLE NTHandle;
    const IID * riid;
    void ** ppvObj;
    /* Shadow: the guest-allocated id for ppvObj arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvObj -> *ppvObj} in the
     * context object table. */
    uint64_t _guest_id_ppvObj;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_OpenSharedHandle(const HANDLE NTHandle,const IID * riid,void ** ppvObj)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvObj: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_OpenSharedHandle(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         HANDLE NTHandle,
                                         const IID * riid,
                                         void ** ppvObj)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_OpenSharedHandle(NTHandle,riid,ppvObj);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_OpenSharedHandle),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)NTHandle));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvObj = 0;
    if (ppvObj) {
        _gid_ppvObj = npt_com_allocate_next_id();
        *ppvObj = (void *)(uintptr_t)_gid_ppvObj;
    }
    npt_encode_uint64_t(enc, &_gid_ppvObj);
}
}

static inline size_t
npt_sizeof_ID3D12Device_OpenSharedHandle_reply(const HANDLE NTHandle,const IID * riid,void ** ppvObj)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NTHandle (input only) */
/* skip riid (input only) */
/* skip ppvObj (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_OpenSharedHandle_reply(struct npt_cs_decoder *dec,
                                               void ** ppvObj,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_OpenSharedHandle))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_OpenSharedHandle reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_OpenSharedHandle));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvObj (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_OpenSharedHandle(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         HANDLE NTHandle,
                                         const IID * riid,
                                         void ** ppvObj,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_OpenSharedHandle(NTHandle,riid,ppvObj);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_OpenSharedHandle_reply(NTHandle,riid,ppvObj)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_OpenSharedHandle(enc, cmd_flags, object_id, NTHandle, riid, ppvObj);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_OpenSharedHandle(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        HANDLE NTHandle,
                                        const IID * riid,
                                        void ** ppvObj)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_OpenSharedHandle(ring, 0, object_id, NTHandle, riid, ppvObj, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_OpenSharedHandle(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       HANDLE NTHandle,
                                       const IID * riid,
                                       void ** ppvObj)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_OpenSharedHandle(ring, NPT_CMD_FLAG_REPLY, object_id, NTHandle, riid, ppvObj, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_OpenSharedHandle_reply(dec, ppvObj, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::OpenSharedHandleByName
 */

struct npt_command_ID3D12Device_OpenSharedHandleByName {
    void *_self;
    const WCHAR * Name;
    DWORD Access;
    HANDLE * pNTHandle;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_OpenSharedHandleByName(const WCHAR * Name,const DWORD Access,const HANDLE * pNTHandle)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (Name) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)Name) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(Name, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_DWORD(&Access, max_mode);
/* skip pNTHandle (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_OpenSharedHandleByName(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const WCHAR * Name,
                                               DWORD Access,
                                               HANDLE * pNTHandle)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_OpenSharedHandleByName(Name,Access,pNTHandle);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_OpenSharedHandleByName),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (Name) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)Name) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, Name, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_DWORD(enc, &Access);
/* skip pNTHandle (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device_OpenSharedHandleByName_reply(const WCHAR * Name,const DWORD Access,const HANDLE * pNTHandle)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Name (input only) */
/* skip Access (input only) */
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_OpenSharedHandleByName_reply(struct npt_cs_decoder *dec,
                                                     HANDLE * pNTHandle,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_OpenSharedHandleByName))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_OpenSharedHandleByName reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_OpenSharedHandleByName));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        if (pNTHandle)
            *pNTHandle = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
}

static inline void
npt_submit_ID3D12Device_OpenSharedHandleByName(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const WCHAR * Name,
                                               DWORD Access,
                                               HANDLE * pNTHandle,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_OpenSharedHandleByName(Name,Access,pNTHandle);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_OpenSharedHandleByName_reply(Name,Access,pNTHandle)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_OpenSharedHandleByName(enc, cmd_flags, object_id, Name, Access, pNTHandle);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_OpenSharedHandleByName(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const WCHAR * Name,
                                              DWORD Access,
                                              HANDLE * pNTHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_OpenSharedHandleByName(ring, 0, object_id, Name, Access, pNTHandle, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_OpenSharedHandleByName(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const WCHAR * Name,
                                             DWORD Access,
                                             HANDLE * pNTHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_OpenSharedHandleByName(ring, NPT_CMD_FLAG_REPLY, object_id, Name, Access, pNTHandle, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_OpenSharedHandleByName_reply(dec, pNTHandle, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::MakeResident
 */

struct npt_command_ID3D12Device_MakeResident {
    void *_self;
    UINT NumObjects;
    ID3D12Pageable ** ppObjects;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_MakeResident(const UINT NumObjects,ID3D12Pageable ** ppObjects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumObjects, max_mode);
    cmd_size += npt_sizeof_array_count(NumObjects);
    cmd_size += sizeof(uint64_t) * NumObjects;
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_MakeResident(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     UINT NumObjects,
                                     ID3D12Pageable ** ppObjects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_MakeResident(NumObjects,ppObjects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_MakeResident),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumObjects);
    if (ppObjects) {
        npt_encode_array_count(enc, NumObjects);
        for (uint32_t _i = 0; _i < (uint32_t)NumObjects; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppObjects[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12Device_MakeResident_reply(const UINT NumObjects,ID3D12Pageable ** ppObjects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumObjects (input only) */
/* skip ppObjects (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_MakeResident_reply(struct npt_cs_decoder *dec,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_MakeResident))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_MakeResident reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_MakeResident));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device_MakeResident(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     UINT NumObjects,
                                     ID3D12Pageable ** ppObjects,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_MakeResident(NumObjects,ppObjects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_MakeResident_reply(NumObjects,ppObjects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_MakeResident(enc, cmd_flags, object_id, NumObjects, ppObjects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_MakeResident(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    UINT NumObjects,
                                    ID3D12Pageable ** ppObjects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_MakeResident(ring, 0, object_id, NumObjects, ppObjects, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_MakeResident(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   UINT NumObjects,
                                   ID3D12Pageable ** ppObjects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_MakeResident(ring, NPT_CMD_FLAG_REPLY, object_id, NumObjects, ppObjects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_MakeResident_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::Evict
 */

struct npt_command_ID3D12Device_Evict {
    void *_self;
    UINT NumObjects;
    ID3D12Pageable ** ppObjects;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_Evict(const UINT NumObjects,ID3D12Pageable ** ppObjects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumObjects, max_mode);
    cmd_size += npt_sizeof_array_count(NumObjects);
    cmd_size += sizeof(uint64_t) * NumObjects;
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_Evict(struct npt_cs_encoder *enc,
                              uint32_t cmd_flags,
                              npt_object_id object_id,
                              UINT NumObjects,
                              ID3D12Pageable ** ppObjects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_Evict(NumObjects,ppObjects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_Evict),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumObjects);
    if (ppObjects) {
        npt_encode_array_count(enc, NumObjects);
        for (uint32_t _i = 0; _i < (uint32_t)NumObjects; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppObjects[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12Device_Evict_reply(const UINT NumObjects,ID3D12Pageable ** ppObjects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumObjects (input only) */
/* skip ppObjects (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_Evict_reply(struct npt_cs_decoder *dec,
                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_Evict))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_Evict reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_Evict));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device_Evict(struct npt_ring *ring,
                              uint32_t cmd_flags,
                              npt_object_id object_id,
                              UINT NumObjects,
                              ID3D12Pageable ** ppObjects,
                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_Evict(NumObjects,ppObjects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_Evict_reply(NumObjects,ppObjects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_Evict(enc, cmd_flags, object_id, NumObjects, ppObjects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_Evict(struct npt_ring *ring,
                             npt_object_id object_id,
                             UINT NumObjects,
                             ID3D12Pageable ** ppObjects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_Evict(ring, 0, object_id, NumObjects, ppObjects, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_Evict(struct npt_ring *ring,
                            npt_object_id object_id,
                            UINT NumObjects,
                            ID3D12Pageable ** ppObjects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_Evict(ring, NPT_CMD_FLAG_REPLY, object_id, NumObjects, ppObjects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_Evict_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateFence
 */

struct npt_command_ID3D12Device_CreateFence {
    void *_self;
    UINT64 InitialValue;
    D3D12_FENCE_FLAGS Flags;
    const IID * riid;
    void ** ppFence;
    /* Shadow: the guest-allocated id for ppFence arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppFence -> *ppFence} in the
     * context object table. */
    uint64_t _guest_id_ppFence;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateFence(const UINT64 InitialValue,const D3D12_FENCE_FLAGS Flags,const IID * riid,void ** ppFence)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT64(&InitialValue, max_mode);
    cmd_size += npt_sizeof_D3D12_FENCE_FLAGS(&Flags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppFence: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateFence(struct npt_cs_encoder *enc,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT64 InitialValue,
                                    D3D12_FENCE_FLAGS Flags,
                                    const IID * riid,
                                    void ** ppFence)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateFence(InitialValue,Flags,riid,ppFence);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateFence),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT64(enc, &InitialValue);
    npt_encode_D3D12_FENCE_FLAGS(enc, &Flags);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppFence = 0;
    if (ppFence) {
        _gid_ppFence = npt_com_allocate_next_id();
        *ppFence = (void *)(uintptr_t)_gid_ppFence;
    }
    npt_encode_uint64_t(enc, &_gid_ppFence);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateFence_reply(const UINT64 InitialValue,const D3D12_FENCE_FLAGS Flags,const IID * riid,void ** ppFence)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip InitialValue (input only) */
/* skip Flags (input only) */
/* skip riid (input only) */
/* skip ppFence (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateFence_reply(struct npt_cs_decoder *dec,
                                          void ** ppFence,
                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateFence))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateFence reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateFence));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppFence (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateFence(struct npt_ring *ring,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT64 InitialValue,
                                    D3D12_FENCE_FLAGS Flags,
                                    const IID * riid,
                                    void ** ppFence,
                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateFence(InitialValue,Flags,riid,ppFence);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateFence_reply(InitialValue,Flags,riid,ppFence)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateFence(enc, cmd_flags, object_id, InitialValue, Flags, riid, ppFence);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateFence(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   UINT64 InitialValue,
                                   D3D12_FENCE_FLAGS Flags,
                                   const IID * riid,
                                   void ** ppFence)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateFence(ring, 0, object_id, InitialValue, Flags, riid, ppFence, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateFence(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  UINT64 InitialValue,
                                  D3D12_FENCE_FLAGS Flags,
                                  const IID * riid,
                                  void ** ppFence)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateFence(ring, NPT_CMD_FLAG_REPLY, object_id, InitialValue, Flags, riid, ppFence, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateFence_reply(dec, ppFence, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::GetDeviceRemovedReason
 */

struct npt_command_ID3D12Device_GetDeviceRemovedReason {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_GetDeviceRemovedReason(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_GetDeviceRemovedReason(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_GetDeviceRemovedReason();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetDeviceRemovedReason),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12Device_GetDeviceRemovedReason_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_GetDeviceRemovedReason_reply(struct npt_cs_decoder *dec,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_GetDeviceRemovedReason))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_GetDeviceRemovedReason reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_GetDeviceRemovedReason));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device_GetDeviceRemovedReason(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_GetDeviceRemovedReason();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_GetDeviceRemovedReason_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_GetDeviceRemovedReason(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_GetDeviceRemovedReason(struct npt_ring *ring,
                                              npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetDeviceRemovedReason(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_GetDeviceRemovedReason(struct npt_ring *ring,
                                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetDeviceRemovedReason(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_GetDeviceRemovedReason_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::GetCopyableFootprints
 */

struct npt_command_ID3D12Device_GetCopyableFootprints {
    void *_self;
    const D3D12_RESOURCE_DESC * pResourceDesc;
    UINT FirstSubresource;
    UINT NumSubresources;
    UINT64 BaseOffset;
    D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts;
    UINT * pNumRows;
    UINT64 * pRowSizeInBytes;
    UINT64 * pTotalBytes;
};

static inline size_t
npt_sizeof_ID3D12Device_GetCopyableFootprints(const D3D12_RESOURCE_DESC * pResourceDesc,const UINT FirstSubresource,const UINT NumSubresources,const UINT64 BaseOffset,const D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,const UINT * pNumRows,const UINT64 * pRowSizeInBytes,const UINT64 * pTotalBytes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pResourceDesc);
    if (pResourceDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(pResourceDesc, max_mode);
    cmd_size += npt_sizeof_UINT(&FirstSubresource, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSubresources, max_mode);
    cmd_size += npt_sizeof_UINT64(&BaseOffset, max_mode);
/* skip pLayouts (output only) */
/* skip pNumRows (output only) */
/* skip pRowSizeInBytes (output only) */
/* skip pTotalBytes (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_GetCopyableFootprints(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const D3D12_RESOURCE_DESC * pResourceDesc,
                                              UINT FirstSubresource,
                                              UINT NumSubresources,
                                              UINT64 BaseOffset,
                                              D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                              UINT * pNumRows,
                                              UINT64 * pRowSizeInBytes,
                                              UINT64 * pTotalBytes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_GetCopyableFootprints(pResourceDesc,FirstSubresource,NumSubresources,BaseOffset,pLayouts,pNumRows,pRowSizeInBytes,pTotalBytes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetCopyableFootprints),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pResourceDesc))
        npt_encode_D3D12_RESOURCE_DESC(enc, pResourceDesc);
    npt_encode_UINT(enc, &FirstSubresource);
    npt_encode_UINT(enc, &NumSubresources);
    npt_encode_UINT64(enc, &BaseOffset);
/* skip pLayouts (output only) */
/* skip pNumRows (output only) */
/* skip pRowSizeInBytes (output only) */
/* skip pTotalBytes (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device_GetCopyableFootprints_reply(const D3D12_RESOURCE_DESC * pResourceDesc,const UINT FirstSubresource,const UINT NumSubresources,const UINT64 BaseOffset,const D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,const UINT * pNumRows,const UINT64 * pRowSizeInBytes,const UINT64 * pTotalBytes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResourceDesc (input only) */
/* skip FirstSubresource (input only) */
/* skip NumSubresources (input only) */
/* skip BaseOffset (input only) */
    cmd_size += npt_sizeof_array_count(pLayouts ? NumSubresources : 0);
    if (pLayouts)
        cmd_size += (size_t)(NumSubresources) * npt_sizeof_D3D12_PLACED_SUBRESOURCE_FOOTPRINT(&(const D3D12_PLACED_SUBRESOURCE_FOOTPRINT){0}, 1);
    cmd_size += npt_sizeof_array_count(pNumRows ? NumSubresources : 0);
    if (pNumRows)
        cmd_size += npt_sizeof_UINT_array(pNumRows, NumSubresources);
    cmd_size += npt_sizeof_array_count(pRowSizeInBytes ? NumSubresources : 0);
    if (pRowSizeInBytes)
        cmd_size += npt_sizeof_UINT64_array(pRowSizeInBytes, NumSubresources);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT64(&(const UINT64){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_GetCopyableFootprints_reply(struct npt_cs_decoder *dec,
                                                    D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                                    UINT * pNumRows,
                                                    UINT64 * pRowSizeInBytes,
                                                    UINT64 * pTotalBytes)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_GetCopyableFootprints))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_GetCopyableFootprints reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_GetCopyableFootprints));
        return;
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pLayouts) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D12_PLACED_SUBRESOURCE_FOOTPRINT(dec, (D3D12_PLACED_SUBRESOURCE_FOOTPRINT *)&pLayouts[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pNumRows)
            npt_decode_UINT_array(dec, (UINT *)pNumRows, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pRowSizeInBytes)
            npt_decode_UINT64_array(dec, (UINT64 *)pRowSizeInBytes, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pTotalBytes)
            npt_decode_UINT64(dec, (UINT64 *)pTotalBytes);
    }
}

static inline void
npt_submit_ID3D12Device_GetCopyableFootprints(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const D3D12_RESOURCE_DESC * pResourceDesc,
                                              UINT FirstSubresource,
                                              UINT NumSubresources,
                                              UINT64 BaseOffset,
                                              D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                              UINT * pNumRows,
                                              UINT64 * pRowSizeInBytes,
                                              UINT64 * pTotalBytes,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_GetCopyableFootprints(pResourceDesc,FirstSubresource,NumSubresources,BaseOffset,pLayouts,pNumRows,pRowSizeInBytes,pTotalBytes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_GetCopyableFootprints_reply(pResourceDesc,FirstSubresource,NumSubresources,BaseOffset,pLayouts,pNumRows,pRowSizeInBytes,pTotalBytes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_GetCopyableFootprints(enc, cmd_flags, object_id, pResourceDesc, FirstSubresource, NumSubresources, BaseOffset, pLayouts, pNumRows, pRowSizeInBytes, pTotalBytes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_GetCopyableFootprints(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const D3D12_RESOURCE_DESC * pResourceDesc,
                                             UINT FirstSubresource,
                                             UINT NumSubresources,
                                             UINT64 BaseOffset,
                                             D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                             UINT * pNumRows,
                                             UINT64 * pRowSizeInBytes,
                                             UINT64 * pTotalBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetCopyableFootprints(ring, 0, object_id, pResourceDesc, FirstSubresource, NumSubresources, BaseOffset, pLayouts, pNumRows, pRowSizeInBytes, pTotalBytes, &submit);
}

static inline void
npt_call_ID3D12Device_GetCopyableFootprints(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const D3D12_RESOURCE_DESC * pResourceDesc,
                                            UINT FirstSubresource,
                                            UINT NumSubresources,
                                            UINT64 BaseOffset,
                                            D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                            UINT * pNumRows,
                                            UINT64 * pRowSizeInBytes,
                                            UINT64 * pTotalBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetCopyableFootprints(ring, NPT_CMD_FLAG_REPLY, object_id, pResourceDesc, FirstSubresource, NumSubresources, BaseOffset, pLayouts, pNumRows, pRowSizeInBytes, pTotalBytes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D12Device_GetCopyableFootprints_reply(dec, pLayouts, pNumRows, pRowSizeInBytes, pTotalBytes);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::CreateQueryHeap
 */

struct npt_command_ID3D12Device_CreateQueryHeap {
    void *_self;
    const D3D12_QUERY_HEAP_DESC * pDesc;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateQueryHeap(const D3D12_QUERY_HEAP_DESC * pDesc,const IID * riid,void ** ppvHeap)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_QUERY_HEAP_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvHeap: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateQueryHeap(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D12_QUERY_HEAP_DESC * pDesc,
                                        const IID * riid,
                                        void ** ppvHeap)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateQueryHeap(pDesc,riid,ppvHeap);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateQueryHeap),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_QUERY_HEAP_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvHeap = 0;
    if (ppvHeap) {
        _gid_ppvHeap = npt_com_allocate_next_id();
        *ppvHeap = (void *)(uintptr_t)_gid_ppvHeap;
    }
    npt_encode_uint64_t(enc, &_gid_ppvHeap);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateQueryHeap_reply(const D3D12_QUERY_HEAP_DESC * pDesc,const IID * riid,void ** ppvHeap)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppvHeap (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateQueryHeap_reply(struct npt_cs_decoder *dec,
                                              void ** ppvHeap,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateQueryHeap))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateQueryHeap reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateQueryHeap));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvHeap (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateQueryHeap(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D12_QUERY_HEAP_DESC * pDesc,
                                        const IID * riid,
                                        void ** ppvHeap,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateQueryHeap(pDesc,riid,ppvHeap);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateQueryHeap_reply(pDesc,riid,ppvHeap)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateQueryHeap(enc, cmd_flags, object_id, pDesc, riid, ppvHeap);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateQueryHeap(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const D3D12_QUERY_HEAP_DESC * pDesc,
                                       const IID * riid,
                                       void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateQueryHeap(ring, 0, object_id, pDesc, riid, ppvHeap, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateQueryHeap(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      const D3D12_QUERY_HEAP_DESC * pDesc,
                                      const IID * riid,
                                      void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateQueryHeap(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppvHeap, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateQueryHeap_reply(dec, ppvHeap, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::SetStablePowerState
 */

struct npt_command_ID3D12Device_SetStablePowerState {
    void *_self;
    BOOL Enable;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_SetStablePowerState(const BOOL Enable)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_SetStablePowerState(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            BOOL Enable)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_SetStablePowerState(Enable);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_SetStablePowerState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_BOOL(enc, &Enable);
}

static inline size_t
npt_sizeof_ID3D12Device_SetStablePowerState_reply(const BOOL Enable)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Enable (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_SetStablePowerState_reply(struct npt_cs_decoder *dec,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_SetStablePowerState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_SetStablePowerState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_SetStablePowerState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device_SetStablePowerState(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            BOOL Enable,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_SetStablePowerState(Enable);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_SetStablePowerState_reply(Enable)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_SetStablePowerState(enc, cmd_flags, object_id, Enable);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_SetStablePowerState(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           BOOL Enable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_SetStablePowerState(ring, 0, object_id, Enable, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_SetStablePowerState(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          BOOL Enable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_SetStablePowerState(ring, NPT_CMD_FLAG_REPLY, object_id, Enable, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_SetStablePowerState_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::CreateCommandSignature
 */

struct npt_command_ID3D12Device_CreateCommandSignature {
    void *_self;
    const D3D12_COMMAND_SIGNATURE_DESC * pDesc;
    ID3D12RootSignature * pRootSignature;
    const IID * riid;
    void ** ppvCommandSignature;
    /* Shadow: the guest-allocated id for ppvCommandSignature arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvCommandSignature -> *ppvCommandSignature} in the
     * context object table. */
    uint64_t _guest_id_ppvCommandSignature;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device_CreateCommandSignature(const D3D12_COMMAND_SIGNATURE_DESC * pDesc,const ID3D12RootSignature * pRootSignature,const IID * riid,void ** ppvCommandSignature)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_COMMAND_SIGNATURE_DESC(pDesc, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvCommandSignature: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_CreateCommandSignature(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const D3D12_COMMAND_SIGNATURE_DESC * pDesc,
                                               ID3D12RootSignature * pRootSignature,
                                               const IID * riid,
                                               void ** ppvCommandSignature)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_CreateCommandSignature(pDesc,pRootSignature,riid,ppvCommandSignature);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommandSignature),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_COMMAND_SIGNATURE_DESC(enc, pDesc);
    npt_encode_com_handle(enc, npt_object_get_id(pRootSignature));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvCommandSignature = 0;
    if (ppvCommandSignature) {
        _gid_ppvCommandSignature = npt_com_allocate_next_id();
        *ppvCommandSignature = (void *)(uintptr_t)_gid_ppvCommandSignature;
    }
    npt_encode_uint64_t(enc, &_gid_ppvCommandSignature);
}
}

static inline size_t
npt_sizeof_ID3D12Device_CreateCommandSignature_reply(const D3D12_COMMAND_SIGNATURE_DESC * pDesc,const ID3D12RootSignature * pRootSignature,const IID * riid,void ** ppvCommandSignature)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip pRootSignature (input only) */
/* skip riid (input only) */
/* skip ppvCommandSignature (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_CreateCommandSignature_reply(struct npt_cs_decoder *dec,
                                                     void ** ppvCommandSignature,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_CreateCommandSignature))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_CreateCommandSignature reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_CreateCommandSignature));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvCommandSignature (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device_CreateCommandSignature(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const D3D12_COMMAND_SIGNATURE_DESC * pDesc,
                                               ID3D12RootSignature * pRootSignature,
                                               const IID * riid,
                                               void ** ppvCommandSignature,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_CreateCommandSignature(pDesc,pRootSignature,riid,ppvCommandSignature);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_CreateCommandSignature_reply(pDesc,pRootSignature,riid,ppvCommandSignature)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_CreateCommandSignature(enc, cmd_flags, object_id, pDesc, pRootSignature, riid, ppvCommandSignature);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_CreateCommandSignature(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const D3D12_COMMAND_SIGNATURE_DESC * pDesc,
                                              ID3D12RootSignature * pRootSignature,
                                              const IID * riid,
                                              void ** ppvCommandSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommandSignature(ring, 0, object_id, pDesc, pRootSignature, riid, ppvCommandSignature, &submit);
}

static inline HRESULT
npt_call_ID3D12Device_CreateCommandSignature(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const D3D12_COMMAND_SIGNATURE_DESC * pDesc,
                                             ID3D12RootSignature * pRootSignature,
                                             const IID * riid,
                                             void ** ppvCommandSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_CreateCommandSignature(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, pRootSignature, riid, ppvCommandSignature, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device_CreateCommandSignature_reply(dec, ppvCommandSignature, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device::GetResourceTiling
 */

struct npt_command_ID3D12Device_GetResourceTiling {
    void *_self;
    ID3D12Resource * pTiledResource;
    UINT * pNumTilesForEntireResource;
    D3D12_PACKED_MIP_INFO * pPackedMipDesc;
    D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips;
    UINT * pNumSubresourceTilings;
    UINT FirstSubresourceTilingToGet;
    D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips;
};

static inline size_t
npt_sizeof_ID3D12Device_GetResourceTiling(const ID3D12Resource * pTiledResource,const UINT * pNumTilesForEntireResource,const D3D12_PACKED_MIP_INFO * pPackedMipDesc,const D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips,const UINT * pNumSubresourceTilings,const UINT FirstSubresourceTilingToGet,const D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pNumTilesForEntireResource (output only) */
/* skip pPackedMipDesc (output only) */
/* skip pStandardTileShapeForNonPackedMips (output only) */
    cmd_size += npt_sizeof_simple_pointer(pNumSubresourceTilings);
    if (pNumSubresourceTilings)
        cmd_size += npt_sizeof_UINT(pNumSubresourceTilings, max_mode);
    cmd_size += npt_sizeof_UINT(&FirstSubresourceTilingToGet, max_mode);
/* skip pSubresourceTilingsForNonPackedMips (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_GetResourceTiling(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D12Resource * pTiledResource,
                                          UINT * pNumTilesForEntireResource,
                                          D3D12_PACKED_MIP_INFO * pPackedMipDesc,
                                          D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                          UINT * pNumSubresourceTilings,
                                          UINT FirstSubresourceTilingToGet,
                                          D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_GetResourceTiling(pTiledResource,pNumTilesForEntireResource,pPackedMipDesc,pStandardTileShapeForNonPackedMips,pNumSubresourceTilings,FirstSubresourceTilingToGet,pSubresourceTilingsForNonPackedMips);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetResourceTiling),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pTiledResource));
/* skip pNumTilesForEntireResource (output only) */
/* skip pPackedMipDesc (output only) */
/* skip pStandardTileShapeForNonPackedMips (output only) */
    if (npt_encode_simple_pointer(enc, pNumSubresourceTilings))
        npt_encode_UINT(enc, pNumSubresourceTilings);
    npt_encode_UINT(enc, &FirstSubresourceTilingToGet);
/* skip pSubresourceTilingsForNonPackedMips (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device_GetResourceTiling_reply(const ID3D12Resource * pTiledResource,const UINT * pNumTilesForEntireResource,const D3D12_PACKED_MIP_INFO * pPackedMipDesc,const D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips,const UINT * pNumSubresourceTilings,const UINT FirstSubresourceTilingToGet,const D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pTiledResource (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D12_PACKED_MIP_INFO(&(const D3D12_PACKED_MIP_INFO){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D12_TILE_SHAPE(&(const D3D12_TILE_SHAPE){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
/* skip FirstSubresourceTilingToGet (input only) */
    cmd_size += npt_sizeof_array_count(pSubresourceTilingsForNonPackedMips ? (pNumSubresourceTilings ? *pNumSubresourceTilings : 0) : 0);
    if (pSubresourceTilingsForNonPackedMips)
        cmd_size += (size_t)((pNumSubresourceTilings ? *pNumSubresourceTilings : 0)) * npt_sizeof_D3D12_SUBRESOURCE_TILING(&(const D3D12_SUBRESOURCE_TILING){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_GetResourceTiling_reply(struct npt_cs_decoder *dec,
                                                UINT * pNumTilesForEntireResource,
                                                D3D12_PACKED_MIP_INFO * pPackedMipDesc,
                                                D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                                UINT * pNumSubresourceTilings,
                                                D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_GetResourceTiling))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_GetResourceTiling reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_GetResourceTiling));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pNumTilesForEntireResource)
            npt_decode_UINT(dec, (UINT *)pNumTilesForEntireResource);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pPackedMipDesc)
            npt_decode_D3D12_PACKED_MIP_INFO(dec, (D3D12_PACKED_MIP_INFO *)pPackedMipDesc);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pStandardTileShapeForNonPackedMips)
            npt_decode_D3D12_TILE_SHAPE(dec, (D3D12_TILE_SHAPE *)pStandardTileShapeForNonPackedMips);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pNumSubresourceTilings)
            npt_decode_UINT(dec, (UINT *)pNumSubresourceTilings);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pSubresourceTilingsForNonPackedMips) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D12_SUBRESOURCE_TILING(dec, (D3D12_SUBRESOURCE_TILING *)&pSubresourceTilingsForNonPackedMips[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D12Device_GetResourceTiling(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D12Resource * pTiledResource,
                                          UINT * pNumTilesForEntireResource,
                                          D3D12_PACKED_MIP_INFO * pPackedMipDesc,
                                          D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                          UINT * pNumSubresourceTilings,
                                          UINT FirstSubresourceTilingToGet,
                                          D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_GetResourceTiling(pTiledResource,pNumTilesForEntireResource,pPackedMipDesc,pStandardTileShapeForNonPackedMips,pNumSubresourceTilings,FirstSubresourceTilingToGet,pSubresourceTilingsForNonPackedMips);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_GetResourceTiling_reply(pTiledResource,pNumTilesForEntireResource,pPackedMipDesc,pStandardTileShapeForNonPackedMips,pNumSubresourceTilings,FirstSubresourceTilingToGet,pSubresourceTilingsForNonPackedMips)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_GetResourceTiling(enc, cmd_flags, object_id, pTiledResource, pNumTilesForEntireResource, pPackedMipDesc, pStandardTileShapeForNonPackedMips, pNumSubresourceTilings, FirstSubresourceTilingToGet, pSubresourceTilingsForNonPackedMips);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_GetResourceTiling(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D12Resource * pTiledResource,
                                         UINT * pNumTilesForEntireResource,
                                         D3D12_PACKED_MIP_INFO * pPackedMipDesc,
                                         D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                         UINT * pNumSubresourceTilings,
                                         UINT FirstSubresourceTilingToGet,
                                         D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetResourceTiling(ring, 0, object_id, pTiledResource, pNumTilesForEntireResource, pPackedMipDesc, pStandardTileShapeForNonPackedMips, pNumSubresourceTilings, FirstSubresourceTilingToGet, pSubresourceTilingsForNonPackedMips, &submit);
}

static inline void
npt_call_ID3D12Device_GetResourceTiling(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        ID3D12Resource * pTiledResource,
                                        UINT * pNumTilesForEntireResource,
                                        D3D12_PACKED_MIP_INFO * pPackedMipDesc,
                                        D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                        UINT * pNumSubresourceTilings,
                                        UINT FirstSubresourceTilingToGet,
                                        D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetResourceTiling(ring, NPT_CMD_FLAG_REPLY, object_id, pTiledResource, pNumTilesForEntireResource, pPackedMipDesc, pStandardTileShapeForNonPackedMips, pNumSubresourceTilings, FirstSubresourceTilingToGet, pSubresourceTilingsForNonPackedMips, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D12Device_GetResourceTiling_reply(dec, pNumTilesForEntireResource, pPackedMipDesc, pStandardTileShapeForNonPackedMips, pNumSubresourceTilings, pSubresourceTilingsForNonPackedMips);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device::GetAdapterLuid
 */

struct npt_command_ID3D12Device_GetAdapterLuid {
    void *_self;
    LUID ret;
};

static inline size_t
npt_sizeof_ID3D12Device_GetAdapterLuid(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device_GetAdapterLuid(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device_GetAdapterLuid();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetAdapterLuid),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12Device_GetAdapterLuid_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_LUID(&(const LUID){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device_GetAdapterLuid_reply(struct npt_cs_decoder *dec,
                                             LUID *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                                  NPT_METHOD_ID3D12Device_GetAdapterLuid))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device_GetAdapterLuid reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                       NPT_METHOD_ID3D12Device_GetAdapterLuid));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_LUID(dec, ret);
}

static inline void
npt_submit_ID3D12Device_GetAdapterLuid(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device_GetAdapterLuid();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device_GetAdapterLuid_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device_GetAdapterLuid(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device_GetAdapterLuid(struct npt_ring *ring,
                                      npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetAdapterLuid(ring, 0, object_id, &submit);
}

static inline LUID
npt_call_ID3D12Device_GetAdapterLuid(struct npt_ring *ring,
                                     npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device_GetAdapterLuid(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        LUID _ret;
        npt_decode_ID3D12Device_GetAdapterLuid_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (LUID){0};
    }
}

/*
 * ID3D12Device1::CreatePipelineLibrary
 */

struct npt_command_ID3D12Device1_CreatePipelineLibrary {
    void *_self;
    const void * pLibraryBlob;
    SIZE_T BlobLength;
    const IID * riid;
    void ** ppPipelineLibrary;
    /* Shadow: the guest-allocated id for ppPipelineLibrary arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineLibrary -> *ppPipelineLibrary} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineLibrary;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device1_CreatePipelineLibrary(const void * pLibraryBlob,const SIZE_T BlobLength,const IID * riid,void ** ppPipelineLibrary)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pLibraryBlob) {
        cmd_size += npt_sizeof_array_count(BlobLength);
        cmd_size += npt_sizeof_blob_array(pLibraryBlob, BlobLength);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&BlobLength, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppPipelineLibrary: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device1_CreatePipelineLibrary(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const void * pLibraryBlob,
                                               SIZE_T BlobLength,
                                               const IID * riid,
                                               void ** ppPipelineLibrary)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device1_CreatePipelineLibrary(pLibraryBlob,BlobLength,riid,ppPipelineLibrary);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                 NPT_METHOD_ID3D12Device1_CreatePipelineLibrary),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pLibraryBlob) {
        npt_encode_array_count(enc, BlobLength);
        npt_encode_blob_array(enc, pLibraryBlob, BlobLength);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &BlobLength);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppPipelineLibrary = 0;
    if (ppPipelineLibrary) {
        _gid_ppPipelineLibrary = npt_com_allocate_next_id();
        *ppPipelineLibrary = (void *)(uintptr_t)_gid_ppPipelineLibrary;
    }
    npt_encode_uint64_t(enc, &_gid_ppPipelineLibrary);
}
}

static inline size_t
npt_sizeof_ID3D12Device1_CreatePipelineLibrary_reply(const void * pLibraryBlob,const SIZE_T BlobLength,const IID * riid,void ** ppPipelineLibrary)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pLibraryBlob (input only) */
/* skip BlobLength (input only) */
/* skip riid (input only) */
/* skip ppPipelineLibrary (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device1_CreatePipelineLibrary_reply(struct npt_cs_decoder *dec,
                                                     void ** ppPipelineLibrary,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                                  NPT_METHOD_ID3D12Device1_CreatePipelineLibrary))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device1_CreatePipelineLibrary reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                       NPT_METHOD_ID3D12Device1_CreatePipelineLibrary));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppPipelineLibrary (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device1_CreatePipelineLibrary(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const void * pLibraryBlob,
                                               SIZE_T BlobLength,
                                               const IID * riid,
                                               void ** ppPipelineLibrary,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device1_CreatePipelineLibrary(pLibraryBlob,BlobLength,riid,ppPipelineLibrary);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device1_CreatePipelineLibrary_reply(pLibraryBlob,BlobLength,riid,ppPipelineLibrary)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device1_CreatePipelineLibrary(enc, cmd_flags, object_id, pLibraryBlob, BlobLength, riid, ppPipelineLibrary);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device1_CreatePipelineLibrary(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const void * pLibraryBlob,
                                              SIZE_T BlobLength,
                                              const IID * riid,
                                              void ** ppPipelineLibrary)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device1_CreatePipelineLibrary(ring, 0, object_id, pLibraryBlob, BlobLength, riid, ppPipelineLibrary, &submit);
}

static inline HRESULT
npt_call_ID3D12Device1_CreatePipelineLibrary(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const void * pLibraryBlob,
                                             SIZE_T BlobLength,
                                             const IID * riid,
                                             void ** ppPipelineLibrary)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device1_CreatePipelineLibrary(ring, NPT_CMD_FLAG_REPLY, object_id, pLibraryBlob, BlobLength, riid, ppPipelineLibrary, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device1_CreatePipelineLibrary_reply(dec, ppPipelineLibrary, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device1::SetEventOnMultipleFenceCompletion
 */

struct npt_command_ID3D12Device1_SetEventOnMultipleFenceCompletion {
    void *_self;
    ID3D12Fence ** ppFences;
    const UINT64 * pFenceValues;
    UINT NumFences;
    D3D12_MULTIPLE_FENCE_WAIT_FLAGS Flags;
    HANDLE hEvent;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device1_SetEventOnMultipleFenceCompletion(ID3D12Fence ** ppFences,const UINT64 * pFenceValues,const UINT NumFences,const D3D12_MULTIPLE_FENCE_WAIT_FLAGS Flags,const HANDLE hEvent)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_array_count(NumFences);
    cmd_size += sizeof(uint64_t) * NumFences;
    cmd_size += npt_sizeof_array_count(pFenceValues ? NumFences : 0);
    if (pFenceValues)
        cmd_size += npt_sizeof_UINT64_array(pFenceValues, NumFences);
    cmd_size += npt_sizeof_UINT(&NumFences, max_mode);
    cmd_size += npt_sizeof_D3D12_MULTIPLE_FENCE_WAIT_FLAGS(&Flags, max_mode);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device1_SetEventOnMultipleFenceCompletion(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D12Fence ** ppFences,
                                                           const UINT64 * pFenceValues,
                                                           UINT NumFences,
                                                           D3D12_MULTIPLE_FENCE_WAIT_FLAGS Flags,
                                                           HANDLE hEvent)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device1_SetEventOnMultipleFenceCompletion(ppFences,pFenceValues,NumFences,Flags,hEvent);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                 NPT_METHOD_ID3D12Device1_SetEventOnMultipleFenceCompletion),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (ppFences) {
        npt_encode_array_count(enc, NumFences);
        for (uint32_t _i = 0; _i < (uint32_t)NumFences; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppFences[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pFenceValues) {
        npt_encode_array_count(enc, NumFences);
        npt_encode_UINT64_array(enc, pFenceValues, NumFences);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumFences);
    npt_encode_D3D12_MULTIPLE_FENCE_WAIT_FLAGS(enc, &Flags);
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
}

static inline size_t
npt_sizeof_ID3D12Device1_SetEventOnMultipleFenceCompletion_reply(ID3D12Fence ** ppFences,const UINT64 * pFenceValues,const UINT NumFences,const D3D12_MULTIPLE_FENCE_WAIT_FLAGS Flags,const HANDLE hEvent)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppFences (input only) */
/* skip pFenceValues (input only) */
/* skip NumFences (input only) */
/* skip Flags (input only) */
/* skip hEvent (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device1_SetEventOnMultipleFenceCompletion_reply(struct npt_cs_decoder *dec,
                                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                                  NPT_METHOD_ID3D12Device1_SetEventOnMultipleFenceCompletion))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device1_SetEventOnMultipleFenceCompletion reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                       NPT_METHOD_ID3D12Device1_SetEventOnMultipleFenceCompletion));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device1_SetEventOnMultipleFenceCompletion(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D12Fence ** ppFences,
                                                           const UINT64 * pFenceValues,
                                                           UINT NumFences,
                                                           D3D12_MULTIPLE_FENCE_WAIT_FLAGS Flags,
                                                           HANDLE hEvent,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device1_SetEventOnMultipleFenceCompletion(ppFences,pFenceValues,NumFences,Flags,hEvent);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device1_SetEventOnMultipleFenceCompletion_reply(ppFences,pFenceValues,NumFences,Flags,hEvent)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device1_SetEventOnMultipleFenceCompletion(enc, cmd_flags, object_id, ppFences, pFenceValues, NumFences, Flags, hEvent);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device1_SetEventOnMultipleFenceCompletion(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D12Fence ** ppFences,
                                                          const UINT64 * pFenceValues,
                                                          UINT NumFences,
                                                          D3D12_MULTIPLE_FENCE_WAIT_FLAGS Flags,
                                                          HANDLE hEvent)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device1_SetEventOnMultipleFenceCompletion(ring, 0, object_id, ppFences, pFenceValues, NumFences, Flags, hEvent, &submit);
}

static inline HRESULT
npt_call_ID3D12Device1_SetEventOnMultipleFenceCompletion(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         ID3D12Fence ** ppFences,
                                                         const UINT64 * pFenceValues,
                                                         UINT NumFences,
                                                         D3D12_MULTIPLE_FENCE_WAIT_FLAGS Flags,
                                                         HANDLE hEvent)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device1_SetEventOnMultipleFenceCompletion(ring, NPT_CMD_FLAG_REPLY, object_id, ppFences, pFenceValues, NumFences, Flags, hEvent, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device1_SetEventOnMultipleFenceCompletion_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device1::SetResidencyPriority
 */

struct npt_command_ID3D12Device1_SetResidencyPriority {
    void *_self;
    UINT NumObjects;
    ID3D12Pageable ** ppObjects;
    const D3D12_RESIDENCY_PRIORITY * pPriorities;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device1_SetResidencyPriority(const UINT NumObjects,ID3D12Pageable ** ppObjects,const D3D12_RESIDENCY_PRIORITY * pPriorities)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumObjects, max_mode);
    cmd_size += npt_sizeof_array_count(NumObjects);
    cmd_size += sizeof(uint64_t) * NumObjects;
    cmd_size += npt_sizeof_array_count(pPriorities ? NumObjects : 0);
    if (pPriorities)
        cmd_size += npt_sizeof_D3D12_RESIDENCY_PRIORITY_array(pPriorities, NumObjects);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device1_SetResidencyPriority(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT NumObjects,
                                              ID3D12Pageable ** ppObjects,
                                              const D3D12_RESIDENCY_PRIORITY * pPriorities)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device1_SetResidencyPriority(NumObjects,ppObjects,pPriorities);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                 NPT_METHOD_ID3D12Device1_SetResidencyPriority),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumObjects);
    if (ppObjects) {
        npt_encode_array_count(enc, NumObjects);
        for (uint32_t _i = 0; _i < (uint32_t)NumObjects; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppObjects[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pPriorities) {
        npt_encode_array_count(enc, NumObjects);
        npt_encode_D3D12_RESIDENCY_PRIORITY_array(enc, pPriorities, NumObjects);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12Device1_SetResidencyPriority_reply(const UINT NumObjects,ID3D12Pageable ** ppObjects,const D3D12_RESIDENCY_PRIORITY * pPriorities)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumObjects (input only) */
/* skip ppObjects (input only) */
/* skip pPriorities (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device1_SetResidencyPriority_reply(struct npt_cs_decoder *dec,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                                  NPT_METHOD_ID3D12Device1_SetResidencyPriority))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device1_SetResidencyPriority reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                       NPT_METHOD_ID3D12Device1_SetResidencyPriority));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device1_SetResidencyPriority(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT NumObjects,
                                              ID3D12Pageable ** ppObjects,
                                              const D3D12_RESIDENCY_PRIORITY * pPriorities,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device1_SetResidencyPriority(NumObjects,ppObjects,pPriorities);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device1_SetResidencyPriority_reply(NumObjects,ppObjects,pPriorities)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device1_SetResidencyPriority(enc, cmd_flags, object_id, NumObjects, ppObjects, pPriorities);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device1_SetResidencyPriority(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT NumObjects,
                                             ID3D12Pageable ** ppObjects,
                                             const D3D12_RESIDENCY_PRIORITY * pPriorities)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device1_SetResidencyPriority(ring, 0, object_id, NumObjects, ppObjects, pPriorities, &submit);
}

static inline HRESULT
npt_call_ID3D12Device1_SetResidencyPriority(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT NumObjects,
                                            ID3D12Pageable ** ppObjects,
                                            const D3D12_RESIDENCY_PRIORITY * pPriorities)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device1_SetResidencyPriority(ring, NPT_CMD_FLAG_REPLY, object_id, NumObjects, ppObjects, pPriorities, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device1_SetResidencyPriority_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device2::CreatePipelineState
 */

struct npt_command_ID3D12Device2_CreatePipelineState {
    void *_self;
    const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc;
    const IID * riid;
    void ** ppPipelineState;
    /* Shadow: the guest-allocated id for ppPipelineState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineState -> *ppPipelineState} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device2_CreatePipelineState(const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_PIPELINE_STATE_STREAM_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppPipelineState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device2_CreatePipelineState(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,
                                             const IID * riid,
                                             void ** ppPipelineState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device2_CreatePipelineState(pDesc,riid,ppPipelineState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device2,
                                 NPT_METHOD_ID3D12Device2_CreatePipelineState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_PIPELINE_STATE_STREAM_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppPipelineState = 0;
    if (ppPipelineState) {
        _gid_ppPipelineState = npt_com_allocate_next_id();
        *ppPipelineState = (void *)(uintptr_t)_gid_ppPipelineState;
    }
    npt_encode_uint64_t(enc, &_gid_ppPipelineState);
}
}

static inline size_t
npt_sizeof_ID3D12Device2_CreatePipelineState_reply(const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,const IID * riid,void ** ppPipelineState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppPipelineState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device2_CreatePipelineState_reply(struct npt_cs_decoder *dec,
                                                   void ** ppPipelineState,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device2,
                                                  NPT_METHOD_ID3D12Device2_CreatePipelineState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device2_CreatePipelineState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device2,
                                       NPT_METHOD_ID3D12Device2_CreatePipelineState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppPipelineState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device2_CreatePipelineState(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,
                                             const IID * riid,
                                             void ** ppPipelineState,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device2_CreatePipelineState(pDesc,riid,ppPipelineState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device2_CreatePipelineState_reply(pDesc,riid,ppPipelineState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device2_CreatePipelineState(enc, cmd_flags, object_id, pDesc, riid, ppPipelineState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device2_CreatePipelineState(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,
                                            const IID * riid,
                                            void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device2_CreatePipelineState(ring, 0, object_id, pDesc, riid, ppPipelineState, &submit);
}

static inline HRESULT
npt_call_ID3D12Device2_CreatePipelineState(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc,
                                           const IID * riid,
                                           void ** ppPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device2_CreatePipelineState(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppPipelineState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device2_CreatePipelineState_reply(dec, ppPipelineState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device3::OpenExistingHeapFromAddress
 */

struct npt_command_ID3D12Device3_OpenExistingHeapFromAddress {
    void *_self;
    const void * pAddress;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device3_OpenExistingHeapFromAddress(const void * pAddress,const IID * riid,void ** ppvHeap)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    /* ERROR: pAddress (void) is not serializable */
    return 0;
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvHeap: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device3_OpenExistingHeapFromAddress(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const void * pAddress,
                                                     const IID * riid,
                                                     void ** ppvHeap)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device3_OpenExistingHeapFromAddress(pAddress,riid,ppvHeap);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                 NPT_METHOD_ID3D12Device3_OpenExistingHeapFromAddress),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    /* ERROR: pAddress (void) is not serializable */
    return;
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvHeap = 0;
    if (ppvHeap) {
        _gid_ppvHeap = npt_com_allocate_next_id();
        *ppvHeap = (void *)(uintptr_t)_gid_ppvHeap;
    }
    npt_encode_uint64_t(enc, &_gid_ppvHeap);
}
}

static inline size_t
npt_sizeof_ID3D12Device3_OpenExistingHeapFromAddress_reply(const void * pAddress,const IID * riid,void ** ppvHeap)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAddress (input only) */
/* skip riid (input only) */
/* skip ppvHeap (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device3_OpenExistingHeapFromAddress_reply(struct npt_cs_decoder *dec,
                                                           void ** ppvHeap,
                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                                  NPT_METHOD_ID3D12Device3_OpenExistingHeapFromAddress))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device3_OpenExistingHeapFromAddress reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                       NPT_METHOD_ID3D12Device3_OpenExistingHeapFromAddress));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvHeap (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device3_OpenExistingHeapFromAddress(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const void * pAddress,
                                                     const IID * riid,
                                                     void ** ppvHeap,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device3_OpenExistingHeapFromAddress(pAddress,riid,ppvHeap);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device3_OpenExistingHeapFromAddress_reply(pAddress,riid,ppvHeap)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device3_OpenExistingHeapFromAddress(enc, cmd_flags, object_id, pAddress, riid, ppvHeap);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device3_OpenExistingHeapFromAddress(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    const void * pAddress,
                                                    const IID * riid,
                                                    void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device3_OpenExistingHeapFromAddress(ring, 0, object_id, pAddress, riid, ppvHeap, &submit);
}

static inline HRESULT
npt_call_ID3D12Device3_OpenExistingHeapFromAddress(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   const void * pAddress,
                                                   const IID * riid,
                                                   void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device3_OpenExistingHeapFromAddress(ring, NPT_CMD_FLAG_REPLY, object_id, pAddress, riid, ppvHeap, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device3_OpenExistingHeapFromAddress_reply(dec, ppvHeap, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device3::OpenExistingHeapFromFileMapping
 */

struct npt_command_ID3D12Device3_OpenExistingHeapFromFileMapping {
    void *_self;
    HANDLE hFileMapping;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device3_OpenExistingHeapFromFileMapping(const HANDLE hFileMapping,const IID * riid,void ** ppvHeap)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvHeap: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device3_OpenExistingHeapFromFileMapping(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         HANDLE hFileMapping,
                                                         const IID * riid,
                                                         void ** ppvHeap)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device3_OpenExistingHeapFromFileMapping(hFileMapping,riid,ppvHeap);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                 NPT_METHOD_ID3D12Device3_OpenExistingHeapFromFileMapping),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hFileMapping));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvHeap = 0;
    if (ppvHeap) {
        _gid_ppvHeap = npt_com_allocate_next_id();
        *ppvHeap = (void *)(uintptr_t)_gid_ppvHeap;
    }
    npt_encode_uint64_t(enc, &_gid_ppvHeap);
}
}

static inline size_t
npt_sizeof_ID3D12Device3_OpenExistingHeapFromFileMapping_reply(const HANDLE hFileMapping,const IID * riid,void ** ppvHeap)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hFileMapping (input only) */
/* skip riid (input only) */
/* skip ppvHeap (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device3_OpenExistingHeapFromFileMapping_reply(struct npt_cs_decoder *dec,
                                                               void ** ppvHeap,
                                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                                  NPT_METHOD_ID3D12Device3_OpenExistingHeapFromFileMapping))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device3_OpenExistingHeapFromFileMapping reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                       NPT_METHOD_ID3D12Device3_OpenExistingHeapFromFileMapping));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvHeap (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device3_OpenExistingHeapFromFileMapping(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         HANDLE hFileMapping,
                                                         const IID * riid,
                                                         void ** ppvHeap,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device3_OpenExistingHeapFromFileMapping(hFileMapping,riid,ppvHeap);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device3_OpenExistingHeapFromFileMapping_reply(hFileMapping,riid,ppvHeap)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device3_OpenExistingHeapFromFileMapping(enc, cmd_flags, object_id, hFileMapping, riid, ppvHeap);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device3_OpenExistingHeapFromFileMapping(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        HANDLE hFileMapping,
                                                        const IID * riid,
                                                        void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device3_OpenExistingHeapFromFileMapping(ring, 0, object_id, hFileMapping, riid, ppvHeap, &submit);
}

static inline HRESULT
npt_call_ID3D12Device3_OpenExistingHeapFromFileMapping(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       HANDLE hFileMapping,
                                                       const IID * riid,
                                                       void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device3_OpenExistingHeapFromFileMapping(ring, NPT_CMD_FLAG_REPLY, object_id, hFileMapping, riid, ppvHeap, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device3_OpenExistingHeapFromFileMapping_reply(dec, ppvHeap, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device3::EnqueueMakeResident
 */

struct npt_command_ID3D12Device3_EnqueueMakeResident {
    void *_self;
    D3D12_RESIDENCY_FLAGS Flags;
    UINT NumObjects;
    ID3D12Pageable ** ppObjects;
    ID3D12Fence * pFenceToSignal;
    UINT64 FenceValueToSignal;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device3_EnqueueMakeResident(const D3D12_RESIDENCY_FLAGS Flags,const UINT NumObjects,ID3D12Pageable ** ppObjects,const ID3D12Fence * pFenceToSignal,const UINT64 FenceValueToSignal)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_RESIDENCY_FLAGS(&Flags, max_mode);
    cmd_size += npt_sizeof_UINT(&NumObjects, max_mode);
    cmd_size += npt_sizeof_array_count(NumObjects);
    cmd_size += sizeof(uint64_t) * NumObjects;
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&FenceValueToSignal, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device3_EnqueueMakeResident(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             D3D12_RESIDENCY_FLAGS Flags,
                                             UINT NumObjects,
                                             ID3D12Pageable ** ppObjects,
                                             ID3D12Fence * pFenceToSignal,
                                             UINT64 FenceValueToSignal)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device3_EnqueueMakeResident(Flags,NumObjects,ppObjects,pFenceToSignal,FenceValueToSignal);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                 NPT_METHOD_ID3D12Device3_EnqueueMakeResident),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_RESIDENCY_FLAGS(enc, &Flags);
    npt_encode_UINT(enc, &NumObjects);
    if (ppObjects) {
        npt_encode_array_count(enc, NumObjects);
        for (uint32_t _i = 0; _i < (uint32_t)NumObjects; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppObjects[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_com_handle(enc, npt_object_get_id(pFenceToSignal));
    npt_encode_UINT64(enc, &FenceValueToSignal);
}

static inline size_t
npt_sizeof_ID3D12Device3_EnqueueMakeResident_reply(const D3D12_RESIDENCY_FLAGS Flags,const UINT NumObjects,ID3D12Pageable ** ppObjects,const ID3D12Fence * pFenceToSignal,const UINT64 FenceValueToSignal)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Flags (input only) */
/* skip NumObjects (input only) */
/* skip ppObjects (input only) */
/* skip pFenceToSignal (input only) */
/* skip FenceValueToSignal (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device3_EnqueueMakeResident_reply(struct npt_cs_decoder *dec,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                                  NPT_METHOD_ID3D12Device3_EnqueueMakeResident))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device3_EnqueueMakeResident reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                       NPT_METHOD_ID3D12Device3_EnqueueMakeResident));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device3_EnqueueMakeResident(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             D3D12_RESIDENCY_FLAGS Flags,
                                             UINT NumObjects,
                                             ID3D12Pageable ** ppObjects,
                                             ID3D12Fence * pFenceToSignal,
                                             UINT64 FenceValueToSignal,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device3_EnqueueMakeResident(Flags,NumObjects,ppObjects,pFenceToSignal,FenceValueToSignal);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device3_EnqueueMakeResident_reply(Flags,NumObjects,ppObjects,pFenceToSignal,FenceValueToSignal)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device3_EnqueueMakeResident(enc, cmd_flags, object_id, Flags, NumObjects, ppObjects, pFenceToSignal, FenceValueToSignal);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device3_EnqueueMakeResident(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            D3D12_RESIDENCY_FLAGS Flags,
                                            UINT NumObjects,
                                            ID3D12Pageable ** ppObjects,
                                            ID3D12Fence * pFenceToSignal,
                                            UINT64 FenceValueToSignal)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device3_EnqueueMakeResident(ring, 0, object_id, Flags, NumObjects, ppObjects, pFenceToSignal, FenceValueToSignal, &submit);
}

static inline HRESULT
npt_call_ID3D12Device3_EnqueueMakeResident(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           D3D12_RESIDENCY_FLAGS Flags,
                                           UINT NumObjects,
                                           ID3D12Pageable ** ppObjects,
                                           ID3D12Fence * pFenceToSignal,
                                           UINT64 FenceValueToSignal)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device3_EnqueueMakeResident(ring, NPT_CMD_FLAG_REPLY, object_id, Flags, NumObjects, ppObjects, pFenceToSignal, FenceValueToSignal, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device3_EnqueueMakeResident_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device4::CreateCommandList1
 */

struct npt_command_ID3D12Device4_CreateCommandList1 {
    void *_self;
    UINT nodeMask;
    D3D12_COMMAND_LIST_TYPE type;
    D3D12_COMMAND_LIST_FLAGS flags;
    const IID * riid;
    void ** ppCommandList;
    /* Shadow: the guest-allocated id for ppCommandList arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandList -> *ppCommandList} in the
     * context object table. */
    uint64_t _guest_id_ppCommandList;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device4_CreateCommandList1(const UINT nodeMask,const D3D12_COMMAND_LIST_TYPE type,const D3D12_COMMAND_LIST_FLAGS flags,const IID * riid,void ** ppCommandList)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&nodeMask, max_mode);
    cmd_size += npt_sizeof_D3D12_COMMAND_LIST_TYPE(&type, max_mode);
    cmd_size += npt_sizeof_D3D12_COMMAND_LIST_FLAGS(&flags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppCommandList: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device4_CreateCommandList1(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT nodeMask,
                                            D3D12_COMMAND_LIST_TYPE type,
                                            D3D12_COMMAND_LIST_FLAGS flags,
                                            const IID * riid,
                                            void ** ppCommandList)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device4_CreateCommandList1(nodeMask,type,flags,riid,ppCommandList);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateCommandList1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &nodeMask);
    npt_encode_D3D12_COMMAND_LIST_TYPE(enc, &type);
    npt_encode_D3D12_COMMAND_LIST_FLAGS(enc, &flags);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppCommandList = 0;
    if (ppCommandList) {
        _gid_ppCommandList = npt_com_allocate_next_id();
        *ppCommandList = (void *)(uintptr_t)_gid_ppCommandList;
    }
    npt_encode_uint64_t(enc, &_gid_ppCommandList);
}
}

static inline size_t
npt_sizeof_ID3D12Device4_CreateCommandList1_reply(const UINT nodeMask,const D3D12_COMMAND_LIST_TYPE type,const D3D12_COMMAND_LIST_FLAGS flags,const IID * riid,void ** ppCommandList)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip nodeMask (input only) */
/* skip type (input only) */
/* skip flags (input only) */
/* skip riid (input only) */
/* skip ppCommandList (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device4_CreateCommandList1_reply(struct npt_cs_decoder *dec,
                                                  void ** ppCommandList,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                                  NPT_METHOD_ID3D12Device4_CreateCommandList1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device4_CreateCommandList1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                       NPT_METHOD_ID3D12Device4_CreateCommandList1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppCommandList (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device4_CreateCommandList1(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT nodeMask,
                                            D3D12_COMMAND_LIST_TYPE type,
                                            D3D12_COMMAND_LIST_FLAGS flags,
                                            const IID * riid,
                                            void ** ppCommandList,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device4_CreateCommandList1(nodeMask,type,flags,riid,ppCommandList);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device4_CreateCommandList1_reply(nodeMask,type,flags,riid,ppCommandList)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device4_CreateCommandList1(enc, cmd_flags, object_id, nodeMask, type, flags, riid, ppCommandList);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device4_CreateCommandList1(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT nodeMask,
                                           D3D12_COMMAND_LIST_TYPE type,
                                           D3D12_COMMAND_LIST_FLAGS flags,
                                           const IID * riid,
                                           void ** ppCommandList)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateCommandList1(ring, 0, object_id, nodeMask, type, flags, riid, ppCommandList, &submit);
}

static inline HRESULT
npt_call_ID3D12Device4_CreateCommandList1(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          UINT nodeMask,
                                          D3D12_COMMAND_LIST_TYPE type,
                                          D3D12_COMMAND_LIST_FLAGS flags,
                                          const IID * riid,
                                          void ** ppCommandList)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateCommandList1(ring, NPT_CMD_FLAG_REPLY, object_id, nodeMask, type, flags, riid, ppCommandList, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device4_CreateCommandList1_reply(dec, ppCommandList, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device4::CreateProtectedResourceSession
 */

struct npt_command_ID3D12Device4_CreateProtectedResourceSession {
    void *_self;
    const D3D12_PROTECTED_RESOURCE_SESSION_DESC * pDesc;
    const IID * riid;
    void ** ppSession;
    /* Shadow: the guest-allocated id for ppSession arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSession -> *ppSession} in the
     * context object table. */
    uint64_t _guest_id_ppSession;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device4_CreateProtectedResourceSession(const D3D12_PROTECTED_RESOURCE_SESSION_DESC * pDesc,const IID * riid,void ** ppSession)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_PROTECTED_RESOURCE_SESSION_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppSession: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device4_CreateProtectedResourceSession(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        const D3D12_PROTECTED_RESOURCE_SESSION_DESC * pDesc,
                                                        const IID * riid,
                                                        void ** ppSession)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device4_CreateProtectedResourceSession(pDesc,riid,ppSession);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateProtectedResourceSession),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_PROTECTED_RESOURCE_SESSION_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppSession = 0;
    if (ppSession) {
        _gid_ppSession = npt_com_allocate_next_id();
        *ppSession = (void *)(uintptr_t)_gid_ppSession;
    }
    npt_encode_uint64_t(enc, &_gid_ppSession);
}
}

static inline size_t
npt_sizeof_ID3D12Device4_CreateProtectedResourceSession_reply(const D3D12_PROTECTED_RESOURCE_SESSION_DESC * pDesc,const IID * riid,void ** ppSession)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppSession (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device4_CreateProtectedResourceSession_reply(struct npt_cs_decoder *dec,
                                                              void ** ppSession,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                                  NPT_METHOD_ID3D12Device4_CreateProtectedResourceSession))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device4_CreateProtectedResourceSession reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                       NPT_METHOD_ID3D12Device4_CreateProtectedResourceSession));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSession (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device4_CreateProtectedResourceSession(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        const D3D12_PROTECTED_RESOURCE_SESSION_DESC * pDesc,
                                                        const IID * riid,
                                                        void ** ppSession,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device4_CreateProtectedResourceSession(pDesc,riid,ppSession);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device4_CreateProtectedResourceSession_reply(pDesc,riid,ppSession)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device4_CreateProtectedResourceSession(enc, cmd_flags, object_id, pDesc, riid, ppSession);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device4_CreateProtectedResourceSession(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       const D3D12_PROTECTED_RESOURCE_SESSION_DESC * pDesc,
                                                       const IID * riid,
                                                       void ** ppSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateProtectedResourceSession(ring, 0, object_id, pDesc, riid, ppSession, &submit);
}

static inline HRESULT
npt_call_ID3D12Device4_CreateProtectedResourceSession(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      const D3D12_PROTECTED_RESOURCE_SESSION_DESC * pDesc,
                                                      const IID * riid,
                                                      void ** ppSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateProtectedResourceSession(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppSession, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device4_CreateProtectedResourceSession_reply(dec, ppSession, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device4::CreateCommittedResource1
 */

struct npt_command_ID3D12Device4_CreateCommittedResource1 {
    void *_self;
    const D3D12_HEAP_PROPERTIES * pHeapProperties;
    D3D12_HEAP_FLAGS HeapFlags;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialResourceState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    const IID * riidResource;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device4_CreateCommittedResource1(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS HeapFlags,const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialResourceState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const IID * riidResource,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pHeapProperties);
    if (pHeapProperties)
        cmd_size += npt_sizeof_D3D12_HEAP_PROPERTIES(pHeapProperties, max_mode);
    cmd_size += npt_sizeof_D3D12_HEAP_FLAGS(&HeapFlags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOURCE_STATES(&InitialResourceState, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riidResource);
    if (riidResource)
        cmd_size += npt_sizeof_IID(riidResource, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device4_CreateCommittedResource1(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                  D3D12_HEAP_FLAGS HeapFlags,
                                                  const D3D12_RESOURCE_DESC * pDesc,
                                                  D3D12_RESOURCE_STATES InitialResourceState,
                                                  const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                  ID3D12ProtectedResourceSession * pProtectedSession,
                                                  const IID * riidResource,
                                                  void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device4_CreateCommittedResource1(pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,pProtectedSession,riidResource,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateCommittedResource1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pHeapProperties))
        npt_encode_D3D12_HEAP_PROPERTIES(enc, pHeapProperties);
    npt_encode_D3D12_HEAP_FLAGS(enc, &HeapFlags);
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC(enc, pDesc);
    npt_encode_D3D12_RESOURCE_STATES(enc, &InitialResourceState);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    npt_encode_com_handle(enc, npt_object_get_id(pProtectedSession));
    if (npt_encode_simple_pointer(enc, riidResource))
        npt_encode_IID(enc, riidResource);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device4_CreateCommittedResource1_reply(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS HeapFlags,const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialResourceState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const IID * riidResource,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pHeapProperties (input only) */
/* skip HeapFlags (input only) */
/* skip pDesc (input only) */
/* skip InitialResourceState (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip pProtectedSession (input only) */
/* skip riidResource (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device4_CreateCommittedResource1_reply(struct npt_cs_decoder *dec,
                                                        void ** ppvResource,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                                  NPT_METHOD_ID3D12Device4_CreateCommittedResource1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device4_CreateCommittedResource1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                       NPT_METHOD_ID3D12Device4_CreateCommittedResource1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device4_CreateCommittedResource1(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                  D3D12_HEAP_FLAGS HeapFlags,
                                                  const D3D12_RESOURCE_DESC * pDesc,
                                                  D3D12_RESOURCE_STATES InitialResourceState,
                                                  const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                  ID3D12ProtectedResourceSession * pProtectedSession,
                                                  const IID * riidResource,
                                                  void ** ppvResource,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device4_CreateCommittedResource1(pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,pProtectedSession,riidResource,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device4_CreateCommittedResource1_reply(pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,pProtectedSession,riidResource,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device4_CreateCommittedResource1(enc, cmd_flags, object_id, pHeapProperties, HeapFlags, pDesc, InitialResourceState, pOptimizedClearValue, pProtectedSession, riidResource, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device4_CreateCommittedResource1(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                 D3D12_HEAP_FLAGS HeapFlags,
                                                 const D3D12_RESOURCE_DESC * pDesc,
                                                 D3D12_RESOURCE_STATES InitialResourceState,
                                                 const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                 ID3D12ProtectedResourceSession * pProtectedSession,
                                                 const IID * riidResource,
                                                 void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateCommittedResource1(ring, 0, object_id, pHeapProperties, HeapFlags, pDesc, InitialResourceState, pOptimizedClearValue, pProtectedSession, riidResource, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device4_CreateCommittedResource1(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                D3D12_HEAP_FLAGS HeapFlags,
                                                const D3D12_RESOURCE_DESC * pDesc,
                                                D3D12_RESOURCE_STATES InitialResourceState,
                                                const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                ID3D12ProtectedResourceSession * pProtectedSession,
                                                const IID * riidResource,
                                                void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateCommittedResource1(ring, NPT_CMD_FLAG_REPLY, object_id, pHeapProperties, HeapFlags, pDesc, InitialResourceState, pOptimizedClearValue, pProtectedSession, riidResource, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device4_CreateCommittedResource1_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device4::CreateHeap1
 */

struct npt_command_ID3D12Device4_CreateHeap1 {
    void *_self;
    const D3D12_HEAP_DESC * pDesc;
    ID3D12ProtectedResourceSession * pProtectedSession;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device4_CreateHeap1(const D3D12_HEAP_DESC * pDesc,const ID3D12ProtectedResourceSession * pProtectedSession,const IID * riid,void ** ppvHeap)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_HEAP_DESC(pDesc, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvHeap: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device4_CreateHeap1(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const D3D12_HEAP_DESC * pDesc,
                                     ID3D12ProtectedResourceSession * pProtectedSession,
                                     const IID * riid,
                                     void ** ppvHeap)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device4_CreateHeap1(pDesc,pProtectedSession,riid,ppvHeap);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateHeap1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_HEAP_DESC(enc, pDesc);
    npt_encode_com_handle(enc, npt_object_get_id(pProtectedSession));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvHeap = 0;
    if (ppvHeap) {
        _gid_ppvHeap = npt_com_allocate_next_id();
        *ppvHeap = (void *)(uintptr_t)_gid_ppvHeap;
    }
    npt_encode_uint64_t(enc, &_gid_ppvHeap);
}
}

static inline size_t
npt_sizeof_ID3D12Device4_CreateHeap1_reply(const D3D12_HEAP_DESC * pDesc,const ID3D12ProtectedResourceSession * pProtectedSession,const IID * riid,void ** ppvHeap)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip pProtectedSession (input only) */
/* skip riid (input only) */
/* skip ppvHeap (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device4_CreateHeap1_reply(struct npt_cs_decoder *dec,
                                           void ** ppvHeap,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                                  NPT_METHOD_ID3D12Device4_CreateHeap1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device4_CreateHeap1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                       NPT_METHOD_ID3D12Device4_CreateHeap1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvHeap (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device4_CreateHeap1(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const D3D12_HEAP_DESC * pDesc,
                                     ID3D12ProtectedResourceSession * pProtectedSession,
                                     const IID * riid,
                                     void ** ppvHeap,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device4_CreateHeap1(pDesc,pProtectedSession,riid,ppvHeap);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device4_CreateHeap1_reply(pDesc,pProtectedSession,riid,ppvHeap)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device4_CreateHeap1(enc, cmd_flags, object_id, pDesc, pProtectedSession, riid, ppvHeap);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device4_CreateHeap1(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const D3D12_HEAP_DESC * pDesc,
                                    ID3D12ProtectedResourceSession * pProtectedSession,
                                    const IID * riid,
                                    void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateHeap1(ring, 0, object_id, pDesc, pProtectedSession, riid, ppvHeap, &submit);
}

static inline HRESULT
npt_call_ID3D12Device4_CreateHeap1(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   const D3D12_HEAP_DESC * pDesc,
                                   ID3D12ProtectedResourceSession * pProtectedSession,
                                   const IID * riid,
                                   void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateHeap1(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, pProtectedSession, riid, ppvHeap, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device4_CreateHeap1_reply(dec, ppvHeap, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device4::CreateReservedResource1
 */

struct npt_command_ID3D12Device4_CreateReservedResource1 {
    void *_self;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device4_CreateReservedResource1(const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const IID * riid,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOURCE_STATES(&InitialState, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device4_CreateReservedResource1(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 const D3D12_RESOURCE_DESC * pDesc,
                                                 D3D12_RESOURCE_STATES InitialState,
                                                 const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                 ID3D12ProtectedResourceSession * pProtectedSession,
                                                 const IID * riid,
                                                 void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device4_CreateReservedResource1(pDesc,InitialState,pOptimizedClearValue,pProtectedSession,riid,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateReservedResource1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC(enc, pDesc);
    npt_encode_D3D12_RESOURCE_STATES(enc, &InitialState);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    npt_encode_com_handle(enc, npt_object_get_id(pProtectedSession));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device4_CreateReservedResource1_reply(const D3D12_RESOURCE_DESC * pDesc,const D3D12_RESOURCE_STATES InitialState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const IID * riid,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip InitialState (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip pProtectedSession (input only) */
/* skip riid (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device4_CreateReservedResource1_reply(struct npt_cs_decoder *dec,
                                                       void ** ppvResource,
                                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                                  NPT_METHOD_ID3D12Device4_CreateReservedResource1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device4_CreateReservedResource1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                       NPT_METHOD_ID3D12Device4_CreateReservedResource1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device4_CreateReservedResource1(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 const D3D12_RESOURCE_DESC * pDesc,
                                                 D3D12_RESOURCE_STATES InitialState,
                                                 const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                 ID3D12ProtectedResourceSession * pProtectedSession,
                                                 const IID * riid,
                                                 void ** ppvResource,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device4_CreateReservedResource1(pDesc,InitialState,pOptimizedClearValue,pProtectedSession,riid,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device4_CreateReservedResource1_reply(pDesc,InitialState,pOptimizedClearValue,pProtectedSession,riid,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device4_CreateReservedResource1(enc, cmd_flags, object_id, pDesc, InitialState, pOptimizedClearValue, pProtectedSession, riid, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device4_CreateReservedResource1(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const D3D12_RESOURCE_DESC * pDesc,
                                                D3D12_RESOURCE_STATES InitialState,
                                                const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                ID3D12ProtectedResourceSession * pProtectedSession,
                                                const IID * riid,
                                                void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateReservedResource1(ring, 0, object_id, pDesc, InitialState, pOptimizedClearValue, pProtectedSession, riid, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device4_CreateReservedResource1(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const D3D12_RESOURCE_DESC * pDesc,
                                               D3D12_RESOURCE_STATES InitialState,
                                               const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                               ID3D12ProtectedResourceSession * pProtectedSession,
                                               const IID * riid,
                                               void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_CreateReservedResource1(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, InitialState, pOptimizedClearValue, pProtectedSession, riid, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device4_CreateReservedResource1_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device4::GetResourceAllocationInfo1
 */

struct npt_command_ID3D12Device4_GetResourceAllocationInfo1 {
    void *_self;
    UINT visibleMask;
    UINT numResourceDescs;
    const D3D12_RESOURCE_DESC * pResourceDescs;
    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1;
    D3D12_RESOURCE_ALLOCATION_INFO ret;
};

static inline size_t
npt_sizeof_ID3D12Device4_GetResourceAllocationInfo1(const UINT visibleMask,const UINT numResourceDescs,const D3D12_RESOURCE_DESC * pResourceDescs,const D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&visibleMask, max_mode);
    cmd_size += npt_sizeof_UINT(&numResourceDescs, max_mode);
    cmd_size += npt_sizeof_array_count(pResourceDescs ? numResourceDescs : 0);
    for (uint32_t _i = 0; _i < (pResourceDescs ? numResourceDescs : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(&pResourceDescs[_i], max_mode);
/* skip pResourceAllocationInfo1 (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device4_GetResourceAllocationInfo1(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT visibleMask,
                                                    UINT numResourceDescs,
                                                    const D3D12_RESOURCE_DESC * pResourceDescs,
                                                    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device4_GetResourceAllocationInfo1(visibleMask,numResourceDescs,pResourceDescs,pResourceAllocationInfo1);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_GetResourceAllocationInfo1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &visibleMask);
    npt_encode_UINT(enc, &numResourceDescs);
    if (pResourceDescs) {
        npt_encode_array_count(enc, numResourceDescs);
        for (uint32_t _i = 0; _i < (uint32_t)(numResourceDescs); _i++)
            npt_encode_D3D12_RESOURCE_DESC(enc, &pResourceDescs[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pResourceAllocationInfo1 (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device4_GetResourceAllocationInfo1_reply(const UINT visibleMask,const UINT numResourceDescs,const D3D12_RESOURCE_DESC * pResourceDescs,const D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_RESOURCE_ALLOCATION_INFO(&(const D3D12_RESOURCE_ALLOCATION_INFO){0}, 1);
/* skip visibleMask (input only) */
/* skip numResourceDescs (input only) */
/* skip pResourceDescs (input only) */
    cmd_size += npt_sizeof_array_count(pResourceAllocationInfo1 ? numResourceDescs : 0);
    if (pResourceAllocationInfo1)
        cmd_size += (size_t)(numResourceDescs) * npt_sizeof_D3D12_RESOURCE_ALLOCATION_INFO1(&(const D3D12_RESOURCE_ALLOCATION_INFO1){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device4_GetResourceAllocationInfo1_reply(struct npt_cs_decoder *dec,
                                                          D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1,
                                                          D3D12_RESOURCE_ALLOCATION_INFO *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                                  NPT_METHOD_ID3D12Device4_GetResourceAllocationInfo1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device4_GetResourceAllocationInfo1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                       NPT_METHOD_ID3D12Device4_GetResourceAllocationInfo1));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_RESOURCE_ALLOCATION_INFO(dec, ret);
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pResourceAllocationInfo1) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D12_RESOURCE_ALLOCATION_INFO1(dec, (D3D12_RESOURCE_ALLOCATION_INFO1 *)&pResourceAllocationInfo1[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D12Device4_GetResourceAllocationInfo1(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT visibleMask,
                                                    UINT numResourceDescs,
                                                    const D3D12_RESOURCE_DESC * pResourceDescs,
                                                    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device4_GetResourceAllocationInfo1(visibleMask,numResourceDescs,pResourceDescs,pResourceAllocationInfo1);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device4_GetResourceAllocationInfo1_reply(visibleMask,numResourceDescs,pResourceDescs,pResourceAllocationInfo1)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device4_GetResourceAllocationInfo1(enc, cmd_flags, object_id, visibleMask, numResourceDescs, pResourceDescs, pResourceAllocationInfo1);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device4_GetResourceAllocationInfo1(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT visibleMask,
                                                   UINT numResourceDescs,
                                                   const D3D12_RESOURCE_DESC * pResourceDescs,
                                                   D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_GetResourceAllocationInfo1(ring, 0, object_id, visibleMask, numResourceDescs, pResourceDescs, pResourceAllocationInfo1, &submit);
}

static inline D3D12_RESOURCE_ALLOCATION_INFO
npt_call_ID3D12Device4_GetResourceAllocationInfo1(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT visibleMask,
                                                  UINT numResourceDescs,
                                                  const D3D12_RESOURCE_DESC * pResourceDescs,
                                                  D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device4_GetResourceAllocationInfo1(ring, NPT_CMD_FLAG_REPLY, object_id, visibleMask, numResourceDescs, pResourceDescs, pResourceAllocationInfo1, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_RESOURCE_ALLOCATION_INFO _ret;
        npt_decode_ID3D12Device4_GetResourceAllocationInfo1_reply(dec, pResourceAllocationInfo1, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_RESOURCE_ALLOCATION_INFO){0};
    }
}

/*
 * ID3D12Device5::CreateLifetimeTracker
 */

struct npt_command_ID3D12Device5_CreateLifetimeTracker {
    void *_self;
    ID3D12LifetimeOwner * pOwner;
    const IID * riid;
    void ** ppvTracker;
    /* Shadow: the guest-allocated id for ppvTracker arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvTracker -> *ppvTracker} in the
     * context object table. */
    uint64_t _guest_id_ppvTracker;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device5_CreateLifetimeTracker(const ID3D12LifetimeOwner * pOwner,const IID * riid,void ** ppvTracker)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvTracker: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device5_CreateLifetimeTracker(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12LifetimeOwner * pOwner,
                                               const IID * riid,
                                               void ** ppvTracker)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device5_CreateLifetimeTracker(pOwner,riid,ppvTracker);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_CreateLifetimeTracker),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pOwner));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvTracker = 0;
    if (ppvTracker) {
        _gid_ppvTracker = npt_com_allocate_next_id();
        *ppvTracker = (void *)(uintptr_t)_gid_ppvTracker;
    }
    npt_encode_uint64_t(enc, &_gid_ppvTracker);
}
}

static inline size_t
npt_sizeof_ID3D12Device5_CreateLifetimeTracker_reply(const ID3D12LifetimeOwner * pOwner,const IID * riid,void ** ppvTracker)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pOwner (input only) */
/* skip riid (input only) */
/* skip ppvTracker (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device5_CreateLifetimeTracker_reply(struct npt_cs_decoder *dec,
                                                     void ** ppvTracker,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                                  NPT_METHOD_ID3D12Device5_CreateLifetimeTracker))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device5_CreateLifetimeTracker reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                       NPT_METHOD_ID3D12Device5_CreateLifetimeTracker));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvTracker (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device5_CreateLifetimeTracker(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12LifetimeOwner * pOwner,
                                               const IID * riid,
                                               void ** ppvTracker,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device5_CreateLifetimeTracker(pOwner,riid,ppvTracker);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device5_CreateLifetimeTracker_reply(pOwner,riid,ppvTracker)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device5_CreateLifetimeTracker(enc, cmd_flags, object_id, pOwner, riid, ppvTracker);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device5_CreateLifetimeTracker(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D12LifetimeOwner * pOwner,
                                              const IID * riid,
                                              void ** ppvTracker)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_CreateLifetimeTracker(ring, 0, object_id, pOwner, riid, ppvTracker, &submit);
}

static inline HRESULT
npt_call_ID3D12Device5_CreateLifetimeTracker(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D12LifetimeOwner * pOwner,
                                             const IID * riid,
                                             void ** ppvTracker)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_CreateLifetimeTracker(ring, NPT_CMD_FLAG_REPLY, object_id, pOwner, riid, ppvTracker, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device5_CreateLifetimeTracker_reply(dec, ppvTracker, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device5::RemoveDevice
 */

struct npt_command_ID3D12Device5_RemoveDevice {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D12Device5_RemoveDevice(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device5_RemoveDevice(struct npt_cs_encoder *enc,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device5_RemoveDevice();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_RemoveDevice),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12Device5_RemoveDevice_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device5_RemoveDevice(struct npt_ring *ring,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device5_RemoveDevice();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device5_RemoveDevice_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device5_RemoveDevice(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device5_RemoveDevice(struct npt_ring *ring,
                                     npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_RemoveDevice(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D12Device5_RemoveDevice(struct npt_ring *ring,
                                    npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_RemoveDevice(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device5::EnumerateMetaCommands
 */

struct npt_command_ID3D12Device5_EnumerateMetaCommands {
    void *_self;
    UINT * pNumMetaCommands;
    D3D12_META_COMMAND_DESC * pDescs;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device5_EnumerateMetaCommands(const UINT * pNumMetaCommands,const D3D12_META_COMMAND_DESC * pDescs)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pNumMetaCommands);
    if (pNumMetaCommands)
        cmd_size += npt_sizeof_UINT(pNumMetaCommands, max_mode);
/* skip pDescs (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device5_EnumerateMetaCommands(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               UINT * pNumMetaCommands,
                                               D3D12_META_COMMAND_DESC * pDescs)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device5_EnumerateMetaCommands(pNumMetaCommands,pDescs);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_EnumerateMetaCommands),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pNumMetaCommands))
        npt_encode_UINT(enc, pNumMetaCommands);
/* skip pDescs (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device5_EnumerateMetaCommands_reply(const UINT * pNumMetaCommands,const D3D12_META_COMMAND_DESC * pDescs)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device5_EnumerateMetaCommands_reply(struct npt_cs_decoder *dec,
                                                     UINT * pNumMetaCommands,
                                                     D3D12_META_COMMAND_DESC * pDescs,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                                  NPT_METHOD_ID3D12Device5_EnumerateMetaCommands))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device5_EnumerateMetaCommands reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                       NPT_METHOD_ID3D12Device5_EnumerateMetaCommands));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pNumMetaCommands)
            npt_decode_UINT(dec, (UINT *)pNumMetaCommands);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pDescs) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D12_META_COMMAND_DESC(dec, (D3D12_META_COMMAND_DESC *)&pDescs[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D12Device5_EnumerateMetaCommands(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               UINT * pNumMetaCommands,
                                               D3D12_META_COMMAND_DESC * pDescs,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device5_EnumerateMetaCommands(pNumMetaCommands,pDescs);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device5_EnumerateMetaCommands_reply(pNumMetaCommands,pDescs)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device5_EnumerateMetaCommands(enc, cmd_flags, object_id, pNumMetaCommands, pDescs);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device5_EnumerateMetaCommands(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT * pNumMetaCommands,
                                              D3D12_META_COMMAND_DESC * pDescs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_EnumerateMetaCommands(ring, 0, object_id, pNumMetaCommands, pDescs, &submit);
}

static inline HRESULT
npt_call_ID3D12Device5_EnumerateMetaCommands(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT * pNumMetaCommands,
                                             D3D12_META_COMMAND_DESC * pDescs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_EnumerateMetaCommands(ring, NPT_CMD_FLAG_REPLY, object_id, pNumMetaCommands, pDescs, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device5_EnumerateMetaCommands_reply(dec, pNumMetaCommands, pDescs, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device5::EnumerateMetaCommandParameters
 */

struct npt_command_ID3D12Device5_EnumerateMetaCommandParameters {
    void *_self;
    const GUID * CommandId;
    D3D12_META_COMMAND_PARAMETER_STAGE Stage;
    UINT * pTotalStructureSizeInBytes;
    UINT * pParameterCount;
    D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device5_EnumerateMetaCommandParameters(const GUID * CommandId,const D3D12_META_COMMAND_PARAMETER_STAGE Stage,const UINT * pTotalStructureSizeInBytes,const UINT * pParameterCount,const D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(CommandId);
    if (CommandId)
        cmd_size += npt_sizeof_GUID(CommandId, max_mode);
    cmd_size += npt_sizeof_D3D12_META_COMMAND_PARAMETER_STAGE(&Stage, max_mode);
/* skip pTotalStructureSizeInBytes (output only) */
    cmd_size += npt_sizeof_simple_pointer(pParameterCount);
    if (pParameterCount)
        cmd_size += npt_sizeof_UINT(pParameterCount, max_mode);
/* skip pParameterDescs (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device5_EnumerateMetaCommandParameters(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        const GUID * CommandId,
                                                        D3D12_META_COMMAND_PARAMETER_STAGE Stage,
                                                        UINT * pTotalStructureSizeInBytes,
                                                        UINT * pParameterCount,
                                                        D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device5_EnumerateMetaCommandParameters(CommandId,Stage,pTotalStructureSizeInBytes,pParameterCount,pParameterDescs);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_EnumerateMetaCommandParameters),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, CommandId))
        npt_encode_GUID(enc, CommandId);
    npt_encode_D3D12_META_COMMAND_PARAMETER_STAGE(enc, &Stage);
/* skip pTotalStructureSizeInBytes (output only) */
    if (npt_encode_simple_pointer(enc, pParameterCount))
        npt_encode_UINT(enc, pParameterCount);
/* skip pParameterDescs (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device5_EnumerateMetaCommandParameters_reply(const GUID * CommandId,const D3D12_META_COMMAND_PARAMETER_STAGE Stage,const UINT * pTotalStructureSizeInBytes,const UINT * pParameterCount,const D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device5_EnumerateMetaCommandParameters_reply(struct npt_cs_decoder *dec,
                                                              UINT * pTotalStructureSizeInBytes,
                                                              UINT * pParameterCount,
                                                              D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                                  NPT_METHOD_ID3D12Device5_EnumerateMetaCommandParameters))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device5_EnumerateMetaCommandParameters reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                       NPT_METHOD_ID3D12Device5_EnumerateMetaCommandParameters));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pTotalStructureSizeInBytes)
            npt_decode_UINT(dec, (UINT *)pTotalStructureSizeInBytes);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pParameterCount)
            npt_decode_UINT(dec, (UINT *)pParameterCount);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pParameterDescs) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D12_META_COMMAND_PARAMETER_DESC(dec, (D3D12_META_COMMAND_PARAMETER_DESC *)&pParameterDescs[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D12Device5_EnumerateMetaCommandParameters(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        const GUID * CommandId,
                                                        D3D12_META_COMMAND_PARAMETER_STAGE Stage,
                                                        UINT * pTotalStructureSizeInBytes,
                                                        UINT * pParameterCount,
                                                        D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device5_EnumerateMetaCommandParameters(CommandId,Stage,pTotalStructureSizeInBytes,pParameterCount,pParameterDescs);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device5_EnumerateMetaCommandParameters_reply(CommandId,Stage,pTotalStructureSizeInBytes,pParameterCount,pParameterDescs)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device5_EnumerateMetaCommandParameters(enc, cmd_flags, object_id, CommandId, Stage, pTotalStructureSizeInBytes, pParameterCount, pParameterDescs);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device5_EnumerateMetaCommandParameters(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       const GUID * CommandId,
                                                       D3D12_META_COMMAND_PARAMETER_STAGE Stage,
                                                       UINT * pTotalStructureSizeInBytes,
                                                       UINT * pParameterCount,
                                                       D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_EnumerateMetaCommandParameters(ring, 0, object_id, CommandId, Stage, pTotalStructureSizeInBytes, pParameterCount, pParameterDescs, &submit);
}

static inline HRESULT
npt_call_ID3D12Device5_EnumerateMetaCommandParameters(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      const GUID * CommandId,
                                                      D3D12_META_COMMAND_PARAMETER_STAGE Stage,
                                                      UINT * pTotalStructureSizeInBytes,
                                                      UINT * pParameterCount,
                                                      D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_EnumerateMetaCommandParameters(ring, NPT_CMD_FLAG_REPLY, object_id, CommandId, Stage, pTotalStructureSizeInBytes, pParameterCount, pParameterDescs, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device5_EnumerateMetaCommandParameters_reply(dec, pTotalStructureSizeInBytes, pParameterCount, pParameterDescs, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device5::CreateMetaCommand
 */

struct npt_command_ID3D12Device5_CreateMetaCommand {
    void *_self;
    const GUID * CommandId;
    UINT NodeMask;
    const void * pCreationParametersData;
    SIZE_T CreationParametersDataSizeInBytes;
    const IID * riid;
    void ** ppMetaCommand;
    /* Shadow: the guest-allocated id for ppMetaCommand arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppMetaCommand -> *ppMetaCommand} in the
     * context object table. */
    uint64_t _guest_id_ppMetaCommand;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device5_CreateMetaCommand(const GUID * CommandId,const UINT NodeMask,const void * pCreationParametersData,const SIZE_T CreationParametersDataSizeInBytes,const IID * riid,void ** ppMetaCommand)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(CommandId);
    if (CommandId)
        cmd_size += npt_sizeof_GUID(CommandId, max_mode);
    cmd_size += npt_sizeof_UINT(&NodeMask, max_mode);
    if (pCreationParametersData) {
        cmd_size += npt_sizeof_array_count(CreationParametersDataSizeInBytes);
        cmd_size += npt_sizeof_blob_array(pCreationParametersData, CreationParametersDataSizeInBytes);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&CreationParametersDataSizeInBytes, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppMetaCommand: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device5_CreateMetaCommand(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const GUID * CommandId,
                                           UINT NodeMask,
                                           const void * pCreationParametersData,
                                           SIZE_T CreationParametersDataSizeInBytes,
                                           const IID * riid,
                                           void ** ppMetaCommand)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device5_CreateMetaCommand(CommandId,NodeMask,pCreationParametersData,CreationParametersDataSizeInBytes,riid,ppMetaCommand);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_CreateMetaCommand),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, CommandId))
        npt_encode_GUID(enc, CommandId);
    npt_encode_UINT(enc, &NodeMask);
    if (pCreationParametersData) {
        npt_encode_array_count(enc, CreationParametersDataSizeInBytes);
        npt_encode_blob_array(enc, pCreationParametersData, CreationParametersDataSizeInBytes);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &CreationParametersDataSizeInBytes);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppMetaCommand = 0;
    if (ppMetaCommand) {
        _gid_ppMetaCommand = npt_com_allocate_next_id();
        *ppMetaCommand = (void *)(uintptr_t)_gid_ppMetaCommand;
    }
    npt_encode_uint64_t(enc, &_gid_ppMetaCommand);
}
}

static inline size_t
npt_sizeof_ID3D12Device5_CreateMetaCommand_reply(const GUID * CommandId,const UINT NodeMask,const void * pCreationParametersData,const SIZE_T CreationParametersDataSizeInBytes,const IID * riid,void ** ppMetaCommand)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip CommandId (input only) */
/* skip NodeMask (input only) */
/* skip pCreationParametersData (input only) */
/* skip CreationParametersDataSizeInBytes (input only) */
/* skip riid (input only) */
/* skip ppMetaCommand (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device5_CreateMetaCommand_reply(struct npt_cs_decoder *dec,
                                                 void ** ppMetaCommand,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                                  NPT_METHOD_ID3D12Device5_CreateMetaCommand))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device5_CreateMetaCommand reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                       NPT_METHOD_ID3D12Device5_CreateMetaCommand));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppMetaCommand (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device5_CreateMetaCommand(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const GUID * CommandId,
                                           UINT NodeMask,
                                           const void * pCreationParametersData,
                                           SIZE_T CreationParametersDataSizeInBytes,
                                           const IID * riid,
                                           void ** ppMetaCommand,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device5_CreateMetaCommand(CommandId,NodeMask,pCreationParametersData,CreationParametersDataSizeInBytes,riid,ppMetaCommand);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device5_CreateMetaCommand_reply(CommandId,NodeMask,pCreationParametersData,CreationParametersDataSizeInBytes,riid,ppMetaCommand)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device5_CreateMetaCommand(enc, cmd_flags, object_id, CommandId, NodeMask, pCreationParametersData, CreationParametersDataSizeInBytes, riid, ppMetaCommand);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device5_CreateMetaCommand(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const GUID * CommandId,
                                          UINT NodeMask,
                                          const void * pCreationParametersData,
                                          SIZE_T CreationParametersDataSizeInBytes,
                                          const IID * riid,
                                          void ** ppMetaCommand)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_CreateMetaCommand(ring, 0, object_id, CommandId, NodeMask, pCreationParametersData, CreationParametersDataSizeInBytes, riid, ppMetaCommand, &submit);
}

static inline HRESULT
npt_call_ID3D12Device5_CreateMetaCommand(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const GUID * CommandId,
                                         UINT NodeMask,
                                         const void * pCreationParametersData,
                                         SIZE_T CreationParametersDataSizeInBytes,
                                         const IID * riid,
                                         void ** ppMetaCommand)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_CreateMetaCommand(ring, NPT_CMD_FLAG_REPLY, object_id, CommandId, NodeMask, pCreationParametersData, CreationParametersDataSizeInBytes, riid, ppMetaCommand, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device5_CreateMetaCommand_reply(dec, ppMetaCommand, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device5::CreateStateObject
 */

struct npt_command_ID3D12Device5_CreateStateObject {
    void *_self;
    const D3D12_STATE_OBJECT_DESC * pDesc;
    const IID * riid;
    void ** ppStateObject;
    /* Shadow: the guest-allocated id for ppStateObject arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppStateObject -> *ppStateObject} in the
     * context object table. */
    uint64_t _guest_id_ppStateObject;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device5_CreateStateObject(const D3D12_STATE_OBJECT_DESC * pDesc,const IID * riid,void ** ppStateObject)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_STATE_OBJECT_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppStateObject: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device5_CreateStateObject(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const D3D12_STATE_OBJECT_DESC * pDesc,
                                           const IID * riid,
                                           void ** ppStateObject)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device5_CreateStateObject(pDesc,riid,ppStateObject);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_CreateStateObject),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_STATE_OBJECT_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppStateObject = 0;
    if (ppStateObject) {
        _gid_ppStateObject = npt_com_allocate_next_id();
        *ppStateObject = (void *)(uintptr_t)_gid_ppStateObject;
    }
    npt_encode_uint64_t(enc, &_gid_ppStateObject);
}
}

static inline size_t
npt_sizeof_ID3D12Device5_CreateStateObject_reply(const D3D12_STATE_OBJECT_DESC * pDesc,const IID * riid,void ** ppStateObject)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppStateObject (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device5_CreateStateObject_reply(struct npt_cs_decoder *dec,
                                                 void ** ppStateObject,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                                  NPT_METHOD_ID3D12Device5_CreateStateObject))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device5_CreateStateObject reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                       NPT_METHOD_ID3D12Device5_CreateStateObject));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppStateObject (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device5_CreateStateObject(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const D3D12_STATE_OBJECT_DESC * pDesc,
                                           const IID * riid,
                                           void ** ppStateObject,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device5_CreateStateObject(pDesc,riid,ppStateObject);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device5_CreateStateObject_reply(pDesc,riid,ppStateObject)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device5_CreateStateObject(enc, cmd_flags, object_id, pDesc, riid, ppStateObject);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device5_CreateStateObject(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const D3D12_STATE_OBJECT_DESC * pDesc,
                                          const IID * riid,
                                          void ** ppStateObject)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_CreateStateObject(ring, 0, object_id, pDesc, riid, ppStateObject, &submit);
}

static inline HRESULT
npt_call_ID3D12Device5_CreateStateObject(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const D3D12_STATE_OBJECT_DESC * pDesc,
                                         const IID * riid,
                                         void ** ppStateObject)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_CreateStateObject(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppStateObject, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device5_CreateStateObject_reply(dec, ppStateObject, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device5::GetRaytracingAccelerationStructurePrebuildInfo
 */

struct npt_command_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo {
    void *_self;
    const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS * pDesc;
    D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo;
};

static inline size_t
npt_sizeof_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS * pDesc,const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS(pDesc, max_mode);
/* skip pInfo (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(struct npt_cs_encoder *enc,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS * pDesc,
                                                                        D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(pDesc,pInfo);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS(enc, pDesc);
/* skip pInfo (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_reply(const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS * pDesc,const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO(&(const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_reply(struct npt_cs_decoder *dec,
                                                                              D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                                  NPT_METHOD_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                       NPT_METHOD_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pInfo)
            npt_decode_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO(dec, (D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO *)pInfo);
    }
}

static inline void
npt_submit_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(struct npt_ring *ring,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS * pDesc,
                                                                        D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo,
                                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(pDesc,pInfo);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_reply(pDesc,pInfo)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(enc, cmd_flags, object_id, pDesc, pInfo);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(struct npt_ring *ring,
                                                                       npt_object_id object_id,
                                                                       const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS * pDesc,
                                                                       D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(ring, 0, object_id, pDesc, pInfo, &submit);
}

static inline void
npt_call_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS * pDesc,
                                                                      D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, pInfo, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_reply(dec, pInfo);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device5::CheckDriverMatchingIdentifier
 */

struct npt_command_ID3D12Device5_CheckDriverMatchingIdentifier {
    void *_self;
    D3D12_SERIALIZED_DATA_TYPE SerializedDataType;
    const D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER * pIdentifierToCheck;
    D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS ret;
};

static inline size_t
npt_sizeof_ID3D12Device5_CheckDriverMatchingIdentifier(const D3D12_SERIALIZED_DATA_TYPE SerializedDataType,const D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER * pIdentifierToCheck)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_SERIALIZED_DATA_TYPE(&SerializedDataType, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pIdentifierToCheck);
    if (pIdentifierToCheck)
        cmd_size += npt_sizeof_D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER(pIdentifierToCheck, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device5_CheckDriverMatchingIdentifier(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       D3D12_SERIALIZED_DATA_TYPE SerializedDataType,
                                                       const D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER * pIdentifierToCheck)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device5_CheckDriverMatchingIdentifier(SerializedDataType,pIdentifierToCheck);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_CheckDriverMatchingIdentifier),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_SERIALIZED_DATA_TYPE(enc, &SerializedDataType);
    if (npt_encode_simple_pointer(enc, pIdentifierToCheck))
        npt_encode_D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER(enc, pIdentifierToCheck);
}

static inline size_t
npt_sizeof_ID3D12Device5_CheckDriverMatchingIdentifier_reply(const D3D12_SERIALIZED_DATA_TYPE SerializedDataType,const D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER * pIdentifierToCheck)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip SerializedDataType (input only) */
/* skip pIdentifierToCheck (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device5_CheckDriverMatchingIdentifier_reply(struct npt_cs_decoder *dec,
                                                             D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                                  NPT_METHOD_ID3D12Device5_CheckDriverMatchingIdentifier))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device5_CheckDriverMatchingIdentifier reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                       NPT_METHOD_ID3D12Device5_CheckDriverMatchingIdentifier));
        if (ret) *ret = (D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS)0;
        return;
    }
    if (ret) *ret = (D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device5_CheckDriverMatchingIdentifier(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       D3D12_SERIALIZED_DATA_TYPE SerializedDataType,
                                                       const D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER * pIdentifierToCheck,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device5_CheckDriverMatchingIdentifier(SerializedDataType,pIdentifierToCheck);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device5_CheckDriverMatchingIdentifier_reply(SerializedDataType,pIdentifierToCheck)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device5_CheckDriverMatchingIdentifier(enc, cmd_flags, object_id, SerializedDataType, pIdentifierToCheck);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device5_CheckDriverMatchingIdentifier(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      D3D12_SERIALIZED_DATA_TYPE SerializedDataType,
                                                      const D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER * pIdentifierToCheck)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_CheckDriverMatchingIdentifier(ring, 0, object_id, SerializedDataType, pIdentifierToCheck, &submit);
}

static inline D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS
npt_call_ID3D12Device5_CheckDriverMatchingIdentifier(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     D3D12_SERIALIZED_DATA_TYPE SerializedDataType,
                                                     const D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER * pIdentifierToCheck)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device5_CheckDriverMatchingIdentifier(ring, NPT_CMD_FLAG_REPLY, object_id, SerializedDataType, pIdentifierToCheck, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS _ret;
        npt_decode_ID3D12Device5_CheckDriverMatchingIdentifier_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS)0;
    }
}

/*
 * ID3D12Device6::SetBackgroundProcessingMode
 */

struct npt_command_ID3D12Device6_SetBackgroundProcessingMode {
    void *_self;
    D3D12_BACKGROUND_PROCESSING_MODE Mode;
    D3D12_MEASUREMENTS_ACTION MeasurementsAction;
    HANDLE hEventToSignalUponCompletion;
    BOOL * pbFurtherMeasurementsDesired;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device6_SetBackgroundProcessingMode(const D3D12_BACKGROUND_PROCESSING_MODE Mode,const D3D12_MEASUREMENTS_ACTION MeasurementsAction,const HANDLE hEventToSignalUponCompletion,const BOOL * pbFurtherMeasurementsDesired)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_BACKGROUND_PROCESSING_MODE(&Mode, max_mode);
    cmd_size += npt_sizeof_D3D12_MEASUREMENTS_ACTION(&MeasurementsAction, max_mode);
    cmd_size += sizeof(uint64_t);
/* skip pbFurtherMeasurementsDesired (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device6_SetBackgroundProcessingMode(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     D3D12_BACKGROUND_PROCESSING_MODE Mode,
                                                     D3D12_MEASUREMENTS_ACTION MeasurementsAction,
                                                     HANDLE hEventToSignalUponCompletion,
                                                     BOOL * pbFurtherMeasurementsDesired)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device6_SetBackgroundProcessingMode(Mode,MeasurementsAction,hEventToSignalUponCompletion,pbFurtherMeasurementsDesired);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device6,
                                 NPT_METHOD_ID3D12Device6_SetBackgroundProcessingMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_BACKGROUND_PROCESSING_MODE(enc, &Mode);
    npt_encode_D3D12_MEASUREMENTS_ACTION(enc, &MeasurementsAction);
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEventToSignalUponCompletion));
/* skip pbFurtherMeasurementsDesired (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device6_SetBackgroundProcessingMode_reply(const D3D12_BACKGROUND_PROCESSING_MODE Mode,const D3D12_MEASUREMENTS_ACTION MeasurementsAction,const HANDLE hEventToSignalUponCompletion,const BOOL * pbFurtherMeasurementsDesired)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Mode (input only) */
/* skip MeasurementsAction (input only) */
/* skip hEventToSignalUponCompletion (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device6_SetBackgroundProcessingMode_reply(struct npt_cs_decoder *dec,
                                                           BOOL * pbFurtherMeasurementsDesired,
                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device6,
                                                  NPT_METHOD_ID3D12Device6_SetBackgroundProcessingMode))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device6_SetBackgroundProcessingMode reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device6,
                                       NPT_METHOD_ID3D12Device6_SetBackgroundProcessingMode));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pbFurtherMeasurementsDesired)
            npt_decode_BOOL(dec, (BOOL *)pbFurtherMeasurementsDesired);
    }
}

static inline void
npt_submit_ID3D12Device6_SetBackgroundProcessingMode(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     D3D12_BACKGROUND_PROCESSING_MODE Mode,
                                                     D3D12_MEASUREMENTS_ACTION MeasurementsAction,
                                                     HANDLE hEventToSignalUponCompletion,
                                                     BOOL * pbFurtherMeasurementsDesired,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device6_SetBackgroundProcessingMode(Mode,MeasurementsAction,hEventToSignalUponCompletion,pbFurtherMeasurementsDesired);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device6_SetBackgroundProcessingMode_reply(Mode,MeasurementsAction,hEventToSignalUponCompletion,pbFurtherMeasurementsDesired)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device6_SetBackgroundProcessingMode(enc, cmd_flags, object_id, Mode, MeasurementsAction, hEventToSignalUponCompletion, pbFurtherMeasurementsDesired);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device6_SetBackgroundProcessingMode(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    D3D12_BACKGROUND_PROCESSING_MODE Mode,
                                                    D3D12_MEASUREMENTS_ACTION MeasurementsAction,
                                                    HANDLE hEventToSignalUponCompletion,
                                                    BOOL * pbFurtherMeasurementsDesired)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device6_SetBackgroundProcessingMode(ring, 0, object_id, Mode, MeasurementsAction, hEventToSignalUponCompletion, pbFurtherMeasurementsDesired, &submit);
}

static inline HRESULT
npt_call_ID3D12Device6_SetBackgroundProcessingMode(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   D3D12_BACKGROUND_PROCESSING_MODE Mode,
                                                   D3D12_MEASUREMENTS_ACTION MeasurementsAction,
                                                   HANDLE hEventToSignalUponCompletion,
                                                   BOOL * pbFurtherMeasurementsDesired)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device6_SetBackgroundProcessingMode(ring, NPT_CMD_FLAG_REPLY, object_id, Mode, MeasurementsAction, hEventToSignalUponCompletion, pbFurtherMeasurementsDesired, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device6_SetBackgroundProcessingMode_reply(dec, pbFurtherMeasurementsDesired, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device7::AddToStateObject
 */

struct npt_command_ID3D12Device7_AddToStateObject {
    void *_self;
    const D3D12_STATE_OBJECT_DESC * pAddition;
    ID3D12StateObject * pStateObjectToGrowFrom;
    const IID * riid;
    void ** ppNewStateObject;
    /* Shadow: the guest-allocated id for ppNewStateObject arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppNewStateObject -> *ppNewStateObject} in the
     * context object table. */
    uint64_t _guest_id_ppNewStateObject;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device7_AddToStateObject(const D3D12_STATE_OBJECT_DESC * pAddition,const ID3D12StateObject * pStateObjectToGrowFrom,const IID * riid,void ** ppNewStateObject)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pAddition);
    if (pAddition)
        cmd_size += npt_sizeof_D3D12_STATE_OBJECT_DESC(pAddition, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppNewStateObject: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device7_AddToStateObject(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const D3D12_STATE_OBJECT_DESC * pAddition,
                                          ID3D12StateObject * pStateObjectToGrowFrom,
                                          const IID * riid,
                                          void ** ppNewStateObject)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device7_AddToStateObject(pAddition,pStateObjectToGrowFrom,riid,ppNewStateObject);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device7,
                                 NPT_METHOD_ID3D12Device7_AddToStateObject),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pAddition))
        npt_encode_D3D12_STATE_OBJECT_DESC(enc, pAddition);
    npt_encode_com_handle(enc, npt_object_get_id(pStateObjectToGrowFrom));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppNewStateObject = 0;
    if (ppNewStateObject) {
        _gid_ppNewStateObject = npt_com_allocate_next_id();
        *ppNewStateObject = (void *)(uintptr_t)_gid_ppNewStateObject;
    }
    npt_encode_uint64_t(enc, &_gid_ppNewStateObject);
}
}

static inline size_t
npt_sizeof_ID3D12Device7_AddToStateObject_reply(const D3D12_STATE_OBJECT_DESC * pAddition,const ID3D12StateObject * pStateObjectToGrowFrom,const IID * riid,void ** ppNewStateObject)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAddition (input only) */
/* skip pStateObjectToGrowFrom (input only) */
/* skip riid (input only) */
/* skip ppNewStateObject (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device7_AddToStateObject_reply(struct npt_cs_decoder *dec,
                                                void ** ppNewStateObject,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device7,
                                                  NPT_METHOD_ID3D12Device7_AddToStateObject))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device7_AddToStateObject reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device7,
                                       NPT_METHOD_ID3D12Device7_AddToStateObject));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppNewStateObject (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device7_AddToStateObject(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const D3D12_STATE_OBJECT_DESC * pAddition,
                                          ID3D12StateObject * pStateObjectToGrowFrom,
                                          const IID * riid,
                                          void ** ppNewStateObject,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device7_AddToStateObject(pAddition,pStateObjectToGrowFrom,riid,ppNewStateObject);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device7_AddToStateObject_reply(pAddition,pStateObjectToGrowFrom,riid,ppNewStateObject)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device7_AddToStateObject(enc, cmd_flags, object_id, pAddition, pStateObjectToGrowFrom, riid, ppNewStateObject);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device7_AddToStateObject(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const D3D12_STATE_OBJECT_DESC * pAddition,
                                         ID3D12StateObject * pStateObjectToGrowFrom,
                                         const IID * riid,
                                         void ** ppNewStateObject)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device7_AddToStateObject(ring, 0, object_id, pAddition, pStateObjectToGrowFrom, riid, ppNewStateObject, &submit);
}

static inline HRESULT
npt_call_ID3D12Device7_AddToStateObject(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const D3D12_STATE_OBJECT_DESC * pAddition,
                                        ID3D12StateObject * pStateObjectToGrowFrom,
                                        const IID * riid,
                                        void ** ppNewStateObject)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device7_AddToStateObject(ring, NPT_CMD_FLAG_REPLY, object_id, pAddition, pStateObjectToGrowFrom, riid, ppNewStateObject, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device7_AddToStateObject_reply(dec, ppNewStateObject, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device7::CreateProtectedResourceSession1
 */

struct npt_command_ID3D12Device7_CreateProtectedResourceSession1 {
    void *_self;
    const D3D12_PROTECTED_RESOURCE_SESSION_DESC1 * pDesc;
    const IID * riid;
    void ** ppSession;
    /* Shadow: the guest-allocated id for ppSession arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSession -> *ppSession} in the
     * context object table. */
    uint64_t _guest_id_ppSession;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device7_CreateProtectedResourceSession1(const D3D12_PROTECTED_RESOURCE_SESSION_DESC1 * pDesc,const IID * riid,void ** ppSession)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_PROTECTED_RESOURCE_SESSION_DESC1(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppSession: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device7_CreateProtectedResourceSession1(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         const D3D12_PROTECTED_RESOURCE_SESSION_DESC1 * pDesc,
                                                         const IID * riid,
                                                         void ** ppSession)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device7_CreateProtectedResourceSession1(pDesc,riid,ppSession);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device7,
                                 NPT_METHOD_ID3D12Device7_CreateProtectedResourceSession1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_PROTECTED_RESOURCE_SESSION_DESC1(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppSession = 0;
    if (ppSession) {
        _gid_ppSession = npt_com_allocate_next_id();
        *ppSession = (void *)(uintptr_t)_gid_ppSession;
    }
    npt_encode_uint64_t(enc, &_gid_ppSession);
}
}

static inline size_t
npt_sizeof_ID3D12Device7_CreateProtectedResourceSession1_reply(const D3D12_PROTECTED_RESOURCE_SESSION_DESC1 * pDesc,const IID * riid,void ** ppSession)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppSession (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device7_CreateProtectedResourceSession1_reply(struct npt_cs_decoder *dec,
                                                               void ** ppSession,
                                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device7,
                                                  NPT_METHOD_ID3D12Device7_CreateProtectedResourceSession1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device7_CreateProtectedResourceSession1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device7,
                                       NPT_METHOD_ID3D12Device7_CreateProtectedResourceSession1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSession (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device7_CreateProtectedResourceSession1(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         const D3D12_PROTECTED_RESOURCE_SESSION_DESC1 * pDesc,
                                                         const IID * riid,
                                                         void ** ppSession,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device7_CreateProtectedResourceSession1(pDesc,riid,ppSession);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device7_CreateProtectedResourceSession1_reply(pDesc,riid,ppSession)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device7_CreateProtectedResourceSession1(enc, cmd_flags, object_id, pDesc, riid, ppSession);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device7_CreateProtectedResourceSession1(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        const D3D12_PROTECTED_RESOURCE_SESSION_DESC1 * pDesc,
                                                        const IID * riid,
                                                        void ** ppSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device7_CreateProtectedResourceSession1(ring, 0, object_id, pDesc, riid, ppSession, &submit);
}

static inline HRESULT
npt_call_ID3D12Device7_CreateProtectedResourceSession1(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       const D3D12_PROTECTED_RESOURCE_SESSION_DESC1 * pDesc,
                                                       const IID * riid,
                                                       void ** ppSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device7_CreateProtectedResourceSession1(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppSession, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device7_CreateProtectedResourceSession1_reply(dec, ppSession, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device8::GetResourceAllocationInfo2
 */

struct npt_command_ID3D12Device8_GetResourceAllocationInfo2 {
    void *_self;
    UINT visibleMask;
    UINT numResourceDescs;
    const D3D12_RESOURCE_DESC1 * pResourceDescs;
    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1;
    D3D12_RESOURCE_ALLOCATION_INFO ret;
};

static inline size_t
npt_sizeof_ID3D12Device8_GetResourceAllocationInfo2(const UINT visibleMask,const UINT numResourceDescs,const D3D12_RESOURCE_DESC1 * pResourceDescs,const D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&visibleMask, max_mode);
    cmd_size += npt_sizeof_UINT(&numResourceDescs, max_mode);
    cmd_size += npt_sizeof_array_count(pResourceDescs ? numResourceDescs : 0);
    for (uint32_t _i = 0; _i < (pResourceDescs ? numResourceDescs : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC1(&pResourceDescs[_i], max_mode);
/* skip pResourceAllocationInfo1 (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device8_GetResourceAllocationInfo2(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT visibleMask,
                                                    UINT numResourceDescs,
                                                    const D3D12_RESOURCE_DESC1 * pResourceDescs,
                                                    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device8_GetResourceAllocationInfo2(visibleMask,numResourceDescs,pResourceDescs,pResourceAllocationInfo1);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_GetResourceAllocationInfo2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &visibleMask);
    npt_encode_UINT(enc, &numResourceDescs);
    if (pResourceDescs) {
        npt_encode_array_count(enc, numResourceDescs);
        for (uint32_t _i = 0; _i < (uint32_t)(numResourceDescs); _i++)
            npt_encode_D3D12_RESOURCE_DESC1(enc, &pResourceDescs[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pResourceAllocationInfo1 (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device8_GetResourceAllocationInfo2_reply(const UINT visibleMask,const UINT numResourceDescs,const D3D12_RESOURCE_DESC1 * pResourceDescs,const D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_RESOURCE_ALLOCATION_INFO(&(const D3D12_RESOURCE_ALLOCATION_INFO){0}, 1);
/* skip visibleMask (input only) */
/* skip numResourceDescs (input only) */
/* skip pResourceDescs (input only) */
    cmd_size += npt_sizeof_array_count(pResourceAllocationInfo1 ? numResourceDescs : 0);
    if (pResourceAllocationInfo1)
        cmd_size += (size_t)(numResourceDescs) * npt_sizeof_D3D12_RESOURCE_ALLOCATION_INFO1(&(const D3D12_RESOURCE_ALLOCATION_INFO1){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device8_GetResourceAllocationInfo2_reply(struct npt_cs_decoder *dec,
                                                          D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1,
                                                          D3D12_RESOURCE_ALLOCATION_INFO *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                                  NPT_METHOD_ID3D12Device8_GetResourceAllocationInfo2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device8_GetResourceAllocationInfo2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                       NPT_METHOD_ID3D12Device8_GetResourceAllocationInfo2));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_RESOURCE_ALLOCATION_INFO(dec, ret);
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pResourceAllocationInfo1) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D12_RESOURCE_ALLOCATION_INFO1(dec, (D3D12_RESOURCE_ALLOCATION_INFO1 *)&pResourceAllocationInfo1[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D12Device8_GetResourceAllocationInfo2(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT visibleMask,
                                                    UINT numResourceDescs,
                                                    const D3D12_RESOURCE_DESC1 * pResourceDescs,
                                                    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device8_GetResourceAllocationInfo2(visibleMask,numResourceDescs,pResourceDescs,pResourceAllocationInfo1);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device8_GetResourceAllocationInfo2_reply(visibleMask,numResourceDescs,pResourceDescs,pResourceAllocationInfo1)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device8_GetResourceAllocationInfo2(enc, cmd_flags, object_id, visibleMask, numResourceDescs, pResourceDescs, pResourceAllocationInfo1);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device8_GetResourceAllocationInfo2(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT visibleMask,
                                                   UINT numResourceDescs,
                                                   const D3D12_RESOURCE_DESC1 * pResourceDescs,
                                                   D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_GetResourceAllocationInfo2(ring, 0, object_id, visibleMask, numResourceDescs, pResourceDescs, pResourceAllocationInfo1, &submit);
}

static inline D3D12_RESOURCE_ALLOCATION_INFO
npt_call_ID3D12Device8_GetResourceAllocationInfo2(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT visibleMask,
                                                  UINT numResourceDescs,
                                                  const D3D12_RESOURCE_DESC1 * pResourceDescs,
                                                  D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_GetResourceAllocationInfo2(ring, NPT_CMD_FLAG_REPLY, object_id, visibleMask, numResourceDescs, pResourceDescs, pResourceAllocationInfo1, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_RESOURCE_ALLOCATION_INFO _ret;
        npt_decode_ID3D12Device8_GetResourceAllocationInfo2_reply(dec, pResourceAllocationInfo1, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_RESOURCE_ALLOCATION_INFO){0};
    }
}

/*
 * ID3D12Device8::CreateCommittedResource2
 */

struct npt_command_ID3D12Device8_CreateCommittedResource2 {
    void *_self;
    const D3D12_HEAP_PROPERTIES * pHeapProperties;
    D3D12_HEAP_FLAGS HeapFlags;
    const D3D12_RESOURCE_DESC1 * pDesc;
    D3D12_RESOURCE_STATES InitialResourceState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    const IID * riidResource;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device8_CreateCommittedResource2(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS HeapFlags,const D3D12_RESOURCE_DESC1 * pDesc,const D3D12_RESOURCE_STATES InitialResourceState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const IID * riidResource,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pHeapProperties);
    if (pHeapProperties)
        cmd_size += npt_sizeof_D3D12_HEAP_PROPERTIES(pHeapProperties, max_mode);
    cmd_size += npt_sizeof_D3D12_HEAP_FLAGS(&HeapFlags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC1(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOURCE_STATES(&InitialResourceState, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riidResource);
    if (riidResource)
        cmd_size += npt_sizeof_IID(riidResource, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device8_CreateCommittedResource2(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                  D3D12_HEAP_FLAGS HeapFlags,
                                                  const D3D12_RESOURCE_DESC1 * pDesc,
                                                  D3D12_RESOURCE_STATES InitialResourceState,
                                                  const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                  ID3D12ProtectedResourceSession * pProtectedSession,
                                                  const IID * riidResource,
                                                  void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device8_CreateCommittedResource2(pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,pProtectedSession,riidResource,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_CreateCommittedResource2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pHeapProperties))
        npt_encode_D3D12_HEAP_PROPERTIES(enc, pHeapProperties);
    npt_encode_D3D12_HEAP_FLAGS(enc, &HeapFlags);
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC1(enc, pDesc);
    npt_encode_D3D12_RESOURCE_STATES(enc, &InitialResourceState);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    npt_encode_com_handle(enc, npt_object_get_id(pProtectedSession));
    if (npt_encode_simple_pointer(enc, riidResource))
        npt_encode_IID(enc, riidResource);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device8_CreateCommittedResource2_reply(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS HeapFlags,const D3D12_RESOURCE_DESC1 * pDesc,const D3D12_RESOURCE_STATES InitialResourceState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const IID * riidResource,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pHeapProperties (input only) */
/* skip HeapFlags (input only) */
/* skip pDesc (input only) */
/* skip InitialResourceState (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip pProtectedSession (input only) */
/* skip riidResource (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device8_CreateCommittedResource2_reply(struct npt_cs_decoder *dec,
                                                        void ** ppvResource,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                                  NPT_METHOD_ID3D12Device8_CreateCommittedResource2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device8_CreateCommittedResource2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                       NPT_METHOD_ID3D12Device8_CreateCommittedResource2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device8_CreateCommittedResource2(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                  D3D12_HEAP_FLAGS HeapFlags,
                                                  const D3D12_RESOURCE_DESC1 * pDesc,
                                                  D3D12_RESOURCE_STATES InitialResourceState,
                                                  const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                  ID3D12ProtectedResourceSession * pProtectedSession,
                                                  const IID * riidResource,
                                                  void ** ppvResource,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device8_CreateCommittedResource2(pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,pProtectedSession,riidResource,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device8_CreateCommittedResource2_reply(pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,pProtectedSession,riidResource,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device8_CreateCommittedResource2(enc, cmd_flags, object_id, pHeapProperties, HeapFlags, pDesc, InitialResourceState, pOptimizedClearValue, pProtectedSession, riidResource, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device8_CreateCommittedResource2(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                 D3D12_HEAP_FLAGS HeapFlags,
                                                 const D3D12_RESOURCE_DESC1 * pDesc,
                                                 D3D12_RESOURCE_STATES InitialResourceState,
                                                 const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                 ID3D12ProtectedResourceSession * pProtectedSession,
                                                 const IID * riidResource,
                                                 void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_CreateCommittedResource2(ring, 0, object_id, pHeapProperties, HeapFlags, pDesc, InitialResourceState, pOptimizedClearValue, pProtectedSession, riidResource, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device8_CreateCommittedResource2(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                D3D12_HEAP_FLAGS HeapFlags,
                                                const D3D12_RESOURCE_DESC1 * pDesc,
                                                D3D12_RESOURCE_STATES InitialResourceState,
                                                const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                ID3D12ProtectedResourceSession * pProtectedSession,
                                                const IID * riidResource,
                                                void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_CreateCommittedResource2(ring, NPT_CMD_FLAG_REPLY, object_id, pHeapProperties, HeapFlags, pDesc, InitialResourceState, pOptimizedClearValue, pProtectedSession, riidResource, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device8_CreateCommittedResource2_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device8::CreatePlacedResource1
 */

struct npt_command_ID3D12Device8_CreatePlacedResource1 {
    void *_self;
    ID3D12Heap * pHeap;
    UINT64 HeapOffset;
    const D3D12_RESOURCE_DESC1 * pDesc;
    D3D12_RESOURCE_STATES InitialState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device8_CreatePlacedResource1(const ID3D12Heap * pHeap,const UINT64 HeapOffset,const D3D12_RESOURCE_DESC1 * pDesc,const D3D12_RESOURCE_STATES InitialState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const IID * riid,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&HeapOffset, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC1(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOURCE_STATES(&InitialState, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device8_CreatePlacedResource1(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12Heap * pHeap,
                                               UINT64 HeapOffset,
                                               const D3D12_RESOURCE_DESC1 * pDesc,
                                               D3D12_RESOURCE_STATES InitialState,
                                               const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                               const IID * riid,
                                               void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device8_CreatePlacedResource1(pHeap,HeapOffset,pDesc,InitialState,pOptimizedClearValue,riid,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_CreatePlacedResource1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pHeap));
    npt_encode_UINT64(enc, &HeapOffset);
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC1(enc, pDesc);
    npt_encode_D3D12_RESOURCE_STATES(enc, &InitialState);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device8_CreatePlacedResource1_reply(const ID3D12Heap * pHeap,const UINT64 HeapOffset,const D3D12_RESOURCE_DESC1 * pDesc,const D3D12_RESOURCE_STATES InitialState,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const IID * riid,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pHeap (input only) */
/* skip HeapOffset (input only) */
/* skip pDesc (input only) */
/* skip InitialState (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip riid (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device8_CreatePlacedResource1_reply(struct npt_cs_decoder *dec,
                                                     void ** ppvResource,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                                  NPT_METHOD_ID3D12Device8_CreatePlacedResource1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device8_CreatePlacedResource1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                       NPT_METHOD_ID3D12Device8_CreatePlacedResource1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device8_CreatePlacedResource1(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12Heap * pHeap,
                                               UINT64 HeapOffset,
                                               const D3D12_RESOURCE_DESC1 * pDesc,
                                               D3D12_RESOURCE_STATES InitialState,
                                               const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                               const IID * riid,
                                               void ** ppvResource,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device8_CreatePlacedResource1(pHeap,HeapOffset,pDesc,InitialState,pOptimizedClearValue,riid,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device8_CreatePlacedResource1_reply(pHeap,HeapOffset,pDesc,InitialState,pOptimizedClearValue,riid,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device8_CreatePlacedResource1(enc, cmd_flags, object_id, pHeap, HeapOffset, pDesc, InitialState, pOptimizedClearValue, riid, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device8_CreatePlacedResource1(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D12Heap * pHeap,
                                              UINT64 HeapOffset,
                                              const D3D12_RESOURCE_DESC1 * pDesc,
                                              D3D12_RESOURCE_STATES InitialState,
                                              const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                              const IID * riid,
                                              void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_CreatePlacedResource1(ring, 0, object_id, pHeap, HeapOffset, pDesc, InitialState, pOptimizedClearValue, riid, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device8_CreatePlacedResource1(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D12Heap * pHeap,
                                             UINT64 HeapOffset,
                                             const D3D12_RESOURCE_DESC1 * pDesc,
                                             D3D12_RESOURCE_STATES InitialState,
                                             const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                             const IID * riid,
                                             void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_CreatePlacedResource1(ring, NPT_CMD_FLAG_REPLY, object_id, pHeap, HeapOffset, pDesc, InitialState, pOptimizedClearValue, riid, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device8_CreatePlacedResource1_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device8::CreateSamplerFeedbackUnorderedAccessView
 */

struct npt_command_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView {
    void *_self;
    ID3D12Resource * pTargetedResource;
    ID3D12Resource * pFeedbackResource;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline size_t
npt_sizeof_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(const ID3D12Resource * pTargetedResource,const ID3D12Resource * pFeedbackResource,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DestDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D12Resource * pTargetedResource,
                                                                  ID3D12Resource * pFeedbackResource,
                                                                  D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(pTargetedResource,pFeedbackResource,DestDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pTargetedResource));
    npt_encode_com_handle(enc, npt_object_get_id(pFeedbackResource));
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DestDescriptor);
}

static inline size_t
npt_sizeof_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView_reply(const ID3D12Resource * pTargetedResource,const ID3D12Resource * pFeedbackResource,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pTargetedResource (input only) */
/* skip pFeedbackResource (input only) */
/* skip DestDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D12Resource * pTargetedResource,
                                                                  ID3D12Resource * pFeedbackResource,
                                                                  D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(pTargetedResource,pFeedbackResource,DestDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView_reply(pTargetedResource,pFeedbackResource,DestDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(enc, cmd_flags, object_id, pTargetedResource, pFeedbackResource, DestDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D12Resource * pTargetedResource,
                                                                 ID3D12Resource * pFeedbackResource,
                                                                 D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(ring, 0, object_id, pTargetedResource, pFeedbackResource, DestDescriptor, &submit);
}

static inline void
npt_call_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D12Resource * pTargetedResource,
                                                                ID3D12Resource * pFeedbackResource,
                                                                D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(ring, NPT_CMD_FLAG_REPLY, object_id, pTargetedResource, pFeedbackResource, DestDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device8::GetCopyableFootprints1
 */

struct npt_command_ID3D12Device8_GetCopyableFootprints1 {
    void *_self;
    const D3D12_RESOURCE_DESC1 * pResourceDesc;
    UINT FirstSubresource;
    UINT NumSubresources;
    UINT64 BaseOffset;
    D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts;
    UINT * pNumRows;
    UINT64 * pRowSizeInBytes;
    UINT64 * pTotalBytes;
};

static inline size_t
npt_sizeof_ID3D12Device8_GetCopyableFootprints1(const D3D12_RESOURCE_DESC1 * pResourceDesc,const UINT FirstSubresource,const UINT NumSubresources,const UINT64 BaseOffset,const D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,const UINT * pNumRows,const UINT64 * pRowSizeInBytes,const UINT64 * pTotalBytes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pResourceDesc);
    if (pResourceDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC1(pResourceDesc, max_mode);
    cmd_size += npt_sizeof_UINT(&FirstSubresource, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSubresources, max_mode);
    cmd_size += npt_sizeof_UINT64(&BaseOffset, max_mode);
/* skip pLayouts (output only) */
/* skip pNumRows (output only) */
/* skip pRowSizeInBytes (output only) */
/* skip pTotalBytes (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device8_GetCopyableFootprints1(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D12_RESOURCE_DESC1 * pResourceDesc,
                                                UINT FirstSubresource,
                                                UINT NumSubresources,
                                                UINT64 BaseOffset,
                                                D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                                UINT * pNumRows,
                                                UINT64 * pRowSizeInBytes,
                                                UINT64 * pTotalBytes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device8_GetCopyableFootprints1(pResourceDesc,FirstSubresource,NumSubresources,BaseOffset,pLayouts,pNumRows,pRowSizeInBytes,pTotalBytes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_GetCopyableFootprints1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pResourceDesc))
        npt_encode_D3D12_RESOURCE_DESC1(enc, pResourceDesc);
    npt_encode_UINT(enc, &FirstSubresource);
    npt_encode_UINT(enc, &NumSubresources);
    npt_encode_UINT64(enc, &BaseOffset);
/* skip pLayouts (output only) */
/* skip pNumRows (output only) */
/* skip pRowSizeInBytes (output only) */
/* skip pTotalBytes (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device8_GetCopyableFootprints1_reply(const D3D12_RESOURCE_DESC1 * pResourceDesc,const UINT FirstSubresource,const UINT NumSubresources,const UINT64 BaseOffset,const D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,const UINT * pNumRows,const UINT64 * pRowSizeInBytes,const UINT64 * pTotalBytes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResourceDesc (input only) */
/* skip FirstSubresource (input only) */
/* skip NumSubresources (input only) */
/* skip BaseOffset (input only) */
    cmd_size += npt_sizeof_array_count(pLayouts ? NumSubresources : 0);
    if (pLayouts)
        cmd_size += (size_t)(NumSubresources) * npt_sizeof_D3D12_PLACED_SUBRESOURCE_FOOTPRINT(&(const D3D12_PLACED_SUBRESOURCE_FOOTPRINT){0}, 1);
    cmd_size += npt_sizeof_array_count(pNumRows ? NumSubresources : 0);
    if (pNumRows)
        cmd_size += npt_sizeof_UINT_array(pNumRows, NumSubresources);
    cmd_size += npt_sizeof_array_count(pRowSizeInBytes ? NumSubresources : 0);
    if (pRowSizeInBytes)
        cmd_size += npt_sizeof_UINT64_array(pRowSizeInBytes, NumSubresources);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT64(&(const UINT64){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device8_GetCopyableFootprints1_reply(struct npt_cs_decoder *dec,
                                                      D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                                      UINT * pNumRows,
                                                      UINT64 * pRowSizeInBytes,
                                                      UINT64 * pTotalBytes)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                                  NPT_METHOD_ID3D12Device8_GetCopyableFootprints1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device8_GetCopyableFootprints1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                       NPT_METHOD_ID3D12Device8_GetCopyableFootprints1));
        return;
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pLayouts) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D12_PLACED_SUBRESOURCE_FOOTPRINT(dec, (D3D12_PLACED_SUBRESOURCE_FOOTPRINT *)&pLayouts[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pNumRows)
            npt_decode_UINT_array(dec, (UINT *)pNumRows, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pRowSizeInBytes)
            npt_decode_UINT64_array(dec, (UINT64 *)pRowSizeInBytes, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pTotalBytes)
            npt_decode_UINT64(dec, (UINT64 *)pTotalBytes);
    }
}

static inline void
npt_submit_ID3D12Device8_GetCopyableFootprints1(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D12_RESOURCE_DESC1 * pResourceDesc,
                                                UINT FirstSubresource,
                                                UINT NumSubresources,
                                                UINT64 BaseOffset,
                                                D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                                UINT * pNumRows,
                                                UINT64 * pRowSizeInBytes,
                                                UINT64 * pTotalBytes,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device8_GetCopyableFootprints1(pResourceDesc,FirstSubresource,NumSubresources,BaseOffset,pLayouts,pNumRows,pRowSizeInBytes,pTotalBytes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device8_GetCopyableFootprints1_reply(pResourceDesc,FirstSubresource,NumSubresources,BaseOffset,pLayouts,pNumRows,pRowSizeInBytes,pTotalBytes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device8_GetCopyableFootprints1(enc, cmd_flags, object_id, pResourceDesc, FirstSubresource, NumSubresources, BaseOffset, pLayouts, pNumRows, pRowSizeInBytes, pTotalBytes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device8_GetCopyableFootprints1(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const D3D12_RESOURCE_DESC1 * pResourceDesc,
                                               UINT FirstSubresource,
                                               UINT NumSubresources,
                                               UINT64 BaseOffset,
                                               D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                               UINT * pNumRows,
                                               UINT64 * pRowSizeInBytes,
                                               UINT64 * pTotalBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_GetCopyableFootprints1(ring, 0, object_id, pResourceDesc, FirstSubresource, NumSubresources, BaseOffset, pLayouts, pNumRows, pRowSizeInBytes, pTotalBytes, &submit);
}

static inline void
npt_call_ID3D12Device8_GetCopyableFootprints1(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const D3D12_RESOURCE_DESC1 * pResourceDesc,
                                              UINT FirstSubresource,
                                              UINT NumSubresources,
                                              UINT64 BaseOffset,
                                              D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts,
                                              UINT * pNumRows,
                                              UINT64 * pRowSizeInBytes,
                                              UINT64 * pTotalBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device8_GetCopyableFootprints1(ring, NPT_CMD_FLAG_REPLY, object_id, pResourceDesc, FirstSubresource, NumSubresources, BaseOffset, pLayouts, pNumRows, pRowSizeInBytes, pTotalBytes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D12Device8_GetCopyableFootprints1_reply(dec, pLayouts, pNumRows, pRowSizeInBytes, pTotalBytes);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device9::CreateShaderCacheSession
 */

struct npt_command_ID3D12Device9_CreateShaderCacheSession {
    void *_self;
    const D3D12_SHADER_CACHE_SESSION_DESC * pDesc;
    const IID * riid;
    void ** ppvSession;
    /* Shadow: the guest-allocated id for ppvSession arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvSession -> *ppvSession} in the
     * context object table. */
    uint64_t _guest_id_ppvSession;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device9_CreateShaderCacheSession(const D3D12_SHADER_CACHE_SESSION_DESC * pDesc,const IID * riid,void ** ppvSession)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_SHADER_CACHE_SESSION_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvSession: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device9_CreateShaderCacheSession(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_SHADER_CACHE_SESSION_DESC * pDesc,
                                                  const IID * riid,
                                                  void ** ppvSession)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device9_CreateShaderCacheSession(pDesc,riid,ppvSession);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                 NPT_METHOD_ID3D12Device9_CreateShaderCacheSession),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_SHADER_CACHE_SESSION_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvSession = 0;
    if (ppvSession) {
        _gid_ppvSession = npt_com_allocate_next_id();
        *ppvSession = (void *)(uintptr_t)_gid_ppvSession;
    }
    npt_encode_uint64_t(enc, &_gid_ppvSession);
}
}

static inline size_t
npt_sizeof_ID3D12Device9_CreateShaderCacheSession_reply(const D3D12_SHADER_CACHE_SESSION_DESC * pDesc,const IID * riid,void ** ppvSession)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip riid (input only) */
/* skip ppvSession (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device9_CreateShaderCacheSession_reply(struct npt_cs_decoder *dec,
                                                        void ** ppvSession,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                                  NPT_METHOD_ID3D12Device9_CreateShaderCacheSession))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device9_CreateShaderCacheSession reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                       NPT_METHOD_ID3D12Device9_CreateShaderCacheSession));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvSession (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device9_CreateShaderCacheSession(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_SHADER_CACHE_SESSION_DESC * pDesc,
                                                  const IID * riid,
                                                  void ** ppvSession,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device9_CreateShaderCacheSession(pDesc,riid,ppvSession);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device9_CreateShaderCacheSession_reply(pDesc,riid,ppvSession)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device9_CreateShaderCacheSession(enc, cmd_flags, object_id, pDesc, riid, ppvSession);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device9_CreateShaderCacheSession(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const D3D12_SHADER_CACHE_SESSION_DESC * pDesc,
                                                 const IID * riid,
                                                 void ** ppvSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device9_CreateShaderCacheSession(ring, 0, object_id, pDesc, riid, ppvSession, &submit);
}

static inline HRESULT
npt_call_ID3D12Device9_CreateShaderCacheSession(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const D3D12_SHADER_CACHE_SESSION_DESC * pDesc,
                                                const IID * riid,
                                                void ** ppvSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device9_CreateShaderCacheSession(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, riid, ppvSession, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device9_CreateShaderCacheSession_reply(dec, ppvSession, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device9::ShaderCacheControl
 */

struct npt_command_ID3D12Device9_ShaderCacheControl {
    void *_self;
    D3D12_SHADER_CACHE_KIND_FLAGS Kinds;
    D3D12_SHADER_CACHE_CONTROL_FLAGS Control;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device9_ShaderCacheControl(const D3D12_SHADER_CACHE_KIND_FLAGS Kinds,const D3D12_SHADER_CACHE_CONTROL_FLAGS Control)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_SHADER_CACHE_KIND_FLAGS(&Kinds, max_mode);
    cmd_size += npt_sizeof_D3D12_SHADER_CACHE_CONTROL_FLAGS(&Control, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device9_ShaderCacheControl(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            D3D12_SHADER_CACHE_KIND_FLAGS Kinds,
                                            D3D12_SHADER_CACHE_CONTROL_FLAGS Control)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device9_ShaderCacheControl(Kinds,Control);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                 NPT_METHOD_ID3D12Device9_ShaderCacheControl),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_SHADER_CACHE_KIND_FLAGS(enc, &Kinds);
    npt_encode_D3D12_SHADER_CACHE_CONTROL_FLAGS(enc, &Control);
}

static inline size_t
npt_sizeof_ID3D12Device9_ShaderCacheControl_reply(const D3D12_SHADER_CACHE_KIND_FLAGS Kinds,const D3D12_SHADER_CACHE_CONTROL_FLAGS Control)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Kinds (input only) */
/* skip Control (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device9_ShaderCacheControl_reply(struct npt_cs_decoder *dec,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                                  NPT_METHOD_ID3D12Device9_ShaderCacheControl))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device9_ShaderCacheControl reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                       NPT_METHOD_ID3D12Device9_ShaderCacheControl));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Device9_ShaderCacheControl(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            D3D12_SHADER_CACHE_KIND_FLAGS Kinds,
                                            D3D12_SHADER_CACHE_CONTROL_FLAGS Control,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device9_ShaderCacheControl(Kinds,Control);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device9_ShaderCacheControl_reply(Kinds,Control)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device9_ShaderCacheControl(enc, cmd_flags, object_id, Kinds, Control);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device9_ShaderCacheControl(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           D3D12_SHADER_CACHE_KIND_FLAGS Kinds,
                                           D3D12_SHADER_CACHE_CONTROL_FLAGS Control)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device9_ShaderCacheControl(ring, 0, object_id, Kinds, Control, &submit);
}

static inline HRESULT
npt_call_ID3D12Device9_ShaderCacheControl(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          D3D12_SHADER_CACHE_KIND_FLAGS Kinds,
                                          D3D12_SHADER_CACHE_CONTROL_FLAGS Control)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device9_ShaderCacheControl(ring, NPT_CMD_FLAG_REPLY, object_id, Kinds, Control, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device9_ShaderCacheControl_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device9::CreateCommandQueue1
 */

struct npt_command_ID3D12Device9_CreateCommandQueue1 {
    void *_self;
    const D3D12_COMMAND_QUEUE_DESC * pDesc;
    const IID * CreatorID;
    const IID * riid;
    void ** ppCommandQueue;
    /* Shadow: the guest-allocated id for ppCommandQueue arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandQueue -> *ppCommandQueue} in the
     * context object table. */
    uint64_t _guest_id_ppCommandQueue;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device9_CreateCommandQueue1(const D3D12_COMMAND_QUEUE_DESC * pDesc,const IID * CreatorID,const IID * riid,void ** ppCommandQueue)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_COMMAND_QUEUE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(CreatorID);
    if (CreatorID)
        cmd_size += npt_sizeof_IID(CreatorID, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppCommandQueue: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device9_CreateCommandQueue1(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const D3D12_COMMAND_QUEUE_DESC * pDesc,
                                             const IID * CreatorID,
                                             const IID * riid,
                                             void ** ppCommandQueue)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device9_CreateCommandQueue1(pDesc,CreatorID,riid,ppCommandQueue);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                 NPT_METHOD_ID3D12Device9_CreateCommandQueue1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_COMMAND_QUEUE_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, CreatorID))
        npt_encode_IID(enc, CreatorID);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppCommandQueue = 0;
    if (ppCommandQueue) {
        _gid_ppCommandQueue = npt_com_allocate_next_id();
        *ppCommandQueue = (void *)(uintptr_t)_gid_ppCommandQueue;
    }
    npt_encode_uint64_t(enc, &_gid_ppCommandQueue);
}
}

static inline size_t
npt_sizeof_ID3D12Device9_CreateCommandQueue1_reply(const D3D12_COMMAND_QUEUE_DESC * pDesc,const IID * CreatorID,const IID * riid,void ** ppCommandQueue)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip CreatorID (input only) */
/* skip riid (input only) */
/* skip ppCommandQueue (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device9_CreateCommandQueue1_reply(struct npt_cs_decoder *dec,
                                                   void ** ppCommandQueue,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                                  NPT_METHOD_ID3D12Device9_CreateCommandQueue1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device9_CreateCommandQueue1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                       NPT_METHOD_ID3D12Device9_CreateCommandQueue1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppCommandQueue (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device9_CreateCommandQueue1(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const D3D12_COMMAND_QUEUE_DESC * pDesc,
                                             const IID * CreatorID,
                                             const IID * riid,
                                             void ** ppCommandQueue,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device9_CreateCommandQueue1(pDesc,CreatorID,riid,ppCommandQueue);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device9_CreateCommandQueue1_reply(pDesc,CreatorID,riid,ppCommandQueue)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device9_CreateCommandQueue1(enc, cmd_flags, object_id, pDesc, CreatorID, riid, ppCommandQueue);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device9_CreateCommandQueue1(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const D3D12_COMMAND_QUEUE_DESC * pDesc,
                                            const IID * CreatorID,
                                            const IID * riid,
                                            void ** ppCommandQueue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device9_CreateCommandQueue1(ring, 0, object_id, pDesc, CreatorID, riid, ppCommandQueue, &submit);
}

static inline HRESULT
npt_call_ID3D12Device9_CreateCommandQueue1(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           const D3D12_COMMAND_QUEUE_DESC * pDesc,
                                           const IID * CreatorID,
                                           const IID * riid,
                                           void ** ppCommandQueue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device9_CreateCommandQueue1(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, CreatorID, riid, ppCommandQueue, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device9_CreateCommandQueue1_reply(dec, ppCommandQueue, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device10::CreateCommittedResource3
 */

struct npt_command_ID3D12Device10_CreateCommittedResource3 {
    void *_self;
    const D3D12_HEAP_PROPERTIES * pHeapProperties;
    D3D12_HEAP_FLAGS HeapFlags;
    const D3D12_RESOURCE_DESC1 * pDesc;
    D3D12_BARRIER_LAYOUT InitialLayout;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    UINT32 NumCastableFormats;
    const DXGI_FORMAT * pCastableFormats;
    const IID * riidResource;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device10_CreateCommittedResource3(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS HeapFlags,const D3D12_RESOURCE_DESC1 * pDesc,const D3D12_BARRIER_LAYOUT InitialLayout,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const UINT32 NumCastableFormats,const DXGI_FORMAT * pCastableFormats,const IID * riidResource,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pHeapProperties);
    if (pHeapProperties)
        cmd_size += npt_sizeof_D3D12_HEAP_PROPERTIES(pHeapProperties, max_mode);
    cmd_size += npt_sizeof_D3D12_HEAP_FLAGS(&HeapFlags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC1(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_BARRIER_LAYOUT(&InitialLayout, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT32(&NumCastableFormats, max_mode);
    cmd_size += npt_sizeof_array_count(pCastableFormats ? NumCastableFormats : 0);
    if (pCastableFormats)
        cmd_size += npt_sizeof_DXGI_FORMAT_array(pCastableFormats, NumCastableFormats);
    cmd_size += npt_sizeof_simple_pointer(riidResource);
    if (riidResource)
        cmd_size += npt_sizeof_IID(riidResource, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device10_CreateCommittedResource3(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                   D3D12_HEAP_FLAGS HeapFlags,
                                                   const D3D12_RESOURCE_DESC1 * pDesc,
                                                   D3D12_BARRIER_LAYOUT InitialLayout,
                                                   const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                   ID3D12ProtectedResourceSession * pProtectedSession,
                                                   UINT32 NumCastableFormats,
                                                   const DXGI_FORMAT * pCastableFormats,
                                                   const IID * riidResource,
                                                   void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device10_CreateCommittedResource3(pHeapProperties,HeapFlags,pDesc,InitialLayout,pOptimizedClearValue,pProtectedSession,NumCastableFormats,pCastableFormats,riidResource,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                 NPT_METHOD_ID3D12Device10_CreateCommittedResource3),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pHeapProperties))
        npt_encode_D3D12_HEAP_PROPERTIES(enc, pHeapProperties);
    npt_encode_D3D12_HEAP_FLAGS(enc, &HeapFlags);
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC1(enc, pDesc);
    npt_encode_D3D12_BARRIER_LAYOUT(enc, &InitialLayout);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    npt_encode_com_handle(enc, npt_object_get_id(pProtectedSession));
    npt_encode_UINT32(enc, &NumCastableFormats);
    if (pCastableFormats) {
        npt_encode_array_count(enc, NumCastableFormats);
        npt_encode_DXGI_FORMAT_array(enc, pCastableFormats, NumCastableFormats);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, riidResource))
        npt_encode_IID(enc, riidResource);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device10_CreateCommittedResource3_reply(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS HeapFlags,const D3D12_RESOURCE_DESC1 * pDesc,const D3D12_BARRIER_LAYOUT InitialLayout,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const UINT32 NumCastableFormats,const DXGI_FORMAT * pCastableFormats,const IID * riidResource,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pHeapProperties (input only) */
/* skip HeapFlags (input only) */
/* skip pDesc (input only) */
/* skip InitialLayout (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip pProtectedSession (input only) */
/* skip NumCastableFormats (input only) */
/* skip pCastableFormats (input only) */
/* skip riidResource (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device10_CreateCommittedResource3_reply(struct npt_cs_decoder *dec,
                                                         void ** ppvResource,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                                  NPT_METHOD_ID3D12Device10_CreateCommittedResource3))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device10_CreateCommittedResource3 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                       NPT_METHOD_ID3D12Device10_CreateCommittedResource3));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device10_CreateCommittedResource3(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                   D3D12_HEAP_FLAGS HeapFlags,
                                                   const D3D12_RESOURCE_DESC1 * pDesc,
                                                   D3D12_BARRIER_LAYOUT InitialLayout,
                                                   const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                   ID3D12ProtectedResourceSession * pProtectedSession,
                                                   UINT32 NumCastableFormats,
                                                   const DXGI_FORMAT * pCastableFormats,
                                                   const IID * riidResource,
                                                   void ** ppvResource,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device10_CreateCommittedResource3(pHeapProperties,HeapFlags,pDesc,InitialLayout,pOptimizedClearValue,pProtectedSession,NumCastableFormats,pCastableFormats,riidResource,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device10_CreateCommittedResource3_reply(pHeapProperties,HeapFlags,pDesc,InitialLayout,pOptimizedClearValue,pProtectedSession,NumCastableFormats,pCastableFormats,riidResource,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device10_CreateCommittedResource3(enc, cmd_flags, object_id, pHeapProperties, HeapFlags, pDesc, InitialLayout, pOptimizedClearValue, pProtectedSession, NumCastableFormats, pCastableFormats, riidResource, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device10_CreateCommittedResource3(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                  D3D12_HEAP_FLAGS HeapFlags,
                                                  const D3D12_RESOURCE_DESC1 * pDesc,
                                                  D3D12_BARRIER_LAYOUT InitialLayout,
                                                  const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                  ID3D12ProtectedResourceSession * pProtectedSession,
                                                  UINT32 NumCastableFormats,
                                                  const DXGI_FORMAT * pCastableFormats,
                                                  const IID * riidResource,
                                                  void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device10_CreateCommittedResource3(ring, 0, object_id, pHeapProperties, HeapFlags, pDesc, InitialLayout, pOptimizedClearValue, pProtectedSession, NumCastableFormats, pCastableFormats, riidResource, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device10_CreateCommittedResource3(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                 D3D12_HEAP_FLAGS HeapFlags,
                                                 const D3D12_RESOURCE_DESC1 * pDesc,
                                                 D3D12_BARRIER_LAYOUT InitialLayout,
                                                 const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                 ID3D12ProtectedResourceSession * pProtectedSession,
                                                 UINT32 NumCastableFormats,
                                                 const DXGI_FORMAT * pCastableFormats,
                                                 const IID * riidResource,
                                                 void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device10_CreateCommittedResource3(ring, NPT_CMD_FLAG_REPLY, object_id, pHeapProperties, HeapFlags, pDesc, InitialLayout, pOptimizedClearValue, pProtectedSession, NumCastableFormats, pCastableFormats, riidResource, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device10_CreateCommittedResource3_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device10::CreatePlacedResource2
 */

struct npt_command_ID3D12Device10_CreatePlacedResource2 {
    void *_self;
    ID3D12Heap * pHeap;
    UINT64 HeapOffset;
    const D3D12_RESOURCE_DESC1 * pDesc;
    D3D12_BARRIER_LAYOUT InitialLayout;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    UINT32 NumCastableFormats;
    const DXGI_FORMAT * pCastableFormats;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device10_CreatePlacedResource2(const ID3D12Heap * pHeap,const UINT64 HeapOffset,const D3D12_RESOURCE_DESC1 * pDesc,const D3D12_BARRIER_LAYOUT InitialLayout,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const UINT32 NumCastableFormats,const DXGI_FORMAT * pCastableFormats,const IID * riid,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&HeapOffset, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC1(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_BARRIER_LAYOUT(&InitialLayout, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += npt_sizeof_UINT32(&NumCastableFormats, max_mode);
    cmd_size += npt_sizeof_array_count(pCastableFormats ? NumCastableFormats : 0);
    if (pCastableFormats)
        cmd_size += npt_sizeof_DXGI_FORMAT_array(pCastableFormats, NumCastableFormats);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device10_CreatePlacedResource2(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D12Heap * pHeap,
                                                UINT64 HeapOffset,
                                                const D3D12_RESOURCE_DESC1 * pDesc,
                                                D3D12_BARRIER_LAYOUT InitialLayout,
                                                const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                UINT32 NumCastableFormats,
                                                const DXGI_FORMAT * pCastableFormats,
                                                const IID * riid,
                                                void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device10_CreatePlacedResource2(pHeap,HeapOffset,pDesc,InitialLayout,pOptimizedClearValue,NumCastableFormats,pCastableFormats,riid,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                 NPT_METHOD_ID3D12Device10_CreatePlacedResource2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pHeap));
    npt_encode_UINT64(enc, &HeapOffset);
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC1(enc, pDesc);
    npt_encode_D3D12_BARRIER_LAYOUT(enc, &InitialLayout);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    npt_encode_UINT32(enc, &NumCastableFormats);
    if (pCastableFormats) {
        npt_encode_array_count(enc, NumCastableFormats);
        npt_encode_DXGI_FORMAT_array(enc, pCastableFormats, NumCastableFormats);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device10_CreatePlacedResource2_reply(const ID3D12Heap * pHeap,const UINT64 HeapOffset,const D3D12_RESOURCE_DESC1 * pDesc,const D3D12_BARRIER_LAYOUT InitialLayout,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const UINT32 NumCastableFormats,const DXGI_FORMAT * pCastableFormats,const IID * riid,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pHeap (input only) */
/* skip HeapOffset (input only) */
/* skip pDesc (input only) */
/* skip InitialLayout (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip NumCastableFormats (input only) */
/* skip pCastableFormats (input only) */
/* skip riid (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device10_CreatePlacedResource2_reply(struct npt_cs_decoder *dec,
                                                      void ** ppvResource,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                                  NPT_METHOD_ID3D12Device10_CreatePlacedResource2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device10_CreatePlacedResource2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                       NPT_METHOD_ID3D12Device10_CreatePlacedResource2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device10_CreatePlacedResource2(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D12Heap * pHeap,
                                                UINT64 HeapOffset,
                                                const D3D12_RESOURCE_DESC1 * pDesc,
                                                D3D12_BARRIER_LAYOUT InitialLayout,
                                                const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                UINT32 NumCastableFormats,
                                                const DXGI_FORMAT * pCastableFormats,
                                                const IID * riid,
                                                void ** ppvResource,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device10_CreatePlacedResource2(pHeap,HeapOffset,pDesc,InitialLayout,pOptimizedClearValue,NumCastableFormats,pCastableFormats,riid,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device10_CreatePlacedResource2_reply(pHeap,HeapOffset,pDesc,InitialLayout,pOptimizedClearValue,NumCastableFormats,pCastableFormats,riid,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device10_CreatePlacedResource2(enc, cmd_flags, object_id, pHeap, HeapOffset, pDesc, InitialLayout, pOptimizedClearValue, NumCastableFormats, pCastableFormats, riid, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device10_CreatePlacedResource2(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D12Heap * pHeap,
                                               UINT64 HeapOffset,
                                               const D3D12_RESOURCE_DESC1 * pDesc,
                                               D3D12_BARRIER_LAYOUT InitialLayout,
                                               const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                               UINT32 NumCastableFormats,
                                               const DXGI_FORMAT * pCastableFormats,
                                               const IID * riid,
                                               void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device10_CreatePlacedResource2(ring, 0, object_id, pHeap, HeapOffset, pDesc, InitialLayout, pOptimizedClearValue, NumCastableFormats, pCastableFormats, riid, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device10_CreatePlacedResource2(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D12Heap * pHeap,
                                              UINT64 HeapOffset,
                                              const D3D12_RESOURCE_DESC1 * pDesc,
                                              D3D12_BARRIER_LAYOUT InitialLayout,
                                              const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                              UINT32 NumCastableFormats,
                                              const DXGI_FORMAT * pCastableFormats,
                                              const IID * riid,
                                              void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device10_CreatePlacedResource2(ring, NPT_CMD_FLAG_REPLY, object_id, pHeap, HeapOffset, pDesc, InitialLayout, pOptimizedClearValue, NumCastableFormats, pCastableFormats, riid, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device10_CreatePlacedResource2_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device10::CreateReservedResource2
 */

struct npt_command_ID3D12Device10_CreateReservedResource2 {
    void *_self;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_BARRIER_LAYOUT InitialLayout;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    UINT32 NumCastableFormats;
    const DXGI_FORMAT * pCastableFormats;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device10_CreateReservedResource2(const D3D12_RESOURCE_DESC * pDesc,const D3D12_BARRIER_LAYOUT InitialLayout,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const UINT32 NumCastableFormats,const DXGI_FORMAT * pCastableFormats,const IID * riid,void ** ppvResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_BARRIER_LAYOUT(&InitialLayout, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOptimizedClearValue);
    if (pOptimizedClearValue)
        cmd_size += npt_sizeof_D3D12_CLEAR_VALUE(pOptimizedClearValue, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT32(&NumCastableFormats, max_mode);
    cmd_size += npt_sizeof_array_count(pCastableFormats ? NumCastableFormats : 0);
    if (pCastableFormats)
        cmd_size += npt_sizeof_DXGI_FORMAT_array(pCastableFormats, NumCastableFormats);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device10_CreateReservedResource2(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_RESOURCE_DESC * pDesc,
                                                  D3D12_BARRIER_LAYOUT InitialLayout,
                                                  const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                  ID3D12ProtectedResourceSession * pProtectedSession,
                                                  UINT32 NumCastableFormats,
                                                  const DXGI_FORMAT * pCastableFormats,
                                                  const IID * riid,
                                                  void ** ppvResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device10_CreateReservedResource2(pDesc,InitialLayout,pOptimizedClearValue,pProtectedSession,NumCastableFormats,pCastableFormats,riid,ppvResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                 NPT_METHOD_ID3D12Device10_CreateReservedResource2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RESOURCE_DESC(enc, pDesc);
    npt_encode_D3D12_BARRIER_LAYOUT(enc, &InitialLayout);
    if (npt_encode_simple_pointer(enc, pOptimizedClearValue))
        npt_encode_D3D12_CLEAR_VALUE(enc, pOptimizedClearValue);
    npt_encode_com_handle(enc, npt_object_get_id(pProtectedSession));
    npt_encode_UINT32(enc, &NumCastableFormats);
    if (pCastableFormats) {
        npt_encode_array_count(enc, NumCastableFormats);
        npt_encode_DXGI_FORMAT_array(enc, pCastableFormats, NumCastableFormats);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
}

static inline size_t
npt_sizeof_ID3D12Device10_CreateReservedResource2_reply(const D3D12_RESOURCE_DESC * pDesc,const D3D12_BARRIER_LAYOUT InitialLayout,const D3D12_CLEAR_VALUE * pOptimizedClearValue,const ID3D12ProtectedResourceSession * pProtectedSession,const UINT32 NumCastableFormats,const DXGI_FORMAT * pCastableFormats,const IID * riid,void ** ppvResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip InitialLayout (input only) */
/* skip pOptimizedClearValue (input only) */
/* skip pProtectedSession (input only) */
/* skip NumCastableFormats (input only) */
/* skip pCastableFormats (input only) */
/* skip riid (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device10_CreateReservedResource2_reply(struct npt_cs_decoder *dec,
                                                        void ** ppvResource,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                                  NPT_METHOD_ID3D12Device10_CreateReservedResource2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device10_CreateReservedResource2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                       NPT_METHOD_ID3D12Device10_CreateReservedResource2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device10_CreateReservedResource2(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_RESOURCE_DESC * pDesc,
                                                  D3D12_BARRIER_LAYOUT InitialLayout,
                                                  const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                  ID3D12ProtectedResourceSession * pProtectedSession,
                                                  UINT32 NumCastableFormats,
                                                  const DXGI_FORMAT * pCastableFormats,
                                                  const IID * riid,
                                                  void ** ppvResource,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device10_CreateReservedResource2(pDesc,InitialLayout,pOptimizedClearValue,pProtectedSession,NumCastableFormats,pCastableFormats,riid,ppvResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device10_CreateReservedResource2_reply(pDesc,InitialLayout,pOptimizedClearValue,pProtectedSession,NumCastableFormats,pCastableFormats,riid,ppvResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device10_CreateReservedResource2(enc, cmd_flags, object_id, pDesc, InitialLayout, pOptimizedClearValue, pProtectedSession, NumCastableFormats, pCastableFormats, riid, ppvResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device10_CreateReservedResource2(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const D3D12_RESOURCE_DESC * pDesc,
                                                 D3D12_BARRIER_LAYOUT InitialLayout,
                                                 const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                 ID3D12ProtectedResourceSession * pProtectedSession,
                                                 UINT32 NumCastableFormats,
                                                 const DXGI_FORMAT * pCastableFormats,
                                                 const IID * riid,
                                                 void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device10_CreateReservedResource2(ring, 0, object_id, pDesc, InitialLayout, pOptimizedClearValue, pProtectedSession, NumCastableFormats, pCastableFormats, riid, ppvResource, &submit);
}

static inline HRESULT
npt_call_ID3D12Device10_CreateReservedResource2(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const D3D12_RESOURCE_DESC * pDesc,
                                                D3D12_BARRIER_LAYOUT InitialLayout,
                                                const D3D12_CLEAR_VALUE * pOptimizedClearValue,
                                                ID3D12ProtectedResourceSession * pProtectedSession,
                                                UINT32 NumCastableFormats,
                                                const DXGI_FORMAT * pCastableFormats,
                                                const IID * riid,
                                                void ** ppvResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device10_CreateReservedResource2(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, InitialLayout, pOptimizedClearValue, pProtectedSession, NumCastableFormats, pCastableFormats, riid, ppvResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device10_CreateReservedResource2_reply(dec, ppvResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device11::CreateSampler2
 */

struct npt_command_ID3D12Device11_CreateSampler2 {
    void *_self;
    const D3D12_SAMPLER_DESC2 * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline size_t
npt_sizeof_ID3D12Device11_CreateSampler2(const D3D12_SAMPLER_DESC2 * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_SAMPLER_DESC2(pDesc, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DestDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device11_CreateSampler2(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const D3D12_SAMPLER_DESC2 * pDesc,
                                         D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device11_CreateSampler2(pDesc,DestDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device11,
                                 NPT_METHOD_ID3D12Device11_CreateSampler2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_SAMPLER_DESC2(enc, pDesc);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DestDescriptor);
}

static inline size_t
npt_sizeof_ID3D12Device11_CreateSampler2_reply(const D3D12_SAMPLER_DESC2 * pDesc,const D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip DestDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Device11_CreateSampler2(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const D3D12_SAMPLER_DESC2 * pDesc,
                                         D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device11_CreateSampler2(pDesc,DestDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device11_CreateSampler2_reply(pDesc,DestDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device11_CreateSampler2(enc, cmd_flags, object_id, pDesc, DestDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device11_CreateSampler2(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const D3D12_SAMPLER_DESC2 * pDesc,
                                        D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device11_CreateSampler2(ring, 0, object_id, pDesc, DestDescriptor, &submit);
}

static inline void
npt_call_ID3D12Device11_CreateSampler2(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const D3D12_SAMPLER_DESC2 * pDesc,
                                       D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device11_CreateSampler2(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, DestDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Device12::GetResourceAllocationInfo3
 */

struct npt_command_ID3D12Device12_GetResourceAllocationInfo3 {
    void *_self;
    UINT visibleMask;
    UINT numResourceDescs;
    const D3D12_RESOURCE_DESC1 * pResourceDescs;
    const UINT32 * pNumCastableFormats;
    const DXGI_FORMAT ** ppCastableFormats;
    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1;
    D3D12_RESOURCE_ALLOCATION_INFO ret;
};

static inline size_t
npt_sizeof_ID3D12Device12_GetResourceAllocationInfo3(const UINT visibleMask,const UINT numResourceDescs,const D3D12_RESOURCE_DESC1 * pResourceDescs,const UINT32 * pNumCastableFormats,const DXGI_FORMAT ** ppCastableFormats,const D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&visibleMask, max_mode);
    cmd_size += npt_sizeof_UINT(&numResourceDescs, max_mode);
    cmd_size += npt_sizeof_array_count(pResourceDescs ? numResourceDescs : 0);
    for (uint32_t _i = 0; _i < (pResourceDescs ? numResourceDescs : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RESOURCE_DESC1(&pResourceDescs[_i], max_mode);
    cmd_size += npt_sizeof_array_count(pNumCastableFormats ? numResourceDescs : 0);
    if (pNumCastableFormats)
        cmd_size += npt_sizeof_UINT32_array(pNumCastableFormats, numResourceDescs);
    /* ppCastableFormats: unsized optional, always NULL */
    cmd_size += npt_sizeof_array_count(0);
/* skip pResourceAllocationInfo1 (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device12_GetResourceAllocationInfo3(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     UINT visibleMask,
                                                     UINT numResourceDescs,
                                                     const D3D12_RESOURCE_DESC1 * pResourceDescs,
                                                     const UINT32 * pNumCastableFormats,
                                                     const DXGI_FORMAT ** ppCastableFormats,
                                                     D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device12_GetResourceAllocationInfo3(visibleMask,numResourceDescs,pResourceDescs,pNumCastableFormats,ppCastableFormats,pResourceAllocationInfo1);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device12,
                                 NPT_METHOD_ID3D12Device12_GetResourceAllocationInfo3),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &visibleMask);
    npt_encode_UINT(enc, &numResourceDescs);
    if (pResourceDescs) {
        npt_encode_array_count(enc, numResourceDescs);
        for (uint32_t _i = 0; _i < (uint32_t)(numResourceDescs); _i++)
            npt_encode_D3D12_RESOURCE_DESC1(enc, &pResourceDescs[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pNumCastableFormats) {
        npt_encode_array_count(enc, numResourceDescs);
        npt_encode_UINT32_array(enc, pNumCastableFormats, numResourceDescs);
    } else {
        npt_encode_array_count(enc, 0);
    }
    /* ppCastableFormats: unsized optional, encode NULL */
    npt_encode_array_count(enc, 0);
/* skip pResourceAllocationInfo1 (output only) */
}

static inline size_t
npt_sizeof_ID3D12Device12_GetResourceAllocationInfo3_reply(const UINT visibleMask,const UINT numResourceDescs,const D3D12_RESOURCE_DESC1 * pResourceDescs,const UINT32 * pNumCastableFormats,const DXGI_FORMAT ** ppCastableFormats,const D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_RESOURCE_ALLOCATION_INFO(&(const D3D12_RESOURCE_ALLOCATION_INFO){0}, 1);
/* skip visibleMask (input only) */
/* skip numResourceDescs (input only) */
/* skip pResourceDescs (input only) */
/* skip pNumCastableFormats (input only) */
/* skip ppCastableFormats (input only) */
    cmd_size += npt_sizeof_array_count(pResourceAllocationInfo1 ? numResourceDescs : 0);
    if (pResourceAllocationInfo1)
        cmd_size += (size_t)(numResourceDescs) * npt_sizeof_D3D12_RESOURCE_ALLOCATION_INFO1(&(const D3D12_RESOURCE_ALLOCATION_INFO1){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device12_GetResourceAllocationInfo3_reply(struct npt_cs_decoder *dec,
                                                           D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1,
                                                           D3D12_RESOURCE_ALLOCATION_INFO *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device12,
                                                  NPT_METHOD_ID3D12Device12_GetResourceAllocationInfo3))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device12_GetResourceAllocationInfo3 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device12,
                                       NPT_METHOD_ID3D12Device12_GetResourceAllocationInfo3));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_RESOURCE_ALLOCATION_INFO(dec, ret);
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pResourceAllocationInfo1) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D12_RESOURCE_ALLOCATION_INFO1(dec, (D3D12_RESOURCE_ALLOCATION_INFO1 *)&pResourceAllocationInfo1[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D12Device12_GetResourceAllocationInfo3(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     UINT visibleMask,
                                                     UINT numResourceDescs,
                                                     const D3D12_RESOURCE_DESC1 * pResourceDescs,
                                                     const UINT32 * pNumCastableFormats,
                                                     const DXGI_FORMAT ** ppCastableFormats,
                                                     D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device12_GetResourceAllocationInfo3(visibleMask,numResourceDescs,pResourceDescs,pNumCastableFormats,ppCastableFormats,pResourceAllocationInfo1);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device12_GetResourceAllocationInfo3_reply(visibleMask,numResourceDescs,pResourceDescs,pNumCastableFormats,ppCastableFormats,pResourceAllocationInfo1)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device12_GetResourceAllocationInfo3(enc, cmd_flags, object_id, visibleMask, numResourceDescs, pResourceDescs, pNumCastableFormats, ppCastableFormats, pResourceAllocationInfo1);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device12_GetResourceAllocationInfo3(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT visibleMask,
                                                    UINT numResourceDescs,
                                                    const D3D12_RESOURCE_DESC1 * pResourceDescs,
                                                    const UINT32 * pNumCastableFormats,
                                                    const DXGI_FORMAT ** ppCastableFormats,
                                                    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device12_GetResourceAllocationInfo3(ring, 0, object_id, visibleMask, numResourceDescs, pResourceDescs, pNumCastableFormats, ppCastableFormats, pResourceAllocationInfo1, &submit);
}

static inline D3D12_RESOURCE_ALLOCATION_INFO
npt_call_ID3D12Device12_GetResourceAllocationInfo3(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT visibleMask,
                                                   UINT numResourceDescs,
                                                   const D3D12_RESOURCE_DESC1 * pResourceDescs,
                                                   const UINT32 * pNumCastableFormats,
                                                   const DXGI_FORMAT ** ppCastableFormats,
                                                   D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device12_GetResourceAllocationInfo3(ring, NPT_CMD_FLAG_REPLY, object_id, visibleMask, numResourceDescs, pResourceDescs, pNumCastableFormats, ppCastableFormats, pResourceAllocationInfo1, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_RESOURCE_ALLOCATION_INFO _ret;
        npt_decode_ID3D12Device12_GetResourceAllocationInfo3_reply(dec, pResourceAllocationInfo1, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_RESOURCE_ALLOCATION_INFO){0};
    }
}

/*
 * ID3D12Device13::OpenExistingHeapFromAddress1
 */

struct npt_command_ID3D12Device13_OpenExistingHeapFromAddress1 {
    void *_self;
    const void * pAddress;
    SIZE_T size;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device13_OpenExistingHeapFromAddress1(const void * pAddress,const SIZE_T size,const IID * riid,void ** ppvHeap)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    /* ERROR: pAddress (void) is not serializable */
    return 0;
    cmd_size += npt_sizeof_SIZE_T(&size, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvHeap: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device13_OpenExistingHeapFromAddress1(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       const void * pAddress,
                                                       SIZE_T size,
                                                       const IID * riid,
                                                       void ** ppvHeap)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device13_OpenExistingHeapFromAddress1(pAddress,size,riid,ppvHeap);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device13,
                                 NPT_METHOD_ID3D12Device13_OpenExistingHeapFromAddress1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    /* ERROR: pAddress (void) is not serializable */
    return;
    npt_encode_SIZE_T(enc, &size);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvHeap = 0;
    if (ppvHeap) {
        _gid_ppvHeap = npt_com_allocate_next_id();
        *ppvHeap = (void *)(uintptr_t)_gid_ppvHeap;
    }
    npt_encode_uint64_t(enc, &_gid_ppvHeap);
}
}

static inline size_t
npt_sizeof_ID3D12Device13_OpenExistingHeapFromAddress1_reply(const void * pAddress,const SIZE_T size,const IID * riid,void ** ppvHeap)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAddress (input only) */
/* skip size (input only) */
/* skip riid (input only) */
/* skip ppvHeap (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device13_OpenExistingHeapFromAddress1_reply(struct npt_cs_decoder *dec,
                                                             void ** ppvHeap,
                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device13,
                                                  NPT_METHOD_ID3D12Device13_OpenExistingHeapFromAddress1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device13_OpenExistingHeapFromAddress1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device13,
                                       NPT_METHOD_ID3D12Device13_OpenExistingHeapFromAddress1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvHeap (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device13_OpenExistingHeapFromAddress1(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       const void * pAddress,
                                                       SIZE_T size,
                                                       const IID * riid,
                                                       void ** ppvHeap,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device13_OpenExistingHeapFromAddress1(pAddress,size,riid,ppvHeap);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device13_OpenExistingHeapFromAddress1_reply(pAddress,size,riid,ppvHeap)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device13_OpenExistingHeapFromAddress1(enc, cmd_flags, object_id, pAddress, size, riid, ppvHeap);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device13_OpenExistingHeapFromAddress1(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      const void * pAddress,
                                                      SIZE_T size,
                                                      const IID * riid,
                                                      void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device13_OpenExistingHeapFromAddress1(ring, 0, object_id, pAddress, size, riid, ppvHeap, &submit);
}

static inline HRESULT
npt_call_ID3D12Device13_OpenExistingHeapFromAddress1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     const void * pAddress,
                                                     SIZE_T size,
                                                     const IID * riid,
                                                     void ** ppvHeap)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device13_OpenExistingHeapFromAddress1(ring, NPT_CMD_FLAG_REPLY, object_id, pAddress, size, riid, ppvHeap, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device13_OpenExistingHeapFromAddress1_reply(dec, ppvHeap, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Device14::CreateRootSignatureFromSubobjectInLibrary
 */

struct npt_command_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary {
    void *_self;
    UINT nodeMask;
    const void * pLibraryBlob;
    SIZE_T blobLengthInBytes;
    const WCHAR * subobjectName;
    const IID * riid;
    void ** ppvRootSignature;
    /* Shadow: the guest-allocated id for ppvRootSignature arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvRootSignature -> *ppvRootSignature} in the
     * context object table. */
    uint64_t _guest_id_ppvRootSignature;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(const UINT nodeMask,const void * pLibraryBlob,const SIZE_T blobLengthInBytes,const WCHAR * subobjectName,const IID * riid,void ** ppvRootSignature)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&nodeMask, max_mode);
    if (pLibraryBlob) {
        cmd_size += npt_sizeof_array_count(blobLengthInBytes);
        cmd_size += npt_sizeof_blob_array(pLibraryBlob, blobLengthInBytes);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&blobLengthInBytes, max_mode);
    if (subobjectName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)subobjectName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(subobjectName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvRootSignature: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(struct npt_cs_encoder *enc,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    UINT nodeMask,
                                                                    const void * pLibraryBlob,
                                                                    SIZE_T blobLengthInBytes,
                                                                    const WCHAR * subobjectName,
                                                                    const IID * riid,
                                                                    void ** ppvRootSignature)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(nodeMask,pLibraryBlob,blobLengthInBytes,subobjectName,riid,ppvRootSignature);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device14,
                                 NPT_METHOD_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &nodeMask);
    if (pLibraryBlob) {
        npt_encode_array_count(enc, blobLengthInBytes);
        npt_encode_blob_array(enc, pLibraryBlob, blobLengthInBytes);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &blobLengthInBytes);
    if (subobjectName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)subobjectName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, subobjectName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvRootSignature = 0;
    if (ppvRootSignature) {
        _gid_ppvRootSignature = npt_com_allocate_next_id();
        *ppvRootSignature = (void *)(uintptr_t)_gid_ppvRootSignature;
    }
    npt_encode_uint64_t(enc, &_gid_ppvRootSignature);
}
}

static inline size_t
npt_sizeof_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_reply(const UINT nodeMask,const void * pLibraryBlob,const SIZE_T blobLengthInBytes,const WCHAR * subobjectName,const IID * riid,void ** ppvRootSignature)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip nodeMask (input only) */
/* skip pLibraryBlob (input only) */
/* skip blobLengthInBytes (input only) */
/* skip subobjectName (input only) */
/* skip riid (input only) */
/* skip ppvRootSignature (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_reply(struct npt_cs_decoder *dec,
                                                                          void ** ppvRootSignature,
                                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device14,
                                                  NPT_METHOD_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device14,
                                       NPT_METHOD_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvRootSignature (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(struct npt_ring *ring,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    UINT nodeMask,
                                                                    const void * pLibraryBlob,
                                                                    SIZE_T blobLengthInBytes,
                                                                    const WCHAR * subobjectName,
                                                                    const IID * riid,
                                                                    void ** ppvRootSignature,
                                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(nodeMask,pLibraryBlob,blobLengthInBytes,subobjectName,riid,ppvRootSignature);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_reply(nodeMask,pLibraryBlob,blobLengthInBytes,subobjectName,riid,ppvRootSignature)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(enc, cmd_flags, object_id, nodeMask, pLibraryBlob, blobLengthInBytes, subobjectName, riid, ppvRootSignature);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(struct npt_ring *ring,
                                                                   npt_object_id object_id,
                                                                   UINT nodeMask,
                                                                   const void * pLibraryBlob,
                                                                   SIZE_T blobLengthInBytes,
                                                                   const WCHAR * subobjectName,
                                                                   const IID * riid,
                                                                   void ** ppvRootSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(ring, 0, object_id, nodeMask, pLibraryBlob, blobLengthInBytes, subobjectName, riid, ppvRootSignature, &submit);
}

static inline HRESULT
npt_call_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  UINT nodeMask,
                                                                  const void * pLibraryBlob,
                                                                  SIZE_T blobLengthInBytes,
                                                                  const WCHAR * subobjectName,
                                                                  const IID * riid,
                                                                  void ** ppvRootSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(ring, NPT_CMD_FLAG_REPLY, object_id, nodeMask, pLibraryBlob, blobLengthInBytes, subobjectName, riid, ppvRootSignature, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_reply(dec, ppvRootSignature, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12DEVICE_H */
