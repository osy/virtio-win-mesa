/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgioutput.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIOUTPUT_H
#define NPT_PROTOCOL_GUEST_IDXGIOUTPUT_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIOutput::GetDesc
 */

struct npt_command_IDXGIOutput_GetDesc {
    void *_self;
    DXGI_OUTPUT_DESC * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_GetDesc(const DXGI_OUTPUT_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_GetDesc(struct npt_cs_encoder *enc,
                               uint32_t cmd_flags,
                               npt_object_id object_id,
                               DXGI_OUTPUT_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_GetDesc(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput_GetDesc_reply(const DXGI_OUTPUT_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_OUTPUT_DESC(&(const DXGI_OUTPUT_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_GetDesc_reply(struct npt_cs_decoder *dec,
                                     DXGI_OUTPUT_DESC * pDesc,
                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_GetDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_GetDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_GetDesc));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_OUTPUT_DESC(dec, (DXGI_OUTPUT_DESC *)pDesc);
    }
}

static inline void
npt_submit_IDXGIOutput_GetDesc(struct npt_ring *ring,
                               uint32_t cmd_flags,
                               npt_object_id object_id,
                               DXGI_OUTPUT_DESC * pDesc,
                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_GetDesc(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_GetDesc_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_GetDesc(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_GetDesc(struct npt_ring *ring,
                              npt_object_id object_id,
                              DXGI_OUTPUT_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetDesc(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_GetDesc(struct npt_ring *ring,
                             npt_object_id object_id,
                             DXGI_OUTPUT_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetDesc(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_GetDesc_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::GetDisplayModeList
 */

struct npt_command_IDXGIOutput_GetDisplayModeList {
    void *_self;
    DXGI_FORMAT EnumFormat;
    UINT Flags;
    UINT * pNumModes;
    DXGI_MODE_DESC * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_GetDisplayModeList(const DXGI_FORMAT EnumFormat,const UINT Flags,const UINT * pNumModes,const DXGI_MODE_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FORMAT(&EnumFormat, max_mode);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pNumModes);
    if (pNumModes)
        cmd_size += npt_sizeof_UINT(pNumModes, max_mode);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_GetDisplayModeList(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_FORMAT EnumFormat,
                                          UINT Flags,
                                          UINT * pNumModes,
                                          DXGI_MODE_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_GetDisplayModeList(EnumFormat,Flags,pNumModes,pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetDisplayModeList),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FORMAT(enc, &EnumFormat);
    npt_encode_UINT(enc, &Flags);
    if (npt_encode_simple_pointer(enc, pNumModes))
        npt_encode_UINT(enc, pNumModes);
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput_GetDisplayModeList_reply(const DXGI_FORMAT EnumFormat,const UINT Flags,const UINT * pNumModes,const DXGI_MODE_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip EnumFormat (input only) */
/* skip Flags (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_array_count(pDesc ? *pNumModes : 0);
    if (pDesc)
        cmd_size += (size_t)(*pNumModes) * npt_sizeof_DXGI_MODE_DESC(&(const DXGI_MODE_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_GetDisplayModeList_reply(struct npt_cs_decoder *dec,
                                                UINT * pNumModes,
                                                DXGI_MODE_DESC * pDesc,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_GetDisplayModeList))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_GetDisplayModeList reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_GetDisplayModeList));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pNumModes)
            npt_decode_UINT(dec, (UINT *)pNumModes);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pDesc) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_DXGI_MODE_DESC(dec, (DXGI_MODE_DESC *)&pDesc[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_IDXGIOutput_GetDisplayModeList(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_FORMAT EnumFormat,
                                          UINT Flags,
                                          UINT * pNumModes,
                                          DXGI_MODE_DESC * pDesc,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_GetDisplayModeList(EnumFormat,Flags,pNumModes,pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_GetDisplayModeList_reply(EnumFormat,Flags,pNumModes,pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_GetDisplayModeList(enc, cmd_flags, object_id, EnumFormat, Flags, pNumModes, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_GetDisplayModeList(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         DXGI_FORMAT EnumFormat,
                                         UINT Flags,
                                         UINT * pNumModes,
                                         DXGI_MODE_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetDisplayModeList(ring, 0, object_id, EnumFormat, Flags, pNumModes, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_GetDisplayModeList(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        DXGI_FORMAT EnumFormat,
                                        UINT Flags,
                                        UINT * pNumModes,
                                        DXGI_MODE_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetDisplayModeList(ring, NPT_CMD_FLAG_REPLY, object_id, EnumFormat, Flags, pNumModes, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_GetDisplayModeList_reply(dec, pNumModes, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::FindClosestMatchingMode
 */

struct npt_command_IDXGIOutput_FindClosestMatchingMode {
    void *_self;
    const DXGI_MODE_DESC * pModeToMatch;
    DXGI_MODE_DESC * pClosestMatch;
    IUnknown * pConcernedDevice;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_FindClosestMatchingMode(const DXGI_MODE_DESC * pModeToMatch,const DXGI_MODE_DESC * pClosestMatch,const IUnknown * pConcernedDevice)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pModeToMatch);
    if (pModeToMatch)
        cmd_size += npt_sizeof_DXGI_MODE_DESC(pModeToMatch, max_mode);
/* skip pClosestMatch (output only) */
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_FindClosestMatchingMode(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const DXGI_MODE_DESC * pModeToMatch,
                                               DXGI_MODE_DESC * pClosestMatch,
                                               IUnknown * pConcernedDevice)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_FindClosestMatchingMode(pModeToMatch,pClosestMatch,pConcernedDevice);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_FindClosestMatchingMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pModeToMatch))
        npt_encode_DXGI_MODE_DESC(enc, pModeToMatch);
/* skip pClosestMatch (output only) */
    npt_encode_com_handle(enc, npt_object_get_id(pConcernedDevice));
}

static inline size_t
npt_sizeof_IDXGIOutput_FindClosestMatchingMode_reply(const DXGI_MODE_DESC * pModeToMatch,const DXGI_MODE_DESC * pClosestMatch,const IUnknown * pConcernedDevice)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pModeToMatch (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_MODE_DESC(&(const DXGI_MODE_DESC){0}, 1);
/* skip pConcernedDevice (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_FindClosestMatchingMode_reply(struct npt_cs_decoder *dec,
                                                     DXGI_MODE_DESC * pClosestMatch,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_FindClosestMatchingMode))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_FindClosestMatchingMode reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_FindClosestMatchingMode));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pClosestMatch)
            npt_decode_DXGI_MODE_DESC(dec, (DXGI_MODE_DESC *)pClosestMatch);
    }
}

static inline void
npt_submit_IDXGIOutput_FindClosestMatchingMode(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const DXGI_MODE_DESC * pModeToMatch,
                                               DXGI_MODE_DESC * pClosestMatch,
                                               IUnknown * pConcernedDevice,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_FindClosestMatchingMode(pModeToMatch,pClosestMatch,pConcernedDevice);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_FindClosestMatchingMode_reply(pModeToMatch,pClosestMatch,pConcernedDevice)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_FindClosestMatchingMode(enc, cmd_flags, object_id, pModeToMatch, pClosestMatch, pConcernedDevice);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_FindClosestMatchingMode(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const DXGI_MODE_DESC * pModeToMatch,
                                              DXGI_MODE_DESC * pClosestMatch,
                                              IUnknown * pConcernedDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_FindClosestMatchingMode(ring, 0, object_id, pModeToMatch, pClosestMatch, pConcernedDevice, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_FindClosestMatchingMode(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const DXGI_MODE_DESC * pModeToMatch,
                                             DXGI_MODE_DESC * pClosestMatch,
                                             IUnknown * pConcernedDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_FindClosestMatchingMode(ring, NPT_CMD_FLAG_REPLY, object_id, pModeToMatch, pClosestMatch, pConcernedDevice, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_FindClosestMatchingMode_reply(dec, pClosestMatch, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::WaitForVBlank
 */

struct npt_command_IDXGIOutput_WaitForVBlank {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_WaitForVBlank(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_WaitForVBlank(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_WaitForVBlank();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_WaitForVBlank),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIOutput_WaitForVBlank_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_WaitForVBlank_reply(struct npt_cs_decoder *dec,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_WaitForVBlank))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_WaitForVBlank reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_WaitForVBlank));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIOutput_WaitForVBlank(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_WaitForVBlank();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_WaitForVBlank_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_WaitForVBlank(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_WaitForVBlank(struct npt_ring *ring,
                                    npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_WaitForVBlank(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_WaitForVBlank(struct npt_ring *ring,
                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_WaitForVBlank(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_WaitForVBlank_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::TakeOwnership
 */

struct npt_command_IDXGIOutput_TakeOwnership {
    void *_self;
    IUnknown * pDevice;
    BOOL Exclusive;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_TakeOwnership(const IUnknown * pDevice,const BOOL Exclusive)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_BOOL(&Exclusive, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_TakeOwnership(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     IUnknown * pDevice,
                                     BOOL Exclusive)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_TakeOwnership(pDevice,Exclusive);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_TakeOwnership),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
    npt_encode_BOOL(enc, &Exclusive);
}

static inline size_t
npt_sizeof_IDXGIOutput_TakeOwnership_reply(const IUnknown * pDevice,const BOOL Exclusive)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip Exclusive (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_TakeOwnership_reply(struct npt_cs_decoder *dec,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_TakeOwnership))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_TakeOwnership reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_TakeOwnership));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIOutput_TakeOwnership(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     IUnknown * pDevice,
                                     BOOL Exclusive,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_TakeOwnership(pDevice,Exclusive);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_TakeOwnership_reply(pDevice,Exclusive)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_TakeOwnership(enc, cmd_flags, object_id, pDevice, Exclusive);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_TakeOwnership(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    IUnknown * pDevice,
                                    BOOL Exclusive)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_TakeOwnership(ring, 0, object_id, pDevice, Exclusive, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_TakeOwnership(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   IUnknown * pDevice,
                                   BOOL Exclusive)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_TakeOwnership(ring, NPT_CMD_FLAG_REPLY, object_id, pDevice, Exclusive, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_TakeOwnership_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::ReleaseOwnership
 */

struct npt_command_IDXGIOutput_ReleaseOwnership {
    void *_self;
};

static inline size_t
npt_sizeof_IDXGIOutput_ReleaseOwnership(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_ReleaseOwnership(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_ReleaseOwnership();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_ReleaseOwnership),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIOutput_ReleaseOwnership_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_IDXGIOutput_ReleaseOwnership(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_ReleaseOwnership();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_ReleaseOwnership_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_ReleaseOwnership(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_ReleaseOwnership(struct npt_ring *ring,
                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_ReleaseOwnership(ring, 0, object_id, &submit);
}

static inline void
npt_call_IDXGIOutput_ReleaseOwnership(struct npt_ring *ring,
                                      npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_ReleaseOwnership(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * IDXGIOutput::GetGammaControlCapabilities
 */

struct npt_command_IDXGIOutput_GetGammaControlCapabilities {
    void *_self;
    DXGI_GAMMA_CONTROL_CAPABILITIES * pGammaCaps;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_GetGammaControlCapabilities(const DXGI_GAMMA_CONTROL_CAPABILITIES * pGammaCaps)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pGammaCaps (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_GetGammaControlCapabilities(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   DXGI_GAMMA_CONTROL_CAPABILITIES * pGammaCaps)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_GetGammaControlCapabilities(pGammaCaps);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetGammaControlCapabilities),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pGammaCaps (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput_GetGammaControlCapabilities_reply(const DXGI_GAMMA_CONTROL_CAPABILITIES * pGammaCaps)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_GAMMA_CONTROL_CAPABILITIES(&(const DXGI_GAMMA_CONTROL_CAPABILITIES){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_GetGammaControlCapabilities_reply(struct npt_cs_decoder *dec,
                                                         DXGI_GAMMA_CONTROL_CAPABILITIES * pGammaCaps,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_GetGammaControlCapabilities))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_GetGammaControlCapabilities reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_GetGammaControlCapabilities));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pGammaCaps)
            npt_decode_DXGI_GAMMA_CONTROL_CAPABILITIES(dec, (DXGI_GAMMA_CONTROL_CAPABILITIES *)pGammaCaps);
    }
}

static inline void
npt_submit_IDXGIOutput_GetGammaControlCapabilities(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   DXGI_GAMMA_CONTROL_CAPABILITIES * pGammaCaps,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_GetGammaControlCapabilities(pGammaCaps);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_GetGammaControlCapabilities_reply(pGammaCaps)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_GetGammaControlCapabilities(enc, cmd_flags, object_id, pGammaCaps);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_GetGammaControlCapabilities(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  DXGI_GAMMA_CONTROL_CAPABILITIES * pGammaCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetGammaControlCapabilities(ring, 0, object_id, pGammaCaps, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_GetGammaControlCapabilities(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 DXGI_GAMMA_CONTROL_CAPABILITIES * pGammaCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetGammaControlCapabilities(ring, NPT_CMD_FLAG_REPLY, object_id, pGammaCaps, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_GetGammaControlCapabilities_reply(dec, pGammaCaps, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::SetGammaControl
 */

struct npt_command_IDXGIOutput_SetGammaControl {
    void *_self;
    const DXGI_GAMMA_CONTROL * pArray;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_SetGammaControl(const DXGI_GAMMA_CONTROL * pArray)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pArray);
    if (pArray)
        cmd_size += npt_sizeof_DXGI_GAMMA_CONTROL(pArray, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_SetGammaControl(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       const DXGI_GAMMA_CONTROL * pArray)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_SetGammaControl(pArray);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_SetGammaControl),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pArray))
        npt_encode_DXGI_GAMMA_CONTROL(enc, pArray);
}

static inline size_t
npt_sizeof_IDXGIOutput_SetGammaControl_reply(const DXGI_GAMMA_CONTROL * pArray)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pArray (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_SetGammaControl_reply(struct npt_cs_decoder *dec,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_SetGammaControl))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_SetGammaControl reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_SetGammaControl));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIOutput_SetGammaControl(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       const DXGI_GAMMA_CONTROL * pArray,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_SetGammaControl(pArray);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_SetGammaControl_reply(pArray)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_SetGammaControl(enc, cmd_flags, object_id, pArray);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_SetGammaControl(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      const DXGI_GAMMA_CONTROL * pArray)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_SetGammaControl(ring, 0, object_id, pArray, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_SetGammaControl(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     const DXGI_GAMMA_CONTROL * pArray)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_SetGammaControl(ring, NPT_CMD_FLAG_REPLY, object_id, pArray, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_SetGammaControl_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::GetGammaControl
 */

struct npt_command_IDXGIOutput_GetGammaControl {
    void *_self;
    DXGI_GAMMA_CONTROL * pArray;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_GetGammaControl(const DXGI_GAMMA_CONTROL * pArray)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pArray (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_GetGammaControl(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       DXGI_GAMMA_CONTROL * pArray)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_GetGammaControl(pArray);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetGammaControl),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pArray (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput_GetGammaControl_reply(const DXGI_GAMMA_CONTROL * pArray)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_GAMMA_CONTROL(&(const DXGI_GAMMA_CONTROL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_GetGammaControl_reply(struct npt_cs_decoder *dec,
                                             DXGI_GAMMA_CONTROL * pArray,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_GetGammaControl))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_GetGammaControl reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_GetGammaControl));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pArray)
            npt_decode_DXGI_GAMMA_CONTROL(dec, (DXGI_GAMMA_CONTROL *)pArray);
    }
}

static inline void
npt_submit_IDXGIOutput_GetGammaControl(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       DXGI_GAMMA_CONTROL * pArray,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_GetGammaControl(pArray);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_GetGammaControl_reply(pArray)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_GetGammaControl(enc, cmd_flags, object_id, pArray);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_GetGammaControl(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      DXGI_GAMMA_CONTROL * pArray)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetGammaControl(ring, 0, object_id, pArray, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_GetGammaControl(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     DXGI_GAMMA_CONTROL * pArray)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetGammaControl(ring, NPT_CMD_FLAG_REPLY, object_id, pArray, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_GetGammaControl_reply(dec, pArray, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::SetDisplaySurface
 */

struct npt_command_IDXGIOutput_SetDisplaySurface {
    void *_self;
    IDXGISurface * pScanoutSurface;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_SetDisplaySurface(const IDXGISurface * pScanoutSurface)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_SetDisplaySurface(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         IDXGISurface * pScanoutSurface)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_SetDisplaySurface(pScanoutSurface);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_SetDisplaySurface),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pScanoutSurface));
}

static inline size_t
npt_sizeof_IDXGIOutput_SetDisplaySurface_reply(const IDXGISurface * pScanoutSurface)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pScanoutSurface (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_SetDisplaySurface_reply(struct npt_cs_decoder *dec,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_SetDisplaySurface))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_SetDisplaySurface reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_SetDisplaySurface));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIOutput_SetDisplaySurface(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         IDXGISurface * pScanoutSurface,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_SetDisplaySurface(pScanoutSurface);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_SetDisplaySurface_reply(pScanoutSurface)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_SetDisplaySurface(enc, cmd_flags, object_id, pScanoutSurface);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_SetDisplaySurface(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        IDXGISurface * pScanoutSurface)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_SetDisplaySurface(ring, 0, object_id, pScanoutSurface, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_SetDisplaySurface(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       IDXGISurface * pScanoutSurface)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_SetDisplaySurface(ring, NPT_CMD_FLAG_REPLY, object_id, pScanoutSurface, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_SetDisplaySurface_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::GetDisplaySurfaceData
 */

struct npt_command_IDXGIOutput_GetDisplaySurfaceData {
    void *_self;
    IDXGISurface * pDestination;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_GetDisplaySurfaceData(const IDXGISurface * pDestination)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_GetDisplaySurfaceData(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             IDXGISurface * pDestination)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_GetDisplaySurfaceData(pDestination);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetDisplaySurfaceData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDestination));
}

static inline size_t
npt_sizeof_IDXGIOutput_GetDisplaySurfaceData_reply(const IDXGISurface * pDestination)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDestination (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_GetDisplaySurfaceData_reply(struct npt_cs_decoder *dec,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_GetDisplaySurfaceData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_GetDisplaySurfaceData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_GetDisplaySurfaceData));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIOutput_GetDisplaySurfaceData(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             IDXGISurface * pDestination,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_GetDisplaySurfaceData(pDestination);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_GetDisplaySurfaceData_reply(pDestination)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_GetDisplaySurfaceData(enc, cmd_flags, object_id, pDestination);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_GetDisplaySurfaceData(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            IDXGISurface * pDestination)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetDisplaySurfaceData(ring, 0, object_id, pDestination, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_GetDisplaySurfaceData(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           IDXGISurface * pDestination)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetDisplaySurfaceData(ring, NPT_CMD_FLAG_REPLY, object_id, pDestination, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_GetDisplaySurfaceData_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput::GetFrameStatistics
 */

struct npt_command_IDXGIOutput_GetFrameStatistics {
    void *_self;
    DXGI_FRAME_STATISTICS * pStats;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput_GetFrameStatistics(const DXGI_FRAME_STATISTICS * pStats)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pStats (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput_GetFrameStatistics(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_FRAME_STATISTICS * pStats)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput_GetFrameStatistics(pStats);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetFrameStatistics),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pStats (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput_GetFrameStatistics_reply(const DXGI_FRAME_STATISTICS * pStats)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_FRAME_STATISTICS(&(const DXGI_FRAME_STATISTICS){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput_GetFrameStatistics_reply(struct npt_cs_decoder *dec,
                                                DXGI_FRAME_STATISTICS * pStats,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                                  NPT_METHOD_IDXGIOutput_GetFrameStatistics))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput_GetFrameStatistics reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                       NPT_METHOD_IDXGIOutput_GetFrameStatistics));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pStats)
            npt_decode_DXGI_FRAME_STATISTICS(dec, (DXGI_FRAME_STATISTICS *)pStats);
    }
}

static inline void
npt_submit_IDXGIOutput_GetFrameStatistics(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          DXGI_FRAME_STATISTICS * pStats,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput_GetFrameStatistics(pStats);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput_GetFrameStatistics_reply(pStats)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput_GetFrameStatistics(enc, cmd_flags, object_id, pStats);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput_GetFrameStatistics(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         DXGI_FRAME_STATISTICS * pStats)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetFrameStatistics(ring, 0, object_id, pStats, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput_GetFrameStatistics(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        DXGI_FRAME_STATISTICS * pStats)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput_GetFrameStatistics(ring, NPT_CMD_FLAG_REPLY, object_id, pStats, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput_GetFrameStatistics_reply(dec, pStats, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput1::GetDisplayModeList1
 */

struct npt_command_IDXGIOutput1_GetDisplayModeList1 {
    void *_self;
    DXGI_FORMAT EnumFormat;
    UINT Flags;
    UINT * pNumModes;
    DXGI_MODE_DESC1 * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput1_GetDisplayModeList1(const DXGI_FORMAT EnumFormat,const UINT Flags,const UINT * pNumModes,const DXGI_MODE_DESC1 * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FORMAT(&EnumFormat, max_mode);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pNumModes);
    if (pNumModes)
        cmd_size += npt_sizeof_UINT(pNumModes, max_mode);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput1_GetDisplayModeList1(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            DXGI_FORMAT EnumFormat,
                                            UINT Flags,
                                            UINT * pNumModes,
                                            DXGI_MODE_DESC1 * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput1_GetDisplayModeList1(EnumFormat,Flags,pNumModes,pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                 NPT_METHOD_IDXGIOutput1_GetDisplayModeList1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FORMAT(enc, &EnumFormat);
    npt_encode_UINT(enc, &Flags);
    if (npt_encode_simple_pointer(enc, pNumModes))
        npt_encode_UINT(enc, pNumModes);
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput1_GetDisplayModeList1_reply(const DXGI_FORMAT EnumFormat,const UINT Flags,const UINT * pNumModes,const DXGI_MODE_DESC1 * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip EnumFormat (input only) */
/* skip Flags (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_array_count(pDesc ? *pNumModes : 0);
    if (pDesc)
        cmd_size += (size_t)(*pNumModes) * npt_sizeof_DXGI_MODE_DESC1(&(const DXGI_MODE_DESC1){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput1_GetDisplayModeList1_reply(struct npt_cs_decoder *dec,
                                                  UINT * pNumModes,
                                                  DXGI_MODE_DESC1 * pDesc,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                                  NPT_METHOD_IDXGIOutput1_GetDisplayModeList1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput1_GetDisplayModeList1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                       NPT_METHOD_IDXGIOutput1_GetDisplayModeList1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pNumModes)
            npt_decode_UINT(dec, (UINT *)pNumModes);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pDesc) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_DXGI_MODE_DESC1(dec, (DXGI_MODE_DESC1 *)&pDesc[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_IDXGIOutput1_GetDisplayModeList1(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            DXGI_FORMAT EnumFormat,
                                            UINT Flags,
                                            UINT * pNumModes,
                                            DXGI_MODE_DESC1 * pDesc,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput1_GetDisplayModeList1(EnumFormat,Flags,pNumModes,pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput1_GetDisplayModeList1_reply(EnumFormat,Flags,pNumModes,pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput1_GetDisplayModeList1(enc, cmd_flags, object_id, EnumFormat, Flags, pNumModes, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput1_GetDisplayModeList1(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           DXGI_FORMAT EnumFormat,
                                           UINT Flags,
                                           UINT * pNumModes,
                                           DXGI_MODE_DESC1 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput1_GetDisplayModeList1(ring, 0, object_id, EnumFormat, Flags, pNumModes, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput1_GetDisplayModeList1(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          DXGI_FORMAT EnumFormat,
                                          UINT Flags,
                                          UINT * pNumModes,
                                          DXGI_MODE_DESC1 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput1_GetDisplayModeList1(ring, NPT_CMD_FLAG_REPLY, object_id, EnumFormat, Flags, pNumModes, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput1_GetDisplayModeList1_reply(dec, pNumModes, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput1::FindClosestMatchingMode1
 */

struct npt_command_IDXGIOutput1_FindClosestMatchingMode1 {
    void *_self;
    const DXGI_MODE_DESC1 * pModeToMatch;
    DXGI_MODE_DESC1 * pClosestMatch;
    IUnknown * pConcernedDevice;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput1_FindClosestMatchingMode1(const DXGI_MODE_DESC1 * pModeToMatch,const DXGI_MODE_DESC1 * pClosestMatch,const IUnknown * pConcernedDevice)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pModeToMatch);
    if (pModeToMatch)
        cmd_size += npt_sizeof_DXGI_MODE_DESC1(pModeToMatch, max_mode);
/* skip pClosestMatch (output only) */
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput1_FindClosestMatchingMode1(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 const DXGI_MODE_DESC1 * pModeToMatch,
                                                 DXGI_MODE_DESC1 * pClosestMatch,
                                                 IUnknown * pConcernedDevice)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput1_FindClosestMatchingMode1(pModeToMatch,pClosestMatch,pConcernedDevice);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                 NPT_METHOD_IDXGIOutput1_FindClosestMatchingMode1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pModeToMatch))
        npt_encode_DXGI_MODE_DESC1(enc, pModeToMatch);
/* skip pClosestMatch (output only) */
    npt_encode_com_handle(enc, npt_object_get_id(pConcernedDevice));
}

static inline size_t
npt_sizeof_IDXGIOutput1_FindClosestMatchingMode1_reply(const DXGI_MODE_DESC1 * pModeToMatch,const DXGI_MODE_DESC1 * pClosestMatch,const IUnknown * pConcernedDevice)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pModeToMatch (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_MODE_DESC1(&(const DXGI_MODE_DESC1){0}, 1);
/* skip pConcernedDevice (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput1_FindClosestMatchingMode1_reply(struct npt_cs_decoder *dec,
                                                       DXGI_MODE_DESC1 * pClosestMatch,
                                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                                  NPT_METHOD_IDXGIOutput1_FindClosestMatchingMode1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput1_FindClosestMatchingMode1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                       NPT_METHOD_IDXGIOutput1_FindClosestMatchingMode1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pClosestMatch)
            npt_decode_DXGI_MODE_DESC1(dec, (DXGI_MODE_DESC1 *)pClosestMatch);
    }
}

static inline void
npt_submit_IDXGIOutput1_FindClosestMatchingMode1(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 const DXGI_MODE_DESC1 * pModeToMatch,
                                                 DXGI_MODE_DESC1 * pClosestMatch,
                                                 IUnknown * pConcernedDevice,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput1_FindClosestMatchingMode1(pModeToMatch,pClosestMatch,pConcernedDevice);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput1_FindClosestMatchingMode1_reply(pModeToMatch,pClosestMatch,pConcernedDevice)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput1_FindClosestMatchingMode1(enc, cmd_flags, object_id, pModeToMatch, pClosestMatch, pConcernedDevice);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput1_FindClosestMatchingMode1(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const DXGI_MODE_DESC1 * pModeToMatch,
                                                DXGI_MODE_DESC1 * pClosestMatch,
                                                IUnknown * pConcernedDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput1_FindClosestMatchingMode1(ring, 0, object_id, pModeToMatch, pClosestMatch, pConcernedDevice, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput1_FindClosestMatchingMode1(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const DXGI_MODE_DESC1 * pModeToMatch,
                                               DXGI_MODE_DESC1 * pClosestMatch,
                                               IUnknown * pConcernedDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput1_FindClosestMatchingMode1(ring, NPT_CMD_FLAG_REPLY, object_id, pModeToMatch, pClosestMatch, pConcernedDevice, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput1_FindClosestMatchingMode1_reply(dec, pClosestMatch, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput1::GetDisplaySurfaceData1
 */

struct npt_command_IDXGIOutput1_GetDisplaySurfaceData1 {
    void *_self;
    IDXGIResource * pDestination;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput1_GetDisplaySurfaceData1(const IDXGIResource * pDestination)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput1_GetDisplaySurfaceData1(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               IDXGIResource * pDestination)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput1_GetDisplaySurfaceData1(pDestination);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                 NPT_METHOD_IDXGIOutput1_GetDisplaySurfaceData1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDestination));
}

static inline size_t
npt_sizeof_IDXGIOutput1_GetDisplaySurfaceData1_reply(const IDXGIResource * pDestination)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDestination (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput1_GetDisplaySurfaceData1_reply(struct npt_cs_decoder *dec,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                                  NPT_METHOD_IDXGIOutput1_GetDisplaySurfaceData1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput1_GetDisplaySurfaceData1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                       NPT_METHOD_IDXGIOutput1_GetDisplaySurfaceData1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIOutput1_GetDisplaySurfaceData1(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               IDXGIResource * pDestination,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput1_GetDisplaySurfaceData1(pDestination);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput1_GetDisplaySurfaceData1_reply(pDestination)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput1_GetDisplaySurfaceData1(enc, cmd_flags, object_id, pDestination);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput1_GetDisplaySurfaceData1(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              IDXGIResource * pDestination)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput1_GetDisplaySurfaceData1(ring, 0, object_id, pDestination, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput1_GetDisplaySurfaceData1(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             IDXGIResource * pDestination)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput1_GetDisplaySurfaceData1(ring, NPT_CMD_FLAG_REPLY, object_id, pDestination, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput1_GetDisplaySurfaceData1_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput1::DuplicateOutput
 */

struct npt_command_IDXGIOutput1_DuplicateOutput {
    void *_self;
    IUnknown * pDevice;
    IDXGIOutputDuplication ** ppOutputDuplication;
    /* Shadow: the guest-allocated id for ppOutputDuplication arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppOutputDuplication -> *ppOutputDuplication} in the
     * context object table. */
    uint64_t _guest_id_ppOutputDuplication;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput1_DuplicateOutput(const IUnknown * pDevice,IDXGIOutputDuplication ** ppOutputDuplication)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppOutputDuplication: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput1_DuplicateOutput(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        IUnknown * pDevice,
                                        IDXGIOutputDuplication ** ppOutputDuplication)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput1_DuplicateOutput(pDevice,ppOutputDuplication);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                 NPT_METHOD_IDXGIOutput1_DuplicateOutput),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
{
    uint64_t _gid_ppOutputDuplication = 0;
    if (ppOutputDuplication) {
        _gid_ppOutputDuplication = npt_com_allocate_next_id();
        *ppOutputDuplication = (IDXGIOutputDuplication *)(uintptr_t)_gid_ppOutputDuplication;
    }
    npt_encode_uint64_t(enc, &_gid_ppOutputDuplication);
}
}

static inline size_t
npt_sizeof_IDXGIOutput1_DuplicateOutput_reply(const IUnknown * pDevice,IDXGIOutputDuplication ** ppOutputDuplication)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip ppOutputDuplication (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput1_DuplicateOutput_reply(struct npt_cs_decoder *dec,
                                              IDXGIOutputDuplication ** ppOutputDuplication,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                                  NPT_METHOD_IDXGIOutput1_DuplicateOutput))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput1_DuplicateOutput reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                       NPT_METHOD_IDXGIOutput1_DuplicateOutput));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppOutputDuplication (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIOutput1_DuplicateOutput(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        IUnknown * pDevice,
                                        IDXGIOutputDuplication ** ppOutputDuplication,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput1_DuplicateOutput(pDevice,ppOutputDuplication);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput1_DuplicateOutput_reply(pDevice,ppOutputDuplication)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput1_DuplicateOutput(enc, cmd_flags, object_id, pDevice, ppOutputDuplication);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput1_DuplicateOutput(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       IUnknown * pDevice,
                                       IDXGIOutputDuplication ** ppOutputDuplication)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput1_DuplicateOutput(ring, 0, object_id, pDevice, ppOutputDuplication, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput1_DuplicateOutput(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      IUnknown * pDevice,
                                      IDXGIOutputDuplication ** ppOutputDuplication)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput1_DuplicateOutput(ring, NPT_CMD_FLAG_REPLY, object_id, pDevice, ppOutputDuplication, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput1_DuplicateOutput_reply(dec, ppOutputDuplication, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput2::SupportsOverlays
 */

struct npt_command_IDXGIOutput2_SupportsOverlays {
    void *_self;
    BOOL ret;
};

static inline size_t
npt_sizeof_IDXGIOutput2_SupportsOverlays(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput2_SupportsOverlays(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput2_SupportsOverlays();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput2,
                                 NPT_METHOD_IDXGIOutput2_SupportsOverlays),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIOutput2_SupportsOverlays_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput2_SupportsOverlays_reply(struct npt_cs_decoder *dec,
                                               BOOL *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput2,
                                                  NPT_METHOD_IDXGIOutput2_SupportsOverlays))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput2_SupportsOverlays reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput2,
                                       NPT_METHOD_IDXGIOutput2_SupportsOverlays));
        if (ret) *ret = (BOOL)0;
        return;
    }
    if (ret) *ret = (BOOL)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIOutput2_SupportsOverlays(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput2_SupportsOverlays();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput2_SupportsOverlays_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput2_SupportsOverlays(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput2_SupportsOverlays(struct npt_ring *ring,
                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput2_SupportsOverlays(ring, 0, object_id, &submit);
}

static inline BOOL
npt_call_IDXGIOutput2_SupportsOverlays(struct npt_ring *ring,
                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput2_SupportsOverlays(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        BOOL _ret;
        npt_decode_IDXGIOutput2_SupportsOverlays_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (BOOL)0;
    }
}

/*
 * IDXGIOutput3::CheckOverlaySupport
 */

struct npt_command_IDXGIOutput3_CheckOverlaySupport {
    void *_self;
    DXGI_FORMAT EnumFormat;
    IUnknown * pConcernedDevice;
    UINT * pFlags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput3_CheckOverlaySupport(const DXGI_FORMAT EnumFormat,const IUnknown * pConcernedDevice,const UINT * pFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FORMAT(&EnumFormat, max_mode);
    cmd_size += sizeof(uint64_t);
/* skip pFlags (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput3_CheckOverlaySupport(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            DXGI_FORMAT EnumFormat,
                                            IUnknown * pConcernedDevice,
                                            UINT * pFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput3_CheckOverlaySupport(EnumFormat,pConcernedDevice,pFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput3,
                                 NPT_METHOD_IDXGIOutput3_CheckOverlaySupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FORMAT(enc, &EnumFormat);
    npt_encode_com_handle(enc, npt_object_get_id(pConcernedDevice));
/* skip pFlags (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput3_CheckOverlaySupport_reply(const DXGI_FORMAT EnumFormat,const IUnknown * pConcernedDevice,const UINT * pFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip EnumFormat (input only) */
/* skip pConcernedDevice (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput3_CheckOverlaySupport_reply(struct npt_cs_decoder *dec,
                                                  UINT * pFlags,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput3,
                                                  NPT_METHOD_IDXGIOutput3_CheckOverlaySupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput3_CheckOverlaySupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput3,
                                       NPT_METHOD_IDXGIOutput3_CheckOverlaySupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pFlags)
            npt_decode_UINT(dec, (UINT *)pFlags);
    }
}

static inline void
npt_submit_IDXGIOutput3_CheckOverlaySupport(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            DXGI_FORMAT EnumFormat,
                                            IUnknown * pConcernedDevice,
                                            UINT * pFlags,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput3_CheckOverlaySupport(EnumFormat,pConcernedDevice,pFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput3_CheckOverlaySupport_reply(EnumFormat,pConcernedDevice,pFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput3_CheckOverlaySupport(enc, cmd_flags, object_id, EnumFormat, pConcernedDevice, pFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput3_CheckOverlaySupport(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           DXGI_FORMAT EnumFormat,
                                           IUnknown * pConcernedDevice,
                                           UINT * pFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput3_CheckOverlaySupport(ring, 0, object_id, EnumFormat, pConcernedDevice, pFlags, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput3_CheckOverlaySupport(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          DXGI_FORMAT EnumFormat,
                                          IUnknown * pConcernedDevice,
                                          UINT * pFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput3_CheckOverlaySupport(ring, NPT_CMD_FLAG_REPLY, object_id, EnumFormat, pConcernedDevice, pFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput3_CheckOverlaySupport_reply(dec, pFlags, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput4::CheckOverlayColorSpaceSupport
 */

struct npt_command_IDXGIOutput4_CheckOverlayColorSpaceSupport {
    void *_self;
    DXGI_FORMAT Format;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
    IUnknown * pConcernedDevice;
    UINT * pFlags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput4_CheckOverlayColorSpaceSupport(const DXGI_FORMAT Format,const DXGI_COLOR_SPACE_TYPE ColorSpace,const IUnknown * pConcernedDevice,const UINT * pFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&ColorSpace, max_mode);
    cmd_size += sizeof(uint64_t);
/* skip pFlags (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput4_CheckOverlayColorSpaceSupport(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      DXGI_FORMAT Format,
                                                      DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                      IUnknown * pConcernedDevice,
                                                      UINT * pFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput4_CheckOverlayColorSpaceSupport(Format,ColorSpace,pConcernedDevice,pFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput4,
                                 NPT_METHOD_IDXGIOutput4_CheckOverlayColorSpaceSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FORMAT(enc, &Format);
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &ColorSpace);
    npt_encode_com_handle(enc, npt_object_get_id(pConcernedDevice));
/* skip pFlags (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput4_CheckOverlayColorSpaceSupport_reply(const DXGI_FORMAT Format,const DXGI_COLOR_SPACE_TYPE ColorSpace,const IUnknown * pConcernedDevice,const UINT * pFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Format (input only) */
/* skip ColorSpace (input only) */
/* skip pConcernedDevice (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput4_CheckOverlayColorSpaceSupport_reply(struct npt_cs_decoder *dec,
                                                            UINT * pFlags,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput4,
                                                  NPT_METHOD_IDXGIOutput4_CheckOverlayColorSpaceSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput4_CheckOverlayColorSpaceSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput4,
                                       NPT_METHOD_IDXGIOutput4_CheckOverlayColorSpaceSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pFlags)
            npt_decode_UINT(dec, (UINT *)pFlags);
    }
}

static inline void
npt_submit_IDXGIOutput4_CheckOverlayColorSpaceSupport(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      DXGI_FORMAT Format,
                                                      DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                      IUnknown * pConcernedDevice,
                                                      UINT * pFlags,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput4_CheckOverlayColorSpaceSupport(Format,ColorSpace,pConcernedDevice,pFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput4_CheckOverlayColorSpaceSupport_reply(Format,ColorSpace,pConcernedDevice,pFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput4_CheckOverlayColorSpaceSupport(enc, cmd_flags, object_id, Format, ColorSpace, pConcernedDevice, pFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput4_CheckOverlayColorSpaceSupport(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     DXGI_FORMAT Format,
                                                     DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                     IUnknown * pConcernedDevice,
                                                     UINT * pFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput4_CheckOverlayColorSpaceSupport(ring, 0, object_id, Format, ColorSpace, pConcernedDevice, pFlags, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput4_CheckOverlayColorSpaceSupport(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    DXGI_FORMAT Format,
                                                    DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                    IUnknown * pConcernedDevice,
                                                    UINT * pFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput4_CheckOverlayColorSpaceSupport(ring, NPT_CMD_FLAG_REPLY, object_id, Format, ColorSpace, pConcernedDevice, pFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput4_CheckOverlayColorSpaceSupport_reply(dec, pFlags, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput5::DuplicateOutput1
 */

struct npt_command_IDXGIOutput5_DuplicateOutput1 {
    void *_self;
    IUnknown * pDevice;
    UINT Flags;
    UINT SupportedFormatsCount;
    const DXGI_FORMAT * pSupportedFormats;
    IDXGIOutputDuplication ** ppOutputDuplication;
    /* Shadow: the guest-allocated id for ppOutputDuplication arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppOutputDuplication -> *ppOutputDuplication} in the
     * context object table. */
    uint64_t _guest_id_ppOutputDuplication;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput5_DuplicateOutput1(const IUnknown * pDevice,const UINT Flags,const UINT SupportedFormatsCount,const DXGI_FORMAT * pSupportedFormats,IDXGIOutputDuplication ** ppOutputDuplication)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    cmd_size += npt_sizeof_UINT(&SupportedFormatsCount, max_mode);
    cmd_size += npt_sizeof_array_count(pSupportedFormats ? SupportedFormatsCount : 0);
    if (pSupportedFormats)
        cmd_size += npt_sizeof_DXGI_FORMAT_array(pSupportedFormats, SupportedFormatsCount);
cmd_size += sizeof(uint64_t);  /* ppOutputDuplication: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput5_DuplicateOutput1(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         IUnknown * pDevice,
                                         UINT Flags,
                                         UINT SupportedFormatsCount,
                                         const DXGI_FORMAT * pSupportedFormats,
                                         IDXGIOutputDuplication ** ppOutputDuplication)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput5_DuplicateOutput1(pDevice,Flags,SupportedFormatsCount,pSupportedFormats,ppOutputDuplication);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput5,
                                 NPT_METHOD_IDXGIOutput5_DuplicateOutput1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
    npt_encode_UINT(enc, &Flags);
    npt_encode_UINT(enc, &SupportedFormatsCount);
    if (pSupportedFormats) {
        npt_encode_array_count(enc, SupportedFormatsCount);
        npt_encode_DXGI_FORMAT_array(enc, pSupportedFormats, SupportedFormatsCount);
    } else {
        npt_encode_array_count(enc, 0);
    }
{
    uint64_t _gid_ppOutputDuplication = 0;
    if (ppOutputDuplication) {
        _gid_ppOutputDuplication = npt_com_allocate_next_id();
        *ppOutputDuplication = (IDXGIOutputDuplication *)(uintptr_t)_gid_ppOutputDuplication;
    }
    npt_encode_uint64_t(enc, &_gid_ppOutputDuplication);
}
}

static inline size_t
npt_sizeof_IDXGIOutput5_DuplicateOutput1_reply(const IUnknown * pDevice,const UINT Flags,const UINT SupportedFormatsCount,const DXGI_FORMAT * pSupportedFormats,IDXGIOutputDuplication ** ppOutputDuplication)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip Flags (input only) */
/* skip SupportedFormatsCount (input only) */
/* skip pSupportedFormats (input only) */
/* skip ppOutputDuplication (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput5_DuplicateOutput1_reply(struct npt_cs_decoder *dec,
                                               IDXGIOutputDuplication ** ppOutputDuplication,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput5,
                                                  NPT_METHOD_IDXGIOutput5_DuplicateOutput1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput5_DuplicateOutput1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput5,
                                       NPT_METHOD_IDXGIOutput5_DuplicateOutput1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppOutputDuplication (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIOutput5_DuplicateOutput1(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         IUnknown * pDevice,
                                         UINT Flags,
                                         UINT SupportedFormatsCount,
                                         const DXGI_FORMAT * pSupportedFormats,
                                         IDXGIOutputDuplication ** ppOutputDuplication,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput5_DuplicateOutput1(pDevice,Flags,SupportedFormatsCount,pSupportedFormats,ppOutputDuplication);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput5_DuplicateOutput1_reply(pDevice,Flags,SupportedFormatsCount,pSupportedFormats,ppOutputDuplication)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput5_DuplicateOutput1(enc, cmd_flags, object_id, pDevice, Flags, SupportedFormatsCount, pSupportedFormats, ppOutputDuplication);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput5_DuplicateOutput1(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        IUnknown * pDevice,
                                        UINT Flags,
                                        UINT SupportedFormatsCount,
                                        const DXGI_FORMAT * pSupportedFormats,
                                        IDXGIOutputDuplication ** ppOutputDuplication)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput5_DuplicateOutput1(ring, 0, object_id, pDevice, Flags, SupportedFormatsCount, pSupportedFormats, ppOutputDuplication, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput5_DuplicateOutput1(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       IUnknown * pDevice,
                                       UINT Flags,
                                       UINT SupportedFormatsCount,
                                       const DXGI_FORMAT * pSupportedFormats,
                                       IDXGIOutputDuplication ** ppOutputDuplication)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput5_DuplicateOutput1(ring, NPT_CMD_FLAG_REPLY, object_id, pDevice, Flags, SupportedFormatsCount, pSupportedFormats, ppOutputDuplication, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput5_DuplicateOutput1_reply(dec, ppOutputDuplication, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput6::GetDesc1
 */

struct npt_command_IDXGIOutput6_GetDesc1 {
    void *_self;
    DXGI_OUTPUT_DESC1 * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput6_GetDesc1(const DXGI_OUTPUT_DESC1 * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput6_GetDesc1(struct npt_cs_encoder *enc,
                                 uint32_t cmd_flags,
                                 npt_object_id object_id,
                                 DXGI_OUTPUT_DESC1 * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput6_GetDesc1(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput6,
                                 NPT_METHOD_IDXGIOutput6_GetDesc1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput6_GetDesc1_reply(const DXGI_OUTPUT_DESC1 * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_OUTPUT_DESC1(&(const DXGI_OUTPUT_DESC1){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput6_GetDesc1_reply(struct npt_cs_decoder *dec,
                                       DXGI_OUTPUT_DESC1 * pDesc,
                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput6,
                                                  NPT_METHOD_IDXGIOutput6_GetDesc1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput6_GetDesc1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput6,
                                       NPT_METHOD_IDXGIOutput6_GetDesc1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_OUTPUT_DESC1(dec, (DXGI_OUTPUT_DESC1 *)pDesc);
    }
}

static inline void
npt_submit_IDXGIOutput6_GetDesc1(struct npt_ring *ring,
                                 uint32_t cmd_flags,
                                 npt_object_id object_id,
                                 DXGI_OUTPUT_DESC1 * pDesc,
                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput6_GetDesc1(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput6_GetDesc1_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput6_GetDesc1(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput6_GetDesc1(struct npt_ring *ring,
                                npt_object_id object_id,
                                DXGI_OUTPUT_DESC1 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput6_GetDesc1(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput6_GetDesc1(struct npt_ring *ring,
                               npt_object_id object_id,
                               DXGI_OUTPUT_DESC1 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput6_GetDesc1(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput6_GetDesc1_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIOutput6::CheckHardwareCompositionSupport
 */

struct npt_command_IDXGIOutput6_CheckHardwareCompositionSupport {
    void *_self;
    UINT * pFlags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIOutput6_CheckHardwareCompositionSupport(const UINT * pFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pFlags (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIOutput6_CheckHardwareCompositionSupport(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        UINT * pFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIOutput6_CheckHardwareCompositionSupport(pFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput6,
                                 NPT_METHOD_IDXGIOutput6_CheckHardwareCompositionSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pFlags (output only) */
}

static inline size_t
npt_sizeof_IDXGIOutput6_CheckHardwareCompositionSupport_reply(const UINT * pFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIOutput6_CheckHardwareCompositionSupport_reply(struct npt_cs_decoder *dec,
                                                              UINT * pFlags,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput6,
                                                  NPT_METHOD_IDXGIOutput6_CheckHardwareCompositionSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIOutput6_CheckHardwareCompositionSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput6,
                                       NPT_METHOD_IDXGIOutput6_CheckHardwareCompositionSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pFlags)
            npt_decode_UINT(dec, (UINT *)pFlags);
    }
}

static inline void
npt_submit_IDXGIOutput6_CheckHardwareCompositionSupport(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        UINT * pFlags,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIOutput6_CheckHardwareCompositionSupport(pFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIOutput6_CheckHardwareCompositionSupport_reply(pFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIOutput6_CheckHardwareCompositionSupport(enc, cmd_flags, object_id, pFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIOutput6_CheckHardwareCompositionSupport(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       UINT * pFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput6_CheckHardwareCompositionSupport(ring, 0, object_id, pFlags, &submit);
}

static inline HRESULT
npt_call_IDXGIOutput6_CheckHardwareCompositionSupport(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      UINT * pFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIOutput6_CheckHardwareCompositionSupport(ring, NPT_CMD_FLAG_REPLY, object_id, pFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIOutput6_CheckHardwareCompositionSupport_reply(dec, pFlags, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIOUTPUT_H */
