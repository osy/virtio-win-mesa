/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM declarations for id3d12resource: per-interface vtable
 * structs, default thunk prototypes, default vtable populators, and default
 * constructors.
 *
 * The thunk bodies, populators, and constructors live in the matching .c
 * file.  This split keeps the headers small and prevents N copies of the
 * thunk code from landing in every TU that pulls in this family.
 */

#ifndef NPT_PROTOCOL_CLIENT_ID3D12RESOURCE_H
#define NPT_PROTOCOL_CLIENT_ID3D12RESOURCE_H

#include "npt_protocol_guest_id3d12resource.h"
#include "npt_protocol_client_id3d12pageable.h"

/* Forward-declare struct npt_device so the client_create prototypes don't
 * trigger the "declared inside parameter list" warning.  The full
 * definition lives in the consumer's npt_device.h, which the .c file pulls
 * in via npt_com.h. */
struct npt_device;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"



/* ================================================================== */
/* ID3D12Resource                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D12ResourceVtbl, by construction.
 */
struct npt_id3d12resource_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D12Object::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * guid, UINT * pDataSize, void * pData);
    /* slot 4: ID3D12Object::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * guid, UINT DataSize, const void * pData);
    /* slot 5: ID3D12Object::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * guid, const IUnknown * pData);
    /* slot 6: ID3D12Object::SetName */
    HRESULT (NPT_STDMETHODCALLTYPE *SetName)(void *self, const WCHAR * Name);
    /* slot 7: ID3D12DeviceChild::GetDevice */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDevice)(void *self, const IID * riid, void ** ppvDevice);
    /* slot 8: ID3D12Resource::Map */
    HRESULT (NPT_STDMETHODCALLTYPE *Map)(void *self, UINT Subresource, const D3D12_RANGE * pReadRange, void ** ppData);
    /* slot 9: ID3D12Resource::Unmap */
    void (NPT_STDMETHODCALLTYPE *Unmap)(void *self, UINT Subresource, const D3D12_RANGE * pWrittenRange);
    /* slot 10: ID3D12Resource::GetDesc */
    D3D12_RESOURCE_DESC * (NPT_STDMETHODCALLTYPE *GetDesc)(void *self, D3D12_RESOURCE_DESC *_ret_out);
    /* slot 11: ID3D12Resource::GetGPUVirtualAddress */
    D3D12_GPU_VIRTUAL_ADDRESS (NPT_STDMETHODCALLTYPE *GetGPUVirtualAddress)(void *self);
    /* slot 12: ID3D12Resource::WriteToSubresource */
    HRESULT (NPT_STDMETHODCALLTYPE *WriteToSubresource)(void *self, UINT DstSubresource, const D3D12_BOX * pDstBox, const void * pSrcData, UINT SrcRowPitch, UINT SrcDepthPitch);
    /* slot 13: ID3D12Resource::ReadFromSubresource */
    HRESULT (NPT_STDMETHODCALLTYPE *ReadFromSubresource)(void *self, void * pDstData, UINT DstRowPitch, UINT DstDepthPitch, UINT SrcSubresource, const D3D12_BOX * pSrcBox);
    /* slot 14: ID3D12Resource::GetHeapProperties */
    HRESULT (NPT_STDMETHODCALLTYPE *GetHeapProperties)(void *self, D3D12_HEAP_PROPERTIES * pHeapProperties, D3D12_HEAP_FLAGS * pHeapFlags);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_Map(void *self, UINT Subresource, const D3D12_RANGE * pReadRange, void ** ppData);
extern void NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_Unmap(void *self, UINT Subresource, const D3D12_RANGE * pWrittenRange);
extern D3D12_RESOURCE_DESC * NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_GetDesc(void *self, D3D12_RESOURCE_DESC *_ret_out);
extern D3D12_GPU_VIRTUAL_ADDRESS NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_GetGPUVirtualAddress(void *self);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_WriteToSubresource(void *self, UINT DstSubresource, const D3D12_BOX * pDstBox, const void * pSrcData, UINT SrcRowPitch, UINT SrcDepthPitch);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_ReadFromSubresource(void *self, void * pDstData, UINT DstRowPitch, UINT DstDepthPitch, UINT SrcSubresource, const D3D12_BOX * pSrcBox);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_GetHeapProperties(void *self, D3D12_HEAP_PROPERTIES * pHeapProperties, D3D12_HEAP_FLAGS * pHeapFlags);

/* Default IUnknown thunks for ID3D12Resource.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12resource_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d12resource_fill_default_vtbl(struct npt_id3d12resource_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d12resource_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d12resource_client_vtbl npt_id3d12resource_default_vtbl_storage;
extern void
npt_id3d12resource_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d12resource_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* ID3D12Resource1                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D12Resource1Vtbl, by construction.
 */
struct npt_id3d12resource1_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D12Object::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * guid, UINT * pDataSize, void * pData);
    /* slot 4: ID3D12Object::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * guid, UINT DataSize, const void * pData);
    /* slot 5: ID3D12Object::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * guid, const IUnknown * pData);
    /* slot 6: ID3D12Object::SetName */
    HRESULT (NPT_STDMETHODCALLTYPE *SetName)(void *self, const WCHAR * Name);
    /* slot 7: ID3D12DeviceChild::GetDevice */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDevice)(void *self, const IID * riid, void ** ppvDevice);
    /* slot 8: ID3D12Resource::Map */
    HRESULT (NPT_STDMETHODCALLTYPE *Map)(void *self, UINT Subresource, const D3D12_RANGE * pReadRange, void ** ppData);
    /* slot 9: ID3D12Resource::Unmap */
    void (NPT_STDMETHODCALLTYPE *Unmap)(void *self, UINT Subresource, const D3D12_RANGE * pWrittenRange);
    /* slot 10: ID3D12Resource::GetDesc */
    D3D12_RESOURCE_DESC * (NPT_STDMETHODCALLTYPE *GetDesc)(void *self, D3D12_RESOURCE_DESC *_ret_out);
    /* slot 11: ID3D12Resource::GetGPUVirtualAddress */
    D3D12_GPU_VIRTUAL_ADDRESS (NPT_STDMETHODCALLTYPE *GetGPUVirtualAddress)(void *self);
    /* slot 12: ID3D12Resource::WriteToSubresource */
    HRESULT (NPT_STDMETHODCALLTYPE *WriteToSubresource)(void *self, UINT DstSubresource, const D3D12_BOX * pDstBox, const void * pSrcData, UINT SrcRowPitch, UINT SrcDepthPitch);
    /* slot 13: ID3D12Resource::ReadFromSubresource */
    HRESULT (NPT_STDMETHODCALLTYPE *ReadFromSubresource)(void *self, void * pDstData, UINT DstRowPitch, UINT DstDepthPitch, UINT SrcSubresource, const D3D12_BOX * pSrcBox);
    /* slot 14: ID3D12Resource::GetHeapProperties */
    HRESULT (NPT_STDMETHODCALLTYPE *GetHeapProperties)(void *self, D3D12_HEAP_PROPERTIES * pHeapProperties, D3D12_HEAP_FLAGS * pHeapFlags);
    /* slot 15: ID3D12Resource1::GetProtectedResourceSession */
    HRESULT (NPT_STDMETHODCALLTYPE *GetProtectedResourceSession)(void *self, const IID * riid, void ** ppProtectedSession);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12resource1_default_GetProtectedResourceSession(void *self, const IID * riid, void ** ppProtectedSession);

/* Default IUnknown thunks for ID3D12Resource1.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12resource1_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12resource1_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12resource1_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d12resource1_fill_default_vtbl(struct npt_id3d12resource1_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d12resource1_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d12resource1_client_vtbl npt_id3d12resource1_default_vtbl_storage;
extern void
npt_id3d12resource1_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d12resource1_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* ID3D12Resource2                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D12Resource2Vtbl, by construction.
 */
struct npt_id3d12resource2_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D12Object::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * guid, UINT * pDataSize, void * pData);
    /* slot 4: ID3D12Object::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * guid, UINT DataSize, const void * pData);
    /* slot 5: ID3D12Object::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * guid, const IUnknown * pData);
    /* slot 6: ID3D12Object::SetName */
    HRESULT (NPT_STDMETHODCALLTYPE *SetName)(void *self, const WCHAR * Name);
    /* slot 7: ID3D12DeviceChild::GetDevice */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDevice)(void *self, const IID * riid, void ** ppvDevice);
    /* slot 8: ID3D12Resource::Map */
    HRESULT (NPT_STDMETHODCALLTYPE *Map)(void *self, UINT Subresource, const D3D12_RANGE * pReadRange, void ** ppData);
    /* slot 9: ID3D12Resource::Unmap */
    void (NPT_STDMETHODCALLTYPE *Unmap)(void *self, UINT Subresource, const D3D12_RANGE * pWrittenRange);
    /* slot 10: ID3D12Resource::GetDesc */
    D3D12_RESOURCE_DESC * (NPT_STDMETHODCALLTYPE *GetDesc)(void *self, D3D12_RESOURCE_DESC *_ret_out);
    /* slot 11: ID3D12Resource::GetGPUVirtualAddress */
    D3D12_GPU_VIRTUAL_ADDRESS (NPT_STDMETHODCALLTYPE *GetGPUVirtualAddress)(void *self);
    /* slot 12: ID3D12Resource::WriteToSubresource */
    HRESULT (NPT_STDMETHODCALLTYPE *WriteToSubresource)(void *self, UINT DstSubresource, const D3D12_BOX * pDstBox, const void * pSrcData, UINT SrcRowPitch, UINT SrcDepthPitch);
    /* slot 13: ID3D12Resource::ReadFromSubresource */
    HRESULT (NPT_STDMETHODCALLTYPE *ReadFromSubresource)(void *self, void * pDstData, UINT DstRowPitch, UINT DstDepthPitch, UINT SrcSubresource, const D3D12_BOX * pSrcBox);
    /* slot 14: ID3D12Resource::GetHeapProperties */
    HRESULT (NPT_STDMETHODCALLTYPE *GetHeapProperties)(void *self, D3D12_HEAP_PROPERTIES * pHeapProperties, D3D12_HEAP_FLAGS * pHeapFlags);
    /* slot 15: ID3D12Resource1::GetProtectedResourceSession */
    HRESULT (NPT_STDMETHODCALLTYPE *GetProtectedResourceSession)(void *self, const IID * riid, void ** ppProtectedSession);
    /* slot 16: ID3D12Resource2::GetDesc1 */
    D3D12_RESOURCE_DESC1 * (NPT_STDMETHODCALLTYPE *GetDesc1)(void *self, D3D12_RESOURCE_DESC1 *_ret_out);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern D3D12_RESOURCE_DESC1 * NPT_STDMETHODCALLTYPE
npt_id3d12resource2_default_GetDesc1(void *self, D3D12_RESOURCE_DESC1 *_ret_out);

/* Default IUnknown thunks for ID3D12Resource2.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12resource2_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12resource2_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12resource2_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d12resource2_fill_default_vtbl(struct npt_id3d12resource2_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d12resource2_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d12resource2_client_vtbl npt_id3d12resource2_default_vtbl_storage;
extern void
npt_id3d12resource2_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d12resource2_client_create(struct npt_device *dev, uint64_t host_id);


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_CLIENT_ID3D12RESOURCE_H */
