/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgidisplaycontrol.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIDISPLAYCONTROL_H
#define NPT_PROTOCOL_GUEST_IDXGIDISPLAYCONTROL_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIDisplayControl::IsStereoEnabled
 */

struct npt_command_IDXGIDisplayControl_IsStereoEnabled {
    void *_self;
    BOOL ret;
};

static inline size_t
npt_sizeof_IDXGIDisplayControl_IsStereoEnabled(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDisplayControl_IsStereoEnabled(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDisplayControl_IsStereoEnabled();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDisplayControl,
                                 NPT_METHOD_IDXGIDisplayControl_IsStereoEnabled),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIDisplayControl_IsStereoEnabled_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGIDisplayControl_IsStereoEnabled_reply(struct npt_cs_decoder *dec,
                                                     BOOL *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDisplayControl,
                                                  NPT_METHOD_IDXGIDisplayControl_IsStereoEnabled))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIDisplayControl_IsStereoEnabled reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDisplayControl,
                                       NPT_METHOD_IDXGIDisplayControl_IsStereoEnabled));
        if (ret) *ret = (BOOL)0;
        return;
    }
    if (ret) *ret = (BOOL)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIDisplayControl_IsStereoEnabled(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDisplayControl_IsStereoEnabled();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDisplayControl_IsStereoEnabled_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDisplayControl_IsStereoEnabled(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDisplayControl_IsStereoEnabled(struct npt_ring *ring,
                                              npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDisplayControl_IsStereoEnabled(ring, 0, object_id, &submit);
}

static inline BOOL
npt_call_IDXGIDisplayControl_IsStereoEnabled(struct npt_ring *ring,
                                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDisplayControl_IsStereoEnabled(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        BOOL _ret;
        npt_decode_IDXGIDisplayControl_IsStereoEnabled_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (BOOL)0;
    }
}

/*
 * IDXGIDisplayControl::SetStereoEnabled
 */

struct npt_command_IDXGIDisplayControl_SetStereoEnabled {
    void *_self;
    BOOL enabled;
};

static inline size_t
npt_sizeof_IDXGIDisplayControl_SetStereoEnabled(const BOOL enabled)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_BOOL(&enabled, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIDisplayControl_SetStereoEnabled(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                BOOL enabled)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIDisplayControl_SetStereoEnabled(enabled);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDisplayControl,
                                 NPT_METHOD_IDXGIDisplayControl_SetStereoEnabled),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_BOOL(enc, &enabled);
}

static inline size_t
npt_sizeof_IDXGIDisplayControl_SetStereoEnabled_reply(const BOOL enabled)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip enabled (input only) */
    return cmd_size;
}

static inline void
npt_submit_IDXGIDisplayControl_SetStereoEnabled(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                BOOL enabled,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIDisplayControl_SetStereoEnabled(enabled);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIDisplayControl_SetStereoEnabled_reply(enabled)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIDisplayControl_SetStereoEnabled(enc, cmd_flags, object_id, enabled);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIDisplayControl_SetStereoEnabled(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               BOOL enabled)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDisplayControl_SetStereoEnabled(ring, 0, object_id, enabled, &submit);
}

static inline void
npt_call_IDXGIDisplayControl_SetStereoEnabled(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              BOOL enabled)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIDisplayControl_SetStereoEnabled(ring, NPT_CMD_FLAG_REPLY, object_id, enabled, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIDISPLAYCONTROL_H */
