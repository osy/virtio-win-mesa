/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12lifetimeowner.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12LIFETIMEOWNER_H
#define NPT_PROTOCOL_GUEST_ID3D12LIFETIMEOWNER_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12LifetimeOwner::LifetimeStateUpdated
 */

struct npt_command_ID3D12LifetimeOwner_LifetimeStateUpdated {
    void *_self;
    D3D12_LIFETIME_STATE NewState;
};

static inline size_t
npt_sizeof_ID3D12LifetimeOwner_LifetimeStateUpdated(const D3D12_LIFETIME_STATE NewState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_LIFETIME_STATE(&NewState, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12LifetimeOwner_LifetimeStateUpdated(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    D3D12_LIFETIME_STATE NewState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12LifetimeOwner_LifetimeStateUpdated(NewState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12LifetimeOwner,
                                 NPT_METHOD_ID3D12LifetimeOwner_LifetimeStateUpdated),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_LIFETIME_STATE(enc, &NewState);
}

static inline size_t
npt_sizeof_ID3D12LifetimeOwner_LifetimeStateUpdated_reply(const D3D12_LIFETIME_STATE NewState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NewState (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12LifetimeOwner_LifetimeStateUpdated(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    D3D12_LIFETIME_STATE NewState,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12LifetimeOwner_LifetimeStateUpdated(NewState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12LifetimeOwner_LifetimeStateUpdated_reply(NewState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12LifetimeOwner_LifetimeStateUpdated(enc, cmd_flags, object_id, NewState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12LifetimeOwner_LifetimeStateUpdated(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   D3D12_LIFETIME_STATE NewState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12LifetimeOwner_LifetimeStateUpdated(ring, 0, object_id, NewState, &submit);
}

static inline void
npt_call_ID3D12LifetimeOwner_LifetimeStateUpdated(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  D3D12_LIFETIME_STATE NewState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12LifetimeOwner_LifetimeStateUpdated(ring, NPT_CMD_FLAG_REPLY, object_id, NewState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12LIFETIMEOWNER_H */
