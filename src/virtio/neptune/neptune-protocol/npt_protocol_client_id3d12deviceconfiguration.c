/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for id3d12deviceconfiguration: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_id3d12deviceconfiguration.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* ID3D12DeviceConfiguration                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12deviceconfiguration_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12DeviceConfiguration,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12deviceconfiguration_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration_default_Release(void *self)
{
    return npt_com_default_release(self);
}

D3D12_DEVICE_CONFIGURATION_DESC * NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration_default_GetDesc(void *self, D3D12_DEVICE_CONFIGURATION_DESC *_ret_out)
{
    D3D12_DEVICE_CONFIGURATION_DESC _ret = npt_call_ID3D12DeviceConfiguration_GetDesc(npt_com_self_ring(self),npt_com_self_id(self));
    /* COM x64 aggregate-return ABI: copy into the caller's hidden
     * return slot and hand the pointer back. */
    if (_ret_out) *_ret_out = _ret;
    return _ret_out;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration_default_GetEnabledExperimentalFeatures(void *self, GUID * pGuids, UINT NumGuids)
{
    HRESULT _ret = npt_call_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(npt_com_self_ring(self),npt_com_self_id(self),pGuids,NumGuids);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration_default_SerializeVersionedRootSignature(void *self, const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pDesc, ID3DBlob ** ppResult, ID3DBlob ** ppError)
{
    ID3DBlob *_raw_ppResult = NULL;
    ID3DBlob *_raw_ppError = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3DBlob **_orig_ppResult = ppResult;
    if (ppResult)
        ppResult = (ID3DBlob **)&_raw_ppResult;
    ID3DBlob **_orig_ppError = ppError;
    if (ppError)
        ppError = (ID3DBlob **)&_raw_ppError;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(npt_com_self_ring(self),npt_com_self_id(self),pDesc,ppResult,ppError);
    } else {
        npt_async_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(npt_com_self_ring(self),npt_com_self_id(self),pDesc,ppResult,ppError);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppResult) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppResult)
            *_orig_ppResult = (ID3DBlob *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D10Blob,
                (uint64_t)(uintptr_t)_raw_ppResult,
                (struct npt_com_base *)self);
        else
            *_orig_ppResult = NULL;
    }
    if (_orig_ppError) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppError)
            *_orig_ppError = (ID3DBlob *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D10Blob,
                (uint64_t)(uintptr_t)_raw_ppError,
                (struct npt_com_base *)self);
        else
            *_orig_ppError = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration_default_CreateVersionedRootSignatureDeserializer(void *self, const void * pBlob, SIZE_T Size, const IID * riid, void ** ppvDeserializer)
{
    void *_raw_ppvDeserializer = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvDeserializer = ppvDeserializer;
    if (ppvDeserializer)
        ppvDeserializer = (void **)&_raw_ppvDeserializer;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(npt_com_self_ring(self),npt_com_self_id(self),pBlob,Size,riid,ppvDeserializer);
    } else {
        npt_async_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(npt_com_self_ring(self),npt_com_self_id(self),pBlob,Size,riid,ppvDeserializer);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppvDeserializer) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppvDeserializer)
            *_orig_ppvDeserializer = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvDeserializer,
                (struct npt_com_base *)self);
        else
            *_orig_ppvDeserializer = NULL;
    }
    return _ret;
}


void
npt_id3d12deviceconfiguration_fill_default_vtbl(struct npt_id3d12deviceconfiguration_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12deviceconfiguration_default_QueryInterface;
    dst->AddRef         = npt_id3d12deviceconfiguration_default_AddRef;
    dst->Release        = npt_id3d12deviceconfiguration_default_Release;
    dst->GetDesc = npt_id3d12deviceconfiguration_default_GetDesc;
    dst->GetEnabledExperimentalFeatures = npt_id3d12deviceconfiguration_default_GetEnabledExperimentalFeatures;
    dst->SerializeVersionedRootSignature = npt_id3d12deviceconfiguration_default_SerializeVersionedRootSignature;
    dst->CreateVersionedRootSignatureDeserializer = npt_id3d12deviceconfiguration_default_CreateVersionedRootSignatureDeserializer;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12deviceconfiguration_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12deviceconfiguration_client_vtbl npt_id3d12deviceconfiguration_default_vtbl_storage;

void
npt_id3d12deviceconfiguration_install_default_vtbl(void)
{
    npt_id3d12deviceconfiguration_fill_default_vtbl(&npt_id3d12deviceconfiguration_default_vtbl_storage);
}

void *
npt_id3d12deviceconfiguration_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12deviceconfiguration_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12DeviceConfiguration1                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12deviceconfiguration1_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12DeviceConfiguration,
    &NPT_IID_ID3D12DeviceConfiguration1,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration1_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12deviceconfiguration1_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration1_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration1_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12deviceconfiguration1_default_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(void *self, const void * pLibraryBlob, SIZE_T Size, const WCHAR * RootSignatureSubobjectName, const IID * riid, void ** ppvDeserializer)
{
    void *_raw_ppvDeserializer = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvDeserializer = ppvDeserializer;
    if (ppvDeserializer)
        ppvDeserializer = (void **)&_raw_ppvDeserializer;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(npt_com_self_ring(self),npt_com_self_id(self),pLibraryBlob,Size,RootSignatureSubobjectName,riid,ppvDeserializer);
    } else {
        npt_async_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(npt_com_self_ring(self),npt_com_self_id(self),pLibraryBlob,Size,RootSignatureSubobjectName,riid,ppvDeserializer);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppvDeserializer) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppvDeserializer)
            *_orig_ppvDeserializer = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvDeserializer,
                (struct npt_com_base *)self);
        else
            *_orig_ppvDeserializer = NULL;
    }
    return _ret;
}


void
npt_id3d12deviceconfiguration1_fill_default_vtbl(struct npt_id3d12deviceconfiguration1_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12deviceconfiguration1_default_QueryInterface;
    dst->AddRef         = npt_id3d12deviceconfiguration1_default_AddRef;
    dst->Release        = npt_id3d12deviceconfiguration1_default_Release;
    dst->GetDesc = npt_id3d12deviceconfiguration_default_GetDesc;
    dst->GetEnabledExperimentalFeatures = npt_id3d12deviceconfiguration_default_GetEnabledExperimentalFeatures;
    dst->SerializeVersionedRootSignature = npt_id3d12deviceconfiguration_default_SerializeVersionedRootSignature;
    dst->CreateVersionedRootSignatureDeserializer = npt_id3d12deviceconfiguration_default_CreateVersionedRootSignatureDeserializer;
    dst->CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary = npt_id3d12deviceconfiguration1_default_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12deviceconfiguration1_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12deviceconfiguration1_client_vtbl npt_id3d12deviceconfiguration1_default_vtbl_storage;

void
npt_id3d12deviceconfiguration1_install_default_vtbl(void)
{
    npt_id3d12deviceconfiguration1_fill_default_vtbl(&npt_id3d12deviceconfiguration1_default_vtbl_storage);
}

void *
npt_id3d12deviceconfiguration1_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12deviceconfiguration1_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
