/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11devicecontext.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11DEVICECONTEXT_H
#define NPT_PROTOCOL_GUEST_ID3D11DEVICECONTEXT_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11DeviceContext::VSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_VSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSSetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_VSSetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_VSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSSetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSSetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_VSSetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_VSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_VSSetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_VSSetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_VSSetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSSetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_VSSetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSSetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::PSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_PSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSSetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
    cmd_size += npt_sizeof_array_count(NumViews);
    cmd_size += sizeof(uint64_t) * NumViews;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_PSSetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_PSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSSetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
    if (ppShaderResourceViews) {
        npt_encode_array_count(enc, NumViews);
        for (uint32_t _i = 0; _i < (uint32_t)NumViews; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppShaderResourceViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSSetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_PSSetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_PSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_PSSetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_PSSetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_PSSetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSSetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_PSSetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSSetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::PSSetShader
 */

struct npt_command_ID3D11DeviceContext_PSSetShader {
    void *_self;
    ID3D11PixelShader * pPixelShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSSetShader(const ID3D11PixelShader * pPixelShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(NumClassInstances);
    cmd_size += sizeof(uint64_t) * NumClassInstances;
    cmd_size += npt_sizeof_UINT(&NumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_PSSetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11PixelShader * pPixelShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_PSSetShader(pPixelShader,ppClassInstances,NumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSSetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pPixelShader));
    if (ppClassInstances) {
        npt_encode_array_count(enc, NumClassInstances);
        for (uint32_t _i = 0; _i < (uint32_t)NumClassInstances; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppClassInstances[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSSetShader_reply(const ID3D11PixelShader * pPixelShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pPixelShader (input only) */
/* skip ppClassInstances (input only) */
/* skip NumClassInstances (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_PSSetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11PixelShader * pPixelShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_PSSetShader(pPixelShader,ppClassInstances,NumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_PSSetShader_reply(pPixelShader,ppClassInstances,NumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_PSSetShader(enc, cmd_flags, object_id, pPixelShader, ppClassInstances, NumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_PSSetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11PixelShader * pPixelShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSSetShader(ring, 0, object_id, pPixelShader, ppClassInstances, NumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_PSSetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11PixelShader * pPixelShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSSetShader(ring, NPT_CMD_FLAG_REPLY, object_id, pPixelShader, ppClassInstances, NumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::PSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_PSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSSetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
    cmd_size += npt_sizeof_array_count(NumSamplers);
    cmd_size += sizeof(uint64_t) * NumSamplers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_PSSetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_PSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSSetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
    if (ppSamplers) {
        npt_encode_array_count(enc, NumSamplers);
        for (uint32_t _i = 0; _i < (uint32_t)NumSamplers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppSamplers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSSetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_PSSetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_PSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_PSSetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_PSSetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_PSSetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSSetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_PSSetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSSetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::VSSetShader
 */

struct npt_command_ID3D11DeviceContext_VSSetShader {
    void *_self;
    ID3D11VertexShader * pVertexShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSSetShader(const ID3D11VertexShader * pVertexShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(NumClassInstances);
    cmd_size += sizeof(uint64_t) * NumClassInstances;
    cmd_size += npt_sizeof_UINT(&NumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_VSSetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11VertexShader * pVertexShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_VSSetShader(pVertexShader,ppClassInstances,NumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSSetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVertexShader));
    if (ppClassInstances) {
        npt_encode_array_count(enc, NumClassInstances);
        for (uint32_t _i = 0; _i < (uint32_t)NumClassInstances; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppClassInstances[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSSetShader_reply(const ID3D11VertexShader * pVertexShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVertexShader (input only) */
/* skip ppClassInstances (input only) */
/* skip NumClassInstances (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_VSSetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11VertexShader * pVertexShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_VSSetShader(pVertexShader,ppClassInstances,NumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_VSSetShader_reply(pVertexShader,ppClassInstances,NumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_VSSetShader(enc, cmd_flags, object_id, pVertexShader, ppClassInstances, NumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_VSSetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11VertexShader * pVertexShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSSetShader(ring, 0, object_id, pVertexShader, ppClassInstances, NumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_VSSetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11VertexShader * pVertexShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSSetShader(ring, NPT_CMD_FLAG_REPLY, object_id, pVertexShader, ppClassInstances, NumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DrawIndexed
 */

struct npt_command_ID3D11DeviceContext_DrawIndexed {
    void *_self;
    UINT IndexCount;
    UINT StartIndexLocation;
    INT BaseVertexLocation;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawIndexed(const UINT IndexCount,const UINT StartIndexLocation,const INT BaseVertexLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&IndexCount, max_mode);
    cmd_size += npt_sizeof_UINT(&StartIndexLocation, max_mode);
    cmd_size += npt_sizeof_INT(&BaseVertexLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DrawIndexed(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           UINT IndexCount,
                                           UINT StartIndexLocation,
                                           INT BaseVertexLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DrawIndexed(IndexCount,StartIndexLocation,BaseVertexLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawIndexed),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &IndexCount);
    npt_encode_UINT(enc, &StartIndexLocation);
    npt_encode_INT(enc, &BaseVertexLocation);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawIndexed_reply(const UINT IndexCount,const UINT StartIndexLocation,const INT BaseVertexLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip IndexCount (input only) */
/* skip StartIndexLocation (input only) */
/* skip BaseVertexLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DrawIndexed(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           UINT IndexCount,
                                           UINT StartIndexLocation,
                                           INT BaseVertexLocation,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DrawIndexed(IndexCount,StartIndexLocation,BaseVertexLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DrawIndexed_reply(IndexCount,StartIndexLocation,BaseVertexLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DrawIndexed(enc, cmd_flags, object_id, IndexCount, StartIndexLocation, BaseVertexLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DrawIndexed(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          UINT IndexCount,
                                          UINT StartIndexLocation,
                                          INT BaseVertexLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawIndexed(ring, 0, object_id, IndexCount, StartIndexLocation, BaseVertexLocation, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DrawIndexed(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         UINT IndexCount,
                                         UINT StartIndexLocation,
                                         INT BaseVertexLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawIndexed(ring, NPT_CMD_FLAG_REPLY, object_id, IndexCount, StartIndexLocation, BaseVertexLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::Draw
 */

struct npt_command_ID3D11DeviceContext_Draw {
    void *_self;
    UINT VertexCount;
    UINT StartVertexLocation;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_Draw(const UINT VertexCount,const UINT StartVertexLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&VertexCount, max_mode);
    cmd_size += npt_sizeof_UINT(&StartVertexLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_Draw(struct npt_cs_encoder *enc,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT VertexCount,
                                    UINT StartVertexLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_Draw(VertexCount,StartVertexLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Draw),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &VertexCount);
    npt_encode_UINT(enc, &StartVertexLocation);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_Draw_reply(const UINT VertexCount,const UINT StartVertexLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip VertexCount (input only) */
/* skip StartVertexLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_Draw(struct npt_ring *ring,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT VertexCount,
                                    UINT StartVertexLocation,
                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_Draw(VertexCount,StartVertexLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_Draw_reply(VertexCount,StartVertexLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_Draw(enc, cmd_flags, object_id, VertexCount, StartVertexLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_Draw(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   UINT VertexCount,
                                   UINT StartVertexLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Draw(ring, 0, object_id, VertexCount, StartVertexLocation, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_Draw(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  UINT VertexCount,
                                  UINT StartVertexLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Draw(ring, NPT_CMD_FLAG_REPLY, object_id, VertexCount, StartVertexLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::Map
 */

struct npt_command_ID3D11DeviceContext_Map {
    void *_self;
    ID3D11Resource * pResource;
    UINT Subresource;
    D3D11_MAP MapType;
    UINT MapFlags;
    D3D11_MAPPED_SUBRESOURCE * pMappedResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_Map(const ID3D11Resource * pResource,const UINT Subresource,const D3D11_MAP MapType,const UINT MapFlags,const D3D11_MAPPED_SUBRESOURCE * pMappedResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&Subresource, max_mode);
    cmd_size += npt_sizeof_D3D11_MAP(&MapType, max_mode);
    cmd_size += npt_sizeof_UINT(&MapFlags, max_mode);
/* skip pMappedResource (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_Map(struct npt_cs_encoder *enc,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   ID3D11Resource * pResource,
                                   UINT Subresource,
                                   D3D11_MAP MapType,
                                   UINT MapFlags,
                                   D3D11_MAPPED_SUBRESOURCE * pMappedResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_Map(pResource,Subresource,MapType,MapFlags,pMappedResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Map),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    npt_encode_UINT(enc, &Subresource);
    npt_encode_D3D11_MAP(enc, &MapType);
    npt_encode_UINT(enc, &MapFlags);
/* skip pMappedResource (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_Map_reply(const ID3D11Resource * pResource,const UINT Subresource,const D3D11_MAP MapType,const UINT MapFlags,const D3D11_MAPPED_SUBRESOURCE * pMappedResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_Map_reply(struct npt_cs_decoder *dec,
                                         D3D11_MAPPED_SUBRESOURCE * pMappedResource,
                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_Map))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_Map reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_Map));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pMappedResource)
            npt_decode_D3D11_MAPPED_SUBRESOURCE(dec, (D3D11_MAPPED_SUBRESOURCE *)pMappedResource);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_Map(struct npt_ring *ring,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   ID3D11Resource * pResource,
                                   UINT Subresource,
                                   D3D11_MAP MapType,
                                   UINT MapFlags,
                                   D3D11_MAPPED_SUBRESOURCE * pMappedResource,
                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_Map(pResource,Subresource,MapType,MapFlags,pMappedResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_Map_reply(pResource,Subresource,MapType,MapFlags,pMappedResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_Map(enc, cmd_flags, object_id, pResource, Subresource, MapType, MapFlags, pMappedResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_Map(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  ID3D11Resource * pResource,
                                  UINT Subresource,
                                  D3D11_MAP MapType,
                                  UINT MapFlags,
                                  D3D11_MAPPED_SUBRESOURCE * pMappedResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Map(ring, 0, object_id, pResource, Subresource, MapType, MapFlags, pMappedResource, &submit);
}

static inline HRESULT
npt_call_ID3D11DeviceContext_Map(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 ID3D11Resource * pResource,
                                 UINT Subresource,
                                 D3D11_MAP MapType,
                                 UINT MapFlags,
                                 D3D11_MAPPED_SUBRESOURCE * pMappedResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Map(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, Subresource, MapType, MapFlags, pMappedResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11DeviceContext_Map_reply(dec, pMappedResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11DeviceContext::Unmap
 */

struct npt_command_ID3D11DeviceContext_Unmap {
    void *_self;
    ID3D11Resource * pResource;
    UINT Subresource;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_Unmap(const ID3D11Resource * pResource,const UINT Subresource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&Subresource, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_Unmap(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     ID3D11Resource * pResource,
                                     UINT Subresource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_Unmap(pResource,Subresource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Unmap),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    npt_encode_UINT(enc, &Subresource);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_Unmap_reply(const ID3D11Resource * pResource,const UINT Subresource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip Subresource (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_Unmap(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     ID3D11Resource * pResource,
                                     UINT Subresource,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_Unmap(pResource,Subresource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_Unmap_reply(pResource,Subresource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_Unmap(enc, cmd_flags, object_id, pResource, Subresource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_Unmap(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    ID3D11Resource * pResource,
                                    UINT Subresource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Unmap(ring, 0, object_id, pResource, Subresource, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_Unmap(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   ID3D11Resource * pResource,
                                   UINT Subresource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Unmap(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, Subresource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::PSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_PSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSSetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_PSSetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_PSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSSetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSSetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_PSSetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_PSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_PSSetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_PSSetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_PSSetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSSetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_PSSetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSSetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::IASetInputLayout
 */

struct npt_command_ID3D11DeviceContext_IASetInputLayout {
    void *_self;
    ID3D11InputLayout * pInputLayout;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_IASetInputLayout(const ID3D11InputLayout * pInputLayout)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_IASetInputLayout(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11InputLayout * pInputLayout)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_IASetInputLayout(pInputLayout);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IASetInputLayout),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pInputLayout));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_IASetInputLayout_reply(const ID3D11InputLayout * pInputLayout)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pInputLayout (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_IASetInputLayout(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11InputLayout * pInputLayout,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_IASetInputLayout(pInputLayout);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_IASetInputLayout_reply(pInputLayout)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_IASetInputLayout(enc, cmd_flags, object_id, pInputLayout);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_IASetInputLayout(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11InputLayout * pInputLayout)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IASetInputLayout(ring, 0, object_id, pInputLayout, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_IASetInputLayout(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11InputLayout * pInputLayout)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IASetInputLayout(ring, NPT_CMD_FLAG_REPLY, object_id, pInputLayout, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::IASetVertexBuffers
 */

struct npt_command_ID3D11DeviceContext_IASetVertexBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppVertexBuffers;
    const UINT * pStrides;
    const UINT * pOffsets;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_IASetVertexBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppVertexBuffers,const UINT * pStrides,const UINT * pOffsets)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    cmd_size += npt_sizeof_array_count(pStrides ? NumBuffers : 0);
    if (pStrides)
        cmd_size += npt_sizeof_UINT_array(pStrides, NumBuffers);
    cmd_size += npt_sizeof_array_count(pOffsets ? NumBuffers : 0);
    if (pOffsets)
        cmd_size += npt_sizeof_UINT_array(pOffsets, NumBuffers);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_IASetVertexBuffers(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppVertexBuffers,
                                                  const UINT * pStrides,
                                                  const UINT * pOffsets)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_IASetVertexBuffers(StartSlot,NumBuffers,ppVertexBuffers,pStrides,pOffsets);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IASetVertexBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppVertexBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppVertexBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pStrides) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pStrides, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pOffsets) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pOffsets, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_IASetVertexBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppVertexBuffers,const UINT * pStrides,const UINT * pOffsets)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppVertexBuffers (input only) */
/* skip pStrides (input only) */
/* skip pOffsets (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_IASetVertexBuffers(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppVertexBuffers,
                                                  const UINT * pStrides,
                                                  const UINT * pOffsets,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_IASetVertexBuffers(StartSlot,NumBuffers,ppVertexBuffers,pStrides,pOffsets);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_IASetVertexBuffers_reply(StartSlot,NumBuffers,ppVertexBuffers,pStrides,pOffsets)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_IASetVertexBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppVertexBuffers, pStrides, pOffsets);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_IASetVertexBuffers(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT StartSlot,
                                                 UINT NumBuffers,
                                                 ID3D11Buffer ** ppVertexBuffers,
                                                 const UINT * pStrides,
                                                 const UINT * pOffsets)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IASetVertexBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppVertexBuffers, pStrides, pOffsets, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_IASetVertexBuffers(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT StartSlot,
                                                UINT NumBuffers,
                                                ID3D11Buffer ** ppVertexBuffers,
                                                const UINT * pStrides,
                                                const UINT * pOffsets)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IASetVertexBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppVertexBuffers, pStrides, pOffsets, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::IASetIndexBuffer
 */

struct npt_command_ID3D11DeviceContext_IASetIndexBuffer {
    void *_self;
    ID3D11Buffer * pIndexBuffer;
    DXGI_FORMAT Format;
    UINT Offset;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_IASetIndexBuffer(const ID3D11Buffer * pIndexBuffer,const DXGI_FORMAT Format,const UINT Offset)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
    cmd_size += npt_sizeof_UINT(&Offset, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_IASetIndexBuffer(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11Buffer * pIndexBuffer,
                                                DXGI_FORMAT Format,
                                                UINT Offset)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_IASetIndexBuffer(pIndexBuffer,Format,Offset);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IASetIndexBuffer),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pIndexBuffer));
    npt_encode_DXGI_FORMAT(enc, &Format);
    npt_encode_UINT(enc, &Offset);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_IASetIndexBuffer_reply(const ID3D11Buffer * pIndexBuffer,const DXGI_FORMAT Format,const UINT Offset)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pIndexBuffer (input only) */
/* skip Format (input only) */
/* skip Offset (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_IASetIndexBuffer(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11Buffer * pIndexBuffer,
                                                DXGI_FORMAT Format,
                                                UINT Offset,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_IASetIndexBuffer(pIndexBuffer,Format,Offset);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_IASetIndexBuffer_reply(pIndexBuffer,Format,Offset)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_IASetIndexBuffer(enc, cmd_flags, object_id, pIndexBuffer, Format, Offset);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_IASetIndexBuffer(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Buffer * pIndexBuffer,
                                               DXGI_FORMAT Format,
                                               UINT Offset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IASetIndexBuffer(ring, 0, object_id, pIndexBuffer, Format, Offset, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_IASetIndexBuffer(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11Buffer * pIndexBuffer,
                                              DXGI_FORMAT Format,
                                              UINT Offset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IASetIndexBuffer(ring, NPT_CMD_FLAG_REPLY, object_id, pIndexBuffer, Format, Offset, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DrawIndexedInstanced
 */

struct npt_command_ID3D11DeviceContext_DrawIndexedInstanced {
    void *_self;
    UINT IndexCountPerInstance;
    UINT InstanceCount;
    UINT StartIndexLocation;
    INT BaseVertexLocation;
    UINT StartInstanceLocation;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawIndexedInstanced(const UINT IndexCountPerInstance,const UINT InstanceCount,const UINT StartIndexLocation,const INT BaseVertexLocation,const UINT StartInstanceLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&IndexCountPerInstance, max_mode);
    cmd_size += npt_sizeof_UINT(&InstanceCount, max_mode);
    cmd_size += npt_sizeof_UINT(&StartIndexLocation, max_mode);
    cmd_size += npt_sizeof_INT(&BaseVertexLocation, max_mode);
    cmd_size += npt_sizeof_UINT(&StartInstanceLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DrawIndexedInstanced(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT IndexCountPerInstance,
                                                    UINT InstanceCount,
                                                    UINT StartIndexLocation,
                                                    INT BaseVertexLocation,
                                                    UINT StartInstanceLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DrawIndexedInstanced(IndexCountPerInstance,InstanceCount,StartIndexLocation,BaseVertexLocation,StartInstanceLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawIndexedInstanced),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &IndexCountPerInstance);
    npt_encode_UINT(enc, &InstanceCount);
    npt_encode_UINT(enc, &StartIndexLocation);
    npt_encode_INT(enc, &BaseVertexLocation);
    npt_encode_UINT(enc, &StartInstanceLocation);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawIndexedInstanced_reply(const UINT IndexCountPerInstance,const UINT InstanceCount,const UINT StartIndexLocation,const INT BaseVertexLocation,const UINT StartInstanceLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip IndexCountPerInstance (input only) */
/* skip InstanceCount (input only) */
/* skip StartIndexLocation (input only) */
/* skip BaseVertexLocation (input only) */
/* skip StartInstanceLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DrawIndexedInstanced(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT IndexCountPerInstance,
                                                    UINT InstanceCount,
                                                    UINT StartIndexLocation,
                                                    INT BaseVertexLocation,
                                                    UINT StartInstanceLocation,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DrawIndexedInstanced(IndexCountPerInstance,InstanceCount,StartIndexLocation,BaseVertexLocation,StartInstanceLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DrawIndexedInstanced_reply(IndexCountPerInstance,InstanceCount,StartIndexLocation,BaseVertexLocation,StartInstanceLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DrawIndexedInstanced(enc, cmd_flags, object_id, IndexCountPerInstance, InstanceCount, StartIndexLocation, BaseVertexLocation, StartInstanceLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DrawIndexedInstanced(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT IndexCountPerInstance,
                                                   UINT InstanceCount,
                                                   UINT StartIndexLocation,
                                                   INT BaseVertexLocation,
                                                   UINT StartInstanceLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawIndexedInstanced(ring, 0, object_id, IndexCountPerInstance, InstanceCount, StartIndexLocation, BaseVertexLocation, StartInstanceLocation, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DrawIndexedInstanced(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT IndexCountPerInstance,
                                                  UINT InstanceCount,
                                                  UINT StartIndexLocation,
                                                  INT BaseVertexLocation,
                                                  UINT StartInstanceLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawIndexedInstanced(ring, NPT_CMD_FLAG_REPLY, object_id, IndexCountPerInstance, InstanceCount, StartIndexLocation, BaseVertexLocation, StartInstanceLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DrawInstanced
 */

struct npt_command_ID3D11DeviceContext_DrawInstanced {
    void *_self;
    UINT VertexCountPerInstance;
    UINT InstanceCount;
    UINT StartVertexLocation;
    UINT StartInstanceLocation;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawInstanced(const UINT VertexCountPerInstance,const UINT InstanceCount,const UINT StartVertexLocation,const UINT StartInstanceLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&VertexCountPerInstance, max_mode);
    cmd_size += npt_sizeof_UINT(&InstanceCount, max_mode);
    cmd_size += npt_sizeof_UINT(&StartVertexLocation, max_mode);
    cmd_size += npt_sizeof_UINT(&StartInstanceLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DrawInstanced(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT VertexCountPerInstance,
                                             UINT InstanceCount,
                                             UINT StartVertexLocation,
                                             UINT StartInstanceLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DrawInstanced(VertexCountPerInstance,InstanceCount,StartVertexLocation,StartInstanceLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawInstanced),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &VertexCountPerInstance);
    npt_encode_UINT(enc, &InstanceCount);
    npt_encode_UINT(enc, &StartVertexLocation);
    npt_encode_UINT(enc, &StartInstanceLocation);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawInstanced_reply(const UINT VertexCountPerInstance,const UINT InstanceCount,const UINT StartVertexLocation,const UINT StartInstanceLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip VertexCountPerInstance (input only) */
/* skip InstanceCount (input only) */
/* skip StartVertexLocation (input only) */
/* skip StartInstanceLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DrawInstanced(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT VertexCountPerInstance,
                                             UINT InstanceCount,
                                             UINT StartVertexLocation,
                                             UINT StartInstanceLocation,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DrawInstanced(VertexCountPerInstance,InstanceCount,StartVertexLocation,StartInstanceLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DrawInstanced_reply(VertexCountPerInstance,InstanceCount,StartVertexLocation,StartInstanceLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DrawInstanced(enc, cmd_flags, object_id, VertexCountPerInstance, InstanceCount, StartVertexLocation, StartInstanceLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DrawInstanced(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT VertexCountPerInstance,
                                            UINT InstanceCount,
                                            UINT StartVertexLocation,
                                            UINT StartInstanceLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawInstanced(ring, 0, object_id, VertexCountPerInstance, InstanceCount, StartVertexLocation, StartInstanceLocation, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DrawInstanced(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT VertexCountPerInstance,
                                           UINT InstanceCount,
                                           UINT StartVertexLocation,
                                           UINT StartInstanceLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawInstanced(ring, NPT_CMD_FLAG_REPLY, object_id, VertexCountPerInstance, InstanceCount, StartVertexLocation, StartInstanceLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_GSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSSetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GSSetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSSetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSSetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_GSSetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GSSetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GSSetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GSSetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSSetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GSSetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSSetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GSSetShader
 */

struct npt_command_ID3D11DeviceContext_GSSetShader {
    void *_self;
    ID3D11GeometryShader * pShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSSetShader(const ID3D11GeometryShader * pShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(NumClassInstances);
    cmd_size += sizeof(uint64_t) * NumClassInstances;
    cmd_size += npt_sizeof_UINT(&NumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GSSetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11GeometryShader * pShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GSSetShader(pShader,ppClassInstances,NumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSSetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pShader));
    if (ppClassInstances) {
        npt_encode_array_count(enc, NumClassInstances);
        for (uint32_t _i = 0; _i < (uint32_t)NumClassInstances; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppClassInstances[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSSetShader_reply(const ID3D11GeometryShader * pShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pShader (input only) */
/* skip ppClassInstances (input only) */
/* skip NumClassInstances (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_GSSetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11GeometryShader * pShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GSSetShader(pShader,ppClassInstances,NumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GSSetShader_reply(pShader,ppClassInstances,NumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GSSetShader(enc, cmd_flags, object_id, pShader, ppClassInstances, NumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GSSetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11GeometryShader * pShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSSetShader(ring, 0, object_id, pShader, ppClassInstances, NumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GSSetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11GeometryShader * pShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSSetShader(ring, NPT_CMD_FLAG_REPLY, object_id, pShader, ppClassInstances, NumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::IASetPrimitiveTopology
 */

struct npt_command_ID3D11DeviceContext_IASetPrimitiveTopology {
    void *_self;
    D3D11_PRIMITIVE_TOPOLOGY Topology;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_IASetPrimitiveTopology(const D3D11_PRIMITIVE_TOPOLOGY Topology)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D11_PRIMITIVE_TOPOLOGY(&Topology, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_IASetPrimitiveTopology(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      D3D11_PRIMITIVE_TOPOLOGY Topology)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_IASetPrimitiveTopology(Topology);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IASetPrimitiveTopology),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D11_PRIMITIVE_TOPOLOGY(enc, &Topology);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_IASetPrimitiveTopology_reply(const D3D11_PRIMITIVE_TOPOLOGY Topology)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Topology (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_IASetPrimitiveTopology(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      D3D11_PRIMITIVE_TOPOLOGY Topology,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_IASetPrimitiveTopology(Topology);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_IASetPrimitiveTopology_reply(Topology)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_IASetPrimitiveTopology(enc, cmd_flags, object_id, Topology);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_IASetPrimitiveTopology(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     D3D11_PRIMITIVE_TOPOLOGY Topology)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IASetPrimitiveTopology(ring, 0, object_id, Topology, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_IASetPrimitiveTopology(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    D3D11_PRIMITIVE_TOPOLOGY Topology)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IASetPrimitiveTopology(ring, NPT_CMD_FLAG_REPLY, object_id, Topology, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::VSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_VSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSSetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
    cmd_size += npt_sizeof_array_count(NumViews);
    cmd_size += sizeof(uint64_t) * NumViews;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_VSSetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_VSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSSetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
    if (ppShaderResourceViews) {
        npt_encode_array_count(enc, NumViews);
        for (uint32_t _i = 0; _i < (uint32_t)NumViews; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppShaderResourceViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSSetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_VSSetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_VSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_VSSetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_VSSetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_VSSetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSSetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_VSSetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSSetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::VSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_VSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSSetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
    cmd_size += npt_sizeof_array_count(NumSamplers);
    cmd_size += sizeof(uint64_t) * NumSamplers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_VSSetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_VSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSSetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
    if (ppSamplers) {
        npt_encode_array_count(enc, NumSamplers);
        for (uint32_t _i = 0; _i < (uint32_t)NumSamplers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppSamplers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSSetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_VSSetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_VSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_VSSetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_VSSetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_VSSetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSSetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_VSSetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSSetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::Begin
 */

struct npt_command_ID3D11DeviceContext_Begin {
    void *_self;
    ID3D11Asynchronous * pAsync;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_Begin(const ID3D11Asynchronous * pAsync)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_Begin(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     ID3D11Asynchronous * pAsync)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_Begin(pAsync);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Begin),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pAsync));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_Begin_reply(const ID3D11Asynchronous * pAsync)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAsync (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_Begin(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     ID3D11Asynchronous * pAsync,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_Begin(pAsync);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_Begin_reply(pAsync)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_Begin(enc, cmd_flags, object_id, pAsync);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_Begin(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    ID3D11Asynchronous * pAsync)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Begin(ring, 0, object_id, pAsync, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_Begin(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   ID3D11Asynchronous * pAsync)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Begin(ring, NPT_CMD_FLAG_REPLY, object_id, pAsync, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::End
 */

struct npt_command_ID3D11DeviceContext_End {
    void *_self;
    ID3D11Asynchronous * pAsync;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_End(const ID3D11Asynchronous * pAsync)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_End(struct npt_cs_encoder *enc,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   ID3D11Asynchronous * pAsync)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_End(pAsync);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_End),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pAsync));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_End_reply(const ID3D11Asynchronous * pAsync)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAsync (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_End(struct npt_ring *ring,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   ID3D11Asynchronous * pAsync,
                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_End(pAsync);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_End_reply(pAsync)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_End(enc, cmd_flags, object_id, pAsync);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_End(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  ID3D11Asynchronous * pAsync)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_End(ring, 0, object_id, pAsync, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_End(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 ID3D11Asynchronous * pAsync)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_End(ring, NPT_CMD_FLAG_REPLY, object_id, pAsync, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GetData
 */

struct npt_command_ID3D11DeviceContext_GetData {
    void *_self;
    ID3D11Asynchronous * pAsync;
    void * pData;
    UINT DataSize;
    UINT GetDataFlags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetData(const ID3D11Asynchronous * pAsync,const void * pData,const UINT DataSize,const UINT GetDataFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pData (output only) */
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
    cmd_size += npt_sizeof_UINT(&GetDataFlags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GetData(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       ID3D11Asynchronous * pAsync,
                                       void * pData,
                                       UINT DataSize,
                                       UINT GetDataFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GetData(pAsync,pData,DataSize,GetDataFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pAsync));
/* skip pData (output only) */
    npt_encode_UINT(enc, &DataSize);
    npt_encode_UINT(enc, &GetDataFlags);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetData_reply(const ID3D11Asynchronous * pAsync,const void * pData,const UINT DataSize,const UINT GetDataFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAsync (input only) */
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip DataSize (input only) */
/* skip GetDataFlags (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_GetData_reply(struct npt_cs_decoder *dec,
                                             void * pData,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_GetData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_GetData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_GetData));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pData)
            npt_decode_blob_array(dec, (void *)pData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_GetData(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       ID3D11Asynchronous * pAsync,
                                       void * pData,
                                       UINT DataSize,
                                       UINT GetDataFlags,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GetData(pAsync,pData,DataSize,GetDataFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GetData_reply(pAsync,pData,DataSize,GetDataFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GetData(enc, cmd_flags, object_id, pAsync, pData, DataSize, GetDataFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GetData(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      ID3D11Asynchronous * pAsync,
                                      void * pData,
                                      UINT DataSize,
                                      UINT GetDataFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetData(ring, 0, object_id, pAsync, pData, DataSize, GetDataFlags, &submit);
}

static inline HRESULT
npt_call_ID3D11DeviceContext_GetData(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     ID3D11Asynchronous * pAsync,
                                     void * pData,
                                     UINT DataSize,
                                     UINT GetDataFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetData(ring, NPT_CMD_FLAG_REPLY, object_id, pAsync, pData, DataSize, GetDataFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11DeviceContext_GetData_reply(dec, pData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11DeviceContext::SetPredication
 */

struct npt_command_ID3D11DeviceContext_SetPredication {
    void *_self;
    ID3D11Predicate * pPredicate;
    BOOL PredicateValue;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_SetPredication(const ID3D11Predicate * pPredicate,const BOOL PredicateValue)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_BOOL(&PredicateValue, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_SetPredication(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11Predicate * pPredicate,
                                              BOOL PredicateValue)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_SetPredication(pPredicate,PredicateValue);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_SetPredication),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pPredicate));
    npt_encode_BOOL(enc, &PredicateValue);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_SetPredication_reply(const ID3D11Predicate * pPredicate,const BOOL PredicateValue)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pPredicate (input only) */
/* skip PredicateValue (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_SetPredication(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11Predicate * pPredicate,
                                              BOOL PredicateValue,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_SetPredication(pPredicate,PredicateValue);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_SetPredication_reply(pPredicate,PredicateValue)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_SetPredication(enc, cmd_flags, object_id, pPredicate, PredicateValue);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_SetPredication(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11Predicate * pPredicate,
                                             BOOL PredicateValue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_SetPredication(ring, 0, object_id, pPredicate, PredicateValue, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_SetPredication(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            ID3D11Predicate * pPredicate,
                                            BOOL PredicateValue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_SetPredication(ring, NPT_CMD_FLAG_REPLY, object_id, pPredicate, PredicateValue, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_GSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSSetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
    cmd_size += npt_sizeof_array_count(NumViews);
    cmd_size += sizeof(uint64_t) * NumViews;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GSSetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSSetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
    if (ppShaderResourceViews) {
        npt_encode_array_count(enc, NumViews);
        for (uint32_t _i = 0; _i < (uint32_t)NumViews; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppShaderResourceViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSSetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_GSSetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GSSetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GSSetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GSSetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSSetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GSSetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSSetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_GSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSSetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
    cmd_size += npt_sizeof_array_count(NumSamplers);
    cmd_size += sizeof(uint64_t) * NumSamplers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GSSetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSSetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
    if (ppSamplers) {
        npt_encode_array_count(enc, NumSamplers);
        for (uint32_t _i = 0; _i < (uint32_t)NumSamplers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppSamplers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSSetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_GSSetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GSSetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GSSetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GSSetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSSetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GSSetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSSetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::OMSetRenderTargets
 */

struct npt_command_ID3D11DeviceContext_OMSetRenderTargets {
    void *_self;
    UINT NumViews;
    ID3D11RenderTargetView ** ppRenderTargetViews;
    ID3D11DepthStencilView * pDepthStencilView;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMSetRenderTargets(const UINT NumViews,ID3D11RenderTargetView ** ppRenderTargetViews,const ID3D11DepthStencilView * pDepthStencilView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
    cmd_size += npt_sizeof_array_count(NumViews);
    cmd_size += sizeof(uint64_t) * NumViews;
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_OMSetRenderTargets(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT NumViews,
                                                  ID3D11RenderTargetView ** ppRenderTargetViews,
                                                  ID3D11DepthStencilView * pDepthStencilView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_OMSetRenderTargets(NumViews,ppRenderTargetViews,pDepthStencilView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMSetRenderTargets),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumViews);
    if (ppRenderTargetViews) {
        npt_encode_array_count(enc, NumViews);
        for (uint32_t _i = 0; _i < (uint32_t)NumViews; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppRenderTargetViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_com_handle(enc, npt_object_get_id(pDepthStencilView));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMSetRenderTargets_reply(const UINT NumViews,ID3D11RenderTargetView ** ppRenderTargetViews,const ID3D11DepthStencilView * pDepthStencilView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumViews (input only) */
/* skip ppRenderTargetViews (input only) */
/* skip pDepthStencilView (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_OMSetRenderTargets(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT NumViews,
                                                  ID3D11RenderTargetView ** ppRenderTargetViews,
                                                  ID3D11DepthStencilView * pDepthStencilView,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_OMSetRenderTargets(NumViews,ppRenderTargetViews,pDepthStencilView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_OMSetRenderTargets_reply(NumViews,ppRenderTargetViews,pDepthStencilView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_OMSetRenderTargets(enc, cmd_flags, object_id, NumViews, ppRenderTargetViews, pDepthStencilView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_OMSetRenderTargets(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT NumViews,
                                                 ID3D11RenderTargetView ** ppRenderTargetViews,
                                                 ID3D11DepthStencilView * pDepthStencilView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMSetRenderTargets(ring, 0, object_id, NumViews, ppRenderTargetViews, pDepthStencilView, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_OMSetRenderTargets(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT NumViews,
                                                ID3D11RenderTargetView ** ppRenderTargetViews,
                                                ID3D11DepthStencilView * pDepthStencilView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMSetRenderTargets(ring, NPT_CMD_FLAG_REPLY, object_id, NumViews, ppRenderTargetViews, pDepthStencilView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::OMSetRenderTargetsAndUnorderedAccessViews
 */

struct npt_command_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews {
    void *_self;
    UINT NumRTVs;
    ID3D11RenderTargetView ** ppRenderTargetViews;
    ID3D11DepthStencilView * pDepthStencilView;
    UINT UAVStartSlot;
    UINT NumUAVs;
    ID3D11UnorderedAccessView ** ppUnorderedAccessViews;
    const UINT * pUAVInitialCounts;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(const UINT NumRTVs,ID3D11RenderTargetView ** ppRenderTargetViews,const ID3D11DepthStencilView * pDepthStencilView,const UINT UAVStartSlot,const UINT NumUAVs,ID3D11UnorderedAccessView ** ppUnorderedAccessViews,const UINT * pUAVInitialCounts)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumRTVs, max_mode);
    cmd_size += npt_sizeof_array_count(NumRTVs);
    cmd_size += sizeof(uint64_t) * NumRTVs;
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&UAVStartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumUAVs, max_mode);
    cmd_size += npt_sizeof_array_count(NumUAVs);
    cmd_size += sizeof(uint64_t) * NumUAVs;
    cmd_size += npt_sizeof_array_count(pUAVInitialCounts ? NumUAVs : 0);
    if (pUAVInitialCounts)
        cmd_size += npt_sizeof_UINT_array(pUAVInitialCounts, NumUAVs);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(struct npt_cs_encoder *enc,
                                                                         uint32_t cmd_flags,
                                                                         npt_object_id object_id,
                                                                         UINT NumRTVs,
                                                                         ID3D11RenderTargetView ** ppRenderTargetViews,
                                                                         ID3D11DepthStencilView * pDepthStencilView,
                                                                         UINT UAVStartSlot,
                                                                         UINT NumUAVs,
                                                                         ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                                         const UINT * pUAVInitialCounts)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(NumRTVs,ppRenderTargetViews,pDepthStencilView,UAVStartSlot,NumUAVs,ppUnorderedAccessViews,pUAVInitialCounts);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumRTVs);
    if (ppRenderTargetViews) {
        npt_encode_array_count(enc, NumRTVs);
        for (uint32_t _i = 0; _i < (uint32_t)NumRTVs; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppRenderTargetViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_com_handle(enc, npt_object_get_id(pDepthStencilView));
    npt_encode_UINT(enc, &UAVStartSlot);
    npt_encode_UINT(enc, &NumUAVs);
    if (ppUnorderedAccessViews) {
        npt_encode_array_count(enc, NumUAVs);
        for (uint32_t _i = 0; _i < (uint32_t)NumUAVs; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppUnorderedAccessViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pUAVInitialCounts) {
        npt_encode_array_count(enc, NumUAVs);
        npt_encode_UINT_array(enc, pUAVInitialCounts, NumUAVs);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews_reply(const UINT NumRTVs,ID3D11RenderTargetView ** ppRenderTargetViews,const ID3D11DepthStencilView * pDepthStencilView,const UINT UAVStartSlot,const UINT NumUAVs,ID3D11UnorderedAccessView ** ppUnorderedAccessViews,const UINT * pUAVInitialCounts)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumRTVs (input only) */
/* skip ppRenderTargetViews (input only) */
/* skip pDepthStencilView (input only) */
/* skip UAVStartSlot (input only) */
/* skip NumUAVs (input only) */
/* skip ppUnorderedAccessViews (input only) */
/* skip pUAVInitialCounts (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(struct npt_ring *ring,
                                                                         uint32_t cmd_flags,
                                                                         npt_object_id object_id,
                                                                         UINT NumRTVs,
                                                                         ID3D11RenderTargetView ** ppRenderTargetViews,
                                                                         ID3D11DepthStencilView * pDepthStencilView,
                                                                         UINT UAVStartSlot,
                                                                         UINT NumUAVs,
                                                                         ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                                         const UINT * pUAVInitialCounts,
                                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(NumRTVs,ppRenderTargetViews,pDepthStencilView,UAVStartSlot,NumUAVs,ppUnorderedAccessViews,pUAVInitialCounts);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews_reply(NumRTVs,ppRenderTargetViews,pDepthStencilView,UAVStartSlot,NumUAVs,ppUnorderedAccessViews,pUAVInitialCounts)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(enc, cmd_flags, object_id, NumRTVs, ppRenderTargetViews, pDepthStencilView, UAVStartSlot, NumUAVs, ppUnorderedAccessViews, pUAVInitialCounts);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(struct npt_ring *ring,
                                                                        npt_object_id object_id,
                                                                        UINT NumRTVs,
                                                                        ID3D11RenderTargetView ** ppRenderTargetViews,
                                                                        ID3D11DepthStencilView * pDepthStencilView,
                                                                        UINT UAVStartSlot,
                                                                        UINT NumUAVs,
                                                                        ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                                        const UINT * pUAVInitialCounts)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(ring, 0, object_id, NumRTVs, ppRenderTargetViews, pDepthStencilView, UAVStartSlot, NumUAVs, ppUnorderedAccessViews, pUAVInitialCounts, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(struct npt_ring *ring,
                                                                       npt_object_id object_id,
                                                                       UINT NumRTVs,
                                                                       ID3D11RenderTargetView ** ppRenderTargetViews,
                                                                       ID3D11DepthStencilView * pDepthStencilView,
                                                                       UINT UAVStartSlot,
                                                                       UINT NumUAVs,
                                                                       ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                                       const UINT * pUAVInitialCounts)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(ring, NPT_CMD_FLAG_REPLY, object_id, NumRTVs, ppRenderTargetViews, pDepthStencilView, UAVStartSlot, NumUAVs, ppUnorderedAccessViews, pUAVInitialCounts, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::OMSetBlendState
 */

struct npt_command_ID3D11DeviceContext_OMSetBlendState {
    void *_self;
    ID3D11BlendState * pBlendState;
    const FLOAT * BlendFactor;
    UINT SampleMask;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMSetBlendState(const ID3D11BlendState * pBlendState,const FLOAT * BlendFactor,const UINT SampleMask)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(BlendFactor ? 4 : 0);
    if (BlendFactor)
        cmd_size += npt_sizeof_FLOAT_array((const FLOAT *)BlendFactor, 4);
    cmd_size += npt_sizeof_UINT(&SampleMask, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_OMSetBlendState(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11BlendState * pBlendState,
                                               const FLOAT * BlendFactor,
                                               UINT SampleMask)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_OMSetBlendState(pBlendState,BlendFactor,SampleMask);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMSetBlendState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pBlendState));
    if (BlendFactor) {
        npt_encode_array_count(enc, 4);
        npt_encode_FLOAT_array(enc, (const FLOAT *)BlendFactor, 4);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &SampleMask);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMSetBlendState_reply(const ID3D11BlendState * pBlendState,const FLOAT * BlendFactor,const UINT SampleMask)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pBlendState (input only) */
/* skip BlendFactor (input only) */
/* skip SampleMask (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_OMSetBlendState(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11BlendState * pBlendState,
                                               const FLOAT * BlendFactor,
                                               UINT SampleMask,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_OMSetBlendState(pBlendState,BlendFactor,SampleMask);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_OMSetBlendState_reply(pBlendState,BlendFactor,SampleMask)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_OMSetBlendState(enc, cmd_flags, object_id, pBlendState, BlendFactor, SampleMask);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_OMSetBlendState(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11BlendState * pBlendState,
                                              const FLOAT * BlendFactor,
                                              UINT SampleMask)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMSetBlendState(ring, 0, object_id, pBlendState, BlendFactor, SampleMask, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_OMSetBlendState(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11BlendState * pBlendState,
                                             const FLOAT * BlendFactor,
                                             UINT SampleMask)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMSetBlendState(ring, NPT_CMD_FLAG_REPLY, object_id, pBlendState, BlendFactor, SampleMask, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::OMSetDepthStencilState
 */

struct npt_command_ID3D11DeviceContext_OMSetDepthStencilState {
    void *_self;
    ID3D11DepthStencilState * pDepthStencilState;
    UINT StencilRef;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMSetDepthStencilState(const ID3D11DepthStencilState * pDepthStencilState,const UINT StencilRef)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StencilRef, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_OMSetDepthStencilState(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11DepthStencilState * pDepthStencilState,
                                                      UINT StencilRef)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_OMSetDepthStencilState(pDepthStencilState,StencilRef);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMSetDepthStencilState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDepthStencilState));
    npt_encode_UINT(enc, &StencilRef);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMSetDepthStencilState_reply(const ID3D11DepthStencilState * pDepthStencilState,const UINT StencilRef)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDepthStencilState (input only) */
/* skip StencilRef (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_OMSetDepthStencilState(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11DepthStencilState * pDepthStencilState,
                                                      UINT StencilRef,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_OMSetDepthStencilState(pDepthStencilState,StencilRef);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_OMSetDepthStencilState_reply(pDepthStencilState,StencilRef)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_OMSetDepthStencilState(enc, cmd_flags, object_id, pDepthStencilState, StencilRef);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_OMSetDepthStencilState(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3D11DepthStencilState * pDepthStencilState,
                                                     UINT StencilRef)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMSetDepthStencilState(ring, 0, object_id, pDepthStencilState, StencilRef, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_OMSetDepthStencilState(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11DepthStencilState * pDepthStencilState,
                                                    UINT StencilRef)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMSetDepthStencilState(ring, NPT_CMD_FLAG_REPLY, object_id, pDepthStencilState, StencilRef, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::SOSetTargets
 */

struct npt_command_ID3D11DeviceContext_SOSetTargets {
    void *_self;
    UINT NumBuffers;
    ID3D11Buffer ** ppSOTargets;
    const UINT * pOffsets;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_SOSetTargets(const UINT NumBuffers,ID3D11Buffer ** ppSOTargets,const UINT * pOffsets)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    cmd_size += npt_sizeof_array_count(pOffsets ? NumBuffers : 0);
    if (pOffsets)
        cmd_size += npt_sizeof_UINT_array(pOffsets, NumBuffers);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_SOSetTargets(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT NumBuffers,
                                            ID3D11Buffer ** ppSOTargets,
                                            const UINT * pOffsets)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_SOSetTargets(NumBuffers,ppSOTargets,pOffsets);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_SOSetTargets),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumBuffers);
    if (ppSOTargets) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppSOTargets[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pOffsets) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pOffsets, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_SOSetTargets_reply(const UINT NumBuffers,ID3D11Buffer ** ppSOTargets,const UINT * pOffsets)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumBuffers (input only) */
/* skip ppSOTargets (input only) */
/* skip pOffsets (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_SOSetTargets(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT NumBuffers,
                                            ID3D11Buffer ** ppSOTargets,
                                            const UINT * pOffsets,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_SOSetTargets(NumBuffers,ppSOTargets,pOffsets);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_SOSetTargets_reply(NumBuffers,ppSOTargets,pOffsets)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_SOSetTargets(enc, cmd_flags, object_id, NumBuffers, ppSOTargets, pOffsets);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_SOSetTargets(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT NumBuffers,
                                           ID3D11Buffer ** ppSOTargets,
                                           const UINT * pOffsets)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_SOSetTargets(ring, 0, object_id, NumBuffers, ppSOTargets, pOffsets, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_SOSetTargets(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          UINT NumBuffers,
                                          ID3D11Buffer ** ppSOTargets,
                                          const UINT * pOffsets)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_SOSetTargets(ring, NPT_CMD_FLAG_REPLY, object_id, NumBuffers, ppSOTargets, pOffsets, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DrawAuto
 */

struct npt_command_ID3D11DeviceContext_DrawAuto {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawAuto(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DrawAuto(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DrawAuto();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawAuto),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawAuto_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DrawAuto(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DrawAuto();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DrawAuto_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DrawAuto(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DrawAuto(struct npt_ring *ring,
                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawAuto(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DrawAuto(struct npt_ring *ring,
                                      npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawAuto(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DrawIndexedInstancedIndirect
 */

struct npt_command_ID3D11DeviceContext_DrawIndexedInstancedIndirect {
    void *_self;
    ID3D11Buffer * pBufferForArgs;
    UINT AlignedByteOffsetForArgs;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawIndexedInstancedIndirect(const ID3D11Buffer * pBufferForArgs,const UINT AlignedByteOffsetForArgs)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&AlignedByteOffsetForArgs, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DrawIndexedInstancedIndirect(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11Buffer * pBufferForArgs,
                                                            UINT AlignedByteOffsetForArgs)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DrawIndexedInstancedIndirect(pBufferForArgs,AlignedByteOffsetForArgs);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawIndexedInstancedIndirect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pBufferForArgs));
    npt_encode_UINT(enc, &AlignedByteOffsetForArgs);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawIndexedInstancedIndirect_reply(const ID3D11Buffer * pBufferForArgs,const UINT AlignedByteOffsetForArgs)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pBufferForArgs (input only) */
/* skip AlignedByteOffsetForArgs (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DrawIndexedInstancedIndirect(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11Buffer * pBufferForArgs,
                                                            UINT AlignedByteOffsetForArgs,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DrawIndexedInstancedIndirect(pBufferForArgs,AlignedByteOffsetForArgs);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DrawIndexedInstancedIndirect_reply(pBufferForArgs,AlignedByteOffsetForArgs)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DrawIndexedInstancedIndirect(enc, cmd_flags, object_id, pBufferForArgs, AlignedByteOffsetForArgs);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DrawIndexedInstancedIndirect(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11Buffer * pBufferForArgs,
                                                           UINT AlignedByteOffsetForArgs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawIndexedInstancedIndirect(ring, 0, object_id, pBufferForArgs, AlignedByteOffsetForArgs, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DrawIndexedInstancedIndirect(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D11Buffer * pBufferForArgs,
                                                          UINT AlignedByteOffsetForArgs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawIndexedInstancedIndirect(ring, NPT_CMD_FLAG_REPLY, object_id, pBufferForArgs, AlignedByteOffsetForArgs, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DrawInstancedIndirect
 */

struct npt_command_ID3D11DeviceContext_DrawInstancedIndirect {
    void *_self;
    ID3D11Buffer * pBufferForArgs;
    UINT AlignedByteOffsetForArgs;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawInstancedIndirect(const ID3D11Buffer * pBufferForArgs,const UINT AlignedByteOffsetForArgs)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&AlignedByteOffsetForArgs, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DrawInstancedIndirect(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11Buffer * pBufferForArgs,
                                                     UINT AlignedByteOffsetForArgs)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DrawInstancedIndirect(pBufferForArgs,AlignedByteOffsetForArgs);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawInstancedIndirect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pBufferForArgs));
    npt_encode_UINT(enc, &AlignedByteOffsetForArgs);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DrawInstancedIndirect_reply(const ID3D11Buffer * pBufferForArgs,const UINT AlignedByteOffsetForArgs)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pBufferForArgs (input only) */
/* skip AlignedByteOffsetForArgs (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DrawInstancedIndirect(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11Buffer * pBufferForArgs,
                                                     UINT AlignedByteOffsetForArgs,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DrawInstancedIndirect(pBufferForArgs,AlignedByteOffsetForArgs);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DrawInstancedIndirect_reply(pBufferForArgs,AlignedByteOffsetForArgs)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DrawInstancedIndirect(enc, cmd_flags, object_id, pBufferForArgs, AlignedByteOffsetForArgs);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DrawInstancedIndirect(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11Buffer * pBufferForArgs,
                                                    UINT AlignedByteOffsetForArgs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawInstancedIndirect(ring, 0, object_id, pBufferForArgs, AlignedByteOffsetForArgs, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DrawInstancedIndirect(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11Buffer * pBufferForArgs,
                                                   UINT AlignedByteOffsetForArgs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DrawInstancedIndirect(ring, NPT_CMD_FLAG_REPLY, object_id, pBufferForArgs, AlignedByteOffsetForArgs, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::Dispatch
 */

struct npt_command_ID3D11DeviceContext_Dispatch {
    void *_self;
    UINT ThreadGroupCountX;
    UINT ThreadGroupCountY;
    UINT ThreadGroupCountZ;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_Dispatch(const UINT ThreadGroupCountX,const UINT ThreadGroupCountY,const UINT ThreadGroupCountZ)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&ThreadGroupCountX, max_mode);
    cmd_size += npt_sizeof_UINT(&ThreadGroupCountY, max_mode);
    cmd_size += npt_sizeof_UINT(&ThreadGroupCountZ, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_Dispatch(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        UINT ThreadGroupCountX,
                                        UINT ThreadGroupCountY,
                                        UINT ThreadGroupCountZ)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_Dispatch(ThreadGroupCountX,ThreadGroupCountY,ThreadGroupCountZ);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Dispatch),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &ThreadGroupCountX);
    npt_encode_UINT(enc, &ThreadGroupCountY);
    npt_encode_UINT(enc, &ThreadGroupCountZ);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_Dispatch_reply(const UINT ThreadGroupCountX,const UINT ThreadGroupCountY,const UINT ThreadGroupCountZ)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ThreadGroupCountX (input only) */
/* skip ThreadGroupCountY (input only) */
/* skip ThreadGroupCountZ (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_Dispatch(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        UINT ThreadGroupCountX,
                                        UINT ThreadGroupCountY,
                                        UINT ThreadGroupCountZ,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_Dispatch(ThreadGroupCountX,ThreadGroupCountY,ThreadGroupCountZ);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_Dispatch_reply(ThreadGroupCountX,ThreadGroupCountY,ThreadGroupCountZ)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_Dispatch(enc, cmd_flags, object_id, ThreadGroupCountX, ThreadGroupCountY, ThreadGroupCountZ);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_Dispatch(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       UINT ThreadGroupCountX,
                                       UINT ThreadGroupCountY,
                                       UINT ThreadGroupCountZ)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Dispatch(ring, 0, object_id, ThreadGroupCountX, ThreadGroupCountY, ThreadGroupCountZ, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_Dispatch(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      UINT ThreadGroupCountX,
                                      UINT ThreadGroupCountY,
                                      UINT ThreadGroupCountZ)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Dispatch(ring, NPT_CMD_FLAG_REPLY, object_id, ThreadGroupCountX, ThreadGroupCountY, ThreadGroupCountZ, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DispatchIndirect
 */

struct npt_command_ID3D11DeviceContext_DispatchIndirect {
    void *_self;
    ID3D11Buffer * pBufferForArgs;
    UINT AlignedByteOffsetForArgs;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DispatchIndirect(const ID3D11Buffer * pBufferForArgs,const UINT AlignedByteOffsetForArgs)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&AlignedByteOffsetForArgs, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DispatchIndirect(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11Buffer * pBufferForArgs,
                                                UINT AlignedByteOffsetForArgs)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DispatchIndirect(pBufferForArgs,AlignedByteOffsetForArgs);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DispatchIndirect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pBufferForArgs));
    npt_encode_UINT(enc, &AlignedByteOffsetForArgs);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DispatchIndirect_reply(const ID3D11Buffer * pBufferForArgs,const UINT AlignedByteOffsetForArgs)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pBufferForArgs (input only) */
/* skip AlignedByteOffsetForArgs (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DispatchIndirect(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11Buffer * pBufferForArgs,
                                                UINT AlignedByteOffsetForArgs,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DispatchIndirect(pBufferForArgs,AlignedByteOffsetForArgs);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DispatchIndirect_reply(pBufferForArgs,AlignedByteOffsetForArgs)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DispatchIndirect(enc, cmd_flags, object_id, pBufferForArgs, AlignedByteOffsetForArgs);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DispatchIndirect(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Buffer * pBufferForArgs,
                                               UINT AlignedByteOffsetForArgs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DispatchIndirect(ring, 0, object_id, pBufferForArgs, AlignedByteOffsetForArgs, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DispatchIndirect(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11Buffer * pBufferForArgs,
                                              UINT AlignedByteOffsetForArgs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DispatchIndirect(ring, NPT_CMD_FLAG_REPLY, object_id, pBufferForArgs, AlignedByteOffsetForArgs, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::RSSetState
 */

struct npt_command_ID3D11DeviceContext_RSSetState {
    void *_self;
    ID3D11RasterizerState * pRasterizerState;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSSetState(const ID3D11RasterizerState * pRasterizerState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_RSSetState(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D11RasterizerState * pRasterizerState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_RSSetState(pRasterizerState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSSetState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pRasterizerState));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSSetState_reply(const ID3D11RasterizerState * pRasterizerState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRasterizerState (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_RSSetState(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D11RasterizerState * pRasterizerState,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_RSSetState(pRasterizerState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_RSSetState_reply(pRasterizerState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_RSSetState(enc, cmd_flags, object_id, pRasterizerState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_RSSetState(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11RasterizerState * pRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSSetState(ring, 0, object_id, pRasterizerState, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_RSSetState(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        ID3D11RasterizerState * pRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSSetState(ring, NPT_CMD_FLAG_REPLY, object_id, pRasterizerState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::RSSetViewports
 */

struct npt_command_ID3D11DeviceContext_RSSetViewports {
    void *_self;
    UINT NumViewports;
    const D3D11_VIEWPORT * pViewports;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSSetViewports(const UINT NumViewports,const D3D11_VIEWPORT * pViewports)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumViewports, max_mode);
    cmd_size += npt_sizeof_array_count(pViewports ? NumViewports : 0);
    for (uint32_t _i = 0; _i < (pViewports ? NumViewports : 0); _i++)
        cmd_size += npt_sizeof_D3D11_VIEWPORT(&pViewports[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_RSSetViewports(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT NumViewports,
                                              const D3D11_VIEWPORT * pViewports)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_RSSetViewports(NumViewports,pViewports);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSSetViewports),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumViewports);
    if (pViewports) {
        npt_encode_array_count(enc, NumViewports);
        for (uint32_t _i = 0; _i < (uint32_t)(NumViewports); _i++)
            npt_encode_D3D11_VIEWPORT(enc, &pViewports[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSSetViewports_reply(const UINT NumViewports,const D3D11_VIEWPORT * pViewports)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumViewports (input only) */
/* skip pViewports (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_RSSetViewports(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT NumViewports,
                                              const D3D11_VIEWPORT * pViewports,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_RSSetViewports(NumViewports,pViewports);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_RSSetViewports_reply(NumViewports,pViewports)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_RSSetViewports(enc, cmd_flags, object_id, NumViewports, pViewports);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_RSSetViewports(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT NumViewports,
                                             const D3D11_VIEWPORT * pViewports)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSSetViewports(ring, 0, object_id, NumViewports, pViewports, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_RSSetViewports(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT NumViewports,
                                            const D3D11_VIEWPORT * pViewports)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSSetViewports(ring, NPT_CMD_FLAG_REPLY, object_id, NumViewports, pViewports, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::RSSetScissorRects
 */

struct npt_command_ID3D11DeviceContext_RSSetScissorRects {
    void *_self;
    UINT NumRects;
    const D3D11_RECT * pRects;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSSetScissorRects(const UINT NumRects,const D3D11_RECT * pRects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumRects, max_mode);
    cmd_size += npt_sizeof_array_count(pRects ? NumRects : 0);
    for (uint32_t _i = 0; _i < (pRects ? NumRects : 0); _i++)
        cmd_size += npt_sizeof_D3D11_RECT(&pRects[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_RSSetScissorRects(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 UINT NumRects,
                                                 const D3D11_RECT * pRects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_RSSetScissorRects(NumRects,pRects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSSetScissorRects),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumRects);
    if (pRects) {
        npt_encode_array_count(enc, NumRects);
        for (uint32_t _i = 0; _i < (uint32_t)(NumRects); _i++)
            npt_encode_D3D11_RECT(enc, &pRects[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSSetScissorRects_reply(const UINT NumRects,const D3D11_RECT * pRects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumRects (input only) */
/* skip pRects (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_RSSetScissorRects(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 UINT NumRects,
                                                 const D3D11_RECT * pRects,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_RSSetScissorRects(NumRects,pRects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_RSSetScissorRects_reply(NumRects,pRects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_RSSetScissorRects(enc, cmd_flags, object_id, NumRects, pRects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_RSSetScissorRects(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT NumRects,
                                                const D3D11_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSSetScissorRects(ring, 0, object_id, NumRects, pRects, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_RSSetScissorRects(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               UINT NumRects,
                                               const D3D11_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSSetScissorRects(ring, NPT_CMD_FLAG_REPLY, object_id, NumRects, pRects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CopySubresourceRegion
 */

struct npt_command_ID3D11DeviceContext_CopySubresourceRegion {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    UINT DstX;
    UINT DstY;
    UINT DstZ;
    ID3D11Resource * pSrcResource;
    UINT SrcSubresource;
    const D3D11_BOX * pSrcBox;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CopySubresourceRegion(const ID3D11Resource * pDstResource,const UINT DstSubresource,const UINT DstX,const UINT DstY,const UINT DstZ,const ID3D11Resource * pSrcResource,const UINT SrcSubresource,const D3D11_BOX * pSrcBox)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DstSubresource, max_mode);
    cmd_size += npt_sizeof_UINT(&DstX, max_mode);
    cmd_size += npt_sizeof_UINT(&DstY, max_mode);
    cmd_size += npt_sizeof_UINT(&DstZ, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&SrcSubresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSrcBox);
    if (pSrcBox)
        cmd_size += npt_sizeof_D3D11_BOX(pSrcBox, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CopySubresourceRegion(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11Resource * pDstResource,
                                                     UINT DstSubresource,
                                                     UINT DstX,
                                                     UINT DstY,
                                                     UINT DstZ,
                                                     ID3D11Resource * pSrcResource,
                                                     UINT SrcSubresource,
                                                     const D3D11_BOX * pSrcBox)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CopySubresourceRegion(pDstResource,DstSubresource,DstX,DstY,DstZ,pSrcResource,SrcSubresource,pSrcBox);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CopySubresourceRegion),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_UINT(enc, &DstSubresource);
    npt_encode_UINT(enc, &DstX);
    npt_encode_UINT(enc, &DstY);
    npt_encode_UINT(enc, &DstZ);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcResource));
    npt_encode_UINT(enc, &SrcSubresource);
    if (npt_encode_simple_pointer(enc, pSrcBox))
        npt_encode_D3D11_BOX(enc, pSrcBox);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CopySubresourceRegion_reply(const ID3D11Resource * pDstResource,const UINT DstSubresource,const UINT DstX,const UINT DstY,const UINT DstZ,const ID3D11Resource * pSrcResource,const UINT SrcSubresource,const D3D11_BOX * pSrcBox)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip DstSubresource (input only) */
/* skip DstX (input only) */
/* skip DstY (input only) */
/* skip DstZ (input only) */
/* skip pSrcResource (input only) */
/* skip SrcSubresource (input only) */
/* skip pSrcBox (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_CopySubresourceRegion(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11Resource * pDstResource,
                                                     UINT DstSubresource,
                                                     UINT DstX,
                                                     UINT DstY,
                                                     UINT DstZ,
                                                     ID3D11Resource * pSrcResource,
                                                     UINT SrcSubresource,
                                                     const D3D11_BOX * pSrcBox,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CopySubresourceRegion(pDstResource,DstSubresource,DstX,DstY,DstZ,pSrcResource,SrcSubresource,pSrcBox);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CopySubresourceRegion_reply(pDstResource,DstSubresource,DstX,DstY,DstZ,pSrcResource,SrcSubresource,pSrcBox)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CopySubresourceRegion(enc, cmd_flags, object_id, pDstResource, DstSubresource, DstX, DstY, DstZ, pSrcResource, SrcSubresource, pSrcBox);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CopySubresourceRegion(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11Resource * pDstResource,
                                                    UINT DstSubresource,
                                                    UINT DstX,
                                                    UINT DstY,
                                                    UINT DstZ,
                                                    ID3D11Resource * pSrcResource,
                                                    UINT SrcSubresource,
                                                    const D3D11_BOX * pSrcBox)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CopySubresourceRegion(ring, 0, object_id, pDstResource, DstSubresource, DstX, DstY, DstZ, pSrcResource, SrcSubresource, pSrcBox, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CopySubresourceRegion(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11Resource * pDstResource,
                                                   UINT DstSubresource,
                                                   UINT DstX,
                                                   UINT DstY,
                                                   UINT DstZ,
                                                   ID3D11Resource * pSrcResource,
                                                   UINT SrcSubresource,
                                                   const D3D11_BOX * pSrcBox)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CopySubresourceRegion(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, DstSubresource, DstX, DstY, DstZ, pSrcResource, SrcSubresource, pSrcBox, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CopyResource
 */

struct npt_command_ID3D11DeviceContext_CopyResource {
    void *_self;
    ID3D11Resource * pDstResource;
    ID3D11Resource * pSrcResource;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CopyResource(const ID3D11Resource * pDstResource,const ID3D11Resource * pSrcResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CopyResource(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11Resource * pDstResource,
                                            ID3D11Resource * pSrcResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CopyResource(pDstResource,pSrcResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CopyResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_com_handle(enc, npt_object_get_id(pSrcResource));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CopyResource_reply(const ID3D11Resource * pDstResource,const ID3D11Resource * pSrcResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip pSrcResource (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_CopyResource(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11Resource * pDstResource,
                                            ID3D11Resource * pSrcResource,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CopyResource(pDstResource,pSrcResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CopyResource_reply(pDstResource,pSrcResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CopyResource(enc, cmd_flags, object_id, pDstResource, pSrcResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CopyResource(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D11Resource * pDstResource,
                                           ID3D11Resource * pSrcResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CopyResource(ring, 0, object_id, pDstResource, pSrcResource, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CopyResource(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11Resource * pDstResource,
                                          ID3D11Resource * pSrcResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CopyResource(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, pSrcResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::UpdateSubresource
 */

struct npt_command_ID3D11DeviceContext_UpdateSubresource {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    const D3D11_BOX * pDstBox;
    const void * pSrcData;
    UINT SrcRowPitch;
    UINT SrcDepthPitch;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_UpdateSubresource(const ID3D11Resource * pDstResource,const UINT DstSubresource,const D3D11_BOX * pDstBox,const void * pSrcData,const UINT SrcRowPitch,const UINT SrcDepthPitch)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DstSubresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDstBox);
    if (pDstBox)
        cmd_size += npt_sizeof_D3D11_BOX(pDstBox, max_mode);
    /* ERROR: pSrcData (void) is not serializable */
    return 0;
    cmd_size += npt_sizeof_UINT(&SrcRowPitch, max_mode);
    cmd_size += npt_sizeof_UINT(&SrcDepthPitch, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_UpdateSubresource(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pDstResource,
                                                 UINT DstSubresource,
                                                 const D3D11_BOX * pDstBox,
                                                 const void * pSrcData,
                                                 UINT SrcRowPitch,
                                                 UINT SrcDepthPitch)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_UpdateSubresource(pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_UpdateSubresource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_UINT(enc, &DstSubresource);
    if (npt_encode_simple_pointer(enc, pDstBox))
        npt_encode_D3D11_BOX(enc, pDstBox);
    /* ERROR: pSrcData (void) is not serializable */
    return;
    npt_encode_UINT(enc, &SrcRowPitch);
    npt_encode_UINT(enc, &SrcDepthPitch);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_UpdateSubresource_reply(const ID3D11Resource * pDstResource,const UINT DstSubresource,const D3D11_BOX * pDstBox,const void * pSrcData,const UINT SrcRowPitch,const UINT SrcDepthPitch)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip DstSubresource (input only) */
/* skip pDstBox (input only) */
/* skip pSrcData (input only) */
/* skip SrcRowPitch (input only) */
/* skip SrcDepthPitch (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_UpdateSubresource(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pDstResource,
                                                 UINT DstSubresource,
                                                 const D3D11_BOX * pDstBox,
                                                 const void * pSrcData,
                                                 UINT SrcRowPitch,
                                                 UINT SrcDepthPitch,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_UpdateSubresource(pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_UpdateSubresource_reply(pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_UpdateSubresource(enc, cmd_flags, object_id, pDstResource, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_UpdateSubresource(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11Resource * pDstResource,
                                                UINT DstSubresource,
                                                const D3D11_BOX * pDstBox,
                                                const void * pSrcData,
                                                UINT SrcRowPitch,
                                                UINT SrcDepthPitch)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_UpdateSubresource(ring, 0, object_id, pDstResource, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_UpdateSubresource(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Resource * pDstResource,
                                               UINT DstSubresource,
                                               const D3D11_BOX * pDstBox,
                                               const void * pSrcData,
                                               UINT SrcRowPitch,
                                               UINT SrcDepthPitch)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_UpdateSubresource(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CopyStructureCount
 */

struct npt_command_ID3D11DeviceContext_CopyStructureCount {
    void *_self;
    ID3D11Buffer * pDstBuffer;
    UINT DstAlignedByteOffset;
    ID3D11UnorderedAccessView * pSrcView;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CopyStructureCount(const ID3D11Buffer * pDstBuffer,const UINT DstAlignedByteOffset,const ID3D11UnorderedAccessView * pSrcView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DstAlignedByteOffset, max_mode);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CopyStructureCount(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11Buffer * pDstBuffer,
                                                  UINT DstAlignedByteOffset,
                                                  ID3D11UnorderedAccessView * pSrcView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CopyStructureCount(pDstBuffer,DstAlignedByteOffset,pSrcView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CopyStructureCount),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstBuffer));
    npt_encode_UINT(enc, &DstAlignedByteOffset);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcView));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CopyStructureCount_reply(const ID3D11Buffer * pDstBuffer,const UINT DstAlignedByteOffset,const ID3D11UnorderedAccessView * pSrcView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstBuffer (input only) */
/* skip DstAlignedByteOffset (input only) */
/* skip pSrcView (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_CopyStructureCount(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11Buffer * pDstBuffer,
                                                  UINT DstAlignedByteOffset,
                                                  ID3D11UnorderedAccessView * pSrcView,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CopyStructureCount(pDstBuffer,DstAlignedByteOffset,pSrcView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CopyStructureCount_reply(pDstBuffer,DstAlignedByteOffset,pSrcView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CopyStructureCount(enc, cmd_flags, object_id, pDstBuffer, DstAlignedByteOffset, pSrcView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CopyStructureCount(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11Buffer * pDstBuffer,
                                                 UINT DstAlignedByteOffset,
                                                 ID3D11UnorderedAccessView * pSrcView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CopyStructureCount(ring, 0, object_id, pDstBuffer, DstAlignedByteOffset, pSrcView, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CopyStructureCount(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11Buffer * pDstBuffer,
                                                UINT DstAlignedByteOffset,
                                                ID3D11UnorderedAccessView * pSrcView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CopyStructureCount(ring, NPT_CMD_FLAG_REPLY, object_id, pDstBuffer, DstAlignedByteOffset, pSrcView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::ClearRenderTargetView
 */

struct npt_command_ID3D11DeviceContext_ClearRenderTargetView {
    void *_self;
    ID3D11RenderTargetView * pRenderTargetView;
    const FLOAT * ColorRGBA;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearRenderTargetView(const ID3D11RenderTargetView * pRenderTargetView,const FLOAT * ColorRGBA)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(4);
    cmd_size += npt_sizeof_FLOAT_array((const FLOAT *)ColorRGBA, 4);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_ClearRenderTargetView(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11RenderTargetView * pRenderTargetView,
                                                     const FLOAT * ColorRGBA)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_ClearRenderTargetView(pRenderTargetView,ColorRGBA);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearRenderTargetView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pRenderTargetView));
    npt_encode_array_count(enc, 4);
    npt_encode_FLOAT_array(enc, (const FLOAT *)ColorRGBA, 4);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearRenderTargetView_reply(const ID3D11RenderTargetView * pRenderTargetView,const FLOAT * ColorRGBA)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRenderTargetView (input only) */
/* skip ColorRGBA (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_ClearRenderTargetView(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11RenderTargetView * pRenderTargetView,
                                                     const FLOAT * ColorRGBA,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_ClearRenderTargetView(pRenderTargetView,ColorRGBA);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_ClearRenderTargetView_reply(pRenderTargetView,ColorRGBA)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_ClearRenderTargetView(enc, cmd_flags, object_id, pRenderTargetView, ColorRGBA);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_ClearRenderTargetView(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11RenderTargetView * pRenderTargetView,
                                                    const FLOAT * ColorRGBA)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearRenderTargetView(ring, 0, object_id, pRenderTargetView, ColorRGBA, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_ClearRenderTargetView(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11RenderTargetView * pRenderTargetView,
                                                   const FLOAT * ColorRGBA)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearRenderTargetView(ring, NPT_CMD_FLAG_REPLY, object_id, pRenderTargetView, ColorRGBA, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::ClearUnorderedAccessViewUint
 */

struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewUint {
    void *_self;
    ID3D11UnorderedAccessView * pUnorderedAccessView;
    const UINT * Values;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewUint(const ID3D11UnorderedAccessView * pUnorderedAccessView,const UINT * Values)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(4);
    cmd_size += npt_sizeof_UINT_array((const UINT *)Values, 4);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_ClearUnorderedAccessViewUint(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11UnorderedAccessView * pUnorderedAccessView,
                                                            const UINT * Values)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewUint(pUnorderedAccessView,Values);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearUnorderedAccessViewUint),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pUnorderedAccessView));
    npt_encode_array_count(enc, 4);
    npt_encode_UINT_array(enc, (const UINT *)Values, 4);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewUint_reply(const ID3D11UnorderedAccessView * pUnorderedAccessView,const UINT * Values)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pUnorderedAccessView (input only) */
/* skip Values (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_ClearUnorderedAccessViewUint(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11UnorderedAccessView * pUnorderedAccessView,
                                                            const UINT * Values,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewUint(pUnorderedAccessView,Values);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewUint_reply(pUnorderedAccessView,Values)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_ClearUnorderedAccessViewUint(enc, cmd_flags, object_id, pUnorderedAccessView, Values);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_ClearUnorderedAccessViewUint(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11UnorderedAccessView * pUnorderedAccessView,
                                                           const UINT * Values)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearUnorderedAccessViewUint(ring, 0, object_id, pUnorderedAccessView, Values, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_ClearUnorderedAccessViewUint(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D11UnorderedAccessView * pUnorderedAccessView,
                                                          const UINT * Values)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearUnorderedAccessViewUint(ring, NPT_CMD_FLAG_REPLY, object_id, pUnorderedAccessView, Values, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::ClearUnorderedAccessViewFloat
 */

struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewFloat {
    void *_self;
    ID3D11UnorderedAccessView * pUnorderedAccessView;
    const FLOAT * Values;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(const ID3D11UnorderedAccessView * pUnorderedAccessView,const FLOAT * Values)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(4);
    cmd_size += npt_sizeof_FLOAT_array((const FLOAT *)Values, 4);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11UnorderedAccessView * pUnorderedAccessView,
                                                             const FLOAT * Values)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(pUnorderedAccessView,Values);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearUnorderedAccessViewFloat),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pUnorderedAccessView));
    npt_encode_array_count(enc, 4);
    npt_encode_FLOAT_array(enc, (const FLOAT *)Values, 4);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewFloat_reply(const ID3D11UnorderedAccessView * pUnorderedAccessView,const FLOAT * Values)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pUnorderedAccessView (input only) */
/* skip Values (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11UnorderedAccessView * pUnorderedAccessView,
                                                             const FLOAT * Values,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(pUnorderedAccessView,Values);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_ClearUnorderedAccessViewFloat_reply(pUnorderedAccessView,Values)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(enc, cmd_flags, object_id, pUnorderedAccessView, Values);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11UnorderedAccessView * pUnorderedAccessView,
                                                            const FLOAT * Values)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(ring, 0, object_id, pUnorderedAccessView, Values, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11UnorderedAccessView * pUnorderedAccessView,
                                                           const FLOAT * Values)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(ring, NPT_CMD_FLAG_REPLY, object_id, pUnorderedAccessView, Values, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::ClearDepthStencilView
 */

struct npt_command_ID3D11DeviceContext_ClearDepthStencilView {
    void *_self;
    ID3D11DepthStencilView * pDepthStencilView;
    UINT ClearFlags;
    FLOAT Depth;
    UINT8 Stencil;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearDepthStencilView(const ID3D11DepthStencilView * pDepthStencilView,const UINT ClearFlags,const FLOAT Depth,const UINT8 Stencil)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&ClearFlags, max_mode);
    cmd_size += npt_sizeof_FLOAT(&Depth, max_mode);
    cmd_size += npt_sizeof_UINT8(&Stencil, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_ClearDepthStencilView(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11DepthStencilView * pDepthStencilView,
                                                     UINT ClearFlags,
                                                     FLOAT Depth,
                                                     UINT8 Stencil)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_ClearDepthStencilView(pDepthStencilView,ClearFlags,Depth,Stencil);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearDepthStencilView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDepthStencilView));
    npt_encode_UINT(enc, &ClearFlags);
    npt_encode_FLOAT(enc, &Depth);
    npt_encode_UINT8(enc, &Stencil);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearDepthStencilView_reply(const ID3D11DepthStencilView * pDepthStencilView,const UINT ClearFlags,const FLOAT Depth,const UINT8 Stencil)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDepthStencilView (input only) */
/* skip ClearFlags (input only) */
/* skip Depth (input only) */
/* skip Stencil (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_ClearDepthStencilView(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11DepthStencilView * pDepthStencilView,
                                                     UINT ClearFlags,
                                                     FLOAT Depth,
                                                     UINT8 Stencil,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_ClearDepthStencilView(pDepthStencilView,ClearFlags,Depth,Stencil);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_ClearDepthStencilView_reply(pDepthStencilView,ClearFlags,Depth,Stencil)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_ClearDepthStencilView(enc, cmd_flags, object_id, pDepthStencilView, ClearFlags, Depth, Stencil);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_ClearDepthStencilView(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11DepthStencilView * pDepthStencilView,
                                                    UINT ClearFlags,
                                                    FLOAT Depth,
                                                    UINT8 Stencil)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearDepthStencilView(ring, 0, object_id, pDepthStencilView, ClearFlags, Depth, Stencil, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_ClearDepthStencilView(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11DepthStencilView * pDepthStencilView,
                                                   UINT ClearFlags,
                                                   FLOAT Depth,
                                                   UINT8 Stencil)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearDepthStencilView(ring, NPT_CMD_FLAG_REPLY, object_id, pDepthStencilView, ClearFlags, Depth, Stencil, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GenerateMips
 */

struct npt_command_ID3D11DeviceContext_GenerateMips {
    void *_self;
    ID3D11ShaderResourceView * pShaderResourceView;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GenerateMips(const ID3D11ShaderResourceView * pShaderResourceView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GenerateMips(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11ShaderResourceView * pShaderResourceView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GenerateMips(pShaderResourceView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GenerateMips),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pShaderResourceView));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GenerateMips_reply(const ID3D11ShaderResourceView * pShaderResourceView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pShaderResourceView (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_GenerateMips(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11ShaderResourceView * pShaderResourceView,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GenerateMips(pShaderResourceView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GenerateMips_reply(pShaderResourceView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GenerateMips(enc, cmd_flags, object_id, pShaderResourceView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GenerateMips(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D11ShaderResourceView * pShaderResourceView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GenerateMips(ring, 0, object_id, pShaderResourceView, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GenerateMips(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11ShaderResourceView * pShaderResourceView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GenerateMips(ring, NPT_CMD_FLAG_REPLY, object_id, pShaderResourceView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::SetResourceMinLOD
 */

struct npt_command_ID3D11DeviceContext_SetResourceMinLOD {
    void *_self;
    ID3D11Resource * pResource;
    FLOAT MinLOD;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_SetResourceMinLOD(const ID3D11Resource * pResource,const FLOAT MinLOD)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_FLOAT(&MinLOD, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_SetResourceMinLOD(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource,
                                                 FLOAT MinLOD)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_SetResourceMinLOD(pResource,MinLOD);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_SetResourceMinLOD),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    npt_encode_FLOAT(enc, &MinLOD);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_SetResourceMinLOD_reply(const ID3D11Resource * pResource,const FLOAT MinLOD)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip MinLOD (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_SetResourceMinLOD(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource,
                                                 FLOAT MinLOD,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_SetResourceMinLOD(pResource,MinLOD);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_SetResourceMinLOD_reply(pResource,MinLOD)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_SetResourceMinLOD(enc, cmd_flags, object_id, pResource, MinLOD);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_SetResourceMinLOD(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11Resource * pResource,
                                                FLOAT MinLOD)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_SetResourceMinLOD(ring, 0, object_id, pResource, MinLOD, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_SetResourceMinLOD(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Resource * pResource,
                                               FLOAT MinLOD)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_SetResourceMinLOD(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, MinLOD, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GetResourceMinLOD
 */

struct npt_command_ID3D11DeviceContext_GetResourceMinLOD {
    void *_self;
    ID3D11Resource * pResource;
    FLOAT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetResourceMinLOD(const ID3D11Resource * pResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GetResourceMinLOD(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GetResourceMinLOD(pResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetResourceMinLOD),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetResourceMinLOD_reply(const ID3D11Resource * pResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_GetResourceMinLOD_reply(struct npt_cs_decoder *dec,
                                                       FLOAT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_GetResourceMinLOD))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_GetResourceMinLOD reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_GetResourceMinLOD));
        if (ret) *ret = (FLOAT)0;
        return;
    }
    if (ret) *ret = (FLOAT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11DeviceContext_GetResourceMinLOD(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GetResourceMinLOD(pResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GetResourceMinLOD_reply(pResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GetResourceMinLOD(enc, cmd_flags, object_id, pResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GetResourceMinLOD(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11Resource * pResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetResourceMinLOD(ring, 0, object_id, pResource, &submit);
}

static inline FLOAT
npt_call_ID3D11DeviceContext_GetResourceMinLOD(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Resource * pResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetResourceMinLOD(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        FLOAT _ret;
        npt_decode_ID3D11DeviceContext_GetResourceMinLOD_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (FLOAT)0;
    }
}

/*
 * ID3D11DeviceContext::ResolveSubresource
 */

struct npt_command_ID3D11DeviceContext_ResolveSubresource {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    ID3D11Resource * pSrcResource;
    UINT SrcSubresource;
    DXGI_FORMAT Format;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_ResolveSubresource(const ID3D11Resource * pDstResource,const UINT DstSubresource,const ID3D11Resource * pSrcResource,const UINT SrcSubresource,const DXGI_FORMAT Format)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DstSubresource, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&SrcSubresource, max_mode);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_ResolveSubresource(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11Resource * pDstResource,
                                                  UINT DstSubresource,
                                                  ID3D11Resource * pSrcResource,
                                                  UINT SrcSubresource,
                                                  DXGI_FORMAT Format)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_ResolveSubresource(pDstResource,DstSubresource,pSrcResource,SrcSubresource,Format);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ResolveSubresource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_UINT(enc, &DstSubresource);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcResource));
    npt_encode_UINT(enc, &SrcSubresource);
    npt_encode_DXGI_FORMAT(enc, &Format);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_ResolveSubresource_reply(const ID3D11Resource * pDstResource,const UINT DstSubresource,const ID3D11Resource * pSrcResource,const UINT SrcSubresource,const DXGI_FORMAT Format)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip DstSubresource (input only) */
/* skip pSrcResource (input only) */
/* skip SrcSubresource (input only) */
/* skip Format (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_ResolveSubresource(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11Resource * pDstResource,
                                                  UINT DstSubresource,
                                                  ID3D11Resource * pSrcResource,
                                                  UINT SrcSubresource,
                                                  DXGI_FORMAT Format,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_ResolveSubresource(pDstResource,DstSubresource,pSrcResource,SrcSubresource,Format);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_ResolveSubresource_reply(pDstResource,DstSubresource,pSrcResource,SrcSubresource,Format)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_ResolveSubresource(enc, cmd_flags, object_id, pDstResource, DstSubresource, pSrcResource, SrcSubresource, Format);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_ResolveSubresource(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pDstResource,
                                                 UINT DstSubresource,
                                                 ID3D11Resource * pSrcResource,
                                                 UINT SrcSubresource,
                                                 DXGI_FORMAT Format)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ResolveSubresource(ring, 0, object_id, pDstResource, DstSubresource, pSrcResource, SrcSubresource, Format, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_ResolveSubresource(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11Resource * pDstResource,
                                                UINT DstSubresource,
                                                ID3D11Resource * pSrcResource,
                                                UINT SrcSubresource,
                                                DXGI_FORMAT Format)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ResolveSubresource(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, DstSubresource, pSrcResource, SrcSubresource, Format, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::ExecuteCommandList
 */

struct npt_command_ID3D11DeviceContext_ExecuteCommandList {
    void *_self;
    ID3D11CommandList * pCommandList;
    BOOL RestoreContextState;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_ExecuteCommandList(const ID3D11CommandList * pCommandList,const BOOL RestoreContextState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_BOOL(&RestoreContextState, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_ExecuteCommandList(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11CommandList * pCommandList,
                                                  BOOL RestoreContextState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_ExecuteCommandList(pCommandList,RestoreContextState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ExecuteCommandList),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCommandList));
    npt_encode_BOOL(enc, &RestoreContextState);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_ExecuteCommandList_reply(const ID3D11CommandList * pCommandList,const BOOL RestoreContextState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCommandList (input only) */
/* skip RestoreContextState (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_ExecuteCommandList(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11CommandList * pCommandList,
                                                  BOOL RestoreContextState,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_ExecuteCommandList(pCommandList,RestoreContextState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_ExecuteCommandList_reply(pCommandList,RestoreContextState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_ExecuteCommandList(enc, cmd_flags, object_id, pCommandList, RestoreContextState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_ExecuteCommandList(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11CommandList * pCommandList,
                                                 BOOL RestoreContextState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ExecuteCommandList(ring, 0, object_id, pCommandList, RestoreContextState, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_ExecuteCommandList(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11CommandList * pCommandList,
                                                BOOL RestoreContextState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ExecuteCommandList(ring, NPT_CMD_FLAG_REPLY, object_id, pCommandList, RestoreContextState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::HSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_HSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSSetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
    cmd_size += npt_sizeof_array_count(NumViews);
    cmd_size += sizeof(uint64_t) * NumViews;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_HSSetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_HSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSSetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
    if (ppShaderResourceViews) {
        npt_encode_array_count(enc, NumViews);
        for (uint32_t _i = 0; _i < (uint32_t)NumViews; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppShaderResourceViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSSetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_HSSetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_HSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_HSSetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_HSSetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_HSSetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSSetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_HSSetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSSetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::HSSetShader
 */

struct npt_command_ID3D11DeviceContext_HSSetShader {
    void *_self;
    ID3D11HullShader * pHullShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSSetShader(const ID3D11HullShader * pHullShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(NumClassInstances);
    cmd_size += sizeof(uint64_t) * NumClassInstances;
    cmd_size += npt_sizeof_UINT(&NumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_HSSetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11HullShader * pHullShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_HSSetShader(pHullShader,ppClassInstances,NumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSSetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pHullShader));
    if (ppClassInstances) {
        npt_encode_array_count(enc, NumClassInstances);
        for (uint32_t _i = 0; _i < (uint32_t)NumClassInstances; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppClassInstances[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSSetShader_reply(const ID3D11HullShader * pHullShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pHullShader (input only) */
/* skip ppClassInstances (input only) */
/* skip NumClassInstances (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_HSSetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11HullShader * pHullShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_HSSetShader(pHullShader,ppClassInstances,NumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_HSSetShader_reply(pHullShader,ppClassInstances,NumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_HSSetShader(enc, cmd_flags, object_id, pHullShader, ppClassInstances, NumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_HSSetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11HullShader * pHullShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSSetShader(ring, 0, object_id, pHullShader, ppClassInstances, NumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_HSSetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11HullShader * pHullShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSSetShader(ring, NPT_CMD_FLAG_REPLY, object_id, pHullShader, ppClassInstances, NumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::HSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_HSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSSetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
    cmd_size += npt_sizeof_array_count(NumSamplers);
    cmd_size += sizeof(uint64_t) * NumSamplers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_HSSetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_HSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSSetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
    if (ppSamplers) {
        npt_encode_array_count(enc, NumSamplers);
        for (uint32_t _i = 0; _i < (uint32_t)NumSamplers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppSamplers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSSetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_HSSetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_HSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_HSSetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_HSSetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_HSSetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSSetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_HSSetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSSetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::HSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_HSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSSetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_HSSetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_HSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSSetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSSetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_HSSetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_HSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_HSSetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_HSSetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_HSSetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSSetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_HSSetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSSetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_DSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSSetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
    cmd_size += npt_sizeof_array_count(NumViews);
    cmd_size += sizeof(uint64_t) * NumViews;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DSSetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSSetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
    if (ppShaderResourceViews) {
        npt_encode_array_count(enc, NumViews);
        for (uint32_t _i = 0; _i < (uint32_t)NumViews; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppShaderResourceViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSSetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DSSetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DSSetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DSSetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DSSetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSSetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DSSetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSSetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DSSetShader
 */

struct npt_command_ID3D11DeviceContext_DSSetShader {
    void *_self;
    ID3D11DomainShader * pDomainShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSSetShader(const ID3D11DomainShader * pDomainShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(NumClassInstances);
    cmd_size += sizeof(uint64_t) * NumClassInstances;
    cmd_size += npt_sizeof_UINT(&NumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DSSetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11DomainShader * pDomainShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DSSetShader(pDomainShader,ppClassInstances,NumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSSetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDomainShader));
    if (ppClassInstances) {
        npt_encode_array_count(enc, NumClassInstances);
        for (uint32_t _i = 0; _i < (uint32_t)NumClassInstances; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppClassInstances[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSSetShader_reply(const ID3D11DomainShader * pDomainShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDomainShader (input only) */
/* skip ppClassInstances (input only) */
/* skip NumClassInstances (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DSSetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11DomainShader * pDomainShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DSSetShader(pDomainShader,ppClassInstances,NumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DSSetShader_reply(pDomainShader,ppClassInstances,NumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DSSetShader(enc, cmd_flags, object_id, pDomainShader, ppClassInstances, NumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DSSetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11DomainShader * pDomainShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSSetShader(ring, 0, object_id, pDomainShader, ppClassInstances, NumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DSSetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11DomainShader * pDomainShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSSetShader(ring, NPT_CMD_FLAG_REPLY, object_id, pDomainShader, ppClassInstances, NumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_DSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSSetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
    cmd_size += npt_sizeof_array_count(NumSamplers);
    cmd_size += sizeof(uint64_t) * NumSamplers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DSSetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSSetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
    if (ppSamplers) {
        npt_encode_array_count(enc, NumSamplers);
        for (uint32_t _i = 0; _i < (uint32_t)NumSamplers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppSamplers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSSetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DSSetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DSSetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DSSetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DSSetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSSetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DSSetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSSetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_DSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSSetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DSSetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSSetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSSetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_DSSetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DSSetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DSSetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DSSetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSSetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DSSetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSSetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_CSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
    cmd_size += npt_sizeof_array_count(NumViews);
    cmd_size += sizeof(uint64_t) * NumViews;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
    if (ppShaderResourceViews) {
        npt_encode_array_count(enc, NumViews);
        for (uint32_t _i = 0; _i < (uint32_t)NumViews; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppShaderResourceViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_CSSetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSSetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSSetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSSetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSSetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSSetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSSetUnorderedAccessViews
 */

struct npt_command_ID3D11DeviceContext_CSSetUnorderedAccessViews {
    void *_self;
    UINT StartSlot;
    UINT NumUAVs;
    ID3D11UnorderedAccessView ** ppUnorderedAccessViews;
    const UINT * pUAVInitialCounts;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetUnorderedAccessViews(const UINT StartSlot,const UINT NumUAVs,ID3D11UnorderedAccessView ** ppUnorderedAccessViews,const UINT * pUAVInitialCounts)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumUAVs, max_mode);
    cmd_size += npt_sizeof_array_count(NumUAVs);
    cmd_size += sizeof(uint64_t) * NumUAVs;
    cmd_size += npt_sizeof_array_count(pUAVInitialCounts ? NumUAVs : 0);
    if (pUAVInitialCounts)
        cmd_size += npt_sizeof_UINT_array(pUAVInitialCounts, NumUAVs);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetUnorderedAccessViews(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         UINT StartSlot,
                                                         UINT NumUAVs,
                                                         ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                         const UINT * pUAVInitialCounts)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSSetUnorderedAccessViews(StartSlot,NumUAVs,ppUnorderedAccessViews,pUAVInitialCounts);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetUnorderedAccessViews),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumUAVs);
    if (ppUnorderedAccessViews) {
        npt_encode_array_count(enc, NumUAVs);
        for (uint32_t _i = 0; _i < (uint32_t)NumUAVs; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppUnorderedAccessViews[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pUAVInitialCounts) {
        npt_encode_array_count(enc, NumUAVs);
        npt_encode_UINT_array(enc, pUAVInitialCounts, NumUAVs);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetUnorderedAccessViews_reply(const UINT StartSlot,const UINT NumUAVs,ID3D11UnorderedAccessView ** ppUnorderedAccessViews,const UINT * pUAVInitialCounts)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumUAVs (input only) */
/* skip ppUnorderedAccessViews (input only) */
/* skip pUAVInitialCounts (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_CSSetUnorderedAccessViews(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         UINT StartSlot,
                                                         UINT NumUAVs,
                                                         ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                         const UINT * pUAVInitialCounts,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSSetUnorderedAccessViews(StartSlot,NumUAVs,ppUnorderedAccessViews,pUAVInitialCounts);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSSetUnorderedAccessViews_reply(StartSlot,NumUAVs,ppUnorderedAccessViews,pUAVInitialCounts)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSSetUnorderedAccessViews(enc, cmd_flags, object_id, StartSlot, NumUAVs, ppUnorderedAccessViews, pUAVInitialCounts);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSSetUnorderedAccessViews(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        UINT StartSlot,
                                                        UINT NumUAVs,
                                                        ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                        const UINT * pUAVInitialCounts)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetUnorderedAccessViews(ring, 0, object_id, StartSlot, NumUAVs, ppUnorderedAccessViews, pUAVInitialCounts, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSSetUnorderedAccessViews(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       UINT StartSlot,
                                                       UINT NumUAVs,
                                                       ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                       const UINT * pUAVInitialCounts)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetUnorderedAccessViews(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumUAVs, ppUnorderedAccessViews, pUAVInitialCounts, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSSetShader
 */

struct npt_command_ID3D11DeviceContext_CSSetShader {
    void *_self;
    ID3D11ComputeShader * pComputeShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetShader(const ID3D11ComputeShader * pComputeShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(NumClassInstances);
    cmd_size += sizeof(uint64_t) * NumClassInstances;
    cmd_size += npt_sizeof_UINT(&NumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11ComputeShader * pComputeShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSSetShader(pComputeShader,ppClassInstances,NumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pComputeShader));
    if (ppClassInstances) {
        npt_encode_array_count(enc, NumClassInstances);
        for (uint32_t _i = 0; _i < (uint32_t)NumClassInstances; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppClassInstances[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetShader_reply(const ID3D11ComputeShader * pComputeShader,ID3D11ClassInstance ** ppClassInstances,const UINT NumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pComputeShader (input only) */
/* skip ppClassInstances (input only) */
/* skip NumClassInstances (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_CSSetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11ComputeShader * pComputeShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT NumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSSetShader(pComputeShader,ppClassInstances,NumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSSetShader_reply(pComputeShader,ppClassInstances,NumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSSetShader(enc, cmd_flags, object_id, pComputeShader, ppClassInstances, NumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSSetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11ComputeShader * pComputeShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetShader(ring, 0, object_id, pComputeShader, ppClassInstances, NumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSSetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11ComputeShader * pComputeShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT NumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetShader(ring, NPT_CMD_FLAG_REPLY, object_id, pComputeShader, ppClassInstances, NumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_CSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
    cmd_size += npt_sizeof_array_count(NumSamplers);
    cmd_size += sizeof(uint64_t) * NumSamplers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
    if (ppSamplers) {
        npt_encode_array_count(enc, NumSamplers);
        for (uint32_t _i = 0; _i < (uint32_t)NumSamplers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppSamplers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_CSSetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSSetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSSetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSSetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSSetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSSetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_CSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSSetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_CSSetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSSetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSSetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSSetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSSetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSSetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSSetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::VSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_VSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSGetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_VSGetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_VSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSGetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSGetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_VSGetConstantBuffers_reply(struct npt_cs_decoder *dec,
                                                          ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_VSGetConstantBuffers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_VSGetConstantBuffers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_VSGetConstantBuffers));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_VSGetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_VSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_VSGetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_VSGetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_VSGetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSGetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_VSGetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSGetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_VSGetConstantBuffers_reply(dec, ppConstantBuffers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::PSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_PSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSGetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumViews) * sizeof(uint64_t);  /* ppShaderResourceViews: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_PSGetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_PSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSGetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
if (ppShaderResourceViews) {
    const uint64_t _cnt_ppShaderResourceViews = (uint64_t)(NumViews);
    npt_encode_array_count(enc, _cnt_ppShaderResourceViews);
    for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppShaderResourceViews[_i] = (ID3D11ShaderResourceView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSGetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_PSGetShaderResources_reply(struct npt_cs_decoder *dec,
                                                          ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_PSGetShaderResources))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_PSGetShaderResources reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_PSGetShaderResources));
        return;
    }
/* skip ppShaderResourceViews (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_PSGetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_PSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_PSGetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_PSGetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_PSGetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSGetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_PSGetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSGetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_PSGetShaderResources_reply(dec, ppShaderResourceViews);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::PSGetShader
 */

struct npt_command_ID3D11DeviceContext_PSGetShader {
    void *_self;
    ID3D11PixelShader ** ppPixelShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppPixelShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPixelShader -> *ppPixelShader} in the
     * context object table. */
    uint64_t _guest_id_ppPixelShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSGetShader(ID3D11PixelShader ** ppPixelShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppPixelShader: guest id */
cmd_size += sizeof(uint64_t) + (size_t)((pNumClassInstances ? *pNumClassInstances : 0)) * sizeof(uint64_t);  /* ppClassInstances: array_count + N guest ids */
    cmd_size += npt_sizeof_simple_pointer(pNumClassInstances);
    if (pNumClassInstances)
        cmd_size += npt_sizeof_UINT(pNumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_PSGetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11PixelShader ** ppPixelShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_PSGetShader(ppPixelShader,ppClassInstances,pNumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSGetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppPixelShader = 0;
    if (ppPixelShader) {
        _gid_ppPixelShader = npt_com_allocate_next_id();
        *ppPixelShader = (ID3D11PixelShader *)(uintptr_t)_gid_ppPixelShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppPixelShader);
}
if (ppClassInstances) {
    const uint64_t _cnt_ppClassInstances = (uint64_t)((pNumClassInstances ? *pNumClassInstances : 0));
    npt_encode_array_count(enc, _cnt_ppClassInstances);
    for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppClassInstances[_i] = (ID3D11ClassInstance *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
    if (npt_encode_simple_pointer(enc, pNumClassInstances))
        npt_encode_UINT(enc, pNumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSGetShader_reply(ID3D11PixelShader ** ppPixelShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppPixelShader (guest-allocated id, registered host-side; not in reply) */
/* skip ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_PSGetShader_reply(struct npt_cs_decoder *dec,
                                                 ID3D11PixelShader ** ppPixelShader,
                                                 ID3D11ClassInstance ** ppClassInstances,
                                                 UINT * pNumClassInstances)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_PSGetShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_PSGetShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_PSGetShader));
        return;
    }
/* skip ppPixelShader (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppClassInstances (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pNumClassInstances)
            npt_decode_UINT(dec, (UINT *)pNumClassInstances);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_PSGetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11PixelShader ** ppPixelShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_PSGetShader(ppPixelShader,ppClassInstances,pNumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_PSGetShader_reply(ppPixelShader,ppClassInstances,pNumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_PSGetShader(enc, cmd_flags, object_id, ppPixelShader, ppClassInstances, pNumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_PSGetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11PixelShader ** ppPixelShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSGetShader(ring, 0, object_id, ppPixelShader, ppClassInstances, pNumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_PSGetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11PixelShader ** ppPixelShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSGetShader(ring, NPT_CMD_FLAG_REPLY, object_id, ppPixelShader, ppClassInstances, pNumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_PSGetShader_reply(dec, ppPixelShader, ppClassInstances, pNumClassInstances);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::PSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_PSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSGetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumSamplers) * sizeof(uint64_t);  /* ppSamplers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_PSGetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_PSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSGetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
if (ppSamplers) {
    const uint64_t _cnt_ppSamplers = (uint64_t)(NumSamplers);
    npt_encode_array_count(enc, _cnt_ppSamplers);
    for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppSamplers[_i] = (ID3D11SamplerState *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSGetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_PSGetSamplers_reply(struct npt_cs_decoder *dec,
                                                   ID3D11SamplerState ** ppSamplers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_PSGetSamplers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_PSGetSamplers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_PSGetSamplers));
        return;
    }
/* skip ppSamplers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_PSGetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_PSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_PSGetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_PSGetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_PSGetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSGetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_PSGetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSGetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_PSGetSamplers_reply(dec, ppSamplers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::VSGetShader
 */

struct npt_command_ID3D11DeviceContext_VSGetShader {
    void *_self;
    ID3D11VertexShader ** ppVertexShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppVertexShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVertexShader -> *ppVertexShader} in the
     * context object table. */
    uint64_t _guest_id_ppVertexShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSGetShader(ID3D11VertexShader ** ppVertexShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppVertexShader: guest id */
cmd_size += sizeof(uint64_t) + (size_t)((pNumClassInstances ? *pNumClassInstances : 0)) * sizeof(uint64_t);  /* ppClassInstances: array_count + N guest ids */
    cmd_size += npt_sizeof_simple_pointer(pNumClassInstances);
    if (pNumClassInstances)
        cmd_size += npt_sizeof_UINT(pNumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_VSGetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11VertexShader ** ppVertexShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_VSGetShader(ppVertexShader,ppClassInstances,pNumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSGetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppVertexShader = 0;
    if (ppVertexShader) {
        _gid_ppVertexShader = npt_com_allocate_next_id();
        *ppVertexShader = (ID3D11VertexShader *)(uintptr_t)_gid_ppVertexShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppVertexShader);
}
if (ppClassInstances) {
    const uint64_t _cnt_ppClassInstances = (uint64_t)((pNumClassInstances ? *pNumClassInstances : 0));
    npt_encode_array_count(enc, _cnt_ppClassInstances);
    for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppClassInstances[_i] = (ID3D11ClassInstance *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
    if (npt_encode_simple_pointer(enc, pNumClassInstances))
        npt_encode_UINT(enc, pNumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSGetShader_reply(ID3D11VertexShader ** ppVertexShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppVertexShader (guest-allocated id, registered host-side; not in reply) */
/* skip ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_VSGetShader_reply(struct npt_cs_decoder *dec,
                                                 ID3D11VertexShader ** ppVertexShader,
                                                 ID3D11ClassInstance ** ppClassInstances,
                                                 UINT * pNumClassInstances)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_VSGetShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_VSGetShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_VSGetShader));
        return;
    }
/* skip ppVertexShader (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppClassInstances (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pNumClassInstances)
            npt_decode_UINT(dec, (UINT *)pNumClassInstances);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_VSGetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11VertexShader ** ppVertexShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_VSGetShader(ppVertexShader,ppClassInstances,pNumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_VSGetShader_reply(ppVertexShader,ppClassInstances,pNumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_VSGetShader(enc, cmd_flags, object_id, ppVertexShader, ppClassInstances, pNumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_VSGetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11VertexShader ** ppVertexShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSGetShader(ring, 0, object_id, ppVertexShader, ppClassInstances, pNumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_VSGetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11VertexShader ** ppVertexShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSGetShader(ring, NPT_CMD_FLAG_REPLY, object_id, ppVertexShader, ppClassInstances, pNumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_VSGetShader_reply(dec, ppVertexShader, ppClassInstances, pNumClassInstances);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::PSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_PSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSGetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_PSGetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_PSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSGetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_PSGetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_PSGetConstantBuffers_reply(struct npt_cs_decoder *dec,
                                                          ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_PSGetConstantBuffers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_PSGetConstantBuffers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_PSGetConstantBuffers));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_PSGetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_PSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_PSGetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_PSGetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_PSGetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSGetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_PSGetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_PSGetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_PSGetConstantBuffers_reply(dec, ppConstantBuffers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::IAGetInputLayout
 */

struct npt_command_ID3D11DeviceContext_IAGetInputLayout {
    void *_self;
    ID3D11InputLayout ** ppInputLayout;
    /* Shadow: the guest-allocated id for ppInputLayout arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppInputLayout -> *ppInputLayout} in the
     * context object table. */
    uint64_t _guest_id_ppInputLayout;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_IAGetInputLayout(ID3D11InputLayout ** ppInputLayout)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppInputLayout: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_IAGetInputLayout(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11InputLayout ** ppInputLayout)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_IAGetInputLayout(ppInputLayout);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IAGetInputLayout),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppInputLayout = 0;
    if (ppInputLayout) {
        _gid_ppInputLayout = npt_com_allocate_next_id();
        *ppInputLayout = (ID3D11InputLayout *)(uintptr_t)_gid_ppInputLayout;
    }
    npt_encode_uint64_t(enc, &_gid_ppInputLayout);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_IAGetInputLayout_reply(ID3D11InputLayout ** ppInputLayout)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppInputLayout (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_IAGetInputLayout_reply(struct npt_cs_decoder *dec,
                                                      ID3D11InputLayout ** ppInputLayout)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_IAGetInputLayout))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_IAGetInputLayout reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_IAGetInputLayout));
        return;
    }
/* skip ppInputLayout (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_IAGetInputLayout(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11InputLayout ** ppInputLayout,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_IAGetInputLayout(ppInputLayout);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_IAGetInputLayout_reply(ppInputLayout)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_IAGetInputLayout(enc, cmd_flags, object_id, ppInputLayout);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_IAGetInputLayout(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11InputLayout ** ppInputLayout)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IAGetInputLayout(ring, 0, object_id, ppInputLayout, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_IAGetInputLayout(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11InputLayout ** ppInputLayout)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IAGetInputLayout(ring, NPT_CMD_FLAG_REPLY, object_id, ppInputLayout, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_IAGetInputLayout_reply(dec, ppInputLayout);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::IAGetVertexBuffers
 */

struct npt_command_ID3D11DeviceContext_IAGetVertexBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppVertexBuffers;
    UINT * pStrides;
    UINT * pOffsets;
    /* Shadow: per-element guest-allocated ids for ppVertexBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppVertexBuffers;
    uint32_t _guest_id_count_ppVertexBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_IAGetVertexBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppVertexBuffers,const UINT * pStrides,const UINT * pOffsets)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppVertexBuffers: array_count + N guest ids */
/* skip pStrides (output only) */
/* skip pOffsets (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_IAGetVertexBuffers(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppVertexBuffers,
                                                  UINT * pStrides,
                                                  UINT * pOffsets)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_IAGetVertexBuffers(StartSlot,NumBuffers,ppVertexBuffers,pStrides,pOffsets);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IAGetVertexBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppVertexBuffers) {
    const uint64_t _cnt_ppVertexBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppVertexBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppVertexBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppVertexBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
/* skip pStrides (output only) */
/* skip pOffsets (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_IAGetVertexBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppVertexBuffers,const UINT * pStrides,const UINT * pOffsets)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppVertexBuffers (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_array_count(pStrides ? NumBuffers : 0);
    if (pStrides)
        cmd_size += npt_sizeof_UINT_array(pStrides, NumBuffers);
    cmd_size += npt_sizeof_array_count(pOffsets ? NumBuffers : 0);
    if (pOffsets)
        cmd_size += npt_sizeof_UINT_array(pOffsets, NumBuffers);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_IAGetVertexBuffers_reply(struct npt_cs_decoder *dec,
                                                        ID3D11Buffer ** ppVertexBuffers,
                                                        UINT * pStrides,
                                                        UINT * pOffsets)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_IAGetVertexBuffers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_IAGetVertexBuffers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_IAGetVertexBuffers));
        return;
    }
/* skip ppVertexBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pStrides)
            npt_decode_UINT_array(dec, (UINT *)pStrides, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pOffsets)
            npt_decode_UINT_array(dec, (UINT *)pOffsets, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_ID3D11DeviceContext_IAGetVertexBuffers(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppVertexBuffers,
                                                  UINT * pStrides,
                                                  UINT * pOffsets,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_IAGetVertexBuffers(StartSlot,NumBuffers,ppVertexBuffers,pStrides,pOffsets);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_IAGetVertexBuffers_reply(StartSlot,NumBuffers,ppVertexBuffers,pStrides,pOffsets)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_IAGetVertexBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppVertexBuffers, pStrides, pOffsets);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_IAGetVertexBuffers(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT StartSlot,
                                                 UINT NumBuffers,
                                                 ID3D11Buffer ** ppVertexBuffers,
                                                 UINT * pStrides,
                                                 UINT * pOffsets)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IAGetVertexBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppVertexBuffers, pStrides, pOffsets, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_IAGetVertexBuffers(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT StartSlot,
                                                UINT NumBuffers,
                                                ID3D11Buffer ** ppVertexBuffers,
                                                UINT * pStrides,
                                                UINT * pOffsets)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IAGetVertexBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppVertexBuffers, pStrides, pOffsets, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_IAGetVertexBuffers_reply(dec, ppVertexBuffers, pStrides, pOffsets);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::IAGetIndexBuffer
 */

struct npt_command_ID3D11DeviceContext_IAGetIndexBuffer {
    void *_self;
    ID3D11Buffer ** pIndexBuffer;
    DXGI_FORMAT * Format;
    UINT * Offset;
    /* Shadow: the guest-allocated id for pIndexBuffer arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_pIndexBuffer -> *pIndexBuffer} in the
     * context object table. */
    uint64_t _guest_id_pIndexBuffer;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_IAGetIndexBuffer(ID3D11Buffer ** pIndexBuffer,const DXGI_FORMAT * Format,const UINT * Offset)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* pIndexBuffer: guest id */
/* skip Format (output only) */
/* skip Offset (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_IAGetIndexBuffer(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11Buffer ** pIndexBuffer,
                                                DXGI_FORMAT * Format,
                                                UINT * Offset)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_IAGetIndexBuffer(pIndexBuffer,Format,Offset);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IAGetIndexBuffer),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_pIndexBuffer = 0;
    if (pIndexBuffer) {
        _gid_pIndexBuffer = npt_com_allocate_next_id();
        *pIndexBuffer = (ID3D11Buffer *)(uintptr_t)_gid_pIndexBuffer;
    }
    npt_encode_uint64_t(enc, &_gid_pIndexBuffer);
}
/* skip Format (output only) */
/* skip Offset (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_IAGetIndexBuffer_reply(ID3D11Buffer ** pIndexBuffer,const DXGI_FORMAT * Format,const UINT * Offset)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pIndexBuffer (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_FORMAT(&(const DXGI_FORMAT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_IAGetIndexBuffer_reply(struct npt_cs_decoder *dec,
                                                      ID3D11Buffer ** pIndexBuffer,
                                                      DXGI_FORMAT * Format,
                                                      UINT * Offset)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_IAGetIndexBuffer))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_IAGetIndexBuffer reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_IAGetIndexBuffer));
        return;
    }
/* skip pIndexBuffer (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (Format)
            npt_decode_DXGI_FORMAT(dec, (DXGI_FORMAT *)Format);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (Offset)
            npt_decode_UINT(dec, (UINT *)Offset);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_IAGetIndexBuffer(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11Buffer ** pIndexBuffer,
                                                DXGI_FORMAT * Format,
                                                UINT * Offset,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_IAGetIndexBuffer(pIndexBuffer,Format,Offset);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_IAGetIndexBuffer_reply(pIndexBuffer,Format,Offset)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_IAGetIndexBuffer(enc, cmd_flags, object_id, pIndexBuffer, Format, Offset);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_IAGetIndexBuffer(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Buffer ** pIndexBuffer,
                                               DXGI_FORMAT * Format,
                                               UINT * Offset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IAGetIndexBuffer(ring, 0, object_id, pIndexBuffer, Format, Offset, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_IAGetIndexBuffer(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11Buffer ** pIndexBuffer,
                                              DXGI_FORMAT * Format,
                                              UINT * Offset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IAGetIndexBuffer(ring, NPT_CMD_FLAG_REPLY, object_id, pIndexBuffer, Format, Offset, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_IAGetIndexBuffer_reply(dec, pIndexBuffer, Format, Offset);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_GSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSGetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GSGetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSGetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSGetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_GSGetConstantBuffers_reply(struct npt_cs_decoder *dec,
                                                          ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_GSGetConstantBuffers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_GSGetConstantBuffers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_GSGetConstantBuffers));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_GSGetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GSGetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GSGetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GSGetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSGetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GSGetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSGetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_GSGetConstantBuffers_reply(dec, ppConstantBuffers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GSGetShader
 */

struct npt_command_ID3D11DeviceContext_GSGetShader {
    void *_self;
    ID3D11GeometryShader ** ppGeometryShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppGeometryShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppGeometryShader -> *ppGeometryShader} in the
     * context object table. */
    uint64_t _guest_id_ppGeometryShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSGetShader(ID3D11GeometryShader ** ppGeometryShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppGeometryShader: guest id */
cmd_size += sizeof(uint64_t) + (size_t)((pNumClassInstances ? *pNumClassInstances : 0)) * sizeof(uint64_t);  /* ppClassInstances: array_count + N guest ids */
    cmd_size += npt_sizeof_simple_pointer(pNumClassInstances);
    if (pNumClassInstances)
        cmd_size += npt_sizeof_UINT(pNumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GSGetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11GeometryShader ** ppGeometryShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GSGetShader(ppGeometryShader,ppClassInstances,pNumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSGetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppGeometryShader = 0;
    if (ppGeometryShader) {
        _gid_ppGeometryShader = npt_com_allocate_next_id();
        *ppGeometryShader = (ID3D11GeometryShader *)(uintptr_t)_gid_ppGeometryShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppGeometryShader);
}
if (ppClassInstances) {
    const uint64_t _cnt_ppClassInstances = (uint64_t)((pNumClassInstances ? *pNumClassInstances : 0));
    npt_encode_array_count(enc, _cnt_ppClassInstances);
    for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppClassInstances[_i] = (ID3D11ClassInstance *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
    if (npt_encode_simple_pointer(enc, pNumClassInstances))
        npt_encode_UINT(enc, pNumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSGetShader_reply(ID3D11GeometryShader ** ppGeometryShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppGeometryShader (guest-allocated id, registered host-side; not in reply) */
/* skip ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_GSGetShader_reply(struct npt_cs_decoder *dec,
                                                 ID3D11GeometryShader ** ppGeometryShader,
                                                 ID3D11ClassInstance ** ppClassInstances,
                                                 UINT * pNumClassInstances)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_GSGetShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_GSGetShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_GSGetShader));
        return;
    }
/* skip ppGeometryShader (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppClassInstances (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pNumClassInstances)
            npt_decode_UINT(dec, (UINT *)pNumClassInstances);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_GSGetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11GeometryShader ** ppGeometryShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GSGetShader(ppGeometryShader,ppClassInstances,pNumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GSGetShader_reply(ppGeometryShader,ppClassInstances,pNumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GSGetShader(enc, cmd_flags, object_id, ppGeometryShader, ppClassInstances, pNumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GSGetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11GeometryShader ** ppGeometryShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSGetShader(ring, 0, object_id, ppGeometryShader, ppClassInstances, pNumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GSGetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11GeometryShader ** ppGeometryShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSGetShader(ring, NPT_CMD_FLAG_REPLY, object_id, ppGeometryShader, ppClassInstances, pNumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_GSGetShader_reply(dec, ppGeometryShader, ppClassInstances, pNumClassInstances);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::IAGetPrimitiveTopology
 */

struct npt_command_ID3D11DeviceContext_IAGetPrimitiveTopology {
    void *_self;
    D3D11_PRIMITIVE_TOPOLOGY * pTopology;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_IAGetPrimitiveTopology(const D3D11_PRIMITIVE_TOPOLOGY * pTopology)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pTopology (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_IAGetPrimitiveTopology(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      D3D11_PRIMITIVE_TOPOLOGY * pTopology)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_IAGetPrimitiveTopology(pTopology);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IAGetPrimitiveTopology),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pTopology (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_IAGetPrimitiveTopology_reply(const D3D11_PRIMITIVE_TOPOLOGY * pTopology)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_PRIMITIVE_TOPOLOGY(&(const D3D11_PRIMITIVE_TOPOLOGY){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_IAGetPrimitiveTopology_reply(struct npt_cs_decoder *dec,
                                                            D3D11_PRIMITIVE_TOPOLOGY * pTopology)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_IAGetPrimitiveTopology))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_IAGetPrimitiveTopology reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_IAGetPrimitiveTopology));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pTopology)
            npt_decode_D3D11_PRIMITIVE_TOPOLOGY(dec, (D3D11_PRIMITIVE_TOPOLOGY *)pTopology);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_IAGetPrimitiveTopology(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      D3D11_PRIMITIVE_TOPOLOGY * pTopology,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_IAGetPrimitiveTopology(pTopology);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_IAGetPrimitiveTopology_reply(pTopology)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_IAGetPrimitiveTopology(enc, cmd_flags, object_id, pTopology);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_IAGetPrimitiveTopology(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     D3D11_PRIMITIVE_TOPOLOGY * pTopology)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IAGetPrimitiveTopology(ring, 0, object_id, pTopology, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_IAGetPrimitiveTopology(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    D3D11_PRIMITIVE_TOPOLOGY * pTopology)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_IAGetPrimitiveTopology(ring, NPT_CMD_FLAG_REPLY, object_id, pTopology, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_IAGetPrimitiveTopology_reply(dec, pTopology);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::VSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_VSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSGetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumViews) * sizeof(uint64_t);  /* ppShaderResourceViews: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_VSGetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_VSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSGetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
if (ppShaderResourceViews) {
    const uint64_t _cnt_ppShaderResourceViews = (uint64_t)(NumViews);
    npt_encode_array_count(enc, _cnt_ppShaderResourceViews);
    for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppShaderResourceViews[_i] = (ID3D11ShaderResourceView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSGetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_VSGetShaderResources_reply(struct npt_cs_decoder *dec,
                                                          ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_VSGetShaderResources))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_VSGetShaderResources reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_VSGetShaderResources));
        return;
    }
/* skip ppShaderResourceViews (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_VSGetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_VSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_VSGetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_VSGetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_VSGetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSGetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_VSGetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSGetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_VSGetShaderResources_reply(dec, ppShaderResourceViews);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::VSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_VSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSGetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumSamplers) * sizeof(uint64_t);  /* ppSamplers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_VSGetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_VSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSGetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
if (ppSamplers) {
    const uint64_t _cnt_ppSamplers = (uint64_t)(NumSamplers);
    npt_encode_array_count(enc, _cnt_ppSamplers);
    for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppSamplers[_i] = (ID3D11SamplerState *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_VSGetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_VSGetSamplers_reply(struct npt_cs_decoder *dec,
                                                   ID3D11SamplerState ** ppSamplers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_VSGetSamplers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_VSGetSamplers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_VSGetSamplers));
        return;
    }
/* skip ppSamplers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_VSGetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_VSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_VSGetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_VSGetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_VSGetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSGetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_VSGetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_VSGetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_VSGetSamplers_reply(dec, ppSamplers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GetPredication
 */

struct npt_command_ID3D11DeviceContext_GetPredication {
    void *_self;
    ID3D11Predicate ** ppPredicate;
    BOOL * pPredicateValue;
    /* Shadow: the guest-allocated id for ppPredicate arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPredicate -> *ppPredicate} in the
     * context object table. */
    uint64_t _guest_id_ppPredicate;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetPredication(ID3D11Predicate ** ppPredicate,const BOOL * pPredicateValue)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppPredicate: guest id */
/* skip pPredicateValue (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GetPredication(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11Predicate ** ppPredicate,
                                              BOOL * pPredicateValue)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GetPredication(ppPredicate,pPredicateValue);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetPredication),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppPredicate = 0;
    if (ppPredicate) {
        _gid_ppPredicate = npt_com_allocate_next_id();
        *ppPredicate = (ID3D11Predicate *)(uintptr_t)_gid_ppPredicate;
    }
    npt_encode_uint64_t(enc, &_gid_ppPredicate);
}
/* skip pPredicateValue (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetPredication_reply(ID3D11Predicate ** ppPredicate,const BOOL * pPredicateValue)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppPredicate (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_GetPredication_reply(struct npt_cs_decoder *dec,
                                                    ID3D11Predicate ** ppPredicate,
                                                    BOOL * pPredicateValue)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_GetPredication))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_GetPredication reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_GetPredication));
        return;
    }
/* skip ppPredicate (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pPredicateValue)
            npt_decode_BOOL(dec, (BOOL *)pPredicateValue);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_GetPredication(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11Predicate ** ppPredicate,
                                              BOOL * pPredicateValue,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GetPredication(ppPredicate,pPredicateValue);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GetPredication_reply(ppPredicate,pPredicateValue)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GetPredication(enc, cmd_flags, object_id, ppPredicate, pPredicateValue);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GetPredication(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11Predicate ** ppPredicate,
                                             BOOL * pPredicateValue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetPredication(ring, 0, object_id, ppPredicate, pPredicateValue, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GetPredication(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            ID3D11Predicate ** ppPredicate,
                                            BOOL * pPredicateValue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetPredication(ring, NPT_CMD_FLAG_REPLY, object_id, ppPredicate, pPredicateValue, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_GetPredication_reply(dec, ppPredicate, pPredicateValue);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_GSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSGetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumViews) * sizeof(uint64_t);  /* ppShaderResourceViews: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GSGetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSGetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
if (ppShaderResourceViews) {
    const uint64_t _cnt_ppShaderResourceViews = (uint64_t)(NumViews);
    npt_encode_array_count(enc, _cnt_ppShaderResourceViews);
    for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppShaderResourceViews[_i] = (ID3D11ShaderResourceView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSGetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_GSGetShaderResources_reply(struct npt_cs_decoder *dec,
                                                          ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_GSGetShaderResources))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_GSGetShaderResources reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_GSGetShaderResources));
        return;
    }
/* skip ppShaderResourceViews (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_GSGetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GSGetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GSGetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GSGetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSGetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GSGetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSGetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_GSGetShaderResources_reply(dec, ppShaderResourceViews);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_GSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSGetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumSamplers) * sizeof(uint64_t);  /* ppSamplers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GSGetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSGetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
if (ppSamplers) {
    const uint64_t _cnt_ppSamplers = (uint64_t)(NumSamplers);
    npt_encode_array_count(enc, _cnt_ppSamplers);
    for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppSamplers[_i] = (ID3D11SamplerState *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GSGetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_GSGetSamplers_reply(struct npt_cs_decoder *dec,
                                                   ID3D11SamplerState ** ppSamplers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_GSGetSamplers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_GSGetSamplers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_GSGetSamplers));
        return;
    }
/* skip ppSamplers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_GSGetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GSGetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GSGetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GSGetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSGetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_GSGetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GSGetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_GSGetSamplers_reply(dec, ppSamplers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::OMGetRenderTargets
 */

struct npt_command_ID3D11DeviceContext_OMGetRenderTargets {
    void *_self;
    UINT NumViews;
    ID3D11RenderTargetView ** ppRenderTargetViews;
    ID3D11DepthStencilView ** ppDepthStencilView;
    /* Shadow: per-element guest-allocated ids for ppRenderTargetViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppRenderTargetViews;
    uint32_t _guest_id_count_ppRenderTargetViews;
    /* Shadow: the guest-allocated id for ppDepthStencilView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilView -> *ppDepthStencilView} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilView;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMGetRenderTargets(const UINT NumViews,ID3D11RenderTargetView ** ppRenderTargetViews,ID3D11DepthStencilView ** ppDepthStencilView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumViews) * sizeof(uint64_t);  /* ppRenderTargetViews: array_count + N guest ids */
cmd_size += sizeof(uint64_t);  /* ppDepthStencilView: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_OMGetRenderTargets(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT NumViews,
                                                  ID3D11RenderTargetView ** ppRenderTargetViews,
                                                  ID3D11DepthStencilView ** ppDepthStencilView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_OMGetRenderTargets(NumViews,ppRenderTargetViews,ppDepthStencilView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMGetRenderTargets),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumViews);
if (ppRenderTargetViews) {
    const uint64_t _cnt_ppRenderTargetViews = (uint64_t)(NumViews);
    npt_encode_array_count(enc, _cnt_ppRenderTargetViews);
    for (uint64_t _i = 0; _i < _cnt_ppRenderTargetViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppRenderTargetViews[_i] = (ID3D11RenderTargetView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
{
    uint64_t _gid_ppDepthStencilView = 0;
    if (ppDepthStencilView) {
        _gid_ppDepthStencilView = npt_com_allocate_next_id();
        *ppDepthStencilView = (ID3D11DepthStencilView *)(uintptr_t)_gid_ppDepthStencilView;
    }
    npt_encode_uint64_t(enc, &_gid_ppDepthStencilView);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMGetRenderTargets_reply(const UINT NumViews,ID3D11RenderTargetView ** ppRenderTargetViews,ID3D11DepthStencilView ** ppDepthStencilView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumViews (input only) */
/* skip ppRenderTargetViews (guest-allocated id, registered host-side; not in reply) */
/* skip ppDepthStencilView (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_OMGetRenderTargets_reply(struct npt_cs_decoder *dec,
                                                        ID3D11RenderTargetView ** ppRenderTargetViews,
                                                        ID3D11DepthStencilView ** ppDepthStencilView)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_OMGetRenderTargets))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_OMGetRenderTargets reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_OMGetRenderTargets));
        return;
    }
/* skip ppRenderTargetViews (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppDepthStencilView (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_OMGetRenderTargets(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT NumViews,
                                                  ID3D11RenderTargetView ** ppRenderTargetViews,
                                                  ID3D11DepthStencilView ** ppDepthStencilView,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_OMGetRenderTargets(NumViews,ppRenderTargetViews,ppDepthStencilView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_OMGetRenderTargets_reply(NumViews,ppRenderTargetViews,ppDepthStencilView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_OMGetRenderTargets(enc, cmd_flags, object_id, NumViews, ppRenderTargetViews, ppDepthStencilView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_OMGetRenderTargets(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT NumViews,
                                                 ID3D11RenderTargetView ** ppRenderTargetViews,
                                                 ID3D11DepthStencilView ** ppDepthStencilView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMGetRenderTargets(ring, 0, object_id, NumViews, ppRenderTargetViews, ppDepthStencilView, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_OMGetRenderTargets(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT NumViews,
                                                ID3D11RenderTargetView ** ppRenderTargetViews,
                                                ID3D11DepthStencilView ** ppDepthStencilView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMGetRenderTargets(ring, NPT_CMD_FLAG_REPLY, object_id, NumViews, ppRenderTargetViews, ppDepthStencilView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_OMGetRenderTargets_reply(dec, ppRenderTargetViews, ppDepthStencilView);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::OMGetRenderTargetsAndUnorderedAccessViews
 */

struct npt_command_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews {
    void *_self;
    UINT NumRTVs;
    ID3D11RenderTargetView ** ppRenderTargetViews;
    ID3D11DepthStencilView ** ppDepthStencilView;
    UINT UAVStartSlot;
    UINT NumUAVs;
    ID3D11UnorderedAccessView ** ppUnorderedAccessViews;
    /* Shadow: per-element guest-allocated ids for ppRenderTargetViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppRenderTargetViews;
    uint32_t _guest_id_count_ppRenderTargetViews;
    /* Shadow: the guest-allocated id for ppDepthStencilView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilView -> *ppDepthStencilView} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilView;
    /* Shadow: per-element guest-allocated ids for ppUnorderedAccessViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppUnorderedAccessViews;
    uint32_t _guest_id_count_ppUnorderedAccessViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(const UINT NumRTVs,ID3D11RenderTargetView ** ppRenderTargetViews,ID3D11DepthStencilView ** ppDepthStencilView,const UINT UAVStartSlot,const UINT NumUAVs,ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumRTVs, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumRTVs) * sizeof(uint64_t);  /* ppRenderTargetViews: array_count + N guest ids */
cmd_size += sizeof(uint64_t);  /* ppDepthStencilView: guest id */
    cmd_size += npt_sizeof_UINT(&UAVStartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumUAVs, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumUAVs) * sizeof(uint64_t);  /* ppUnorderedAccessViews: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(struct npt_cs_encoder *enc,
                                                                         uint32_t cmd_flags,
                                                                         npt_object_id object_id,
                                                                         UINT NumRTVs,
                                                                         ID3D11RenderTargetView ** ppRenderTargetViews,
                                                                         ID3D11DepthStencilView ** ppDepthStencilView,
                                                                         UINT UAVStartSlot,
                                                                         UINT NumUAVs,
                                                                         ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(NumRTVs,ppRenderTargetViews,ppDepthStencilView,UAVStartSlot,NumUAVs,ppUnorderedAccessViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumRTVs);
if (ppRenderTargetViews) {
    const uint64_t _cnt_ppRenderTargetViews = (uint64_t)(NumRTVs);
    npt_encode_array_count(enc, _cnt_ppRenderTargetViews);
    for (uint64_t _i = 0; _i < _cnt_ppRenderTargetViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppRenderTargetViews[_i] = (ID3D11RenderTargetView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
{
    uint64_t _gid_ppDepthStencilView = 0;
    if (ppDepthStencilView) {
        _gid_ppDepthStencilView = npt_com_allocate_next_id();
        *ppDepthStencilView = (ID3D11DepthStencilView *)(uintptr_t)_gid_ppDepthStencilView;
    }
    npt_encode_uint64_t(enc, &_gid_ppDepthStencilView);
}
    npt_encode_UINT(enc, &UAVStartSlot);
    npt_encode_UINT(enc, &NumUAVs);
if (ppUnorderedAccessViews) {
    const uint64_t _cnt_ppUnorderedAccessViews = (uint64_t)(NumUAVs);
    npt_encode_array_count(enc, _cnt_ppUnorderedAccessViews);
    for (uint64_t _i = 0; _i < _cnt_ppUnorderedAccessViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppUnorderedAccessViews[_i] = (ID3D11UnorderedAccessView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_reply(const UINT NumRTVs,ID3D11RenderTargetView ** ppRenderTargetViews,ID3D11DepthStencilView ** ppDepthStencilView,const UINT UAVStartSlot,const UINT NumUAVs,ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumRTVs (input only) */
/* skip ppRenderTargetViews (guest-allocated id, registered host-side; not in reply) */
/* skip ppDepthStencilView (guest-allocated id, registered host-side; not in reply) */
/* skip UAVStartSlot (input only) */
/* skip NumUAVs (input only) */
/* skip ppUnorderedAccessViews (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_reply(struct npt_cs_decoder *dec,
                                                                               ID3D11RenderTargetView ** ppRenderTargetViews,
                                                                               ID3D11DepthStencilView ** ppDepthStencilView,
                                                                               ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews));
        return;
    }
/* skip ppRenderTargetViews (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppDepthStencilView (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppUnorderedAccessViews (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(struct npt_ring *ring,
                                                                         uint32_t cmd_flags,
                                                                         npt_object_id object_id,
                                                                         UINT NumRTVs,
                                                                         ID3D11RenderTargetView ** ppRenderTargetViews,
                                                                         ID3D11DepthStencilView ** ppDepthStencilView,
                                                                         UINT UAVStartSlot,
                                                                         UINT NumUAVs,
                                                                         ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(NumRTVs,ppRenderTargetViews,ppDepthStencilView,UAVStartSlot,NumUAVs,ppUnorderedAccessViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_reply(NumRTVs,ppRenderTargetViews,ppDepthStencilView,UAVStartSlot,NumUAVs,ppUnorderedAccessViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(enc, cmd_flags, object_id, NumRTVs, ppRenderTargetViews, ppDepthStencilView, UAVStartSlot, NumUAVs, ppUnorderedAccessViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(struct npt_ring *ring,
                                                                        npt_object_id object_id,
                                                                        UINT NumRTVs,
                                                                        ID3D11RenderTargetView ** ppRenderTargetViews,
                                                                        ID3D11DepthStencilView ** ppDepthStencilView,
                                                                        UINT UAVStartSlot,
                                                                        UINT NumUAVs,
                                                                        ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(ring, 0, object_id, NumRTVs, ppRenderTargetViews, ppDepthStencilView, UAVStartSlot, NumUAVs, ppUnorderedAccessViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(struct npt_ring *ring,
                                                                       npt_object_id object_id,
                                                                       UINT NumRTVs,
                                                                       ID3D11RenderTargetView ** ppRenderTargetViews,
                                                                       ID3D11DepthStencilView ** ppDepthStencilView,
                                                                       UINT UAVStartSlot,
                                                                       UINT NumUAVs,
                                                                       ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(ring, NPT_CMD_FLAG_REPLY, object_id, NumRTVs, ppRenderTargetViews, ppDepthStencilView, UAVStartSlot, NumUAVs, ppUnorderedAccessViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_reply(dec, ppRenderTargetViews, ppDepthStencilView, ppUnorderedAccessViews);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::OMGetBlendState
 */

struct npt_command_ID3D11DeviceContext_OMGetBlendState {
    void *_self;
    ID3D11BlendState ** ppBlendState;
    FLOAT * BlendFactor;
    UINT * pSampleMask;
    /* Shadow: the guest-allocated id for ppBlendState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppBlendState -> *ppBlendState} in the
     * context object table. */
    uint64_t _guest_id_ppBlendState;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMGetBlendState(ID3D11BlendState ** ppBlendState,const FLOAT * BlendFactor,const UINT * pSampleMask)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppBlendState: guest id */
/* skip BlendFactor (output only) */
/* skip pSampleMask (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_OMGetBlendState(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11BlendState ** ppBlendState,
                                               FLOAT * BlendFactor,
                                               UINT * pSampleMask)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_OMGetBlendState(ppBlendState,BlendFactor,pSampleMask);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMGetBlendState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppBlendState = 0;
    if (ppBlendState) {
        _gid_ppBlendState = npt_com_allocate_next_id();
        *ppBlendState = (ID3D11BlendState *)(uintptr_t)_gid_ppBlendState;
    }
    npt_encode_uint64_t(enc, &_gid_ppBlendState);
}
/* skip BlendFactor (output only) */
/* skip pSampleMask (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMGetBlendState_reply(ID3D11BlendState ** ppBlendState,const FLOAT * BlendFactor,const UINT * pSampleMask)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppBlendState (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_array_count(BlendFactor ? 4 : 0);
    if (BlendFactor)
        cmd_size += npt_sizeof_FLOAT_array((const FLOAT *)BlendFactor, 4);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_OMGetBlendState_reply(struct npt_cs_decoder *dec,
                                                     ID3D11BlendState ** ppBlendState,
                                                     FLOAT * BlendFactor,
                                                     UINT * pSampleMask)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_OMGetBlendState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_OMGetBlendState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_OMGetBlendState));
        return;
    }
/* skip ppBlendState (guest-allocated id, wrapper built on client thunk; not in reply) */
    (void)npt_decode_array_count(dec, 4);
    {
        FLOAT *_buf = (FLOAT *)BlendFactor;
        if (!_buf)
            _buf = npt_cs_decoder_alloc_temp_array(dec, sizeof(FLOAT), (4));
        if (_buf)
            npt_decode_FLOAT_array(dec, _buf, 4);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pSampleMask)
            npt_decode_UINT(dec, (UINT *)pSampleMask);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_OMGetBlendState(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11BlendState ** ppBlendState,
                                               FLOAT * BlendFactor,
                                               UINT * pSampleMask,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_OMGetBlendState(ppBlendState,BlendFactor,pSampleMask);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_OMGetBlendState_reply(ppBlendState,BlendFactor,pSampleMask)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_OMGetBlendState(enc, cmd_flags, object_id, ppBlendState, BlendFactor, pSampleMask);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_OMGetBlendState(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11BlendState ** ppBlendState,
                                              FLOAT * BlendFactor,
                                              UINT * pSampleMask)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMGetBlendState(ring, 0, object_id, ppBlendState, BlendFactor, pSampleMask, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_OMGetBlendState(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11BlendState ** ppBlendState,
                                             FLOAT * BlendFactor,
                                             UINT * pSampleMask)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMGetBlendState(ring, NPT_CMD_FLAG_REPLY, object_id, ppBlendState, BlendFactor, pSampleMask, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_OMGetBlendState_reply(dec, ppBlendState, BlendFactor, pSampleMask);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::OMGetDepthStencilState
 */

struct npt_command_ID3D11DeviceContext_OMGetDepthStencilState {
    void *_self;
    ID3D11DepthStencilState ** ppDepthStencilState;
    UINT * pStencilRef;
    /* Shadow: the guest-allocated id for ppDepthStencilState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilState -> *ppDepthStencilState} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilState;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMGetDepthStencilState(ID3D11DepthStencilState ** ppDepthStencilState,const UINT * pStencilRef)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppDepthStencilState: guest id */
/* skip pStencilRef (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_OMGetDepthStencilState(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11DepthStencilState ** ppDepthStencilState,
                                                      UINT * pStencilRef)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_OMGetDepthStencilState(ppDepthStencilState,pStencilRef);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMGetDepthStencilState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppDepthStencilState = 0;
    if (ppDepthStencilState) {
        _gid_ppDepthStencilState = npt_com_allocate_next_id();
        *ppDepthStencilState = (ID3D11DepthStencilState *)(uintptr_t)_gid_ppDepthStencilState;
    }
    npt_encode_uint64_t(enc, &_gid_ppDepthStencilState);
}
/* skip pStencilRef (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_OMGetDepthStencilState_reply(ID3D11DepthStencilState ** ppDepthStencilState,const UINT * pStencilRef)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppDepthStencilState (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_OMGetDepthStencilState_reply(struct npt_cs_decoder *dec,
                                                            ID3D11DepthStencilState ** ppDepthStencilState,
                                                            UINT * pStencilRef)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_OMGetDepthStencilState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_OMGetDepthStencilState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_OMGetDepthStencilState));
        return;
    }
/* skip ppDepthStencilState (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pStencilRef)
            npt_decode_UINT(dec, (UINT *)pStencilRef);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_OMGetDepthStencilState(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11DepthStencilState ** ppDepthStencilState,
                                                      UINT * pStencilRef,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_OMGetDepthStencilState(ppDepthStencilState,pStencilRef);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_OMGetDepthStencilState_reply(ppDepthStencilState,pStencilRef)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_OMGetDepthStencilState(enc, cmd_flags, object_id, ppDepthStencilState, pStencilRef);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_OMGetDepthStencilState(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3D11DepthStencilState ** ppDepthStencilState,
                                                     UINT * pStencilRef)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMGetDepthStencilState(ring, 0, object_id, ppDepthStencilState, pStencilRef, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_OMGetDepthStencilState(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11DepthStencilState ** ppDepthStencilState,
                                                    UINT * pStencilRef)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_OMGetDepthStencilState(ring, NPT_CMD_FLAG_REPLY, object_id, ppDepthStencilState, pStencilRef, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_OMGetDepthStencilState_reply(dec, ppDepthStencilState, pStencilRef);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::SOGetTargets
 */

struct npt_command_ID3D11DeviceContext_SOGetTargets {
    void *_self;
    UINT NumBuffers;
    ID3D11Buffer ** ppSOTargets;
    /* Shadow: per-element guest-allocated ids for ppSOTargets.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSOTargets;
    uint32_t _guest_id_count_ppSOTargets;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_SOGetTargets(const UINT NumBuffers,ID3D11Buffer ** ppSOTargets)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppSOTargets: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_SOGetTargets(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT NumBuffers,
                                            ID3D11Buffer ** ppSOTargets)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_SOGetTargets(NumBuffers,ppSOTargets);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_SOGetTargets),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumBuffers);
if (ppSOTargets) {
    const uint64_t _cnt_ppSOTargets = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppSOTargets);
    for (uint64_t _i = 0; _i < _cnt_ppSOTargets; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppSOTargets[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_SOGetTargets_reply(const UINT NumBuffers,ID3D11Buffer ** ppSOTargets)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumBuffers (input only) */
/* skip ppSOTargets (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_SOGetTargets_reply(struct npt_cs_decoder *dec,
                                                  ID3D11Buffer ** ppSOTargets)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_SOGetTargets))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_SOGetTargets reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_SOGetTargets));
        return;
    }
/* skip ppSOTargets (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_SOGetTargets(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            UINT NumBuffers,
                                            ID3D11Buffer ** ppSOTargets,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_SOGetTargets(NumBuffers,ppSOTargets);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_SOGetTargets_reply(NumBuffers,ppSOTargets)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_SOGetTargets(enc, cmd_flags, object_id, NumBuffers, ppSOTargets);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_SOGetTargets(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT NumBuffers,
                                           ID3D11Buffer ** ppSOTargets)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_SOGetTargets(ring, 0, object_id, NumBuffers, ppSOTargets, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_SOGetTargets(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          UINT NumBuffers,
                                          ID3D11Buffer ** ppSOTargets)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_SOGetTargets(ring, NPT_CMD_FLAG_REPLY, object_id, NumBuffers, ppSOTargets, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_SOGetTargets_reply(dec, ppSOTargets);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::RSGetState
 */

struct npt_command_ID3D11DeviceContext_RSGetState {
    void *_self;
    ID3D11RasterizerState ** ppRasterizerState;
    /* Shadow: the guest-allocated id for ppRasterizerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRasterizerState -> *ppRasterizerState} in the
     * context object table. */
    uint64_t _guest_id_ppRasterizerState;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSGetState(ID3D11RasterizerState ** ppRasterizerState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppRasterizerState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_RSGetState(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D11RasterizerState ** ppRasterizerState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_RSGetState(ppRasterizerState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSGetState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppRasterizerState = 0;
    if (ppRasterizerState) {
        _gid_ppRasterizerState = npt_com_allocate_next_id();
        *ppRasterizerState = (ID3D11RasterizerState *)(uintptr_t)_gid_ppRasterizerState;
    }
    npt_encode_uint64_t(enc, &_gid_ppRasterizerState);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSGetState_reply(ID3D11RasterizerState ** ppRasterizerState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppRasterizerState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_RSGetState_reply(struct npt_cs_decoder *dec,
                                                ID3D11RasterizerState ** ppRasterizerState)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_RSGetState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_RSGetState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_RSGetState));
        return;
    }
/* skip ppRasterizerState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_RSGetState(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D11RasterizerState ** ppRasterizerState,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_RSGetState(ppRasterizerState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_RSGetState_reply(ppRasterizerState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_RSGetState(enc, cmd_flags, object_id, ppRasterizerState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_RSGetState(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11RasterizerState ** ppRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSGetState(ring, 0, object_id, ppRasterizerState, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_RSGetState(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        ID3D11RasterizerState ** ppRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSGetState(ring, NPT_CMD_FLAG_REPLY, object_id, ppRasterizerState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_RSGetState_reply(dec, ppRasterizerState);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::RSGetViewports
 */

struct npt_command_ID3D11DeviceContext_RSGetViewports {
    void *_self;
    UINT * pNumViewports;
    D3D11_VIEWPORT * pViewports;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSGetViewports(const UINT * pNumViewports,const D3D11_VIEWPORT * pViewports)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pNumViewports);
    if (pNumViewports)
        cmd_size += npt_sizeof_UINT(pNumViewports, max_mode);
/* skip pViewports (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_RSGetViewports(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT * pNumViewports,
                                              D3D11_VIEWPORT * pViewports)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_RSGetViewports(pNumViewports,pViewports);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSGetViewports),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pNumViewports))
        npt_encode_UINT(enc, pNumViewports);
/* skip pViewports (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSGetViewports_reply(const UINT * pNumViewports,const D3D11_VIEWPORT * pViewports)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_array_count(pViewports ? *pNumViewports : 0);
    if (pViewports)
        cmd_size += (size_t)(*pNumViewports) * npt_sizeof_D3D11_VIEWPORT(&(const D3D11_VIEWPORT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_RSGetViewports_reply(struct npt_cs_decoder *dec,
                                                    UINT * pNumViewports,
                                                    D3D11_VIEWPORT * pViewports)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_RSGetViewports))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_RSGetViewports reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_RSGetViewports));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pNumViewports)
            npt_decode_UINT(dec, (UINT *)pNumViewports);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pViewports) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D11_VIEWPORT(dec, (D3D11_VIEWPORT *)&pViewports[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D11DeviceContext_RSGetViewports(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT * pNumViewports,
                                              D3D11_VIEWPORT * pViewports,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_RSGetViewports(pNumViewports,pViewports);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_RSGetViewports_reply(pNumViewports,pViewports)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_RSGetViewports(enc, cmd_flags, object_id, pNumViewports, pViewports);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_RSGetViewports(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT * pNumViewports,
                                             D3D11_VIEWPORT * pViewports)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSGetViewports(ring, 0, object_id, pNumViewports, pViewports, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_RSGetViewports(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT * pNumViewports,
                                            D3D11_VIEWPORT * pViewports)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSGetViewports(ring, NPT_CMD_FLAG_REPLY, object_id, pNumViewports, pViewports, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_RSGetViewports_reply(dec, pNumViewports, pViewports);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::RSGetScissorRects
 */

struct npt_command_ID3D11DeviceContext_RSGetScissorRects {
    void *_self;
    UINT * pNumRects;
    D3D11_RECT * pRects;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSGetScissorRects(const UINT * pNumRects,const D3D11_RECT * pRects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pNumRects);
    if (pNumRects)
        cmd_size += npt_sizeof_UINT(pNumRects, max_mode);
/* skip pRects (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_RSGetScissorRects(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 UINT * pNumRects,
                                                 D3D11_RECT * pRects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_RSGetScissorRects(pNumRects,pRects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSGetScissorRects),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pNumRects))
        npt_encode_UINT(enc, pNumRects);
/* skip pRects (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_RSGetScissorRects_reply(const UINT * pNumRects,const D3D11_RECT * pRects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_array_count(pRects ? *pNumRects : 0);
    if (pRects)
        cmd_size += (size_t)(*pNumRects) * npt_sizeof_D3D11_RECT(&(const D3D11_RECT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_RSGetScissorRects_reply(struct npt_cs_decoder *dec,
                                                       UINT * pNumRects,
                                                       D3D11_RECT * pRects)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_RSGetScissorRects))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_RSGetScissorRects reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_RSGetScissorRects));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pNumRects)
            npt_decode_UINT(dec, (UINT *)pNumRects);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pRects) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D11_RECT(dec, (D3D11_RECT *)&pRects[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D11DeviceContext_RSGetScissorRects(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 UINT * pNumRects,
                                                 D3D11_RECT * pRects,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_RSGetScissorRects(pNumRects,pRects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_RSGetScissorRects_reply(pNumRects,pRects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_RSGetScissorRects(enc, cmd_flags, object_id, pNumRects, pRects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_RSGetScissorRects(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT * pNumRects,
                                                D3D11_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSGetScissorRects(ring, 0, object_id, pNumRects, pRects, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_RSGetScissorRects(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               UINT * pNumRects,
                                               D3D11_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_RSGetScissorRects(ring, NPT_CMD_FLAG_REPLY, object_id, pNumRects, pRects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_RSGetScissorRects_reply(dec, pNumRects, pRects);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::HSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_HSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSGetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumViews) * sizeof(uint64_t);  /* ppShaderResourceViews: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_HSGetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_HSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSGetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
if (ppShaderResourceViews) {
    const uint64_t _cnt_ppShaderResourceViews = (uint64_t)(NumViews);
    npt_encode_array_count(enc, _cnt_ppShaderResourceViews);
    for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppShaderResourceViews[_i] = (ID3D11ShaderResourceView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSGetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_HSGetShaderResources_reply(struct npt_cs_decoder *dec,
                                                          ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_HSGetShaderResources))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_HSGetShaderResources reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_HSGetShaderResources));
        return;
    }
/* skip ppShaderResourceViews (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_HSGetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_HSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_HSGetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_HSGetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_HSGetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSGetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_HSGetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSGetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_HSGetShaderResources_reply(dec, ppShaderResourceViews);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::HSGetShader
 */

struct npt_command_ID3D11DeviceContext_HSGetShader {
    void *_self;
    ID3D11HullShader ** ppHullShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppHullShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppHullShader -> *ppHullShader} in the
     * context object table. */
    uint64_t _guest_id_ppHullShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSGetShader(ID3D11HullShader ** ppHullShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppHullShader: guest id */
cmd_size += sizeof(uint64_t) + (size_t)((pNumClassInstances ? *pNumClassInstances : 0)) * sizeof(uint64_t);  /* ppClassInstances: array_count + N guest ids */
    cmd_size += npt_sizeof_simple_pointer(pNumClassInstances);
    if (pNumClassInstances)
        cmd_size += npt_sizeof_UINT(pNumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_HSGetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11HullShader ** ppHullShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_HSGetShader(ppHullShader,ppClassInstances,pNumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSGetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppHullShader = 0;
    if (ppHullShader) {
        _gid_ppHullShader = npt_com_allocate_next_id();
        *ppHullShader = (ID3D11HullShader *)(uintptr_t)_gid_ppHullShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppHullShader);
}
if (ppClassInstances) {
    const uint64_t _cnt_ppClassInstances = (uint64_t)((pNumClassInstances ? *pNumClassInstances : 0));
    npt_encode_array_count(enc, _cnt_ppClassInstances);
    for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppClassInstances[_i] = (ID3D11ClassInstance *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
    if (npt_encode_simple_pointer(enc, pNumClassInstances))
        npt_encode_UINT(enc, pNumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSGetShader_reply(ID3D11HullShader ** ppHullShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppHullShader (guest-allocated id, registered host-side; not in reply) */
/* skip ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_HSGetShader_reply(struct npt_cs_decoder *dec,
                                                 ID3D11HullShader ** ppHullShader,
                                                 ID3D11ClassInstance ** ppClassInstances,
                                                 UINT * pNumClassInstances)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_HSGetShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_HSGetShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_HSGetShader));
        return;
    }
/* skip ppHullShader (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppClassInstances (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pNumClassInstances)
            npt_decode_UINT(dec, (UINT *)pNumClassInstances);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_HSGetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11HullShader ** ppHullShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_HSGetShader(ppHullShader,ppClassInstances,pNumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_HSGetShader_reply(ppHullShader,ppClassInstances,pNumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_HSGetShader(enc, cmd_flags, object_id, ppHullShader, ppClassInstances, pNumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_HSGetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11HullShader ** ppHullShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSGetShader(ring, 0, object_id, ppHullShader, ppClassInstances, pNumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_HSGetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11HullShader ** ppHullShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSGetShader(ring, NPT_CMD_FLAG_REPLY, object_id, ppHullShader, ppClassInstances, pNumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_HSGetShader_reply(dec, ppHullShader, ppClassInstances, pNumClassInstances);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::HSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_HSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSGetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumSamplers) * sizeof(uint64_t);  /* ppSamplers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_HSGetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_HSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSGetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
if (ppSamplers) {
    const uint64_t _cnt_ppSamplers = (uint64_t)(NumSamplers);
    npt_encode_array_count(enc, _cnt_ppSamplers);
    for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppSamplers[_i] = (ID3D11SamplerState *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSGetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_HSGetSamplers_reply(struct npt_cs_decoder *dec,
                                                   ID3D11SamplerState ** ppSamplers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_HSGetSamplers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_HSGetSamplers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_HSGetSamplers));
        return;
    }
/* skip ppSamplers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_HSGetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_HSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_HSGetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_HSGetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_HSGetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSGetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_HSGetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSGetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_HSGetSamplers_reply(dec, ppSamplers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::HSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_HSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSGetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_HSGetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_HSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSGetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_HSGetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_HSGetConstantBuffers_reply(struct npt_cs_decoder *dec,
                                                          ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_HSGetConstantBuffers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_HSGetConstantBuffers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_HSGetConstantBuffers));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_HSGetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_HSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_HSGetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_HSGetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_HSGetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSGetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_HSGetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_HSGetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_HSGetConstantBuffers_reply(dec, ppConstantBuffers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_DSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSGetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumViews) * sizeof(uint64_t);  /* ppShaderResourceViews: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DSGetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSGetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
if (ppShaderResourceViews) {
    const uint64_t _cnt_ppShaderResourceViews = (uint64_t)(NumViews);
    npt_encode_array_count(enc, _cnt_ppShaderResourceViews);
    for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppShaderResourceViews[_i] = (ID3D11ShaderResourceView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSGetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_DSGetShaderResources_reply(struct npt_cs_decoder *dec,
                                                          ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_DSGetShaderResources))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_DSGetShaderResources reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_DSGetShaderResources));
        return;
    }
/* skip ppShaderResourceViews (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_DSGetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DSGetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DSGetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DSGetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSGetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DSGetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSGetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_DSGetShaderResources_reply(dec, ppShaderResourceViews);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DSGetShader
 */

struct npt_command_ID3D11DeviceContext_DSGetShader {
    void *_self;
    ID3D11DomainShader ** ppDomainShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppDomainShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDomainShader -> *ppDomainShader} in the
     * context object table. */
    uint64_t _guest_id_ppDomainShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSGetShader(ID3D11DomainShader ** ppDomainShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppDomainShader: guest id */
cmd_size += sizeof(uint64_t) + (size_t)((pNumClassInstances ? *pNumClassInstances : 0)) * sizeof(uint64_t);  /* ppClassInstances: array_count + N guest ids */
    cmd_size += npt_sizeof_simple_pointer(pNumClassInstances);
    if (pNumClassInstances)
        cmd_size += npt_sizeof_UINT(pNumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DSGetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11DomainShader ** ppDomainShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DSGetShader(ppDomainShader,ppClassInstances,pNumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSGetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppDomainShader = 0;
    if (ppDomainShader) {
        _gid_ppDomainShader = npt_com_allocate_next_id();
        *ppDomainShader = (ID3D11DomainShader *)(uintptr_t)_gid_ppDomainShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppDomainShader);
}
if (ppClassInstances) {
    const uint64_t _cnt_ppClassInstances = (uint64_t)((pNumClassInstances ? *pNumClassInstances : 0));
    npt_encode_array_count(enc, _cnt_ppClassInstances);
    for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppClassInstances[_i] = (ID3D11ClassInstance *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
    if (npt_encode_simple_pointer(enc, pNumClassInstances))
        npt_encode_UINT(enc, pNumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSGetShader_reply(ID3D11DomainShader ** ppDomainShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppDomainShader (guest-allocated id, registered host-side; not in reply) */
/* skip ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_DSGetShader_reply(struct npt_cs_decoder *dec,
                                                 ID3D11DomainShader ** ppDomainShader,
                                                 ID3D11ClassInstance ** ppClassInstances,
                                                 UINT * pNumClassInstances)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_DSGetShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_DSGetShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_DSGetShader));
        return;
    }
/* skip ppDomainShader (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppClassInstances (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pNumClassInstances)
            npt_decode_UINT(dec, (UINT *)pNumClassInstances);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_DSGetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11DomainShader ** ppDomainShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DSGetShader(ppDomainShader,ppClassInstances,pNumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DSGetShader_reply(ppDomainShader,ppClassInstances,pNumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DSGetShader(enc, cmd_flags, object_id, ppDomainShader, ppClassInstances, pNumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DSGetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11DomainShader ** ppDomainShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSGetShader(ring, 0, object_id, ppDomainShader, ppClassInstances, pNumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DSGetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11DomainShader ** ppDomainShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSGetShader(ring, NPT_CMD_FLAG_REPLY, object_id, ppDomainShader, ppClassInstances, pNumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_DSGetShader_reply(dec, ppDomainShader, ppClassInstances, pNumClassInstances);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_DSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSGetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumSamplers) * sizeof(uint64_t);  /* ppSamplers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DSGetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSGetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
if (ppSamplers) {
    const uint64_t _cnt_ppSamplers = (uint64_t)(NumSamplers);
    npt_encode_array_count(enc, _cnt_ppSamplers);
    for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppSamplers[_i] = (ID3D11SamplerState *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSGetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_DSGetSamplers_reply(struct npt_cs_decoder *dec,
                                                   ID3D11SamplerState ** ppSamplers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_DSGetSamplers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_DSGetSamplers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_DSGetSamplers));
        return;
    }
/* skip ppSamplers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_DSGetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DSGetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DSGetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DSGetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSGetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DSGetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSGetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_DSGetSamplers_reply(dec, ppSamplers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::DSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_DSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSGetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_DSGetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_DSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSGetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_DSGetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_DSGetConstantBuffers_reply(struct npt_cs_decoder *dec,
                                                          ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_DSGetConstantBuffers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_DSGetConstantBuffers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_DSGetConstantBuffers));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_DSGetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_DSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_DSGetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_DSGetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_DSGetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSGetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_DSGetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_DSGetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_DSGetConstantBuffers_reply(dec, ppConstantBuffers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_CSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetShaderResources(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumViews) * sizeof(uint64_t);  /* ppShaderResourceViews: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetShaderResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetShaderResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
if (ppShaderResourceViews) {
    const uint64_t _cnt_ppShaderResourceViews = (uint64_t)(NumViews);
    npt_encode_array_count(enc, _cnt_ppShaderResourceViews);
    for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppShaderResourceViews[_i] = (ID3D11ShaderResourceView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetShaderResources_reply(const UINT StartSlot,const UINT NumViews,ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_CSGetShaderResources_reply(struct npt_cs_decoder *dec,
                                                          ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_CSGetShaderResources))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_CSGetShaderResources reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_CSGetShaderResources));
        return;
    }
/* skip ppShaderResourceViews (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_CSGetShaderResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumViews,
                                                    ID3D11ShaderResourceView ** ppShaderResourceViews,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSGetShaderResources(StartSlot,NumViews,ppShaderResourceViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSGetShaderResources_reply(StartSlot,NumViews,ppShaderResourceViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSGetShaderResources(enc, cmd_flags, object_id, StartSlot, NumViews, ppShaderResourceViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSGetShaderResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumViews,
                                                   ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetShaderResources(ring, 0, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSGetShaderResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  ID3D11ShaderResourceView ** ppShaderResourceViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetShaderResources(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, ppShaderResourceViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_CSGetShaderResources_reply(dec, ppShaderResourceViews);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSGetUnorderedAccessViews
 */

struct npt_command_ID3D11DeviceContext_CSGetUnorderedAccessViews {
    void *_self;
    UINT StartSlot;
    UINT NumUAVs;
    ID3D11UnorderedAccessView ** ppUnorderedAccessViews;
    /* Shadow: per-element guest-allocated ids for ppUnorderedAccessViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppUnorderedAccessViews;
    uint32_t _guest_id_count_ppUnorderedAccessViews;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetUnorderedAccessViews(const UINT StartSlot,const UINT NumUAVs,ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumUAVs, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumUAVs) * sizeof(uint64_t);  /* ppUnorderedAccessViews: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetUnorderedAccessViews(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         UINT StartSlot,
                                                         UINT NumUAVs,
                                                         ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSGetUnorderedAccessViews(StartSlot,NumUAVs,ppUnorderedAccessViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetUnorderedAccessViews),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumUAVs);
if (ppUnorderedAccessViews) {
    const uint64_t _cnt_ppUnorderedAccessViews = (uint64_t)(NumUAVs);
    npt_encode_array_count(enc, _cnt_ppUnorderedAccessViews);
    for (uint64_t _i = 0; _i < _cnt_ppUnorderedAccessViews; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppUnorderedAccessViews[_i] = (ID3D11UnorderedAccessView *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetUnorderedAccessViews_reply(const UINT StartSlot,const UINT NumUAVs,ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumUAVs (input only) */
/* skip ppUnorderedAccessViews (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_CSGetUnorderedAccessViews_reply(struct npt_cs_decoder *dec,
                                                               ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_CSGetUnorderedAccessViews))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_CSGetUnorderedAccessViews reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_CSGetUnorderedAccessViews));
        return;
    }
/* skip ppUnorderedAccessViews (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_CSGetUnorderedAccessViews(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         UINT StartSlot,
                                                         UINT NumUAVs,
                                                         ID3D11UnorderedAccessView ** ppUnorderedAccessViews,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSGetUnorderedAccessViews(StartSlot,NumUAVs,ppUnorderedAccessViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSGetUnorderedAccessViews_reply(StartSlot,NumUAVs,ppUnorderedAccessViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSGetUnorderedAccessViews(enc, cmd_flags, object_id, StartSlot, NumUAVs, ppUnorderedAccessViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSGetUnorderedAccessViews(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        UINT StartSlot,
                                                        UINT NumUAVs,
                                                        ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetUnorderedAccessViews(ring, 0, object_id, StartSlot, NumUAVs, ppUnorderedAccessViews, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSGetUnorderedAccessViews(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       UINT StartSlot,
                                                       UINT NumUAVs,
                                                       ID3D11UnorderedAccessView ** ppUnorderedAccessViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetUnorderedAccessViews(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumUAVs, ppUnorderedAccessViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_CSGetUnorderedAccessViews_reply(dec, ppUnorderedAccessViews);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSGetShader
 */

struct npt_command_ID3D11DeviceContext_CSGetShader {
    void *_self;
    ID3D11ComputeShader ** ppComputeShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppComputeShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppComputeShader -> *ppComputeShader} in the
     * context object table. */
    uint64_t _guest_id_ppComputeShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetShader(ID3D11ComputeShader ** ppComputeShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppComputeShader: guest id */
cmd_size += sizeof(uint64_t) + (size_t)((pNumClassInstances ? *pNumClassInstances : 0)) * sizeof(uint64_t);  /* ppClassInstances: array_count + N guest ids */
    cmd_size += npt_sizeof_simple_pointer(pNumClassInstances);
    if (pNumClassInstances)
        cmd_size += npt_sizeof_UINT(pNumClassInstances, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11ComputeShader ** ppComputeShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSGetShader(ppComputeShader,ppClassInstances,pNumClassInstances);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppComputeShader = 0;
    if (ppComputeShader) {
        _gid_ppComputeShader = npt_com_allocate_next_id();
        *ppComputeShader = (ID3D11ComputeShader *)(uintptr_t)_gid_ppComputeShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppComputeShader);
}
if (ppClassInstances) {
    const uint64_t _cnt_ppClassInstances = (uint64_t)((pNumClassInstances ? *pNumClassInstances : 0));
    npt_encode_array_count(enc, _cnt_ppClassInstances);
    for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppClassInstances[_i] = (ID3D11ClassInstance *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
    if (npt_encode_simple_pointer(enc, pNumClassInstances))
        npt_encode_UINT(enc, pNumClassInstances);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetShader_reply(ID3D11ComputeShader ** ppComputeShader,ID3D11ClassInstance ** ppClassInstances,const UINT * pNumClassInstances)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppComputeShader (guest-allocated id, registered host-side; not in reply) */
/* skip ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_CSGetShader_reply(struct npt_cs_decoder *dec,
                                                 ID3D11ComputeShader ** ppComputeShader,
                                                 ID3D11ClassInstance ** ppClassInstances,
                                                 UINT * pNumClassInstances)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_CSGetShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_CSGetShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_CSGetShader));
        return;
    }
/* skip ppComputeShader (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppClassInstances (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pNumClassInstances)
            npt_decode_UINT(dec, (UINT *)pNumClassInstances);
    }
}

static inline void
npt_submit_ID3D11DeviceContext_CSGetShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11ComputeShader ** ppComputeShader,
                                           ID3D11ClassInstance ** ppClassInstances,
                                           UINT * pNumClassInstances,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSGetShader(ppComputeShader,ppClassInstances,pNumClassInstances);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSGetShader_reply(ppComputeShader,ppClassInstances,pNumClassInstances)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSGetShader(enc, cmd_flags, object_id, ppComputeShader, ppClassInstances, pNumClassInstances);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSGetShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11ComputeShader ** ppComputeShader,
                                          ID3D11ClassInstance ** ppClassInstances,
                                          UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetShader(ring, 0, object_id, ppComputeShader, ppClassInstances, pNumClassInstances, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSGetShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11ComputeShader ** ppComputeShader,
                                         ID3D11ClassInstance ** ppClassInstances,
                                         UINT * pNumClassInstances)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetShader(ring, NPT_CMD_FLAG_REPLY, object_id, ppComputeShader, ppClassInstances, pNumClassInstances, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_CSGetShader_reply(dec, ppComputeShader, ppClassInstances, pNumClassInstances);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_CSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetSamplers(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSamplers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumSamplers) * sizeof(uint64_t);  /* ppSamplers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetSamplers(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetSamplers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumSamplers);
if (ppSamplers) {
    const uint64_t _cnt_ppSamplers = (uint64_t)(NumSamplers);
    npt_encode_array_count(enc, _cnt_ppSamplers);
    for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppSamplers[_i] = (ID3D11SamplerState *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetSamplers_reply(const UINT StartSlot,const UINT NumSamplers,ID3D11SamplerState ** ppSamplers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumSamplers (input only) */
/* skip ppSamplers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_CSGetSamplers_reply(struct npt_cs_decoder *dec,
                                                   ID3D11SamplerState ** ppSamplers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_CSGetSamplers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_CSGetSamplers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_CSGetSamplers));
        return;
    }
/* skip ppSamplers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_CSGetSamplers(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT StartSlot,
                                             UINT NumSamplers,
                                             ID3D11SamplerState ** ppSamplers,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSGetSamplers(StartSlot,NumSamplers,ppSamplers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSGetSamplers_reply(StartSlot,NumSamplers,ppSamplers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSGetSamplers(enc, cmd_flags, object_id, StartSlot, NumSamplers, ppSamplers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSGetSamplers(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT StartSlot,
                                            UINT NumSamplers,
                                            ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetSamplers(ring, 0, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSGetSamplers(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT StartSlot,
                                           UINT NumSamplers,
                                           ID3D11SamplerState ** ppSamplers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetSamplers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumSamplers, ppSamplers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_CSGetSamplers_reply(dec, ppSamplers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::CSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_CSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetConstantBuffers(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetConstantBuffers(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_CSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetConstantBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_CSGetConstantBuffers_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_CSGetConstantBuffers_reply(struct npt_cs_decoder *dec,
                                                          ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_CSGetConstantBuffers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_CSGetConstantBuffers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_CSGetConstantBuffers));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_CSGetConstantBuffers(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_CSGetConstantBuffers(StartSlot,NumBuffers,ppConstantBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_CSGetConstantBuffers_reply(StartSlot,NumBuffers,ppConstantBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_CSGetConstantBuffers(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_CSGetConstantBuffers(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StartSlot,
                                                   UINT NumBuffers,
                                                   ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetConstantBuffers(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_CSGetConstantBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumBuffers,
                                                  ID3D11Buffer ** ppConstantBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_CSGetConstantBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext_CSGetConstantBuffers_reply(dec, ppConstantBuffers);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::ClearState
 */

struct npt_command_ID3D11DeviceContext_ClearState {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearState(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_ClearState(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_ClearState();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_ClearState_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_ClearState(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_ClearState();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_ClearState_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_ClearState(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_ClearState(struct npt_ring *ring,
                                         npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearState(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_ClearState(struct npt_ring *ring,
                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_ClearState(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::Flush
 */

struct npt_command_ID3D11DeviceContext_Flush {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_Flush(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_Flush(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_Flush();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Flush),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_Flush_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext_Flush(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_Flush();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_Flush_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_Flush(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_Flush(struct npt_ring *ring,
                                    npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Flush(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D11DeviceContext_Flush(struct npt_ring *ring,
                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_Flush(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext::GetType
 */

struct npt_command_ID3D11DeviceContext_GetType {
    void *_self;
    D3D11_DEVICE_CONTEXT_TYPE ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetType(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GetType(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GetType();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetType),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetType_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_GetType_reply(struct npt_cs_decoder *dec,
                                             D3D11_DEVICE_CONTEXT_TYPE *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_GetType))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_GetType reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_GetType));
        if (ret) *ret = (D3D11_DEVICE_CONTEXT_TYPE)0;
        return;
    }
    if (ret) *ret = (D3D11_DEVICE_CONTEXT_TYPE)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11DeviceContext_GetType(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GetType();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GetType_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GetType(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GetType(struct npt_ring *ring,
                                      npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetType(ring, 0, object_id, &submit);
}

static inline D3D11_DEVICE_CONTEXT_TYPE
npt_call_ID3D11DeviceContext_GetType(struct npt_ring *ring,
                                     npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetType(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D11_DEVICE_CONTEXT_TYPE _ret;
        npt_decode_ID3D11DeviceContext_GetType_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D11_DEVICE_CONTEXT_TYPE)0;
    }
}

/*
 * ID3D11DeviceContext::GetContextFlags
 */

struct npt_command_ID3D11DeviceContext_GetContextFlags {
    void *_self;
    UINT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetContextFlags(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_GetContextFlags(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_GetContextFlags();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetContextFlags),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_GetContextFlags_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_GetContextFlags_reply(struct npt_cs_decoder *dec,
                                                     UINT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_GetContextFlags))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_GetContextFlags reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_GetContextFlags));
        if (ret) *ret = (UINT)0;
        return;
    }
    if (ret) *ret = (UINT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11DeviceContext_GetContextFlags(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_GetContextFlags();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_GetContextFlags_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_GetContextFlags(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_GetContextFlags(struct npt_ring *ring,
                                              npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetContextFlags(ring, 0, object_id, &submit);
}

static inline UINT
npt_call_ID3D11DeviceContext_GetContextFlags(struct npt_ring *ring,
                                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_GetContextFlags(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT _ret;
        npt_decode_ID3D11DeviceContext_GetContextFlags_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT)0;
    }
}

/*
 * ID3D11DeviceContext::FinishCommandList
 */

struct npt_command_ID3D11DeviceContext_FinishCommandList {
    void *_self;
    BOOL RestoreDeferredContextState;
    ID3D11CommandList ** ppCommandList;
    /* Shadow: the guest-allocated id for ppCommandList arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandList -> *ppCommandList} in the
     * context object table. */
    uint64_t _guest_id_ppCommandList;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext_FinishCommandList(const BOOL RestoreDeferredContextState,ID3D11CommandList ** ppCommandList)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_BOOL(&RestoreDeferredContextState, max_mode);
cmd_size += sizeof(uint64_t);  /* ppCommandList: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext_FinishCommandList(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 BOOL RestoreDeferredContextState,
                                                 ID3D11CommandList ** ppCommandList)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext_FinishCommandList(RestoreDeferredContextState,ppCommandList);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_FinishCommandList),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_BOOL(enc, &RestoreDeferredContextState);
{
    uint64_t _gid_ppCommandList = 0;
    if (ppCommandList) {
        _gid_ppCommandList = npt_com_allocate_next_id();
        *ppCommandList = (ID3D11CommandList *)(uintptr_t)_gid_ppCommandList;
    }
    npt_encode_uint64_t(enc, &_gid_ppCommandList);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext_FinishCommandList_reply(const BOOL RestoreDeferredContextState,ID3D11CommandList ** ppCommandList)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RestoreDeferredContextState (input only) */
/* skip ppCommandList (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext_FinishCommandList_reply(struct npt_cs_decoder *dec,
                                                       ID3D11CommandList ** ppCommandList,
                                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                                  NPT_METHOD_ID3D11DeviceContext_FinishCommandList))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext_FinishCommandList reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                       NPT_METHOD_ID3D11DeviceContext_FinishCommandList));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppCommandList (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext_FinishCommandList(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 BOOL RestoreDeferredContextState,
                                                 ID3D11CommandList ** ppCommandList,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext_FinishCommandList(RestoreDeferredContextState,ppCommandList);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext_FinishCommandList_reply(RestoreDeferredContextState,ppCommandList)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext_FinishCommandList(enc, cmd_flags, object_id, RestoreDeferredContextState, ppCommandList);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext_FinishCommandList(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                BOOL RestoreDeferredContextState,
                                                ID3D11CommandList ** ppCommandList)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_FinishCommandList(ring, 0, object_id, RestoreDeferredContextState, ppCommandList, &submit);
}

static inline HRESULT
npt_call_ID3D11DeviceContext_FinishCommandList(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               BOOL RestoreDeferredContextState,
                                               ID3D11CommandList ** ppCommandList)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext_FinishCommandList(ring, NPT_CMD_FLAG_REPLY, object_id, RestoreDeferredContextState, ppCommandList, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11DeviceContext_FinishCommandList_reply(dec, ppCommandList, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11DeviceContext1::CopySubresourceRegion1
 */

struct npt_command_ID3D11DeviceContext1_CopySubresourceRegion1 {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    UINT DstX;
    UINT DstY;
    UINT DstZ;
    ID3D11Resource * pSrcResource;
    UINT SrcSubresource;
    const D3D11_BOX * pSrcBox;
    UINT CopyFlags;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_CopySubresourceRegion1(const ID3D11Resource * pDstResource,const UINT DstSubresource,const UINT DstX,const UINT DstY,const UINT DstZ,const ID3D11Resource * pSrcResource,const UINT SrcSubresource,const D3D11_BOX * pSrcBox,const UINT CopyFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DstSubresource, max_mode);
    cmd_size += npt_sizeof_UINT(&DstX, max_mode);
    cmd_size += npt_sizeof_UINT(&DstY, max_mode);
    cmd_size += npt_sizeof_UINT(&DstZ, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&SrcSubresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSrcBox);
    if (pSrcBox)
        cmd_size += npt_sizeof_D3D11_BOX(pSrcBox, max_mode);
    cmd_size += npt_sizeof_UINT(&CopyFlags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_CopySubresourceRegion1(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       ID3D11Resource * pDstResource,
                                                       UINT DstSubresource,
                                                       UINT DstX,
                                                       UINT DstY,
                                                       UINT DstZ,
                                                       ID3D11Resource * pSrcResource,
                                                       UINT SrcSubresource,
                                                       const D3D11_BOX * pSrcBox,
                                                       UINT CopyFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_CopySubresourceRegion1(pDstResource,DstSubresource,DstX,DstY,DstZ,pSrcResource,SrcSubresource,pSrcBox,CopyFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_CopySubresourceRegion1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_UINT(enc, &DstSubresource);
    npt_encode_UINT(enc, &DstX);
    npt_encode_UINT(enc, &DstY);
    npt_encode_UINT(enc, &DstZ);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcResource));
    npt_encode_UINT(enc, &SrcSubresource);
    if (npt_encode_simple_pointer(enc, pSrcBox))
        npt_encode_D3D11_BOX(enc, pSrcBox);
    npt_encode_UINT(enc, &CopyFlags);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_CopySubresourceRegion1_reply(const ID3D11Resource * pDstResource,const UINT DstSubresource,const UINT DstX,const UINT DstY,const UINT DstZ,const ID3D11Resource * pSrcResource,const UINT SrcSubresource,const D3D11_BOX * pSrcBox,const UINT CopyFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip DstSubresource (input only) */
/* skip DstX (input only) */
/* skip DstY (input only) */
/* skip DstZ (input only) */
/* skip pSrcResource (input only) */
/* skip SrcSubresource (input only) */
/* skip pSrcBox (input only) */
/* skip CopyFlags (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_CopySubresourceRegion1(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       ID3D11Resource * pDstResource,
                                                       UINT DstSubresource,
                                                       UINT DstX,
                                                       UINT DstY,
                                                       UINT DstZ,
                                                       ID3D11Resource * pSrcResource,
                                                       UINT SrcSubresource,
                                                       const D3D11_BOX * pSrcBox,
                                                       UINT CopyFlags,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_CopySubresourceRegion1(pDstResource,DstSubresource,DstX,DstY,DstZ,pSrcResource,SrcSubresource,pSrcBox,CopyFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_CopySubresourceRegion1_reply(pDstResource,DstSubresource,DstX,DstY,DstZ,pSrcResource,SrcSubresource,pSrcBox,CopyFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_CopySubresourceRegion1(enc, cmd_flags, object_id, pDstResource, DstSubresource, DstX, DstY, DstZ, pSrcResource, SrcSubresource, pSrcBox, CopyFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_CopySubresourceRegion1(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      ID3D11Resource * pDstResource,
                                                      UINT DstSubresource,
                                                      UINT DstX,
                                                      UINT DstY,
                                                      UINT DstZ,
                                                      ID3D11Resource * pSrcResource,
                                                      UINT SrcSubresource,
                                                      const D3D11_BOX * pSrcBox,
                                                      UINT CopyFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_CopySubresourceRegion1(ring, 0, object_id, pDstResource, DstSubresource, DstX, DstY, DstZ, pSrcResource, SrcSubresource, pSrcBox, CopyFlags, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_CopySubresourceRegion1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3D11Resource * pDstResource,
                                                     UINT DstSubresource,
                                                     UINT DstX,
                                                     UINT DstY,
                                                     UINT DstZ,
                                                     ID3D11Resource * pSrcResource,
                                                     UINT SrcSubresource,
                                                     const D3D11_BOX * pSrcBox,
                                                     UINT CopyFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_CopySubresourceRegion1(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, DstSubresource, DstX, DstY, DstZ, pSrcResource, SrcSubresource, pSrcBox, CopyFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::UpdateSubresource1
 */

struct npt_command_ID3D11DeviceContext1_UpdateSubresource1 {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    const D3D11_BOX * pDstBox;
    const void * pSrcData;
    UINT SrcRowPitch;
    UINT SrcDepthPitch;
    UINT CopyFlags;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_UpdateSubresource1(const ID3D11Resource * pDstResource,const UINT DstSubresource,const D3D11_BOX * pDstBox,const void * pSrcData,const UINT SrcRowPitch,const UINT SrcDepthPitch,const UINT CopyFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DstSubresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDstBox);
    if (pDstBox)
        cmd_size += npt_sizeof_D3D11_BOX(pDstBox, max_mode);
    /* ERROR: pSrcData (void) is not serializable */
    return 0;
    cmd_size += npt_sizeof_UINT(&SrcRowPitch, max_mode);
    cmd_size += npt_sizeof_UINT(&SrcDepthPitch, max_mode);
    cmd_size += npt_sizeof_UINT(&CopyFlags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_UpdateSubresource1(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11Resource * pDstResource,
                                                   UINT DstSubresource,
                                                   const D3D11_BOX * pDstBox,
                                                   const void * pSrcData,
                                                   UINT SrcRowPitch,
                                                   UINT SrcDepthPitch,
                                                   UINT CopyFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_UpdateSubresource1(pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch,CopyFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_UpdateSubresource1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_UINT(enc, &DstSubresource);
    if (npt_encode_simple_pointer(enc, pDstBox))
        npt_encode_D3D11_BOX(enc, pDstBox);
    /* ERROR: pSrcData (void) is not serializable */
    return;
    npt_encode_UINT(enc, &SrcRowPitch);
    npt_encode_UINT(enc, &SrcDepthPitch);
    npt_encode_UINT(enc, &CopyFlags);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_UpdateSubresource1_reply(const ID3D11Resource * pDstResource,const UINT DstSubresource,const D3D11_BOX * pDstBox,const void * pSrcData,const UINT SrcRowPitch,const UINT SrcDepthPitch,const UINT CopyFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip DstSubresource (input only) */
/* skip pDstBox (input only) */
/* skip pSrcData (input only) */
/* skip SrcRowPitch (input only) */
/* skip SrcDepthPitch (input only) */
/* skip CopyFlags (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_UpdateSubresource1(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11Resource * pDstResource,
                                                   UINT DstSubresource,
                                                   const D3D11_BOX * pDstBox,
                                                   const void * pSrcData,
                                                   UINT SrcRowPitch,
                                                   UINT SrcDepthPitch,
                                                   UINT CopyFlags,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_UpdateSubresource1(pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch,CopyFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_UpdateSubresource1_reply(pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch,CopyFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_UpdateSubresource1(enc, cmd_flags, object_id, pDstResource, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch, CopyFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_UpdateSubresource1(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D11Resource * pDstResource,
                                                  UINT DstSubresource,
                                                  const D3D11_BOX * pDstBox,
                                                  const void * pSrcData,
                                                  UINT SrcRowPitch,
                                                  UINT SrcDepthPitch,
                                                  UINT CopyFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_UpdateSubresource1(ring, 0, object_id, pDstResource, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch, CopyFlags, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_UpdateSubresource1(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pDstResource,
                                                 UINT DstSubresource,
                                                 const D3D11_BOX * pDstBox,
                                                 const void * pSrcData,
                                                 UINT SrcRowPitch,
                                                 UINT SrcDepthPitch,
                                                 UINT CopyFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_UpdateSubresource1(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch, CopyFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::DiscardResource
 */

struct npt_command_ID3D11DeviceContext1_DiscardResource {
    void *_self;
    ID3D11Resource * pResource;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DiscardResource(const ID3D11Resource * pResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_DiscardResource(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11Resource * pResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_DiscardResource(pResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DiscardResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DiscardResource_reply(const ID3D11Resource * pResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_DiscardResource(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11Resource * pResource,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_DiscardResource(pResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_DiscardResource_reply(pResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_DiscardResource(enc, cmd_flags, object_id, pResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_DiscardResource(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Resource * pResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DiscardResource(ring, 0, object_id, pResource, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_DiscardResource(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11Resource * pResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DiscardResource(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::DiscardView
 */

struct npt_command_ID3D11DeviceContext1_DiscardView {
    void *_self;
    ID3D11View * pResourceView;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DiscardView(const ID3D11View * pResourceView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_DiscardView(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11View * pResourceView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_DiscardView(pResourceView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DiscardView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResourceView));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DiscardView_reply(const ID3D11View * pResourceView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResourceView (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_DiscardView(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11View * pResourceView,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_DiscardView(pResourceView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_DiscardView_reply(pResourceView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_DiscardView(enc, cmd_flags, object_id, pResourceView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_DiscardView(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D11View * pResourceView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DiscardView(ring, 0, object_id, pResourceView, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_DiscardView(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11View * pResourceView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DiscardView(ring, NPT_CMD_FLAG_REPLY, object_id, pResourceView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::VSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_VSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_VSSetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_VSSetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_VSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_VSSetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pFirstConstant) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pFirstConstant, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pNumConstants) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pNumConstants, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_VSSetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
/* skip pFirstConstant (input only) */
/* skip pNumConstants (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_VSSetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_VSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_VSSetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_VSSetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_VSSetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     const UINT * pFirstConstant,
                                                     const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_VSSetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_VSSetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    const UINT * pFirstConstant,
                                                    const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_VSSetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::HSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_HSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_HSSetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_HSSetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_HSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_HSSetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pFirstConstant) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pFirstConstant, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pNumConstants) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pNumConstants, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_HSSetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
/* skip pFirstConstant (input only) */
/* skip pNumConstants (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_HSSetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_HSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_HSSetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_HSSetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_HSSetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     const UINT * pFirstConstant,
                                                     const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_HSSetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_HSSetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    const UINT * pFirstConstant,
                                                    const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_HSSetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::DSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_DSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DSSetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_DSSetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_DSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DSSetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pFirstConstant) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pFirstConstant, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pNumConstants) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pNumConstants, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DSSetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
/* skip pFirstConstant (input only) */
/* skip pNumConstants (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_DSSetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_DSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_DSSetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_DSSetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_DSSetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     const UINT * pFirstConstant,
                                                     const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DSSetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_DSSetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    const UINT * pFirstConstant,
                                                    const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DSSetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::GSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_GSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_GSSetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_GSSetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_GSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_GSSetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pFirstConstant) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pFirstConstant, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pNumConstants) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pNumConstants, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_GSSetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
/* skip pFirstConstant (input only) */
/* skip pNumConstants (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_GSSetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_GSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_GSSetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_GSSetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_GSSetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     const UINT * pFirstConstant,
                                                     const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_GSSetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_GSSetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    const UINT * pFirstConstant,
                                                    const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_GSSetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::PSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_PSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_PSSetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_PSSetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_PSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_PSSetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pFirstConstant) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pFirstConstant, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pNumConstants) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pNumConstants, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_PSSetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
/* skip pFirstConstant (input only) */
/* skip pNumConstants (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_PSSetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_PSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_PSSetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_PSSetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_PSSetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     const UINT * pFirstConstant,
                                                     const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_PSSetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_PSSetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    const UINT * pFirstConstant,
                                                    const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_PSSetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::CSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_CSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_CSSetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(NumBuffers);
    cmd_size += sizeof(uint64_t) * NumBuffers;
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_CSSetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_CSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_CSSetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
    if (ppConstantBuffers) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)NumBuffers; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppConstantBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pFirstConstant) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pFirstConstant, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pNumConstants) {
        npt_encode_array_count(enc, NumBuffers);
        npt_encode_UINT_array(enc, pNumConstants, NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_CSSetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (input only) */
/* skip pFirstConstant (input only) */
/* skip pNumConstants (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_CSSetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      const UINT * pFirstConstant,
                                                      const UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_CSSetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_CSSetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_CSSetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_CSSetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     const UINT * pFirstConstant,
                                                     const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_CSSetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_CSSetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    const UINT * pFirstConstant,
                                                    const UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_CSSetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::VSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_VSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_VSGetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_VSGetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_VSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_VSGetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_VSGetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext1_VSGetConstantBuffers1_reply(struct npt_cs_decoder *dec,
                                                            ID3D11Buffer ** ppConstantBuffers,
                                                            UINT * pFirstConstant,
                                                            UINT * pNumConstants)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                                  NPT_METHOD_ID3D11DeviceContext1_VSGetConstantBuffers1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext1_VSGetConstantBuffers1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                       NPT_METHOD_ID3D11DeviceContext1_VSGetConstantBuffers1));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pFirstConstant)
            npt_decode_UINT_array(dec, (UINT *)pFirstConstant, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pNumConstants)
            npt_decode_UINT_array(dec, (UINT *)pNumConstants, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_ID3D11DeviceContext1_VSGetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_VSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_VSGetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_VSGetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_VSGetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     UINT * pFirstConstant,
                                                     UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_VSGetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_VSGetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    UINT * pFirstConstant,
                                                    UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_VSGetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext1_VSGetConstantBuffers1_reply(dec, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::HSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_HSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_HSGetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_HSGetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_HSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_HSGetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_HSGetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext1_HSGetConstantBuffers1_reply(struct npt_cs_decoder *dec,
                                                            ID3D11Buffer ** ppConstantBuffers,
                                                            UINT * pFirstConstant,
                                                            UINT * pNumConstants)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                                  NPT_METHOD_ID3D11DeviceContext1_HSGetConstantBuffers1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext1_HSGetConstantBuffers1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                       NPT_METHOD_ID3D11DeviceContext1_HSGetConstantBuffers1));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pFirstConstant)
            npt_decode_UINT_array(dec, (UINT *)pFirstConstant, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pNumConstants)
            npt_decode_UINT_array(dec, (UINT *)pNumConstants, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_ID3D11DeviceContext1_HSGetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_HSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_HSGetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_HSGetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_HSGetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     UINT * pFirstConstant,
                                                     UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_HSGetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_HSGetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    UINT * pFirstConstant,
                                                    UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_HSGetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext1_HSGetConstantBuffers1_reply(dec, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::DSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_DSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DSGetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_DSGetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_DSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DSGetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DSGetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext1_DSGetConstantBuffers1_reply(struct npt_cs_decoder *dec,
                                                            ID3D11Buffer ** ppConstantBuffers,
                                                            UINT * pFirstConstant,
                                                            UINT * pNumConstants)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                                  NPT_METHOD_ID3D11DeviceContext1_DSGetConstantBuffers1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext1_DSGetConstantBuffers1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                       NPT_METHOD_ID3D11DeviceContext1_DSGetConstantBuffers1));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pFirstConstant)
            npt_decode_UINT_array(dec, (UINT *)pFirstConstant, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pNumConstants)
            npt_decode_UINT_array(dec, (UINT *)pNumConstants, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_ID3D11DeviceContext1_DSGetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_DSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_DSGetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_DSGetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_DSGetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     UINT * pFirstConstant,
                                                     UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DSGetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_DSGetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    UINT * pFirstConstant,
                                                    UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DSGetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext1_DSGetConstantBuffers1_reply(dec, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::GSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_GSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_GSGetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_GSGetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_GSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_GSGetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_GSGetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext1_GSGetConstantBuffers1_reply(struct npt_cs_decoder *dec,
                                                            ID3D11Buffer ** ppConstantBuffers,
                                                            UINT * pFirstConstant,
                                                            UINT * pNumConstants)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                                  NPT_METHOD_ID3D11DeviceContext1_GSGetConstantBuffers1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext1_GSGetConstantBuffers1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                       NPT_METHOD_ID3D11DeviceContext1_GSGetConstantBuffers1));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pFirstConstant)
            npt_decode_UINT_array(dec, (UINT *)pFirstConstant, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pNumConstants)
            npt_decode_UINT_array(dec, (UINT *)pNumConstants, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_ID3D11DeviceContext1_GSGetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_GSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_GSGetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_GSGetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_GSGetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     UINT * pFirstConstant,
                                                     UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_GSGetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_GSGetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    UINT * pFirstConstant,
                                                    UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_GSGetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext1_GSGetConstantBuffers1_reply(dec, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::PSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_PSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_PSGetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_PSGetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_PSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_PSGetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_PSGetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext1_PSGetConstantBuffers1_reply(struct npt_cs_decoder *dec,
                                                            ID3D11Buffer ** ppConstantBuffers,
                                                            UINT * pFirstConstant,
                                                            UINT * pNumConstants)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                                  NPT_METHOD_ID3D11DeviceContext1_PSGetConstantBuffers1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext1_PSGetConstantBuffers1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                       NPT_METHOD_ID3D11DeviceContext1_PSGetConstantBuffers1));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pFirstConstant)
            npt_decode_UINT_array(dec, (UINT *)pFirstConstant, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pNumConstants)
            npt_decode_UINT_array(dec, (UINT *)pNumConstants, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_ID3D11DeviceContext1_PSGetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_PSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_PSGetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_PSGetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_PSGetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     UINT * pFirstConstant,
                                                     UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_PSGetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_PSGetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    UINT * pFirstConstant,
                                                    UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_PSGetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext1_PSGetConstantBuffers1_reply(dec, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::CSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_CSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_CSGetConstantBuffers1(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
cmd_size += sizeof(uint64_t) + (size_t)(NumBuffers) * sizeof(uint64_t);  /* ppConstantBuffers: array_count + N guest ids */
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_CSGetConstantBuffers1(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_CSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_CSGetConstantBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumBuffers);
if (ppConstantBuffers) {
    const uint64_t _cnt_ppConstantBuffers = (uint64_t)(NumBuffers);
    npt_encode_array_count(enc, _cnt_ppConstantBuffers);
    for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
        const uint64_t _gid = npt_com_allocate_next_id();
        ppConstantBuffers[_i] = (ID3D11Buffer *)(uintptr_t)_gid;
        npt_encode_uint64_t(enc, &_gid);
    }
} else {
    npt_encode_array_count(enc, 0);
}
/* skip pFirstConstant (output only) */
/* skip pNumConstants (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_CSGetConstantBuffers1_reply(const UINT StartSlot,const UINT NumBuffers,ID3D11Buffer ** ppConstantBuffers,const UINT * pFirstConstant,const UINT * pNumConstants)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumBuffers (input only) */
/* skip ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_array_count(pFirstConstant ? NumBuffers : 0);
    if (pFirstConstant)
        cmd_size += npt_sizeof_UINT_array(pFirstConstant, NumBuffers);
    cmd_size += npt_sizeof_array_count(pNumConstants ? NumBuffers : 0);
    if (pNumConstants)
        cmd_size += npt_sizeof_UINT_array(pNumConstants, NumBuffers);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext1_CSGetConstantBuffers1_reply(struct npt_cs_decoder *dec,
                                                            ID3D11Buffer ** ppConstantBuffers,
                                                            UINT * pFirstConstant,
                                                            UINT * pNumConstants)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                                  NPT_METHOD_ID3D11DeviceContext1_CSGetConstantBuffers1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext1_CSGetConstantBuffers1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                       NPT_METHOD_ID3D11DeviceContext1_CSGetConstantBuffers1));
        return;
    }
/* skip ppConstantBuffers (guest-allocated id, wrapper built on client thunk; not in reply) */
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pFirstConstant)
            npt_decode_UINT_array(dec, (UINT *)pFirstConstant, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pNumConstants)
            npt_decode_UINT_array(dec, (UINT *)pNumConstants, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_ID3D11DeviceContext1_CSGetConstantBuffers1(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumBuffers,
                                                      ID3D11Buffer ** ppConstantBuffers,
                                                      UINT * pFirstConstant,
                                                      UINT * pNumConstants,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_CSGetConstantBuffers1(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_CSGetConstantBuffers1_reply(StartSlot,NumBuffers,ppConstantBuffers,pFirstConstant,pNumConstants)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_CSGetConstantBuffers1(enc, cmd_flags, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_CSGetConstantBuffers1(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT StartSlot,
                                                     UINT NumBuffers,
                                                     ID3D11Buffer ** ppConstantBuffers,
                                                     UINT * pFirstConstant,
                                                     UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_CSGetConstantBuffers1(ring, 0, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_CSGetConstantBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StartSlot,
                                                    UINT NumBuffers,
                                                    ID3D11Buffer ** ppConstantBuffers,
                                                    UINT * pFirstConstant,
                                                    UINT * pNumConstants)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_CSGetConstantBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumBuffers, ppConstantBuffers, pFirstConstant, pNumConstants, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext1_CSGetConstantBuffers1_reply(dec, ppConstantBuffers, pFirstConstant, pNumConstants);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::SwapDeviceContextState
 */

struct npt_command_ID3D11DeviceContext1_SwapDeviceContextState {
    void *_self;
    ID3DDeviceContextState * pState;
    ID3DDeviceContextState ** ppPreviousState;
    /* Shadow: the guest-allocated id for ppPreviousState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPreviousState -> *ppPreviousState} in the
     * context object table. */
    uint64_t _guest_id_ppPreviousState;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_SwapDeviceContextState(const ID3DDeviceContextState * pState,ID3DDeviceContextState ** ppPreviousState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppPreviousState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_SwapDeviceContextState(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       ID3DDeviceContextState * pState,
                                                       ID3DDeviceContextState ** ppPreviousState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_SwapDeviceContextState(pState,ppPreviousState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_SwapDeviceContextState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pState));
{
    uint64_t _gid_ppPreviousState = 0;
    if (ppPreviousState) {
        _gid_ppPreviousState = npt_com_allocate_next_id();
        *ppPreviousState = (ID3DDeviceContextState *)(uintptr_t)_gid_ppPreviousState;
    }
    npt_encode_uint64_t(enc, &_gid_ppPreviousState);
}
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_SwapDeviceContextState_reply(const ID3DDeviceContextState * pState,ID3DDeviceContextState ** ppPreviousState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pState (input only) */
/* skip ppPreviousState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext1_SwapDeviceContextState_reply(struct npt_cs_decoder *dec,
                                                             ID3DDeviceContextState ** ppPreviousState)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                                  NPT_METHOD_ID3D11DeviceContext1_SwapDeviceContextState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext1_SwapDeviceContextState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                       NPT_METHOD_ID3D11DeviceContext1_SwapDeviceContextState));
        return;
    }
/* skip ppPreviousState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11DeviceContext1_SwapDeviceContextState(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       ID3DDeviceContextState * pState,
                                                       ID3DDeviceContextState ** ppPreviousState,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_SwapDeviceContextState(pState,ppPreviousState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_SwapDeviceContextState_reply(pState,ppPreviousState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_SwapDeviceContextState(enc, cmd_flags, object_id, pState, ppPreviousState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_SwapDeviceContextState(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      ID3DDeviceContextState * pState,
                                                      ID3DDeviceContextState ** ppPreviousState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_SwapDeviceContextState(ring, 0, object_id, pState, ppPreviousState, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_SwapDeviceContextState(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3DDeviceContextState * pState,
                                                     ID3DDeviceContextState ** ppPreviousState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_SwapDeviceContextState(ring, NPT_CMD_FLAG_REPLY, object_id, pState, ppPreviousState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext1_SwapDeviceContextState_reply(dec, ppPreviousState);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::ClearView
 */

struct npt_command_ID3D11DeviceContext1_ClearView {
    void *_self;
    ID3D11View * pView;
    const FLOAT * Color;
    const D3D11_RECT * pRect;
    UINT NumRects;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_ClearView(const ID3D11View * pView,const FLOAT * Color,const D3D11_RECT * pRect,const UINT NumRects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(4);
    cmd_size += npt_sizeof_FLOAT_array((const FLOAT *)Color, 4);
    cmd_size += npt_sizeof_array_count(pRect ? NumRects : 0);
    for (uint32_t _i = 0; _i < (pRect ? NumRects : 0); _i++)
        cmd_size += npt_sizeof_D3D11_RECT(&pRect[_i], max_mode);
    cmd_size += npt_sizeof_UINT(&NumRects, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_ClearView(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D11View * pView,
                                          const FLOAT * Color,
                                          const D3D11_RECT * pRect,
                                          UINT NumRects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_ClearView(pView,Color,pRect,NumRects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_ClearView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pView));
    npt_encode_array_count(enc, 4);
    npt_encode_FLOAT_array(enc, (const FLOAT *)Color, 4);
    if (pRect) {
        npt_encode_array_count(enc, NumRects);
        for (uint32_t _i = 0; _i < (uint32_t)(NumRects); _i++)
            npt_encode_D3D11_RECT(enc, &pRect[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumRects);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_ClearView_reply(const ID3D11View * pView,const FLOAT * Color,const D3D11_RECT * pRect,const UINT NumRects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pView (input only) */
/* skip Color (input only) */
/* skip pRect (input only) */
/* skip NumRects (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_ClearView(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D11View * pView,
                                          const FLOAT * Color,
                                          const D3D11_RECT * pRect,
                                          UINT NumRects,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_ClearView(pView,Color,pRect,NumRects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_ClearView_reply(pView,Color,pRect,NumRects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_ClearView(enc, cmd_flags, object_id, pView, Color, pRect, NumRects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_ClearView(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11View * pView,
                                         const FLOAT * Color,
                                         const D3D11_RECT * pRect,
                                         UINT NumRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_ClearView(ring, 0, object_id, pView, Color, pRect, NumRects, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_ClearView(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        ID3D11View * pView,
                                        const FLOAT * Color,
                                        const D3D11_RECT * pRect,
                                        UINT NumRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_ClearView(ring, NPT_CMD_FLAG_REPLY, object_id, pView, Color, pRect, NumRects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext1::DiscardView1
 */

struct npt_command_ID3D11DeviceContext1_DiscardView1 {
    void *_self;
    ID3D11View * pResourceView;
    const D3D11_RECT * pRects;
    UINT NumRects;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DiscardView1(const ID3D11View * pResourceView,const D3D11_RECT * pRects,const UINT NumRects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(pRects ? NumRects : 0);
    for (uint32_t _i = 0; _i < (pRects ? NumRects : 0); _i++)
        cmd_size += npt_sizeof_D3D11_RECT(&pRects[_i], max_mode);
    cmd_size += npt_sizeof_UINT(&NumRects, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext1_DiscardView1(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             ID3D11View * pResourceView,
                                             const D3D11_RECT * pRects,
                                             UINT NumRects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext1_DiscardView1(pResourceView,pRects,NumRects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DiscardView1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResourceView));
    if (pRects) {
        npt_encode_array_count(enc, NumRects);
        for (uint32_t _i = 0; _i < (uint32_t)(NumRects); _i++)
            npt_encode_D3D11_RECT(enc, &pRects[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumRects);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext1_DiscardView1_reply(const ID3D11View * pResourceView,const D3D11_RECT * pRects,const UINT NumRects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResourceView (input only) */
/* skip pRects (input only) */
/* skip NumRects (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext1_DiscardView1(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             ID3D11View * pResourceView,
                                             const D3D11_RECT * pRects,
                                             UINT NumRects,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext1_DiscardView1(pResourceView,pRects,NumRects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext1_DiscardView1_reply(pResourceView,pRects,NumRects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext1_DiscardView1(enc, cmd_flags, object_id, pResourceView, pRects, NumRects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext1_DiscardView1(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            ID3D11View * pResourceView,
                                            const D3D11_RECT * pRects,
                                            UINT NumRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DiscardView1(ring, 0, object_id, pResourceView, pRects, NumRects, &submit);
}

static inline void
npt_call_ID3D11DeviceContext1_DiscardView1(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D11View * pResourceView,
                                           const D3D11_RECT * pRects,
                                           UINT NumRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext1_DiscardView1(ring, NPT_CMD_FLAG_REPLY, object_id, pResourceView, pRects, NumRects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext2::UpdateTileMappings
 */

struct npt_command_ID3D11DeviceContext2_UpdateTileMappings {
    void *_self;
    ID3D11Resource * pTiledResource;
    UINT NumTiledResourceRegions;
    const D3D11_TILED_RESOURCE_COORDINATE * pTiledResourceRegionStartCoordinates;
    const D3D11_TILE_REGION_SIZE * pTiledResourceRegionSizes;
    ID3D11Buffer * pTilePool;
    UINT NumRanges;
    const UINT * pRangeFlags;
    const UINT * pTilePoolStartOffsets;
    const UINT * pRangeTileCounts;
    UINT Flags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_UpdateTileMappings(const ID3D11Resource * pTiledResource,const UINT NumTiledResourceRegions,const D3D11_TILED_RESOURCE_COORDINATE * pTiledResourceRegionStartCoordinates,const D3D11_TILE_REGION_SIZE * pTiledResourceRegionSizes,const ID3D11Buffer * pTilePool,const UINT NumRanges,const UINT * pRangeFlags,const UINT * pTilePoolStartOffsets,const UINT * pRangeTileCounts,const UINT Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&NumTiledResourceRegions, max_mode);
    cmd_size += npt_sizeof_array_count(pTiledResourceRegionStartCoordinates ? NumTiledResourceRegions : 0);
    for (uint32_t _i = 0; _i < (pTiledResourceRegionStartCoordinates ? NumTiledResourceRegions : 0); _i++)
        cmd_size += npt_sizeof_D3D11_TILED_RESOURCE_COORDINATE(&pTiledResourceRegionStartCoordinates[_i], max_mode);
    cmd_size += npt_sizeof_array_count(pTiledResourceRegionSizes ? NumTiledResourceRegions : 0);
    for (uint32_t _i = 0; _i < (pTiledResourceRegionSizes ? NumTiledResourceRegions : 0); _i++)
        cmd_size += npt_sizeof_D3D11_TILE_REGION_SIZE(&pTiledResourceRegionSizes[_i], max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&NumRanges, max_mode);
    cmd_size += npt_sizeof_array_count(pRangeFlags ? NumRanges : 0);
    if (pRangeFlags)
        cmd_size += npt_sizeof_UINT_array(pRangeFlags, NumRanges);
    cmd_size += npt_sizeof_array_count(pTilePoolStartOffsets ? NumRanges : 0);
    if (pTilePoolStartOffsets)
        cmd_size += npt_sizeof_UINT_array(pTilePoolStartOffsets, NumRanges);
    cmd_size += npt_sizeof_array_count(pRangeTileCounts ? NumRanges : 0);
    if (pRangeTileCounts)
        cmd_size += npt_sizeof_UINT_array(pRangeTileCounts, NumRanges);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_UpdateTileMappings(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11Resource * pTiledResource,
                                                   UINT NumTiledResourceRegions,
                                                   const D3D11_TILED_RESOURCE_COORDINATE * pTiledResourceRegionStartCoordinates,
                                                   const D3D11_TILE_REGION_SIZE * pTiledResourceRegionSizes,
                                                   ID3D11Buffer * pTilePool,
                                                   UINT NumRanges,
                                                   const UINT * pRangeFlags,
                                                   const UINT * pTilePoolStartOffsets,
                                                   const UINT * pRangeTileCounts,
                                                   UINT Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_UpdateTileMappings(pTiledResource,NumTiledResourceRegions,pTiledResourceRegionStartCoordinates,pTiledResourceRegionSizes,pTilePool,NumRanges,pRangeFlags,pTilePoolStartOffsets,pRangeTileCounts,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_UpdateTileMappings),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pTiledResource));
    npt_encode_UINT(enc, &NumTiledResourceRegions);
    if (pTiledResourceRegionStartCoordinates) {
        npt_encode_array_count(enc, NumTiledResourceRegions);
        for (uint32_t _i = 0; _i < (uint32_t)(NumTiledResourceRegions); _i++)
            npt_encode_D3D11_TILED_RESOURCE_COORDINATE(enc, &pTiledResourceRegionStartCoordinates[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pTiledResourceRegionSizes) {
        npt_encode_array_count(enc, NumTiledResourceRegions);
        for (uint32_t _i = 0; _i < (uint32_t)(NumTiledResourceRegions); _i++)
            npt_encode_D3D11_TILE_REGION_SIZE(enc, &pTiledResourceRegionSizes[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_com_handle(enc, npt_object_get_id(pTilePool));
    npt_encode_UINT(enc, &NumRanges);
    if (pRangeFlags) {
        npt_encode_array_count(enc, NumRanges);
        npt_encode_UINT_array(enc, pRangeFlags, NumRanges);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pTilePoolStartOffsets) {
        npt_encode_array_count(enc, NumRanges);
        npt_encode_UINT_array(enc, pTilePoolStartOffsets, NumRanges);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pRangeTileCounts) {
        npt_encode_array_count(enc, NumRanges);
        npt_encode_UINT_array(enc, pRangeTileCounts, NumRanges);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &Flags);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_UpdateTileMappings_reply(const ID3D11Resource * pTiledResource,const UINT NumTiledResourceRegions,const D3D11_TILED_RESOURCE_COORDINATE * pTiledResourceRegionStartCoordinates,const D3D11_TILE_REGION_SIZE * pTiledResourceRegionSizes,const ID3D11Buffer * pTilePool,const UINT NumRanges,const UINT * pRangeFlags,const UINT * pTilePoolStartOffsets,const UINT * pRangeTileCounts,const UINT Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pTiledResource (input only) */
/* skip NumTiledResourceRegions (input only) */
/* skip pTiledResourceRegionStartCoordinates (input only) */
/* skip pTiledResourceRegionSizes (input only) */
/* skip pTilePool (input only) */
/* skip NumRanges (input only) */
/* skip pRangeFlags (input only) */
/* skip pTilePoolStartOffsets (input only) */
/* skip pRangeTileCounts (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext2_UpdateTileMappings_reply(struct npt_cs_decoder *dec,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                                  NPT_METHOD_ID3D11DeviceContext2_UpdateTileMappings))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext2_UpdateTileMappings reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                       NPT_METHOD_ID3D11DeviceContext2_UpdateTileMappings));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11DeviceContext2_UpdateTileMappings(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11Resource * pTiledResource,
                                                   UINT NumTiledResourceRegions,
                                                   const D3D11_TILED_RESOURCE_COORDINATE * pTiledResourceRegionStartCoordinates,
                                                   const D3D11_TILE_REGION_SIZE * pTiledResourceRegionSizes,
                                                   ID3D11Buffer * pTilePool,
                                                   UINT NumRanges,
                                                   const UINT * pRangeFlags,
                                                   const UINT * pTilePoolStartOffsets,
                                                   const UINT * pRangeTileCounts,
                                                   UINT Flags,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_UpdateTileMappings(pTiledResource,NumTiledResourceRegions,pTiledResourceRegionStartCoordinates,pTiledResourceRegionSizes,pTilePool,NumRanges,pRangeFlags,pTilePoolStartOffsets,pRangeTileCounts,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_UpdateTileMappings_reply(pTiledResource,NumTiledResourceRegions,pTiledResourceRegionStartCoordinates,pTiledResourceRegionSizes,pTilePool,NumRanges,pRangeFlags,pTilePoolStartOffsets,pRangeTileCounts,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_UpdateTileMappings(enc, cmd_flags, object_id, pTiledResource, NumTiledResourceRegions, pTiledResourceRegionStartCoordinates, pTiledResourceRegionSizes, pTilePool, NumRanges, pRangeFlags, pTilePoolStartOffsets, pRangeTileCounts, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_UpdateTileMappings(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D11Resource * pTiledResource,
                                                  UINT NumTiledResourceRegions,
                                                  const D3D11_TILED_RESOURCE_COORDINATE * pTiledResourceRegionStartCoordinates,
                                                  const D3D11_TILE_REGION_SIZE * pTiledResourceRegionSizes,
                                                  ID3D11Buffer * pTilePool,
                                                  UINT NumRanges,
                                                  const UINT * pRangeFlags,
                                                  const UINT * pTilePoolStartOffsets,
                                                  const UINT * pRangeTileCounts,
                                                  UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_UpdateTileMappings(ring, 0, object_id, pTiledResource, NumTiledResourceRegions, pTiledResourceRegionStartCoordinates, pTiledResourceRegionSizes, pTilePool, NumRanges, pRangeFlags, pTilePoolStartOffsets, pRangeTileCounts, Flags, &submit);
}

static inline HRESULT
npt_call_ID3D11DeviceContext2_UpdateTileMappings(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pTiledResource,
                                                 UINT NumTiledResourceRegions,
                                                 const D3D11_TILED_RESOURCE_COORDINATE * pTiledResourceRegionStartCoordinates,
                                                 const D3D11_TILE_REGION_SIZE * pTiledResourceRegionSizes,
                                                 ID3D11Buffer * pTilePool,
                                                 UINT NumRanges,
                                                 const UINT * pRangeFlags,
                                                 const UINT * pTilePoolStartOffsets,
                                                 const UINT * pRangeTileCounts,
                                                 UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_UpdateTileMappings(ring, NPT_CMD_FLAG_REPLY, object_id, pTiledResource, NumTiledResourceRegions, pTiledResourceRegionStartCoordinates, pTiledResourceRegionSizes, pTilePool, NumRanges, pRangeFlags, pTilePoolStartOffsets, pRangeTileCounts, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11DeviceContext2_UpdateTileMappings_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11DeviceContext2::CopyTileMappings
 */

struct npt_command_ID3D11DeviceContext2_CopyTileMappings {
    void *_self;
    ID3D11Resource * pDestTiledResource;
    const D3D11_TILED_RESOURCE_COORDINATE * pDestRegionStartCoordinate;
    ID3D11Resource * pSourceTiledResource;
    const D3D11_TILED_RESOURCE_COORDINATE * pSourceRegionStartCoordinate;
    const D3D11_TILE_REGION_SIZE * pTileRegionSize;
    UINT Flags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_CopyTileMappings(const ID3D11Resource * pDestTiledResource,const D3D11_TILED_RESOURCE_COORDINATE * pDestRegionStartCoordinate,const ID3D11Resource * pSourceTiledResource,const D3D11_TILED_RESOURCE_COORDINATE * pSourceRegionStartCoordinate,const D3D11_TILE_REGION_SIZE * pTileRegionSize,const UINT Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDestRegionStartCoordinate);
    if (pDestRegionStartCoordinate)
        cmd_size += npt_sizeof_D3D11_TILED_RESOURCE_COORDINATE(pDestRegionStartCoordinate, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pSourceRegionStartCoordinate);
    if (pSourceRegionStartCoordinate)
        cmd_size += npt_sizeof_D3D11_TILED_RESOURCE_COORDINATE(pSourceRegionStartCoordinate, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pTileRegionSize);
    if (pTileRegionSize)
        cmd_size += npt_sizeof_D3D11_TILE_REGION_SIZE(pTileRegionSize, max_mode);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_CopyTileMappings(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pDestTiledResource,
                                                 const D3D11_TILED_RESOURCE_COORDINATE * pDestRegionStartCoordinate,
                                                 ID3D11Resource * pSourceTiledResource,
                                                 const D3D11_TILED_RESOURCE_COORDINATE * pSourceRegionStartCoordinate,
                                                 const D3D11_TILE_REGION_SIZE * pTileRegionSize,
                                                 UINT Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_CopyTileMappings(pDestTiledResource,pDestRegionStartCoordinate,pSourceTiledResource,pSourceRegionStartCoordinate,pTileRegionSize,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_CopyTileMappings),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDestTiledResource));
    if (npt_encode_simple_pointer(enc, pDestRegionStartCoordinate))
        npt_encode_D3D11_TILED_RESOURCE_COORDINATE(enc, pDestRegionStartCoordinate);
    npt_encode_com_handle(enc, npt_object_get_id(pSourceTiledResource));
    if (npt_encode_simple_pointer(enc, pSourceRegionStartCoordinate))
        npt_encode_D3D11_TILED_RESOURCE_COORDINATE(enc, pSourceRegionStartCoordinate);
    if (npt_encode_simple_pointer(enc, pTileRegionSize))
        npt_encode_D3D11_TILE_REGION_SIZE(enc, pTileRegionSize);
    npt_encode_UINT(enc, &Flags);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_CopyTileMappings_reply(const ID3D11Resource * pDestTiledResource,const D3D11_TILED_RESOURCE_COORDINATE * pDestRegionStartCoordinate,const ID3D11Resource * pSourceTiledResource,const D3D11_TILED_RESOURCE_COORDINATE * pSourceRegionStartCoordinate,const D3D11_TILE_REGION_SIZE * pTileRegionSize,const UINT Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDestTiledResource (input only) */
/* skip pDestRegionStartCoordinate (input only) */
/* skip pSourceTiledResource (input only) */
/* skip pSourceRegionStartCoordinate (input only) */
/* skip pTileRegionSize (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext2_CopyTileMappings_reply(struct npt_cs_decoder *dec,
                                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                                  NPT_METHOD_ID3D11DeviceContext2_CopyTileMappings))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext2_CopyTileMappings reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                       NPT_METHOD_ID3D11DeviceContext2_CopyTileMappings));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11DeviceContext2_CopyTileMappings(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pDestTiledResource,
                                                 const D3D11_TILED_RESOURCE_COORDINATE * pDestRegionStartCoordinate,
                                                 ID3D11Resource * pSourceTiledResource,
                                                 const D3D11_TILED_RESOURCE_COORDINATE * pSourceRegionStartCoordinate,
                                                 const D3D11_TILE_REGION_SIZE * pTileRegionSize,
                                                 UINT Flags,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_CopyTileMappings(pDestTiledResource,pDestRegionStartCoordinate,pSourceTiledResource,pSourceRegionStartCoordinate,pTileRegionSize,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_CopyTileMappings_reply(pDestTiledResource,pDestRegionStartCoordinate,pSourceTiledResource,pSourceRegionStartCoordinate,pTileRegionSize,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_CopyTileMappings(enc, cmd_flags, object_id, pDestTiledResource, pDestRegionStartCoordinate, pSourceTiledResource, pSourceRegionStartCoordinate, pTileRegionSize, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_CopyTileMappings(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11Resource * pDestTiledResource,
                                                const D3D11_TILED_RESOURCE_COORDINATE * pDestRegionStartCoordinate,
                                                ID3D11Resource * pSourceTiledResource,
                                                const D3D11_TILED_RESOURCE_COORDINATE * pSourceRegionStartCoordinate,
                                                const D3D11_TILE_REGION_SIZE * pTileRegionSize,
                                                UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_CopyTileMappings(ring, 0, object_id, pDestTiledResource, pDestRegionStartCoordinate, pSourceTiledResource, pSourceRegionStartCoordinate, pTileRegionSize, Flags, &submit);
}

static inline HRESULT
npt_call_ID3D11DeviceContext2_CopyTileMappings(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Resource * pDestTiledResource,
                                               const D3D11_TILED_RESOURCE_COORDINATE * pDestRegionStartCoordinate,
                                               ID3D11Resource * pSourceTiledResource,
                                               const D3D11_TILED_RESOURCE_COORDINATE * pSourceRegionStartCoordinate,
                                               const D3D11_TILE_REGION_SIZE * pTileRegionSize,
                                               UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_CopyTileMappings(ring, NPT_CMD_FLAG_REPLY, object_id, pDestTiledResource, pDestRegionStartCoordinate, pSourceTiledResource, pSourceRegionStartCoordinate, pTileRegionSize, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11DeviceContext2_CopyTileMappings_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11DeviceContext2::CopyTiles
 */

struct npt_command_ID3D11DeviceContext2_CopyTiles {
    void *_self;
    ID3D11Resource * pTiledResource;
    const D3D11_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate;
    const D3D11_TILE_REGION_SIZE * pTileRegionSize;
    ID3D11Buffer * pBuffer;
    UINT64 BufferStartOffsetInBytes;
    UINT Flags;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_CopyTiles(const ID3D11Resource * pTiledResource,const D3D11_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,const D3D11_TILE_REGION_SIZE * pTileRegionSize,const ID3D11Buffer * pBuffer,const UINT64 BufferStartOffsetInBytes,const UINT Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pTileRegionStartCoordinate);
    if (pTileRegionStartCoordinate)
        cmd_size += npt_sizeof_D3D11_TILED_RESOURCE_COORDINATE(pTileRegionStartCoordinate, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pTileRegionSize);
    if (pTileRegionSize)
        cmd_size += npt_sizeof_D3D11_TILE_REGION_SIZE(pTileRegionSize, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&BufferStartOffsetInBytes, max_mode);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_CopyTiles(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D11Resource * pTiledResource,
                                          const D3D11_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,
                                          const D3D11_TILE_REGION_SIZE * pTileRegionSize,
                                          ID3D11Buffer * pBuffer,
                                          UINT64 BufferStartOffsetInBytes,
                                          UINT Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_CopyTiles(pTiledResource,pTileRegionStartCoordinate,pTileRegionSize,pBuffer,BufferStartOffsetInBytes,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_CopyTiles),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pTiledResource));
    if (npt_encode_simple_pointer(enc, pTileRegionStartCoordinate))
        npt_encode_D3D11_TILED_RESOURCE_COORDINATE(enc, pTileRegionStartCoordinate);
    if (npt_encode_simple_pointer(enc, pTileRegionSize))
        npt_encode_D3D11_TILE_REGION_SIZE(enc, pTileRegionSize);
    npt_encode_com_handle(enc, npt_object_get_id(pBuffer));
    npt_encode_UINT64(enc, &BufferStartOffsetInBytes);
    npt_encode_UINT(enc, &Flags);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_CopyTiles_reply(const ID3D11Resource * pTiledResource,const D3D11_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,const D3D11_TILE_REGION_SIZE * pTileRegionSize,const ID3D11Buffer * pBuffer,const UINT64 BufferStartOffsetInBytes,const UINT Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pTiledResource (input only) */
/* skip pTileRegionStartCoordinate (input only) */
/* skip pTileRegionSize (input only) */
/* skip pBuffer (input only) */
/* skip BufferStartOffsetInBytes (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext2_CopyTiles(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          ID3D11Resource * pTiledResource,
                                          const D3D11_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,
                                          const D3D11_TILE_REGION_SIZE * pTileRegionSize,
                                          ID3D11Buffer * pBuffer,
                                          UINT64 BufferStartOffsetInBytes,
                                          UINT Flags,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_CopyTiles(pTiledResource,pTileRegionStartCoordinate,pTileRegionSize,pBuffer,BufferStartOffsetInBytes,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_CopyTiles_reply(pTiledResource,pTileRegionStartCoordinate,pTileRegionSize,pBuffer,BufferStartOffsetInBytes,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_CopyTiles(enc, cmd_flags, object_id, pTiledResource, pTileRegionStartCoordinate, pTileRegionSize, pBuffer, BufferStartOffsetInBytes, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_CopyTiles(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11Resource * pTiledResource,
                                         const D3D11_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,
                                         const D3D11_TILE_REGION_SIZE * pTileRegionSize,
                                         ID3D11Buffer * pBuffer,
                                         UINT64 BufferStartOffsetInBytes,
                                         UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_CopyTiles(ring, 0, object_id, pTiledResource, pTileRegionStartCoordinate, pTileRegionSize, pBuffer, BufferStartOffsetInBytes, Flags, &submit);
}

static inline void
npt_call_ID3D11DeviceContext2_CopyTiles(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        ID3D11Resource * pTiledResource,
                                        const D3D11_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,
                                        const D3D11_TILE_REGION_SIZE * pTileRegionSize,
                                        ID3D11Buffer * pBuffer,
                                        UINT64 BufferStartOffsetInBytes,
                                        UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_CopyTiles(ring, NPT_CMD_FLAG_REPLY, object_id, pTiledResource, pTileRegionStartCoordinate, pTileRegionSize, pBuffer, BufferStartOffsetInBytes, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext2::UpdateTiles
 */

struct npt_command_ID3D11DeviceContext2_UpdateTiles {
    void *_self;
    ID3D11Resource * pDestTiledResource;
    const D3D11_TILED_RESOURCE_COORDINATE * pDestTileRegionStartCoordinate;
    const D3D11_TILE_REGION_SIZE * pDestTileRegionSize;
    const void * pSourceTileData;
    UINT Flags;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_UpdateTiles(const ID3D11Resource * pDestTiledResource,const D3D11_TILED_RESOURCE_COORDINATE * pDestTileRegionStartCoordinate,const D3D11_TILE_REGION_SIZE * pDestTileRegionSize,const void * pSourceTileData,const UINT Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDestTileRegionStartCoordinate);
    if (pDestTileRegionStartCoordinate)
        cmd_size += npt_sizeof_D3D11_TILED_RESOURCE_COORDINATE(pDestTileRegionStartCoordinate, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDestTileRegionSize);
    if (pDestTileRegionSize)
        cmd_size += npt_sizeof_D3D11_TILE_REGION_SIZE(pDestTileRegionSize, max_mode);
    /* ERROR: pSourceTileData (void) is not serializable */
    return 0;
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_UpdateTiles(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11Resource * pDestTiledResource,
                                            const D3D11_TILED_RESOURCE_COORDINATE * pDestTileRegionStartCoordinate,
                                            const D3D11_TILE_REGION_SIZE * pDestTileRegionSize,
                                            const void * pSourceTileData,
                                            UINT Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_UpdateTiles(pDestTiledResource,pDestTileRegionStartCoordinate,pDestTileRegionSize,pSourceTileData,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_UpdateTiles),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDestTiledResource));
    if (npt_encode_simple_pointer(enc, pDestTileRegionStartCoordinate))
        npt_encode_D3D11_TILED_RESOURCE_COORDINATE(enc, pDestTileRegionStartCoordinate);
    if (npt_encode_simple_pointer(enc, pDestTileRegionSize))
        npt_encode_D3D11_TILE_REGION_SIZE(enc, pDestTileRegionSize);
    /* ERROR: pSourceTileData (void) is not serializable */
    return;
    npt_encode_UINT(enc, &Flags);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_UpdateTiles_reply(const ID3D11Resource * pDestTiledResource,const D3D11_TILED_RESOURCE_COORDINATE * pDestTileRegionStartCoordinate,const D3D11_TILE_REGION_SIZE * pDestTileRegionSize,const void * pSourceTileData,const UINT Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDestTiledResource (input only) */
/* skip pDestTileRegionStartCoordinate (input only) */
/* skip pDestTileRegionSize (input only) */
/* skip pSourceTileData (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext2_UpdateTiles(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11Resource * pDestTiledResource,
                                            const D3D11_TILED_RESOURCE_COORDINATE * pDestTileRegionStartCoordinate,
                                            const D3D11_TILE_REGION_SIZE * pDestTileRegionSize,
                                            const void * pSourceTileData,
                                            UINT Flags,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_UpdateTiles(pDestTiledResource,pDestTileRegionStartCoordinate,pDestTileRegionSize,pSourceTileData,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_UpdateTiles_reply(pDestTiledResource,pDestTileRegionStartCoordinate,pDestTileRegionSize,pSourceTileData,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_UpdateTiles(enc, cmd_flags, object_id, pDestTiledResource, pDestTileRegionStartCoordinate, pDestTileRegionSize, pSourceTileData, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_UpdateTiles(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D11Resource * pDestTiledResource,
                                           const D3D11_TILED_RESOURCE_COORDINATE * pDestTileRegionStartCoordinate,
                                           const D3D11_TILE_REGION_SIZE * pDestTileRegionSize,
                                           const void * pSourceTileData,
                                           UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_UpdateTiles(ring, 0, object_id, pDestTiledResource, pDestTileRegionStartCoordinate, pDestTileRegionSize, pSourceTileData, Flags, &submit);
}

static inline void
npt_call_ID3D11DeviceContext2_UpdateTiles(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11Resource * pDestTiledResource,
                                          const D3D11_TILED_RESOURCE_COORDINATE * pDestTileRegionStartCoordinate,
                                          const D3D11_TILE_REGION_SIZE * pDestTileRegionSize,
                                          const void * pSourceTileData,
                                          UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_UpdateTiles(ring, NPT_CMD_FLAG_REPLY, object_id, pDestTiledResource, pDestTileRegionStartCoordinate, pDestTileRegionSize, pSourceTileData, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext2::ResizeTilePool
 */

struct npt_command_ID3D11DeviceContext2_ResizeTilePool {
    void *_self;
    ID3D11Buffer * pTilePool;
    UINT64 NewSizeInBytes;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_ResizeTilePool(const ID3D11Buffer * pTilePool,const UINT64 NewSizeInBytes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&NewSizeInBytes, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_ResizeTilePool(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11Buffer * pTilePool,
                                               UINT64 NewSizeInBytes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_ResizeTilePool(pTilePool,NewSizeInBytes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_ResizeTilePool),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pTilePool));
    npt_encode_UINT64(enc, &NewSizeInBytes);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_ResizeTilePool_reply(const ID3D11Buffer * pTilePool,const UINT64 NewSizeInBytes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pTilePool (input only) */
/* skip NewSizeInBytes (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext2_ResizeTilePool_reply(struct npt_cs_decoder *dec,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                                  NPT_METHOD_ID3D11DeviceContext2_ResizeTilePool))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext2_ResizeTilePool reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                       NPT_METHOD_ID3D11DeviceContext2_ResizeTilePool));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11DeviceContext2_ResizeTilePool(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11Buffer * pTilePool,
                                               UINT64 NewSizeInBytes,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_ResizeTilePool(pTilePool,NewSizeInBytes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_ResizeTilePool_reply(pTilePool,NewSizeInBytes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_ResizeTilePool(enc, cmd_flags, object_id, pTilePool, NewSizeInBytes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_ResizeTilePool(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11Buffer * pTilePool,
                                              UINT64 NewSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_ResizeTilePool(ring, 0, object_id, pTilePool, NewSizeInBytes, &submit);
}

static inline HRESULT
npt_call_ID3D11DeviceContext2_ResizeTilePool(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11Buffer * pTilePool,
                                             UINT64 NewSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_ResizeTilePool(ring, NPT_CMD_FLAG_REPLY, object_id, pTilePool, NewSizeInBytes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11DeviceContext2_ResizeTilePool_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11DeviceContext2::TiledResourceBarrier
 */

struct npt_command_ID3D11DeviceContext2_TiledResourceBarrier {
    void *_self;
    ID3D11DeviceChild * pTiledResourceOrViewAccessBeforeBarrier;
    ID3D11DeviceChild * pTiledResourceOrViewAccessAfterBarrier;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_TiledResourceBarrier(const ID3D11DeviceChild * pTiledResourceOrViewAccessBeforeBarrier,const ID3D11DeviceChild * pTiledResourceOrViewAccessAfterBarrier)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_TiledResourceBarrier(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11DeviceChild * pTiledResourceOrViewAccessBeforeBarrier,
                                                     ID3D11DeviceChild * pTiledResourceOrViewAccessAfterBarrier)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_TiledResourceBarrier(pTiledResourceOrViewAccessBeforeBarrier,pTiledResourceOrViewAccessAfterBarrier);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_TiledResourceBarrier),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pTiledResourceOrViewAccessBeforeBarrier));
    npt_encode_com_handle(enc, npt_object_get_id(pTiledResourceOrViewAccessAfterBarrier));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_TiledResourceBarrier_reply(const ID3D11DeviceChild * pTiledResourceOrViewAccessBeforeBarrier,const ID3D11DeviceChild * pTiledResourceOrViewAccessAfterBarrier)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pTiledResourceOrViewAccessBeforeBarrier (input only) */
/* skip pTiledResourceOrViewAccessAfterBarrier (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext2_TiledResourceBarrier(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11DeviceChild * pTiledResourceOrViewAccessBeforeBarrier,
                                                     ID3D11DeviceChild * pTiledResourceOrViewAccessAfterBarrier,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_TiledResourceBarrier(pTiledResourceOrViewAccessBeforeBarrier,pTiledResourceOrViewAccessAfterBarrier);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_TiledResourceBarrier_reply(pTiledResourceOrViewAccessBeforeBarrier,pTiledResourceOrViewAccessAfterBarrier)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_TiledResourceBarrier(enc, cmd_flags, object_id, pTiledResourceOrViewAccessBeforeBarrier, pTiledResourceOrViewAccessAfterBarrier);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_TiledResourceBarrier(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11DeviceChild * pTiledResourceOrViewAccessBeforeBarrier,
                                                    ID3D11DeviceChild * pTiledResourceOrViewAccessAfterBarrier)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_TiledResourceBarrier(ring, 0, object_id, pTiledResourceOrViewAccessBeforeBarrier, pTiledResourceOrViewAccessAfterBarrier, &submit);
}

static inline void
npt_call_ID3D11DeviceContext2_TiledResourceBarrier(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11DeviceChild * pTiledResourceOrViewAccessBeforeBarrier,
                                                   ID3D11DeviceChild * pTiledResourceOrViewAccessAfterBarrier)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_TiledResourceBarrier(ring, NPT_CMD_FLAG_REPLY, object_id, pTiledResourceOrViewAccessBeforeBarrier, pTiledResourceOrViewAccessAfterBarrier, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext2::IsAnnotationEnabled
 */

struct npt_command_ID3D11DeviceContext2_IsAnnotationEnabled {
    void *_self;
    BOOL ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_IsAnnotationEnabled(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_IsAnnotationEnabled(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_IsAnnotationEnabled();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_IsAnnotationEnabled),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_IsAnnotationEnabled_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext2_IsAnnotationEnabled_reply(struct npt_cs_decoder *dec,
                                                          BOOL *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                                  NPT_METHOD_ID3D11DeviceContext2_IsAnnotationEnabled))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext2_IsAnnotationEnabled reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                       NPT_METHOD_ID3D11DeviceContext2_IsAnnotationEnabled));
        if (ret) *ret = (BOOL)0;
        return;
    }
    if (ret) *ret = (BOOL)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11DeviceContext2_IsAnnotationEnabled(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_IsAnnotationEnabled();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_IsAnnotationEnabled_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_IsAnnotationEnabled(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_IsAnnotationEnabled(struct npt_ring *ring,
                                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_IsAnnotationEnabled(ring, 0, object_id, &submit);
}

static inline BOOL
npt_call_ID3D11DeviceContext2_IsAnnotationEnabled(struct npt_ring *ring,
                                                  npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_IsAnnotationEnabled(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        BOOL _ret;
        npt_decode_ID3D11DeviceContext2_IsAnnotationEnabled_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (BOOL)0;
    }
}

/*
 * ID3D11DeviceContext2::SetMarkerInt
 */

struct npt_command_ID3D11DeviceContext2_SetMarkerInt {
    void *_self;
    const WCHAR * pLabel;
    INT Data;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_SetMarkerInt(const WCHAR * pLabel,const INT Data)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pLabel) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pLabel) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(pLabel, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_INT(&Data, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_SetMarkerInt(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const WCHAR * pLabel,
                                             INT Data)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_SetMarkerInt(pLabel,Data);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_SetMarkerInt),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pLabel) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pLabel) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, pLabel, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_INT(enc, &Data);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_SetMarkerInt_reply(const WCHAR * pLabel,const INT Data)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pLabel (input only) */
/* skip Data (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext2_SetMarkerInt(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const WCHAR * pLabel,
                                             INT Data,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_SetMarkerInt(pLabel,Data);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_SetMarkerInt_reply(pLabel,Data)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_SetMarkerInt(enc, cmd_flags, object_id, pLabel, Data);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_SetMarkerInt(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const WCHAR * pLabel,
                                            INT Data)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_SetMarkerInt(ring, 0, object_id, pLabel, Data, &submit);
}

static inline void
npt_call_ID3D11DeviceContext2_SetMarkerInt(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           const WCHAR * pLabel,
                                           INT Data)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_SetMarkerInt(ring, NPT_CMD_FLAG_REPLY, object_id, pLabel, Data, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext2::BeginEventInt
 */

struct npt_command_ID3D11DeviceContext2_BeginEventInt {
    void *_self;
    const WCHAR * pLabel;
    INT Data;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_BeginEventInt(const WCHAR * pLabel,const INT Data)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pLabel) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pLabel) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(pLabel, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_INT(&Data, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_BeginEventInt(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const WCHAR * pLabel,
                                              INT Data)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_BeginEventInt(pLabel,Data);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_BeginEventInt),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pLabel) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)pLabel) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, pLabel, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_INT(enc, &Data);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_BeginEventInt_reply(const WCHAR * pLabel,const INT Data)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pLabel (input only) */
/* skip Data (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext2_BeginEventInt(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const WCHAR * pLabel,
                                              INT Data,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_BeginEventInt(pLabel,Data);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_BeginEventInt_reply(pLabel,Data)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_BeginEventInt(enc, cmd_flags, object_id, pLabel, Data);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_BeginEventInt(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const WCHAR * pLabel,
                                             INT Data)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_BeginEventInt(ring, 0, object_id, pLabel, Data, &submit);
}

static inline void
npt_call_ID3D11DeviceContext2_BeginEventInt(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const WCHAR * pLabel,
                                            INT Data)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_BeginEventInt(ring, NPT_CMD_FLAG_REPLY, object_id, pLabel, Data, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext2::EndEvent
 */

struct npt_command_ID3D11DeviceContext2_EndEvent {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext2_EndEvent(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext2_EndEvent(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext2_EndEvent();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_EndEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext2_EndEvent_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext2_EndEvent(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext2_EndEvent();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext2_EndEvent_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext2_EndEvent(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext2_EndEvent(struct npt_ring *ring,
                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_EndEvent(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D11DeviceContext2_EndEvent(struct npt_ring *ring,
                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext2_EndEvent(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext3::Flush1
 */

struct npt_command_ID3D11DeviceContext3_Flush1 {
    void *_self;
    D3D11_CONTEXT_TYPE ContextType;
    HANDLE hEvent;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext3_Flush1(const D3D11_CONTEXT_TYPE ContextType,const HANDLE hEvent)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D11_CONTEXT_TYPE(&ContextType, max_mode);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext3_Flush1(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       D3D11_CONTEXT_TYPE ContextType,
                                       HANDLE hEvent)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext3_Flush1(ContextType,hEvent);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext3,
                                 NPT_METHOD_ID3D11DeviceContext3_Flush1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D11_CONTEXT_TYPE(enc, &ContextType);
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
}

static inline size_t
npt_sizeof_ID3D11DeviceContext3_Flush1_reply(const D3D11_CONTEXT_TYPE ContextType,const HANDLE hEvent)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ContextType (input only) */
/* skip hEvent (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext3_Flush1(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       D3D11_CONTEXT_TYPE ContextType,
                                       HANDLE hEvent,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext3_Flush1(ContextType,hEvent);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext3_Flush1_reply(ContextType,hEvent)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext3_Flush1(enc, cmd_flags, object_id, ContextType, hEvent);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext3_Flush1(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      D3D11_CONTEXT_TYPE ContextType,
                                      HANDLE hEvent)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext3_Flush1(ring, 0, object_id, ContextType, hEvent, &submit);
}

static inline void
npt_call_ID3D11DeviceContext3_Flush1(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     D3D11_CONTEXT_TYPE ContextType,
                                     HANDLE hEvent)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext3_Flush1(ring, NPT_CMD_FLAG_REPLY, object_id, ContextType, hEvent, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext3::SetHardwareProtectionState
 */

struct npt_command_ID3D11DeviceContext3_SetHardwareProtectionState {
    void *_self;
    BOOL HwProtectionEnable;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext3_SetHardwareProtectionState(const BOOL HwProtectionEnable)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_BOOL(&HwProtectionEnable, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext3_SetHardwareProtectionState(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           BOOL HwProtectionEnable)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext3_SetHardwareProtectionState(HwProtectionEnable);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext3,
                                 NPT_METHOD_ID3D11DeviceContext3_SetHardwareProtectionState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_BOOL(enc, &HwProtectionEnable);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext3_SetHardwareProtectionState_reply(const BOOL HwProtectionEnable)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip HwProtectionEnable (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11DeviceContext3_SetHardwareProtectionState(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           BOOL HwProtectionEnable,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext3_SetHardwareProtectionState(HwProtectionEnable);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext3_SetHardwareProtectionState_reply(HwProtectionEnable)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext3_SetHardwareProtectionState(enc, cmd_flags, object_id, HwProtectionEnable);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext3_SetHardwareProtectionState(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          BOOL HwProtectionEnable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext3_SetHardwareProtectionState(ring, 0, object_id, HwProtectionEnable, &submit);
}

static inline void
npt_call_ID3D11DeviceContext3_SetHardwareProtectionState(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         BOOL HwProtectionEnable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext3_SetHardwareProtectionState(ring, NPT_CMD_FLAG_REPLY, object_id, HwProtectionEnable, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext3::GetHardwareProtectionState
 */

struct npt_command_ID3D11DeviceContext3_GetHardwareProtectionState {
    void *_self;
    BOOL * pHwProtectionEnable;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext3_GetHardwareProtectionState(const BOOL * pHwProtectionEnable)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pHwProtectionEnable (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext3_GetHardwareProtectionState(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           BOOL * pHwProtectionEnable)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext3_GetHardwareProtectionState(pHwProtectionEnable);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext3,
                                 NPT_METHOD_ID3D11DeviceContext3_GetHardwareProtectionState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pHwProtectionEnable (output only) */
}

static inline size_t
npt_sizeof_ID3D11DeviceContext3_GetHardwareProtectionState_reply(const BOOL * pHwProtectionEnable)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext3_GetHardwareProtectionState_reply(struct npt_cs_decoder *dec,
                                                                 BOOL * pHwProtectionEnable)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext3,
                                                  NPT_METHOD_ID3D11DeviceContext3_GetHardwareProtectionState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext3_GetHardwareProtectionState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext3,
                                       NPT_METHOD_ID3D11DeviceContext3_GetHardwareProtectionState));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pHwProtectionEnable)
            npt_decode_BOOL(dec, (BOOL *)pHwProtectionEnable);
    }
}

static inline void
npt_submit_ID3D11DeviceContext3_GetHardwareProtectionState(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           BOOL * pHwProtectionEnable,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext3_GetHardwareProtectionState(pHwProtectionEnable);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext3_GetHardwareProtectionState_reply(pHwProtectionEnable)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext3_GetHardwareProtectionState(enc, cmd_flags, object_id, pHwProtectionEnable);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext3_GetHardwareProtectionState(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          BOOL * pHwProtectionEnable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext3_GetHardwareProtectionState(ring, 0, object_id, pHwProtectionEnable, &submit);
}

static inline void
npt_call_ID3D11DeviceContext3_GetHardwareProtectionState(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         BOOL * pHwProtectionEnable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext3_GetHardwareProtectionState(ring, NPT_CMD_FLAG_REPLY, object_id, pHwProtectionEnable, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11DeviceContext3_GetHardwareProtectionState_reply(dec, pHwProtectionEnable);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11DeviceContext4::Signal
 */

struct npt_command_ID3D11DeviceContext4_Signal {
    void *_self;
    ID3D11Fence * pFence;
    UINT64 Value;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext4_Signal(const ID3D11Fence * pFence,const UINT64 Value)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&Value, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext4_Signal(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       ID3D11Fence * pFence,
                                       UINT64 Value)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext4_Signal(pFence,Value);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext4,
                                 NPT_METHOD_ID3D11DeviceContext4_Signal),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pFence));
    npt_encode_UINT64(enc, &Value);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext4_Signal_reply(const ID3D11Fence * pFence,const UINT64 Value)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pFence (input only) */
/* skip Value (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext4_Signal_reply(struct npt_cs_decoder *dec,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext4,
                                                  NPT_METHOD_ID3D11DeviceContext4_Signal))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext4_Signal reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext4,
                                       NPT_METHOD_ID3D11DeviceContext4_Signal));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11DeviceContext4_Signal(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       ID3D11Fence * pFence,
                                       UINT64 Value,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext4_Signal(pFence,Value);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext4_Signal_reply(pFence,Value)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext4_Signal(enc, cmd_flags, object_id, pFence, Value);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext4_Signal(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      ID3D11Fence * pFence,
                                      UINT64 Value)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext4_Signal(ring, 0, object_id, pFence, Value, &submit);
}

static inline HRESULT
npt_call_ID3D11DeviceContext4_Signal(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     ID3D11Fence * pFence,
                                     UINT64 Value)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext4_Signal(ring, NPT_CMD_FLAG_REPLY, object_id, pFence, Value, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11DeviceContext4_Signal_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11DeviceContext4::Wait
 */

struct npt_command_ID3D11DeviceContext4_Wait {
    void *_self;
    ID3D11Fence * pFence;
    UINT64 Value;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11DeviceContext4_Wait(const ID3D11Fence * pFence,const UINT64 Value)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&Value, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11DeviceContext4_Wait(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     ID3D11Fence * pFence,
                                     UINT64 Value)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11DeviceContext4_Wait(pFence,Value);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext4,
                                 NPT_METHOD_ID3D11DeviceContext4_Wait),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pFence));
    npt_encode_UINT64(enc, &Value);
}

static inline size_t
npt_sizeof_ID3D11DeviceContext4_Wait_reply(const ID3D11Fence * pFence,const UINT64 Value)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pFence (input only) */
/* skip Value (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11DeviceContext4_Wait_reply(struct npt_cs_decoder *dec,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext4,
                                                  NPT_METHOD_ID3D11DeviceContext4_Wait))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11DeviceContext4_Wait reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext4,
                                       NPT_METHOD_ID3D11DeviceContext4_Wait));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11DeviceContext4_Wait(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     ID3D11Fence * pFence,
                                     UINT64 Value,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11DeviceContext4_Wait(pFence,Value);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11DeviceContext4_Wait_reply(pFence,Value)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11DeviceContext4_Wait(enc, cmd_flags, object_id, pFence, Value);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11DeviceContext4_Wait(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    ID3D11Fence * pFence,
                                    UINT64 Value)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext4_Wait(ring, 0, object_id, pFence, Value, &submit);
}

static inline HRESULT
npt_call_ID3D11DeviceContext4_Wait(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   ID3D11Fence * pFence,
                                   UINT64 Value)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11DeviceContext4_Wait(ring, NPT_CMD_FLAG_REPLY, object_id, pFence, Value, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11DeviceContext4_Wait_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11DEVICECONTEXT_H */
