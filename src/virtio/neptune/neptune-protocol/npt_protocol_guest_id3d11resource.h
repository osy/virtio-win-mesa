/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11resource.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11RESOURCE_H
#define NPT_PROTOCOL_GUEST_ID3D11RESOURCE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11Resource::GetType
 */

struct npt_command_ID3D11Resource_GetType {
    void *_self;
    D3D11_RESOURCE_DIMENSION * pResourceDimension;
};

static inline size_t
npt_sizeof_ID3D11Resource_GetType(const D3D11_RESOURCE_DIMENSION * pResourceDimension)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pResourceDimension (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Resource_GetType(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  D3D11_RESOURCE_DIMENSION * pResourceDimension)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Resource_GetType(pResourceDimension);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                 NPT_METHOD_ID3D11Resource_GetType),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pResourceDimension (output only) */
}

static inline size_t
npt_sizeof_ID3D11Resource_GetType_reply(const D3D11_RESOURCE_DIMENSION * pResourceDimension)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_RESOURCE_DIMENSION(&(const D3D11_RESOURCE_DIMENSION){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Resource_GetType_reply(struct npt_cs_decoder *dec,
                                        D3D11_RESOURCE_DIMENSION * pResourceDimension)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                                  NPT_METHOD_ID3D11Resource_GetType))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Resource_GetType reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                       NPT_METHOD_ID3D11Resource_GetType));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pResourceDimension)
            npt_decode_D3D11_RESOURCE_DIMENSION(dec, (D3D11_RESOURCE_DIMENSION *)pResourceDimension);
    }
}

static inline void
npt_submit_ID3D11Resource_GetType(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  D3D11_RESOURCE_DIMENSION * pResourceDimension,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Resource_GetType(pResourceDimension);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Resource_GetType_reply(pResourceDimension)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Resource_GetType(enc, cmd_flags, object_id, pResourceDimension);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Resource_GetType(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 D3D11_RESOURCE_DIMENSION * pResourceDimension)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Resource_GetType(ring, 0, object_id, pResourceDimension, &submit);
}

static inline void
npt_call_ID3D11Resource_GetType(struct npt_ring *ring,
                                npt_object_id object_id,
                                D3D11_RESOURCE_DIMENSION * pResourceDimension)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Resource_GetType(ring, NPT_CMD_FLAG_REPLY, object_id, pResourceDimension, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11Resource_GetType_reply(dec, pResourceDimension);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Resource::SetEvictionPriority
 */

struct npt_command_ID3D11Resource_SetEvictionPriority {
    void *_self;
    UINT EvictionPriority;
};

static inline size_t
npt_sizeof_ID3D11Resource_SetEvictionPriority(const UINT EvictionPriority)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&EvictionPriority, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Resource_SetEvictionPriority(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT EvictionPriority)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Resource_SetEvictionPriority(EvictionPriority);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                 NPT_METHOD_ID3D11Resource_SetEvictionPriority),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &EvictionPriority);
}

static inline size_t
npt_sizeof_ID3D11Resource_SetEvictionPriority_reply(const UINT EvictionPriority)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip EvictionPriority (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11Resource_SetEvictionPriority(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT EvictionPriority,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Resource_SetEvictionPriority(EvictionPriority);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Resource_SetEvictionPriority_reply(EvictionPriority)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Resource_SetEvictionPriority(enc, cmd_flags, object_id, EvictionPriority);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Resource_SetEvictionPriority(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT EvictionPriority)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Resource_SetEvictionPriority(ring, 0, object_id, EvictionPriority, &submit);
}

static inline void
npt_call_ID3D11Resource_SetEvictionPriority(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT EvictionPriority)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Resource_SetEvictionPriority(ring, NPT_CMD_FLAG_REPLY, object_id, EvictionPriority, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Resource::GetEvictionPriority
 */

struct npt_command_ID3D11Resource_GetEvictionPriority {
    void *_self;
    UINT ret;
};

static inline size_t
npt_sizeof_ID3D11Resource_GetEvictionPriority(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Resource_GetEvictionPriority(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Resource_GetEvictionPriority();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                 NPT_METHOD_ID3D11Resource_GetEvictionPriority),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11Resource_GetEvictionPriority_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Resource_GetEvictionPriority_reply(struct npt_cs_decoder *dec,
                                                    UINT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                                  NPT_METHOD_ID3D11Resource_GetEvictionPriority))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Resource_GetEvictionPriority reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                       NPT_METHOD_ID3D11Resource_GetEvictionPriority));
        if (ret) *ret = (UINT)0;
        return;
    }
    if (ret) *ret = (UINT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Resource_GetEvictionPriority(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Resource_GetEvictionPriority();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Resource_GetEvictionPriority_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Resource_GetEvictionPriority(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Resource_GetEvictionPriority(struct npt_ring *ring,
                                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Resource_GetEvictionPriority(ring, 0, object_id, &submit);
}

static inline UINT
npt_call_ID3D11Resource_GetEvictionPriority(struct npt_ring *ring,
                                            npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Resource_GetEvictionPriority(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT _ret;
        npt_decode_ID3D11Resource_GetEvictionPriority_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT)0;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11RESOURCE_H */
