/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for id3d12versionedrootsignaturedeserializer: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_id3d12versionedrootsignaturedeserializer.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* ID3D12VersionedRootSignatureDeserializer                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12versionedrootsignaturedeserializer_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12VersionedRootSignatureDeserializer,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12versionedrootsignaturedeserializer_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12versionedrootsignaturedeserializer_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Pre-allocate the guest id and speculatively build the wrapper
     * BEFORE the host round-trip.  On E_NOINTERFACE the wrapper is
     * never used (the host rejected the QI, so its id is unregistered)
     * and we must Release the speculative wrapper to keep the refcount
     * straight.  The host round-trip carries just HRESULT. */
    uint64_t _guest_id = npt_com_allocate_next_id();
    /* Pass `self` as parent so the new wrapper inherits its instance_ring
     * (DC/SC shared ring pinning).  QI typically returns the same host
     * pointer as `self`, so the cache hit short-circuits before any new
     * wrapper is created -- but on the cache-miss path the new wrapper
     * must inherit the same ring or generated methods on the QI'd
     * interface would scatter onto a different host dispatch thread
     * than every other method on the same underlying object. */
    void *wrapper = npt_com_get_or_wrap(
        npt_com_self_device(self), riid, _guest_id,
        (struct npt_com_base *)self);
    if (!wrapper) {
        if (ppvObject) *ppvObject = NULL;
        return NPT_E_OUTOFMEMORY;
    }
    HRESULT host_hr = npt_com_send_query_interface(
        npt_com_self_device(self),
        npt_com_self_id(self),
        riid, _guest_id);
    if (host_hr == NPT_S_OK) {
        if (ppvObject)
            *ppvObject = wrapper;
        return NPT_S_OK;
    }
    /* QI failed host-side: the guest_id stays unregistered.  Release the
     * speculative wrapper so the guest-side refcount drains cleanly.
     * (npt_com_default_release still issues COM_RELEASE to the host,
     * which is a no-op there because the id was never registered.) */
    struct npt_com_base *com = wrapper;
    typedef ULONG (NPT_STDMETHODCALLTYPE *release_fn_t)(void *);
    ((release_fn_t)com->lpVtbl[2])(com);
    if (ppvObject) *ppvObject = NULL;
    return host_hr;
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12versionedrootsignaturedeserializer_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12versionedrootsignaturedeserializer_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12versionedrootsignaturedeserializer_default_GetRootSignatureDescAtVersion(void *self, D3D_ROOT_SIGNATURE_VERSION convertToVersion, const D3D12_VERSIONED_ROOT_SIGNATURE_DESC ** ppDesc)
{
    HRESULT _ret = npt_call_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(npt_com_self_ring(self),npt_com_self_id(self),convertToVersion,ppDesc);
    return _ret;
}

D3D12_VERSIONED_ROOT_SIGNATURE_DESC NPT_STDMETHODCALLTYPE
npt_id3d12versionedrootsignaturedeserializer_default_GetUnconvertedRootSignatureDesc(void *self)
{
    D3D12_VERSIONED_ROOT_SIGNATURE_DESC _ret = npt_call_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc(npt_com_self_ring(self),npt_com_self_id(self));
    return _ret;
}


void
npt_id3d12versionedrootsignaturedeserializer_fill_default_vtbl(struct npt_id3d12versionedrootsignaturedeserializer_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12versionedrootsignaturedeserializer_default_QueryInterface;
    dst->AddRef         = npt_id3d12versionedrootsignaturedeserializer_default_AddRef;
    dst->Release        = npt_id3d12versionedrootsignaturedeserializer_default_Release;
    dst->GetRootSignatureDescAtVersion = npt_id3d12versionedrootsignaturedeserializer_default_GetRootSignatureDescAtVersion;
    dst->GetUnconvertedRootSignatureDesc = npt_id3d12versionedrootsignaturedeserializer_default_GetUnconvertedRootSignatureDesc;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12versionedrootsignaturedeserializer_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12versionedrootsignaturedeserializer_client_vtbl npt_id3d12versionedrootsignaturedeserializer_default_vtbl_storage;

void
npt_id3d12versionedrootsignaturedeserializer_install_default_vtbl(void)
{
    npt_id3d12versionedrootsignaturedeserializer_fill_default_vtbl(&npt_id3d12versionedrootsignaturedeserializer_default_vtbl_storage);
}

void *
npt_id3d12versionedrootsignaturedeserializer_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12versionedrootsignaturedeserializer_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
