/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM declarations for id3d12pipelinelibrary: per-interface vtable
 * structs, default thunk prototypes, default vtable populators, and default
 * constructors.
 *
 * The thunk bodies, populators, and constructors live in the matching .c
 * file.  This split keeps the headers small and prevents N copies of the
 * thunk code from landing in every TU that pulls in this family.
 */

#ifndef NPT_PROTOCOL_CLIENT_ID3D12PIPELINELIBRARY_H
#define NPT_PROTOCOL_CLIENT_ID3D12PIPELINELIBRARY_H

#include "npt_protocol_guest_id3d12pipelinelibrary.h"
#include "npt_protocol_client_id3d12devicechild.h"

/* Forward-declare struct npt_device so the client_create prototypes don't
 * trigger the "declared inside parameter list" warning.  The full
 * definition lives in the consumer's npt_device.h, which the .c file pulls
 * in via npt_com.h. */
struct npt_device;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"



/* ================================================================== */
/* ID3D12PipelineLibrary                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D12PipelineLibraryVtbl, by construction.
 */
struct npt_id3d12pipelinelibrary_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D12Object::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * guid, UINT * pDataSize, void * pData);
    /* slot 4: ID3D12Object::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * guid, UINT DataSize, const void * pData);
    /* slot 5: ID3D12Object::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * guid, const IUnknown * pData);
    /* slot 6: ID3D12Object::SetName */
    HRESULT (NPT_STDMETHODCALLTYPE *SetName)(void *self, const WCHAR * Name);
    /* slot 7: ID3D12DeviceChild::GetDevice */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDevice)(void *self, const IID * riid, void ** ppvDevice);
    /* slot 8: ID3D12PipelineLibrary::StorePipeline */
    HRESULT (NPT_STDMETHODCALLTYPE *StorePipeline)(void *self, const WCHAR * pName, ID3D12PipelineState * pPipeline);
    /* slot 9: ID3D12PipelineLibrary::LoadGraphicsPipeline */
    HRESULT (NPT_STDMETHODCALLTYPE *LoadGraphicsPipeline)(void *self, const WCHAR * pName, const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc, const IID * riid, void ** ppPipelineState);
    /* slot 10: ID3D12PipelineLibrary::LoadComputePipeline */
    HRESULT (NPT_STDMETHODCALLTYPE *LoadComputePipeline)(void *self, const WCHAR * pName, const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc, const IID * riid, void ** ppPipelineState);
    /* slot 11: ID3D12PipelineLibrary::GetSerializedSize */
    SIZE_T (NPT_STDMETHODCALLTYPE *GetSerializedSize)(void *self);
    /* slot 12: ID3D12PipelineLibrary::Serialize */
    HRESULT (NPT_STDMETHODCALLTYPE *Serialize)(void *self, void * pData, SIZE_T DataSizeInBytes);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary_default_StorePipeline(void *self, const WCHAR * pName, ID3D12PipelineState * pPipeline);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary_default_LoadGraphicsPipeline(void *self, const WCHAR * pName, const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc, const IID * riid, void ** ppPipelineState);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary_default_LoadComputePipeline(void *self, const WCHAR * pName, const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc, const IID * riid, void ** ppPipelineState);
extern SIZE_T NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary_default_GetSerializedSize(void *self);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary_default_Serialize(void *self, void * pData, SIZE_T DataSizeInBytes);

/* Default IUnknown thunks for ID3D12PipelineLibrary.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d12pipelinelibrary_fill_default_vtbl(struct npt_id3d12pipelinelibrary_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d12pipelinelibrary_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d12pipelinelibrary_client_vtbl npt_id3d12pipelinelibrary_default_vtbl_storage;
extern void
npt_id3d12pipelinelibrary_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d12pipelinelibrary_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* ID3D12PipelineLibrary1                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * ID3D12PipelineLibrary1Vtbl, by construction.
 */
struct npt_id3d12pipelinelibrary1_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: ID3D12Object::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * guid, UINT * pDataSize, void * pData);
    /* slot 4: ID3D12Object::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * guid, UINT DataSize, const void * pData);
    /* slot 5: ID3D12Object::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * guid, const IUnknown * pData);
    /* slot 6: ID3D12Object::SetName */
    HRESULT (NPT_STDMETHODCALLTYPE *SetName)(void *self, const WCHAR * Name);
    /* slot 7: ID3D12DeviceChild::GetDevice */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDevice)(void *self, const IID * riid, void ** ppvDevice);
    /* slot 8: ID3D12PipelineLibrary::StorePipeline */
    HRESULT (NPT_STDMETHODCALLTYPE *StorePipeline)(void *self, const WCHAR * pName, ID3D12PipelineState * pPipeline);
    /* slot 9: ID3D12PipelineLibrary::LoadGraphicsPipeline */
    HRESULT (NPT_STDMETHODCALLTYPE *LoadGraphicsPipeline)(void *self, const WCHAR * pName, const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc, const IID * riid, void ** ppPipelineState);
    /* slot 10: ID3D12PipelineLibrary::LoadComputePipeline */
    HRESULT (NPT_STDMETHODCALLTYPE *LoadComputePipeline)(void *self, const WCHAR * pName, const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc, const IID * riid, void ** ppPipelineState);
    /* slot 11: ID3D12PipelineLibrary::GetSerializedSize */
    SIZE_T (NPT_STDMETHODCALLTYPE *GetSerializedSize)(void *self);
    /* slot 12: ID3D12PipelineLibrary::Serialize */
    HRESULT (NPT_STDMETHODCALLTYPE *Serialize)(void *self, void * pData, SIZE_T DataSizeInBytes);
    /* slot 13: ID3D12PipelineLibrary1::LoadPipeline */
    HRESULT (NPT_STDMETHODCALLTYPE *LoadPipeline)(void *self, const WCHAR * pName, const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc, const IID * riid, void ** ppPipelineState);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary1_default_LoadPipeline(void *self, const WCHAR * pName, const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc, const IID * riid, void ** ppPipelineState);

/* Default IUnknown thunks for ID3D12PipelineLibrary1.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary1_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary1_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_id3d12pipelinelibrary1_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_id3d12pipelinelibrary1_fill_default_vtbl(struct npt_id3d12pipelinelibrary1_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_id3d12pipelinelibrary1_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_id3d12pipelinelibrary1_client_vtbl npt_id3d12pipelinelibrary1_default_vtbl_storage;
extern void
npt_id3d12pipelinelibrary1_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_id3d12pipelinelibrary1_client_create(struct npt_device *dev, uint64_t host_id);


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_CLIENT_ID3D12PIPELINELIBRARY_H */
