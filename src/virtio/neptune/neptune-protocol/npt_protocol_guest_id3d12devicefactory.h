/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12devicefactory.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12DEVICEFACTORY_H
#define NPT_PROTOCOL_GUEST_ID3D12DEVICEFACTORY_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12DeviceFactory::InitializeFromGlobalState
 */

struct npt_command_ID3D12DeviceFactory_InitializeFromGlobalState {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceFactory_InitializeFromGlobalState(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceFactory_InitializeFromGlobalState(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceFactory_InitializeFromGlobalState();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_InitializeFromGlobalState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12DeviceFactory_InitializeFromGlobalState_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceFactory_InitializeFromGlobalState_reply(struct npt_cs_decoder *dec,
                                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                                  NPT_METHOD_ID3D12DeviceFactory_InitializeFromGlobalState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceFactory_InitializeFromGlobalState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                       NPT_METHOD_ID3D12DeviceFactory_InitializeFromGlobalState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12DeviceFactory_InitializeFromGlobalState(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceFactory_InitializeFromGlobalState();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceFactory_InitializeFromGlobalState_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceFactory_InitializeFromGlobalState(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceFactory_InitializeFromGlobalState(struct npt_ring *ring,
                                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_InitializeFromGlobalState(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceFactory_InitializeFromGlobalState(struct npt_ring *ring,
                                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_InitializeFromGlobalState(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceFactory_InitializeFromGlobalState_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceFactory::ApplyToGlobalState
 */

struct npt_command_ID3D12DeviceFactory_ApplyToGlobalState {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceFactory_ApplyToGlobalState(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceFactory_ApplyToGlobalState(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceFactory_ApplyToGlobalState();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_ApplyToGlobalState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12DeviceFactory_ApplyToGlobalState_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceFactory_ApplyToGlobalState_reply(struct npt_cs_decoder *dec,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                                  NPT_METHOD_ID3D12DeviceFactory_ApplyToGlobalState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceFactory_ApplyToGlobalState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                       NPT_METHOD_ID3D12DeviceFactory_ApplyToGlobalState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12DeviceFactory_ApplyToGlobalState(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceFactory_ApplyToGlobalState();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceFactory_ApplyToGlobalState_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceFactory_ApplyToGlobalState(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceFactory_ApplyToGlobalState(struct npt_ring *ring,
                                                 npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_ApplyToGlobalState(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceFactory_ApplyToGlobalState(struct npt_ring *ring,
                                                npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_ApplyToGlobalState(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceFactory_ApplyToGlobalState_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceFactory::SetFlags
 */

struct npt_command_ID3D12DeviceFactory_SetFlags {
    void *_self;
    D3D12_DEVICE_FACTORY_FLAGS flags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceFactory_SetFlags(const D3D12_DEVICE_FACTORY_FLAGS flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_DEVICE_FACTORY_FLAGS(&flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceFactory_SetFlags(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        D3D12_DEVICE_FACTORY_FLAGS flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceFactory_SetFlags(flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_SetFlags),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_DEVICE_FACTORY_FLAGS(enc, &flags);
}

static inline size_t
npt_sizeof_ID3D12DeviceFactory_SetFlags_reply(const D3D12_DEVICE_FACTORY_FLAGS flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip flags (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceFactory_SetFlags_reply(struct npt_cs_decoder *dec,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                                  NPT_METHOD_ID3D12DeviceFactory_SetFlags))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceFactory_SetFlags reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                       NPT_METHOD_ID3D12DeviceFactory_SetFlags));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12DeviceFactory_SetFlags(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        D3D12_DEVICE_FACTORY_FLAGS flags,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceFactory_SetFlags(flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceFactory_SetFlags_reply(flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceFactory_SetFlags(enc, cmd_flags, object_id, flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceFactory_SetFlags(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       D3D12_DEVICE_FACTORY_FLAGS flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_SetFlags(ring, 0, object_id, flags, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceFactory_SetFlags(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      D3D12_DEVICE_FACTORY_FLAGS flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_SetFlags(ring, NPT_CMD_FLAG_REPLY, object_id, flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceFactory_SetFlags_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceFactory::GetFlags
 */

struct npt_command_ID3D12DeviceFactory_GetFlags {
    void *_self;
    D3D12_DEVICE_FACTORY_FLAGS ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceFactory_GetFlags(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceFactory_GetFlags(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceFactory_GetFlags();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_GetFlags),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12DeviceFactory_GetFlags_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceFactory_GetFlags_reply(struct npt_cs_decoder *dec,
                                              D3D12_DEVICE_FACTORY_FLAGS *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                                  NPT_METHOD_ID3D12DeviceFactory_GetFlags))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceFactory_GetFlags reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                       NPT_METHOD_ID3D12DeviceFactory_GetFlags));
        if (ret) *ret = (D3D12_DEVICE_FACTORY_FLAGS)0;
        return;
    }
    if (ret) *ret = (D3D12_DEVICE_FACTORY_FLAGS)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12DeviceFactory_GetFlags(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceFactory_GetFlags();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceFactory_GetFlags_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceFactory_GetFlags(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceFactory_GetFlags(struct npt_ring *ring,
                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_GetFlags(ring, 0, object_id, &submit);
}

static inline D3D12_DEVICE_FACTORY_FLAGS
npt_call_ID3D12DeviceFactory_GetFlags(struct npt_ring *ring,
                                      npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_GetFlags(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_DEVICE_FACTORY_FLAGS _ret;
        npt_decode_ID3D12DeviceFactory_GetFlags_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_DEVICE_FACTORY_FLAGS)0;
    }
}

/*
 * ID3D12DeviceFactory::GetConfigurationInterface
 */

struct npt_command_ID3D12DeviceFactory_GetConfigurationInterface {
    void *_self;
    const CLSID * clsid;
    const IID * iid;
    void ** ppv;
    /* Shadow: the guest-allocated id for ppv arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppv -> *ppv} in the
     * context object table. */
    uint64_t _guest_id_ppv;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceFactory_GetConfigurationInterface(const CLSID * clsid,const IID * iid,void ** ppv)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(clsid);
    if (clsid)
        cmd_size += npt_sizeof_CLSID(clsid, max_mode);
    cmd_size += npt_sizeof_simple_pointer(iid);
    if (iid)
        cmd_size += npt_sizeof_IID(iid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppv: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceFactory_GetConfigurationInterface(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         const CLSID * clsid,
                                                         const IID * iid,
                                                         void ** ppv)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceFactory_GetConfigurationInterface(clsid,iid,ppv);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_GetConfigurationInterface),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, clsid))
        npt_encode_CLSID(enc, clsid);
    if (npt_encode_simple_pointer(enc, iid))
        npt_encode_IID(enc, iid);
{
    uint64_t _gid_ppv = 0;
    if (ppv) {
        _gid_ppv = npt_com_allocate_next_id();
        *ppv = (void *)(uintptr_t)_gid_ppv;
    }
    npt_encode_uint64_t(enc, &_gid_ppv);
}
}

static inline size_t
npt_sizeof_ID3D12DeviceFactory_GetConfigurationInterface_reply(const CLSID * clsid,const IID * iid,void ** ppv)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip clsid (input only) */
/* skip iid (input only) */
/* skip ppv (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceFactory_GetConfigurationInterface_reply(struct npt_cs_decoder *dec,
                                                               void ** ppv,
                                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                                  NPT_METHOD_ID3D12DeviceFactory_GetConfigurationInterface))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceFactory_GetConfigurationInterface reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                       NPT_METHOD_ID3D12DeviceFactory_GetConfigurationInterface));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppv (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12DeviceFactory_GetConfigurationInterface(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         const CLSID * clsid,
                                                         const IID * iid,
                                                         void ** ppv,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceFactory_GetConfigurationInterface(clsid,iid,ppv);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceFactory_GetConfigurationInterface_reply(clsid,iid,ppv)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceFactory_GetConfigurationInterface(enc, cmd_flags, object_id, clsid, iid, ppv);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceFactory_GetConfigurationInterface(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        const CLSID * clsid,
                                                        const IID * iid,
                                                        void ** ppv)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_GetConfigurationInterface(ring, 0, object_id, clsid, iid, ppv, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceFactory_GetConfigurationInterface(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       const CLSID * clsid,
                                                       const IID * iid,
                                                       void ** ppv)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_GetConfigurationInterface(ring, NPT_CMD_FLAG_REPLY, object_id, clsid, iid, ppv, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceFactory_GetConfigurationInterface_reply(dec, ppv, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceFactory::EnableExperimentalFeatures
 */

struct npt_command_ID3D12DeviceFactory_EnableExperimentalFeatures {
    void *_self;
    UINT NumFeatures;
    const IID * pIIDs;
    void * pConfigurationStructs;
    UINT * pConfigurationStructSizes;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceFactory_EnableExperimentalFeatures(const UINT NumFeatures,const IID * pIIDs,const void * pConfigurationStructs,const UINT * pConfigurationStructSizes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumFeatures, max_mode);
    cmd_size += npt_sizeof_array_count(pIIDs ? NumFeatures : 0);
    for (uint32_t _i = 0; _i < (pIIDs ? NumFeatures : 0); _i++)
        cmd_size += npt_sizeof_IID(&pIIDs[_i], max_mode);
    if (pConfigurationStructs) {
        cmd_size += npt_sizeof_array_count(NumFeatures);
        cmd_size += npt_sizeof_blob_array(pConfigurationStructs, NumFeatures);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_array_count(pConfigurationStructSizes ? NumFeatures : 0);
    if (pConfigurationStructSizes)
        cmd_size += npt_sizeof_UINT_array(pConfigurationStructSizes, NumFeatures);
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceFactory_EnableExperimentalFeatures(struct npt_cs_encoder *enc,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          UINT NumFeatures,
                                                          const IID * pIIDs,
                                                          void * pConfigurationStructs,
                                                          UINT * pConfigurationStructSizes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceFactory_EnableExperimentalFeatures(NumFeatures,pIIDs,pConfigurationStructs,pConfigurationStructSizes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_EnableExperimentalFeatures),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumFeatures);
    if (pIIDs) {
        npt_encode_array_count(enc, NumFeatures);
        for (uint32_t _i = 0; _i < (uint32_t)(NumFeatures); _i++)
            npt_encode_IID(enc, &pIIDs[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pConfigurationStructs) {
        npt_encode_array_count(enc, NumFeatures);
        npt_encode_blob_array(enc, pConfigurationStructs, NumFeatures);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pConfigurationStructSizes) {
        npt_encode_array_count(enc, NumFeatures);
        npt_encode_UINT_array(enc, pConfigurationStructSizes, NumFeatures);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12DeviceFactory_EnableExperimentalFeatures_reply(const UINT NumFeatures,const IID * pIIDs,const void * pConfigurationStructs,const UINT * pConfigurationStructSizes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumFeatures (input only) */
/* skip pIIDs (input only) */
/* skip pConfigurationStructs (input only) */
/* skip pConfigurationStructSizes (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceFactory_EnableExperimentalFeatures_reply(struct npt_cs_decoder *dec,
                                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                                  NPT_METHOD_ID3D12DeviceFactory_EnableExperimentalFeatures))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceFactory_EnableExperimentalFeatures reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                       NPT_METHOD_ID3D12DeviceFactory_EnableExperimentalFeatures));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12DeviceFactory_EnableExperimentalFeatures(struct npt_ring *ring,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          UINT NumFeatures,
                                                          const IID * pIIDs,
                                                          void * pConfigurationStructs,
                                                          UINT * pConfigurationStructSizes,
                                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceFactory_EnableExperimentalFeatures(NumFeatures,pIIDs,pConfigurationStructs,pConfigurationStructSizes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceFactory_EnableExperimentalFeatures_reply(NumFeatures,pIIDs,pConfigurationStructs,pConfigurationStructSizes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceFactory_EnableExperimentalFeatures(enc, cmd_flags, object_id, NumFeatures, pIIDs, pConfigurationStructs, pConfigurationStructSizes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceFactory_EnableExperimentalFeatures(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         UINT NumFeatures,
                                                         const IID * pIIDs,
                                                         void * pConfigurationStructs,
                                                         UINT * pConfigurationStructSizes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_EnableExperimentalFeatures(ring, 0, object_id, NumFeatures, pIIDs, pConfigurationStructs, pConfigurationStructSizes, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceFactory_EnableExperimentalFeatures(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        UINT NumFeatures,
                                                        const IID * pIIDs,
                                                        void * pConfigurationStructs,
                                                        UINT * pConfigurationStructSizes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_EnableExperimentalFeatures(ring, NPT_CMD_FLAG_REPLY, object_id, NumFeatures, pIIDs, pConfigurationStructs, pConfigurationStructSizes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceFactory_EnableExperimentalFeatures_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceFactory::CreateDevice
 */

struct npt_command_ID3D12DeviceFactory_CreateDevice {
    void *_self;
    IUnknown * adapter;
    D3D_FEATURE_LEVEL FeatureLevel;
    const IID * riid;
    void ** ppvDevice;
    /* Shadow: the guest-allocated id for ppvDevice arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvDevice -> *ppvDevice} in the
     * context object table. */
    uint64_t _guest_id_ppvDevice;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceFactory_CreateDevice(const IUnknown * adapter,const D3D_FEATURE_LEVEL FeatureLevel,const IID * riid,void ** ppvDevice)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D_FEATURE_LEVEL(&FeatureLevel, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvDevice: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceFactory_CreateDevice(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            IUnknown * adapter,
                                            D3D_FEATURE_LEVEL FeatureLevel,
                                            const IID * riid,
                                            void ** ppvDevice)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceFactory_CreateDevice(adapter,FeatureLevel,riid,ppvDevice);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_CreateDevice),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(adapter));
    npt_encode_D3D_FEATURE_LEVEL(enc, &FeatureLevel);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvDevice = 0;
    if (ppvDevice) {
        _gid_ppvDevice = npt_com_allocate_next_id();
        *ppvDevice = (void *)(uintptr_t)_gid_ppvDevice;
    }
    npt_encode_uint64_t(enc, &_gid_ppvDevice);
}
}

static inline size_t
npt_sizeof_ID3D12DeviceFactory_CreateDevice_reply(const IUnknown * adapter,const D3D_FEATURE_LEVEL FeatureLevel,const IID * riid,void ** ppvDevice)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip adapter (input only) */
/* skip FeatureLevel (input only) */
/* skip riid (input only) */
/* skip ppvDevice (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceFactory_CreateDevice_reply(struct npt_cs_decoder *dec,
                                                  void ** ppvDevice,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                                  NPT_METHOD_ID3D12DeviceFactory_CreateDevice))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceFactory_CreateDevice reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                       NPT_METHOD_ID3D12DeviceFactory_CreateDevice));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvDevice (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12DeviceFactory_CreateDevice(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            IUnknown * adapter,
                                            D3D_FEATURE_LEVEL FeatureLevel,
                                            const IID * riid,
                                            void ** ppvDevice,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceFactory_CreateDevice(adapter,FeatureLevel,riid,ppvDevice);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceFactory_CreateDevice_reply(adapter,FeatureLevel,riid,ppvDevice)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceFactory_CreateDevice(enc, cmd_flags, object_id, adapter, FeatureLevel, riid, ppvDevice);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceFactory_CreateDevice(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           IUnknown * adapter,
                                           D3D_FEATURE_LEVEL FeatureLevel,
                                           const IID * riid,
                                           void ** ppvDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_CreateDevice(ring, 0, object_id, adapter, FeatureLevel, riid, ppvDevice, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceFactory_CreateDevice(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          IUnknown * adapter,
                                          D3D_FEATURE_LEVEL FeatureLevel,
                                          const IID * riid,
                                          void ** ppvDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceFactory_CreateDevice(ring, NPT_CMD_FLAG_REPLY, object_id, adapter, FeatureLevel, riid, ppvDevice, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceFactory_CreateDevice_reply(dec, ppvDevice, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12DEVICEFACTORY_H */
