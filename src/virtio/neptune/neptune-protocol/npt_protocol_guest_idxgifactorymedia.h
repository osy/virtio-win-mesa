/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgifactorymedia.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIFACTORYMEDIA_H
#define NPT_PROTOCOL_GUEST_IDXGIFACTORYMEDIA_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIFactoryMedia::CreateSwapChainForCompositionSurfaceHandle
 */

struct npt_command_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle {
    void *_self;
    IUnknown * pDevice;
    HANDLE hSurface;
    const DXGI_SWAP_CHAIN_DESC1 * pDesc;
    IDXGIOutput * pRestrictToOutput;
    IDXGISwapChain1 ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(const IUnknown * pDevice,const HANDLE hSurface,const DXGI_SWAP_CHAIN_DESC1 * pDesc,const IDXGIOutput * pRestrictToOutput,IDXGISwapChain1 ** ppSwapChain)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_DESC1(pDesc, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppSwapChain: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(struct npt_cs_encoder *enc,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        IUnknown * pDevice,
                                                                        HANDLE hSurface,
                                                                        const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                                        IDXGIOutput * pRestrictToOutput,
                                                                        IDXGISwapChain1 ** ppSwapChain)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(pDevice,hSurface,pDesc,pRestrictToOutput,ppSwapChain);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactoryMedia,
                                 NPT_METHOD_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hSurface));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_DXGI_SWAP_CHAIN_DESC1(enc, pDesc);
    npt_encode_com_handle(enc, npt_object_get_id(pRestrictToOutput));
{
    uint64_t _gid_ppSwapChain = 0;
    if (ppSwapChain) {
        _gid_ppSwapChain = npt_com_allocate_next_id();
        *ppSwapChain = (IDXGISwapChain1 *)(uintptr_t)_gid_ppSwapChain;
    }
    npt_encode_uint64_t(enc, &_gid_ppSwapChain);
}
}

static inline size_t
npt_sizeof_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle_reply(const IUnknown * pDevice,const HANDLE hSurface,const DXGI_SWAP_CHAIN_DESC1 * pDesc,const IDXGIOutput * pRestrictToOutput,IDXGISwapChain1 ** ppSwapChain)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip hSurface (input only) */
/* skip pDesc (input only) */
/* skip pRestrictToOutput (input only) */
/* skip ppSwapChain (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle_reply(struct npt_cs_decoder *dec,
                                                                              IDXGISwapChain1 ** ppSwapChain,
                                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactoryMedia,
                                                  NPT_METHOD_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactoryMedia,
                                       NPT_METHOD_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSwapChain (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(struct npt_ring *ring,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        IUnknown * pDevice,
                                                                        HANDLE hSurface,
                                                                        const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                                        IDXGIOutput * pRestrictToOutput,
                                                                        IDXGISwapChain1 ** ppSwapChain,
                                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(pDevice,hSurface,pDesc,pRestrictToOutput,ppSwapChain);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle_reply(pDevice,hSurface,pDesc,pRestrictToOutput,ppSwapChain)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(enc, cmd_flags, object_id, pDevice, hSurface, pDesc, pRestrictToOutput, ppSwapChain);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(struct npt_ring *ring,
                                                                       npt_object_id object_id,
                                                                       IUnknown * pDevice,
                                                                       HANDLE hSurface,
                                                                       const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                                       IDXGIOutput * pRestrictToOutput,
                                                                       IDXGISwapChain1 ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(ring, 0, object_id, pDevice, hSurface, pDesc, pRestrictToOutput, ppSwapChain, &submit);
}

static inline HRESULT
npt_call_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      IUnknown * pDevice,
                                                                      HANDLE hSurface,
                                                                      const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                                      IDXGIOutput * pRestrictToOutput,
                                                                      IDXGISwapChain1 ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle(ring, NPT_CMD_FLAG_REPLY, object_id, pDevice, hSurface, pDesc, pRestrictToOutput, ppSwapChain, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactoryMedia_CreateSwapChainForCompositionSurfaceHandle_reply(dec, ppSwapChain, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactoryMedia::CreateDecodeSwapChainForCompositionSurfaceHandle
 */

struct npt_command_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle {
    void *_self;
    IUnknown * pDevice;
    HANDLE hSurface;
    DXGI_DECODE_SWAP_CHAIN_DESC * pDesc;
    IDXGIResource * pYuvDecodeBuffers;
    IDXGIOutput * pRestrictToOutput;
    IDXGIDecodeSwapChain ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(const IUnknown * pDevice,const HANDLE hSurface,const DXGI_DECODE_SWAP_CHAIN_DESC * pDesc,const IDXGIResource * pYuvDecodeBuffers,const IDXGIOutput * pRestrictToOutput,IDXGIDecodeSwapChain ** ppSwapChain)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_DXGI_DECODE_SWAP_CHAIN_DESC(pDesc, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppSwapChain: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(struct npt_cs_encoder *enc,
                                                                              uint32_t cmd_flags,
                                                                              npt_object_id object_id,
                                                                              IUnknown * pDevice,
                                                                              HANDLE hSurface,
                                                                              DXGI_DECODE_SWAP_CHAIN_DESC * pDesc,
                                                                              IDXGIResource * pYuvDecodeBuffers,
                                                                              IDXGIOutput * pRestrictToOutput,
                                                                              IDXGIDecodeSwapChain ** ppSwapChain)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(pDevice,hSurface,pDesc,pYuvDecodeBuffers,pRestrictToOutput,ppSwapChain);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactoryMedia,
                                 NPT_METHOD_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hSurface));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_DXGI_DECODE_SWAP_CHAIN_DESC(enc, pDesc);
    npt_encode_com_handle(enc, npt_object_get_id(pYuvDecodeBuffers));
    npt_encode_com_handle(enc, npt_object_get_id(pRestrictToOutput));
{
    uint64_t _gid_ppSwapChain = 0;
    if (ppSwapChain) {
        _gid_ppSwapChain = npt_com_allocate_next_id();
        *ppSwapChain = (IDXGIDecodeSwapChain *)(uintptr_t)_gid_ppSwapChain;
    }
    npt_encode_uint64_t(enc, &_gid_ppSwapChain);
}
}

static inline size_t
npt_sizeof_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle_reply(const IUnknown * pDevice,const HANDLE hSurface,const DXGI_DECODE_SWAP_CHAIN_DESC * pDesc,const IDXGIResource * pYuvDecodeBuffers,const IDXGIOutput * pRestrictToOutput,IDXGIDecodeSwapChain ** ppSwapChain)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip hSurface (input only) */
/* skip pDesc (input only) */
/* skip pYuvDecodeBuffers (input only) */
/* skip pRestrictToOutput (input only) */
/* skip ppSwapChain (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle_reply(struct npt_cs_decoder *dec,
                                                                                    IDXGIDecodeSwapChain ** ppSwapChain,
                                                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactoryMedia,
                                                  NPT_METHOD_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactoryMedia,
                                       NPT_METHOD_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSwapChain (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(struct npt_ring *ring,
                                                                              uint32_t cmd_flags,
                                                                              npt_object_id object_id,
                                                                              IUnknown * pDevice,
                                                                              HANDLE hSurface,
                                                                              DXGI_DECODE_SWAP_CHAIN_DESC * pDesc,
                                                                              IDXGIResource * pYuvDecodeBuffers,
                                                                              IDXGIOutput * pRestrictToOutput,
                                                                              IDXGIDecodeSwapChain ** ppSwapChain,
                                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(pDevice,hSurface,pDesc,pYuvDecodeBuffers,pRestrictToOutput,ppSwapChain);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle_reply(pDevice,hSurface,pDesc,pYuvDecodeBuffers,pRestrictToOutput,ppSwapChain)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(enc, cmd_flags, object_id, pDevice, hSurface, pDesc, pYuvDecodeBuffers, pRestrictToOutput, ppSwapChain);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(struct npt_ring *ring,
                                                                             npt_object_id object_id,
                                                                             IUnknown * pDevice,
                                                                             HANDLE hSurface,
                                                                             DXGI_DECODE_SWAP_CHAIN_DESC * pDesc,
                                                                             IDXGIResource * pYuvDecodeBuffers,
                                                                             IDXGIOutput * pRestrictToOutput,
                                                                             IDXGIDecodeSwapChain ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(ring, 0, object_id, pDevice, hSurface, pDesc, pYuvDecodeBuffers, pRestrictToOutput, ppSwapChain, &submit);
}

static inline HRESULT
npt_call_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(struct npt_ring *ring,
                                                                            npt_object_id object_id,
                                                                            IUnknown * pDevice,
                                                                            HANDLE hSurface,
                                                                            DXGI_DECODE_SWAP_CHAIN_DESC * pDesc,
                                                                            IDXGIResource * pYuvDecodeBuffers,
                                                                            IDXGIOutput * pRestrictToOutput,
                                                                            IDXGIDecodeSwapChain ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle(ring, NPT_CMD_FLAG_REPLY, object_id, pDevice, hSurface, pDesc, pYuvDecodeBuffers, pRestrictToOutput, ppSwapChain, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactoryMedia_CreateDecodeSwapChainForCompositionSurfaceHandle_reply(dec, ppSwapChain, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIFACTORYMEDIA_H */
