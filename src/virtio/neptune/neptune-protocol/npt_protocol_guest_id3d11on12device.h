/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11on12device.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11ON12DEVICE_H
#define NPT_PROTOCOL_GUEST_ID3D11ON12DEVICE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11On12Device::CreateWrappedResource
 */

struct npt_command_ID3D11On12Device_CreateWrappedResource {
    void *_self;
    IUnknown * pResource12;
    const D3D11_RESOURCE_FLAGS * pFlags11;
    D3D12_RESOURCE_STATES InState;
    D3D12_RESOURCE_STATES OutState;
    const IID * riid;
    void ** ppResource11;
    /* Shadow: the guest-allocated id for ppResource11 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResource11 -> *ppResource11} in the
     * context object table. */
    uint64_t _guest_id_ppResource11;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11On12Device_CreateWrappedResource(const IUnknown * pResource12,const D3D11_RESOURCE_FLAGS * pFlags11,const D3D12_RESOURCE_STATES InState,const D3D12_RESOURCE_STATES OutState,const IID * riid,void ** ppResource11)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pFlags11);
    if (pFlags11)
        cmd_size += npt_sizeof_D3D11_RESOURCE_FLAGS(pFlags11, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOURCE_STATES(&InState, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOURCE_STATES(&OutState, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppResource11: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11On12Device_CreateWrappedResource(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  IUnknown * pResource12,
                                                  const D3D11_RESOURCE_FLAGS * pFlags11,
                                                  D3D12_RESOURCE_STATES InState,
                                                  D3D12_RESOURCE_STATES OutState,
                                                  const IID * riid,
                                                  void ** ppResource11)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11On12Device_CreateWrappedResource(pResource12,pFlags11,InState,OutState,riid,ppResource11);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device,
                                 NPT_METHOD_ID3D11On12Device_CreateWrappedResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource12));
    if (npt_encode_simple_pointer(enc, pFlags11))
        npt_encode_D3D11_RESOURCE_FLAGS(enc, pFlags11);
    npt_encode_D3D12_RESOURCE_STATES(enc, &InState);
    npt_encode_D3D12_RESOURCE_STATES(enc, &OutState);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppResource11 = 0;
    if (ppResource11) {
        _gid_ppResource11 = npt_com_allocate_next_id();
        *ppResource11 = (void *)(uintptr_t)_gid_ppResource11;
    }
    npt_encode_uint64_t(enc, &_gid_ppResource11);
}
}

static inline size_t
npt_sizeof_ID3D11On12Device_CreateWrappedResource_reply(const IUnknown * pResource12,const D3D11_RESOURCE_FLAGS * pFlags11,const D3D12_RESOURCE_STATES InState,const D3D12_RESOURCE_STATES OutState,const IID * riid,void ** ppResource11)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource12 (input only) */
/* skip pFlags11 (input only) */
/* skip InState (input only) */
/* skip OutState (input only) */
/* skip riid (input only) */
/* skip ppResource11 (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11On12Device_CreateWrappedResource_reply(struct npt_cs_decoder *dec,
                                                        void ** ppResource11,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device,
                                                  NPT_METHOD_ID3D11On12Device_CreateWrappedResource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11On12Device_CreateWrappedResource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device,
                                       NPT_METHOD_ID3D11On12Device_CreateWrappedResource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppResource11 (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11On12Device_CreateWrappedResource(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  IUnknown * pResource12,
                                                  const D3D11_RESOURCE_FLAGS * pFlags11,
                                                  D3D12_RESOURCE_STATES InState,
                                                  D3D12_RESOURCE_STATES OutState,
                                                  const IID * riid,
                                                  void ** ppResource11,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11On12Device_CreateWrappedResource(pResource12,pFlags11,InState,OutState,riid,ppResource11);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11On12Device_CreateWrappedResource_reply(pResource12,pFlags11,InState,OutState,riid,ppResource11)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11On12Device_CreateWrappedResource(enc, cmd_flags, object_id, pResource12, pFlags11, InState, OutState, riid, ppResource11);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11On12Device_CreateWrappedResource(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 IUnknown * pResource12,
                                                 const D3D11_RESOURCE_FLAGS * pFlags11,
                                                 D3D12_RESOURCE_STATES InState,
                                                 D3D12_RESOURCE_STATES OutState,
                                                 const IID * riid,
                                                 void ** ppResource11)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device_CreateWrappedResource(ring, 0, object_id, pResource12, pFlags11, InState, OutState, riid, ppResource11, &submit);
}

static inline HRESULT
npt_call_ID3D11On12Device_CreateWrappedResource(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                IUnknown * pResource12,
                                                const D3D11_RESOURCE_FLAGS * pFlags11,
                                                D3D12_RESOURCE_STATES InState,
                                                D3D12_RESOURCE_STATES OutState,
                                                const IID * riid,
                                                void ** ppResource11)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device_CreateWrappedResource(ring, NPT_CMD_FLAG_REPLY, object_id, pResource12, pFlags11, InState, OutState, riid, ppResource11, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11On12Device_CreateWrappedResource_reply(dec, ppResource11, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11On12Device::ReleaseWrappedResources
 */

struct npt_command_ID3D11On12Device_ReleaseWrappedResources {
    void *_self;
    ID3D11Resource ** ppResources;
    UINT NumResources;
};

static inline size_t
npt_sizeof_ID3D11On12Device_ReleaseWrappedResources(ID3D11Resource ** ppResources,const UINT NumResources)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_array_count(NumResources);
    cmd_size += sizeof(uint64_t) * NumResources;
    cmd_size += npt_sizeof_UINT(&NumResources, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11On12Device_ReleaseWrappedResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    ID3D11Resource ** ppResources,
                                                    UINT NumResources)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11On12Device_ReleaseWrappedResources(ppResources,NumResources);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device,
                                 NPT_METHOD_ID3D11On12Device_ReleaseWrappedResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (ppResources) {
        npt_encode_array_count(enc, NumResources);
        for (uint32_t _i = 0; _i < (uint32_t)NumResources; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppResources[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumResources);
}

static inline size_t
npt_sizeof_ID3D11On12Device_ReleaseWrappedResources_reply(ID3D11Resource ** ppResources,const UINT NumResources)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppResources (input only) */
/* skip NumResources (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11On12Device_ReleaseWrappedResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    ID3D11Resource ** ppResources,
                                                    UINT NumResources,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11On12Device_ReleaseWrappedResources(ppResources,NumResources);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11On12Device_ReleaseWrappedResources_reply(ppResources,NumResources)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11On12Device_ReleaseWrappedResources(enc, cmd_flags, object_id, ppResources, NumResources);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11On12Device_ReleaseWrappedResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11Resource ** ppResources,
                                                   UINT NumResources)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device_ReleaseWrappedResources(ring, 0, object_id, ppResources, NumResources, &submit);
}

static inline void
npt_call_ID3D11On12Device_ReleaseWrappedResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D11Resource ** ppResources,
                                                  UINT NumResources)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device_ReleaseWrappedResources(ring, NPT_CMD_FLAG_REPLY, object_id, ppResources, NumResources, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11On12Device::AcquireWrappedResources
 */

struct npt_command_ID3D11On12Device_AcquireWrappedResources {
    void *_self;
    ID3D11Resource ** ppResources;
    UINT NumResources;
};

static inline size_t
npt_sizeof_ID3D11On12Device_AcquireWrappedResources(ID3D11Resource ** ppResources,const UINT NumResources)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_array_count(NumResources);
    cmd_size += sizeof(uint64_t) * NumResources;
    cmd_size += npt_sizeof_UINT(&NumResources, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11On12Device_AcquireWrappedResources(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    ID3D11Resource ** ppResources,
                                                    UINT NumResources)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11On12Device_AcquireWrappedResources(ppResources,NumResources);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device,
                                 NPT_METHOD_ID3D11On12Device_AcquireWrappedResources),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (ppResources) {
        npt_encode_array_count(enc, NumResources);
        for (uint32_t _i = 0; _i < (uint32_t)NumResources; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppResources[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumResources);
}

static inline size_t
npt_sizeof_ID3D11On12Device_AcquireWrappedResources_reply(ID3D11Resource ** ppResources,const UINT NumResources)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppResources (input only) */
/* skip NumResources (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11On12Device_AcquireWrappedResources(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    ID3D11Resource ** ppResources,
                                                    UINT NumResources,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11On12Device_AcquireWrappedResources(ppResources,NumResources);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11On12Device_AcquireWrappedResources_reply(ppResources,NumResources)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11On12Device_AcquireWrappedResources(enc, cmd_flags, object_id, ppResources, NumResources);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11On12Device_AcquireWrappedResources(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11Resource ** ppResources,
                                                   UINT NumResources)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device_AcquireWrappedResources(ring, 0, object_id, ppResources, NumResources, &submit);
}

static inline void
npt_call_ID3D11On12Device_AcquireWrappedResources(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D11Resource ** ppResources,
                                                  UINT NumResources)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device_AcquireWrappedResources(ring, NPT_CMD_FLAG_REPLY, object_id, ppResources, NumResources, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11On12Device1::GetD3D12Device
 */

struct npt_command_ID3D11On12Device1_GetD3D12Device {
    void *_self;
    const IID * riid;
    void ** ppvDevice;
    /* Shadow: the guest-allocated id for ppvDevice arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvDevice -> *ppvDevice} in the
     * context object table. */
    uint64_t _guest_id_ppvDevice;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11On12Device1_GetD3D12Device(const IID * riid,void ** ppvDevice)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvDevice: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11On12Device1_GetD3D12Device(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            const IID * riid,
                                            void ** ppvDevice)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11On12Device1_GetD3D12Device(riid,ppvDevice);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device1,
                                 NPT_METHOD_ID3D11On12Device1_GetD3D12Device),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvDevice = 0;
    if (ppvDevice) {
        _gid_ppvDevice = npt_com_allocate_next_id();
        *ppvDevice = (void *)(uintptr_t)_gid_ppvDevice;
    }
    npt_encode_uint64_t(enc, &_gid_ppvDevice);
}
}

static inline size_t
npt_sizeof_ID3D11On12Device1_GetD3D12Device_reply(const IID * riid,void ** ppvDevice)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip riid (input only) */
/* skip ppvDevice (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11On12Device1_GetD3D12Device_reply(struct npt_cs_decoder *dec,
                                                  void ** ppvDevice,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device1,
                                                  NPT_METHOD_ID3D11On12Device1_GetD3D12Device))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11On12Device1_GetD3D12Device reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device1,
                                       NPT_METHOD_ID3D11On12Device1_GetD3D12Device));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvDevice (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11On12Device1_GetD3D12Device(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            const IID * riid,
                                            void ** ppvDevice,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11On12Device1_GetD3D12Device(riid,ppvDevice);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11On12Device1_GetD3D12Device_reply(riid,ppvDevice)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11On12Device1_GetD3D12Device(enc, cmd_flags, object_id, riid, ppvDevice);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11On12Device1_GetD3D12Device(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           const IID * riid,
                                           void ** ppvDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device1_GetD3D12Device(ring, 0, object_id, riid, ppvDevice, &submit);
}

static inline HRESULT
npt_call_ID3D11On12Device1_GetD3D12Device(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const IID * riid,
                                          void ** ppvDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device1_GetD3D12Device(ring, NPT_CMD_FLAG_REPLY, object_id, riid, ppvDevice, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11On12Device1_GetD3D12Device_reply(dec, ppvDevice, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11On12Device2::UnwrapUnderlyingResource
 */

struct npt_command_ID3D11On12Device2_UnwrapUnderlyingResource {
    void *_self;
    ID3D11Resource * pResource11;
    ID3D12CommandQueue * pCommandQueue;
    const IID * riid;
    void ** ppvResource12;
    /* Shadow: the guest-allocated id for ppvResource12 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource12 -> *ppvResource12} in the
     * context object table. */
    uint64_t _guest_id_ppvResource12;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11On12Device2_UnwrapUnderlyingResource(const ID3D11Resource * pResource11,const ID3D12CommandQueue * pCommandQueue,const IID * riid,void ** ppvResource12)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource12: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11On12Device2_UnwrapUnderlyingResource(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11Resource * pResource11,
                                                      ID3D12CommandQueue * pCommandQueue,
                                                      const IID * riid,
                                                      void ** ppvResource12)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11On12Device2_UnwrapUnderlyingResource(pResource11,pCommandQueue,riid,ppvResource12);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device2,
                                 NPT_METHOD_ID3D11On12Device2_UnwrapUnderlyingResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource11));
    npt_encode_com_handle(enc, npt_object_get_id(pCommandQueue));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvResource12 = 0;
    if (ppvResource12) {
        _gid_ppvResource12 = npt_com_allocate_next_id();
        *ppvResource12 = (void *)(uintptr_t)_gid_ppvResource12;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource12);
}
}

static inline size_t
npt_sizeof_ID3D11On12Device2_UnwrapUnderlyingResource_reply(const ID3D11Resource * pResource11,const ID3D12CommandQueue * pCommandQueue,const IID * riid,void ** ppvResource12)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource11 (input only) */
/* skip pCommandQueue (input only) */
/* skip riid (input only) */
/* skip ppvResource12 (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11On12Device2_UnwrapUnderlyingResource_reply(struct npt_cs_decoder *dec,
                                                            void ** ppvResource12,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device2,
                                                  NPT_METHOD_ID3D11On12Device2_UnwrapUnderlyingResource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11On12Device2_UnwrapUnderlyingResource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device2,
                                       NPT_METHOD_ID3D11On12Device2_UnwrapUnderlyingResource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource12 (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11On12Device2_UnwrapUnderlyingResource(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11Resource * pResource11,
                                                      ID3D12CommandQueue * pCommandQueue,
                                                      const IID * riid,
                                                      void ** ppvResource12,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11On12Device2_UnwrapUnderlyingResource(pResource11,pCommandQueue,riid,ppvResource12);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11On12Device2_UnwrapUnderlyingResource_reply(pResource11,pCommandQueue,riid,ppvResource12)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11On12Device2_UnwrapUnderlyingResource(enc, cmd_flags, object_id, pResource11, pCommandQueue, riid, ppvResource12);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11On12Device2_UnwrapUnderlyingResource(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3D11Resource * pResource11,
                                                     ID3D12CommandQueue * pCommandQueue,
                                                     const IID * riid,
                                                     void ** ppvResource12)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device2_UnwrapUnderlyingResource(ring, 0, object_id, pResource11, pCommandQueue, riid, ppvResource12, &submit);
}

static inline HRESULT
npt_call_ID3D11On12Device2_UnwrapUnderlyingResource(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11Resource * pResource11,
                                                    ID3D12CommandQueue * pCommandQueue,
                                                    const IID * riid,
                                                    void ** ppvResource12)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device2_UnwrapUnderlyingResource(ring, NPT_CMD_FLAG_REPLY, object_id, pResource11, pCommandQueue, riid, ppvResource12, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11On12Device2_UnwrapUnderlyingResource_reply(dec, ppvResource12, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11On12Device2::ReturnUnderlyingResource
 */

struct npt_command_ID3D11On12Device2_ReturnUnderlyingResource {
    void *_self;
    ID3D11Resource * pResource11;
    UINT NumSync;
    UINT64 * pSignalValues;
    ID3D12Fence ** ppFences;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11On12Device2_ReturnUnderlyingResource(const ID3D11Resource * pResource11,const UINT NumSync,const UINT64 * pSignalValues,ID3D12Fence ** ppFences)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&NumSync, max_mode);
    cmd_size += npt_sizeof_array_count(pSignalValues ? NumSync : 0);
    if (pSignalValues)
        cmd_size += npt_sizeof_UINT64_array(pSignalValues, NumSync);
    cmd_size += npt_sizeof_array_count(NumSync);
    cmd_size += sizeof(uint64_t) * NumSync;
    return cmd_size;
}

static inline void
npt_encode_ID3D11On12Device2_ReturnUnderlyingResource(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11Resource * pResource11,
                                                      UINT NumSync,
                                                      UINT64 * pSignalValues,
                                                      ID3D12Fence ** ppFences)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11On12Device2_ReturnUnderlyingResource(pResource11,NumSync,pSignalValues,ppFences);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device2,
                                 NPT_METHOD_ID3D11On12Device2_ReturnUnderlyingResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource11));
    npt_encode_UINT(enc, &NumSync);
    if (pSignalValues) {
        npt_encode_array_count(enc, NumSync);
        npt_encode_UINT64_array(enc, pSignalValues, NumSync);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (ppFences) {
        npt_encode_array_count(enc, NumSync);
        for (uint32_t _i = 0; _i < (uint32_t)NumSync; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppFences[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11On12Device2_ReturnUnderlyingResource_reply(const ID3D11Resource * pResource11,const UINT NumSync,const UINT64 * pSignalValues,ID3D12Fence ** ppFences)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource11 (input only) */
/* skip NumSync (input only) */
/* skip pSignalValues (input only) */
/* skip ppFences (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11On12Device2_ReturnUnderlyingResource_reply(struct npt_cs_decoder *dec,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device2,
                                                  NPT_METHOD_ID3D11On12Device2_ReturnUnderlyingResource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11On12Device2_ReturnUnderlyingResource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device2,
                                       NPT_METHOD_ID3D11On12Device2_ReturnUnderlyingResource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11On12Device2_ReturnUnderlyingResource(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11Resource * pResource11,
                                                      UINT NumSync,
                                                      UINT64 * pSignalValues,
                                                      ID3D12Fence ** ppFences,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11On12Device2_ReturnUnderlyingResource(pResource11,NumSync,pSignalValues,ppFences);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11On12Device2_ReturnUnderlyingResource_reply(pResource11,NumSync,pSignalValues,ppFences)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11On12Device2_ReturnUnderlyingResource(enc, cmd_flags, object_id, pResource11, NumSync, pSignalValues, ppFences);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11On12Device2_ReturnUnderlyingResource(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3D11Resource * pResource11,
                                                     UINT NumSync,
                                                     UINT64 * pSignalValues,
                                                     ID3D12Fence ** ppFences)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device2_ReturnUnderlyingResource(ring, 0, object_id, pResource11, NumSync, pSignalValues, ppFences, &submit);
}

static inline HRESULT
npt_call_ID3D11On12Device2_ReturnUnderlyingResource(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11Resource * pResource11,
                                                    UINT NumSync,
                                                    UINT64 * pSignalValues,
                                                    ID3D12Fence ** ppFences)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11On12Device2_ReturnUnderlyingResource(ring, NPT_CMD_FLAG_REPLY, object_id, pResource11, NumSync, pSignalValues, ppFences, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11On12Device2_ReturnUnderlyingResource_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11ON12DEVICE_H */
