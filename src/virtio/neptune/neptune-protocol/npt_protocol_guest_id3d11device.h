/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11device.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11DEVICE_H
#define NPT_PROTOCOL_GUEST_ID3D11DEVICE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11Device::CreateBuffer
 */

struct npt_command_ID3D11Device_CreateBuffer {
    void *_self;
    const D3D11_BUFFER_DESC * pDesc;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Buffer ** ppBuffer;
    /* Shadow: the guest-allocated id for ppBuffer arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppBuffer -> *ppBuffer} in the
     * context object table. */
    uint64_t _guest_id_ppBuffer;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateBuffer(const D3D11_BUFFER_DESC * pDesc,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Buffer ** ppBuffer)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_BUFFER_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pInitialData);
    if (pInitialData)
        cmd_size += npt_sizeof_D3D11_SUBRESOURCE_DATA(pInitialData, max_mode);
cmd_size += sizeof(uint64_t);  /* ppBuffer: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateBuffer(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const D3D11_BUFFER_DESC * pDesc,
                                     const D3D11_SUBRESOURCE_DATA * pInitialData,
                                     ID3D11Buffer ** ppBuffer)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateBuffer(pDesc,pInitialData,ppBuffer);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateBuffer),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_BUFFER_DESC(enc, pDesc);
    if (npt_encode_simple_pointer(enc, pInitialData))
        npt_encode_D3D11_SUBRESOURCE_DATA(enc, pInitialData);
{
    uint64_t _gid_ppBuffer = 0;
    if (ppBuffer) {
        _gid_ppBuffer = npt_com_allocate_next_id();
        *ppBuffer = (ID3D11Buffer *)(uintptr_t)_gid_ppBuffer;
    }
    npt_encode_uint64_t(enc, &_gid_ppBuffer);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateBuffer_reply(const D3D11_BUFFER_DESC * pDesc,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Buffer ** ppBuffer)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip pInitialData (input only) */
/* skip ppBuffer (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateBuffer_reply(struct npt_cs_decoder *dec,
                                           ID3D11Buffer ** ppBuffer,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateBuffer))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateBuffer reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateBuffer));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppBuffer (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateBuffer(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const D3D11_BUFFER_DESC * pDesc,
                                     const D3D11_SUBRESOURCE_DATA * pInitialData,
                                     ID3D11Buffer ** ppBuffer,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateBuffer(pDesc,pInitialData,ppBuffer);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateBuffer_reply(pDesc,pInitialData,ppBuffer)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateBuffer(enc, cmd_flags, object_id, pDesc, pInitialData, ppBuffer);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateBuffer(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const D3D11_BUFFER_DESC * pDesc,
                                    const D3D11_SUBRESOURCE_DATA * pInitialData,
                                    ID3D11Buffer ** ppBuffer)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateBuffer(ring, 0, object_id, pDesc, pInitialData, ppBuffer, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateBuffer(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   const D3D11_BUFFER_DESC * pDesc,
                                   const D3D11_SUBRESOURCE_DATA * pInitialData,
                                   ID3D11Buffer ** ppBuffer)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateBuffer(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, pInitialData, ppBuffer, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateBuffer_reply(dec, ppBuffer, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateTexture1D
 */

struct npt_command_ID3D11Device_CreateTexture1D {
    void *_self;
    const D3D11_TEXTURE1D_DESC * pDesc;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture1D ** ppTexture1D;
    /* Shadow: the guest-allocated id for ppTexture1D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture1D -> *ppTexture1D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture1D;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateTexture1D(const D3D11_TEXTURE1D_DESC * pDesc,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture1D ** ppTexture1D)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_TEXTURE1D_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_array_count(pInitialData ? pDesc->MipLevels * pDesc->ArraySize : 0);
    for (uint32_t _i = 0; _i < (pInitialData ? pDesc->MipLevels * pDesc->ArraySize : 0); _i++)
        cmd_size += npt_sizeof_D3D11_SUBRESOURCE_DATA(&pInitialData[_i], max_mode);
cmd_size += sizeof(uint64_t);  /* ppTexture1D: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateTexture1D(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D11_TEXTURE1D_DESC * pDesc,
                                        const D3D11_SUBRESOURCE_DATA * pInitialData,
                                        ID3D11Texture1D ** ppTexture1D)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateTexture1D(pDesc,pInitialData,ppTexture1D);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateTexture1D),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_TEXTURE1D_DESC(enc, pDesc);
    if (pInitialData) {
        npt_encode_array_count(enc, pDesc->MipLevels * pDesc->ArraySize);
        for (uint32_t _i = 0; _i < (uint32_t)(pDesc->MipLevels * pDesc->ArraySize); _i++)
            npt_encode_D3D11_SUBRESOURCE_DATA(enc, &pInitialData[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
{
    uint64_t _gid_ppTexture1D = 0;
    if (ppTexture1D) {
        _gid_ppTexture1D = npt_com_allocate_next_id();
        *ppTexture1D = (ID3D11Texture1D *)(uintptr_t)_gid_ppTexture1D;
    }
    npt_encode_uint64_t(enc, &_gid_ppTexture1D);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateTexture1D_reply(const D3D11_TEXTURE1D_DESC * pDesc,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture1D ** ppTexture1D)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip pInitialData (input only) */
/* skip ppTexture1D (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateTexture1D_reply(struct npt_cs_decoder *dec,
                                              ID3D11Texture1D ** ppTexture1D,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateTexture1D))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateTexture1D reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateTexture1D));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppTexture1D (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateTexture1D(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D11_TEXTURE1D_DESC * pDesc,
                                        const D3D11_SUBRESOURCE_DATA * pInitialData,
                                        ID3D11Texture1D ** ppTexture1D,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateTexture1D(pDesc,pInitialData,ppTexture1D);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateTexture1D_reply(pDesc,pInitialData,ppTexture1D)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateTexture1D(enc, cmd_flags, object_id, pDesc, pInitialData, ppTexture1D);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateTexture1D(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const D3D11_TEXTURE1D_DESC * pDesc,
                                       const D3D11_SUBRESOURCE_DATA * pInitialData,
                                       ID3D11Texture1D ** ppTexture1D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateTexture1D(ring, 0, object_id, pDesc, pInitialData, ppTexture1D, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateTexture1D(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      const D3D11_TEXTURE1D_DESC * pDesc,
                                      const D3D11_SUBRESOURCE_DATA * pInitialData,
                                      ID3D11Texture1D ** ppTexture1D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateTexture1D(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, pInitialData, ppTexture1D, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateTexture1D_reply(dec, ppTexture1D, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateTexture2D
 */

struct npt_command_ID3D11Device_CreateTexture2D {
    void *_self;
    const D3D11_TEXTURE2D_DESC * pDesc;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture2D ** ppTexture2D;
    /* Shadow: the guest-allocated id for ppTexture2D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture2D -> *ppTexture2D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture2D;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateTexture2D(const D3D11_TEXTURE2D_DESC * pDesc,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture2D ** ppTexture2D)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_TEXTURE2D_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_array_count(pInitialData ? pDesc->MipLevels * pDesc->ArraySize : 0);
    for (uint32_t _i = 0; _i < (pInitialData ? pDesc->MipLevels * pDesc->ArraySize : 0); _i++)
        cmd_size += npt_sizeof_D3D11_SUBRESOURCE_DATA(&pInitialData[_i], max_mode);
cmd_size += sizeof(uint64_t);  /* ppTexture2D: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateTexture2D(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D11_TEXTURE2D_DESC * pDesc,
                                        const D3D11_SUBRESOURCE_DATA * pInitialData,
                                        ID3D11Texture2D ** ppTexture2D)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateTexture2D(pDesc,pInitialData,ppTexture2D);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateTexture2D),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_TEXTURE2D_DESC(enc, pDesc);
    if (pInitialData) {
        npt_encode_array_count(enc, pDesc->MipLevels * pDesc->ArraySize);
        for (uint32_t _i = 0; _i < (uint32_t)(pDesc->MipLevels * pDesc->ArraySize); _i++)
            npt_encode_D3D11_SUBRESOURCE_DATA(enc, &pInitialData[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
{
    uint64_t _gid_ppTexture2D = 0;
    if (ppTexture2D) {
        _gid_ppTexture2D = npt_com_allocate_next_id();
        *ppTexture2D = (ID3D11Texture2D *)(uintptr_t)_gid_ppTexture2D;
    }
    npt_encode_uint64_t(enc, &_gid_ppTexture2D);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateTexture2D_reply(const D3D11_TEXTURE2D_DESC * pDesc,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture2D ** ppTexture2D)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip pInitialData (input only) */
/* skip ppTexture2D (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateTexture2D_reply(struct npt_cs_decoder *dec,
                                              ID3D11Texture2D ** ppTexture2D,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateTexture2D))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateTexture2D reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateTexture2D));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppTexture2D (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateTexture2D(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D11_TEXTURE2D_DESC * pDesc,
                                        const D3D11_SUBRESOURCE_DATA * pInitialData,
                                        ID3D11Texture2D ** ppTexture2D,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateTexture2D(pDesc,pInitialData,ppTexture2D);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateTexture2D_reply(pDesc,pInitialData,ppTexture2D)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateTexture2D(enc, cmd_flags, object_id, pDesc, pInitialData, ppTexture2D);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateTexture2D(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const D3D11_TEXTURE2D_DESC * pDesc,
                                       const D3D11_SUBRESOURCE_DATA * pInitialData,
                                       ID3D11Texture2D ** ppTexture2D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateTexture2D(ring, 0, object_id, pDesc, pInitialData, ppTexture2D, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateTexture2D(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      const D3D11_TEXTURE2D_DESC * pDesc,
                                      const D3D11_SUBRESOURCE_DATA * pInitialData,
                                      ID3D11Texture2D ** ppTexture2D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateTexture2D(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, pInitialData, ppTexture2D, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateTexture2D_reply(dec, ppTexture2D, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateTexture3D
 */

struct npt_command_ID3D11Device_CreateTexture3D {
    void *_self;
    const D3D11_TEXTURE3D_DESC * pDesc;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture3D ** ppTexture3D;
    /* Shadow: the guest-allocated id for ppTexture3D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture3D -> *ppTexture3D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture3D;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateTexture3D(const D3D11_TEXTURE3D_DESC * pDesc,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture3D ** ppTexture3D)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_TEXTURE3D_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_array_count(pInitialData ? pDesc->MipLevels : 0);
    for (uint32_t _i = 0; _i < (pInitialData ? pDesc->MipLevels : 0); _i++)
        cmd_size += npt_sizeof_D3D11_SUBRESOURCE_DATA(&pInitialData[_i], max_mode);
cmd_size += sizeof(uint64_t);  /* ppTexture3D: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateTexture3D(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D11_TEXTURE3D_DESC * pDesc,
                                        const D3D11_SUBRESOURCE_DATA * pInitialData,
                                        ID3D11Texture3D ** ppTexture3D)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateTexture3D(pDesc,pInitialData,ppTexture3D);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateTexture3D),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_TEXTURE3D_DESC(enc, pDesc);
    if (pInitialData) {
        npt_encode_array_count(enc, pDesc->MipLevels);
        for (uint32_t _i = 0; _i < (uint32_t)(pDesc->MipLevels); _i++)
            npt_encode_D3D11_SUBRESOURCE_DATA(enc, &pInitialData[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
{
    uint64_t _gid_ppTexture3D = 0;
    if (ppTexture3D) {
        _gid_ppTexture3D = npt_com_allocate_next_id();
        *ppTexture3D = (ID3D11Texture3D *)(uintptr_t)_gid_ppTexture3D;
    }
    npt_encode_uint64_t(enc, &_gid_ppTexture3D);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateTexture3D_reply(const D3D11_TEXTURE3D_DESC * pDesc,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture3D ** ppTexture3D)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip pInitialData (input only) */
/* skip ppTexture3D (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateTexture3D_reply(struct npt_cs_decoder *dec,
                                              ID3D11Texture3D ** ppTexture3D,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateTexture3D))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateTexture3D reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateTexture3D));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppTexture3D (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateTexture3D(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D11_TEXTURE3D_DESC * pDesc,
                                        const D3D11_SUBRESOURCE_DATA * pInitialData,
                                        ID3D11Texture3D ** ppTexture3D,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateTexture3D(pDesc,pInitialData,ppTexture3D);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateTexture3D_reply(pDesc,pInitialData,ppTexture3D)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateTexture3D(enc, cmd_flags, object_id, pDesc, pInitialData, ppTexture3D);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateTexture3D(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const D3D11_TEXTURE3D_DESC * pDesc,
                                       const D3D11_SUBRESOURCE_DATA * pInitialData,
                                       ID3D11Texture3D ** ppTexture3D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateTexture3D(ring, 0, object_id, pDesc, pInitialData, ppTexture3D, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateTexture3D(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      const D3D11_TEXTURE3D_DESC * pDesc,
                                      const D3D11_SUBRESOURCE_DATA * pInitialData,
                                      ID3D11Texture3D ** ppTexture3D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateTexture3D(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, pInitialData, ppTexture3D, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateTexture3D_reply(dec, ppTexture3D, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateShaderResourceView
 */

struct npt_command_ID3D11Device_CreateShaderResourceView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_SHADER_RESOURCE_VIEW_DESC * pDesc;
    ID3D11ShaderResourceView ** ppSRView;
    /* Shadow: the guest-allocated id for ppSRView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSRView -> *ppSRView} in the
     * context object table. */
    uint64_t _guest_id_ppSRView;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateShaderResourceView(const ID3D11Resource * pResource,const D3D11_SHADER_RESOURCE_VIEW_DESC * pDesc,ID3D11ShaderResourceView ** ppSRView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_SHADER_RESOURCE_VIEW_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppSRView: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateShaderResourceView(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource,
                                                 const D3D11_SHADER_RESOURCE_VIEW_DESC * pDesc,
                                                 ID3D11ShaderResourceView ** ppSRView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateShaderResourceView(pResource,pDesc,ppSRView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateShaderResourceView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_SHADER_RESOURCE_VIEW_DESC(enc, pDesc);
{
    uint64_t _gid_ppSRView = 0;
    if (ppSRView) {
        _gid_ppSRView = npt_com_allocate_next_id();
        *ppSRView = (ID3D11ShaderResourceView *)(uintptr_t)_gid_ppSRView;
    }
    npt_encode_uint64_t(enc, &_gid_ppSRView);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateShaderResourceView_reply(const ID3D11Resource * pResource,const D3D11_SHADER_RESOURCE_VIEW_DESC * pDesc,ID3D11ShaderResourceView ** ppSRView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc (input only) */
/* skip ppSRView (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateShaderResourceView_reply(struct npt_cs_decoder *dec,
                                                       ID3D11ShaderResourceView ** ppSRView,
                                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateShaderResourceView))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateShaderResourceView reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateShaderResourceView));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSRView (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateShaderResourceView(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource,
                                                 const D3D11_SHADER_RESOURCE_VIEW_DESC * pDesc,
                                                 ID3D11ShaderResourceView ** ppSRView,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateShaderResourceView(pResource,pDesc,ppSRView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateShaderResourceView_reply(pResource,pDesc,ppSRView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateShaderResourceView(enc, cmd_flags, object_id, pResource, pDesc, ppSRView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateShaderResourceView(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11Resource * pResource,
                                                const D3D11_SHADER_RESOURCE_VIEW_DESC * pDesc,
                                                ID3D11ShaderResourceView ** ppSRView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateShaderResourceView(ring, 0, object_id, pResource, pDesc, ppSRView, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateShaderResourceView(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Resource * pResource,
                                               const D3D11_SHADER_RESOURCE_VIEW_DESC * pDesc,
                                               ID3D11ShaderResourceView ** ppSRView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateShaderResourceView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc, ppSRView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateShaderResourceView_reply(dec, ppSRView, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateUnorderedAccessView
 */

struct npt_command_ID3D11Device_CreateUnorderedAccessView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_UNORDERED_ACCESS_VIEW_DESC * pDesc;
    ID3D11UnorderedAccessView ** ppUAView;
    /* Shadow: the guest-allocated id for ppUAView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppUAView -> *ppUAView} in the
     * context object table. */
    uint64_t _guest_id_ppUAView;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateUnorderedAccessView(const ID3D11Resource * pResource,const D3D11_UNORDERED_ACCESS_VIEW_DESC * pDesc,ID3D11UnorderedAccessView ** ppUAView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_UNORDERED_ACCESS_VIEW_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppUAView: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateUnorderedAccessView(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11Resource * pResource,
                                                  const D3D11_UNORDERED_ACCESS_VIEW_DESC * pDesc,
                                                  ID3D11UnorderedAccessView ** ppUAView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateUnorderedAccessView(pResource,pDesc,ppUAView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateUnorderedAccessView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_UNORDERED_ACCESS_VIEW_DESC(enc, pDesc);
{
    uint64_t _gid_ppUAView = 0;
    if (ppUAView) {
        _gid_ppUAView = npt_com_allocate_next_id();
        *ppUAView = (ID3D11UnorderedAccessView *)(uintptr_t)_gid_ppUAView;
    }
    npt_encode_uint64_t(enc, &_gid_ppUAView);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateUnorderedAccessView_reply(const ID3D11Resource * pResource,const D3D11_UNORDERED_ACCESS_VIEW_DESC * pDesc,ID3D11UnorderedAccessView ** ppUAView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc (input only) */
/* skip ppUAView (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateUnorderedAccessView_reply(struct npt_cs_decoder *dec,
                                                        ID3D11UnorderedAccessView ** ppUAView,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateUnorderedAccessView))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateUnorderedAccessView reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateUnorderedAccessView));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppUAView (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateUnorderedAccessView(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11Resource * pResource,
                                                  const D3D11_UNORDERED_ACCESS_VIEW_DESC * pDesc,
                                                  ID3D11UnorderedAccessView ** ppUAView,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateUnorderedAccessView(pResource,pDesc,ppUAView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateUnorderedAccessView_reply(pResource,pDesc,ppUAView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateUnorderedAccessView(enc, cmd_flags, object_id, pResource, pDesc, ppUAView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateUnorderedAccessView(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource,
                                                 const D3D11_UNORDERED_ACCESS_VIEW_DESC * pDesc,
                                                 ID3D11UnorderedAccessView ** ppUAView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateUnorderedAccessView(ring, 0, object_id, pResource, pDesc, ppUAView, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateUnorderedAccessView(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11Resource * pResource,
                                                const D3D11_UNORDERED_ACCESS_VIEW_DESC * pDesc,
                                                ID3D11UnorderedAccessView ** ppUAView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateUnorderedAccessView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc, ppUAView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateUnorderedAccessView_reply(dec, ppUAView, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateRenderTargetView
 */

struct npt_command_ID3D11Device_CreateRenderTargetView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_RENDER_TARGET_VIEW_DESC * pDesc;
    ID3D11RenderTargetView ** ppRTView;
    /* Shadow: the guest-allocated id for ppRTView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRTView -> *ppRTView} in the
     * context object table. */
    uint64_t _guest_id_ppRTView;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateRenderTargetView(const ID3D11Resource * pResource,const D3D11_RENDER_TARGET_VIEW_DESC * pDesc,ID3D11RenderTargetView ** ppRTView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_RENDER_TARGET_VIEW_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppRTView: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateRenderTargetView(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11Resource * pResource,
                                               const D3D11_RENDER_TARGET_VIEW_DESC * pDesc,
                                               ID3D11RenderTargetView ** ppRTView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateRenderTargetView(pResource,pDesc,ppRTView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateRenderTargetView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_RENDER_TARGET_VIEW_DESC(enc, pDesc);
{
    uint64_t _gid_ppRTView = 0;
    if (ppRTView) {
        _gid_ppRTView = npt_com_allocate_next_id();
        *ppRTView = (ID3D11RenderTargetView *)(uintptr_t)_gid_ppRTView;
    }
    npt_encode_uint64_t(enc, &_gid_ppRTView);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateRenderTargetView_reply(const ID3D11Resource * pResource,const D3D11_RENDER_TARGET_VIEW_DESC * pDesc,ID3D11RenderTargetView ** ppRTView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc (input only) */
/* skip ppRTView (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateRenderTargetView_reply(struct npt_cs_decoder *dec,
                                                     ID3D11RenderTargetView ** ppRTView,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateRenderTargetView))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateRenderTargetView reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateRenderTargetView));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppRTView (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateRenderTargetView(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11Resource * pResource,
                                               const D3D11_RENDER_TARGET_VIEW_DESC * pDesc,
                                               ID3D11RenderTargetView ** ppRTView,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateRenderTargetView(pResource,pDesc,ppRTView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateRenderTargetView_reply(pResource,pDesc,ppRTView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateRenderTargetView(enc, cmd_flags, object_id, pResource, pDesc, ppRTView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateRenderTargetView(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11Resource * pResource,
                                              const D3D11_RENDER_TARGET_VIEW_DESC * pDesc,
                                              ID3D11RenderTargetView ** ppRTView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateRenderTargetView(ring, 0, object_id, pResource, pDesc, ppRTView, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateRenderTargetView(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11Resource * pResource,
                                             const D3D11_RENDER_TARGET_VIEW_DESC * pDesc,
                                             ID3D11RenderTargetView ** ppRTView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateRenderTargetView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc, ppRTView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateRenderTargetView_reply(dec, ppRTView, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateDepthStencilView
 */

struct npt_command_ID3D11Device_CreateDepthStencilView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_DEPTH_STENCIL_VIEW_DESC * pDesc;
    ID3D11DepthStencilView ** ppDepthStencilView;
    /* Shadow: the guest-allocated id for ppDepthStencilView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilView -> *ppDepthStencilView} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilView;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateDepthStencilView(const ID3D11Resource * pResource,const D3D11_DEPTH_STENCIL_VIEW_DESC * pDesc,ID3D11DepthStencilView ** ppDepthStencilView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_DEPTH_STENCIL_VIEW_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDepthStencilView: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateDepthStencilView(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11Resource * pResource,
                                               const D3D11_DEPTH_STENCIL_VIEW_DESC * pDesc,
                                               ID3D11DepthStencilView ** ppDepthStencilView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateDepthStencilView(pResource,pDesc,ppDepthStencilView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateDepthStencilView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_DEPTH_STENCIL_VIEW_DESC(enc, pDesc);
{
    uint64_t _gid_ppDepthStencilView = 0;
    if (ppDepthStencilView) {
        _gid_ppDepthStencilView = npt_com_allocate_next_id();
        *ppDepthStencilView = (ID3D11DepthStencilView *)(uintptr_t)_gid_ppDepthStencilView;
    }
    npt_encode_uint64_t(enc, &_gid_ppDepthStencilView);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateDepthStencilView_reply(const ID3D11Resource * pResource,const D3D11_DEPTH_STENCIL_VIEW_DESC * pDesc,ID3D11DepthStencilView ** ppDepthStencilView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc (input only) */
/* skip ppDepthStencilView (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateDepthStencilView_reply(struct npt_cs_decoder *dec,
                                                     ID3D11DepthStencilView ** ppDepthStencilView,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateDepthStencilView))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateDepthStencilView reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateDepthStencilView));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDepthStencilView (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateDepthStencilView(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11Resource * pResource,
                                               const D3D11_DEPTH_STENCIL_VIEW_DESC * pDesc,
                                               ID3D11DepthStencilView ** ppDepthStencilView,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateDepthStencilView(pResource,pDesc,ppDepthStencilView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateDepthStencilView_reply(pResource,pDesc,ppDepthStencilView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateDepthStencilView(enc, cmd_flags, object_id, pResource, pDesc, ppDepthStencilView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateDepthStencilView(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11Resource * pResource,
                                              const D3D11_DEPTH_STENCIL_VIEW_DESC * pDesc,
                                              ID3D11DepthStencilView ** ppDepthStencilView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateDepthStencilView(ring, 0, object_id, pResource, pDesc, ppDepthStencilView, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateDepthStencilView(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11Resource * pResource,
                                             const D3D11_DEPTH_STENCIL_VIEW_DESC * pDesc,
                                             ID3D11DepthStencilView ** ppDepthStencilView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateDepthStencilView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc, ppDepthStencilView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateDepthStencilView_reply(dec, ppDepthStencilView, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateInputLayout
 */

struct npt_command_ID3D11Device_CreateInputLayout {
    void *_self;
    const D3D11_INPUT_ELEMENT_DESC * pInputElementDescs;
    UINT NumElements;
    const void * pShaderBytecodeWithInputSignature;
    SIZE_T BytecodeLength;
    ID3D11InputLayout ** ppInputLayout;
    /* Shadow: the guest-allocated id for ppInputLayout arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppInputLayout -> *ppInputLayout} in the
     * context object table. */
    uint64_t _guest_id_ppInputLayout;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateInputLayout(const D3D11_INPUT_ELEMENT_DESC * pInputElementDescs,const UINT NumElements,const void * pShaderBytecodeWithInputSignature,const SIZE_T BytecodeLength,ID3D11InputLayout ** ppInputLayout)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_array_count(pInputElementDescs ? NumElements : 0);
    for (uint32_t _i = 0; _i < (pInputElementDescs ? NumElements : 0); _i++)
        cmd_size += npt_sizeof_D3D11_INPUT_ELEMENT_DESC(&pInputElementDescs[_i], max_mode);
    cmd_size += npt_sizeof_UINT(&NumElements, max_mode);
    if (pShaderBytecodeWithInputSignature) {
        cmd_size += npt_sizeof_array_count(BytecodeLength);
        cmd_size += npt_sizeof_blob_array(pShaderBytecodeWithInputSignature, BytecodeLength);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&BytecodeLength, max_mode);
cmd_size += sizeof(uint64_t);  /* ppInputLayout: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateInputLayout(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const D3D11_INPUT_ELEMENT_DESC * pInputElementDescs,
                                          UINT NumElements,
                                          const void * pShaderBytecodeWithInputSignature,
                                          SIZE_T BytecodeLength,
                                          ID3D11InputLayout ** ppInputLayout)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateInputLayout(pInputElementDescs,NumElements,pShaderBytecodeWithInputSignature,BytecodeLength,ppInputLayout);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateInputLayout),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pInputElementDescs) {
        npt_encode_array_count(enc, NumElements);
        for (uint32_t _i = 0; _i < (uint32_t)(NumElements); _i++)
            npt_encode_D3D11_INPUT_ELEMENT_DESC(enc, &pInputElementDescs[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumElements);
    if (pShaderBytecodeWithInputSignature) {
        npt_encode_array_count(enc, BytecodeLength);
        npt_encode_blob_array(enc, pShaderBytecodeWithInputSignature, BytecodeLength);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &BytecodeLength);
{
    uint64_t _gid_ppInputLayout = 0;
    if (ppInputLayout) {
        _gid_ppInputLayout = npt_com_allocate_next_id();
        *ppInputLayout = (ID3D11InputLayout *)(uintptr_t)_gid_ppInputLayout;
    }
    npt_encode_uint64_t(enc, &_gid_ppInputLayout);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateInputLayout_reply(const D3D11_INPUT_ELEMENT_DESC * pInputElementDescs,const UINT NumElements,const void * pShaderBytecodeWithInputSignature,const SIZE_T BytecodeLength,ID3D11InputLayout ** ppInputLayout)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pInputElementDescs (input only) */
/* skip NumElements (input only) */
/* skip pShaderBytecodeWithInputSignature (input only) */
/* skip BytecodeLength (input only) */
/* skip ppInputLayout (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateInputLayout_reply(struct npt_cs_decoder *dec,
                                                ID3D11InputLayout ** ppInputLayout,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateInputLayout))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateInputLayout reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateInputLayout));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppInputLayout (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateInputLayout(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const D3D11_INPUT_ELEMENT_DESC * pInputElementDescs,
                                          UINT NumElements,
                                          const void * pShaderBytecodeWithInputSignature,
                                          SIZE_T BytecodeLength,
                                          ID3D11InputLayout ** ppInputLayout,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateInputLayout(pInputElementDescs,NumElements,pShaderBytecodeWithInputSignature,BytecodeLength,ppInputLayout);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateInputLayout_reply(pInputElementDescs,NumElements,pShaderBytecodeWithInputSignature,BytecodeLength,ppInputLayout)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateInputLayout(enc, cmd_flags, object_id, pInputElementDescs, NumElements, pShaderBytecodeWithInputSignature, BytecodeLength, ppInputLayout);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateInputLayout(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const D3D11_INPUT_ELEMENT_DESC * pInputElementDescs,
                                         UINT NumElements,
                                         const void * pShaderBytecodeWithInputSignature,
                                         SIZE_T BytecodeLength,
                                         ID3D11InputLayout ** ppInputLayout)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateInputLayout(ring, 0, object_id, pInputElementDescs, NumElements, pShaderBytecodeWithInputSignature, BytecodeLength, ppInputLayout, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateInputLayout(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const D3D11_INPUT_ELEMENT_DESC * pInputElementDescs,
                                        UINT NumElements,
                                        const void * pShaderBytecodeWithInputSignature,
                                        SIZE_T BytecodeLength,
                                        ID3D11InputLayout ** ppInputLayout)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateInputLayout(ring, NPT_CMD_FLAG_REPLY, object_id, pInputElementDescs, NumElements, pShaderBytecodeWithInputSignature, BytecodeLength, ppInputLayout, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateInputLayout_reply(dec, ppInputLayout, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateVertexShader
 */

struct npt_command_ID3D11Device_CreateVertexShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11VertexShader ** ppVertexShader;
    /* Shadow: the guest-allocated id for ppVertexShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVertexShader -> *ppVertexShader} in the
     * context object table. */
    uint64_t _guest_id_ppVertexShader;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateVertexShader(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11VertexShader ** ppVertexShader)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pShaderBytecode) {
        cmd_size += npt_sizeof_array_count(BytecodeLength);
        cmd_size += npt_sizeof_blob_array(pShaderBytecode, BytecodeLength);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&BytecodeLength, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppVertexShader: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateVertexShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const void * pShaderBytecode,
                                           SIZE_T BytecodeLength,
                                           ID3D11ClassLinkage * pClassLinkage,
                                           ID3D11VertexShader ** ppVertexShader)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateVertexShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppVertexShader);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateVertexShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pShaderBytecode) {
        npt_encode_array_count(enc, BytecodeLength);
        npt_encode_blob_array(enc, pShaderBytecode, BytecodeLength);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &BytecodeLength);
    npt_encode_com_handle(enc, npt_object_get_id(pClassLinkage));
{
    uint64_t _gid_ppVertexShader = 0;
    if (ppVertexShader) {
        _gid_ppVertexShader = npt_com_allocate_next_id();
        *ppVertexShader = (ID3D11VertexShader *)(uintptr_t)_gid_ppVertexShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppVertexShader);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateVertexShader_reply(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11VertexShader ** ppVertexShader)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pShaderBytecode (input only) */
/* skip BytecodeLength (input only) */
/* skip pClassLinkage (input only) */
/* skip ppVertexShader (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateVertexShader_reply(struct npt_cs_decoder *dec,
                                                 ID3D11VertexShader ** ppVertexShader,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateVertexShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateVertexShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateVertexShader));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppVertexShader (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateVertexShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const void * pShaderBytecode,
                                           SIZE_T BytecodeLength,
                                           ID3D11ClassLinkage * pClassLinkage,
                                           ID3D11VertexShader ** ppVertexShader,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateVertexShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppVertexShader);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateVertexShader_reply(pShaderBytecode,BytecodeLength,pClassLinkage,ppVertexShader)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateVertexShader(enc, cmd_flags, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppVertexShader);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateVertexShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const void * pShaderBytecode,
                                          SIZE_T BytecodeLength,
                                          ID3D11ClassLinkage * pClassLinkage,
                                          ID3D11VertexShader ** ppVertexShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateVertexShader(ring, 0, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppVertexShader, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateVertexShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const void * pShaderBytecode,
                                         SIZE_T BytecodeLength,
                                         ID3D11ClassLinkage * pClassLinkage,
                                         ID3D11VertexShader ** ppVertexShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateVertexShader(ring, NPT_CMD_FLAG_REPLY, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppVertexShader, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateVertexShader_reply(dec, ppVertexShader, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateGeometryShader
 */

struct npt_command_ID3D11Device_CreateGeometryShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11GeometryShader ** ppGeometryShader;
    /* Shadow: the guest-allocated id for ppGeometryShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppGeometryShader -> *ppGeometryShader} in the
     * context object table. */
    uint64_t _guest_id_ppGeometryShader;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateGeometryShader(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11GeometryShader ** ppGeometryShader)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pShaderBytecode) {
        cmd_size += npt_sizeof_array_count(BytecodeLength);
        cmd_size += npt_sizeof_blob_array(pShaderBytecode, BytecodeLength);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&BytecodeLength, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppGeometryShader: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateGeometryShader(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const void * pShaderBytecode,
                                             SIZE_T BytecodeLength,
                                             ID3D11ClassLinkage * pClassLinkage,
                                             ID3D11GeometryShader ** ppGeometryShader)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateGeometryShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppGeometryShader);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateGeometryShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pShaderBytecode) {
        npt_encode_array_count(enc, BytecodeLength);
        npt_encode_blob_array(enc, pShaderBytecode, BytecodeLength);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &BytecodeLength);
    npt_encode_com_handle(enc, npt_object_get_id(pClassLinkage));
{
    uint64_t _gid_ppGeometryShader = 0;
    if (ppGeometryShader) {
        _gid_ppGeometryShader = npt_com_allocate_next_id();
        *ppGeometryShader = (ID3D11GeometryShader *)(uintptr_t)_gid_ppGeometryShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppGeometryShader);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateGeometryShader_reply(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11GeometryShader ** ppGeometryShader)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pShaderBytecode (input only) */
/* skip BytecodeLength (input only) */
/* skip pClassLinkage (input only) */
/* skip ppGeometryShader (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateGeometryShader_reply(struct npt_cs_decoder *dec,
                                                   ID3D11GeometryShader ** ppGeometryShader,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateGeometryShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateGeometryShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateGeometryShader));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppGeometryShader (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateGeometryShader(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             const void * pShaderBytecode,
                                             SIZE_T BytecodeLength,
                                             ID3D11ClassLinkage * pClassLinkage,
                                             ID3D11GeometryShader ** ppGeometryShader,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateGeometryShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppGeometryShader);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateGeometryShader_reply(pShaderBytecode,BytecodeLength,pClassLinkage,ppGeometryShader)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateGeometryShader(enc, cmd_flags, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppGeometryShader);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateGeometryShader(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const void * pShaderBytecode,
                                            SIZE_T BytecodeLength,
                                            ID3D11ClassLinkage * pClassLinkage,
                                            ID3D11GeometryShader ** ppGeometryShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateGeometryShader(ring, 0, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppGeometryShader, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateGeometryShader(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           const void * pShaderBytecode,
                                           SIZE_T BytecodeLength,
                                           ID3D11ClassLinkage * pClassLinkage,
                                           ID3D11GeometryShader ** ppGeometryShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateGeometryShader(ring, NPT_CMD_FLAG_REPLY, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppGeometryShader, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateGeometryShader_reply(dec, ppGeometryShader, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateGeometryShaderWithStreamOutput
 */

struct npt_command_ID3D11Device_CreateGeometryShaderWithStreamOutput {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    const D3D11_SO_DECLARATION_ENTRY * pSODeclaration;
    UINT NumEntries;
    const UINT * pBufferStrides;
    UINT NumStrides;
    UINT RasterizedStream;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11GeometryShader ** ppGeometryShader;
    /* Shadow: the guest-allocated id for ppGeometryShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppGeometryShader -> *ppGeometryShader} in the
     * context object table. */
    uint64_t _guest_id_ppGeometryShader;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateGeometryShaderWithStreamOutput(const void * pShaderBytecode,const SIZE_T BytecodeLength,const D3D11_SO_DECLARATION_ENTRY * pSODeclaration,const UINT NumEntries,const UINT * pBufferStrides,const UINT NumStrides,const UINT RasterizedStream,const ID3D11ClassLinkage * pClassLinkage,ID3D11GeometryShader ** ppGeometryShader)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pShaderBytecode) {
        cmd_size += npt_sizeof_array_count(BytecodeLength);
        cmd_size += npt_sizeof_blob_array(pShaderBytecode, BytecodeLength);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&BytecodeLength, max_mode);
    cmd_size += npt_sizeof_array_count(pSODeclaration ? NumEntries : 0);
    for (uint32_t _i = 0; _i < (pSODeclaration ? NumEntries : 0); _i++)
        cmd_size += npt_sizeof_D3D11_SO_DECLARATION_ENTRY(&pSODeclaration[_i], max_mode);
    cmd_size += npt_sizeof_UINT(&NumEntries, max_mode);
    cmd_size += npt_sizeof_array_count(pBufferStrides ? NumStrides : 0);
    if (pBufferStrides)
        cmd_size += npt_sizeof_UINT_array(pBufferStrides, NumStrides);
    cmd_size += npt_sizeof_UINT(&NumStrides, max_mode);
    cmd_size += npt_sizeof_UINT(&RasterizedStream, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppGeometryShader: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateGeometryShaderWithStreamOutput(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             const void * pShaderBytecode,
                                                             SIZE_T BytecodeLength,
                                                             const D3D11_SO_DECLARATION_ENTRY * pSODeclaration,
                                                             UINT NumEntries,
                                                             const UINT * pBufferStrides,
                                                             UINT NumStrides,
                                                             UINT RasterizedStream,
                                                             ID3D11ClassLinkage * pClassLinkage,
                                                             ID3D11GeometryShader ** ppGeometryShader)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateGeometryShaderWithStreamOutput(pShaderBytecode,BytecodeLength,pSODeclaration,NumEntries,pBufferStrides,NumStrides,RasterizedStream,pClassLinkage,ppGeometryShader);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateGeometryShaderWithStreamOutput),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pShaderBytecode) {
        npt_encode_array_count(enc, BytecodeLength);
        npt_encode_blob_array(enc, pShaderBytecode, BytecodeLength);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &BytecodeLength);
    if (pSODeclaration) {
        npt_encode_array_count(enc, NumEntries);
        for (uint32_t _i = 0; _i < (uint32_t)(NumEntries); _i++)
            npt_encode_D3D11_SO_DECLARATION_ENTRY(enc, &pSODeclaration[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumEntries);
    if (pBufferStrides) {
        npt_encode_array_count(enc, NumStrides);
        npt_encode_UINT_array(enc, pBufferStrides, NumStrides);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumStrides);
    npt_encode_UINT(enc, &RasterizedStream);
    npt_encode_com_handle(enc, npt_object_get_id(pClassLinkage));
{
    uint64_t _gid_ppGeometryShader = 0;
    if (ppGeometryShader) {
        _gid_ppGeometryShader = npt_com_allocate_next_id();
        *ppGeometryShader = (ID3D11GeometryShader *)(uintptr_t)_gid_ppGeometryShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppGeometryShader);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateGeometryShaderWithStreamOutput_reply(const void * pShaderBytecode,const SIZE_T BytecodeLength,const D3D11_SO_DECLARATION_ENTRY * pSODeclaration,const UINT NumEntries,const UINT * pBufferStrides,const UINT NumStrides,const UINT RasterizedStream,const ID3D11ClassLinkage * pClassLinkage,ID3D11GeometryShader ** ppGeometryShader)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pShaderBytecode (input only) */
/* skip BytecodeLength (input only) */
/* skip pSODeclaration (input only) */
/* skip NumEntries (input only) */
/* skip pBufferStrides (input only) */
/* skip NumStrides (input only) */
/* skip RasterizedStream (input only) */
/* skip pClassLinkage (input only) */
/* skip ppGeometryShader (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateGeometryShaderWithStreamOutput_reply(struct npt_cs_decoder *dec,
                                                                   ID3D11GeometryShader ** ppGeometryShader,
                                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateGeometryShaderWithStreamOutput))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateGeometryShaderWithStreamOutput reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateGeometryShaderWithStreamOutput));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppGeometryShader (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateGeometryShaderWithStreamOutput(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             const void * pShaderBytecode,
                                                             SIZE_T BytecodeLength,
                                                             const D3D11_SO_DECLARATION_ENTRY * pSODeclaration,
                                                             UINT NumEntries,
                                                             const UINT * pBufferStrides,
                                                             UINT NumStrides,
                                                             UINT RasterizedStream,
                                                             ID3D11ClassLinkage * pClassLinkage,
                                                             ID3D11GeometryShader ** ppGeometryShader,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateGeometryShaderWithStreamOutput(pShaderBytecode,BytecodeLength,pSODeclaration,NumEntries,pBufferStrides,NumStrides,RasterizedStream,pClassLinkage,ppGeometryShader);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateGeometryShaderWithStreamOutput_reply(pShaderBytecode,BytecodeLength,pSODeclaration,NumEntries,pBufferStrides,NumStrides,RasterizedStream,pClassLinkage,ppGeometryShader)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateGeometryShaderWithStreamOutput(enc, cmd_flags, object_id, pShaderBytecode, BytecodeLength, pSODeclaration, NumEntries, pBufferStrides, NumStrides, RasterizedStream, pClassLinkage, ppGeometryShader);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateGeometryShaderWithStreamOutput(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            const void * pShaderBytecode,
                                                            SIZE_T BytecodeLength,
                                                            const D3D11_SO_DECLARATION_ENTRY * pSODeclaration,
                                                            UINT NumEntries,
                                                            const UINT * pBufferStrides,
                                                            UINT NumStrides,
                                                            UINT RasterizedStream,
                                                            ID3D11ClassLinkage * pClassLinkage,
                                                            ID3D11GeometryShader ** ppGeometryShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateGeometryShaderWithStreamOutput(ring, 0, object_id, pShaderBytecode, BytecodeLength, pSODeclaration, NumEntries, pBufferStrides, NumStrides, RasterizedStream, pClassLinkage, ppGeometryShader, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateGeometryShaderWithStreamOutput(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           const void * pShaderBytecode,
                                                           SIZE_T BytecodeLength,
                                                           const D3D11_SO_DECLARATION_ENTRY * pSODeclaration,
                                                           UINT NumEntries,
                                                           const UINT * pBufferStrides,
                                                           UINT NumStrides,
                                                           UINT RasterizedStream,
                                                           ID3D11ClassLinkage * pClassLinkage,
                                                           ID3D11GeometryShader ** ppGeometryShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateGeometryShaderWithStreamOutput(ring, NPT_CMD_FLAG_REPLY, object_id, pShaderBytecode, BytecodeLength, pSODeclaration, NumEntries, pBufferStrides, NumStrides, RasterizedStream, pClassLinkage, ppGeometryShader, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateGeometryShaderWithStreamOutput_reply(dec, ppGeometryShader, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreatePixelShader
 */

struct npt_command_ID3D11Device_CreatePixelShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11PixelShader ** ppPixelShader;
    /* Shadow: the guest-allocated id for ppPixelShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPixelShader -> *ppPixelShader} in the
     * context object table. */
    uint64_t _guest_id_ppPixelShader;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreatePixelShader(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11PixelShader ** ppPixelShader)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pShaderBytecode) {
        cmd_size += npt_sizeof_array_count(BytecodeLength);
        cmd_size += npt_sizeof_blob_array(pShaderBytecode, BytecodeLength);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&BytecodeLength, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppPixelShader: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreatePixelShader(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const void * pShaderBytecode,
                                          SIZE_T BytecodeLength,
                                          ID3D11ClassLinkage * pClassLinkage,
                                          ID3D11PixelShader ** ppPixelShader)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreatePixelShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppPixelShader);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreatePixelShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pShaderBytecode) {
        npt_encode_array_count(enc, BytecodeLength);
        npt_encode_blob_array(enc, pShaderBytecode, BytecodeLength);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &BytecodeLength);
    npt_encode_com_handle(enc, npt_object_get_id(pClassLinkage));
{
    uint64_t _gid_ppPixelShader = 0;
    if (ppPixelShader) {
        _gid_ppPixelShader = npt_com_allocate_next_id();
        *ppPixelShader = (ID3D11PixelShader *)(uintptr_t)_gid_ppPixelShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppPixelShader);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreatePixelShader_reply(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11PixelShader ** ppPixelShader)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pShaderBytecode (input only) */
/* skip BytecodeLength (input only) */
/* skip pClassLinkage (input only) */
/* skip ppPixelShader (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreatePixelShader_reply(struct npt_cs_decoder *dec,
                                                ID3D11PixelShader ** ppPixelShader,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreatePixelShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreatePixelShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreatePixelShader));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppPixelShader (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreatePixelShader(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const void * pShaderBytecode,
                                          SIZE_T BytecodeLength,
                                          ID3D11ClassLinkage * pClassLinkage,
                                          ID3D11PixelShader ** ppPixelShader,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreatePixelShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppPixelShader);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreatePixelShader_reply(pShaderBytecode,BytecodeLength,pClassLinkage,ppPixelShader)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreatePixelShader(enc, cmd_flags, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppPixelShader);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreatePixelShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const void * pShaderBytecode,
                                         SIZE_T BytecodeLength,
                                         ID3D11ClassLinkage * pClassLinkage,
                                         ID3D11PixelShader ** ppPixelShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreatePixelShader(ring, 0, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppPixelShader, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreatePixelShader(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const void * pShaderBytecode,
                                        SIZE_T BytecodeLength,
                                        ID3D11ClassLinkage * pClassLinkage,
                                        ID3D11PixelShader ** ppPixelShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreatePixelShader(ring, NPT_CMD_FLAG_REPLY, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppPixelShader, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreatePixelShader_reply(dec, ppPixelShader, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateHullShader
 */

struct npt_command_ID3D11Device_CreateHullShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11HullShader ** ppHullShader;
    /* Shadow: the guest-allocated id for ppHullShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppHullShader -> *ppHullShader} in the
     * context object table. */
    uint64_t _guest_id_ppHullShader;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateHullShader(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11HullShader ** ppHullShader)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pShaderBytecode) {
        cmd_size += npt_sizeof_array_count(BytecodeLength);
        cmd_size += npt_sizeof_blob_array(pShaderBytecode, BytecodeLength);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&BytecodeLength, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppHullShader: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateHullShader(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const void * pShaderBytecode,
                                         SIZE_T BytecodeLength,
                                         ID3D11ClassLinkage * pClassLinkage,
                                         ID3D11HullShader ** ppHullShader)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateHullShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppHullShader);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateHullShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pShaderBytecode) {
        npt_encode_array_count(enc, BytecodeLength);
        npt_encode_blob_array(enc, pShaderBytecode, BytecodeLength);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &BytecodeLength);
    npt_encode_com_handle(enc, npt_object_get_id(pClassLinkage));
{
    uint64_t _gid_ppHullShader = 0;
    if (ppHullShader) {
        _gid_ppHullShader = npt_com_allocate_next_id();
        *ppHullShader = (ID3D11HullShader *)(uintptr_t)_gid_ppHullShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppHullShader);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateHullShader_reply(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11HullShader ** ppHullShader)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pShaderBytecode (input only) */
/* skip BytecodeLength (input only) */
/* skip pClassLinkage (input only) */
/* skip ppHullShader (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateHullShader_reply(struct npt_cs_decoder *dec,
                                               ID3D11HullShader ** ppHullShader,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateHullShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateHullShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateHullShader));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppHullShader (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateHullShader(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const void * pShaderBytecode,
                                         SIZE_T BytecodeLength,
                                         ID3D11ClassLinkage * pClassLinkage,
                                         ID3D11HullShader ** ppHullShader,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateHullShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppHullShader);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateHullShader_reply(pShaderBytecode,BytecodeLength,pClassLinkage,ppHullShader)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateHullShader(enc, cmd_flags, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppHullShader);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateHullShader(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const void * pShaderBytecode,
                                        SIZE_T BytecodeLength,
                                        ID3D11ClassLinkage * pClassLinkage,
                                        ID3D11HullShader ** ppHullShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateHullShader(ring, 0, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppHullShader, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateHullShader(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const void * pShaderBytecode,
                                       SIZE_T BytecodeLength,
                                       ID3D11ClassLinkage * pClassLinkage,
                                       ID3D11HullShader ** ppHullShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateHullShader(ring, NPT_CMD_FLAG_REPLY, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppHullShader, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateHullShader_reply(dec, ppHullShader, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateDomainShader
 */

struct npt_command_ID3D11Device_CreateDomainShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11DomainShader ** ppDomainShader;
    /* Shadow: the guest-allocated id for ppDomainShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDomainShader -> *ppDomainShader} in the
     * context object table. */
    uint64_t _guest_id_ppDomainShader;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateDomainShader(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11DomainShader ** ppDomainShader)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pShaderBytecode) {
        cmd_size += npt_sizeof_array_count(BytecodeLength);
        cmd_size += npt_sizeof_blob_array(pShaderBytecode, BytecodeLength);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&BytecodeLength, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppDomainShader: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateDomainShader(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const void * pShaderBytecode,
                                           SIZE_T BytecodeLength,
                                           ID3D11ClassLinkage * pClassLinkage,
                                           ID3D11DomainShader ** ppDomainShader)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateDomainShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppDomainShader);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateDomainShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pShaderBytecode) {
        npt_encode_array_count(enc, BytecodeLength);
        npt_encode_blob_array(enc, pShaderBytecode, BytecodeLength);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &BytecodeLength);
    npt_encode_com_handle(enc, npt_object_get_id(pClassLinkage));
{
    uint64_t _gid_ppDomainShader = 0;
    if (ppDomainShader) {
        _gid_ppDomainShader = npt_com_allocate_next_id();
        *ppDomainShader = (ID3D11DomainShader *)(uintptr_t)_gid_ppDomainShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppDomainShader);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateDomainShader_reply(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11DomainShader ** ppDomainShader)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pShaderBytecode (input only) */
/* skip BytecodeLength (input only) */
/* skip pClassLinkage (input only) */
/* skip ppDomainShader (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateDomainShader_reply(struct npt_cs_decoder *dec,
                                                 ID3D11DomainShader ** ppDomainShader,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateDomainShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateDomainShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateDomainShader));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDomainShader (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateDomainShader(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const void * pShaderBytecode,
                                           SIZE_T BytecodeLength,
                                           ID3D11ClassLinkage * pClassLinkage,
                                           ID3D11DomainShader ** ppDomainShader,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateDomainShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppDomainShader);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateDomainShader_reply(pShaderBytecode,BytecodeLength,pClassLinkage,ppDomainShader)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateDomainShader(enc, cmd_flags, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppDomainShader);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateDomainShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const void * pShaderBytecode,
                                          SIZE_T BytecodeLength,
                                          ID3D11ClassLinkage * pClassLinkage,
                                          ID3D11DomainShader ** ppDomainShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateDomainShader(ring, 0, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppDomainShader, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateDomainShader(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const void * pShaderBytecode,
                                         SIZE_T BytecodeLength,
                                         ID3D11ClassLinkage * pClassLinkage,
                                         ID3D11DomainShader ** ppDomainShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateDomainShader(ring, NPT_CMD_FLAG_REPLY, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppDomainShader, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateDomainShader_reply(dec, ppDomainShader, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateComputeShader
 */

struct npt_command_ID3D11Device_CreateComputeShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11ComputeShader ** ppComputeShader;
    /* Shadow: the guest-allocated id for ppComputeShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppComputeShader -> *ppComputeShader} in the
     * context object table. */
    uint64_t _guest_id_ppComputeShader;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateComputeShader(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11ComputeShader ** ppComputeShader)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pShaderBytecode) {
        cmd_size += npt_sizeof_array_count(BytecodeLength);
        cmd_size += npt_sizeof_blob_array(pShaderBytecode, BytecodeLength);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&BytecodeLength, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppComputeShader: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateComputeShader(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            const void * pShaderBytecode,
                                            SIZE_T BytecodeLength,
                                            ID3D11ClassLinkage * pClassLinkage,
                                            ID3D11ComputeShader ** ppComputeShader)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateComputeShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppComputeShader);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateComputeShader),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pShaderBytecode) {
        npt_encode_array_count(enc, BytecodeLength);
        npt_encode_blob_array(enc, pShaderBytecode, BytecodeLength);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &BytecodeLength);
    npt_encode_com_handle(enc, npt_object_get_id(pClassLinkage));
{
    uint64_t _gid_ppComputeShader = 0;
    if (ppComputeShader) {
        _gid_ppComputeShader = npt_com_allocate_next_id();
        *ppComputeShader = (ID3D11ComputeShader *)(uintptr_t)_gid_ppComputeShader;
    }
    npt_encode_uint64_t(enc, &_gid_ppComputeShader);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateComputeShader_reply(const void * pShaderBytecode,const SIZE_T BytecodeLength,const ID3D11ClassLinkage * pClassLinkage,ID3D11ComputeShader ** ppComputeShader)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pShaderBytecode (input only) */
/* skip BytecodeLength (input only) */
/* skip pClassLinkage (input only) */
/* skip ppComputeShader (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateComputeShader_reply(struct npt_cs_decoder *dec,
                                                  ID3D11ComputeShader ** ppComputeShader,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateComputeShader))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateComputeShader reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateComputeShader));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppComputeShader (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateComputeShader(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            const void * pShaderBytecode,
                                            SIZE_T BytecodeLength,
                                            ID3D11ClassLinkage * pClassLinkage,
                                            ID3D11ComputeShader ** ppComputeShader,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateComputeShader(pShaderBytecode,BytecodeLength,pClassLinkage,ppComputeShader);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateComputeShader_reply(pShaderBytecode,BytecodeLength,pClassLinkage,ppComputeShader)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateComputeShader(enc, cmd_flags, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppComputeShader);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateComputeShader(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           const void * pShaderBytecode,
                                           SIZE_T BytecodeLength,
                                           ID3D11ClassLinkage * pClassLinkage,
                                           ID3D11ComputeShader ** ppComputeShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateComputeShader(ring, 0, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppComputeShader, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateComputeShader(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const void * pShaderBytecode,
                                          SIZE_T BytecodeLength,
                                          ID3D11ClassLinkage * pClassLinkage,
                                          ID3D11ComputeShader ** ppComputeShader)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateComputeShader(ring, NPT_CMD_FLAG_REPLY, object_id, pShaderBytecode, BytecodeLength, pClassLinkage, ppComputeShader, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateComputeShader_reply(dec, ppComputeShader, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateClassLinkage
 */

struct npt_command_ID3D11Device_CreateClassLinkage {
    void *_self;
    ID3D11ClassLinkage ** ppLinkage;
    /* Shadow: the guest-allocated id for ppLinkage arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppLinkage -> *ppLinkage} in the
     * context object table. */
    uint64_t _guest_id_ppLinkage;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateClassLinkage(ID3D11ClassLinkage ** ppLinkage)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppLinkage: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateClassLinkage(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11ClassLinkage ** ppLinkage)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateClassLinkage(ppLinkage);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateClassLinkage),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppLinkage = 0;
    if (ppLinkage) {
        _gid_ppLinkage = npt_com_allocate_next_id();
        *ppLinkage = (ID3D11ClassLinkage *)(uintptr_t)_gid_ppLinkage;
    }
    npt_encode_uint64_t(enc, &_gid_ppLinkage);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateClassLinkage_reply(ID3D11ClassLinkage ** ppLinkage)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppLinkage (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateClassLinkage_reply(struct npt_cs_decoder *dec,
                                                 ID3D11ClassLinkage ** ppLinkage,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateClassLinkage))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateClassLinkage reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateClassLinkage));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppLinkage (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateClassLinkage(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11ClassLinkage ** ppLinkage,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateClassLinkage(ppLinkage);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateClassLinkage_reply(ppLinkage)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateClassLinkage(enc, cmd_flags, object_id, ppLinkage);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateClassLinkage(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11ClassLinkage ** ppLinkage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateClassLinkage(ring, 0, object_id, ppLinkage, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateClassLinkage(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11ClassLinkage ** ppLinkage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateClassLinkage(ring, NPT_CMD_FLAG_REPLY, object_id, ppLinkage, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateClassLinkage_reply(dec, ppLinkage, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateBlendState
 */

struct npt_command_ID3D11Device_CreateBlendState {
    void *_self;
    const D3D11_BLEND_DESC * pBlendStateDesc;
    ID3D11BlendState ** ppBlendState;
    /* Shadow: the guest-allocated id for ppBlendState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppBlendState -> *ppBlendState} in the
     * context object table. */
    uint64_t _guest_id_ppBlendState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateBlendState(const D3D11_BLEND_DESC * pBlendStateDesc,ID3D11BlendState ** ppBlendState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pBlendStateDesc);
    if (pBlendStateDesc)
        cmd_size += npt_sizeof_D3D11_BLEND_DESC(pBlendStateDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppBlendState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateBlendState(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const D3D11_BLEND_DESC * pBlendStateDesc,
                                         ID3D11BlendState ** ppBlendState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateBlendState(pBlendStateDesc,ppBlendState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateBlendState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pBlendStateDesc))
        npt_encode_D3D11_BLEND_DESC(enc, pBlendStateDesc);
{
    uint64_t _gid_ppBlendState = 0;
    if (ppBlendState) {
        _gid_ppBlendState = npt_com_allocate_next_id();
        *ppBlendState = (ID3D11BlendState *)(uintptr_t)_gid_ppBlendState;
    }
    npt_encode_uint64_t(enc, &_gid_ppBlendState);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateBlendState_reply(const D3D11_BLEND_DESC * pBlendStateDesc,ID3D11BlendState ** ppBlendState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pBlendStateDesc (input only) */
/* skip ppBlendState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateBlendState_reply(struct npt_cs_decoder *dec,
                                               ID3D11BlendState ** ppBlendState,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateBlendState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateBlendState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateBlendState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppBlendState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateBlendState(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const D3D11_BLEND_DESC * pBlendStateDesc,
                                         ID3D11BlendState ** ppBlendState,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateBlendState(pBlendStateDesc,ppBlendState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateBlendState_reply(pBlendStateDesc,ppBlendState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateBlendState(enc, cmd_flags, object_id, pBlendStateDesc, ppBlendState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateBlendState(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const D3D11_BLEND_DESC * pBlendStateDesc,
                                        ID3D11BlendState ** ppBlendState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateBlendState(ring, 0, object_id, pBlendStateDesc, ppBlendState, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateBlendState(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const D3D11_BLEND_DESC * pBlendStateDesc,
                                       ID3D11BlendState ** ppBlendState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateBlendState(ring, NPT_CMD_FLAG_REPLY, object_id, pBlendStateDesc, ppBlendState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateBlendState_reply(dec, ppBlendState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateDepthStencilState
 */

struct npt_command_ID3D11Device_CreateDepthStencilState {
    void *_self;
    const D3D11_DEPTH_STENCIL_DESC * pDepthStencilDesc;
    ID3D11DepthStencilState ** ppDepthStencilState;
    /* Shadow: the guest-allocated id for ppDepthStencilState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilState -> *ppDepthStencilState} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateDepthStencilState(const D3D11_DEPTH_STENCIL_DESC * pDepthStencilDesc,ID3D11DepthStencilState ** ppDepthStencilState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDepthStencilDesc);
    if (pDepthStencilDesc)
        cmd_size += npt_sizeof_D3D11_DEPTH_STENCIL_DESC(pDepthStencilDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDepthStencilState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateDepthStencilState(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D11_DEPTH_STENCIL_DESC * pDepthStencilDesc,
                                                ID3D11DepthStencilState ** ppDepthStencilState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateDepthStencilState(pDepthStencilDesc,ppDepthStencilState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateDepthStencilState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDepthStencilDesc))
        npt_encode_D3D11_DEPTH_STENCIL_DESC(enc, pDepthStencilDesc);
{
    uint64_t _gid_ppDepthStencilState = 0;
    if (ppDepthStencilState) {
        _gid_ppDepthStencilState = npt_com_allocate_next_id();
        *ppDepthStencilState = (ID3D11DepthStencilState *)(uintptr_t)_gid_ppDepthStencilState;
    }
    npt_encode_uint64_t(enc, &_gid_ppDepthStencilState);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateDepthStencilState_reply(const D3D11_DEPTH_STENCIL_DESC * pDepthStencilDesc,ID3D11DepthStencilState ** ppDepthStencilState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDepthStencilDesc (input only) */
/* skip ppDepthStencilState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateDepthStencilState_reply(struct npt_cs_decoder *dec,
                                                      ID3D11DepthStencilState ** ppDepthStencilState,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateDepthStencilState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateDepthStencilState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateDepthStencilState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDepthStencilState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateDepthStencilState(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D11_DEPTH_STENCIL_DESC * pDepthStencilDesc,
                                                ID3D11DepthStencilState ** ppDepthStencilState,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateDepthStencilState(pDepthStencilDesc,ppDepthStencilState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateDepthStencilState_reply(pDepthStencilDesc,ppDepthStencilState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateDepthStencilState(enc, cmd_flags, object_id, pDepthStencilDesc, ppDepthStencilState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateDepthStencilState(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const D3D11_DEPTH_STENCIL_DESC * pDepthStencilDesc,
                                               ID3D11DepthStencilState ** ppDepthStencilState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateDepthStencilState(ring, 0, object_id, pDepthStencilDesc, ppDepthStencilState, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateDepthStencilState(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const D3D11_DEPTH_STENCIL_DESC * pDepthStencilDesc,
                                              ID3D11DepthStencilState ** ppDepthStencilState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateDepthStencilState(ring, NPT_CMD_FLAG_REPLY, object_id, pDepthStencilDesc, ppDepthStencilState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateDepthStencilState_reply(dec, ppDepthStencilState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateRasterizerState
 */

struct npt_command_ID3D11Device_CreateRasterizerState {
    void *_self;
    const D3D11_RASTERIZER_DESC * pRasterizerDesc;
    ID3D11RasterizerState ** ppRasterizerState;
    /* Shadow: the guest-allocated id for ppRasterizerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRasterizerState -> *ppRasterizerState} in the
     * context object table. */
    uint64_t _guest_id_ppRasterizerState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateRasterizerState(const D3D11_RASTERIZER_DESC * pRasterizerDesc,ID3D11RasterizerState ** ppRasterizerState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pRasterizerDesc);
    if (pRasterizerDesc)
        cmd_size += npt_sizeof_D3D11_RASTERIZER_DESC(pRasterizerDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppRasterizerState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateRasterizerState(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const D3D11_RASTERIZER_DESC * pRasterizerDesc,
                                              ID3D11RasterizerState ** ppRasterizerState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateRasterizerState(pRasterizerDesc,ppRasterizerState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateRasterizerState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pRasterizerDesc))
        npt_encode_D3D11_RASTERIZER_DESC(enc, pRasterizerDesc);
{
    uint64_t _gid_ppRasterizerState = 0;
    if (ppRasterizerState) {
        _gid_ppRasterizerState = npt_com_allocate_next_id();
        *ppRasterizerState = (ID3D11RasterizerState *)(uintptr_t)_gid_ppRasterizerState;
    }
    npt_encode_uint64_t(enc, &_gid_ppRasterizerState);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateRasterizerState_reply(const D3D11_RASTERIZER_DESC * pRasterizerDesc,ID3D11RasterizerState ** ppRasterizerState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRasterizerDesc (input only) */
/* skip ppRasterizerState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateRasterizerState_reply(struct npt_cs_decoder *dec,
                                                    ID3D11RasterizerState ** ppRasterizerState,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateRasterizerState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateRasterizerState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateRasterizerState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppRasterizerState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateRasterizerState(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const D3D11_RASTERIZER_DESC * pRasterizerDesc,
                                              ID3D11RasterizerState ** ppRasterizerState,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateRasterizerState(pRasterizerDesc,ppRasterizerState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateRasterizerState_reply(pRasterizerDesc,ppRasterizerState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateRasterizerState(enc, cmd_flags, object_id, pRasterizerDesc, ppRasterizerState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateRasterizerState(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const D3D11_RASTERIZER_DESC * pRasterizerDesc,
                                             ID3D11RasterizerState ** ppRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateRasterizerState(ring, 0, object_id, pRasterizerDesc, ppRasterizerState, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateRasterizerState(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const D3D11_RASTERIZER_DESC * pRasterizerDesc,
                                            ID3D11RasterizerState ** ppRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateRasterizerState(ring, NPT_CMD_FLAG_REPLY, object_id, pRasterizerDesc, ppRasterizerState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateRasterizerState_reply(dec, ppRasterizerState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateSamplerState
 */

struct npt_command_ID3D11Device_CreateSamplerState {
    void *_self;
    const D3D11_SAMPLER_DESC * pSamplerDesc;
    ID3D11SamplerState ** ppSamplerState;
    /* Shadow: the guest-allocated id for ppSamplerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSamplerState -> *ppSamplerState} in the
     * context object table. */
    uint64_t _guest_id_ppSamplerState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateSamplerState(const D3D11_SAMPLER_DESC * pSamplerDesc,ID3D11SamplerState ** ppSamplerState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pSamplerDesc);
    if (pSamplerDesc)
        cmd_size += npt_sizeof_D3D11_SAMPLER_DESC(pSamplerDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppSamplerState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateSamplerState(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const D3D11_SAMPLER_DESC * pSamplerDesc,
                                           ID3D11SamplerState ** ppSamplerState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateSamplerState(pSamplerDesc,ppSamplerState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateSamplerState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pSamplerDesc))
        npt_encode_D3D11_SAMPLER_DESC(enc, pSamplerDesc);
{
    uint64_t _gid_ppSamplerState = 0;
    if (ppSamplerState) {
        _gid_ppSamplerState = npt_com_allocate_next_id();
        *ppSamplerState = (ID3D11SamplerState *)(uintptr_t)_gid_ppSamplerState;
    }
    npt_encode_uint64_t(enc, &_gid_ppSamplerState);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateSamplerState_reply(const D3D11_SAMPLER_DESC * pSamplerDesc,ID3D11SamplerState ** ppSamplerState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pSamplerDesc (input only) */
/* skip ppSamplerState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateSamplerState_reply(struct npt_cs_decoder *dec,
                                                 ID3D11SamplerState ** ppSamplerState,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateSamplerState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateSamplerState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateSamplerState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSamplerState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateSamplerState(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const D3D11_SAMPLER_DESC * pSamplerDesc,
                                           ID3D11SamplerState ** ppSamplerState,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateSamplerState(pSamplerDesc,ppSamplerState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateSamplerState_reply(pSamplerDesc,ppSamplerState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateSamplerState(enc, cmd_flags, object_id, pSamplerDesc, ppSamplerState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateSamplerState(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const D3D11_SAMPLER_DESC * pSamplerDesc,
                                          ID3D11SamplerState ** ppSamplerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateSamplerState(ring, 0, object_id, pSamplerDesc, ppSamplerState, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateSamplerState(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const D3D11_SAMPLER_DESC * pSamplerDesc,
                                         ID3D11SamplerState ** ppSamplerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateSamplerState(ring, NPT_CMD_FLAG_REPLY, object_id, pSamplerDesc, ppSamplerState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateSamplerState_reply(dec, ppSamplerState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateQuery
 */

struct npt_command_ID3D11Device_CreateQuery {
    void *_self;
    const D3D11_QUERY_DESC * pQueryDesc;
    ID3D11Query ** ppQuery;
    /* Shadow: the guest-allocated id for ppQuery arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppQuery -> *ppQuery} in the
     * context object table. */
    uint64_t _guest_id_ppQuery;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateQuery(const D3D11_QUERY_DESC * pQueryDesc,ID3D11Query ** ppQuery)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pQueryDesc);
    if (pQueryDesc)
        cmd_size += npt_sizeof_D3D11_QUERY_DESC(pQueryDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppQuery: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateQuery(struct npt_cs_encoder *enc,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    const D3D11_QUERY_DESC * pQueryDesc,
                                    ID3D11Query ** ppQuery)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateQuery(pQueryDesc,ppQuery);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateQuery),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pQueryDesc))
        npt_encode_D3D11_QUERY_DESC(enc, pQueryDesc);
{
    uint64_t _gid_ppQuery = 0;
    if (ppQuery) {
        _gid_ppQuery = npt_com_allocate_next_id();
        *ppQuery = (ID3D11Query *)(uintptr_t)_gid_ppQuery;
    }
    npt_encode_uint64_t(enc, &_gid_ppQuery);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateQuery_reply(const D3D11_QUERY_DESC * pQueryDesc,ID3D11Query ** ppQuery)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pQueryDesc (input only) */
/* skip ppQuery (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateQuery_reply(struct npt_cs_decoder *dec,
                                          ID3D11Query ** ppQuery,
                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateQuery))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateQuery reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateQuery));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppQuery (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateQuery(struct npt_ring *ring,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    const D3D11_QUERY_DESC * pQueryDesc,
                                    ID3D11Query ** ppQuery,
                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateQuery(pQueryDesc,ppQuery);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateQuery_reply(pQueryDesc,ppQuery)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateQuery(enc, cmd_flags, object_id, pQueryDesc, ppQuery);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateQuery(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   const D3D11_QUERY_DESC * pQueryDesc,
                                   ID3D11Query ** ppQuery)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateQuery(ring, 0, object_id, pQueryDesc, ppQuery, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateQuery(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  const D3D11_QUERY_DESC * pQueryDesc,
                                  ID3D11Query ** ppQuery)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateQuery(ring, NPT_CMD_FLAG_REPLY, object_id, pQueryDesc, ppQuery, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateQuery_reply(dec, ppQuery, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreatePredicate
 */

struct npt_command_ID3D11Device_CreatePredicate {
    void *_self;
    const D3D11_QUERY_DESC * pPredicateDesc;
    ID3D11Predicate ** ppPredicate;
    /* Shadow: the guest-allocated id for ppPredicate arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPredicate -> *ppPredicate} in the
     * context object table. */
    uint64_t _guest_id_ppPredicate;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreatePredicate(const D3D11_QUERY_DESC * pPredicateDesc,ID3D11Predicate ** ppPredicate)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pPredicateDesc);
    if (pPredicateDesc)
        cmd_size += npt_sizeof_D3D11_QUERY_DESC(pPredicateDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppPredicate: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreatePredicate(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D11_QUERY_DESC * pPredicateDesc,
                                        ID3D11Predicate ** ppPredicate)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreatePredicate(pPredicateDesc,ppPredicate);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreatePredicate),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pPredicateDesc))
        npt_encode_D3D11_QUERY_DESC(enc, pPredicateDesc);
{
    uint64_t _gid_ppPredicate = 0;
    if (ppPredicate) {
        _gid_ppPredicate = npt_com_allocate_next_id();
        *ppPredicate = (ID3D11Predicate *)(uintptr_t)_gid_ppPredicate;
    }
    npt_encode_uint64_t(enc, &_gid_ppPredicate);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreatePredicate_reply(const D3D11_QUERY_DESC * pPredicateDesc,ID3D11Predicate ** ppPredicate)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pPredicateDesc (input only) */
/* skip ppPredicate (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreatePredicate_reply(struct npt_cs_decoder *dec,
                                              ID3D11Predicate ** ppPredicate,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreatePredicate))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreatePredicate reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreatePredicate));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppPredicate (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreatePredicate(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        const D3D11_QUERY_DESC * pPredicateDesc,
                                        ID3D11Predicate ** ppPredicate,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreatePredicate(pPredicateDesc,ppPredicate);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreatePredicate_reply(pPredicateDesc,ppPredicate)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreatePredicate(enc, cmd_flags, object_id, pPredicateDesc, ppPredicate);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreatePredicate(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const D3D11_QUERY_DESC * pPredicateDesc,
                                       ID3D11Predicate ** ppPredicate)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreatePredicate(ring, 0, object_id, pPredicateDesc, ppPredicate, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreatePredicate(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      const D3D11_QUERY_DESC * pPredicateDesc,
                                      ID3D11Predicate ** ppPredicate)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreatePredicate(ring, NPT_CMD_FLAG_REPLY, object_id, pPredicateDesc, ppPredicate, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreatePredicate_reply(dec, ppPredicate, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateCounter
 */

struct npt_command_ID3D11Device_CreateCounter {
    void *_self;
    const D3D11_COUNTER_DESC * pCounterDesc;
    ID3D11Counter ** ppCounter;
    /* Shadow: the guest-allocated id for ppCounter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCounter -> *ppCounter} in the
     * context object table. */
    uint64_t _guest_id_ppCounter;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateCounter(const D3D11_COUNTER_DESC * pCounterDesc,ID3D11Counter ** ppCounter)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pCounterDesc);
    if (pCounterDesc)
        cmd_size += npt_sizeof_D3D11_COUNTER_DESC(pCounterDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppCounter: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateCounter(struct npt_cs_encoder *enc,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const D3D11_COUNTER_DESC * pCounterDesc,
                                      ID3D11Counter ** ppCounter)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateCounter(pCounterDesc,ppCounter);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateCounter),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pCounterDesc))
        npt_encode_D3D11_COUNTER_DESC(enc, pCounterDesc);
{
    uint64_t _gid_ppCounter = 0;
    if (ppCounter) {
        _gid_ppCounter = npt_com_allocate_next_id();
        *ppCounter = (ID3D11Counter *)(uintptr_t)_gid_ppCounter;
    }
    npt_encode_uint64_t(enc, &_gid_ppCounter);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateCounter_reply(const D3D11_COUNTER_DESC * pCounterDesc,ID3D11Counter ** ppCounter)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCounterDesc (input only) */
/* skip ppCounter (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateCounter_reply(struct npt_cs_decoder *dec,
                                            ID3D11Counter ** ppCounter,
                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateCounter))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateCounter reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateCounter));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppCounter (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateCounter(struct npt_ring *ring,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const D3D11_COUNTER_DESC * pCounterDesc,
                                      ID3D11Counter ** ppCounter,
                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateCounter(pCounterDesc,ppCounter);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateCounter_reply(pCounterDesc,ppCounter)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateCounter(enc, cmd_flags, object_id, pCounterDesc, ppCounter);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateCounter(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     const D3D11_COUNTER_DESC * pCounterDesc,
                                     ID3D11Counter ** ppCounter)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateCounter(ring, 0, object_id, pCounterDesc, ppCounter, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateCounter(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const D3D11_COUNTER_DESC * pCounterDesc,
                                    ID3D11Counter ** ppCounter)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateCounter(ring, NPT_CMD_FLAG_REPLY, object_id, pCounterDesc, ppCounter, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateCounter_reply(dec, ppCounter, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CreateDeferredContext
 */

struct npt_command_ID3D11Device_CreateDeferredContext {
    void *_self;
    UINT ContextFlags;
    ID3D11DeviceContext ** ppDeferredContext;
    /* Shadow: the guest-allocated id for ppDeferredContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDeferredContext -> *ppDeferredContext} in the
     * context object table. */
    uint64_t _guest_id_ppDeferredContext;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CreateDeferredContext(const UINT ContextFlags,ID3D11DeviceContext ** ppDeferredContext)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&ContextFlags, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDeferredContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CreateDeferredContext(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT ContextFlags,
                                              ID3D11DeviceContext ** ppDeferredContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CreateDeferredContext(ContextFlags,ppDeferredContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateDeferredContext),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &ContextFlags);
{
    uint64_t _gid_ppDeferredContext = 0;
    if (ppDeferredContext) {
        _gid_ppDeferredContext = npt_com_allocate_next_id();
        *ppDeferredContext = (ID3D11DeviceContext *)(uintptr_t)_gid_ppDeferredContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppDeferredContext);
}
}

static inline size_t
npt_sizeof_ID3D11Device_CreateDeferredContext_reply(const UINT ContextFlags,ID3D11DeviceContext ** ppDeferredContext)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ContextFlags (input only) */
/* skip ppDeferredContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CreateDeferredContext_reply(struct npt_cs_decoder *dec,
                                                    ID3D11DeviceContext ** ppDeferredContext,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CreateDeferredContext))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CreateDeferredContext reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CreateDeferredContext));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDeferredContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_CreateDeferredContext(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT ContextFlags,
                                              ID3D11DeviceContext ** ppDeferredContext,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CreateDeferredContext(ContextFlags,ppDeferredContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CreateDeferredContext_reply(ContextFlags,ppDeferredContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CreateDeferredContext(enc, cmd_flags, object_id, ContextFlags, ppDeferredContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CreateDeferredContext(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT ContextFlags,
                                             ID3D11DeviceContext ** ppDeferredContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateDeferredContext(ring, 0, object_id, ContextFlags, ppDeferredContext, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CreateDeferredContext(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT ContextFlags,
                                            ID3D11DeviceContext ** ppDeferredContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CreateDeferredContext(ring, NPT_CMD_FLAG_REPLY, object_id, ContextFlags, ppDeferredContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CreateDeferredContext_reply(dec, ppDeferredContext, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::OpenSharedResource
 */

struct npt_command_ID3D11Device_OpenSharedResource {
    void *_self;
    HANDLE hResource;
    const IID * ReturnedInterface;
    void ** ppResource;
    /* Shadow: the guest-allocated id for ppResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResource -> *ppResource} in the
     * context object table. */
    uint64_t _guest_id_ppResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_OpenSharedResource(const HANDLE hResource,const IID * ReturnedInterface,void ** ppResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(ReturnedInterface);
    if (ReturnedInterface)
        cmd_size += npt_sizeof_IID(ReturnedInterface, max_mode);
cmd_size += sizeof(uint64_t);  /* ppResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_OpenSharedResource(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           HANDLE hResource,
                                           const IID * ReturnedInterface,
                                           void ** ppResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_OpenSharedResource(hResource,ReturnedInterface,ppResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_OpenSharedResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hResource));
    if (npt_encode_simple_pointer(enc, ReturnedInterface))
        npt_encode_IID(enc, ReturnedInterface);
{
    uint64_t _gid_ppResource = 0;
    if (ppResource) {
        _gid_ppResource = npt_com_allocate_next_id();
        *ppResource = (void *)(uintptr_t)_gid_ppResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppResource);
}
}

static inline size_t
npt_sizeof_ID3D11Device_OpenSharedResource_reply(const HANDLE hResource,const IID * ReturnedInterface,void ** ppResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hResource (input only) */
/* skip ReturnedInterface (input only) */
/* skip ppResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_OpenSharedResource_reply(struct npt_cs_decoder *dec,
                                                 void ** ppResource,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_OpenSharedResource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_OpenSharedResource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_OpenSharedResource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_OpenSharedResource(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           HANDLE hResource,
                                           const IID * ReturnedInterface,
                                           void ** ppResource,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_OpenSharedResource(hResource,ReturnedInterface,ppResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_OpenSharedResource_reply(hResource,ReturnedInterface,ppResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_OpenSharedResource(enc, cmd_flags, object_id, hResource, ReturnedInterface, ppResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_OpenSharedResource(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          HANDLE hResource,
                                          const IID * ReturnedInterface,
                                          void ** ppResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_OpenSharedResource(ring, 0, object_id, hResource, ReturnedInterface, ppResource, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_OpenSharedResource(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         HANDLE hResource,
                                         const IID * ReturnedInterface,
                                         void ** ppResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_OpenSharedResource(ring, NPT_CMD_FLAG_REPLY, object_id, hResource, ReturnedInterface, ppResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_OpenSharedResource_reply(dec, ppResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CheckFormatSupport
 */

struct npt_command_ID3D11Device_CheckFormatSupport {
    void *_self;
    DXGI_FORMAT Format;
    UINT * pFormatSupport;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CheckFormatSupport(const DXGI_FORMAT Format,const UINT * pFormatSupport)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
/* skip pFormatSupport (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CheckFormatSupport(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           DXGI_FORMAT Format,
                                           UINT * pFormatSupport)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CheckFormatSupport(Format,pFormatSupport);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckFormatSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FORMAT(enc, &Format);
/* skip pFormatSupport (output only) */
}

static inline size_t
npt_sizeof_ID3D11Device_CheckFormatSupport_reply(const DXGI_FORMAT Format,const UINT * pFormatSupport)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Format (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CheckFormatSupport_reply(struct npt_cs_decoder *dec,
                                                 UINT * pFormatSupport,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CheckFormatSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CheckFormatSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CheckFormatSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pFormatSupport)
            npt_decode_UINT(dec, (UINT *)pFormatSupport);
    }
}

static inline void
npt_submit_ID3D11Device_CheckFormatSupport(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           DXGI_FORMAT Format,
                                           UINT * pFormatSupport,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CheckFormatSupport(Format,pFormatSupport);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CheckFormatSupport_reply(Format,pFormatSupport)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CheckFormatSupport(enc, cmd_flags, object_id, Format, pFormatSupport);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CheckFormatSupport(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          DXGI_FORMAT Format,
                                          UINT * pFormatSupport)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckFormatSupport(ring, 0, object_id, Format, pFormatSupport, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CheckFormatSupport(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         DXGI_FORMAT Format,
                                         UINT * pFormatSupport)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckFormatSupport(ring, NPT_CMD_FLAG_REPLY, object_id, Format, pFormatSupport, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CheckFormatSupport_reply(dec, pFormatSupport, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CheckMultisampleQualityLevels
 */

struct npt_command_ID3D11Device_CheckMultisampleQualityLevels {
    void *_self;
    DXGI_FORMAT Format;
    UINT SampleCount;
    UINT * pNumQualityLevels;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CheckMultisampleQualityLevels(const DXGI_FORMAT Format,const UINT SampleCount,const UINT * pNumQualityLevels)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
    cmd_size += npt_sizeof_UINT(&SampleCount, max_mode);
/* skip pNumQualityLevels (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CheckMultisampleQualityLevels(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      DXGI_FORMAT Format,
                                                      UINT SampleCount,
                                                      UINT * pNumQualityLevels)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CheckMultisampleQualityLevels(Format,SampleCount,pNumQualityLevels);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckMultisampleQualityLevels),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FORMAT(enc, &Format);
    npt_encode_UINT(enc, &SampleCount);
/* skip pNumQualityLevels (output only) */
}

static inline size_t
npt_sizeof_ID3D11Device_CheckMultisampleQualityLevels_reply(const DXGI_FORMAT Format,const UINT SampleCount,const UINT * pNumQualityLevels)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Format (input only) */
/* skip SampleCount (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CheckMultisampleQualityLevels_reply(struct npt_cs_decoder *dec,
                                                            UINT * pNumQualityLevels,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CheckMultisampleQualityLevels))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CheckMultisampleQualityLevels reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CheckMultisampleQualityLevels));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pNumQualityLevels)
            npt_decode_UINT(dec, (UINT *)pNumQualityLevels);
    }
}

static inline void
npt_submit_ID3D11Device_CheckMultisampleQualityLevels(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      DXGI_FORMAT Format,
                                                      UINT SampleCount,
                                                      UINT * pNumQualityLevels,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CheckMultisampleQualityLevels(Format,SampleCount,pNumQualityLevels);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CheckMultisampleQualityLevels_reply(Format,SampleCount,pNumQualityLevels)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CheckMultisampleQualityLevels(enc, cmd_flags, object_id, Format, SampleCount, pNumQualityLevels);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CheckMultisampleQualityLevels(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     DXGI_FORMAT Format,
                                                     UINT SampleCount,
                                                     UINT * pNumQualityLevels)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckMultisampleQualityLevels(ring, 0, object_id, Format, SampleCount, pNumQualityLevels, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CheckMultisampleQualityLevels(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    DXGI_FORMAT Format,
                                                    UINT SampleCount,
                                                    UINT * pNumQualityLevels)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckMultisampleQualityLevels(ring, NPT_CMD_FLAG_REPLY, object_id, Format, SampleCount, pNumQualityLevels, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CheckMultisampleQualityLevels_reply(dec, pNumQualityLevels, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CheckCounterInfo
 */

struct npt_command_ID3D11Device_CheckCounterInfo {
    void *_self;
    D3D11_COUNTER_INFO * pCounterInfo;
};

static inline size_t
npt_sizeof_ID3D11Device_CheckCounterInfo(const D3D11_COUNTER_INFO * pCounterInfo)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pCounterInfo (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CheckCounterInfo(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         D3D11_COUNTER_INFO * pCounterInfo)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CheckCounterInfo(pCounterInfo);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckCounterInfo),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pCounterInfo (output only) */
}

static inline size_t
npt_sizeof_ID3D11Device_CheckCounterInfo_reply(const D3D11_COUNTER_INFO * pCounterInfo)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_COUNTER_INFO(&(const D3D11_COUNTER_INFO){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CheckCounterInfo_reply(struct npt_cs_decoder *dec,
                                               D3D11_COUNTER_INFO * pCounterInfo)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CheckCounterInfo))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CheckCounterInfo reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CheckCounterInfo));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pCounterInfo)
            npt_decode_D3D11_COUNTER_INFO(dec, (D3D11_COUNTER_INFO *)pCounterInfo);
    }
}

static inline void
npt_submit_ID3D11Device_CheckCounterInfo(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         D3D11_COUNTER_INFO * pCounterInfo,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CheckCounterInfo(pCounterInfo);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CheckCounterInfo_reply(pCounterInfo)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CheckCounterInfo(enc, cmd_flags, object_id, pCounterInfo);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CheckCounterInfo(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        D3D11_COUNTER_INFO * pCounterInfo)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckCounterInfo(ring, 0, object_id, pCounterInfo, &submit);
}

static inline void
npt_call_ID3D11Device_CheckCounterInfo(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       D3D11_COUNTER_INFO * pCounterInfo)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckCounterInfo(ring, NPT_CMD_FLAG_REPLY, object_id, pCounterInfo, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11Device_CheckCounterInfo_reply(dec, pCounterInfo);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Device::CheckCounter
 */

struct npt_command_ID3D11Device_CheckCounter {
    void *_self;
    const D3D11_COUNTER_DESC * pDesc;
    D3D11_COUNTER_TYPE * pType;
    UINT * pActiveCounters;
    CHAR * szName;
    UINT * pNameLength;
    CHAR * szUnits;
    UINT * pUnitsLength;
    CHAR * szDescription;
    UINT * pDescriptionLength;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CheckCounter(const D3D11_COUNTER_DESC * pDesc,const D3D11_COUNTER_TYPE * pType,const UINT * pActiveCounters,const CHAR * szName,const UINT * pNameLength,const CHAR * szUnits,const UINT * pUnitsLength,const CHAR * szDescription,const UINT * pDescriptionLength)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_COUNTER_DESC(pDesc, max_mode);
/* skip pType (output only) */
/* skip pActiveCounters (output only) */
/* skip szName (output only) */
    cmd_size += npt_sizeof_simple_pointer(pNameLength);
    if (pNameLength)
        cmd_size += npt_sizeof_UINT(pNameLength, max_mode);
/* skip szUnits (output only) */
    cmd_size += npt_sizeof_simple_pointer(pUnitsLength);
    if (pUnitsLength)
        cmd_size += npt_sizeof_UINT(pUnitsLength, max_mode);
/* skip szDescription (output only) */
    cmd_size += npt_sizeof_simple_pointer(pDescriptionLength);
    if (pDescriptionLength)
        cmd_size += npt_sizeof_UINT(pDescriptionLength, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CheckCounter(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const D3D11_COUNTER_DESC * pDesc,
                                     D3D11_COUNTER_TYPE * pType,
                                     UINT * pActiveCounters,
                                     CHAR * szName,
                                     UINT * pNameLength,
                                     CHAR * szUnits,
                                     UINT * pUnitsLength,
                                     CHAR * szDescription,
                                     UINT * pDescriptionLength)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CheckCounter(pDesc,pType,pActiveCounters,szName,pNameLength,szUnits,pUnitsLength,szDescription,pDescriptionLength);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckCounter),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_COUNTER_DESC(enc, pDesc);
/* skip pType (output only) */
/* skip pActiveCounters (output only) */
/* skip szName (output only) */
    if (npt_encode_simple_pointer(enc, pNameLength))
        npt_encode_UINT(enc, pNameLength);
/* skip szUnits (output only) */
    if (npt_encode_simple_pointer(enc, pUnitsLength))
        npt_encode_UINT(enc, pUnitsLength);
/* skip szDescription (output only) */
    if (npt_encode_simple_pointer(enc, pDescriptionLength))
        npt_encode_UINT(enc, pDescriptionLength);
}

static inline size_t
npt_sizeof_ID3D11Device_CheckCounter_reply(const D3D11_COUNTER_DESC * pDesc,const D3D11_COUNTER_TYPE * pType,const UINT * pActiveCounters,const CHAR * szName,const UINT * pNameLength,const CHAR * szUnits,const UINT * pUnitsLength,const CHAR * szDescription,const UINT * pDescriptionLength)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_COUNTER_TYPE(&(const D3D11_COUNTER_TYPE){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_array_count(szName ? (pNameLength ? *pNameLength : 0) : 0);
    if (szName)
        cmd_size += npt_sizeof_CHAR_array(szName, (pNameLength ? *pNameLength : 0));
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_array_count(szUnits ? (pUnitsLength ? *pUnitsLength : 0) : 0);
    if (szUnits)
        cmd_size += npt_sizeof_CHAR_array(szUnits, (pUnitsLength ? *pUnitsLength : 0));
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_array_count(szDescription ? (pDescriptionLength ? *pDescriptionLength : 0) : 0);
    if (szDescription)
        cmd_size += npt_sizeof_CHAR_array(szDescription, (pDescriptionLength ? *pDescriptionLength : 0));
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CheckCounter_reply(struct npt_cs_decoder *dec,
                                           D3D11_COUNTER_TYPE * pType,
                                           UINT * pActiveCounters,
                                           CHAR * szName,
                                           UINT * pNameLength,
                                           CHAR * szUnits,
                                           UINT * pUnitsLength,
                                           CHAR * szDescription,
                                           UINT * pDescriptionLength,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CheckCounter))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CheckCounter reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CheckCounter));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pType)
            npt_decode_D3D11_COUNTER_TYPE(dec, (D3D11_COUNTER_TYPE *)pType);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pActiveCounters)
            npt_decode_UINT(dec, (UINT *)pActiveCounters);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && szName)
            npt_decode_CHAR_array(dec, (CHAR *)szName, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pNameLength)
            npt_decode_UINT(dec, (UINT *)pNameLength);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && szUnits)
            npt_decode_CHAR_array(dec, (CHAR *)szUnits, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pUnitsLength)
            npt_decode_UINT(dec, (UINT *)pUnitsLength);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && szDescription)
            npt_decode_CHAR_array(dec, (CHAR *)szDescription, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pDescriptionLength)
            npt_decode_UINT(dec, (UINT *)pDescriptionLength);
    }
}

static inline void
npt_submit_ID3D11Device_CheckCounter(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     const D3D11_COUNTER_DESC * pDesc,
                                     D3D11_COUNTER_TYPE * pType,
                                     UINT * pActiveCounters,
                                     CHAR * szName,
                                     UINT * pNameLength,
                                     CHAR * szUnits,
                                     UINT * pUnitsLength,
                                     CHAR * szDescription,
                                     UINT * pDescriptionLength,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CheckCounter(pDesc,pType,pActiveCounters,szName,pNameLength,szUnits,pUnitsLength,szDescription,pDescriptionLength);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CheckCounter_reply(pDesc,pType,pActiveCounters,szName,pNameLength,szUnits,pUnitsLength,szDescription,pDescriptionLength)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CheckCounter(enc, cmd_flags, object_id, pDesc, pType, pActiveCounters, szName, pNameLength, szUnits, pUnitsLength, szDescription, pDescriptionLength);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CheckCounter(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const D3D11_COUNTER_DESC * pDesc,
                                    D3D11_COUNTER_TYPE * pType,
                                    UINT * pActiveCounters,
                                    CHAR * szName,
                                    UINT * pNameLength,
                                    CHAR * szUnits,
                                    UINT * pUnitsLength,
                                    CHAR * szDescription,
                                    UINT * pDescriptionLength)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckCounter(ring, 0, object_id, pDesc, pType, pActiveCounters, szName, pNameLength, szUnits, pUnitsLength, szDescription, pDescriptionLength, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CheckCounter(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   const D3D11_COUNTER_DESC * pDesc,
                                   D3D11_COUNTER_TYPE * pType,
                                   UINT * pActiveCounters,
                                   CHAR * szName,
                                   UINT * pNameLength,
                                   CHAR * szUnits,
                                   UINT * pUnitsLength,
                                   CHAR * szDescription,
                                   UINT * pDescriptionLength)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckCounter(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, pType, pActiveCounters, szName, pNameLength, szUnits, pUnitsLength, szDescription, pDescriptionLength, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CheckCounter_reply(dec, pType, pActiveCounters, szName, pNameLength, szUnits, pUnitsLength, szDescription, pDescriptionLength, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::CheckFeatureSupport
 */

struct npt_command_ID3D11Device_CheckFeatureSupport {
    void *_self;
    D3D11_FEATURE Feature;
    void * pFeatureSupportData;
    UINT FeatureSupportDataSize;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_CheckFeatureSupport(const D3D11_FEATURE Feature,const void * pFeatureSupportData,const UINT FeatureSupportDataSize)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D11_FEATURE(&Feature, max_mode);
/* skip pFeatureSupportData (output only) */
    cmd_size += npt_sizeof_UINT(&FeatureSupportDataSize, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_CheckFeatureSupport(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            D3D11_FEATURE Feature,
                                            void * pFeatureSupportData,
                                            UINT FeatureSupportDataSize)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_CheckFeatureSupport(Feature,pFeatureSupportData,FeatureSupportDataSize);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckFeatureSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D11_FEATURE(enc, &Feature);
/* skip pFeatureSupportData (output only) */
    npt_encode_UINT(enc, &FeatureSupportDataSize);
}

static inline size_t
npt_sizeof_ID3D11Device_CheckFeatureSupport_reply(const D3D11_FEATURE Feature,const void * pFeatureSupportData,const UINT FeatureSupportDataSize)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Feature (input only) */
    if (pFeatureSupportData) {
        cmd_size += npt_sizeof_array_count(FeatureSupportDataSize);
        cmd_size += npt_sizeof_blob_array(pFeatureSupportData, FeatureSupportDataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip FeatureSupportDataSize (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_CheckFeatureSupport_reply(struct npt_cs_decoder *dec,
                                                  void * pFeatureSupportData,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_CheckFeatureSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_CheckFeatureSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_CheckFeatureSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pFeatureSupportData)
            npt_decode_blob_array(dec, (void *)pFeatureSupportData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11Device_CheckFeatureSupport(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            D3D11_FEATURE Feature,
                                            void * pFeatureSupportData,
                                            UINT FeatureSupportDataSize,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_CheckFeatureSupport(Feature,pFeatureSupportData,FeatureSupportDataSize);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_CheckFeatureSupport_reply(Feature,pFeatureSupportData,FeatureSupportDataSize)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_CheckFeatureSupport(enc, cmd_flags, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_CheckFeatureSupport(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           D3D11_FEATURE Feature,
                                           void * pFeatureSupportData,
                                           UINT FeatureSupportDataSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckFeatureSupport(ring, 0, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_CheckFeatureSupport(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          D3D11_FEATURE Feature,
                                          void * pFeatureSupportData,
                                          UINT FeatureSupportDataSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_CheckFeatureSupport(ring, NPT_CMD_FLAG_REPLY, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_CheckFeatureSupport_reply(dec, pFeatureSupportData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::GetPrivateData
 */

struct npt_command_ID3D11Device_GetPrivateData {
    void *_self;
    const GUID * guid;
    UINT * pDataSize;
    void * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_GetPrivateData(const GUID * guid,const UINT * pDataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(guid);
    if (guid)
        cmd_size += npt_sizeof_GUID(guid, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDataSize);
    if (pDataSize)
        cmd_size += npt_sizeof_UINT(pDataSize, max_mode);
/* skip pData (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_GetPrivateData(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       const GUID * guid,
                                       UINT * pDataSize,
                                       void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_GetPrivateData(guid,pDataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetPrivateData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, guid))
        npt_encode_GUID(enc, guid);
    if (npt_encode_simple_pointer(enc, pDataSize))
        npt_encode_UINT(enc, pDataSize);
/* skip pData (output only) */
}

static inline size_t
npt_sizeof_ID3D11Device_GetPrivateData_reply(const GUID * guid,const UINT * pDataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip guid (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    if (pData) {
        cmd_size += npt_sizeof_array_count(*pDataSize);
        cmd_size += npt_sizeof_blob_array(pData, *pDataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_GetPrivateData_reply(struct npt_cs_decoder *dec,
                                             UINT * pDataSize,
                                             void * pData,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_GetPrivateData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_GetPrivateData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_GetPrivateData));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDataSize)
            npt_decode_UINT(dec, (UINT *)pDataSize);
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pData)
            npt_decode_blob_array(dec, (void *)pData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11Device_GetPrivateData(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       const GUID * guid,
                                       UINT * pDataSize,
                                       void * pData,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_GetPrivateData(guid,pDataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_GetPrivateData_reply(guid,pDataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_GetPrivateData(enc, cmd_flags, object_id, guid, pDataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_GetPrivateData(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      const GUID * guid,
                                      UINT * pDataSize,
                                      void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetPrivateData(ring, 0, object_id, guid, pDataSize, pData, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_GetPrivateData(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     const GUID * guid,
                                     UINT * pDataSize,
                                     void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetPrivateData(ring, NPT_CMD_FLAG_REPLY, object_id, guid, pDataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_GetPrivateData_reply(dec, pDataSize, pData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::SetPrivateData
 */

struct npt_command_ID3D11Device_SetPrivateData {
    void *_self;
    const GUID * guid;
    UINT DataSize;
    const void * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_SetPrivateData(const GUID * guid,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(guid);
    if (guid)
        cmd_size += npt_sizeof_GUID(guid, max_mode);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_SetPrivateData(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       const GUID * guid,
                                       UINT DataSize,
                                       const void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_SetPrivateData(guid,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_SetPrivateData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, guid))
        npt_encode_GUID(enc, guid);
    npt_encode_UINT(enc, &DataSize);
    if (pData) {
        npt_encode_array_count(enc, DataSize);
        npt_encode_blob_array(enc, pData, DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11Device_SetPrivateData_reply(const GUID * guid,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip guid (input only) */
/* skip DataSize (input only) */
/* skip pData (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_SetPrivateData_reply(struct npt_cs_decoder *dec,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_SetPrivateData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_SetPrivateData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_SetPrivateData));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Device_SetPrivateData(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       const GUID * guid,
                                       UINT DataSize,
                                       const void * pData,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_SetPrivateData(guid,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_SetPrivateData_reply(guid,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_SetPrivateData(enc, cmd_flags, object_id, guid, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_SetPrivateData(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      const GUID * guid,
                                      UINT DataSize,
                                      const void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_SetPrivateData(ring, 0, object_id, guid, DataSize, pData, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_SetPrivateData(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     const GUID * guid,
                                     UINT DataSize,
                                     const void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_SetPrivateData(ring, NPT_CMD_FLAG_REPLY, object_id, guid, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_SetPrivateData_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::SetPrivateDataInterface
 */

struct npt_command_ID3D11Device_SetPrivateDataInterface {
    void *_self;
    const GUID * guid;
    const IUnknown * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_SetPrivateDataInterface(const GUID * guid,const IUnknown * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(guid);
    if (guid)
        cmd_size += npt_sizeof_GUID(guid, max_mode);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_SetPrivateDataInterface(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const GUID * guid,
                                                const IUnknown * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_SetPrivateDataInterface(guid,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_SetPrivateDataInterface),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, guid))
        npt_encode_GUID(enc, guid);
    npt_encode_com_handle(enc, npt_object_get_id(pData));
}

static inline size_t
npt_sizeof_ID3D11Device_SetPrivateDataInterface_reply(const GUID * guid,const IUnknown * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip guid (input only) */
/* skip pData (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_SetPrivateDataInterface_reply(struct npt_cs_decoder *dec,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_SetPrivateDataInterface))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_SetPrivateDataInterface reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_SetPrivateDataInterface));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Device_SetPrivateDataInterface(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const GUID * guid,
                                                const IUnknown * pData,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_SetPrivateDataInterface(guid,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_SetPrivateDataInterface_reply(guid,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_SetPrivateDataInterface(enc, cmd_flags, object_id, guid, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_SetPrivateDataInterface(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const GUID * guid,
                                               const IUnknown * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_SetPrivateDataInterface(ring, 0, object_id, guid, pData, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_SetPrivateDataInterface(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const GUID * guid,
                                              const IUnknown * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_SetPrivateDataInterface(ring, NPT_CMD_FLAG_REPLY, object_id, guid, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_SetPrivateDataInterface_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::GetFeatureLevel
 */

struct npt_command_ID3D11Device_GetFeatureLevel {
    void *_self;
    D3D_FEATURE_LEVEL ret;
};

static inline size_t
npt_sizeof_ID3D11Device_GetFeatureLevel(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_GetFeatureLevel(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_GetFeatureLevel();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetFeatureLevel),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11Device_GetFeatureLevel_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_GetFeatureLevel_reply(struct npt_cs_decoder *dec,
                                              D3D_FEATURE_LEVEL *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_GetFeatureLevel))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_GetFeatureLevel reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_GetFeatureLevel));
        if (ret) *ret = (D3D_FEATURE_LEVEL)0;
        return;
    }
    if (ret) *ret = (D3D_FEATURE_LEVEL)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Device_GetFeatureLevel(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_GetFeatureLevel();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_GetFeatureLevel_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_GetFeatureLevel(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_GetFeatureLevel(struct npt_ring *ring,
                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetFeatureLevel(ring, 0, object_id, &submit);
}

static inline D3D_FEATURE_LEVEL
npt_call_ID3D11Device_GetFeatureLevel(struct npt_ring *ring,
                                      npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetFeatureLevel(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D_FEATURE_LEVEL _ret;
        npt_decode_ID3D11Device_GetFeatureLevel_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D_FEATURE_LEVEL)0;
    }
}

/*
 * ID3D11Device::GetCreationFlags
 */

struct npt_command_ID3D11Device_GetCreationFlags {
    void *_self;
    UINT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_GetCreationFlags(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_GetCreationFlags(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_GetCreationFlags();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetCreationFlags),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11Device_GetCreationFlags_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_GetCreationFlags_reply(struct npt_cs_decoder *dec,
                                               UINT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_GetCreationFlags))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_GetCreationFlags reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_GetCreationFlags));
        if (ret) *ret = (UINT)0;
        return;
    }
    if (ret) *ret = (UINT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Device_GetCreationFlags(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_GetCreationFlags();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_GetCreationFlags_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_GetCreationFlags(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_GetCreationFlags(struct npt_ring *ring,
                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetCreationFlags(ring, 0, object_id, &submit);
}

static inline UINT
npt_call_ID3D11Device_GetCreationFlags(struct npt_ring *ring,
                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetCreationFlags(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT _ret;
        npt_decode_ID3D11Device_GetCreationFlags_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT)0;
    }
}

/*
 * ID3D11Device::GetDeviceRemovedReason
 */

struct npt_command_ID3D11Device_GetDeviceRemovedReason {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_GetDeviceRemovedReason(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_GetDeviceRemovedReason(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_GetDeviceRemovedReason();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetDeviceRemovedReason),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11Device_GetDeviceRemovedReason_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_GetDeviceRemovedReason_reply(struct npt_cs_decoder *dec,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_GetDeviceRemovedReason))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_GetDeviceRemovedReason reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_GetDeviceRemovedReason));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Device_GetDeviceRemovedReason(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_GetDeviceRemovedReason();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_GetDeviceRemovedReason_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_GetDeviceRemovedReason(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_GetDeviceRemovedReason(struct npt_ring *ring,
                                              npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetDeviceRemovedReason(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_GetDeviceRemovedReason(struct npt_ring *ring,
                                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetDeviceRemovedReason(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_GetDeviceRemovedReason_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::GetImmediateContext
 */

struct npt_command_ID3D11Device_GetImmediateContext {
    void *_self;
    ID3D11DeviceContext ** ppImmediateContext;
    /* Shadow: the guest-allocated id for ppImmediateContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppImmediateContext -> *ppImmediateContext} in the
     * context object table. */
    uint64_t _guest_id_ppImmediateContext;
};

static inline size_t
npt_sizeof_ID3D11Device_GetImmediateContext(ID3D11DeviceContext ** ppImmediateContext)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppImmediateContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_GetImmediateContext(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11DeviceContext ** ppImmediateContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_GetImmediateContext(ppImmediateContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetImmediateContext),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppImmediateContext = 0;
    if (ppImmediateContext) {
        _gid_ppImmediateContext = npt_com_allocate_next_id();
        *ppImmediateContext = (ID3D11DeviceContext *)(uintptr_t)_gid_ppImmediateContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppImmediateContext);
}
}

static inline size_t
npt_sizeof_ID3D11Device_GetImmediateContext_reply(ID3D11DeviceContext ** ppImmediateContext)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_GetImmediateContext_reply(struct npt_cs_decoder *dec,
                                                  ID3D11DeviceContext ** ppImmediateContext)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_GetImmediateContext))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_GetImmediateContext reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_GetImmediateContext));
        return;
    }
/* skip ppImmediateContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device_GetImmediateContext(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11DeviceContext ** ppImmediateContext,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_GetImmediateContext(ppImmediateContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_GetImmediateContext_reply(ppImmediateContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_GetImmediateContext(enc, cmd_flags, object_id, ppImmediateContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_GetImmediateContext(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D11DeviceContext ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetImmediateContext(ring, 0, object_id, ppImmediateContext, &submit);
}

static inline void
npt_call_ID3D11Device_GetImmediateContext(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11DeviceContext ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetImmediateContext(ring, NPT_CMD_FLAG_REPLY, object_id, ppImmediateContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11Device_GetImmediateContext_reply(dec, ppImmediateContext);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Device::SetExceptionMode
 */

struct npt_command_ID3D11Device_SetExceptionMode {
    void *_self;
    UINT RaiseFlags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_SetExceptionMode(const UINT RaiseFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RaiseFlags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_SetExceptionMode(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         UINT RaiseFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_SetExceptionMode(RaiseFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_SetExceptionMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RaiseFlags);
}

static inline size_t
npt_sizeof_ID3D11Device_SetExceptionMode_reply(const UINT RaiseFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RaiseFlags (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_SetExceptionMode_reply(struct npt_cs_decoder *dec,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_SetExceptionMode))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_SetExceptionMode reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_SetExceptionMode));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Device_SetExceptionMode(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         UINT RaiseFlags,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_SetExceptionMode(RaiseFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_SetExceptionMode_reply(RaiseFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_SetExceptionMode(enc, cmd_flags, object_id, RaiseFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_SetExceptionMode(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        UINT RaiseFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_SetExceptionMode(ring, 0, object_id, RaiseFlags, &submit);
}

static inline HRESULT
npt_call_ID3D11Device_SetExceptionMode(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       UINT RaiseFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_SetExceptionMode(ring, NPT_CMD_FLAG_REPLY, object_id, RaiseFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device_SetExceptionMode_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device::GetExceptionMode
 */

struct npt_command_ID3D11Device_GetExceptionMode {
    void *_self;
    UINT ret;
};

static inline size_t
npt_sizeof_ID3D11Device_GetExceptionMode(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device_GetExceptionMode(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device_GetExceptionMode();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetExceptionMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11Device_GetExceptionMode_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device_GetExceptionMode_reply(struct npt_cs_decoder *dec,
                                               UINT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                                  NPT_METHOD_ID3D11Device_GetExceptionMode))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device_GetExceptionMode reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                       NPT_METHOD_ID3D11Device_GetExceptionMode));
        if (ret) *ret = (UINT)0;
        return;
    }
    if (ret) *ret = (UINT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11Device_GetExceptionMode(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device_GetExceptionMode();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device_GetExceptionMode_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device_GetExceptionMode(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device_GetExceptionMode(struct npt_ring *ring,
                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetExceptionMode(ring, 0, object_id, &submit);
}

static inline UINT
npt_call_ID3D11Device_GetExceptionMode(struct npt_ring *ring,
                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device_GetExceptionMode(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT _ret;
        npt_decode_ID3D11Device_GetExceptionMode_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT)0;
    }
}

/*
 * ID3D11Device1::GetImmediateContext1
 */

struct npt_command_ID3D11Device1_GetImmediateContext1 {
    void *_self;
    ID3D11DeviceContext1 ** ppImmediateContext;
    /* Shadow: the guest-allocated id for ppImmediateContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppImmediateContext -> *ppImmediateContext} in the
     * context object table. */
    uint64_t _guest_id_ppImmediateContext;
};

static inline size_t
npt_sizeof_ID3D11Device1_GetImmediateContext1(ID3D11DeviceContext1 ** ppImmediateContext)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppImmediateContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device1_GetImmediateContext1(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11DeviceContext1 ** ppImmediateContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device1_GetImmediateContext1(ppImmediateContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_GetImmediateContext1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppImmediateContext = 0;
    if (ppImmediateContext) {
        _gid_ppImmediateContext = npt_com_allocate_next_id();
        *ppImmediateContext = (ID3D11DeviceContext1 *)(uintptr_t)_gid_ppImmediateContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppImmediateContext);
}
}

static inline size_t
npt_sizeof_ID3D11Device1_GetImmediateContext1_reply(ID3D11DeviceContext1 ** ppImmediateContext)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device1_GetImmediateContext1_reply(struct npt_cs_decoder *dec,
                                                    ID3D11DeviceContext1 ** ppImmediateContext)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                                  NPT_METHOD_ID3D11Device1_GetImmediateContext1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device1_GetImmediateContext1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                       NPT_METHOD_ID3D11Device1_GetImmediateContext1));
        return;
    }
/* skip ppImmediateContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device1_GetImmediateContext1(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11DeviceContext1 ** ppImmediateContext,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device1_GetImmediateContext1(ppImmediateContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device1_GetImmediateContext1_reply(ppImmediateContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device1_GetImmediateContext1(enc, cmd_flags, object_id, ppImmediateContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device1_GetImmediateContext1(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11DeviceContext1 ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_GetImmediateContext1(ring, 0, object_id, ppImmediateContext, &submit);
}

static inline void
npt_call_ID3D11Device1_GetImmediateContext1(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            ID3D11DeviceContext1 ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_GetImmediateContext1(ring, NPT_CMD_FLAG_REPLY, object_id, ppImmediateContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11Device1_GetImmediateContext1_reply(dec, ppImmediateContext);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Device1::CreateDeferredContext1
 */

struct npt_command_ID3D11Device1_CreateDeferredContext1 {
    void *_self;
    UINT ContextFlags;
    ID3D11DeviceContext1 ** ppDeferredContext;
    /* Shadow: the guest-allocated id for ppDeferredContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDeferredContext -> *ppDeferredContext} in the
     * context object table. */
    uint64_t _guest_id_ppDeferredContext;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device1_CreateDeferredContext1(const UINT ContextFlags,ID3D11DeviceContext1 ** ppDeferredContext)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&ContextFlags, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDeferredContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device1_CreateDeferredContext1(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT ContextFlags,
                                                ID3D11DeviceContext1 ** ppDeferredContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device1_CreateDeferredContext1(ContextFlags,ppDeferredContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_CreateDeferredContext1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &ContextFlags);
{
    uint64_t _gid_ppDeferredContext = 0;
    if (ppDeferredContext) {
        _gid_ppDeferredContext = npt_com_allocate_next_id();
        *ppDeferredContext = (ID3D11DeviceContext1 *)(uintptr_t)_gid_ppDeferredContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppDeferredContext);
}
}

static inline size_t
npt_sizeof_ID3D11Device1_CreateDeferredContext1_reply(const UINT ContextFlags,ID3D11DeviceContext1 ** ppDeferredContext)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ContextFlags (input only) */
/* skip ppDeferredContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device1_CreateDeferredContext1_reply(struct npt_cs_decoder *dec,
                                                      ID3D11DeviceContext1 ** ppDeferredContext,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                                  NPT_METHOD_ID3D11Device1_CreateDeferredContext1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device1_CreateDeferredContext1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                       NPT_METHOD_ID3D11Device1_CreateDeferredContext1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDeferredContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device1_CreateDeferredContext1(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT ContextFlags,
                                                ID3D11DeviceContext1 ** ppDeferredContext,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device1_CreateDeferredContext1(ContextFlags,ppDeferredContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device1_CreateDeferredContext1_reply(ContextFlags,ppDeferredContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device1_CreateDeferredContext1(enc, cmd_flags, object_id, ContextFlags, ppDeferredContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device1_CreateDeferredContext1(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               UINT ContextFlags,
                                               ID3D11DeviceContext1 ** ppDeferredContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_CreateDeferredContext1(ring, 0, object_id, ContextFlags, ppDeferredContext, &submit);
}

static inline HRESULT
npt_call_ID3D11Device1_CreateDeferredContext1(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT ContextFlags,
                                              ID3D11DeviceContext1 ** ppDeferredContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_CreateDeferredContext1(ring, NPT_CMD_FLAG_REPLY, object_id, ContextFlags, ppDeferredContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device1_CreateDeferredContext1_reply(dec, ppDeferredContext, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device1::CreateBlendState1
 */

struct npt_command_ID3D11Device1_CreateBlendState1 {
    void *_self;
    const D3D11_BLEND_DESC1 * pBlendStateDesc;
    ID3D11BlendState1 ** ppBlendState;
    /* Shadow: the guest-allocated id for ppBlendState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppBlendState -> *ppBlendState} in the
     * context object table. */
    uint64_t _guest_id_ppBlendState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device1_CreateBlendState1(const D3D11_BLEND_DESC1 * pBlendStateDesc,ID3D11BlendState1 ** ppBlendState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pBlendStateDesc);
    if (pBlendStateDesc)
        cmd_size += npt_sizeof_D3D11_BLEND_DESC1(pBlendStateDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppBlendState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device1_CreateBlendState1(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const D3D11_BLEND_DESC1 * pBlendStateDesc,
                                           ID3D11BlendState1 ** ppBlendState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device1_CreateBlendState1(pBlendStateDesc,ppBlendState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_CreateBlendState1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pBlendStateDesc))
        npt_encode_D3D11_BLEND_DESC1(enc, pBlendStateDesc);
{
    uint64_t _gid_ppBlendState = 0;
    if (ppBlendState) {
        _gid_ppBlendState = npt_com_allocate_next_id();
        *ppBlendState = (ID3D11BlendState1 *)(uintptr_t)_gid_ppBlendState;
    }
    npt_encode_uint64_t(enc, &_gid_ppBlendState);
}
}

static inline size_t
npt_sizeof_ID3D11Device1_CreateBlendState1_reply(const D3D11_BLEND_DESC1 * pBlendStateDesc,ID3D11BlendState1 ** ppBlendState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pBlendStateDesc (input only) */
/* skip ppBlendState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device1_CreateBlendState1_reply(struct npt_cs_decoder *dec,
                                                 ID3D11BlendState1 ** ppBlendState,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                                  NPT_METHOD_ID3D11Device1_CreateBlendState1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device1_CreateBlendState1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                       NPT_METHOD_ID3D11Device1_CreateBlendState1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppBlendState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device1_CreateBlendState1(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           const D3D11_BLEND_DESC1 * pBlendStateDesc,
                                           ID3D11BlendState1 ** ppBlendState,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device1_CreateBlendState1(pBlendStateDesc,ppBlendState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device1_CreateBlendState1_reply(pBlendStateDesc,ppBlendState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device1_CreateBlendState1(enc, cmd_flags, object_id, pBlendStateDesc, ppBlendState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device1_CreateBlendState1(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const D3D11_BLEND_DESC1 * pBlendStateDesc,
                                          ID3D11BlendState1 ** ppBlendState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_CreateBlendState1(ring, 0, object_id, pBlendStateDesc, ppBlendState, &submit);
}

static inline HRESULT
npt_call_ID3D11Device1_CreateBlendState1(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const D3D11_BLEND_DESC1 * pBlendStateDesc,
                                         ID3D11BlendState1 ** ppBlendState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_CreateBlendState1(ring, NPT_CMD_FLAG_REPLY, object_id, pBlendStateDesc, ppBlendState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device1_CreateBlendState1_reply(dec, ppBlendState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device1::CreateRasterizerState1
 */

struct npt_command_ID3D11Device1_CreateRasterizerState1 {
    void *_self;
    const D3D11_RASTERIZER_DESC1 * pRasterizerDesc;
    ID3D11RasterizerState1 ** ppRasterizerState;
    /* Shadow: the guest-allocated id for ppRasterizerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRasterizerState -> *ppRasterizerState} in the
     * context object table. */
    uint64_t _guest_id_ppRasterizerState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device1_CreateRasterizerState1(const D3D11_RASTERIZER_DESC1 * pRasterizerDesc,ID3D11RasterizerState1 ** ppRasterizerState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pRasterizerDesc);
    if (pRasterizerDesc)
        cmd_size += npt_sizeof_D3D11_RASTERIZER_DESC1(pRasterizerDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppRasterizerState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device1_CreateRasterizerState1(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D11_RASTERIZER_DESC1 * pRasterizerDesc,
                                                ID3D11RasterizerState1 ** ppRasterizerState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device1_CreateRasterizerState1(pRasterizerDesc,ppRasterizerState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_CreateRasterizerState1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pRasterizerDesc))
        npt_encode_D3D11_RASTERIZER_DESC1(enc, pRasterizerDesc);
{
    uint64_t _gid_ppRasterizerState = 0;
    if (ppRasterizerState) {
        _gid_ppRasterizerState = npt_com_allocate_next_id();
        *ppRasterizerState = (ID3D11RasterizerState1 *)(uintptr_t)_gid_ppRasterizerState;
    }
    npt_encode_uint64_t(enc, &_gid_ppRasterizerState);
}
}

static inline size_t
npt_sizeof_ID3D11Device1_CreateRasterizerState1_reply(const D3D11_RASTERIZER_DESC1 * pRasterizerDesc,ID3D11RasterizerState1 ** ppRasterizerState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRasterizerDesc (input only) */
/* skip ppRasterizerState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device1_CreateRasterizerState1_reply(struct npt_cs_decoder *dec,
                                                      ID3D11RasterizerState1 ** ppRasterizerState,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                                  NPT_METHOD_ID3D11Device1_CreateRasterizerState1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device1_CreateRasterizerState1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                       NPT_METHOD_ID3D11Device1_CreateRasterizerState1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppRasterizerState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device1_CreateRasterizerState1(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D11_RASTERIZER_DESC1 * pRasterizerDesc,
                                                ID3D11RasterizerState1 ** ppRasterizerState,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device1_CreateRasterizerState1(pRasterizerDesc,ppRasterizerState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device1_CreateRasterizerState1_reply(pRasterizerDesc,ppRasterizerState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device1_CreateRasterizerState1(enc, cmd_flags, object_id, pRasterizerDesc, ppRasterizerState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device1_CreateRasterizerState1(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const D3D11_RASTERIZER_DESC1 * pRasterizerDesc,
                                               ID3D11RasterizerState1 ** ppRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_CreateRasterizerState1(ring, 0, object_id, pRasterizerDesc, ppRasterizerState, &submit);
}

static inline HRESULT
npt_call_ID3D11Device1_CreateRasterizerState1(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const D3D11_RASTERIZER_DESC1 * pRasterizerDesc,
                                              ID3D11RasterizerState1 ** ppRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_CreateRasterizerState1(ring, NPT_CMD_FLAG_REPLY, object_id, pRasterizerDesc, ppRasterizerState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device1_CreateRasterizerState1_reply(dec, ppRasterizerState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device1::CreateDeviceContextState
 */

struct npt_command_ID3D11Device1_CreateDeviceContextState {
    void *_self;
    UINT Flags;
    const D3D_FEATURE_LEVEL * pFeatureLevels;
    UINT FeatureLevels;
    UINT SDKVersion;
    const IID * EmulatedInterface;
    D3D_FEATURE_LEVEL * pChosenFeatureLevel;
    ID3DDeviceContextState ** ppContextState;
    /* Shadow: the guest-allocated id for ppContextState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppContextState -> *ppContextState} in the
     * context object table. */
    uint64_t _guest_id_ppContextState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device1_CreateDeviceContextState(const UINT Flags,const D3D_FEATURE_LEVEL * pFeatureLevels,const UINT FeatureLevels,const UINT SDKVersion,const IID * EmulatedInterface,const D3D_FEATURE_LEVEL * pChosenFeatureLevel,ID3DDeviceContextState ** ppContextState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    cmd_size += npt_sizeof_array_count(pFeatureLevels ? FeatureLevels : 0);
    if (pFeatureLevels)
        cmd_size += npt_sizeof_D3D_FEATURE_LEVEL_array(pFeatureLevels, FeatureLevels);
    cmd_size += npt_sizeof_UINT(&FeatureLevels, max_mode);
    cmd_size += npt_sizeof_UINT(&SDKVersion, max_mode);
    cmd_size += npt_sizeof_simple_pointer(EmulatedInterface);
    if (EmulatedInterface)
        cmd_size += npt_sizeof_IID(EmulatedInterface, max_mode);
/* skip pChosenFeatureLevel (output only) */
cmd_size += sizeof(uint64_t);  /* ppContextState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device1_CreateDeviceContextState(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT Flags,
                                                  const D3D_FEATURE_LEVEL * pFeatureLevels,
                                                  UINT FeatureLevels,
                                                  UINT SDKVersion,
                                                  const IID * EmulatedInterface,
                                                  D3D_FEATURE_LEVEL * pChosenFeatureLevel,
                                                  ID3DDeviceContextState ** ppContextState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device1_CreateDeviceContextState(Flags,pFeatureLevels,FeatureLevels,SDKVersion,EmulatedInterface,pChosenFeatureLevel,ppContextState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_CreateDeviceContextState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Flags);
    if (pFeatureLevels) {
        npt_encode_array_count(enc, FeatureLevels);
        npt_encode_D3D_FEATURE_LEVEL_array(enc, pFeatureLevels, FeatureLevels);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &FeatureLevels);
    npt_encode_UINT(enc, &SDKVersion);
    if (npt_encode_simple_pointer(enc, EmulatedInterface))
        npt_encode_IID(enc, EmulatedInterface);
/* skip pChosenFeatureLevel (output only) */
{
    uint64_t _gid_ppContextState = 0;
    if (ppContextState) {
        _gid_ppContextState = npt_com_allocate_next_id();
        *ppContextState = (ID3DDeviceContextState *)(uintptr_t)_gid_ppContextState;
    }
    npt_encode_uint64_t(enc, &_gid_ppContextState);
}
}

static inline size_t
npt_sizeof_ID3D11Device1_CreateDeviceContextState_reply(const UINT Flags,const D3D_FEATURE_LEVEL * pFeatureLevels,const UINT FeatureLevels,const UINT SDKVersion,const IID * EmulatedInterface,const D3D_FEATURE_LEVEL * pChosenFeatureLevel,ID3DDeviceContextState ** ppContextState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Flags (input only) */
/* skip pFeatureLevels (input only) */
/* skip FeatureLevels (input only) */
/* skip SDKVersion (input only) */
/* skip EmulatedInterface (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D_FEATURE_LEVEL(&(const D3D_FEATURE_LEVEL){0}, 1);
/* skip ppContextState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device1_CreateDeviceContextState_reply(struct npt_cs_decoder *dec,
                                                        D3D_FEATURE_LEVEL * pChosenFeatureLevel,
                                                        ID3DDeviceContextState ** ppContextState,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                                  NPT_METHOD_ID3D11Device1_CreateDeviceContextState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device1_CreateDeviceContextState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                       NPT_METHOD_ID3D11Device1_CreateDeviceContextState));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pChosenFeatureLevel)
            npt_decode_D3D_FEATURE_LEVEL(dec, (D3D_FEATURE_LEVEL *)pChosenFeatureLevel);
    }
/* skip ppContextState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device1_CreateDeviceContextState(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT Flags,
                                                  const D3D_FEATURE_LEVEL * pFeatureLevels,
                                                  UINT FeatureLevels,
                                                  UINT SDKVersion,
                                                  const IID * EmulatedInterface,
                                                  D3D_FEATURE_LEVEL * pChosenFeatureLevel,
                                                  ID3DDeviceContextState ** ppContextState,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device1_CreateDeviceContextState(Flags,pFeatureLevels,FeatureLevels,SDKVersion,EmulatedInterface,pChosenFeatureLevel,ppContextState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device1_CreateDeviceContextState_reply(Flags,pFeatureLevels,FeatureLevels,SDKVersion,EmulatedInterface,pChosenFeatureLevel,ppContextState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device1_CreateDeviceContextState(enc, cmd_flags, object_id, Flags, pFeatureLevels, FeatureLevels, SDKVersion, EmulatedInterface, pChosenFeatureLevel, ppContextState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device1_CreateDeviceContextState(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT Flags,
                                                 const D3D_FEATURE_LEVEL * pFeatureLevels,
                                                 UINT FeatureLevels,
                                                 UINT SDKVersion,
                                                 const IID * EmulatedInterface,
                                                 D3D_FEATURE_LEVEL * pChosenFeatureLevel,
                                                 ID3DDeviceContextState ** ppContextState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_CreateDeviceContextState(ring, 0, object_id, Flags, pFeatureLevels, FeatureLevels, SDKVersion, EmulatedInterface, pChosenFeatureLevel, ppContextState, &submit);
}

static inline HRESULT
npt_call_ID3D11Device1_CreateDeviceContextState(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT Flags,
                                                const D3D_FEATURE_LEVEL * pFeatureLevels,
                                                UINT FeatureLevels,
                                                UINT SDKVersion,
                                                const IID * EmulatedInterface,
                                                D3D_FEATURE_LEVEL * pChosenFeatureLevel,
                                                ID3DDeviceContextState ** ppContextState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_CreateDeviceContextState(ring, NPT_CMD_FLAG_REPLY, object_id, Flags, pFeatureLevels, FeatureLevels, SDKVersion, EmulatedInterface, pChosenFeatureLevel, ppContextState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device1_CreateDeviceContextState_reply(dec, pChosenFeatureLevel, ppContextState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device1::OpenSharedResource1
 */

struct npt_command_ID3D11Device1_OpenSharedResource1 {
    void *_self;
    HANDLE hResource;
    const IID * returnedInterface;
    void ** ppResource;
    /* Shadow: the guest-allocated id for ppResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResource -> *ppResource} in the
     * context object table. */
    uint64_t _guest_id_ppResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device1_OpenSharedResource1(const HANDLE hResource,const IID * returnedInterface,void ** ppResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(returnedInterface);
    if (returnedInterface)
        cmd_size += npt_sizeof_IID(returnedInterface, max_mode);
cmd_size += sizeof(uint64_t);  /* ppResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device1_OpenSharedResource1(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             HANDLE hResource,
                                             const IID * returnedInterface,
                                             void ** ppResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device1_OpenSharedResource1(hResource,returnedInterface,ppResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_OpenSharedResource1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hResource));
    if (npt_encode_simple_pointer(enc, returnedInterface))
        npt_encode_IID(enc, returnedInterface);
{
    uint64_t _gid_ppResource = 0;
    if (ppResource) {
        _gid_ppResource = npt_com_allocate_next_id();
        *ppResource = (void *)(uintptr_t)_gid_ppResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppResource);
}
}

static inline size_t
npt_sizeof_ID3D11Device1_OpenSharedResource1_reply(const HANDLE hResource,const IID * returnedInterface,void ** ppResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hResource (input only) */
/* skip returnedInterface (input only) */
/* skip ppResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device1_OpenSharedResource1_reply(struct npt_cs_decoder *dec,
                                                   void ** ppResource,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                                  NPT_METHOD_ID3D11Device1_OpenSharedResource1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device1_OpenSharedResource1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                       NPT_METHOD_ID3D11Device1_OpenSharedResource1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device1_OpenSharedResource1(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             HANDLE hResource,
                                             const IID * returnedInterface,
                                             void ** ppResource,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device1_OpenSharedResource1(hResource,returnedInterface,ppResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device1_OpenSharedResource1_reply(hResource,returnedInterface,ppResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device1_OpenSharedResource1(enc, cmd_flags, object_id, hResource, returnedInterface, ppResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device1_OpenSharedResource1(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            HANDLE hResource,
                                            const IID * returnedInterface,
                                            void ** ppResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_OpenSharedResource1(ring, 0, object_id, hResource, returnedInterface, ppResource, &submit);
}

static inline HRESULT
npt_call_ID3D11Device1_OpenSharedResource1(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           HANDLE hResource,
                                           const IID * returnedInterface,
                                           void ** ppResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_OpenSharedResource1(ring, NPT_CMD_FLAG_REPLY, object_id, hResource, returnedInterface, ppResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device1_OpenSharedResource1_reply(dec, ppResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device1::OpenSharedResourceByName
 */

struct npt_command_ID3D11Device1_OpenSharedResourceByName {
    void *_self;
    const WCHAR * lpName;
    DWORD dwDesiredAccess;
    const IID * returnedInterface;
    void ** ppResource;
    /* Shadow: the guest-allocated id for ppResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResource -> *ppResource} in the
     * context object table. */
    uint64_t _guest_id_ppResource;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device1_OpenSharedResourceByName(const WCHAR * lpName,const DWORD dwDesiredAccess,const IID * returnedInterface,void ** ppResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (lpName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)lpName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(lpName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_DWORD(&dwDesiredAccess, max_mode);
    cmd_size += npt_sizeof_simple_pointer(returnedInterface);
    if (returnedInterface)
        cmd_size += npt_sizeof_IID(returnedInterface, max_mode);
cmd_size += sizeof(uint64_t);  /* ppResource: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device1_OpenSharedResourceByName(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const WCHAR * lpName,
                                                  DWORD dwDesiredAccess,
                                                  const IID * returnedInterface,
                                                  void ** ppResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device1_OpenSharedResourceByName(lpName,dwDesiredAccess,returnedInterface,ppResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_OpenSharedResourceByName),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (lpName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)lpName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, lpName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_DWORD(enc, &dwDesiredAccess);
    if (npt_encode_simple_pointer(enc, returnedInterface))
        npt_encode_IID(enc, returnedInterface);
{
    uint64_t _gid_ppResource = 0;
    if (ppResource) {
        _gid_ppResource = npt_com_allocate_next_id();
        *ppResource = (void *)(uintptr_t)_gid_ppResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppResource);
}
}

static inline size_t
npt_sizeof_ID3D11Device1_OpenSharedResourceByName_reply(const WCHAR * lpName,const DWORD dwDesiredAccess,const IID * returnedInterface,void ** ppResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip lpName (input only) */
/* skip dwDesiredAccess (input only) */
/* skip returnedInterface (input only) */
/* skip ppResource (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device1_OpenSharedResourceByName_reply(struct npt_cs_decoder *dec,
                                                        void ** ppResource,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                                  NPT_METHOD_ID3D11Device1_OpenSharedResourceByName))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device1_OpenSharedResourceByName reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                       NPT_METHOD_ID3D11Device1_OpenSharedResourceByName));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppResource (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device1_OpenSharedResourceByName(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const WCHAR * lpName,
                                                  DWORD dwDesiredAccess,
                                                  const IID * returnedInterface,
                                                  void ** ppResource,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device1_OpenSharedResourceByName(lpName,dwDesiredAccess,returnedInterface,ppResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device1_OpenSharedResourceByName_reply(lpName,dwDesiredAccess,returnedInterface,ppResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device1_OpenSharedResourceByName(enc, cmd_flags, object_id, lpName, dwDesiredAccess, returnedInterface, ppResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device1_OpenSharedResourceByName(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const WCHAR * lpName,
                                                 DWORD dwDesiredAccess,
                                                 const IID * returnedInterface,
                                                 void ** ppResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_OpenSharedResourceByName(ring, 0, object_id, lpName, dwDesiredAccess, returnedInterface, ppResource, &submit);
}

static inline HRESULT
npt_call_ID3D11Device1_OpenSharedResourceByName(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const WCHAR * lpName,
                                                DWORD dwDesiredAccess,
                                                const IID * returnedInterface,
                                                void ** ppResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device1_OpenSharedResourceByName(ring, NPT_CMD_FLAG_REPLY, object_id, lpName, dwDesiredAccess, returnedInterface, ppResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device1_OpenSharedResourceByName_reply(dec, ppResource, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device2::GetImmediateContext2
 */

struct npt_command_ID3D11Device2_GetImmediateContext2 {
    void *_self;
    ID3D11DeviceContext2 ** ppImmediateContext;
    /* Shadow: the guest-allocated id for ppImmediateContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppImmediateContext -> *ppImmediateContext} in the
     * context object table. */
    uint64_t _guest_id_ppImmediateContext;
};

static inline size_t
npt_sizeof_ID3D11Device2_GetImmediateContext2(ID3D11DeviceContext2 ** ppImmediateContext)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppImmediateContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device2_GetImmediateContext2(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11DeviceContext2 ** ppImmediateContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device2_GetImmediateContext2(ppImmediateContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                 NPT_METHOD_ID3D11Device2_GetImmediateContext2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppImmediateContext = 0;
    if (ppImmediateContext) {
        _gid_ppImmediateContext = npt_com_allocate_next_id();
        *ppImmediateContext = (ID3D11DeviceContext2 *)(uintptr_t)_gid_ppImmediateContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppImmediateContext);
}
}

static inline size_t
npt_sizeof_ID3D11Device2_GetImmediateContext2_reply(ID3D11DeviceContext2 ** ppImmediateContext)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device2_GetImmediateContext2_reply(struct npt_cs_decoder *dec,
                                                    ID3D11DeviceContext2 ** ppImmediateContext)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                                  NPT_METHOD_ID3D11Device2_GetImmediateContext2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device2_GetImmediateContext2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                       NPT_METHOD_ID3D11Device2_GetImmediateContext2));
        return;
    }
/* skip ppImmediateContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device2_GetImmediateContext2(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11DeviceContext2 ** ppImmediateContext,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device2_GetImmediateContext2(ppImmediateContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device2_GetImmediateContext2_reply(ppImmediateContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device2_GetImmediateContext2(enc, cmd_flags, object_id, ppImmediateContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device2_GetImmediateContext2(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11DeviceContext2 ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device2_GetImmediateContext2(ring, 0, object_id, ppImmediateContext, &submit);
}

static inline void
npt_call_ID3D11Device2_GetImmediateContext2(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            ID3D11DeviceContext2 ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device2_GetImmediateContext2(ring, NPT_CMD_FLAG_REPLY, object_id, ppImmediateContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11Device2_GetImmediateContext2_reply(dec, ppImmediateContext);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Device2::CreateDeferredContext2
 */

struct npt_command_ID3D11Device2_CreateDeferredContext2 {
    void *_self;
    UINT ContextFlags;
    ID3D11DeviceContext2 ** ppDeferredContext;
    /* Shadow: the guest-allocated id for ppDeferredContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDeferredContext -> *ppDeferredContext} in the
     * context object table. */
    uint64_t _guest_id_ppDeferredContext;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device2_CreateDeferredContext2(const UINT ContextFlags,ID3D11DeviceContext2 ** ppDeferredContext)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&ContextFlags, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDeferredContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device2_CreateDeferredContext2(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT ContextFlags,
                                                ID3D11DeviceContext2 ** ppDeferredContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device2_CreateDeferredContext2(ContextFlags,ppDeferredContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                 NPT_METHOD_ID3D11Device2_CreateDeferredContext2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &ContextFlags);
{
    uint64_t _gid_ppDeferredContext = 0;
    if (ppDeferredContext) {
        _gid_ppDeferredContext = npt_com_allocate_next_id();
        *ppDeferredContext = (ID3D11DeviceContext2 *)(uintptr_t)_gid_ppDeferredContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppDeferredContext);
}
}

static inline size_t
npt_sizeof_ID3D11Device2_CreateDeferredContext2_reply(const UINT ContextFlags,ID3D11DeviceContext2 ** ppDeferredContext)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ContextFlags (input only) */
/* skip ppDeferredContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device2_CreateDeferredContext2_reply(struct npt_cs_decoder *dec,
                                                      ID3D11DeviceContext2 ** ppDeferredContext,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                                  NPT_METHOD_ID3D11Device2_CreateDeferredContext2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device2_CreateDeferredContext2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                       NPT_METHOD_ID3D11Device2_CreateDeferredContext2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDeferredContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device2_CreateDeferredContext2(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT ContextFlags,
                                                ID3D11DeviceContext2 ** ppDeferredContext,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device2_CreateDeferredContext2(ContextFlags,ppDeferredContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device2_CreateDeferredContext2_reply(ContextFlags,ppDeferredContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device2_CreateDeferredContext2(enc, cmd_flags, object_id, ContextFlags, ppDeferredContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device2_CreateDeferredContext2(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               UINT ContextFlags,
                                               ID3D11DeviceContext2 ** ppDeferredContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device2_CreateDeferredContext2(ring, 0, object_id, ContextFlags, ppDeferredContext, &submit);
}

static inline HRESULT
npt_call_ID3D11Device2_CreateDeferredContext2(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT ContextFlags,
                                              ID3D11DeviceContext2 ** ppDeferredContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device2_CreateDeferredContext2(ring, NPT_CMD_FLAG_REPLY, object_id, ContextFlags, ppDeferredContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device2_CreateDeferredContext2_reply(dec, ppDeferredContext, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device2::GetResourceTiling
 */

struct npt_command_ID3D11Device2_GetResourceTiling {
    void *_self;
    ID3D11Resource * pTiledResource;
    UINT * pNumTilesForEntireResource;
    D3D11_PACKED_MIP_DESC * pPackedMipDesc;
    D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips;
    UINT * pNumSubresourceTilings;
    UINT FirstSubresourceTilingToGet;
    D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips;
};

static inline size_t
npt_sizeof_ID3D11Device2_GetResourceTiling(const ID3D11Resource * pTiledResource,const UINT * pNumTilesForEntireResource,const D3D11_PACKED_MIP_DESC * pPackedMipDesc,const D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips,const UINT * pNumSubresourceTilings,const UINT FirstSubresourceTilingToGet,const D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pNumTilesForEntireResource (output only) */
/* skip pPackedMipDesc (output only) */
/* skip pStandardTileShapeForNonPackedMips (output only) */
    cmd_size += npt_sizeof_simple_pointer(pNumSubresourceTilings);
    if (pNumSubresourceTilings)
        cmd_size += npt_sizeof_UINT(pNumSubresourceTilings, max_mode);
    cmd_size += npt_sizeof_UINT(&FirstSubresourceTilingToGet, max_mode);
/* skip pSubresourceTilingsForNonPackedMips (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device2_GetResourceTiling(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11Resource * pTiledResource,
                                           UINT * pNumTilesForEntireResource,
                                           D3D11_PACKED_MIP_DESC * pPackedMipDesc,
                                           D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                           UINT * pNumSubresourceTilings,
                                           UINT FirstSubresourceTilingToGet,
                                           D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device2_GetResourceTiling(pTiledResource,pNumTilesForEntireResource,pPackedMipDesc,pStandardTileShapeForNonPackedMips,pNumSubresourceTilings,FirstSubresourceTilingToGet,pSubresourceTilingsForNonPackedMips);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                 NPT_METHOD_ID3D11Device2_GetResourceTiling),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pTiledResource));
/* skip pNumTilesForEntireResource (output only) */
/* skip pPackedMipDesc (output only) */
/* skip pStandardTileShapeForNonPackedMips (output only) */
    if (npt_encode_simple_pointer(enc, pNumSubresourceTilings))
        npt_encode_UINT(enc, pNumSubresourceTilings);
    npt_encode_UINT(enc, &FirstSubresourceTilingToGet);
/* skip pSubresourceTilingsForNonPackedMips (output only) */
}

static inline size_t
npt_sizeof_ID3D11Device2_GetResourceTiling_reply(const ID3D11Resource * pTiledResource,const UINT * pNumTilesForEntireResource,const D3D11_PACKED_MIP_DESC * pPackedMipDesc,const D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips,const UINT * pNumSubresourceTilings,const UINT FirstSubresourceTilingToGet,const D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pTiledResource (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_PACKED_MIP_DESC(&(const D3D11_PACKED_MIP_DESC){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_TILE_SHAPE(&(const D3D11_TILE_SHAPE){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
/* skip FirstSubresourceTilingToGet (input only) */
    cmd_size += npt_sizeof_array_count(pSubresourceTilingsForNonPackedMips ? (pNumSubresourceTilings ? *pNumSubresourceTilings : 0) : 0);
    if (pSubresourceTilingsForNonPackedMips)
        cmd_size += (size_t)((pNumSubresourceTilings ? *pNumSubresourceTilings : 0)) * npt_sizeof_D3D11_SUBRESOURCE_TILING(&(const D3D11_SUBRESOURCE_TILING){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device2_GetResourceTiling_reply(struct npt_cs_decoder *dec,
                                                 UINT * pNumTilesForEntireResource,
                                                 D3D11_PACKED_MIP_DESC * pPackedMipDesc,
                                                 D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                                 UINT * pNumSubresourceTilings,
                                                 D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                                  NPT_METHOD_ID3D11Device2_GetResourceTiling))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device2_GetResourceTiling reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                       NPT_METHOD_ID3D11Device2_GetResourceTiling));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pNumTilesForEntireResource)
            npt_decode_UINT(dec, (UINT *)pNumTilesForEntireResource);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pPackedMipDesc)
            npt_decode_D3D11_PACKED_MIP_DESC(dec, (D3D11_PACKED_MIP_DESC *)pPackedMipDesc);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pStandardTileShapeForNonPackedMips)
            npt_decode_D3D11_TILE_SHAPE(dec, (D3D11_TILE_SHAPE *)pStandardTileShapeForNonPackedMips);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pNumSubresourceTilings)
            npt_decode_UINT(dec, (UINT *)pNumSubresourceTilings);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pSubresourceTilingsForNonPackedMips) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_D3D11_SUBRESOURCE_TILING(dec, (D3D11_SUBRESOURCE_TILING *)&pSubresourceTilingsForNonPackedMips[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D11Device2_GetResourceTiling(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D11Resource * pTiledResource,
                                           UINT * pNumTilesForEntireResource,
                                           D3D11_PACKED_MIP_DESC * pPackedMipDesc,
                                           D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                           UINT * pNumSubresourceTilings,
                                           UINT FirstSubresourceTilingToGet,
                                           D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device2_GetResourceTiling(pTiledResource,pNumTilesForEntireResource,pPackedMipDesc,pStandardTileShapeForNonPackedMips,pNumSubresourceTilings,FirstSubresourceTilingToGet,pSubresourceTilingsForNonPackedMips);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device2_GetResourceTiling_reply(pTiledResource,pNumTilesForEntireResource,pPackedMipDesc,pStandardTileShapeForNonPackedMips,pNumSubresourceTilings,FirstSubresourceTilingToGet,pSubresourceTilingsForNonPackedMips)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device2_GetResourceTiling(enc, cmd_flags, object_id, pTiledResource, pNumTilesForEntireResource, pPackedMipDesc, pStandardTileShapeForNonPackedMips, pNumSubresourceTilings, FirstSubresourceTilingToGet, pSubresourceTilingsForNonPackedMips);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device2_GetResourceTiling(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11Resource * pTiledResource,
                                          UINT * pNumTilesForEntireResource,
                                          D3D11_PACKED_MIP_DESC * pPackedMipDesc,
                                          D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                          UINT * pNumSubresourceTilings,
                                          UINT FirstSubresourceTilingToGet,
                                          D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device2_GetResourceTiling(ring, 0, object_id, pTiledResource, pNumTilesForEntireResource, pPackedMipDesc, pStandardTileShapeForNonPackedMips, pNumSubresourceTilings, FirstSubresourceTilingToGet, pSubresourceTilingsForNonPackedMips, &submit);
}

static inline void
npt_call_ID3D11Device2_GetResourceTiling(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D11Resource * pTiledResource,
                                         UINT * pNumTilesForEntireResource,
                                         D3D11_PACKED_MIP_DESC * pPackedMipDesc,
                                         D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips,
                                         UINT * pNumSubresourceTilings,
                                         UINT FirstSubresourceTilingToGet,
                                         D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device2_GetResourceTiling(ring, NPT_CMD_FLAG_REPLY, object_id, pTiledResource, pNumTilesForEntireResource, pPackedMipDesc, pStandardTileShapeForNonPackedMips, pNumSubresourceTilings, FirstSubresourceTilingToGet, pSubresourceTilingsForNonPackedMips, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11Device2_GetResourceTiling_reply(dec, pNumTilesForEntireResource, pPackedMipDesc, pStandardTileShapeForNonPackedMips, pNumSubresourceTilings, pSubresourceTilingsForNonPackedMips);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Device2::CheckMultisampleQualityLevels1
 */

struct npt_command_ID3D11Device2_CheckMultisampleQualityLevels1 {
    void *_self;
    DXGI_FORMAT Format;
    UINT SampleCount;
    UINT Flags;
    UINT * pNumQualityLevels;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device2_CheckMultisampleQualityLevels1(const DXGI_FORMAT Format,const UINT SampleCount,const UINT Flags,const UINT * pNumQualityLevels)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
    cmd_size += npt_sizeof_UINT(&SampleCount, max_mode);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
/* skip pNumQualityLevels (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device2_CheckMultisampleQualityLevels1(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        DXGI_FORMAT Format,
                                                        UINT SampleCount,
                                                        UINT Flags,
                                                        UINT * pNumQualityLevels)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device2_CheckMultisampleQualityLevels1(Format,SampleCount,Flags,pNumQualityLevels);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                 NPT_METHOD_ID3D11Device2_CheckMultisampleQualityLevels1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FORMAT(enc, &Format);
    npt_encode_UINT(enc, &SampleCount);
    npt_encode_UINT(enc, &Flags);
/* skip pNumQualityLevels (output only) */
}

static inline size_t
npt_sizeof_ID3D11Device2_CheckMultisampleQualityLevels1_reply(const DXGI_FORMAT Format,const UINT SampleCount,const UINT Flags,const UINT * pNumQualityLevels)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Format (input only) */
/* skip SampleCount (input only) */
/* skip Flags (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device2_CheckMultisampleQualityLevels1_reply(struct npt_cs_decoder *dec,
                                                              UINT * pNumQualityLevels,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                                  NPT_METHOD_ID3D11Device2_CheckMultisampleQualityLevels1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device2_CheckMultisampleQualityLevels1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                       NPT_METHOD_ID3D11Device2_CheckMultisampleQualityLevels1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pNumQualityLevels)
            npt_decode_UINT(dec, (UINT *)pNumQualityLevels);
    }
}

static inline void
npt_submit_ID3D11Device2_CheckMultisampleQualityLevels1(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        DXGI_FORMAT Format,
                                                        UINT SampleCount,
                                                        UINT Flags,
                                                        UINT * pNumQualityLevels,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device2_CheckMultisampleQualityLevels1(Format,SampleCount,Flags,pNumQualityLevels);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device2_CheckMultisampleQualityLevels1_reply(Format,SampleCount,Flags,pNumQualityLevels)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device2_CheckMultisampleQualityLevels1(enc, cmd_flags, object_id, Format, SampleCount, Flags, pNumQualityLevels);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device2_CheckMultisampleQualityLevels1(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       DXGI_FORMAT Format,
                                                       UINT SampleCount,
                                                       UINT Flags,
                                                       UINT * pNumQualityLevels)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device2_CheckMultisampleQualityLevels1(ring, 0, object_id, Format, SampleCount, Flags, pNumQualityLevels, &submit);
}

static inline HRESULT
npt_call_ID3D11Device2_CheckMultisampleQualityLevels1(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      DXGI_FORMAT Format,
                                                      UINT SampleCount,
                                                      UINT Flags,
                                                      UINT * pNumQualityLevels)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device2_CheckMultisampleQualityLevels1(ring, NPT_CMD_FLAG_REPLY, object_id, Format, SampleCount, Flags, pNumQualityLevels, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device2_CheckMultisampleQualityLevels1_reply(dec, pNumQualityLevels, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device3::CreateTexture2D1
 */

struct npt_command_ID3D11Device3_CreateTexture2D1 {
    void *_self;
    const D3D11_TEXTURE2D_DESC1 * pDesc1;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture2D1 ** ppTexture2D;
    /* Shadow: the guest-allocated id for ppTexture2D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture2D -> *ppTexture2D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture2D;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device3_CreateTexture2D1(const D3D11_TEXTURE2D_DESC1 * pDesc1,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture2D1 ** ppTexture2D)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc1);
    if (pDesc1)
        cmd_size += npt_sizeof_D3D11_TEXTURE2D_DESC1(pDesc1, max_mode);
    cmd_size += npt_sizeof_array_count(pInitialData ? pDesc1->MipLevels * pDesc1->ArraySize : 0);
    for (uint32_t _i = 0; _i < (pInitialData ? pDesc1->MipLevels * pDesc1->ArraySize : 0); _i++)
        cmd_size += npt_sizeof_D3D11_SUBRESOURCE_DATA(&pInitialData[_i], max_mode);
cmd_size += sizeof(uint64_t);  /* ppTexture2D: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_CreateTexture2D1(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const D3D11_TEXTURE2D_DESC1 * pDesc1,
                                          const D3D11_SUBRESOURCE_DATA * pInitialData,
                                          ID3D11Texture2D1 ** ppTexture2D)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_CreateTexture2D1(pDesc1,pInitialData,ppTexture2D);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateTexture2D1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc1))
        npt_encode_D3D11_TEXTURE2D_DESC1(enc, pDesc1);
    if (pInitialData) {
        npt_encode_array_count(enc, pDesc1->MipLevels * pDesc1->ArraySize);
        for (uint32_t _i = 0; _i < (uint32_t)(pDesc1->MipLevels * pDesc1->ArraySize); _i++)
            npt_encode_D3D11_SUBRESOURCE_DATA(enc, &pInitialData[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
{
    uint64_t _gid_ppTexture2D = 0;
    if (ppTexture2D) {
        _gid_ppTexture2D = npt_com_allocate_next_id();
        *ppTexture2D = (ID3D11Texture2D1 *)(uintptr_t)_gid_ppTexture2D;
    }
    npt_encode_uint64_t(enc, &_gid_ppTexture2D);
}
}

static inline size_t
npt_sizeof_ID3D11Device3_CreateTexture2D1_reply(const D3D11_TEXTURE2D_DESC1 * pDesc1,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture2D1 ** ppTexture2D)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc1 (input only) */
/* skip pInitialData (input only) */
/* skip ppTexture2D (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_CreateTexture2D1_reply(struct npt_cs_decoder *dec,
                                                ID3D11Texture2D1 ** ppTexture2D,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_CreateTexture2D1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_CreateTexture2D1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_CreateTexture2D1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppTexture2D (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device3_CreateTexture2D1(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const D3D11_TEXTURE2D_DESC1 * pDesc1,
                                          const D3D11_SUBRESOURCE_DATA * pInitialData,
                                          ID3D11Texture2D1 ** ppTexture2D,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_CreateTexture2D1(pDesc1,pInitialData,ppTexture2D);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_CreateTexture2D1_reply(pDesc1,pInitialData,ppTexture2D)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_CreateTexture2D1(enc, cmd_flags, object_id, pDesc1, pInitialData, ppTexture2D);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_CreateTexture2D1(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const D3D11_TEXTURE2D_DESC1 * pDesc1,
                                         const D3D11_SUBRESOURCE_DATA * pInitialData,
                                         ID3D11Texture2D1 ** ppTexture2D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateTexture2D1(ring, 0, object_id, pDesc1, pInitialData, ppTexture2D, &submit);
}

static inline HRESULT
npt_call_ID3D11Device3_CreateTexture2D1(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const D3D11_TEXTURE2D_DESC1 * pDesc1,
                                        const D3D11_SUBRESOURCE_DATA * pInitialData,
                                        ID3D11Texture2D1 ** ppTexture2D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateTexture2D1(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc1, pInitialData, ppTexture2D, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device3_CreateTexture2D1_reply(dec, ppTexture2D, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device3::CreateTexture3D1
 */

struct npt_command_ID3D11Device3_CreateTexture3D1 {
    void *_self;
    const D3D11_TEXTURE3D_DESC1 * pDesc1;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture3D1 ** ppTexture3D;
    /* Shadow: the guest-allocated id for ppTexture3D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture3D -> *ppTexture3D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture3D;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device3_CreateTexture3D1(const D3D11_TEXTURE3D_DESC1 * pDesc1,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture3D1 ** ppTexture3D)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc1);
    if (pDesc1)
        cmd_size += npt_sizeof_D3D11_TEXTURE3D_DESC1(pDesc1, max_mode);
    cmd_size += npt_sizeof_array_count(pInitialData ? pDesc1->MipLevels : 0);
    for (uint32_t _i = 0; _i < (pInitialData ? pDesc1->MipLevels : 0); _i++)
        cmd_size += npt_sizeof_D3D11_SUBRESOURCE_DATA(&pInitialData[_i], max_mode);
cmd_size += sizeof(uint64_t);  /* ppTexture3D: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_CreateTexture3D1(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const D3D11_TEXTURE3D_DESC1 * pDesc1,
                                          const D3D11_SUBRESOURCE_DATA * pInitialData,
                                          ID3D11Texture3D1 ** ppTexture3D)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_CreateTexture3D1(pDesc1,pInitialData,ppTexture3D);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateTexture3D1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc1))
        npt_encode_D3D11_TEXTURE3D_DESC1(enc, pDesc1);
    if (pInitialData) {
        npt_encode_array_count(enc, pDesc1->MipLevels);
        for (uint32_t _i = 0; _i < (uint32_t)(pDesc1->MipLevels); _i++)
            npt_encode_D3D11_SUBRESOURCE_DATA(enc, &pInitialData[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
{
    uint64_t _gid_ppTexture3D = 0;
    if (ppTexture3D) {
        _gid_ppTexture3D = npt_com_allocate_next_id();
        *ppTexture3D = (ID3D11Texture3D1 *)(uintptr_t)_gid_ppTexture3D;
    }
    npt_encode_uint64_t(enc, &_gid_ppTexture3D);
}
}

static inline size_t
npt_sizeof_ID3D11Device3_CreateTexture3D1_reply(const D3D11_TEXTURE3D_DESC1 * pDesc1,const D3D11_SUBRESOURCE_DATA * pInitialData,ID3D11Texture3D1 ** ppTexture3D)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc1 (input only) */
/* skip pInitialData (input only) */
/* skip ppTexture3D (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_CreateTexture3D1_reply(struct npt_cs_decoder *dec,
                                                ID3D11Texture3D1 ** ppTexture3D,
                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_CreateTexture3D1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_CreateTexture3D1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_CreateTexture3D1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppTexture3D (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device3_CreateTexture3D1(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          const D3D11_TEXTURE3D_DESC1 * pDesc1,
                                          const D3D11_SUBRESOURCE_DATA * pInitialData,
                                          ID3D11Texture3D1 ** ppTexture3D,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_CreateTexture3D1(pDesc1,pInitialData,ppTexture3D);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_CreateTexture3D1_reply(pDesc1,pInitialData,ppTexture3D)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_CreateTexture3D1(enc, cmd_flags, object_id, pDesc1, pInitialData, ppTexture3D);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_CreateTexture3D1(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         const D3D11_TEXTURE3D_DESC1 * pDesc1,
                                         const D3D11_SUBRESOURCE_DATA * pInitialData,
                                         ID3D11Texture3D1 ** ppTexture3D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateTexture3D1(ring, 0, object_id, pDesc1, pInitialData, ppTexture3D, &submit);
}

static inline HRESULT
npt_call_ID3D11Device3_CreateTexture3D1(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const D3D11_TEXTURE3D_DESC1 * pDesc1,
                                        const D3D11_SUBRESOURCE_DATA * pInitialData,
                                        ID3D11Texture3D1 ** ppTexture3D)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateTexture3D1(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc1, pInitialData, ppTexture3D, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device3_CreateTexture3D1_reply(dec, ppTexture3D, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device3::CreateRasterizerState2
 */

struct npt_command_ID3D11Device3_CreateRasterizerState2 {
    void *_self;
    const D3D11_RASTERIZER_DESC2 * pRasterizerDesc;
    ID3D11RasterizerState2 ** ppRasterizerState;
    /* Shadow: the guest-allocated id for ppRasterizerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRasterizerState -> *ppRasterizerState} in the
     * context object table. */
    uint64_t _guest_id_ppRasterizerState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device3_CreateRasterizerState2(const D3D11_RASTERIZER_DESC2 * pRasterizerDesc,ID3D11RasterizerState2 ** ppRasterizerState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pRasterizerDesc);
    if (pRasterizerDesc)
        cmd_size += npt_sizeof_D3D11_RASTERIZER_DESC2(pRasterizerDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppRasterizerState: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_CreateRasterizerState2(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D11_RASTERIZER_DESC2 * pRasterizerDesc,
                                                ID3D11RasterizerState2 ** ppRasterizerState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_CreateRasterizerState2(pRasterizerDesc,ppRasterizerState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateRasterizerState2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pRasterizerDesc))
        npt_encode_D3D11_RASTERIZER_DESC2(enc, pRasterizerDesc);
{
    uint64_t _gid_ppRasterizerState = 0;
    if (ppRasterizerState) {
        _gid_ppRasterizerState = npt_com_allocate_next_id();
        *ppRasterizerState = (ID3D11RasterizerState2 *)(uintptr_t)_gid_ppRasterizerState;
    }
    npt_encode_uint64_t(enc, &_gid_ppRasterizerState);
}
}

static inline size_t
npt_sizeof_ID3D11Device3_CreateRasterizerState2_reply(const D3D11_RASTERIZER_DESC2 * pRasterizerDesc,ID3D11RasterizerState2 ** ppRasterizerState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRasterizerDesc (input only) */
/* skip ppRasterizerState (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_CreateRasterizerState2_reply(struct npt_cs_decoder *dec,
                                                      ID3D11RasterizerState2 ** ppRasterizerState,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_CreateRasterizerState2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_CreateRasterizerState2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_CreateRasterizerState2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppRasterizerState (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device3_CreateRasterizerState2(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D11_RASTERIZER_DESC2 * pRasterizerDesc,
                                                ID3D11RasterizerState2 ** ppRasterizerState,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_CreateRasterizerState2(pRasterizerDesc,ppRasterizerState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_CreateRasterizerState2_reply(pRasterizerDesc,ppRasterizerState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_CreateRasterizerState2(enc, cmd_flags, object_id, pRasterizerDesc, ppRasterizerState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_CreateRasterizerState2(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const D3D11_RASTERIZER_DESC2 * pRasterizerDesc,
                                               ID3D11RasterizerState2 ** ppRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateRasterizerState2(ring, 0, object_id, pRasterizerDesc, ppRasterizerState, &submit);
}

static inline HRESULT
npt_call_ID3D11Device3_CreateRasterizerState2(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const D3D11_RASTERIZER_DESC2 * pRasterizerDesc,
                                              ID3D11RasterizerState2 ** ppRasterizerState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateRasterizerState2(ring, NPT_CMD_FLAG_REPLY, object_id, pRasterizerDesc, ppRasterizerState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device3_CreateRasterizerState2_reply(dec, ppRasterizerState, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device3::CreateShaderResourceView1
 */

struct npt_command_ID3D11Device3_CreateShaderResourceView1 {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_SHADER_RESOURCE_VIEW_DESC1 * pDesc1;
    ID3D11ShaderResourceView1 ** ppSRView1;
    /* Shadow: the guest-allocated id for ppSRView1 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSRView1 -> *ppSRView1} in the
     * context object table. */
    uint64_t _guest_id_ppSRView1;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device3_CreateShaderResourceView1(const ID3D11Resource * pResource,const D3D11_SHADER_RESOURCE_VIEW_DESC1 * pDesc1,ID3D11ShaderResourceView1 ** ppSRView1)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc1);
    if (pDesc1)
        cmd_size += npt_sizeof_D3D11_SHADER_RESOURCE_VIEW_DESC1(pDesc1, max_mode);
cmd_size += sizeof(uint64_t);  /* ppSRView1: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_CreateShaderResourceView1(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11Resource * pResource,
                                                   const D3D11_SHADER_RESOURCE_VIEW_DESC1 * pDesc1,
                                                   ID3D11ShaderResourceView1 ** ppSRView1)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_CreateShaderResourceView1(pResource,pDesc1,ppSRView1);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateShaderResourceView1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc1))
        npt_encode_D3D11_SHADER_RESOURCE_VIEW_DESC1(enc, pDesc1);
{
    uint64_t _gid_ppSRView1 = 0;
    if (ppSRView1) {
        _gid_ppSRView1 = npt_com_allocate_next_id();
        *ppSRView1 = (ID3D11ShaderResourceView1 *)(uintptr_t)_gid_ppSRView1;
    }
    npt_encode_uint64_t(enc, &_gid_ppSRView1);
}
}

static inline size_t
npt_sizeof_ID3D11Device3_CreateShaderResourceView1_reply(const ID3D11Resource * pResource,const D3D11_SHADER_RESOURCE_VIEW_DESC1 * pDesc1,ID3D11ShaderResourceView1 ** ppSRView1)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc1 (input only) */
/* skip ppSRView1 (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_CreateShaderResourceView1_reply(struct npt_cs_decoder *dec,
                                                         ID3D11ShaderResourceView1 ** ppSRView1,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_CreateShaderResourceView1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_CreateShaderResourceView1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_CreateShaderResourceView1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSRView1 (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device3_CreateShaderResourceView1(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11Resource * pResource,
                                                   const D3D11_SHADER_RESOURCE_VIEW_DESC1 * pDesc1,
                                                   ID3D11ShaderResourceView1 ** ppSRView1,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_CreateShaderResourceView1(pResource,pDesc1,ppSRView1);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_CreateShaderResourceView1_reply(pResource,pDesc1,ppSRView1)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_CreateShaderResourceView1(enc, cmd_flags, object_id, pResource, pDesc1, ppSRView1);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_CreateShaderResourceView1(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D11Resource * pResource,
                                                  const D3D11_SHADER_RESOURCE_VIEW_DESC1 * pDesc1,
                                                  ID3D11ShaderResourceView1 ** ppSRView1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateShaderResourceView1(ring, 0, object_id, pResource, pDesc1, ppSRView1, &submit);
}

static inline HRESULT
npt_call_ID3D11Device3_CreateShaderResourceView1(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource,
                                                 const D3D11_SHADER_RESOURCE_VIEW_DESC1 * pDesc1,
                                                 ID3D11ShaderResourceView1 ** ppSRView1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateShaderResourceView1(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc1, ppSRView1, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device3_CreateShaderResourceView1_reply(dec, ppSRView1, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device3::CreateUnorderedAccessView1
 */

struct npt_command_ID3D11Device3_CreateUnorderedAccessView1 {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_UNORDERED_ACCESS_VIEW_DESC1 * pDesc1;
    ID3D11UnorderedAccessView1 ** ppUAView1;
    /* Shadow: the guest-allocated id for ppUAView1 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppUAView1 -> *ppUAView1} in the
     * context object table. */
    uint64_t _guest_id_ppUAView1;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device3_CreateUnorderedAccessView1(const ID3D11Resource * pResource,const D3D11_UNORDERED_ACCESS_VIEW_DESC1 * pDesc1,ID3D11UnorderedAccessView1 ** ppUAView1)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc1);
    if (pDesc1)
        cmd_size += npt_sizeof_D3D11_UNORDERED_ACCESS_VIEW_DESC1(pDesc1, max_mode);
cmd_size += sizeof(uint64_t);  /* ppUAView1: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_CreateUnorderedAccessView1(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    ID3D11Resource * pResource,
                                                    const D3D11_UNORDERED_ACCESS_VIEW_DESC1 * pDesc1,
                                                    ID3D11UnorderedAccessView1 ** ppUAView1)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_CreateUnorderedAccessView1(pResource,pDesc1,ppUAView1);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateUnorderedAccessView1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc1))
        npt_encode_D3D11_UNORDERED_ACCESS_VIEW_DESC1(enc, pDesc1);
{
    uint64_t _gid_ppUAView1 = 0;
    if (ppUAView1) {
        _gid_ppUAView1 = npt_com_allocate_next_id();
        *ppUAView1 = (ID3D11UnorderedAccessView1 *)(uintptr_t)_gid_ppUAView1;
    }
    npt_encode_uint64_t(enc, &_gid_ppUAView1);
}
}

static inline size_t
npt_sizeof_ID3D11Device3_CreateUnorderedAccessView1_reply(const ID3D11Resource * pResource,const D3D11_UNORDERED_ACCESS_VIEW_DESC1 * pDesc1,ID3D11UnorderedAccessView1 ** ppUAView1)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc1 (input only) */
/* skip ppUAView1 (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_CreateUnorderedAccessView1_reply(struct npt_cs_decoder *dec,
                                                          ID3D11UnorderedAccessView1 ** ppUAView1,
                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_CreateUnorderedAccessView1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_CreateUnorderedAccessView1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_CreateUnorderedAccessView1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppUAView1 (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device3_CreateUnorderedAccessView1(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    ID3D11Resource * pResource,
                                                    const D3D11_UNORDERED_ACCESS_VIEW_DESC1 * pDesc1,
                                                    ID3D11UnorderedAccessView1 ** ppUAView1,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_CreateUnorderedAccessView1(pResource,pDesc1,ppUAView1);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_CreateUnorderedAccessView1_reply(pResource,pDesc1,ppUAView1)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_CreateUnorderedAccessView1(enc, cmd_flags, object_id, pResource, pDesc1, ppUAView1);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_CreateUnorderedAccessView1(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11Resource * pResource,
                                                   const D3D11_UNORDERED_ACCESS_VIEW_DESC1 * pDesc1,
                                                   ID3D11UnorderedAccessView1 ** ppUAView1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateUnorderedAccessView1(ring, 0, object_id, pResource, pDesc1, ppUAView1, &submit);
}

static inline HRESULT
npt_call_ID3D11Device3_CreateUnorderedAccessView1(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D11Resource * pResource,
                                                  const D3D11_UNORDERED_ACCESS_VIEW_DESC1 * pDesc1,
                                                  ID3D11UnorderedAccessView1 ** ppUAView1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateUnorderedAccessView1(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc1, ppUAView1, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device3_CreateUnorderedAccessView1_reply(dec, ppUAView1, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device3::CreateRenderTargetView1
 */

struct npt_command_ID3D11Device3_CreateRenderTargetView1 {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_RENDER_TARGET_VIEW_DESC1 * pDesc1;
    ID3D11RenderTargetView1 ** ppRTView1;
    /* Shadow: the guest-allocated id for ppRTView1 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRTView1 -> *ppRTView1} in the
     * context object table. */
    uint64_t _guest_id_ppRTView1;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device3_CreateRenderTargetView1(const ID3D11Resource * pResource,const D3D11_RENDER_TARGET_VIEW_DESC1 * pDesc1,ID3D11RenderTargetView1 ** ppRTView1)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc1);
    if (pDesc1)
        cmd_size += npt_sizeof_D3D11_RENDER_TARGET_VIEW_DESC1(pDesc1, max_mode);
cmd_size += sizeof(uint64_t);  /* ppRTView1: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_CreateRenderTargetView1(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource,
                                                 const D3D11_RENDER_TARGET_VIEW_DESC1 * pDesc1,
                                                 ID3D11RenderTargetView1 ** ppRTView1)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_CreateRenderTargetView1(pResource,pDesc1,ppRTView1);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateRenderTargetView1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc1))
        npt_encode_D3D11_RENDER_TARGET_VIEW_DESC1(enc, pDesc1);
{
    uint64_t _gid_ppRTView1 = 0;
    if (ppRTView1) {
        _gid_ppRTView1 = npt_com_allocate_next_id();
        *ppRTView1 = (ID3D11RenderTargetView1 *)(uintptr_t)_gid_ppRTView1;
    }
    npt_encode_uint64_t(enc, &_gid_ppRTView1);
}
}

static inline size_t
npt_sizeof_ID3D11Device3_CreateRenderTargetView1_reply(const ID3D11Resource * pResource,const D3D11_RENDER_TARGET_VIEW_DESC1 * pDesc1,ID3D11RenderTargetView1 ** ppRTView1)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc1 (input only) */
/* skip ppRTView1 (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_CreateRenderTargetView1_reply(struct npt_cs_decoder *dec,
                                                       ID3D11RenderTargetView1 ** ppRTView1,
                                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_CreateRenderTargetView1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_CreateRenderTargetView1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_CreateRenderTargetView1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppRTView1 (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device3_CreateRenderTargetView1(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 ID3D11Resource * pResource,
                                                 const D3D11_RENDER_TARGET_VIEW_DESC1 * pDesc1,
                                                 ID3D11RenderTargetView1 ** ppRTView1,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_CreateRenderTargetView1(pResource,pDesc1,ppRTView1);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_CreateRenderTargetView1_reply(pResource,pDesc1,ppRTView1)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_CreateRenderTargetView1(enc, cmd_flags, object_id, pResource, pDesc1, ppRTView1);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_CreateRenderTargetView1(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11Resource * pResource,
                                                const D3D11_RENDER_TARGET_VIEW_DESC1 * pDesc1,
                                                ID3D11RenderTargetView1 ** ppRTView1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateRenderTargetView1(ring, 0, object_id, pResource, pDesc1, ppRTView1, &submit);
}

static inline HRESULT
npt_call_ID3D11Device3_CreateRenderTargetView1(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11Resource * pResource,
                                               const D3D11_RENDER_TARGET_VIEW_DESC1 * pDesc1,
                                               ID3D11RenderTargetView1 ** ppRTView1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateRenderTargetView1(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc1, ppRTView1, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device3_CreateRenderTargetView1_reply(dec, ppRTView1, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device3::CreateQuery1
 */

struct npt_command_ID3D11Device3_CreateQuery1 {
    void *_self;
    const D3D11_QUERY_DESC1 * pQueryDesc1;
    ID3D11Query1 ** ppQuery1;
    /* Shadow: the guest-allocated id for ppQuery1 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppQuery1 -> *ppQuery1} in the
     * context object table. */
    uint64_t _guest_id_ppQuery1;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device3_CreateQuery1(const D3D11_QUERY_DESC1 * pQueryDesc1,ID3D11Query1 ** ppQuery1)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pQueryDesc1);
    if (pQueryDesc1)
        cmd_size += npt_sizeof_D3D11_QUERY_DESC1(pQueryDesc1, max_mode);
cmd_size += sizeof(uint64_t);  /* ppQuery1: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_CreateQuery1(struct npt_cs_encoder *enc,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const D3D11_QUERY_DESC1 * pQueryDesc1,
                                      ID3D11Query1 ** ppQuery1)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_CreateQuery1(pQueryDesc1,ppQuery1);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateQuery1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pQueryDesc1))
        npt_encode_D3D11_QUERY_DESC1(enc, pQueryDesc1);
{
    uint64_t _gid_ppQuery1 = 0;
    if (ppQuery1) {
        _gid_ppQuery1 = npt_com_allocate_next_id();
        *ppQuery1 = (ID3D11Query1 *)(uintptr_t)_gid_ppQuery1;
    }
    npt_encode_uint64_t(enc, &_gid_ppQuery1);
}
}

static inline size_t
npt_sizeof_ID3D11Device3_CreateQuery1_reply(const D3D11_QUERY_DESC1 * pQueryDesc1,ID3D11Query1 ** ppQuery1)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pQueryDesc1 (input only) */
/* skip ppQuery1 (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_CreateQuery1_reply(struct npt_cs_decoder *dec,
                                            ID3D11Query1 ** ppQuery1,
                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_CreateQuery1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_CreateQuery1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_CreateQuery1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppQuery1 (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device3_CreateQuery1(struct npt_ring *ring,
                                      uint32_t cmd_flags,
                                      npt_object_id object_id,
                                      const D3D11_QUERY_DESC1 * pQueryDesc1,
                                      ID3D11Query1 ** ppQuery1,
                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_CreateQuery1(pQueryDesc1,ppQuery1);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_CreateQuery1_reply(pQueryDesc1,ppQuery1)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_CreateQuery1(enc, cmd_flags, object_id, pQueryDesc1, ppQuery1);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_CreateQuery1(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     const D3D11_QUERY_DESC1 * pQueryDesc1,
                                     ID3D11Query1 ** ppQuery1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateQuery1(ring, 0, object_id, pQueryDesc1, ppQuery1, &submit);
}

static inline HRESULT
npt_call_ID3D11Device3_CreateQuery1(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    const D3D11_QUERY_DESC1 * pQueryDesc1,
                                    ID3D11Query1 ** ppQuery1)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateQuery1(ring, NPT_CMD_FLAG_REPLY, object_id, pQueryDesc1, ppQuery1, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device3_CreateQuery1_reply(dec, ppQuery1, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device3::GetImmediateContext3
 */

struct npt_command_ID3D11Device3_GetImmediateContext3 {
    void *_self;
    ID3D11DeviceContext3 ** ppImmediateContext;
    /* Shadow: the guest-allocated id for ppImmediateContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppImmediateContext -> *ppImmediateContext} in the
     * context object table. */
    uint64_t _guest_id_ppImmediateContext;
};

static inline size_t
npt_sizeof_ID3D11Device3_GetImmediateContext3(ID3D11DeviceContext3 ** ppImmediateContext)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
cmd_size += sizeof(uint64_t);  /* ppImmediateContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_GetImmediateContext3(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11DeviceContext3 ** ppImmediateContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_GetImmediateContext3(ppImmediateContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_GetImmediateContext3),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
{
    uint64_t _gid_ppImmediateContext = 0;
    if (ppImmediateContext) {
        _gid_ppImmediateContext = npt_com_allocate_next_id();
        *ppImmediateContext = (ID3D11DeviceContext3 *)(uintptr_t)_gid_ppImmediateContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppImmediateContext);
}
}

static inline size_t
npt_sizeof_ID3D11Device3_GetImmediateContext3_reply(ID3D11DeviceContext3 ** ppImmediateContext)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_GetImmediateContext3_reply(struct npt_cs_decoder *dec,
                                                    ID3D11DeviceContext3 ** ppImmediateContext)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_GetImmediateContext3))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_GetImmediateContext3 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_GetImmediateContext3));
        return;
    }
/* skip ppImmediateContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device3_GetImmediateContext3(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11DeviceContext3 ** ppImmediateContext,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_GetImmediateContext3(ppImmediateContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_GetImmediateContext3_reply(ppImmediateContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_GetImmediateContext3(enc, cmd_flags, object_id, ppImmediateContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_GetImmediateContext3(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11DeviceContext3 ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_GetImmediateContext3(ring, 0, object_id, ppImmediateContext, &submit);
}

static inline void
npt_call_ID3D11Device3_GetImmediateContext3(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            ID3D11DeviceContext3 ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_GetImmediateContext3(ring, NPT_CMD_FLAG_REPLY, object_id, ppImmediateContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11Device3_GetImmediateContext3_reply(dec, ppImmediateContext);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Device3::CreateDeferredContext3
 */

struct npt_command_ID3D11Device3_CreateDeferredContext3 {
    void *_self;
    UINT ContextFlags;
    ID3D11DeviceContext3 ** ppDeferredContext;
    /* Shadow: the guest-allocated id for ppDeferredContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDeferredContext -> *ppDeferredContext} in the
     * context object table. */
    uint64_t _guest_id_ppDeferredContext;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device3_CreateDeferredContext3(const UINT ContextFlags,ID3D11DeviceContext3 ** ppDeferredContext)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&ContextFlags, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDeferredContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_CreateDeferredContext3(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT ContextFlags,
                                                ID3D11DeviceContext3 ** ppDeferredContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_CreateDeferredContext3(ContextFlags,ppDeferredContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateDeferredContext3),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &ContextFlags);
{
    uint64_t _gid_ppDeferredContext = 0;
    if (ppDeferredContext) {
        _gid_ppDeferredContext = npt_com_allocate_next_id();
        *ppDeferredContext = (ID3D11DeviceContext3 *)(uintptr_t)_gid_ppDeferredContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppDeferredContext);
}
}

static inline size_t
npt_sizeof_ID3D11Device3_CreateDeferredContext3_reply(const UINT ContextFlags,ID3D11DeviceContext3 ** ppDeferredContext)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ContextFlags (input only) */
/* skip ppDeferredContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_CreateDeferredContext3_reply(struct npt_cs_decoder *dec,
                                                      ID3D11DeviceContext3 ** ppDeferredContext,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_CreateDeferredContext3))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_CreateDeferredContext3 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_CreateDeferredContext3));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDeferredContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device3_CreateDeferredContext3(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT ContextFlags,
                                                ID3D11DeviceContext3 ** ppDeferredContext,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_CreateDeferredContext3(ContextFlags,ppDeferredContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_CreateDeferredContext3_reply(ContextFlags,ppDeferredContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_CreateDeferredContext3(enc, cmd_flags, object_id, ContextFlags, ppDeferredContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_CreateDeferredContext3(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               UINT ContextFlags,
                                               ID3D11DeviceContext3 ** ppDeferredContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateDeferredContext3(ring, 0, object_id, ContextFlags, ppDeferredContext, &submit);
}

static inline HRESULT
npt_call_ID3D11Device3_CreateDeferredContext3(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT ContextFlags,
                                              ID3D11DeviceContext3 ** ppDeferredContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_CreateDeferredContext3(ring, NPT_CMD_FLAG_REPLY, object_id, ContextFlags, ppDeferredContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device3_CreateDeferredContext3_reply(dec, ppDeferredContext, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device3::WriteToSubresource
 */

struct npt_command_ID3D11Device3_WriteToSubresource {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    const D3D11_BOX * pDstBox;
    const void * pSrcData;
    UINT SrcRowPitch;
    UINT SrcDepthPitch;
};

static inline size_t
npt_sizeof_ID3D11Device3_WriteToSubresource(const ID3D11Resource * pDstResource,const UINT DstSubresource,const D3D11_BOX * pDstBox,const void * pSrcData,const UINT SrcRowPitch,const UINT SrcDepthPitch)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DstSubresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDstBox);
    if (pDstBox)
        cmd_size += npt_sizeof_D3D11_BOX(pDstBox, max_mode);
    /* ERROR: pSrcData (void) is not serializable */
    return 0;
    cmd_size += npt_sizeof_UINT(&SrcRowPitch, max_mode);
    cmd_size += npt_sizeof_UINT(&SrcDepthPitch, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_WriteToSubresource(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11Resource * pDstResource,
                                            UINT DstSubresource,
                                            const D3D11_BOX * pDstBox,
                                            const void * pSrcData,
                                            UINT SrcRowPitch,
                                            UINT SrcDepthPitch)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_WriteToSubresource(pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_WriteToSubresource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_UINT(enc, &DstSubresource);
    if (npt_encode_simple_pointer(enc, pDstBox))
        npt_encode_D3D11_BOX(enc, pDstBox);
    /* ERROR: pSrcData (void) is not serializable */
    return;
    npt_encode_UINT(enc, &SrcRowPitch);
    npt_encode_UINT(enc, &SrcDepthPitch);
}

static inline size_t
npt_sizeof_ID3D11Device3_WriteToSubresource_reply(const ID3D11Resource * pDstResource,const UINT DstSubresource,const D3D11_BOX * pDstBox,const void * pSrcData,const UINT SrcRowPitch,const UINT SrcDepthPitch)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip DstSubresource (input only) */
/* skip pDstBox (input only) */
/* skip pSrcData (input only) */
/* skip SrcRowPitch (input only) */
/* skip SrcDepthPitch (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11Device3_WriteToSubresource(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11Resource * pDstResource,
                                            UINT DstSubresource,
                                            const D3D11_BOX * pDstBox,
                                            const void * pSrcData,
                                            UINT SrcRowPitch,
                                            UINT SrcDepthPitch,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_WriteToSubresource(pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_WriteToSubresource_reply(pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_WriteToSubresource(enc, cmd_flags, object_id, pDstResource, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_WriteToSubresource(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D11Resource * pDstResource,
                                           UINT DstSubresource,
                                           const D3D11_BOX * pDstBox,
                                           const void * pSrcData,
                                           UINT SrcRowPitch,
                                           UINT SrcDepthPitch)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_WriteToSubresource(ring, 0, object_id, pDstResource, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch, &submit);
}

static inline void
npt_call_ID3D11Device3_WriteToSubresource(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11Resource * pDstResource,
                                          UINT DstSubresource,
                                          const D3D11_BOX * pDstBox,
                                          const void * pSrcData,
                                          UINT SrcRowPitch,
                                          UINT SrcDepthPitch)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_WriteToSubresource(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Device3::ReadFromSubresource
 */

struct npt_command_ID3D11Device3_ReadFromSubresource {
    void *_self;
    void * pDstData;
    UINT DstRowPitch;
    UINT DstDepthPitch;
    ID3D11Resource * pSrcResource;
    UINT SrcSubresource;
    const D3D11_BOX * pSrcBox;
};

static inline size_t
npt_sizeof_ID3D11Device3_ReadFromSubresource(const void * pDstData,const UINT DstRowPitch,const UINT DstDepthPitch,const ID3D11Resource * pSrcResource,const UINT SrcSubresource,const D3D11_BOX * pSrcBox)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDstData (output only) */
    cmd_size += npt_sizeof_UINT(&DstRowPitch, max_mode);
    cmd_size += npt_sizeof_UINT(&DstDepthPitch, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&SrcSubresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSrcBox);
    if (pSrcBox)
        cmd_size += npt_sizeof_D3D11_BOX(pSrcBox, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device3_ReadFromSubresource(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             void * pDstData,
                                             UINT DstRowPitch,
                                             UINT DstDepthPitch,
                                             ID3D11Resource * pSrcResource,
                                             UINT SrcSubresource,
                                             const D3D11_BOX * pSrcBox)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device3_ReadFromSubresource(pDstData,DstRowPitch,DstDepthPitch,pSrcResource,SrcSubresource,pSrcBox);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_ReadFromSubresource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDstData (output only) */
    npt_encode_UINT(enc, &DstRowPitch);
    npt_encode_UINT(enc, &DstDepthPitch);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcResource));
    npt_encode_UINT(enc, &SrcSubresource);
    if (npt_encode_simple_pointer(enc, pSrcBox))
        npt_encode_D3D11_BOX(enc, pSrcBox);
}

static inline size_t
npt_sizeof_ID3D11Device3_ReadFromSubresource_reply(const void * pDstData,const UINT DstRowPitch,const UINT DstDepthPitch,const ID3D11Resource * pSrcResource,const UINT SrcSubresource,const D3D11_BOX * pSrcBox)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* ERROR: pDstData (void) is not serializable */
    return 0;
/* skip DstRowPitch (input only) */
/* skip DstDepthPitch (input only) */
/* skip pSrcResource (input only) */
/* skip SrcSubresource (input only) */
/* skip pSrcBox (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device3_ReadFromSubresource_reply(struct npt_cs_decoder *dec,
                                                   void * pDstData)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                                  NPT_METHOD_ID3D11Device3_ReadFromSubresource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device3_ReadFromSubresource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                       NPT_METHOD_ID3D11Device3_ReadFromSubresource));
        return;
    }
    /* ERROR: pDstData (void) is not serializable */
    return;
}

static inline void
npt_submit_ID3D11Device3_ReadFromSubresource(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             void * pDstData,
                                             UINT DstRowPitch,
                                             UINT DstDepthPitch,
                                             ID3D11Resource * pSrcResource,
                                             UINT SrcSubresource,
                                             const D3D11_BOX * pSrcBox,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device3_ReadFromSubresource(pDstData,DstRowPitch,DstDepthPitch,pSrcResource,SrcSubresource,pSrcBox);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device3_ReadFromSubresource_reply(pDstData,DstRowPitch,DstDepthPitch,pSrcResource,SrcSubresource,pSrcBox)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device3_ReadFromSubresource(enc, cmd_flags, object_id, pDstData, DstRowPitch, DstDepthPitch, pSrcResource, SrcSubresource, pSrcBox);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device3_ReadFromSubresource(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            void * pDstData,
                                            UINT DstRowPitch,
                                            UINT DstDepthPitch,
                                            ID3D11Resource * pSrcResource,
                                            UINT SrcSubresource,
                                            const D3D11_BOX * pSrcBox)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_ReadFromSubresource(ring, 0, object_id, pDstData, DstRowPitch, DstDepthPitch, pSrcResource, SrcSubresource, pSrcBox, &submit);
}

static inline void
npt_call_ID3D11Device3_ReadFromSubresource(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           void * pDstData,
                                           UINT DstRowPitch,
                                           UINT DstDepthPitch,
                                           ID3D11Resource * pSrcResource,
                                           UINT SrcSubresource,
                                           const D3D11_BOX * pSrcBox)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device3_ReadFromSubresource(ring, NPT_CMD_FLAG_REPLY, object_id, pDstData, DstRowPitch, DstDepthPitch, pSrcResource, SrcSubresource, pSrcBox, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11Device3_ReadFromSubresource_reply(dec, pDstData);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Device4::RegisterDeviceRemovedEvent
 */

struct npt_command_ID3D11Device4_RegisterDeviceRemovedEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device4_RegisterDeviceRemovedEvent(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pdwCookie (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device4_RegisterDeviceRemovedEvent(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    HANDLE hEvent,
                                                    DWORD * pdwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device4_RegisterDeviceRemovedEvent(hEvent,pdwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device4,
                                 NPT_METHOD_ID3D11Device4_RegisterDeviceRemovedEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
/* skip pdwCookie (output only) */
}

static inline size_t
npt_sizeof_ID3D11Device4_RegisterDeviceRemovedEvent_reply(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hEvent (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DWORD(&(const DWORD){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device4_RegisterDeviceRemovedEvent_reply(struct npt_cs_decoder *dec,
                                                          DWORD * pdwCookie,
                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device4,
                                                  NPT_METHOD_ID3D11Device4_RegisterDeviceRemovedEvent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device4_RegisterDeviceRemovedEvent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device4,
                                       NPT_METHOD_ID3D11Device4_RegisterDeviceRemovedEvent));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pdwCookie)
            npt_decode_DWORD(dec, (DWORD *)pdwCookie);
    }
}

static inline void
npt_submit_ID3D11Device4_RegisterDeviceRemovedEvent(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    HANDLE hEvent,
                                                    DWORD * pdwCookie,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device4_RegisterDeviceRemovedEvent(hEvent,pdwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device4_RegisterDeviceRemovedEvent_reply(hEvent,pdwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device4_RegisterDeviceRemovedEvent(enc, cmd_flags, object_id, hEvent, pdwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device4_RegisterDeviceRemovedEvent(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   HANDLE hEvent,
                                                   DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device4_RegisterDeviceRemovedEvent(ring, 0, object_id, hEvent, pdwCookie, &submit);
}

static inline HRESULT
npt_call_ID3D11Device4_RegisterDeviceRemovedEvent(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  HANDLE hEvent,
                                                  DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device4_RegisterDeviceRemovedEvent(ring, NPT_CMD_FLAG_REPLY, object_id, hEvent, pdwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device4_RegisterDeviceRemovedEvent_reply(dec, pdwCookie, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device4::UnregisterDeviceRemoved
 */

struct npt_command_ID3D11Device4_UnregisterDeviceRemoved {
    void *_self;
    DWORD dwCookie;
};

static inline size_t
npt_sizeof_ID3D11Device4_UnregisterDeviceRemoved(const DWORD dwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DWORD(&dwCookie, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device4_UnregisterDeviceRemoved(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 DWORD dwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device4_UnregisterDeviceRemoved(dwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device4,
                                 NPT_METHOD_ID3D11Device4_UnregisterDeviceRemoved),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DWORD(enc, &dwCookie);
}

static inline size_t
npt_sizeof_ID3D11Device4_UnregisterDeviceRemoved_reply(const DWORD dwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip dwCookie (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11Device4_UnregisterDeviceRemoved(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 DWORD dwCookie,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device4_UnregisterDeviceRemoved(dwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device4_UnregisterDeviceRemoved_reply(dwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device4_UnregisterDeviceRemoved(enc, cmd_flags, object_id, dwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device4_UnregisterDeviceRemoved(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device4_UnregisterDeviceRemoved(ring, 0, object_id, dwCookie, &submit);
}

static inline void
npt_call_ID3D11Device4_UnregisterDeviceRemoved(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device4_UnregisterDeviceRemoved(ring, NPT_CMD_FLAG_REPLY, object_id, dwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11Device5::OpenSharedFence
 */

struct npt_command_ID3D11Device5_OpenSharedFence {
    void *_self;
    HANDLE hFence;
    const IID * ReturnedInterface;
    void ** ppFence;
    /* Shadow: the guest-allocated id for ppFence arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppFence -> *ppFence} in the
     * context object table. */
    uint64_t _guest_id_ppFence;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device5_OpenSharedFence(const HANDLE hFence,const IID * ReturnedInterface,void ** ppFence)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(ReturnedInterface);
    if (ReturnedInterface)
        cmd_size += npt_sizeof_IID(ReturnedInterface, max_mode);
cmd_size += sizeof(uint64_t);  /* ppFence: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device5_OpenSharedFence(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         HANDLE hFence,
                                         const IID * ReturnedInterface,
                                         void ** ppFence)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device5_OpenSharedFence(hFence,ReturnedInterface,ppFence);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device5,
                                 NPT_METHOD_ID3D11Device5_OpenSharedFence),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hFence));
    if (npt_encode_simple_pointer(enc, ReturnedInterface))
        npt_encode_IID(enc, ReturnedInterface);
{
    uint64_t _gid_ppFence = 0;
    if (ppFence) {
        _gid_ppFence = npt_com_allocate_next_id();
        *ppFence = (void *)(uintptr_t)_gid_ppFence;
    }
    npt_encode_uint64_t(enc, &_gid_ppFence);
}
}

static inline size_t
npt_sizeof_ID3D11Device5_OpenSharedFence_reply(const HANDLE hFence,const IID * ReturnedInterface,void ** ppFence)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hFence (input only) */
/* skip ReturnedInterface (input only) */
/* skip ppFence (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device5_OpenSharedFence_reply(struct npt_cs_decoder *dec,
                                               void ** ppFence,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device5,
                                                  NPT_METHOD_ID3D11Device5_OpenSharedFence))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device5_OpenSharedFence reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device5,
                                       NPT_METHOD_ID3D11Device5_OpenSharedFence));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppFence (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device5_OpenSharedFence(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         HANDLE hFence,
                                         const IID * ReturnedInterface,
                                         void ** ppFence,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device5_OpenSharedFence(hFence,ReturnedInterface,ppFence);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device5_OpenSharedFence_reply(hFence,ReturnedInterface,ppFence)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device5_OpenSharedFence(enc, cmd_flags, object_id, hFence, ReturnedInterface, ppFence);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device5_OpenSharedFence(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        HANDLE hFence,
                                        const IID * ReturnedInterface,
                                        void ** ppFence)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device5_OpenSharedFence(ring, 0, object_id, hFence, ReturnedInterface, ppFence, &submit);
}

static inline HRESULT
npt_call_ID3D11Device5_OpenSharedFence(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       HANDLE hFence,
                                       const IID * ReturnedInterface,
                                       void ** ppFence)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device5_OpenSharedFence(ring, NPT_CMD_FLAG_REPLY, object_id, hFence, ReturnedInterface, ppFence, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device5_OpenSharedFence_reply(dec, ppFence, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11Device5::CreateFence
 */

struct npt_command_ID3D11Device5_CreateFence {
    void *_self;
    UINT64 InitialValue;
    D3D11_FENCE_FLAG Flags;
    const IID * ReturnedInterface;
    void ** ppFence;
    /* Shadow: the guest-allocated id for ppFence arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppFence -> *ppFence} in the
     * context object table. */
    uint64_t _guest_id_ppFence;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11Device5_CreateFence(const UINT64 InitialValue,const D3D11_FENCE_FLAG Flags,const IID * ReturnedInterface,void ** ppFence)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT64(&InitialValue, max_mode);
    cmd_size += npt_sizeof_D3D11_FENCE_FLAG(&Flags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(ReturnedInterface);
    if (ReturnedInterface)
        cmd_size += npt_sizeof_IID(ReturnedInterface, max_mode);
cmd_size += sizeof(uint64_t);  /* ppFence: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11Device5_CreateFence(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     UINT64 InitialValue,
                                     D3D11_FENCE_FLAG Flags,
                                     const IID * ReturnedInterface,
                                     void ** ppFence)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11Device5_CreateFence(InitialValue,Flags,ReturnedInterface,ppFence);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device5,
                                 NPT_METHOD_ID3D11Device5_CreateFence),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT64(enc, &InitialValue);
    npt_encode_D3D11_FENCE_FLAG(enc, &Flags);
    if (npt_encode_simple_pointer(enc, ReturnedInterface))
        npt_encode_IID(enc, ReturnedInterface);
{
    uint64_t _gid_ppFence = 0;
    if (ppFence) {
        _gid_ppFence = npt_com_allocate_next_id();
        *ppFence = (void *)(uintptr_t)_gid_ppFence;
    }
    npt_encode_uint64_t(enc, &_gid_ppFence);
}
}

static inline size_t
npt_sizeof_ID3D11Device5_CreateFence_reply(const UINT64 InitialValue,const D3D11_FENCE_FLAG Flags,const IID * ReturnedInterface,void ** ppFence)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip InitialValue (input only) */
/* skip Flags (input only) */
/* skip ReturnedInterface (input only) */
/* skip ppFence (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11Device5_CreateFence_reply(struct npt_cs_decoder *dec,
                                           void ** ppFence,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device5,
                                                  NPT_METHOD_ID3D11Device5_CreateFence))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11Device5_CreateFence reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device5,
                                       NPT_METHOD_ID3D11Device5_CreateFence));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppFence (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11Device5_CreateFence(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     UINT64 InitialValue,
                                     D3D11_FENCE_FLAG Flags,
                                     const IID * ReturnedInterface,
                                     void ** ppFence,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11Device5_CreateFence(InitialValue,Flags,ReturnedInterface,ppFence);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11Device5_CreateFence_reply(InitialValue,Flags,ReturnedInterface,ppFence)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11Device5_CreateFence(enc, cmd_flags, object_id, InitialValue, Flags, ReturnedInterface, ppFence);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11Device5_CreateFence(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    UINT64 InitialValue,
                                    D3D11_FENCE_FLAG Flags,
                                    const IID * ReturnedInterface,
                                    void ** ppFence)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device5_CreateFence(ring, 0, object_id, InitialValue, Flags, ReturnedInterface, ppFence, &submit);
}

static inline HRESULT
npt_call_ID3D11Device5_CreateFence(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   UINT64 InitialValue,
                                   D3D11_FENCE_FLAG Flags,
                                   const IID * ReturnedInterface,
                                   void ** ppFence)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11Device5_CreateFence(ring, NPT_CMD_FLAG_REPLY, object_id, InitialValue, Flags, ReturnedInterface, ppFence, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11Device5_CreateFence_reply(dec, ppFence, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11DEVICE_H */
