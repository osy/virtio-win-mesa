/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for id3d11device: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_id3d11device.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* ID3D11Device                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d11device_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D11Device,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d11device_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device_default_Release(void *self)
{
    return npt_com_default_release(self);
}

/* skip_default: ID3D11Device::CreateBuffer -- override required.
 * The default body aborts loudly so a missing override is a runtime
 * failure rather than a silent data drop. */
HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateBuffer(void *self, const D3D11_BUFFER_DESC * pDesc, const D3D11_SUBRESOURCE_DATA * pInitialData, ID3D11Buffer ** ppBuffer)
{
    (void)self;
    (void)pDesc;
    (void)pInitialData;
    (void)ppBuffer;
    npt_com_assert_overridden("ID3D11Device", "CreateBuffer");
    return (HRESULT)0x80004005 /* E_FAIL */;
}

/* skip_default: ID3D11Device::CreateTexture1D -- override required.
 * The default body aborts loudly so a missing override is a runtime
 * failure rather than a silent data drop. */
HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateTexture1D(void *self, const D3D11_TEXTURE1D_DESC * pDesc, const D3D11_SUBRESOURCE_DATA * pInitialData, ID3D11Texture1D ** ppTexture1D)
{
    (void)self;
    (void)pDesc;
    (void)pInitialData;
    (void)ppTexture1D;
    npt_com_assert_overridden("ID3D11Device", "CreateTexture1D");
    return (HRESULT)0x80004005 /* E_FAIL */;
}

/* skip_default: ID3D11Device::CreateTexture2D -- override required.
 * The default body aborts loudly so a missing override is a runtime
 * failure rather than a silent data drop. */
HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateTexture2D(void *self, const D3D11_TEXTURE2D_DESC * pDesc, const D3D11_SUBRESOURCE_DATA * pInitialData, ID3D11Texture2D ** ppTexture2D)
{
    (void)self;
    (void)pDesc;
    (void)pInitialData;
    (void)ppTexture2D;
    npt_com_assert_overridden("ID3D11Device", "CreateTexture2D");
    return (HRESULT)0x80004005 /* E_FAIL */;
}

/* skip_default: ID3D11Device::CreateTexture3D -- override required.
 * The default body aborts loudly so a missing override is a runtime
 * failure rather than a silent data drop. */
HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateTexture3D(void *self, const D3D11_TEXTURE3D_DESC * pDesc, const D3D11_SUBRESOURCE_DATA * pInitialData, ID3D11Texture3D ** ppTexture3D)
{
    (void)self;
    (void)pDesc;
    (void)pInitialData;
    (void)ppTexture3D;
    npt_com_assert_overridden("ID3D11Device", "CreateTexture3D");
    return (HRESULT)0x80004005 /* E_FAIL */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateShaderResourceView(void *self, ID3D11Resource * pResource, const D3D11_SHADER_RESOURCE_VIEW_DESC * pDesc, ID3D11ShaderResourceView ** ppSRView)
{
    ID3D11ShaderResourceView *_raw_ppSRView = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11ShaderResourceView **_orig_ppSRView = ppSRView;
    if (ppSRView)
        ppSRView = (ID3D11ShaderResourceView **)&_raw_ppSRView;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateShaderResourceView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppSRView);
    } else {
        npt_async_ID3D11Device_CreateShaderResourceView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppSRView);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppSRView) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppSRView)
            *_orig_ppSRView = (ID3D11ShaderResourceView *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11ShaderResourceView,
                (uint64_t)(uintptr_t)_raw_ppSRView,
                (struct npt_com_base *)self);
        else
            *_orig_ppSRView = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateUnorderedAccessView(void *self, ID3D11Resource * pResource, const D3D11_UNORDERED_ACCESS_VIEW_DESC * pDesc, ID3D11UnorderedAccessView ** ppUAView)
{
    ID3D11UnorderedAccessView *_raw_ppUAView = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11UnorderedAccessView **_orig_ppUAView = ppUAView;
    if (ppUAView)
        ppUAView = (ID3D11UnorderedAccessView **)&_raw_ppUAView;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateUnorderedAccessView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppUAView);
    } else {
        npt_async_ID3D11Device_CreateUnorderedAccessView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppUAView);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppUAView) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppUAView)
            *_orig_ppUAView = (ID3D11UnorderedAccessView *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11UnorderedAccessView,
                (uint64_t)(uintptr_t)_raw_ppUAView,
                (struct npt_com_base *)self);
        else
            *_orig_ppUAView = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateRenderTargetView(void *self, ID3D11Resource * pResource, const D3D11_RENDER_TARGET_VIEW_DESC * pDesc, ID3D11RenderTargetView ** ppRTView)
{
    ID3D11RenderTargetView *_raw_ppRTView = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11RenderTargetView **_orig_ppRTView = ppRTView;
    if (ppRTView)
        ppRTView = (ID3D11RenderTargetView **)&_raw_ppRTView;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateRenderTargetView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppRTView);
    } else {
        npt_async_ID3D11Device_CreateRenderTargetView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppRTView);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppRTView) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppRTView)
            *_orig_ppRTView = (ID3D11RenderTargetView *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11RenderTargetView,
                (uint64_t)(uintptr_t)_raw_ppRTView,
                (struct npt_com_base *)self);
        else
            *_orig_ppRTView = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateDepthStencilView(void *self, ID3D11Resource * pResource, const D3D11_DEPTH_STENCIL_VIEW_DESC * pDesc, ID3D11DepthStencilView ** ppDepthStencilView)
{
    ID3D11DepthStencilView *_raw_ppDepthStencilView = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DepthStencilView **_orig_ppDepthStencilView = ppDepthStencilView;
    if (ppDepthStencilView)
        ppDepthStencilView = (ID3D11DepthStencilView **)&_raw_ppDepthStencilView;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateDepthStencilView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppDepthStencilView);
    } else {
        npt_async_ID3D11Device_CreateDepthStencilView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,ppDepthStencilView);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppDepthStencilView) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppDepthStencilView)
            *_orig_ppDepthStencilView = (ID3D11DepthStencilView *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DepthStencilView,
                (uint64_t)(uintptr_t)_raw_ppDepthStencilView,
                (struct npt_com_base *)self);
        else
            *_orig_ppDepthStencilView = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateInputLayout(void *self, const D3D11_INPUT_ELEMENT_DESC * pInputElementDescs, UINT NumElements, const void * pShaderBytecodeWithInputSignature, SIZE_T BytecodeLength, ID3D11InputLayout ** ppInputLayout)
{
    ID3D11InputLayout *_raw_ppInputLayout = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11InputLayout **_orig_ppInputLayout = ppInputLayout;
    if (ppInputLayout)
        ppInputLayout = (ID3D11InputLayout **)&_raw_ppInputLayout;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateInputLayout(npt_com_self_ring(self),npt_com_self_id(self),pInputElementDescs,NumElements,pShaderBytecodeWithInputSignature,BytecodeLength,ppInputLayout);
    } else {
        npt_async_ID3D11Device_CreateInputLayout(npt_com_self_ring(self),npt_com_self_id(self),pInputElementDescs,NumElements,pShaderBytecodeWithInputSignature,BytecodeLength,ppInputLayout);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppInputLayout) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppInputLayout)
            *_orig_ppInputLayout = (ID3D11InputLayout *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11InputLayout,
                (uint64_t)(uintptr_t)_raw_ppInputLayout,
                (struct npt_com_base *)self);
        else
            *_orig_ppInputLayout = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateVertexShader(void *self, const void * pShaderBytecode, SIZE_T BytecodeLength, ID3D11ClassLinkage * pClassLinkage, ID3D11VertexShader ** ppVertexShader)
{
    ID3D11VertexShader *_raw_ppVertexShader = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11VertexShader **_orig_ppVertexShader = ppVertexShader;
    if (ppVertexShader)
        ppVertexShader = (ID3D11VertexShader **)&_raw_ppVertexShader;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateVertexShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppVertexShader);
    } else {
        npt_async_ID3D11Device_CreateVertexShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppVertexShader);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppVertexShader) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppVertexShader)
            *_orig_ppVertexShader = (ID3D11VertexShader *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11VertexShader,
                (uint64_t)(uintptr_t)_raw_ppVertexShader,
                (struct npt_com_base *)self);
        else
            *_orig_ppVertexShader = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateGeometryShader(void *self, const void * pShaderBytecode, SIZE_T BytecodeLength, ID3D11ClassLinkage * pClassLinkage, ID3D11GeometryShader ** ppGeometryShader)
{
    ID3D11GeometryShader *_raw_ppGeometryShader = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11GeometryShader **_orig_ppGeometryShader = ppGeometryShader;
    if (ppGeometryShader)
        ppGeometryShader = (ID3D11GeometryShader **)&_raw_ppGeometryShader;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateGeometryShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppGeometryShader);
    } else {
        npt_async_ID3D11Device_CreateGeometryShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppGeometryShader);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppGeometryShader) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppGeometryShader)
            *_orig_ppGeometryShader = (ID3D11GeometryShader *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11GeometryShader,
                (uint64_t)(uintptr_t)_raw_ppGeometryShader,
                (struct npt_com_base *)self);
        else
            *_orig_ppGeometryShader = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateGeometryShaderWithStreamOutput(void *self, const void * pShaderBytecode, SIZE_T BytecodeLength, const D3D11_SO_DECLARATION_ENTRY * pSODeclaration, UINT NumEntries, const UINT * pBufferStrides, UINT NumStrides, UINT RasterizedStream, ID3D11ClassLinkage * pClassLinkage, ID3D11GeometryShader ** ppGeometryShader)
{
    ID3D11GeometryShader *_raw_ppGeometryShader = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11GeometryShader **_orig_ppGeometryShader = ppGeometryShader;
    if (ppGeometryShader)
        ppGeometryShader = (ID3D11GeometryShader **)&_raw_ppGeometryShader;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateGeometryShaderWithStreamOutput(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pSODeclaration,NumEntries,pBufferStrides,NumStrides,RasterizedStream,pClassLinkage,ppGeometryShader);
    } else {
        npt_async_ID3D11Device_CreateGeometryShaderWithStreamOutput(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pSODeclaration,NumEntries,pBufferStrides,NumStrides,RasterizedStream,pClassLinkage,ppGeometryShader);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppGeometryShader) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppGeometryShader)
            *_orig_ppGeometryShader = (ID3D11GeometryShader *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11GeometryShader,
                (uint64_t)(uintptr_t)_raw_ppGeometryShader,
                (struct npt_com_base *)self);
        else
            *_orig_ppGeometryShader = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreatePixelShader(void *self, const void * pShaderBytecode, SIZE_T BytecodeLength, ID3D11ClassLinkage * pClassLinkage, ID3D11PixelShader ** ppPixelShader)
{
    ID3D11PixelShader *_raw_ppPixelShader = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11PixelShader **_orig_ppPixelShader = ppPixelShader;
    if (ppPixelShader)
        ppPixelShader = (ID3D11PixelShader **)&_raw_ppPixelShader;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreatePixelShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppPixelShader);
    } else {
        npt_async_ID3D11Device_CreatePixelShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppPixelShader);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppPixelShader) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppPixelShader)
            *_orig_ppPixelShader = (ID3D11PixelShader *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11PixelShader,
                (uint64_t)(uintptr_t)_raw_ppPixelShader,
                (struct npt_com_base *)self);
        else
            *_orig_ppPixelShader = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateHullShader(void *self, const void * pShaderBytecode, SIZE_T BytecodeLength, ID3D11ClassLinkage * pClassLinkage, ID3D11HullShader ** ppHullShader)
{
    ID3D11HullShader *_raw_ppHullShader = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11HullShader **_orig_ppHullShader = ppHullShader;
    if (ppHullShader)
        ppHullShader = (ID3D11HullShader **)&_raw_ppHullShader;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateHullShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppHullShader);
    } else {
        npt_async_ID3D11Device_CreateHullShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppHullShader);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppHullShader) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppHullShader)
            *_orig_ppHullShader = (ID3D11HullShader *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11HullShader,
                (uint64_t)(uintptr_t)_raw_ppHullShader,
                (struct npt_com_base *)self);
        else
            *_orig_ppHullShader = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateDomainShader(void *self, const void * pShaderBytecode, SIZE_T BytecodeLength, ID3D11ClassLinkage * pClassLinkage, ID3D11DomainShader ** ppDomainShader)
{
    ID3D11DomainShader *_raw_ppDomainShader = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DomainShader **_orig_ppDomainShader = ppDomainShader;
    if (ppDomainShader)
        ppDomainShader = (ID3D11DomainShader **)&_raw_ppDomainShader;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateDomainShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppDomainShader);
    } else {
        npt_async_ID3D11Device_CreateDomainShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppDomainShader);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppDomainShader) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppDomainShader)
            *_orig_ppDomainShader = (ID3D11DomainShader *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DomainShader,
                (uint64_t)(uintptr_t)_raw_ppDomainShader,
                (struct npt_com_base *)self);
        else
            *_orig_ppDomainShader = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateComputeShader(void *self, const void * pShaderBytecode, SIZE_T BytecodeLength, ID3D11ClassLinkage * pClassLinkage, ID3D11ComputeShader ** ppComputeShader)
{
    ID3D11ComputeShader *_raw_ppComputeShader = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11ComputeShader **_orig_ppComputeShader = ppComputeShader;
    if (ppComputeShader)
        ppComputeShader = (ID3D11ComputeShader **)&_raw_ppComputeShader;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateComputeShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppComputeShader);
    } else {
        npt_async_ID3D11Device_CreateComputeShader(npt_com_self_ring(self),npt_com_self_id(self),pShaderBytecode,BytecodeLength,pClassLinkage,ppComputeShader);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppComputeShader) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppComputeShader)
            *_orig_ppComputeShader = (ID3D11ComputeShader *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11ComputeShader,
                (uint64_t)(uintptr_t)_raw_ppComputeShader,
                (struct npt_com_base *)self);
        else
            *_orig_ppComputeShader = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateClassLinkage(void *self, ID3D11ClassLinkage ** ppLinkage)
{
    ID3D11ClassLinkage *_raw_ppLinkage = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11ClassLinkage **_orig_ppLinkage = ppLinkage;
    if (ppLinkage)
        ppLinkage = (ID3D11ClassLinkage **)&_raw_ppLinkage;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateClassLinkage(npt_com_self_ring(self),npt_com_self_id(self),ppLinkage);
    } else {
        npt_async_ID3D11Device_CreateClassLinkage(npt_com_self_ring(self),npt_com_self_id(self),ppLinkage);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppLinkage) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppLinkage)
            *_orig_ppLinkage = (ID3D11ClassLinkage *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11ClassLinkage,
                (uint64_t)(uintptr_t)_raw_ppLinkage,
                (struct npt_com_base *)self);
        else
            *_orig_ppLinkage = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateBlendState(void *self, const D3D11_BLEND_DESC * pBlendStateDesc, ID3D11BlendState ** ppBlendState)
{
    ID3D11BlendState *_raw_ppBlendState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11BlendState **_orig_ppBlendState = ppBlendState;
    if (ppBlendState)
        ppBlendState = (ID3D11BlendState **)&_raw_ppBlendState;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateBlendState(npt_com_self_ring(self),npt_com_self_id(self),pBlendStateDesc,ppBlendState);
    } else {
        npt_async_ID3D11Device_CreateBlendState(npt_com_self_ring(self),npt_com_self_id(self),pBlendStateDesc,ppBlendState);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppBlendState) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppBlendState)
            *_orig_ppBlendState = (ID3D11BlendState *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11BlendState,
                (uint64_t)(uintptr_t)_raw_ppBlendState,
                (struct npt_com_base *)self);
        else
            *_orig_ppBlendState = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateDepthStencilState(void *self, const D3D11_DEPTH_STENCIL_DESC * pDepthStencilDesc, ID3D11DepthStencilState ** ppDepthStencilState)
{
    ID3D11DepthStencilState *_raw_ppDepthStencilState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DepthStencilState **_orig_ppDepthStencilState = ppDepthStencilState;
    if (ppDepthStencilState)
        ppDepthStencilState = (ID3D11DepthStencilState **)&_raw_ppDepthStencilState;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateDepthStencilState(npt_com_self_ring(self),npt_com_self_id(self),pDepthStencilDesc,ppDepthStencilState);
    } else {
        npt_async_ID3D11Device_CreateDepthStencilState(npt_com_self_ring(self),npt_com_self_id(self),pDepthStencilDesc,ppDepthStencilState);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppDepthStencilState) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppDepthStencilState)
            *_orig_ppDepthStencilState = (ID3D11DepthStencilState *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DepthStencilState,
                (uint64_t)(uintptr_t)_raw_ppDepthStencilState,
                (struct npt_com_base *)self);
        else
            *_orig_ppDepthStencilState = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateRasterizerState(void *self, const D3D11_RASTERIZER_DESC * pRasterizerDesc, ID3D11RasterizerState ** ppRasterizerState)
{
    ID3D11RasterizerState *_raw_ppRasterizerState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11RasterizerState **_orig_ppRasterizerState = ppRasterizerState;
    if (ppRasterizerState)
        ppRasterizerState = (ID3D11RasterizerState **)&_raw_ppRasterizerState;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateRasterizerState(npt_com_self_ring(self),npt_com_self_id(self),pRasterizerDesc,ppRasterizerState);
    } else {
        npt_async_ID3D11Device_CreateRasterizerState(npt_com_self_ring(self),npt_com_self_id(self),pRasterizerDesc,ppRasterizerState);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppRasterizerState) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppRasterizerState)
            *_orig_ppRasterizerState = (ID3D11RasterizerState *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11RasterizerState,
                (uint64_t)(uintptr_t)_raw_ppRasterizerState,
                (struct npt_com_base *)self);
        else
            *_orig_ppRasterizerState = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateSamplerState(void *self, const D3D11_SAMPLER_DESC * pSamplerDesc, ID3D11SamplerState ** ppSamplerState)
{
    ID3D11SamplerState *_raw_ppSamplerState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11SamplerState **_orig_ppSamplerState = ppSamplerState;
    if (ppSamplerState)
        ppSamplerState = (ID3D11SamplerState **)&_raw_ppSamplerState;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateSamplerState(npt_com_self_ring(self),npt_com_self_id(self),pSamplerDesc,ppSamplerState);
    } else {
        npt_async_ID3D11Device_CreateSamplerState(npt_com_self_ring(self),npt_com_self_id(self),pSamplerDesc,ppSamplerState);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppSamplerState) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppSamplerState)
            *_orig_ppSamplerState = (ID3D11SamplerState *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11SamplerState,
                (uint64_t)(uintptr_t)_raw_ppSamplerState,
                (struct npt_com_base *)self);
        else
            *_orig_ppSamplerState = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateQuery(void *self, const D3D11_QUERY_DESC * pQueryDesc, ID3D11Query ** ppQuery)
{
    ID3D11Query *_raw_ppQuery = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11Query **_orig_ppQuery = ppQuery;
    if (ppQuery)
        ppQuery = (ID3D11Query **)&_raw_ppQuery;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateQuery(npt_com_self_ring(self),npt_com_self_id(self),pQueryDesc,ppQuery);
    } else {
        npt_async_ID3D11Device_CreateQuery(npt_com_self_ring(self),npt_com_self_id(self),pQueryDesc,ppQuery);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppQuery) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppQuery)
            *_orig_ppQuery = (ID3D11Query *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11Query,
                (uint64_t)(uintptr_t)_raw_ppQuery,
                (struct npt_com_base *)self);
        else
            *_orig_ppQuery = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreatePredicate(void *self, const D3D11_QUERY_DESC * pPredicateDesc, ID3D11Predicate ** ppPredicate)
{
    ID3D11Predicate *_raw_ppPredicate = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11Predicate **_orig_ppPredicate = ppPredicate;
    if (ppPredicate)
        ppPredicate = (ID3D11Predicate **)&_raw_ppPredicate;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreatePredicate(npt_com_self_ring(self),npt_com_self_id(self),pPredicateDesc,ppPredicate);
    } else {
        npt_async_ID3D11Device_CreatePredicate(npt_com_self_ring(self),npt_com_self_id(self),pPredicateDesc,ppPredicate);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppPredicate) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppPredicate)
            *_orig_ppPredicate = (ID3D11Predicate *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11Predicate,
                (uint64_t)(uintptr_t)_raw_ppPredicate,
                (struct npt_com_base *)self);
        else
            *_orig_ppPredicate = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateCounter(void *self, const D3D11_COUNTER_DESC * pCounterDesc, ID3D11Counter ** ppCounter)
{
    ID3D11Counter *_raw_ppCounter = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11Counter **_orig_ppCounter = ppCounter;
    if (ppCounter)
        ppCounter = (ID3D11Counter **)&_raw_ppCounter;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateCounter(npt_com_self_ring(self),npt_com_self_id(self),pCounterDesc,ppCounter);
    } else {
        npt_async_ID3D11Device_CreateCounter(npt_com_self_ring(self),npt_com_self_id(self),pCounterDesc,ppCounter);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppCounter) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppCounter)
            *_orig_ppCounter = (ID3D11Counter *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11Counter,
                (uint64_t)(uintptr_t)_raw_ppCounter,
                (struct npt_com_base *)self);
        else
            *_orig_ppCounter = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CreateDeferredContext(void *self, UINT ContextFlags, ID3D11DeviceContext ** ppDeferredContext)
{
    ID3D11DeviceContext *_raw_ppDeferredContext = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DeviceContext **_orig_ppDeferredContext = ppDeferredContext;
    if (ppDeferredContext)
        ppDeferredContext = (ID3D11DeviceContext **)&_raw_ppDeferredContext;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_CreateDeferredContext(npt_com_self_ring(self),npt_com_self_id(self),ContextFlags,ppDeferredContext);
    } else {
        npt_async_ID3D11Device_CreateDeferredContext(npt_com_self_ring(self),npt_com_self_id(self),ContextFlags,ppDeferredContext);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppDeferredContext) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppDeferredContext)
            *_orig_ppDeferredContext = (ID3D11DeviceContext *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DeviceContext,
                (uint64_t)(uintptr_t)_raw_ppDeferredContext,
                (struct npt_com_base *)self);
        else
            *_orig_ppDeferredContext = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_OpenSharedResource(void *self, HANDLE hResource, const IID * ReturnedInterface, void ** ppResource)
{
    void *_raw_ppResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppResource = ppResource;
    if (ppResource)
        ppResource = (void **)&_raw_ppResource;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device_OpenSharedResource(npt_com_self_ring(self),npt_com_self_id(self),hResource,ReturnedInterface,ppResource);
    } else {
        npt_async_ID3D11Device_OpenSharedResource(npt_com_self_ring(self),npt_com_self_id(self),hResource,ReturnedInterface,ppResource);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppResource) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppResource)
            *_orig_ppResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                ReturnedInterface,
                (uint64_t)(uintptr_t)_raw_ppResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppResource = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CheckFormatSupport(void *self, DXGI_FORMAT Format, UINT * pFormatSupport)
{
    HRESULT _ret = npt_call_ID3D11Device_CheckFormatSupport(npt_com_self_ring(self),npt_com_self_id(self),Format,pFormatSupport);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CheckMultisampleQualityLevels(void *self, DXGI_FORMAT Format, UINT SampleCount, UINT * pNumQualityLevels)
{
    HRESULT _ret = npt_call_ID3D11Device_CheckMultisampleQualityLevels(npt_com_self_ring(self),npt_com_self_id(self),Format,SampleCount,pNumQualityLevels);
    return _ret;
}

void NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CheckCounterInfo(void *self, D3D11_COUNTER_INFO * pCounterInfo)
{
    npt_call_ID3D11Device_CheckCounterInfo(npt_com_self_ring(self),npt_com_self_id(self),pCounterInfo);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CheckCounter(void *self, const D3D11_COUNTER_DESC * pDesc, D3D11_COUNTER_TYPE * pType, UINT * pActiveCounters, CHAR * szName, UINT * pNameLength, CHAR * szUnits, UINT * pUnitsLength, CHAR * szDescription, UINT * pDescriptionLength)
{
    HRESULT _ret = npt_call_ID3D11Device_CheckCounter(npt_com_self_ring(self),npt_com_self_id(self),pDesc,pType,pActiveCounters,szName,pNameLength,szUnits,pUnitsLength,szDescription,pDescriptionLength);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_CheckFeatureSupport(void *self, D3D11_FEATURE Feature, void * pFeatureSupportData, UINT FeatureSupportDataSize)
{
    HRESULT _ret = npt_call_ID3D11Device_CheckFeatureSupport(npt_com_self_ring(self),npt_com_self_id(self),Feature,pFeatureSupportData,FeatureSupportDataSize);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_GetPrivateData(void *self, const GUID * guid, UINT * pDataSize, void * pData)
{
    HRESULT _ret = npt_call_ID3D11Device_GetPrivateData(npt_com_self_ring(self),npt_com_self_id(self),guid,pDataSize,pData);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_SetPrivateData(void *self, const GUID * guid, UINT DataSize, const void * pData)
{
    npt_async_ID3D11Device_SetPrivateData(npt_com_self_ring(self),npt_com_self_id(self),guid,DataSize,pData);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_SetPrivateDataInterface(void *self, const GUID * guid, const IUnknown * pData)
{
    npt_async_ID3D11Device_SetPrivateDataInterface(npt_com_self_ring(self),npt_com_self_id(self),guid,pData);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

D3D_FEATURE_LEVEL NPT_STDMETHODCALLTYPE
npt_id3d11device_default_GetFeatureLevel(void *self)
{
    D3D_FEATURE_LEVEL _ret = npt_call_ID3D11Device_GetFeatureLevel(npt_com_self_ring(self),npt_com_self_id(self));
    return _ret;
}

UINT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_GetCreationFlags(void *self)
{
    UINT _ret = npt_call_ID3D11Device_GetCreationFlags(npt_com_self_ring(self),npt_com_self_id(self));
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_GetDeviceRemovedReason(void *self)
{
    npt_async_ID3D11Device_GetDeviceRemovedReason(npt_com_self_ring(self),npt_com_self_id(self));
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

void NPT_STDMETHODCALLTYPE
npt_id3d11device_default_GetImmediateContext(void *self, ID3D11DeviceContext ** ppImmediateContext)
{
    ID3D11DeviceContext *_raw_ppImmediateContext = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DeviceContext **_orig_ppImmediateContext = ppImmediateContext;
    if (ppImmediateContext)
        ppImmediateContext = (ID3D11DeviceContext **)&_raw_ppImmediateContext;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    if (npt_com_self_device(self)->multi_ring_enabled) {
        npt_call_ID3D11Device_GetImmediateContext(npt_com_self_ring(self),npt_com_self_id(self),ppImmediateContext);
    } else {
        npt_async_ID3D11Device_GetImmediateContext(npt_com_self_ring(self),npt_com_self_id(self),ppImmediateContext);
    }
    if (_orig_ppImmediateContext) {
        if (_raw_ppImmediateContext)
            *_orig_ppImmediateContext = (ID3D11DeviceContext *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DeviceContext,
                (uint64_t)(uintptr_t)_raw_ppImmediateContext,
                (struct npt_com_base *)self);
        else
            *_orig_ppImmediateContext = NULL;
    }
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_SetExceptionMode(void *self, UINT RaiseFlags)
{
    npt_async_ID3D11Device_SetExceptionMode(npt_com_self_ring(self),npt_com_self_id(self),RaiseFlags);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

UINT NPT_STDMETHODCALLTYPE
npt_id3d11device_default_GetExceptionMode(void *self)
{
    UINT _ret = npt_call_ID3D11Device_GetExceptionMode(npt_com_self_ring(self),npt_com_self_id(self));
    return _ret;
}


void
npt_id3d11device_fill_default_vtbl(struct npt_id3d11device_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d11device_default_QueryInterface;
    dst->AddRef         = npt_id3d11device_default_AddRef;
    dst->Release        = npt_id3d11device_default_Release;
    dst->CreateBuffer = npt_id3d11device_default_CreateBuffer;
    dst->CreateTexture1D = npt_id3d11device_default_CreateTexture1D;
    dst->CreateTexture2D = npt_id3d11device_default_CreateTexture2D;
    dst->CreateTexture3D = npt_id3d11device_default_CreateTexture3D;
    dst->CreateShaderResourceView = npt_id3d11device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d11device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d11device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d11device_default_CreateDepthStencilView;
    dst->CreateInputLayout = npt_id3d11device_default_CreateInputLayout;
    dst->CreateVertexShader = npt_id3d11device_default_CreateVertexShader;
    dst->CreateGeometryShader = npt_id3d11device_default_CreateGeometryShader;
    dst->CreateGeometryShaderWithStreamOutput = npt_id3d11device_default_CreateGeometryShaderWithStreamOutput;
    dst->CreatePixelShader = npt_id3d11device_default_CreatePixelShader;
    dst->CreateHullShader = npt_id3d11device_default_CreateHullShader;
    dst->CreateDomainShader = npt_id3d11device_default_CreateDomainShader;
    dst->CreateComputeShader = npt_id3d11device_default_CreateComputeShader;
    dst->CreateClassLinkage = npt_id3d11device_default_CreateClassLinkage;
    dst->CreateBlendState = npt_id3d11device_default_CreateBlendState;
    dst->CreateDepthStencilState = npt_id3d11device_default_CreateDepthStencilState;
    dst->CreateRasterizerState = npt_id3d11device_default_CreateRasterizerState;
    dst->CreateSamplerState = npt_id3d11device_default_CreateSamplerState;
    dst->CreateQuery = npt_id3d11device_default_CreateQuery;
    dst->CreatePredicate = npt_id3d11device_default_CreatePredicate;
    dst->CreateCounter = npt_id3d11device_default_CreateCounter;
    dst->CreateDeferredContext = npt_id3d11device_default_CreateDeferredContext;
    dst->OpenSharedResource = npt_id3d11device_default_OpenSharedResource;
    dst->CheckFormatSupport = npt_id3d11device_default_CheckFormatSupport;
    dst->CheckMultisampleQualityLevels = npt_id3d11device_default_CheckMultisampleQualityLevels;
    dst->CheckCounterInfo = npt_id3d11device_default_CheckCounterInfo;
    dst->CheckCounter = npt_id3d11device_default_CheckCounter;
    dst->CheckFeatureSupport = npt_id3d11device_default_CheckFeatureSupport;
    dst->GetPrivateData = npt_id3d11device_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d11device_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d11device_default_SetPrivateDataInterface;
    dst->GetFeatureLevel = npt_id3d11device_default_GetFeatureLevel;
    dst->GetCreationFlags = npt_id3d11device_default_GetCreationFlags;
    dst->GetDeviceRemovedReason = npt_id3d11device_default_GetDeviceRemovedReason;
    dst->GetImmediateContext = npt_id3d11device_default_GetImmediateContext;
    dst->SetExceptionMode = npt_id3d11device_default_SetExceptionMode;
    dst->GetExceptionMode = npt_id3d11device_default_GetExceptionMode;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d11device_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d11device_client_vtbl npt_id3d11device_default_vtbl_storage;

void
npt_id3d11device_install_default_vtbl(void)
{
    npt_id3d11device_fill_default_vtbl(&npt_id3d11device_default_vtbl_storage);
}

void *
npt_id3d11device_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d11device_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D11Device1                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d11device1_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D11Device,
    &NPT_IID_ID3D11Device1,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d11device1_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_Release(void *self)
{
    return npt_com_default_release(self);
}

void NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_GetImmediateContext1(void *self, ID3D11DeviceContext1 ** ppImmediateContext)
{
    ID3D11DeviceContext1 *_raw_ppImmediateContext = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DeviceContext1 **_orig_ppImmediateContext = ppImmediateContext;
    if (ppImmediateContext)
        ppImmediateContext = (ID3D11DeviceContext1 **)&_raw_ppImmediateContext;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    if (npt_com_self_device(self)->multi_ring_enabled) {
        npt_call_ID3D11Device1_GetImmediateContext1(npt_com_self_ring(self),npt_com_self_id(self),ppImmediateContext);
    } else {
        npt_async_ID3D11Device1_GetImmediateContext1(npt_com_self_ring(self),npt_com_self_id(self),ppImmediateContext);
    }
    if (_orig_ppImmediateContext) {
        if (_raw_ppImmediateContext)
            *_orig_ppImmediateContext = (ID3D11DeviceContext1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DeviceContext1,
                (uint64_t)(uintptr_t)_raw_ppImmediateContext,
                (struct npt_com_base *)self);
        else
            *_orig_ppImmediateContext = NULL;
    }
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_CreateDeferredContext1(void *self, UINT ContextFlags, ID3D11DeviceContext1 ** ppDeferredContext)
{
    ID3D11DeviceContext1 *_raw_ppDeferredContext = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DeviceContext1 **_orig_ppDeferredContext = ppDeferredContext;
    if (ppDeferredContext)
        ppDeferredContext = (ID3D11DeviceContext1 **)&_raw_ppDeferredContext;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device1_CreateDeferredContext1(npt_com_self_ring(self),npt_com_self_id(self),ContextFlags,ppDeferredContext);
    } else {
        npt_async_ID3D11Device1_CreateDeferredContext1(npt_com_self_ring(self),npt_com_self_id(self),ContextFlags,ppDeferredContext);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppDeferredContext) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppDeferredContext)
            *_orig_ppDeferredContext = (ID3D11DeviceContext1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DeviceContext1,
                (uint64_t)(uintptr_t)_raw_ppDeferredContext,
                (struct npt_com_base *)self);
        else
            *_orig_ppDeferredContext = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_CreateBlendState1(void *self, const D3D11_BLEND_DESC1 * pBlendStateDesc, ID3D11BlendState1 ** ppBlendState)
{
    ID3D11BlendState1 *_raw_ppBlendState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11BlendState1 **_orig_ppBlendState = ppBlendState;
    if (ppBlendState)
        ppBlendState = (ID3D11BlendState1 **)&_raw_ppBlendState;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device1_CreateBlendState1(npt_com_self_ring(self),npt_com_self_id(self),pBlendStateDesc,ppBlendState);
    } else {
        npt_async_ID3D11Device1_CreateBlendState1(npt_com_self_ring(self),npt_com_self_id(self),pBlendStateDesc,ppBlendState);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppBlendState) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppBlendState)
            *_orig_ppBlendState = (ID3D11BlendState1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11BlendState1,
                (uint64_t)(uintptr_t)_raw_ppBlendState,
                (struct npt_com_base *)self);
        else
            *_orig_ppBlendState = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_CreateRasterizerState1(void *self, const D3D11_RASTERIZER_DESC1 * pRasterizerDesc, ID3D11RasterizerState1 ** ppRasterizerState)
{
    ID3D11RasterizerState1 *_raw_ppRasterizerState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11RasterizerState1 **_orig_ppRasterizerState = ppRasterizerState;
    if (ppRasterizerState)
        ppRasterizerState = (ID3D11RasterizerState1 **)&_raw_ppRasterizerState;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device1_CreateRasterizerState1(npt_com_self_ring(self),npt_com_self_id(self),pRasterizerDesc,ppRasterizerState);
    } else {
        npt_async_ID3D11Device1_CreateRasterizerState1(npt_com_self_ring(self),npt_com_self_id(self),pRasterizerDesc,ppRasterizerState);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppRasterizerState) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppRasterizerState)
            *_orig_ppRasterizerState = (ID3D11RasterizerState1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11RasterizerState1,
                (uint64_t)(uintptr_t)_raw_ppRasterizerState,
                (struct npt_com_base *)self);
        else
            *_orig_ppRasterizerState = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_CreateDeviceContextState(void *self, UINT Flags, const D3D_FEATURE_LEVEL * pFeatureLevels, UINT FeatureLevels, UINT SDKVersion, const IID * EmulatedInterface, D3D_FEATURE_LEVEL * pChosenFeatureLevel, ID3DDeviceContextState ** ppContextState)
{
    ID3DDeviceContextState *_raw_ppContextState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3DDeviceContextState **_orig_ppContextState = ppContextState;
    if (ppContextState)
        ppContextState = (ID3DDeviceContextState **)&_raw_ppContextState;
    HRESULT _ret = npt_call_ID3D11Device1_CreateDeviceContextState(npt_com_self_ring(self),npt_com_self_id(self),Flags,pFeatureLevels,FeatureLevels,SDKVersion,EmulatedInterface,pChosenFeatureLevel,ppContextState);
    if (_orig_ppContextState) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppContextState)
            *_orig_ppContextState = (ID3DDeviceContextState *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3DDeviceContextState,
                (uint64_t)(uintptr_t)_raw_ppContextState,
                (struct npt_com_base *)self);
        else
            *_orig_ppContextState = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_OpenSharedResource1(void *self, HANDLE hResource, const IID * returnedInterface, void ** ppResource)
{
    void *_raw_ppResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppResource = ppResource;
    if (ppResource)
        ppResource = (void **)&_raw_ppResource;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device1_OpenSharedResource1(npt_com_self_ring(self),npt_com_self_id(self),hResource,returnedInterface,ppResource);
    } else {
        npt_async_ID3D11Device1_OpenSharedResource1(npt_com_self_ring(self),npt_com_self_id(self),hResource,returnedInterface,ppResource);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppResource) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppResource)
            *_orig_ppResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                returnedInterface,
                (uint64_t)(uintptr_t)_raw_ppResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppResource = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device1_default_OpenSharedResourceByName(void *self, const WCHAR * lpName, DWORD dwDesiredAccess, const IID * returnedInterface, void ** ppResource)
{
    void *_raw_ppResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppResource = ppResource;
    if (ppResource)
        ppResource = (void **)&_raw_ppResource;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device1_OpenSharedResourceByName(npt_com_self_ring(self),npt_com_self_id(self),lpName,dwDesiredAccess,returnedInterface,ppResource);
    } else {
        npt_async_ID3D11Device1_OpenSharedResourceByName(npt_com_self_ring(self),npt_com_self_id(self),lpName,dwDesiredAccess,returnedInterface,ppResource);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppResource) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppResource)
            *_orig_ppResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                returnedInterface,
                (uint64_t)(uintptr_t)_raw_ppResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppResource = NULL;
    }
    return _ret;
}


void
npt_id3d11device1_fill_default_vtbl(struct npt_id3d11device1_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d11device1_default_QueryInterface;
    dst->AddRef         = npt_id3d11device1_default_AddRef;
    dst->Release        = npt_id3d11device1_default_Release;
    dst->CreateBuffer = npt_id3d11device_default_CreateBuffer;
    dst->CreateTexture1D = npt_id3d11device_default_CreateTexture1D;
    dst->CreateTexture2D = npt_id3d11device_default_CreateTexture2D;
    dst->CreateTexture3D = npt_id3d11device_default_CreateTexture3D;
    dst->CreateShaderResourceView = npt_id3d11device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d11device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d11device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d11device_default_CreateDepthStencilView;
    dst->CreateInputLayout = npt_id3d11device_default_CreateInputLayout;
    dst->CreateVertexShader = npt_id3d11device_default_CreateVertexShader;
    dst->CreateGeometryShader = npt_id3d11device_default_CreateGeometryShader;
    dst->CreateGeometryShaderWithStreamOutput = npt_id3d11device_default_CreateGeometryShaderWithStreamOutput;
    dst->CreatePixelShader = npt_id3d11device_default_CreatePixelShader;
    dst->CreateHullShader = npt_id3d11device_default_CreateHullShader;
    dst->CreateDomainShader = npt_id3d11device_default_CreateDomainShader;
    dst->CreateComputeShader = npt_id3d11device_default_CreateComputeShader;
    dst->CreateClassLinkage = npt_id3d11device_default_CreateClassLinkage;
    dst->CreateBlendState = npt_id3d11device_default_CreateBlendState;
    dst->CreateDepthStencilState = npt_id3d11device_default_CreateDepthStencilState;
    dst->CreateRasterizerState = npt_id3d11device_default_CreateRasterizerState;
    dst->CreateSamplerState = npt_id3d11device_default_CreateSamplerState;
    dst->CreateQuery = npt_id3d11device_default_CreateQuery;
    dst->CreatePredicate = npt_id3d11device_default_CreatePredicate;
    dst->CreateCounter = npt_id3d11device_default_CreateCounter;
    dst->CreateDeferredContext = npt_id3d11device_default_CreateDeferredContext;
    dst->OpenSharedResource = npt_id3d11device_default_OpenSharedResource;
    dst->CheckFormatSupport = npt_id3d11device_default_CheckFormatSupport;
    dst->CheckMultisampleQualityLevels = npt_id3d11device_default_CheckMultisampleQualityLevels;
    dst->CheckCounterInfo = npt_id3d11device_default_CheckCounterInfo;
    dst->CheckCounter = npt_id3d11device_default_CheckCounter;
    dst->CheckFeatureSupport = npt_id3d11device_default_CheckFeatureSupport;
    dst->GetPrivateData = npt_id3d11device_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d11device_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d11device_default_SetPrivateDataInterface;
    dst->GetFeatureLevel = npt_id3d11device_default_GetFeatureLevel;
    dst->GetCreationFlags = npt_id3d11device_default_GetCreationFlags;
    dst->GetDeviceRemovedReason = npt_id3d11device_default_GetDeviceRemovedReason;
    dst->GetImmediateContext = npt_id3d11device_default_GetImmediateContext;
    dst->SetExceptionMode = npt_id3d11device_default_SetExceptionMode;
    dst->GetExceptionMode = npt_id3d11device_default_GetExceptionMode;
    dst->GetImmediateContext1 = npt_id3d11device1_default_GetImmediateContext1;
    dst->CreateDeferredContext1 = npt_id3d11device1_default_CreateDeferredContext1;
    dst->CreateBlendState1 = npt_id3d11device1_default_CreateBlendState1;
    dst->CreateRasterizerState1 = npt_id3d11device1_default_CreateRasterizerState1;
    dst->CreateDeviceContextState = npt_id3d11device1_default_CreateDeviceContextState;
    dst->OpenSharedResource1 = npt_id3d11device1_default_OpenSharedResource1;
    dst->OpenSharedResourceByName = npt_id3d11device1_default_OpenSharedResourceByName;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d11device1_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d11device1_client_vtbl npt_id3d11device1_default_vtbl_storage;

void
npt_id3d11device1_install_default_vtbl(void)
{
    npt_id3d11device1_fill_default_vtbl(&npt_id3d11device1_default_vtbl_storage);
}

void *
npt_id3d11device1_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d11device1_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D11Device2                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d11device2_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D11Device,
    &NPT_IID_ID3D11Device1,
    &NPT_IID_ID3D11Device2,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device2_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d11device2_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device2_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device2_default_Release(void *self)
{
    return npt_com_default_release(self);
}

void NPT_STDMETHODCALLTYPE
npt_id3d11device2_default_GetImmediateContext2(void *self, ID3D11DeviceContext2 ** ppImmediateContext)
{
    ID3D11DeviceContext2 *_raw_ppImmediateContext = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DeviceContext2 **_orig_ppImmediateContext = ppImmediateContext;
    if (ppImmediateContext)
        ppImmediateContext = (ID3D11DeviceContext2 **)&_raw_ppImmediateContext;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    if (npt_com_self_device(self)->multi_ring_enabled) {
        npt_call_ID3D11Device2_GetImmediateContext2(npt_com_self_ring(self),npt_com_self_id(self),ppImmediateContext);
    } else {
        npt_async_ID3D11Device2_GetImmediateContext2(npt_com_self_ring(self),npt_com_self_id(self),ppImmediateContext);
    }
    if (_orig_ppImmediateContext) {
        if (_raw_ppImmediateContext)
            *_orig_ppImmediateContext = (ID3D11DeviceContext2 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DeviceContext2,
                (uint64_t)(uintptr_t)_raw_ppImmediateContext,
                (struct npt_com_base *)self);
        else
            *_orig_ppImmediateContext = NULL;
    }
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device2_default_CreateDeferredContext2(void *self, UINT ContextFlags, ID3D11DeviceContext2 ** ppDeferredContext)
{
    ID3D11DeviceContext2 *_raw_ppDeferredContext = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DeviceContext2 **_orig_ppDeferredContext = ppDeferredContext;
    if (ppDeferredContext)
        ppDeferredContext = (ID3D11DeviceContext2 **)&_raw_ppDeferredContext;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device2_CreateDeferredContext2(npt_com_self_ring(self),npt_com_self_id(self),ContextFlags,ppDeferredContext);
    } else {
        npt_async_ID3D11Device2_CreateDeferredContext2(npt_com_self_ring(self),npt_com_self_id(self),ContextFlags,ppDeferredContext);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppDeferredContext) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppDeferredContext)
            *_orig_ppDeferredContext = (ID3D11DeviceContext2 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DeviceContext2,
                (uint64_t)(uintptr_t)_raw_ppDeferredContext,
                (struct npt_com_base *)self);
        else
            *_orig_ppDeferredContext = NULL;
    }
    return _ret;
}

void NPT_STDMETHODCALLTYPE
npt_id3d11device2_default_GetResourceTiling(void *self, ID3D11Resource * pTiledResource, UINT * pNumTilesForEntireResource, D3D11_PACKED_MIP_DESC * pPackedMipDesc, D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips, UINT * pNumSubresourceTilings, UINT FirstSubresourceTilingToGet, D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    npt_call_ID3D11Device2_GetResourceTiling(npt_com_self_ring(self),npt_com_self_id(self),pTiledResource,pNumTilesForEntireResource,pPackedMipDesc,pStandardTileShapeForNonPackedMips,pNumSubresourceTilings,FirstSubresourceTilingToGet,pSubresourceTilingsForNonPackedMips);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device2_default_CheckMultisampleQualityLevels1(void *self, DXGI_FORMAT Format, UINT SampleCount, UINT Flags, UINT * pNumQualityLevels)
{
    HRESULT _ret = npt_call_ID3D11Device2_CheckMultisampleQualityLevels1(npt_com_self_ring(self),npt_com_self_id(self),Format,SampleCount,Flags,pNumQualityLevels);
    return _ret;
}


void
npt_id3d11device2_fill_default_vtbl(struct npt_id3d11device2_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d11device2_default_QueryInterface;
    dst->AddRef         = npt_id3d11device2_default_AddRef;
    dst->Release        = npt_id3d11device2_default_Release;
    dst->CreateBuffer = npt_id3d11device_default_CreateBuffer;
    dst->CreateTexture1D = npt_id3d11device_default_CreateTexture1D;
    dst->CreateTexture2D = npt_id3d11device_default_CreateTexture2D;
    dst->CreateTexture3D = npt_id3d11device_default_CreateTexture3D;
    dst->CreateShaderResourceView = npt_id3d11device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d11device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d11device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d11device_default_CreateDepthStencilView;
    dst->CreateInputLayout = npt_id3d11device_default_CreateInputLayout;
    dst->CreateVertexShader = npt_id3d11device_default_CreateVertexShader;
    dst->CreateGeometryShader = npt_id3d11device_default_CreateGeometryShader;
    dst->CreateGeometryShaderWithStreamOutput = npt_id3d11device_default_CreateGeometryShaderWithStreamOutput;
    dst->CreatePixelShader = npt_id3d11device_default_CreatePixelShader;
    dst->CreateHullShader = npt_id3d11device_default_CreateHullShader;
    dst->CreateDomainShader = npt_id3d11device_default_CreateDomainShader;
    dst->CreateComputeShader = npt_id3d11device_default_CreateComputeShader;
    dst->CreateClassLinkage = npt_id3d11device_default_CreateClassLinkage;
    dst->CreateBlendState = npt_id3d11device_default_CreateBlendState;
    dst->CreateDepthStencilState = npt_id3d11device_default_CreateDepthStencilState;
    dst->CreateRasterizerState = npt_id3d11device_default_CreateRasterizerState;
    dst->CreateSamplerState = npt_id3d11device_default_CreateSamplerState;
    dst->CreateQuery = npt_id3d11device_default_CreateQuery;
    dst->CreatePredicate = npt_id3d11device_default_CreatePredicate;
    dst->CreateCounter = npt_id3d11device_default_CreateCounter;
    dst->CreateDeferredContext = npt_id3d11device_default_CreateDeferredContext;
    dst->OpenSharedResource = npt_id3d11device_default_OpenSharedResource;
    dst->CheckFormatSupport = npt_id3d11device_default_CheckFormatSupport;
    dst->CheckMultisampleQualityLevels = npt_id3d11device_default_CheckMultisampleQualityLevels;
    dst->CheckCounterInfo = npt_id3d11device_default_CheckCounterInfo;
    dst->CheckCounter = npt_id3d11device_default_CheckCounter;
    dst->CheckFeatureSupport = npt_id3d11device_default_CheckFeatureSupport;
    dst->GetPrivateData = npt_id3d11device_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d11device_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d11device_default_SetPrivateDataInterface;
    dst->GetFeatureLevel = npt_id3d11device_default_GetFeatureLevel;
    dst->GetCreationFlags = npt_id3d11device_default_GetCreationFlags;
    dst->GetDeviceRemovedReason = npt_id3d11device_default_GetDeviceRemovedReason;
    dst->GetImmediateContext = npt_id3d11device_default_GetImmediateContext;
    dst->SetExceptionMode = npt_id3d11device_default_SetExceptionMode;
    dst->GetExceptionMode = npt_id3d11device_default_GetExceptionMode;
    dst->GetImmediateContext1 = npt_id3d11device1_default_GetImmediateContext1;
    dst->CreateDeferredContext1 = npt_id3d11device1_default_CreateDeferredContext1;
    dst->CreateBlendState1 = npt_id3d11device1_default_CreateBlendState1;
    dst->CreateRasterizerState1 = npt_id3d11device1_default_CreateRasterizerState1;
    dst->CreateDeviceContextState = npt_id3d11device1_default_CreateDeviceContextState;
    dst->OpenSharedResource1 = npt_id3d11device1_default_OpenSharedResource1;
    dst->OpenSharedResourceByName = npt_id3d11device1_default_OpenSharedResourceByName;
    dst->GetImmediateContext2 = npt_id3d11device2_default_GetImmediateContext2;
    dst->CreateDeferredContext2 = npt_id3d11device2_default_CreateDeferredContext2;
    dst->GetResourceTiling = npt_id3d11device2_default_GetResourceTiling;
    dst->CheckMultisampleQualityLevels1 = npt_id3d11device2_default_CheckMultisampleQualityLevels1;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d11device2_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d11device2_client_vtbl npt_id3d11device2_default_vtbl_storage;

void
npt_id3d11device2_install_default_vtbl(void)
{
    npt_id3d11device2_fill_default_vtbl(&npt_id3d11device2_default_vtbl_storage);
}

void *
npt_id3d11device2_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d11device2_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D11Device3                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d11device3_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D11Device,
    &NPT_IID_ID3D11Device1,
    &NPT_IID_ID3D11Device2,
    &NPT_IID_ID3D11Device3,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d11device3_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_CreateTexture2D1(void *self, const D3D11_TEXTURE2D_DESC1 * pDesc1, const D3D11_SUBRESOURCE_DATA * pInitialData, ID3D11Texture2D1 ** ppTexture2D)
{
    ID3D11Texture2D1 *_raw_ppTexture2D = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11Texture2D1 **_orig_ppTexture2D = ppTexture2D;
    if (ppTexture2D)
        ppTexture2D = (ID3D11Texture2D1 **)&_raw_ppTexture2D;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device3_CreateTexture2D1(npt_com_self_ring(self),npt_com_self_id(self),pDesc1,pInitialData,ppTexture2D);
    } else {
        npt_async_ID3D11Device3_CreateTexture2D1(npt_com_self_ring(self),npt_com_self_id(self),pDesc1,pInitialData,ppTexture2D);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppTexture2D) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppTexture2D)
            *_orig_ppTexture2D = (ID3D11Texture2D1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11Texture2D1,
                (uint64_t)(uintptr_t)_raw_ppTexture2D,
                (struct npt_com_base *)self);
        else
            *_orig_ppTexture2D = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_CreateTexture3D1(void *self, const D3D11_TEXTURE3D_DESC1 * pDesc1, const D3D11_SUBRESOURCE_DATA * pInitialData, ID3D11Texture3D1 ** ppTexture3D)
{
    ID3D11Texture3D1 *_raw_ppTexture3D = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11Texture3D1 **_orig_ppTexture3D = ppTexture3D;
    if (ppTexture3D)
        ppTexture3D = (ID3D11Texture3D1 **)&_raw_ppTexture3D;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device3_CreateTexture3D1(npt_com_self_ring(self),npt_com_self_id(self),pDesc1,pInitialData,ppTexture3D);
    } else {
        npt_async_ID3D11Device3_CreateTexture3D1(npt_com_self_ring(self),npt_com_self_id(self),pDesc1,pInitialData,ppTexture3D);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppTexture3D) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppTexture3D)
            *_orig_ppTexture3D = (ID3D11Texture3D1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11Texture3D1,
                (uint64_t)(uintptr_t)_raw_ppTexture3D,
                (struct npt_com_base *)self);
        else
            *_orig_ppTexture3D = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_CreateRasterizerState2(void *self, const D3D11_RASTERIZER_DESC2 * pRasterizerDesc, ID3D11RasterizerState2 ** ppRasterizerState)
{
    ID3D11RasterizerState2 *_raw_ppRasterizerState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11RasterizerState2 **_orig_ppRasterizerState = ppRasterizerState;
    if (ppRasterizerState)
        ppRasterizerState = (ID3D11RasterizerState2 **)&_raw_ppRasterizerState;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device3_CreateRasterizerState2(npt_com_self_ring(self),npt_com_self_id(self),pRasterizerDesc,ppRasterizerState);
    } else {
        npt_async_ID3D11Device3_CreateRasterizerState2(npt_com_self_ring(self),npt_com_self_id(self),pRasterizerDesc,ppRasterizerState);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppRasterizerState) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppRasterizerState)
            *_orig_ppRasterizerState = (ID3D11RasterizerState2 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11RasterizerState2,
                (uint64_t)(uintptr_t)_raw_ppRasterizerState,
                (struct npt_com_base *)self);
        else
            *_orig_ppRasterizerState = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_CreateShaderResourceView1(void *self, ID3D11Resource * pResource, const D3D11_SHADER_RESOURCE_VIEW_DESC1 * pDesc1, ID3D11ShaderResourceView1 ** ppSRView1)
{
    ID3D11ShaderResourceView1 *_raw_ppSRView1 = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11ShaderResourceView1 **_orig_ppSRView1 = ppSRView1;
    if (ppSRView1)
        ppSRView1 = (ID3D11ShaderResourceView1 **)&_raw_ppSRView1;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device3_CreateShaderResourceView1(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc1,ppSRView1);
    } else {
        npt_async_ID3D11Device3_CreateShaderResourceView1(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc1,ppSRView1);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppSRView1) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppSRView1)
            *_orig_ppSRView1 = (ID3D11ShaderResourceView1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11ShaderResourceView1,
                (uint64_t)(uintptr_t)_raw_ppSRView1,
                (struct npt_com_base *)self);
        else
            *_orig_ppSRView1 = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_CreateUnorderedAccessView1(void *self, ID3D11Resource * pResource, const D3D11_UNORDERED_ACCESS_VIEW_DESC1 * pDesc1, ID3D11UnorderedAccessView1 ** ppUAView1)
{
    ID3D11UnorderedAccessView1 *_raw_ppUAView1 = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11UnorderedAccessView1 **_orig_ppUAView1 = ppUAView1;
    if (ppUAView1)
        ppUAView1 = (ID3D11UnorderedAccessView1 **)&_raw_ppUAView1;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device3_CreateUnorderedAccessView1(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc1,ppUAView1);
    } else {
        npt_async_ID3D11Device3_CreateUnorderedAccessView1(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc1,ppUAView1);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppUAView1) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppUAView1)
            *_orig_ppUAView1 = (ID3D11UnorderedAccessView1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11UnorderedAccessView1,
                (uint64_t)(uintptr_t)_raw_ppUAView1,
                (struct npt_com_base *)self);
        else
            *_orig_ppUAView1 = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_CreateRenderTargetView1(void *self, ID3D11Resource * pResource, const D3D11_RENDER_TARGET_VIEW_DESC1 * pDesc1, ID3D11RenderTargetView1 ** ppRTView1)
{
    ID3D11RenderTargetView1 *_raw_ppRTView1 = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11RenderTargetView1 **_orig_ppRTView1 = ppRTView1;
    if (ppRTView1)
        ppRTView1 = (ID3D11RenderTargetView1 **)&_raw_ppRTView1;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device3_CreateRenderTargetView1(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc1,ppRTView1);
    } else {
        npt_async_ID3D11Device3_CreateRenderTargetView1(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc1,ppRTView1);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppRTView1) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppRTView1)
            *_orig_ppRTView1 = (ID3D11RenderTargetView1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11RenderTargetView1,
                (uint64_t)(uintptr_t)_raw_ppRTView1,
                (struct npt_com_base *)self);
        else
            *_orig_ppRTView1 = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_CreateQuery1(void *self, const D3D11_QUERY_DESC1 * pQueryDesc1, ID3D11Query1 ** ppQuery1)
{
    ID3D11Query1 *_raw_ppQuery1 = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11Query1 **_orig_ppQuery1 = ppQuery1;
    if (ppQuery1)
        ppQuery1 = (ID3D11Query1 **)&_raw_ppQuery1;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device3_CreateQuery1(npt_com_self_ring(self),npt_com_self_id(self),pQueryDesc1,ppQuery1);
    } else {
        npt_async_ID3D11Device3_CreateQuery1(npt_com_self_ring(self),npt_com_self_id(self),pQueryDesc1,ppQuery1);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppQuery1) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppQuery1)
            *_orig_ppQuery1 = (ID3D11Query1 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11Query1,
                (uint64_t)(uintptr_t)_raw_ppQuery1,
                (struct npt_com_base *)self);
        else
            *_orig_ppQuery1 = NULL;
    }
    return _ret;
}

void NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_GetImmediateContext3(void *self, ID3D11DeviceContext3 ** ppImmediateContext)
{
    ID3D11DeviceContext3 *_raw_ppImmediateContext = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DeviceContext3 **_orig_ppImmediateContext = ppImmediateContext;
    if (ppImmediateContext)
        ppImmediateContext = (ID3D11DeviceContext3 **)&_raw_ppImmediateContext;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    if (npt_com_self_device(self)->multi_ring_enabled) {
        npt_call_ID3D11Device3_GetImmediateContext3(npt_com_self_ring(self),npt_com_self_id(self),ppImmediateContext);
    } else {
        npt_async_ID3D11Device3_GetImmediateContext3(npt_com_self_ring(self),npt_com_self_id(self),ppImmediateContext);
    }
    if (_orig_ppImmediateContext) {
        if (_raw_ppImmediateContext)
            *_orig_ppImmediateContext = (ID3D11DeviceContext3 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DeviceContext3,
                (uint64_t)(uintptr_t)_raw_ppImmediateContext,
                (struct npt_com_base *)self);
        else
            *_orig_ppImmediateContext = NULL;
    }
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_CreateDeferredContext3(void *self, UINT ContextFlags, ID3D11DeviceContext3 ** ppDeferredContext)
{
    ID3D11DeviceContext3 *_raw_ppDeferredContext = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    ID3D11DeviceContext3 **_orig_ppDeferredContext = ppDeferredContext;
    if (ppDeferredContext)
        ppDeferredContext = (ID3D11DeviceContext3 **)&_raw_ppDeferredContext;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device3_CreateDeferredContext3(npt_com_self_ring(self),npt_com_self_id(self),ContextFlags,ppDeferredContext);
    } else {
        npt_async_ID3D11Device3_CreateDeferredContext3(npt_com_self_ring(self),npt_com_self_id(self),ContextFlags,ppDeferredContext);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppDeferredContext) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppDeferredContext)
            *_orig_ppDeferredContext = (ID3D11DeviceContext3 *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                &NPT_IID_ID3D11DeviceContext3,
                (uint64_t)(uintptr_t)_raw_ppDeferredContext,
                (struct npt_com_base *)self);
        else
            *_orig_ppDeferredContext = NULL;
    }
    return _ret;
}

void NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_WriteToSubresource(void *self, ID3D11Resource * pDstResource, UINT DstSubresource, const D3D11_BOX * pDstBox, const void * pSrcData, UINT SrcRowPitch, UINT SrcDepthPitch)
{
    npt_async_ID3D11Device3_WriteToSubresource(npt_com_self_ring(self),npt_com_self_id(self),pDstResource,DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch);
}

void NPT_STDMETHODCALLTYPE
npt_id3d11device3_default_ReadFromSubresource(void *self, void * pDstData, UINT DstRowPitch, UINT DstDepthPitch, ID3D11Resource * pSrcResource, UINT SrcSubresource, const D3D11_BOX * pSrcBox)
{
    npt_call_ID3D11Device3_ReadFromSubresource(npt_com_self_ring(self),npt_com_self_id(self),pDstData,DstRowPitch,DstDepthPitch,pSrcResource,SrcSubresource,pSrcBox);
}


void
npt_id3d11device3_fill_default_vtbl(struct npt_id3d11device3_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d11device3_default_QueryInterface;
    dst->AddRef         = npt_id3d11device3_default_AddRef;
    dst->Release        = npt_id3d11device3_default_Release;
    dst->CreateBuffer = npt_id3d11device_default_CreateBuffer;
    dst->CreateTexture1D = npt_id3d11device_default_CreateTexture1D;
    dst->CreateTexture2D = npt_id3d11device_default_CreateTexture2D;
    dst->CreateTexture3D = npt_id3d11device_default_CreateTexture3D;
    dst->CreateShaderResourceView = npt_id3d11device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d11device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d11device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d11device_default_CreateDepthStencilView;
    dst->CreateInputLayout = npt_id3d11device_default_CreateInputLayout;
    dst->CreateVertexShader = npt_id3d11device_default_CreateVertexShader;
    dst->CreateGeometryShader = npt_id3d11device_default_CreateGeometryShader;
    dst->CreateGeometryShaderWithStreamOutput = npt_id3d11device_default_CreateGeometryShaderWithStreamOutput;
    dst->CreatePixelShader = npt_id3d11device_default_CreatePixelShader;
    dst->CreateHullShader = npt_id3d11device_default_CreateHullShader;
    dst->CreateDomainShader = npt_id3d11device_default_CreateDomainShader;
    dst->CreateComputeShader = npt_id3d11device_default_CreateComputeShader;
    dst->CreateClassLinkage = npt_id3d11device_default_CreateClassLinkage;
    dst->CreateBlendState = npt_id3d11device_default_CreateBlendState;
    dst->CreateDepthStencilState = npt_id3d11device_default_CreateDepthStencilState;
    dst->CreateRasterizerState = npt_id3d11device_default_CreateRasterizerState;
    dst->CreateSamplerState = npt_id3d11device_default_CreateSamplerState;
    dst->CreateQuery = npt_id3d11device_default_CreateQuery;
    dst->CreatePredicate = npt_id3d11device_default_CreatePredicate;
    dst->CreateCounter = npt_id3d11device_default_CreateCounter;
    dst->CreateDeferredContext = npt_id3d11device_default_CreateDeferredContext;
    dst->OpenSharedResource = npt_id3d11device_default_OpenSharedResource;
    dst->CheckFormatSupport = npt_id3d11device_default_CheckFormatSupport;
    dst->CheckMultisampleQualityLevels = npt_id3d11device_default_CheckMultisampleQualityLevels;
    dst->CheckCounterInfo = npt_id3d11device_default_CheckCounterInfo;
    dst->CheckCounter = npt_id3d11device_default_CheckCounter;
    dst->CheckFeatureSupport = npt_id3d11device_default_CheckFeatureSupport;
    dst->GetPrivateData = npt_id3d11device_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d11device_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d11device_default_SetPrivateDataInterface;
    dst->GetFeatureLevel = npt_id3d11device_default_GetFeatureLevel;
    dst->GetCreationFlags = npt_id3d11device_default_GetCreationFlags;
    dst->GetDeviceRemovedReason = npt_id3d11device_default_GetDeviceRemovedReason;
    dst->GetImmediateContext = npt_id3d11device_default_GetImmediateContext;
    dst->SetExceptionMode = npt_id3d11device_default_SetExceptionMode;
    dst->GetExceptionMode = npt_id3d11device_default_GetExceptionMode;
    dst->GetImmediateContext1 = npt_id3d11device1_default_GetImmediateContext1;
    dst->CreateDeferredContext1 = npt_id3d11device1_default_CreateDeferredContext1;
    dst->CreateBlendState1 = npt_id3d11device1_default_CreateBlendState1;
    dst->CreateRasterizerState1 = npt_id3d11device1_default_CreateRasterizerState1;
    dst->CreateDeviceContextState = npt_id3d11device1_default_CreateDeviceContextState;
    dst->OpenSharedResource1 = npt_id3d11device1_default_OpenSharedResource1;
    dst->OpenSharedResourceByName = npt_id3d11device1_default_OpenSharedResourceByName;
    dst->GetImmediateContext2 = npt_id3d11device2_default_GetImmediateContext2;
    dst->CreateDeferredContext2 = npt_id3d11device2_default_CreateDeferredContext2;
    dst->GetResourceTiling = npt_id3d11device2_default_GetResourceTiling;
    dst->CheckMultisampleQualityLevels1 = npt_id3d11device2_default_CheckMultisampleQualityLevels1;
    dst->CreateTexture2D1 = npt_id3d11device3_default_CreateTexture2D1;
    dst->CreateTexture3D1 = npt_id3d11device3_default_CreateTexture3D1;
    dst->CreateRasterizerState2 = npt_id3d11device3_default_CreateRasterizerState2;
    dst->CreateShaderResourceView1 = npt_id3d11device3_default_CreateShaderResourceView1;
    dst->CreateUnorderedAccessView1 = npt_id3d11device3_default_CreateUnorderedAccessView1;
    dst->CreateRenderTargetView1 = npt_id3d11device3_default_CreateRenderTargetView1;
    dst->CreateQuery1 = npt_id3d11device3_default_CreateQuery1;
    dst->GetImmediateContext3 = npt_id3d11device3_default_GetImmediateContext3;
    dst->CreateDeferredContext3 = npt_id3d11device3_default_CreateDeferredContext3;
    dst->WriteToSubresource = npt_id3d11device3_default_WriteToSubresource;
    dst->ReadFromSubresource = npt_id3d11device3_default_ReadFromSubresource;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d11device3_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d11device3_client_vtbl npt_id3d11device3_default_vtbl_storage;

void
npt_id3d11device3_install_default_vtbl(void)
{
    npt_id3d11device3_fill_default_vtbl(&npt_id3d11device3_default_vtbl_storage);
}

void *
npt_id3d11device3_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d11device3_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D11Device4                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d11device4_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D11Device,
    &NPT_IID_ID3D11Device1,
    &NPT_IID_ID3D11Device2,
    &NPT_IID_ID3D11Device3,
    &NPT_IID_ID3D11Device4,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device4_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d11device4_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device4_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device4_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device4_default_RegisterDeviceRemovedEvent(void *self, HANDLE hEvent, DWORD * pdwCookie)
{
    HRESULT _ret = npt_call_ID3D11Device4_RegisterDeviceRemovedEvent(npt_com_self_ring(self),npt_com_self_id(self),hEvent,pdwCookie);
    return _ret;
}

void NPT_STDMETHODCALLTYPE
npt_id3d11device4_default_UnregisterDeviceRemoved(void *self, DWORD dwCookie)
{
    npt_async_ID3D11Device4_UnregisterDeviceRemoved(npt_com_self_ring(self),npt_com_self_id(self),dwCookie);
}


void
npt_id3d11device4_fill_default_vtbl(struct npt_id3d11device4_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d11device4_default_QueryInterface;
    dst->AddRef         = npt_id3d11device4_default_AddRef;
    dst->Release        = npt_id3d11device4_default_Release;
    dst->CreateBuffer = npt_id3d11device_default_CreateBuffer;
    dst->CreateTexture1D = npt_id3d11device_default_CreateTexture1D;
    dst->CreateTexture2D = npt_id3d11device_default_CreateTexture2D;
    dst->CreateTexture3D = npt_id3d11device_default_CreateTexture3D;
    dst->CreateShaderResourceView = npt_id3d11device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d11device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d11device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d11device_default_CreateDepthStencilView;
    dst->CreateInputLayout = npt_id3d11device_default_CreateInputLayout;
    dst->CreateVertexShader = npt_id3d11device_default_CreateVertexShader;
    dst->CreateGeometryShader = npt_id3d11device_default_CreateGeometryShader;
    dst->CreateGeometryShaderWithStreamOutput = npt_id3d11device_default_CreateGeometryShaderWithStreamOutput;
    dst->CreatePixelShader = npt_id3d11device_default_CreatePixelShader;
    dst->CreateHullShader = npt_id3d11device_default_CreateHullShader;
    dst->CreateDomainShader = npt_id3d11device_default_CreateDomainShader;
    dst->CreateComputeShader = npt_id3d11device_default_CreateComputeShader;
    dst->CreateClassLinkage = npt_id3d11device_default_CreateClassLinkage;
    dst->CreateBlendState = npt_id3d11device_default_CreateBlendState;
    dst->CreateDepthStencilState = npt_id3d11device_default_CreateDepthStencilState;
    dst->CreateRasterizerState = npt_id3d11device_default_CreateRasterizerState;
    dst->CreateSamplerState = npt_id3d11device_default_CreateSamplerState;
    dst->CreateQuery = npt_id3d11device_default_CreateQuery;
    dst->CreatePredicate = npt_id3d11device_default_CreatePredicate;
    dst->CreateCounter = npt_id3d11device_default_CreateCounter;
    dst->CreateDeferredContext = npt_id3d11device_default_CreateDeferredContext;
    dst->OpenSharedResource = npt_id3d11device_default_OpenSharedResource;
    dst->CheckFormatSupport = npt_id3d11device_default_CheckFormatSupport;
    dst->CheckMultisampleQualityLevels = npt_id3d11device_default_CheckMultisampleQualityLevels;
    dst->CheckCounterInfo = npt_id3d11device_default_CheckCounterInfo;
    dst->CheckCounter = npt_id3d11device_default_CheckCounter;
    dst->CheckFeatureSupport = npt_id3d11device_default_CheckFeatureSupport;
    dst->GetPrivateData = npt_id3d11device_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d11device_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d11device_default_SetPrivateDataInterface;
    dst->GetFeatureLevel = npt_id3d11device_default_GetFeatureLevel;
    dst->GetCreationFlags = npt_id3d11device_default_GetCreationFlags;
    dst->GetDeviceRemovedReason = npt_id3d11device_default_GetDeviceRemovedReason;
    dst->GetImmediateContext = npt_id3d11device_default_GetImmediateContext;
    dst->SetExceptionMode = npt_id3d11device_default_SetExceptionMode;
    dst->GetExceptionMode = npt_id3d11device_default_GetExceptionMode;
    dst->GetImmediateContext1 = npt_id3d11device1_default_GetImmediateContext1;
    dst->CreateDeferredContext1 = npt_id3d11device1_default_CreateDeferredContext1;
    dst->CreateBlendState1 = npt_id3d11device1_default_CreateBlendState1;
    dst->CreateRasterizerState1 = npt_id3d11device1_default_CreateRasterizerState1;
    dst->CreateDeviceContextState = npt_id3d11device1_default_CreateDeviceContextState;
    dst->OpenSharedResource1 = npt_id3d11device1_default_OpenSharedResource1;
    dst->OpenSharedResourceByName = npt_id3d11device1_default_OpenSharedResourceByName;
    dst->GetImmediateContext2 = npt_id3d11device2_default_GetImmediateContext2;
    dst->CreateDeferredContext2 = npt_id3d11device2_default_CreateDeferredContext2;
    dst->GetResourceTiling = npt_id3d11device2_default_GetResourceTiling;
    dst->CheckMultisampleQualityLevels1 = npt_id3d11device2_default_CheckMultisampleQualityLevels1;
    dst->CreateTexture2D1 = npt_id3d11device3_default_CreateTexture2D1;
    dst->CreateTexture3D1 = npt_id3d11device3_default_CreateTexture3D1;
    dst->CreateRasterizerState2 = npt_id3d11device3_default_CreateRasterizerState2;
    dst->CreateShaderResourceView1 = npt_id3d11device3_default_CreateShaderResourceView1;
    dst->CreateUnorderedAccessView1 = npt_id3d11device3_default_CreateUnorderedAccessView1;
    dst->CreateRenderTargetView1 = npt_id3d11device3_default_CreateRenderTargetView1;
    dst->CreateQuery1 = npt_id3d11device3_default_CreateQuery1;
    dst->GetImmediateContext3 = npt_id3d11device3_default_GetImmediateContext3;
    dst->CreateDeferredContext3 = npt_id3d11device3_default_CreateDeferredContext3;
    dst->WriteToSubresource = npt_id3d11device3_default_WriteToSubresource;
    dst->ReadFromSubresource = npt_id3d11device3_default_ReadFromSubresource;
    dst->RegisterDeviceRemovedEvent = npt_id3d11device4_default_RegisterDeviceRemovedEvent;
    dst->UnregisterDeviceRemoved = npt_id3d11device4_default_UnregisterDeviceRemoved;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d11device4_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d11device4_client_vtbl npt_id3d11device4_default_vtbl_storage;

void
npt_id3d11device4_install_default_vtbl(void)
{
    npt_id3d11device4_fill_default_vtbl(&npt_id3d11device4_default_vtbl_storage);
}

void *
npt_id3d11device4_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d11device4_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D11Device5                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d11device5_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D11Device,
    &NPT_IID_ID3D11Device1,
    &NPT_IID_ID3D11Device2,
    &NPT_IID_ID3D11Device3,
    &NPT_IID_ID3D11Device4,
    &NPT_IID_ID3D11Device5,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device5_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d11device5_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device5_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d11device5_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device5_default_OpenSharedFence(void *self, HANDLE hFence, const IID * ReturnedInterface, void ** ppFence)
{
    void *_raw_ppFence = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppFence = ppFence;
    if (ppFence)
        ppFence = (void **)&_raw_ppFence;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device5_OpenSharedFence(npt_com_self_ring(self),npt_com_self_id(self),hFence,ReturnedInterface,ppFence);
    } else {
        npt_async_ID3D11Device5_OpenSharedFence(npt_com_self_ring(self),npt_com_self_id(self),hFence,ReturnedInterface,ppFence);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppFence) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppFence)
            *_orig_ppFence = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                ReturnedInterface,
                (uint64_t)(uintptr_t)_raw_ppFence,
                (struct npt_com_base *)self);
        else
            *_orig_ppFence = NULL;
    }
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d11device5_default_CreateFence(void *self, UINT64 InitialValue, D3D11_FENCE_FLAG Flags, const IID * ReturnedInterface, void ** ppFence)
{
    void *_raw_ppFence = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppFence = ppFence;
    if (ppFence)
        ppFence = (void **)&_raw_ppFence;
    /* Runtime-conditional sync: with multi_ring_enabled the caller may
     * use the new handle on a different ring, so we must wait for the
     * host register.  With multi_ring off, all traffic is FIFO-ordered
     * on primary and async is race-free. */
    HRESULT _ret;
    if (npt_com_self_device(self)->multi_ring_enabled) {
        _ret = npt_call_ID3D11Device5_CreateFence(npt_com_self_ring(self),npt_com_self_id(self),InitialValue,Flags,ReturnedInterface,ppFence);
    } else {
        npt_async_ID3D11Device5_CreateFence(npt_com_self_ring(self),npt_com_self_id(self),InitialValue,Flags,ReturnedInterface,ppFence);
        _ret = (HRESULT)0;  /* deferred-fatal: assume S_OK */
    }
    if (_orig_ppFence) {
        /* Sync HRESULT: discard the stashed id on failure -- the host
         * won't have registered it, so any wrapper we build would be
         * bogus.  Under the deferred-fatal model an unregistered id
         * surfaces at first use, but returning the right HRESULT here
         * lets well-behaved callers short-circuit without an allocation. */
        if (NPT_SUCCEEDED((HRESULT)_ret) && _raw_ppFence)
            *_orig_ppFence = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                ReturnedInterface,
                (uint64_t)(uintptr_t)_raw_ppFence,
                (struct npt_com_base *)self);
        else
            *_orig_ppFence = NULL;
    }
    return _ret;
}


void
npt_id3d11device5_fill_default_vtbl(struct npt_id3d11device5_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d11device5_default_QueryInterface;
    dst->AddRef         = npt_id3d11device5_default_AddRef;
    dst->Release        = npt_id3d11device5_default_Release;
    dst->CreateBuffer = npt_id3d11device_default_CreateBuffer;
    dst->CreateTexture1D = npt_id3d11device_default_CreateTexture1D;
    dst->CreateTexture2D = npt_id3d11device_default_CreateTexture2D;
    dst->CreateTexture3D = npt_id3d11device_default_CreateTexture3D;
    dst->CreateShaderResourceView = npt_id3d11device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d11device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d11device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d11device_default_CreateDepthStencilView;
    dst->CreateInputLayout = npt_id3d11device_default_CreateInputLayout;
    dst->CreateVertexShader = npt_id3d11device_default_CreateVertexShader;
    dst->CreateGeometryShader = npt_id3d11device_default_CreateGeometryShader;
    dst->CreateGeometryShaderWithStreamOutput = npt_id3d11device_default_CreateGeometryShaderWithStreamOutput;
    dst->CreatePixelShader = npt_id3d11device_default_CreatePixelShader;
    dst->CreateHullShader = npt_id3d11device_default_CreateHullShader;
    dst->CreateDomainShader = npt_id3d11device_default_CreateDomainShader;
    dst->CreateComputeShader = npt_id3d11device_default_CreateComputeShader;
    dst->CreateClassLinkage = npt_id3d11device_default_CreateClassLinkage;
    dst->CreateBlendState = npt_id3d11device_default_CreateBlendState;
    dst->CreateDepthStencilState = npt_id3d11device_default_CreateDepthStencilState;
    dst->CreateRasterizerState = npt_id3d11device_default_CreateRasterizerState;
    dst->CreateSamplerState = npt_id3d11device_default_CreateSamplerState;
    dst->CreateQuery = npt_id3d11device_default_CreateQuery;
    dst->CreatePredicate = npt_id3d11device_default_CreatePredicate;
    dst->CreateCounter = npt_id3d11device_default_CreateCounter;
    dst->CreateDeferredContext = npt_id3d11device_default_CreateDeferredContext;
    dst->OpenSharedResource = npt_id3d11device_default_OpenSharedResource;
    dst->CheckFormatSupport = npt_id3d11device_default_CheckFormatSupport;
    dst->CheckMultisampleQualityLevels = npt_id3d11device_default_CheckMultisampleQualityLevels;
    dst->CheckCounterInfo = npt_id3d11device_default_CheckCounterInfo;
    dst->CheckCounter = npt_id3d11device_default_CheckCounter;
    dst->CheckFeatureSupport = npt_id3d11device_default_CheckFeatureSupport;
    dst->GetPrivateData = npt_id3d11device_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d11device_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d11device_default_SetPrivateDataInterface;
    dst->GetFeatureLevel = npt_id3d11device_default_GetFeatureLevel;
    dst->GetCreationFlags = npt_id3d11device_default_GetCreationFlags;
    dst->GetDeviceRemovedReason = npt_id3d11device_default_GetDeviceRemovedReason;
    dst->GetImmediateContext = npt_id3d11device_default_GetImmediateContext;
    dst->SetExceptionMode = npt_id3d11device_default_SetExceptionMode;
    dst->GetExceptionMode = npt_id3d11device_default_GetExceptionMode;
    dst->GetImmediateContext1 = npt_id3d11device1_default_GetImmediateContext1;
    dst->CreateDeferredContext1 = npt_id3d11device1_default_CreateDeferredContext1;
    dst->CreateBlendState1 = npt_id3d11device1_default_CreateBlendState1;
    dst->CreateRasterizerState1 = npt_id3d11device1_default_CreateRasterizerState1;
    dst->CreateDeviceContextState = npt_id3d11device1_default_CreateDeviceContextState;
    dst->OpenSharedResource1 = npt_id3d11device1_default_OpenSharedResource1;
    dst->OpenSharedResourceByName = npt_id3d11device1_default_OpenSharedResourceByName;
    dst->GetImmediateContext2 = npt_id3d11device2_default_GetImmediateContext2;
    dst->CreateDeferredContext2 = npt_id3d11device2_default_CreateDeferredContext2;
    dst->GetResourceTiling = npt_id3d11device2_default_GetResourceTiling;
    dst->CheckMultisampleQualityLevels1 = npt_id3d11device2_default_CheckMultisampleQualityLevels1;
    dst->CreateTexture2D1 = npt_id3d11device3_default_CreateTexture2D1;
    dst->CreateTexture3D1 = npt_id3d11device3_default_CreateTexture3D1;
    dst->CreateRasterizerState2 = npt_id3d11device3_default_CreateRasterizerState2;
    dst->CreateShaderResourceView1 = npt_id3d11device3_default_CreateShaderResourceView1;
    dst->CreateUnorderedAccessView1 = npt_id3d11device3_default_CreateUnorderedAccessView1;
    dst->CreateRenderTargetView1 = npt_id3d11device3_default_CreateRenderTargetView1;
    dst->CreateQuery1 = npt_id3d11device3_default_CreateQuery1;
    dst->GetImmediateContext3 = npt_id3d11device3_default_GetImmediateContext3;
    dst->CreateDeferredContext3 = npt_id3d11device3_default_CreateDeferredContext3;
    dst->WriteToSubresource = npt_id3d11device3_default_WriteToSubresource;
    dst->ReadFromSubresource = npt_id3d11device3_default_ReadFromSubresource;
    dst->RegisterDeviceRemovedEvent = npt_id3d11device4_default_RegisterDeviceRemovedEvent;
    dst->UnregisterDeviceRemoved = npt_id3d11device4_default_UnregisterDeviceRemoved;
    dst->OpenSharedFence = npt_id3d11device5_default_OpenSharedFence;
    dst->CreateFence = npt_id3d11device5_default_CreateFence;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d11device5_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d11device5_client_vtbl npt_id3d11device5_default_vtbl_storage;

void
npt_id3d11device5_install_default_vtbl(void)
{
    npt_id3d11device5_fill_default_vtbl(&npt_id3d11device5_default_vtbl_storage);
}

void *
npt_id3d11device5_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d11device5_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
