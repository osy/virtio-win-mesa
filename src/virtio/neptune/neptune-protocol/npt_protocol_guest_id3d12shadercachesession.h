/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12shadercachesession.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12SHADERCACHESESSION_H
#define NPT_PROTOCOL_GUEST_ID3D12SHADERCACHESESSION_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12ShaderCacheSession::FindValue
 */

struct npt_command_ID3D12ShaderCacheSession_FindValue {
    void *_self;
    const void * pKey;
    UINT KeySize;
    void * pValue;
    UINT * pValueSize;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12ShaderCacheSession_FindValue(const void * pKey,const UINT KeySize,const void * pValue,const UINT * pValueSize)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pKey) {
        cmd_size += npt_sizeof_array_count(KeySize);
        cmd_size += npt_sizeof_blob_array(pKey, KeySize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&KeySize, max_mode);
/* skip pValue (output only) */
    cmd_size += npt_sizeof_simple_pointer(pValueSize);
    if (pValueSize)
        cmd_size += npt_sizeof_UINT(pValueSize, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12ShaderCacheSession_FindValue(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const void * pKey,
                                              UINT KeySize,
                                              void * pValue,
                                              UINT * pValueSize)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12ShaderCacheSession_FindValue(pKey,KeySize,pValue,pValueSize);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                 NPT_METHOD_ID3D12ShaderCacheSession_FindValue),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pKey) {
        npt_encode_array_count(enc, KeySize);
        npt_encode_blob_array(enc, pKey, KeySize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &KeySize);
/* skip pValue (output only) */
    if (npt_encode_simple_pointer(enc, pValueSize))
        npt_encode_UINT(enc, pValueSize);
}

static inline size_t
npt_sizeof_ID3D12ShaderCacheSession_FindValue_reply(const void * pKey,const UINT KeySize,const void * pValue,const UINT * pValueSize)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pKey (input only) */
/* skip KeySize (input only) */
    if (pValue) {
        cmd_size += npt_sizeof_array_count(*pValueSize);
        cmd_size += npt_sizeof_blob_array(pValue, *pValueSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12ShaderCacheSession_FindValue_reply(struct npt_cs_decoder *dec,
                                                    void * pValue,
                                                    UINT * pValueSize,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                                  NPT_METHOD_ID3D12ShaderCacheSession_FindValue))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12ShaderCacheSession_FindValue reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                       NPT_METHOD_ID3D12ShaderCacheSession_FindValue));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pValue)
            npt_decode_blob_array(dec, (void *)pValue, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pValueSize)
            npt_decode_UINT(dec, (UINT *)pValueSize);
    }
}

static inline void
npt_submit_ID3D12ShaderCacheSession_FindValue(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const void * pKey,
                                              UINT KeySize,
                                              void * pValue,
                                              UINT * pValueSize,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12ShaderCacheSession_FindValue(pKey,KeySize,pValue,pValueSize);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12ShaderCacheSession_FindValue_reply(pKey,KeySize,pValue,pValueSize)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12ShaderCacheSession_FindValue(enc, cmd_flags, object_id, pKey, KeySize, pValue, pValueSize);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12ShaderCacheSession_FindValue(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const void * pKey,
                                             UINT KeySize,
                                             void * pValue,
                                             UINT * pValueSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12ShaderCacheSession_FindValue(ring, 0, object_id, pKey, KeySize, pValue, pValueSize, &submit);
}

static inline HRESULT
npt_call_ID3D12ShaderCacheSession_FindValue(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const void * pKey,
                                            UINT KeySize,
                                            void * pValue,
                                            UINT * pValueSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12ShaderCacheSession_FindValue(ring, NPT_CMD_FLAG_REPLY, object_id, pKey, KeySize, pValue, pValueSize, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12ShaderCacheSession_FindValue_reply(dec, pValue, pValueSize, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12ShaderCacheSession::StoreValue
 */

struct npt_command_ID3D12ShaderCacheSession_StoreValue {
    void *_self;
    const void * pKey;
    UINT KeySize;
    const void * pValue;
    UINT ValueSize;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12ShaderCacheSession_StoreValue(const void * pKey,const UINT KeySize,const void * pValue,const UINT ValueSize)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pKey) {
        cmd_size += npt_sizeof_array_count(KeySize);
        cmd_size += npt_sizeof_blob_array(pKey, KeySize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&KeySize, max_mode);
    if (pValue) {
        cmd_size += npt_sizeof_array_count(ValueSize);
        cmd_size += npt_sizeof_blob_array(pValue, ValueSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&ValueSize, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12ShaderCacheSession_StoreValue(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const void * pKey,
                                               UINT KeySize,
                                               const void * pValue,
                                               UINT ValueSize)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12ShaderCacheSession_StoreValue(pKey,KeySize,pValue,ValueSize);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                 NPT_METHOD_ID3D12ShaderCacheSession_StoreValue),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pKey) {
        npt_encode_array_count(enc, KeySize);
        npt_encode_blob_array(enc, pKey, KeySize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &KeySize);
    if (pValue) {
        npt_encode_array_count(enc, ValueSize);
        npt_encode_blob_array(enc, pValue, ValueSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &ValueSize);
}

static inline size_t
npt_sizeof_ID3D12ShaderCacheSession_StoreValue_reply(const void * pKey,const UINT KeySize,const void * pValue,const UINT ValueSize)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pKey (input only) */
/* skip KeySize (input only) */
/* skip pValue (input only) */
/* skip ValueSize (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12ShaderCacheSession_StoreValue_reply(struct npt_cs_decoder *dec,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                                  NPT_METHOD_ID3D12ShaderCacheSession_StoreValue))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12ShaderCacheSession_StoreValue reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                       NPT_METHOD_ID3D12ShaderCacheSession_StoreValue));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12ShaderCacheSession_StoreValue(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               const void * pKey,
                                               UINT KeySize,
                                               const void * pValue,
                                               UINT ValueSize,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12ShaderCacheSession_StoreValue(pKey,KeySize,pValue,ValueSize);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12ShaderCacheSession_StoreValue_reply(pKey,KeySize,pValue,ValueSize)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12ShaderCacheSession_StoreValue(enc, cmd_flags, object_id, pKey, KeySize, pValue, ValueSize);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12ShaderCacheSession_StoreValue(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const void * pKey,
                                              UINT KeySize,
                                              const void * pValue,
                                              UINT ValueSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12ShaderCacheSession_StoreValue(ring, 0, object_id, pKey, KeySize, pValue, ValueSize, &submit);
}

static inline HRESULT
npt_call_ID3D12ShaderCacheSession_StoreValue(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const void * pKey,
                                             UINT KeySize,
                                             const void * pValue,
                                             UINT ValueSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12ShaderCacheSession_StoreValue(ring, NPT_CMD_FLAG_REPLY, object_id, pKey, KeySize, pValue, ValueSize, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12ShaderCacheSession_StoreValue_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12ShaderCacheSession::SetDeleteOnDestroy
 */

struct npt_command_ID3D12ShaderCacheSession_SetDeleteOnDestroy {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D12ShaderCacheSession_SetDeleteOnDestroy(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12ShaderCacheSession_SetDeleteOnDestroy(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12ShaderCacheSession_SetDeleteOnDestroy();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                 NPT_METHOD_ID3D12ShaderCacheSession_SetDeleteOnDestroy),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12ShaderCacheSession_SetDeleteOnDestroy_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D12ShaderCacheSession_SetDeleteOnDestroy(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12ShaderCacheSession_SetDeleteOnDestroy();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12ShaderCacheSession_SetDeleteOnDestroy_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12ShaderCacheSession_SetDeleteOnDestroy(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12ShaderCacheSession_SetDeleteOnDestroy(struct npt_ring *ring,
                                                      npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12ShaderCacheSession_SetDeleteOnDestroy(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D12ShaderCacheSession_SetDeleteOnDestroy(struct npt_ring *ring,
                                                     npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12ShaderCacheSession_SetDeleteOnDestroy(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12ShaderCacheSession::GetDesc
 */

struct npt_command_ID3D12ShaderCacheSession_GetDesc {
    void *_self;
    D3D12_SHADER_CACHE_SESSION_DESC ret;
};

static inline size_t
npt_sizeof_ID3D12ShaderCacheSession_GetDesc(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12ShaderCacheSession_GetDesc(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12ShaderCacheSession_GetDesc();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                 NPT_METHOD_ID3D12ShaderCacheSession_GetDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12ShaderCacheSession_GetDesc_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_SHADER_CACHE_SESSION_DESC(&(const D3D12_SHADER_CACHE_SESSION_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12ShaderCacheSession_GetDesc_reply(struct npt_cs_decoder *dec,
                                                  D3D12_SHADER_CACHE_SESSION_DESC *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                                  NPT_METHOD_ID3D12ShaderCacheSession_GetDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12ShaderCacheSession_GetDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12ShaderCacheSession,
                                       NPT_METHOD_ID3D12ShaderCacheSession_GetDesc));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_SHADER_CACHE_SESSION_DESC(dec, ret);
}

static inline void
npt_submit_ID3D12ShaderCacheSession_GetDesc(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12ShaderCacheSession_GetDesc();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12ShaderCacheSession_GetDesc_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12ShaderCacheSession_GetDesc(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12ShaderCacheSession_GetDesc(struct npt_ring *ring,
                                           npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12ShaderCacheSession_GetDesc(ring, 0, object_id, &submit);
}

static inline D3D12_SHADER_CACHE_SESSION_DESC
npt_call_ID3D12ShaderCacheSession_GetDesc(struct npt_ring *ring,
                                          npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12ShaderCacheSession_GetDesc(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_SHADER_CACHE_SESSION_DESC _ret;
        npt_decode_ID3D12ShaderCacheSession_GetDesc_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_SHADER_CACHE_SESSION_DESC){0};
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12SHADERCACHESESSION_H */
