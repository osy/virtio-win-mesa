# Vendored Neptune protocol headers, generated by neptune-protocol's
# codegen.  Keep them as LF on every platform so the bytes in the working
# tree match what the upstream generator emits (and what its tests run
# against), and so MSVC's /utf-8 preprocessor doesn't choke on
# `\<CR><LF>` macro-continuation lines.
* text eol=lf
