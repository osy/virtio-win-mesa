/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for idxgiobject: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_idxgiobject.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* IDXGIObject                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_idxgiobject_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_IDXGIObject,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiobject_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_idxgiobject_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_idxgiobject_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_idxgiobject_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiobject_default_SetPrivateData(void *self, const GUID * Name, UINT DataSize, const void * pData)
{
    npt_async_IDXGIObject_SetPrivateData(npt_com_self_ring(self),npt_com_self_id(self),Name,DataSize,pData);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiobject_default_SetPrivateDataInterface(void *self, const GUID * Name, const IUnknown * pUnknown)
{
    npt_async_IDXGIObject_SetPrivateDataInterface(npt_com_self_ring(self),npt_com_self_id(self),Name,pUnknown);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiobject_default_GetPrivateData(void *self, const GUID * Name, UINT * pDataSize, void * pData)
{
    HRESULT _ret = npt_call_IDXGIObject_GetPrivateData(npt_com_self_ring(self),npt_com_self_id(self),Name,pDataSize,pData);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiobject_default_GetParent(void *self, const IID * riid, void ** ppParent)
{
    void *_raw_ppParent = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppParent = ppParent;
    if (ppParent)
        ppParent = (void **)&_raw_ppParent;
    npt_async_IDXGIObject_GetParent(npt_com_self_ring(self),npt_com_self_id(self),riid,ppParent);
    if (_orig_ppParent) {
        if (_raw_ppParent)
            *_orig_ppParent = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppParent,
                (struct npt_com_base *)self);
        else
            *_orig_ppParent = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_idxgiobject_fill_default_vtbl(struct npt_idxgiobject_client_vtbl *dst)
{
    dst->QueryInterface = npt_idxgiobject_default_QueryInterface;
    dst->AddRef         = npt_idxgiobject_default_AddRef;
    dst->Release        = npt_idxgiobject_default_Release;
    dst->SetPrivateData = npt_idxgiobject_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_idxgiobject_default_SetPrivateDataInterface;
    dst->GetPrivateData = npt_idxgiobject_default_GetPrivateData;
    dst->GetParent = npt_idxgiobject_default_GetParent;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_idxgiobject_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_idxgiobject_client_vtbl npt_idxgiobject_default_vtbl_storage;

void
npt_idxgiobject_install_default_vtbl(void)
{
    npt_idxgiobject_fill_default_vtbl(&npt_idxgiobject_default_vtbl_storage);
}

void *
npt_idxgiobject_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_idxgiobject_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
