/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12swapchainassistant.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12SWAPCHAINASSISTANT_H
#define NPT_PROTOCOL_GUEST_ID3D12SWAPCHAINASSISTANT_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12SwapChainAssistant::GetLUID
 */

struct npt_command_ID3D12SwapChainAssistant_GetLUID {
    void *_self;
    LUID ret;
};

static inline size_t
npt_sizeof_ID3D12SwapChainAssistant_GetLUID(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12SwapChainAssistant_GetLUID(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12SwapChainAssistant_GetLUID();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                 NPT_METHOD_ID3D12SwapChainAssistant_GetLUID),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12SwapChainAssistant_GetLUID_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_LUID(&(const LUID){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12SwapChainAssistant_GetLUID_reply(struct npt_cs_decoder *dec,
                                                  LUID *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                                  NPT_METHOD_ID3D12SwapChainAssistant_GetLUID))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12SwapChainAssistant_GetLUID reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                       NPT_METHOD_ID3D12SwapChainAssistant_GetLUID));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_LUID(dec, ret);
}

static inline void
npt_submit_ID3D12SwapChainAssistant_GetLUID(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12SwapChainAssistant_GetLUID();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12SwapChainAssistant_GetLUID_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12SwapChainAssistant_GetLUID(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12SwapChainAssistant_GetLUID(struct npt_ring *ring,
                                           npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SwapChainAssistant_GetLUID(ring, 0, object_id, &submit);
}

static inline LUID
npt_call_ID3D12SwapChainAssistant_GetLUID(struct npt_ring *ring,
                                          npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SwapChainAssistant_GetLUID(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        LUID _ret;
        npt_decode_ID3D12SwapChainAssistant_GetLUID_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (LUID){0};
    }
}

/*
 * ID3D12SwapChainAssistant::GetSwapChainObject
 */

struct npt_command_ID3D12SwapChainAssistant_GetSwapChainObject {
    void *_self;
    const IID * riid;
    void ** ppv;
    /* Shadow: the guest-allocated id for ppv arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppv -> *ppv} in the
     * context object table. */
    uint64_t _guest_id_ppv;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12SwapChainAssistant_GetSwapChainObject(const IID * riid,void ** ppv)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppv: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12SwapChainAssistant_GetSwapChainObject(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       const IID * riid,
                                                       void ** ppv)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12SwapChainAssistant_GetSwapChainObject(riid,ppv);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                 NPT_METHOD_ID3D12SwapChainAssistant_GetSwapChainObject),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppv = 0;
    if (ppv) {
        _gid_ppv = npt_com_allocate_next_id();
        *ppv = (void *)(uintptr_t)_gid_ppv;
    }
    npt_encode_uint64_t(enc, &_gid_ppv);
}
}

static inline size_t
npt_sizeof_ID3D12SwapChainAssistant_GetSwapChainObject_reply(const IID * riid,void ** ppv)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip riid (input only) */
/* skip ppv (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12SwapChainAssistant_GetSwapChainObject_reply(struct npt_cs_decoder *dec,
                                                             void ** ppv,
                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                                  NPT_METHOD_ID3D12SwapChainAssistant_GetSwapChainObject))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12SwapChainAssistant_GetSwapChainObject reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                       NPT_METHOD_ID3D12SwapChainAssistant_GetSwapChainObject));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppv (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12SwapChainAssistant_GetSwapChainObject(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       const IID * riid,
                                                       void ** ppv,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12SwapChainAssistant_GetSwapChainObject(riid,ppv);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12SwapChainAssistant_GetSwapChainObject_reply(riid,ppv)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12SwapChainAssistant_GetSwapChainObject(enc, cmd_flags, object_id, riid, ppv);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12SwapChainAssistant_GetSwapChainObject(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      const IID * riid,
                                                      void ** ppv)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SwapChainAssistant_GetSwapChainObject(ring, 0, object_id, riid, ppv, &submit);
}

static inline HRESULT
npt_call_ID3D12SwapChainAssistant_GetSwapChainObject(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     const IID * riid,
                                                     void ** ppv)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SwapChainAssistant_GetSwapChainObject(ring, NPT_CMD_FLAG_REPLY, object_id, riid, ppv, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12SwapChainAssistant_GetSwapChainObject_reply(dec, ppv, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12SwapChainAssistant::GetCurrentResourceAndCommandQueue
 */

struct npt_command_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue {
    void *_self;
    const IID * riidResource;
    void ** ppvResource;
    const IID * riidQueue;
    void ** ppvQueue;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    /* Shadow: the guest-allocated id for ppvQueue arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvQueue -> *ppvQueue} in the
     * context object table. */
    uint64_t _guest_id_ppvQueue;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(const IID * riidResource,void ** ppvResource,const IID * riidQueue,void ** ppvQueue)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(riidResource);
    if (riidResource)
        cmd_size += npt_sizeof_IID(riidResource, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvResource: guest id */
    cmd_size += npt_sizeof_simple_pointer(riidQueue);
    if (riidQueue)
        cmd_size += npt_sizeof_IID(riidQueue, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvQueue: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(struct npt_cs_encoder *enc,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      const IID * riidResource,
                                                                      void ** ppvResource,
                                                                      const IID * riidQueue,
                                                                      void ** ppvQueue)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(riidResource,ppvResource,riidQueue,ppvQueue);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                 NPT_METHOD_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, riidResource))
        npt_encode_IID(enc, riidResource);
{
    uint64_t _gid_ppvResource = 0;
    if (ppvResource) {
        _gid_ppvResource = npt_com_allocate_next_id();
        *ppvResource = (void *)(uintptr_t)_gid_ppvResource;
    }
    npt_encode_uint64_t(enc, &_gid_ppvResource);
}
    if (npt_encode_simple_pointer(enc, riidQueue))
        npt_encode_IID(enc, riidQueue);
{
    uint64_t _gid_ppvQueue = 0;
    if (ppvQueue) {
        _gid_ppvQueue = npt_com_allocate_next_id();
        *ppvQueue = (void *)(uintptr_t)_gid_ppvQueue;
    }
    npt_encode_uint64_t(enc, &_gid_ppvQueue);
}
}

static inline size_t
npt_sizeof_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_reply(const IID * riidResource,void ** ppvResource,const IID * riidQueue,void ** ppvQueue)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip riidResource (input only) */
/* skip ppvResource (guest-allocated id, registered host-side; not in reply) */
/* skip riidQueue (input only) */
/* skip ppvQueue (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_reply(struct npt_cs_decoder *dec,
                                                                            void ** ppvResource,
                                                                            void ** ppvQueue,
                                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                                  NPT_METHOD_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                       NPT_METHOD_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvResource (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppvQueue (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(struct npt_ring *ring,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      const IID * riidResource,
                                                                      void ** ppvResource,
                                                                      const IID * riidQueue,
                                                                      void ** ppvQueue,
                                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(riidResource,ppvResource,riidQueue,ppvQueue);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_reply(riidResource,ppvResource,riidQueue,ppvQueue)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(enc, cmd_flags, object_id, riidResource, ppvResource, riidQueue, ppvQueue);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     const IID * riidResource,
                                                                     void ** ppvResource,
                                                                     const IID * riidQueue,
                                                                     void ** ppvQueue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(ring, 0, object_id, riidResource, ppvResource, riidQueue, ppvQueue, &submit);
}

static inline HRESULT
npt_call_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    const IID * riidResource,
                                                                    void ** ppvResource,
                                                                    const IID * riidQueue,
                                                                    void ** ppvQueue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(ring, NPT_CMD_FLAG_REPLY, object_id, riidResource, ppvResource, riidQueue, ppvQueue, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_reply(dec, ppvResource, ppvQueue, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12SwapChainAssistant::InsertImplicitSync
 */

struct npt_command_ID3D12SwapChainAssistant_InsertImplicitSync {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12SwapChainAssistant_InsertImplicitSync(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12SwapChainAssistant_InsertImplicitSync(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12SwapChainAssistant_InsertImplicitSync();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                 NPT_METHOD_ID3D12SwapChainAssistant_InsertImplicitSync),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12SwapChainAssistant_InsertImplicitSync_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D12SwapChainAssistant_InsertImplicitSync_reply(struct npt_cs_decoder *dec,
                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                                  NPT_METHOD_ID3D12SwapChainAssistant_InsertImplicitSync))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12SwapChainAssistant_InsertImplicitSync reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                       NPT_METHOD_ID3D12SwapChainAssistant_InsertImplicitSync));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12SwapChainAssistant_InsertImplicitSync(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12SwapChainAssistant_InsertImplicitSync();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12SwapChainAssistant_InsertImplicitSync_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12SwapChainAssistant_InsertImplicitSync(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12SwapChainAssistant_InsertImplicitSync(struct npt_ring *ring,
                                                      npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SwapChainAssistant_InsertImplicitSync(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_ID3D12SwapChainAssistant_InsertImplicitSync(struct npt_ring *ring,
                                                     npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SwapChainAssistant_InsertImplicitSync(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12SwapChainAssistant_InsertImplicitSync_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12SWAPCHAINASSISTANT_H */
