/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgiadapter.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIADAPTER_H
#define NPT_PROTOCOL_GUEST_IDXGIADAPTER_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIAdapter::EnumOutputs
 */

struct npt_command_IDXGIAdapter_EnumOutputs {
    void *_self;
    UINT Output;
    IDXGIOutput ** ppOutput;
    /* Shadow: the guest-allocated id for ppOutput arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppOutput -> *ppOutput} in the
     * context object table. */
    uint64_t _guest_id_ppOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter_EnumOutputs(const UINT Output,IDXGIOutput ** ppOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Output, max_mode);
cmd_size += sizeof(uint64_t);  /* ppOutput: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter_EnumOutputs(struct npt_cs_encoder *enc,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT Output,
                                    IDXGIOutput ** ppOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter_EnumOutputs(Output,ppOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                 NPT_METHOD_IDXGIAdapter_EnumOutputs),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Output);
{
    uint64_t _gid_ppOutput = 0;
    if (ppOutput) {
        _gid_ppOutput = npt_com_allocate_next_id();
        *ppOutput = (IDXGIOutput *)(uintptr_t)_gid_ppOutput;
    }
    npt_encode_uint64_t(enc, &_gid_ppOutput);
}
}

static inline size_t
npt_sizeof_IDXGIAdapter_EnumOutputs_reply(const UINT Output,IDXGIOutput ** ppOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Output (input only) */
/* skip ppOutput (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter_EnumOutputs_reply(struct npt_cs_decoder *dec,
                                          IDXGIOutput ** ppOutput,
                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                                  NPT_METHOD_IDXGIAdapter_EnumOutputs))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter_EnumOutputs reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                       NPT_METHOD_IDXGIAdapter_EnumOutputs));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppOutput (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIAdapter_EnumOutputs(struct npt_ring *ring,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    UINT Output,
                                    IDXGIOutput ** ppOutput,
                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter_EnumOutputs(Output,ppOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter_EnumOutputs_reply(Output,ppOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter_EnumOutputs(enc, cmd_flags, object_id, Output, ppOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter_EnumOutputs(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   UINT Output,
                                   IDXGIOutput ** ppOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter_EnumOutputs(ring, 0, object_id, Output, ppOutput, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter_EnumOutputs(struct npt_ring *ring,
                                  npt_object_id object_id,
                                  UINT Output,
                                  IDXGIOutput ** ppOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter_EnumOutputs(ring, NPT_CMD_FLAG_REPLY, object_id, Output, ppOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter_EnumOutputs_reply(dec, ppOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIAdapter::GetDesc
 */

struct npt_command_IDXGIAdapter_GetDesc {
    void *_self;
    DXGI_ADAPTER_DESC * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter_GetDesc(const DXGI_ADAPTER_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter_GetDesc(struct npt_cs_encoder *enc,
                                uint32_t cmd_flags,
                                npt_object_id object_id,
                                DXGI_ADAPTER_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter_GetDesc(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                 NPT_METHOD_IDXGIAdapter_GetDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGIAdapter_GetDesc_reply(const DXGI_ADAPTER_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_ADAPTER_DESC(&(const DXGI_ADAPTER_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter_GetDesc_reply(struct npt_cs_decoder *dec,
                                      DXGI_ADAPTER_DESC * pDesc,
                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                                  NPT_METHOD_IDXGIAdapter_GetDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter_GetDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                       NPT_METHOD_IDXGIAdapter_GetDesc));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_ADAPTER_DESC(dec, (DXGI_ADAPTER_DESC *)pDesc);
    }
}

static inline void
npt_submit_IDXGIAdapter_GetDesc(struct npt_ring *ring,
                                uint32_t cmd_flags,
                                npt_object_id object_id,
                                DXGI_ADAPTER_DESC * pDesc,
                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter_GetDesc(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter_GetDesc_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter_GetDesc(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter_GetDesc(struct npt_ring *ring,
                               npt_object_id object_id,
                               DXGI_ADAPTER_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter_GetDesc(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter_GetDesc(struct npt_ring *ring,
                              npt_object_id object_id,
                              DXGI_ADAPTER_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter_GetDesc(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter_GetDesc_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIAdapter::CheckInterfaceSupport
 */

struct npt_command_IDXGIAdapter_CheckInterfaceSupport {
    void *_self;
    const GUID * InterfaceName;
    LARGE_INTEGER * pUMDVersion;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter_CheckInterfaceSupport(const GUID * InterfaceName,const LARGE_INTEGER * pUMDVersion)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(InterfaceName);
    if (InterfaceName)
        cmd_size += npt_sizeof_GUID(InterfaceName, max_mode);
/* skip pUMDVersion (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter_CheckInterfaceSupport(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const GUID * InterfaceName,
                                              LARGE_INTEGER * pUMDVersion)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter_CheckInterfaceSupport(InterfaceName,pUMDVersion);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                 NPT_METHOD_IDXGIAdapter_CheckInterfaceSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, InterfaceName))
        npt_encode_GUID(enc, InterfaceName);
/* skip pUMDVersion (output only) */
}

static inline size_t
npt_sizeof_IDXGIAdapter_CheckInterfaceSupport_reply(const GUID * InterfaceName,const LARGE_INTEGER * pUMDVersion)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip InterfaceName (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_LARGE_INTEGER(&(const LARGE_INTEGER){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter_CheckInterfaceSupport_reply(struct npt_cs_decoder *dec,
                                                    LARGE_INTEGER * pUMDVersion,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                                  NPT_METHOD_IDXGIAdapter_CheckInterfaceSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter_CheckInterfaceSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                       NPT_METHOD_IDXGIAdapter_CheckInterfaceSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pUMDVersion)
            npt_decode_LARGE_INTEGER(dec, (LARGE_INTEGER *)pUMDVersion);
    }
}

static inline void
npt_submit_IDXGIAdapter_CheckInterfaceSupport(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              const GUID * InterfaceName,
                                              LARGE_INTEGER * pUMDVersion,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter_CheckInterfaceSupport(InterfaceName,pUMDVersion);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter_CheckInterfaceSupport_reply(InterfaceName,pUMDVersion)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter_CheckInterfaceSupport(enc, cmd_flags, object_id, InterfaceName, pUMDVersion);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter_CheckInterfaceSupport(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             const GUID * InterfaceName,
                                             LARGE_INTEGER * pUMDVersion)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter_CheckInterfaceSupport(ring, 0, object_id, InterfaceName, pUMDVersion, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter_CheckInterfaceSupport(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            const GUID * InterfaceName,
                                            LARGE_INTEGER * pUMDVersion)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter_CheckInterfaceSupport(ring, NPT_CMD_FLAG_REPLY, object_id, InterfaceName, pUMDVersion, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter_CheckInterfaceSupport_reply(dec, pUMDVersion, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIAdapter1::GetDesc1
 */

struct npt_command_IDXGIAdapter1_GetDesc1 {
    void *_self;
    DXGI_ADAPTER_DESC1 * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter1_GetDesc1(const DXGI_ADAPTER_DESC1 * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter1_GetDesc1(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_ADAPTER_DESC1 * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter1_GetDesc1(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter1,
                                 NPT_METHOD_IDXGIAdapter1_GetDesc1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGIAdapter1_GetDesc1_reply(const DXGI_ADAPTER_DESC1 * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_ADAPTER_DESC1(&(const DXGI_ADAPTER_DESC1){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter1_GetDesc1_reply(struct npt_cs_decoder *dec,
                                        DXGI_ADAPTER_DESC1 * pDesc,
                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter1,
                                                  NPT_METHOD_IDXGIAdapter1_GetDesc1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter1_GetDesc1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter1,
                                       NPT_METHOD_IDXGIAdapter1_GetDesc1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_ADAPTER_DESC1(dec, (DXGI_ADAPTER_DESC1 *)pDesc);
    }
}

static inline void
npt_submit_IDXGIAdapter1_GetDesc1(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_ADAPTER_DESC1 * pDesc,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter1_GetDesc1(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter1_GetDesc1_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter1_GetDesc1(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter1_GetDesc1(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 DXGI_ADAPTER_DESC1 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter1_GetDesc1(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter1_GetDesc1(struct npt_ring *ring,
                                npt_object_id object_id,
                                DXGI_ADAPTER_DESC1 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter1_GetDesc1(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter1_GetDesc1_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIAdapter2::GetDesc2
 */

struct npt_command_IDXGIAdapter2_GetDesc2 {
    void *_self;
    DXGI_ADAPTER_DESC2 * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter2_GetDesc2(const DXGI_ADAPTER_DESC2 * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter2_GetDesc2(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_ADAPTER_DESC2 * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter2_GetDesc2(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter2,
                                 NPT_METHOD_IDXGIAdapter2_GetDesc2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGIAdapter2_GetDesc2_reply(const DXGI_ADAPTER_DESC2 * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_ADAPTER_DESC2(&(const DXGI_ADAPTER_DESC2){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter2_GetDesc2_reply(struct npt_cs_decoder *dec,
                                        DXGI_ADAPTER_DESC2 * pDesc,
                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter2,
                                                  NPT_METHOD_IDXGIAdapter2_GetDesc2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter2_GetDesc2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter2,
                                       NPT_METHOD_IDXGIAdapter2_GetDesc2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_ADAPTER_DESC2(dec, (DXGI_ADAPTER_DESC2 *)pDesc);
    }
}

static inline void
npt_submit_IDXGIAdapter2_GetDesc2(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_ADAPTER_DESC2 * pDesc,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter2_GetDesc2(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter2_GetDesc2_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter2_GetDesc2(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter2_GetDesc2(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 DXGI_ADAPTER_DESC2 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter2_GetDesc2(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter2_GetDesc2(struct npt_ring *ring,
                                npt_object_id object_id,
                                DXGI_ADAPTER_DESC2 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter2_GetDesc2(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter2_GetDesc2_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIAdapter3::RegisterHardwareContentProtectionTeardownStatusEvent
 */

struct npt_command_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pdwCookie (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(struct npt_cs_encoder *enc,
                                                                              uint32_t cmd_flags,
                                                                              npt_object_id object_id,
                                                                              HANDLE hEvent,
                                                                              DWORD * pdwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(hEvent,pdwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
/* skip pdwCookie (output only) */
}

static inline size_t
npt_sizeof_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_reply(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hEvent (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DWORD(&(const DWORD){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_reply(struct npt_cs_decoder *dec,
                                                                                    DWORD * pdwCookie,
                                                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                                  NPT_METHOD_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                       NPT_METHOD_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pdwCookie)
            npt_decode_DWORD(dec, (DWORD *)pdwCookie);
    }
}

static inline void
npt_submit_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(struct npt_ring *ring,
                                                                              uint32_t cmd_flags,
                                                                              npt_object_id object_id,
                                                                              HANDLE hEvent,
                                                                              DWORD * pdwCookie,
                                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(hEvent,pdwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_reply(hEvent,pdwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(enc, cmd_flags, object_id, hEvent, pdwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(struct npt_ring *ring,
                                                                             npt_object_id object_id,
                                                                             HANDLE hEvent,
                                                                             DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(ring, 0, object_id, hEvent, pdwCookie, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(struct npt_ring *ring,
                                                                            npt_object_id object_id,
                                                                            HANDLE hEvent,
                                                                            DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(ring, NPT_CMD_FLAG_REPLY, object_id, hEvent, pdwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_reply(dec, pdwCookie, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIAdapter3::UnregisterHardwareContentProtectionTeardownStatus
 */

struct npt_command_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus {
    void *_self;
    DWORD dwCookie;
};

static inline size_t
npt_sizeof_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(const DWORD dwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DWORD(&dwCookie, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(struct npt_cs_encoder *enc,
                                                                           uint32_t cmd_flags,
                                                                           npt_object_id object_id,
                                                                           DWORD dwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(dwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DWORD(enc, &dwCookie);
}

static inline size_t
npt_sizeof_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus_reply(const DWORD dwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip dwCookie (input only) */
    return cmd_size;
}

static inline void
npt_submit_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(struct npt_ring *ring,
                                                                           uint32_t cmd_flags,
                                                                           npt_object_id object_id,
                                                                           DWORD dwCookie,
                                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(dwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus_reply(dwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(enc, cmd_flags, object_id, dwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(struct npt_ring *ring,
                                                                          npt_object_id object_id,
                                                                          DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(ring, 0, object_id, dwCookie, &submit);
}

static inline void
npt_call_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(struct npt_ring *ring,
                                                                         npt_object_id object_id,
                                                                         DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(ring, NPT_CMD_FLAG_REPLY, object_id, dwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * IDXGIAdapter3::QueryVideoMemoryInfo
 */

struct npt_command_IDXGIAdapter3_QueryVideoMemoryInfo {
    void *_self;
    UINT NodeIndex;
    DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup;
    DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter3_QueryVideoMemoryInfo(const UINT NodeIndex,const DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,const DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NodeIndex, max_mode);
    cmd_size += npt_sizeof_DXGI_MEMORY_SEGMENT_GROUP(&MemorySegmentGroup, max_mode);
/* skip pVideoMemoryInfo (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter3_QueryVideoMemoryInfo(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT NodeIndex,
                                              DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,
                                              DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter3_QueryVideoMemoryInfo(NodeIndex,MemorySegmentGroup,pVideoMemoryInfo);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_QueryVideoMemoryInfo),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NodeIndex);
    npt_encode_DXGI_MEMORY_SEGMENT_GROUP(enc, &MemorySegmentGroup);
/* skip pVideoMemoryInfo (output only) */
}

static inline size_t
npt_sizeof_IDXGIAdapter3_QueryVideoMemoryInfo_reply(const UINT NodeIndex,const DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,const DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NodeIndex (input only) */
/* skip MemorySegmentGroup (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_QUERY_VIDEO_MEMORY_INFO(&(const DXGI_QUERY_VIDEO_MEMORY_INFO){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter3_QueryVideoMemoryInfo_reply(struct npt_cs_decoder *dec,
                                                    DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                                  NPT_METHOD_IDXGIAdapter3_QueryVideoMemoryInfo))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter3_QueryVideoMemoryInfo reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                       NPT_METHOD_IDXGIAdapter3_QueryVideoMemoryInfo));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pVideoMemoryInfo)
            npt_decode_DXGI_QUERY_VIDEO_MEMORY_INFO(dec, (DXGI_QUERY_VIDEO_MEMORY_INFO *)pVideoMemoryInfo);
    }
}

static inline void
npt_submit_IDXGIAdapter3_QueryVideoMemoryInfo(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT NodeIndex,
                                              DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,
                                              DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter3_QueryVideoMemoryInfo(NodeIndex,MemorySegmentGroup,pVideoMemoryInfo);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter3_QueryVideoMemoryInfo_reply(NodeIndex,MemorySegmentGroup,pVideoMemoryInfo)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter3_QueryVideoMemoryInfo(enc, cmd_flags, object_id, NodeIndex, MemorySegmentGroup, pVideoMemoryInfo);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter3_QueryVideoMemoryInfo(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT NodeIndex,
                                             DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,
                                             DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_QueryVideoMemoryInfo(ring, 0, object_id, NodeIndex, MemorySegmentGroup, pVideoMemoryInfo, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter3_QueryVideoMemoryInfo(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT NodeIndex,
                                            DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,
                                            DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_QueryVideoMemoryInfo(ring, NPT_CMD_FLAG_REPLY, object_id, NodeIndex, MemorySegmentGroup, pVideoMemoryInfo, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter3_QueryVideoMemoryInfo_reply(dec, pVideoMemoryInfo, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIAdapter3::SetVideoMemoryReservation
 */

struct npt_command_IDXGIAdapter3_SetVideoMemoryReservation {
    void *_self;
    UINT NodeIndex;
    DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup;
    UINT64 Reservation;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter3_SetVideoMemoryReservation(const UINT NodeIndex,const DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,const UINT64 Reservation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NodeIndex, max_mode);
    cmd_size += npt_sizeof_DXGI_MEMORY_SEGMENT_GROUP(&MemorySegmentGroup, max_mode);
    cmd_size += npt_sizeof_UINT64(&Reservation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter3_SetVideoMemoryReservation(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT NodeIndex,
                                                   DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,
                                                   UINT64 Reservation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter3_SetVideoMemoryReservation(NodeIndex,MemorySegmentGroup,Reservation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_SetVideoMemoryReservation),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NodeIndex);
    npt_encode_DXGI_MEMORY_SEGMENT_GROUP(enc, &MemorySegmentGroup);
    npt_encode_UINT64(enc, &Reservation);
}

static inline size_t
npt_sizeof_IDXGIAdapter3_SetVideoMemoryReservation_reply(const UINT NodeIndex,const DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,const UINT64 Reservation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NodeIndex (input only) */
/* skip MemorySegmentGroup (input only) */
/* skip Reservation (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter3_SetVideoMemoryReservation_reply(struct npt_cs_decoder *dec,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                                  NPT_METHOD_IDXGIAdapter3_SetVideoMemoryReservation))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter3_SetVideoMemoryReservation reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                       NPT_METHOD_IDXGIAdapter3_SetVideoMemoryReservation));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIAdapter3_SetVideoMemoryReservation(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT NodeIndex,
                                                   DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,
                                                   UINT64 Reservation,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter3_SetVideoMemoryReservation(NodeIndex,MemorySegmentGroup,Reservation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter3_SetVideoMemoryReservation_reply(NodeIndex,MemorySegmentGroup,Reservation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter3_SetVideoMemoryReservation(enc, cmd_flags, object_id, NodeIndex, MemorySegmentGroup, Reservation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter3_SetVideoMemoryReservation(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT NodeIndex,
                                                  DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,
                                                  UINT64 Reservation)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_SetVideoMemoryReservation(ring, 0, object_id, NodeIndex, MemorySegmentGroup, Reservation, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter3_SetVideoMemoryReservation(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT NodeIndex,
                                                 DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup,
                                                 UINT64 Reservation)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_SetVideoMemoryReservation(ring, NPT_CMD_FLAG_REPLY, object_id, NodeIndex, MemorySegmentGroup, Reservation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter3_SetVideoMemoryReservation_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIAdapter3::RegisterVideoMemoryBudgetChangeNotificationEvent
 */

struct npt_command_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pdwCookie (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(struct npt_cs_encoder *enc,
                                                                          uint32_t cmd_flags,
                                                                          npt_object_id object_id,
                                                                          HANDLE hEvent,
                                                                          DWORD * pdwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(hEvent,pdwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
/* skip pdwCookie (output only) */
}

static inline size_t
npt_sizeof_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_reply(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hEvent (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DWORD(&(const DWORD){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_reply(struct npt_cs_decoder *dec,
                                                                                DWORD * pdwCookie,
                                                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                                  NPT_METHOD_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                       NPT_METHOD_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pdwCookie)
            npt_decode_DWORD(dec, (DWORD *)pdwCookie);
    }
}

static inline void
npt_submit_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(struct npt_ring *ring,
                                                                          uint32_t cmd_flags,
                                                                          npt_object_id object_id,
                                                                          HANDLE hEvent,
                                                                          DWORD * pdwCookie,
                                                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(hEvent,pdwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_reply(hEvent,pdwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(enc, cmd_flags, object_id, hEvent, pdwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(struct npt_ring *ring,
                                                                         npt_object_id object_id,
                                                                         HANDLE hEvent,
                                                                         DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(ring, 0, object_id, hEvent, pdwCookie, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(struct npt_ring *ring,
                                                                        npt_object_id object_id,
                                                                        HANDLE hEvent,
                                                                        DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(ring, NPT_CMD_FLAG_REPLY, object_id, hEvent, pdwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_reply(dec, pdwCookie, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIAdapter3::UnregisterVideoMemoryBudgetChangeNotification
 */

struct npt_command_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification {
    void *_self;
    DWORD dwCookie;
};

static inline size_t
npt_sizeof_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(const DWORD dwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DWORD(&dwCookie, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(struct npt_cs_encoder *enc,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       DWORD dwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(dwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DWORD(enc, &dwCookie);
}

static inline size_t
npt_sizeof_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification_reply(const DWORD dwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip dwCookie (input only) */
    return cmd_size;
}

static inline void
npt_submit_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(struct npt_ring *ring,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       DWORD dwCookie,
                                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(dwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification_reply(dwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(enc, cmd_flags, object_id, dwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(ring, 0, object_id, dwCookie, &submit);
}

static inline void
npt_call_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(ring, NPT_CMD_FLAG_REPLY, object_id, dwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * IDXGIAdapter4::GetDesc3
 */

struct npt_command_IDXGIAdapter4_GetDesc3 {
    void *_self;
    DXGI_ADAPTER_DESC3 * pDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIAdapter4_GetDesc3(const DXGI_ADAPTER_DESC3 * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIAdapter4_GetDesc3(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_ADAPTER_DESC3 * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIAdapter4_GetDesc3(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter4,
                                 NPT_METHOD_IDXGIAdapter4_GetDesc3),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDesc (output only) */
}

static inline size_t
npt_sizeof_IDXGIAdapter4_GetDesc3_reply(const DXGI_ADAPTER_DESC3 * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_ADAPTER_DESC3(&(const DXGI_ADAPTER_DESC3){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIAdapter4_GetDesc3_reply(struct npt_cs_decoder *dec,
                                        DXGI_ADAPTER_DESC3 * pDesc,
                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter4,
                                                  NPT_METHOD_IDXGIAdapter4_GetDesc3))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIAdapter4_GetDesc3 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter4,
                                       NPT_METHOD_IDXGIAdapter4_GetDesc3));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDesc)
            npt_decode_DXGI_ADAPTER_DESC3(dec, (DXGI_ADAPTER_DESC3 *)pDesc);
    }
}

static inline void
npt_submit_IDXGIAdapter4_GetDesc3(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  DXGI_ADAPTER_DESC3 * pDesc,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIAdapter4_GetDesc3(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIAdapter4_GetDesc3_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIAdapter4_GetDesc3(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIAdapter4_GetDesc3(struct npt_ring *ring,
                                 npt_object_id object_id,
                                 DXGI_ADAPTER_DESC3 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter4_GetDesc3(ring, 0, object_id, pDesc, &submit);
}

static inline HRESULT
npt_call_IDXGIAdapter4_GetDesc3(struct npt_ring *ring,
                                npt_object_id object_id,
                                DXGI_ADAPTER_DESC3 * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIAdapter4_GetDesc3(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIAdapter4_GetDesc3_reply(dec, pDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIADAPTER_H */
