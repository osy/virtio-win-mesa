/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12sdkconfiguration.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12SDKCONFIGURATION_H
#define NPT_PROTOCOL_GUEST_ID3D12SDKCONFIGURATION_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12SDKConfiguration::SetSDKVersion
 */

struct npt_command_ID3D12SDKConfiguration_SetSDKVersion {
    void *_self;
    UINT SDKVersion;
    const CHAR * SDKPath;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12SDKConfiguration_SetSDKVersion(const UINT SDKVersion,const CHAR * SDKPath)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&SDKVersion, max_mode);
    if (SDKPath) {
        const size_t _str_size = strlen((const char *)SDKPath) + 1;
        cmd_size += npt_sizeof_array_count(_str_size);
        cmd_size += npt_sizeof_blob_array(SDKPath, _str_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D12SDKConfiguration_SetSDKVersion(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT SDKVersion,
                                                const CHAR * SDKPath)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12SDKConfiguration_SetSDKVersion(SDKVersion,SDKPath);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SDKConfiguration,
                                 NPT_METHOD_ID3D12SDKConfiguration_SetSDKVersion),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &SDKVersion);
    if (SDKPath) {
        const size_t _str_size = strlen((const char *)SDKPath) + 1;
        npt_encode_array_count(enc, _str_size);
        npt_encode_blob_array(enc, SDKPath, _str_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12SDKConfiguration_SetSDKVersion_reply(const UINT SDKVersion,const CHAR * SDKPath)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip SDKVersion (input only) */
/* skip SDKPath (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12SDKConfiguration_SetSDKVersion_reply(struct npt_cs_decoder *dec,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SDKConfiguration,
                                                  NPT_METHOD_ID3D12SDKConfiguration_SetSDKVersion))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12SDKConfiguration_SetSDKVersion reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SDKConfiguration,
                                       NPT_METHOD_ID3D12SDKConfiguration_SetSDKVersion));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12SDKConfiguration_SetSDKVersion(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT SDKVersion,
                                                const CHAR * SDKPath,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12SDKConfiguration_SetSDKVersion(SDKVersion,SDKPath);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12SDKConfiguration_SetSDKVersion_reply(SDKVersion,SDKPath)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12SDKConfiguration_SetSDKVersion(enc, cmd_flags, object_id, SDKVersion, SDKPath);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12SDKConfiguration_SetSDKVersion(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               UINT SDKVersion,
                                               const CHAR * SDKPath)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SDKConfiguration_SetSDKVersion(ring, 0, object_id, SDKVersion, SDKPath, &submit);
}

static inline HRESULT
npt_call_ID3D12SDKConfiguration_SetSDKVersion(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT SDKVersion,
                                              const CHAR * SDKPath)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SDKConfiguration_SetSDKVersion(ring, NPT_CMD_FLAG_REPLY, object_id, SDKVersion, SDKPath, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12SDKConfiguration_SetSDKVersion_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12SDKConfiguration1::CreateDeviceFactory
 */

struct npt_command_ID3D12SDKConfiguration1_CreateDeviceFactory {
    void *_self;
    UINT SDKVersion;
    const CHAR * SDKPath;
    const IID * riid;
    void ** ppvFactory;
    /* Shadow: the guest-allocated id for ppvFactory arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvFactory -> *ppvFactory} in the
     * context object table. */
    uint64_t _guest_id_ppvFactory;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12SDKConfiguration1_CreateDeviceFactory(const UINT SDKVersion,const CHAR * SDKPath,const IID * riid,void ** ppvFactory)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&SDKVersion, max_mode);
    if (SDKPath) {
        const size_t _str_size = strlen((const char *)SDKPath) + 1;
        cmd_size += npt_sizeof_array_count(_str_size);
        cmd_size += npt_sizeof_blob_array(SDKPath, _str_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvFactory: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12SDKConfiguration1_CreateDeviceFactory(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       UINT SDKVersion,
                                                       const CHAR * SDKPath,
                                                       const IID * riid,
                                                       void ** ppvFactory)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12SDKConfiguration1_CreateDeviceFactory(SDKVersion,SDKPath,riid,ppvFactory);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SDKConfiguration1,
                                 NPT_METHOD_ID3D12SDKConfiguration1_CreateDeviceFactory),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &SDKVersion);
    if (SDKPath) {
        const size_t _str_size = strlen((const char *)SDKPath) + 1;
        npt_encode_array_count(enc, _str_size);
        npt_encode_blob_array(enc, SDKPath, _str_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvFactory = 0;
    if (ppvFactory) {
        _gid_ppvFactory = npt_com_allocate_next_id();
        *ppvFactory = (void *)(uintptr_t)_gid_ppvFactory;
    }
    npt_encode_uint64_t(enc, &_gid_ppvFactory);
}
}

static inline size_t
npt_sizeof_ID3D12SDKConfiguration1_CreateDeviceFactory_reply(const UINT SDKVersion,const CHAR * SDKPath,const IID * riid,void ** ppvFactory)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip SDKVersion (input only) */
/* skip SDKPath (input only) */
/* skip riid (input only) */
/* skip ppvFactory (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12SDKConfiguration1_CreateDeviceFactory_reply(struct npt_cs_decoder *dec,
                                                             void ** ppvFactory,
                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SDKConfiguration1,
                                                  NPT_METHOD_ID3D12SDKConfiguration1_CreateDeviceFactory))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12SDKConfiguration1_CreateDeviceFactory reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SDKConfiguration1,
                                       NPT_METHOD_ID3D12SDKConfiguration1_CreateDeviceFactory));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvFactory (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12SDKConfiguration1_CreateDeviceFactory(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       UINT SDKVersion,
                                                       const CHAR * SDKPath,
                                                       const IID * riid,
                                                       void ** ppvFactory,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12SDKConfiguration1_CreateDeviceFactory(SDKVersion,SDKPath,riid,ppvFactory);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12SDKConfiguration1_CreateDeviceFactory_reply(SDKVersion,SDKPath,riid,ppvFactory)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12SDKConfiguration1_CreateDeviceFactory(enc, cmd_flags, object_id, SDKVersion, SDKPath, riid, ppvFactory);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12SDKConfiguration1_CreateDeviceFactory(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      UINT SDKVersion,
                                                      const CHAR * SDKPath,
                                                      const IID * riid,
                                                      void ** ppvFactory)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SDKConfiguration1_CreateDeviceFactory(ring, 0, object_id, SDKVersion, SDKPath, riid, ppvFactory, &submit);
}

static inline HRESULT
npt_call_ID3D12SDKConfiguration1_CreateDeviceFactory(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT SDKVersion,
                                                     const CHAR * SDKPath,
                                                     const IID * riid,
                                                     void ** ppvFactory)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SDKConfiguration1_CreateDeviceFactory(ring, NPT_CMD_FLAG_REPLY, object_id, SDKVersion, SDKPath, riid, ppvFactory, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12SDKConfiguration1_CreateDeviceFactory_reply(dec, ppvFactory, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12SDKConfiguration1::FreeUnusedSDKs
 */

struct npt_command_ID3D12SDKConfiguration1_FreeUnusedSDKs {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D12SDKConfiguration1_FreeUnusedSDKs(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12SDKConfiguration1_FreeUnusedSDKs(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12SDKConfiguration1_FreeUnusedSDKs();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SDKConfiguration1,
                                 NPT_METHOD_ID3D12SDKConfiguration1_FreeUnusedSDKs),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12SDKConfiguration1_FreeUnusedSDKs_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D12SDKConfiguration1_FreeUnusedSDKs(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12SDKConfiguration1_FreeUnusedSDKs();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12SDKConfiguration1_FreeUnusedSDKs_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12SDKConfiguration1_FreeUnusedSDKs(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12SDKConfiguration1_FreeUnusedSDKs(struct npt_ring *ring,
                                                 npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SDKConfiguration1_FreeUnusedSDKs(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D12SDKConfiguration1_FreeUnusedSDKs(struct npt_ring *ring,
                                                npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12SDKConfiguration1_FreeUnusedSDKs(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12SDKCONFIGURATION_H */
