/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11videocontext.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11VIDEOCONTEXT_H
#define NPT_PROTOCOL_GUEST_ID3D11VIDEOCONTEXT_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11VideoContext::GetDecoderBuffer
 */

struct npt_command_ID3D11VideoContext_GetDecoderBuffer {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    D3D11_VIDEO_DECODER_BUFFER_TYPE Type;
    UINT * pBufferSize;
    void ** ppBuffer;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_GetDecoderBuffer(const ID3D11VideoDecoder * pDecoder,const D3D11_VIDEO_DECODER_BUFFER_TYPE Type,const UINT * pBufferSize,void ** ppBuffer)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_BUFFER_TYPE(&Type, max_mode);
/* skip pBufferSize (output only) */
/* skip ppBuffer (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_GetDecoderBuffer(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11VideoDecoder * pDecoder,
                                               D3D11_VIDEO_DECODER_BUFFER_TYPE Type,
                                               UINT * pBufferSize,
                                               void ** ppBuffer)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_GetDecoderBuffer(pDecoder,Type,pBufferSize,ppBuffer);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_GetDecoderBuffer),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    npt_encode_D3D11_VIDEO_DECODER_BUFFER_TYPE(enc, &Type);
/* skip pBufferSize (output only) */
/* skip ppBuffer (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_GetDecoderBuffer_reply(const ID3D11VideoDecoder * pDecoder,const D3D11_VIDEO_DECODER_BUFFER_TYPE Type,const UINT * pBufferSize,void ** ppBuffer)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip Type (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_array_count(*pBufferSize);
    cmd_size += (size_t)(*pBufferSize) * (npt_sizeof_simple_pointer((const void *)1) + npt_sizeof_void(NULL, 1));
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_GetDecoderBuffer_reply(struct npt_cs_decoder *dec,
                                                     UINT * pBufferSize,
                                                     void ** ppBuffer,
                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_GetDecoderBuffer))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_GetDecoderBuffer reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_GetDecoderBuffer));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pBufferSize)
            npt_decode_UINT(dec, (UINT *)pBufferSize);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && ppBuffer) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_void(dec, (void *)&ppBuffer[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D11VideoContext_GetDecoderBuffer(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11VideoDecoder * pDecoder,
                                               D3D11_VIDEO_DECODER_BUFFER_TYPE Type,
                                               UINT * pBufferSize,
                                               void ** ppBuffer,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_GetDecoderBuffer(pDecoder,Type,pBufferSize,ppBuffer);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_GetDecoderBuffer_reply(pDecoder,Type,pBufferSize,ppBuffer)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_GetDecoderBuffer(enc, cmd_flags, object_id, pDecoder, Type, pBufferSize, ppBuffer);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_GetDecoderBuffer(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11VideoDecoder * pDecoder,
                                              D3D11_VIDEO_DECODER_BUFFER_TYPE Type,
                                              UINT * pBufferSize,
                                              void ** ppBuffer)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_GetDecoderBuffer(ring, 0, object_id, pDecoder, Type, pBufferSize, ppBuffer, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_GetDecoderBuffer(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11VideoDecoder * pDecoder,
                                             D3D11_VIDEO_DECODER_BUFFER_TYPE Type,
                                             UINT * pBufferSize,
                                             void ** ppBuffer)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_GetDecoderBuffer(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, Type, pBufferSize, ppBuffer, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_GetDecoderBuffer_reply(dec, pBufferSize, ppBuffer, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::ReleaseDecoderBuffer
 */

struct npt_command_ID3D11VideoContext_ReleaseDecoderBuffer {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    D3D11_VIDEO_DECODER_BUFFER_TYPE Type;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_ReleaseDecoderBuffer(const ID3D11VideoDecoder * pDecoder,const D3D11_VIDEO_DECODER_BUFFER_TYPE Type)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_BUFFER_TYPE(&Type, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_ReleaseDecoderBuffer(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11VideoDecoder * pDecoder,
                                                   D3D11_VIDEO_DECODER_BUFFER_TYPE Type)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_ReleaseDecoderBuffer(pDecoder,Type);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_ReleaseDecoderBuffer),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    npt_encode_D3D11_VIDEO_DECODER_BUFFER_TYPE(enc, &Type);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_ReleaseDecoderBuffer_reply(const ID3D11VideoDecoder * pDecoder,const D3D11_VIDEO_DECODER_BUFFER_TYPE Type)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip Type (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_ReleaseDecoderBuffer_reply(struct npt_cs_decoder *dec,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_ReleaseDecoderBuffer))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_ReleaseDecoderBuffer reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_ReleaseDecoderBuffer));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext_ReleaseDecoderBuffer(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11VideoDecoder * pDecoder,
                                                   D3D11_VIDEO_DECODER_BUFFER_TYPE Type,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_ReleaseDecoderBuffer(pDecoder,Type);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_ReleaseDecoderBuffer_reply(pDecoder,Type)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_ReleaseDecoderBuffer(enc, cmd_flags, object_id, pDecoder, Type);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_ReleaseDecoderBuffer(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D11VideoDecoder * pDecoder,
                                                  D3D11_VIDEO_DECODER_BUFFER_TYPE Type)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_ReleaseDecoderBuffer(ring, 0, object_id, pDecoder, Type, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_ReleaseDecoderBuffer(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11VideoDecoder * pDecoder,
                                                 D3D11_VIDEO_DECODER_BUFFER_TYPE Type)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_ReleaseDecoderBuffer(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, Type, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_ReleaseDecoderBuffer_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::DecoderBeginFrame
 */

struct npt_command_ID3D11VideoContext_DecoderBeginFrame {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    ID3D11VideoDecoderOutputView * pView;
    UINT ContentKeySize;
    const void * pContentKey;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_DecoderBeginFrame(const ID3D11VideoDecoder * pDecoder,const ID3D11VideoDecoderOutputView * pView,const UINT ContentKeySize,const void * pContentKey)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&ContentKeySize, max_mode);
    if (pContentKey) {
        cmd_size += npt_sizeof_array_count(ContentKeySize);
        cmd_size += npt_sizeof_blob_array(pContentKey, ContentKeySize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_DecoderBeginFrame(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11VideoDecoder * pDecoder,
                                                ID3D11VideoDecoderOutputView * pView,
                                                UINT ContentKeySize,
                                                const void * pContentKey)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_DecoderBeginFrame(pDecoder,pView,ContentKeySize,pContentKey);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_DecoderBeginFrame),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    npt_encode_com_handle(enc, npt_object_get_id(pView));
    npt_encode_UINT(enc, &ContentKeySize);
    if (pContentKey) {
        npt_encode_array_count(enc, ContentKeySize);
        npt_encode_blob_array(enc, pContentKey, ContentKeySize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext_DecoderBeginFrame_reply(const ID3D11VideoDecoder * pDecoder,const ID3D11VideoDecoderOutputView * pView,const UINT ContentKeySize,const void * pContentKey)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip pView (input only) */
/* skip ContentKeySize (input only) */
/* skip pContentKey (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_DecoderBeginFrame_reply(struct npt_cs_decoder *dec,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_DecoderBeginFrame))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_DecoderBeginFrame reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_DecoderBeginFrame));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext_DecoderBeginFrame(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11VideoDecoder * pDecoder,
                                                ID3D11VideoDecoderOutputView * pView,
                                                UINT ContentKeySize,
                                                const void * pContentKey,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_DecoderBeginFrame(pDecoder,pView,ContentKeySize,pContentKey);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_DecoderBeginFrame_reply(pDecoder,pView,ContentKeySize,pContentKey)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_DecoderBeginFrame(enc, cmd_flags, object_id, pDecoder, pView, ContentKeySize, pContentKey);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_DecoderBeginFrame(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11VideoDecoder * pDecoder,
                                               ID3D11VideoDecoderOutputView * pView,
                                               UINT ContentKeySize,
                                               const void * pContentKey)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_DecoderBeginFrame(ring, 0, object_id, pDecoder, pView, ContentKeySize, pContentKey, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_DecoderBeginFrame(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11VideoDecoder * pDecoder,
                                              ID3D11VideoDecoderOutputView * pView,
                                              UINT ContentKeySize,
                                              const void * pContentKey)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_DecoderBeginFrame(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, pView, ContentKeySize, pContentKey, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_DecoderBeginFrame_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::DecoderEndFrame
 */

struct npt_command_ID3D11VideoContext_DecoderEndFrame {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_DecoderEndFrame(const ID3D11VideoDecoder * pDecoder)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_DecoderEndFrame(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11VideoDecoder * pDecoder)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_DecoderEndFrame(pDecoder);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_DecoderEndFrame),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
}

static inline size_t
npt_sizeof_ID3D11VideoContext_DecoderEndFrame_reply(const ID3D11VideoDecoder * pDecoder)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_DecoderEndFrame_reply(struct npt_cs_decoder *dec,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_DecoderEndFrame))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_DecoderEndFrame reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_DecoderEndFrame));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext_DecoderEndFrame(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D11VideoDecoder * pDecoder,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_DecoderEndFrame(pDecoder);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_DecoderEndFrame_reply(pDecoder)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_DecoderEndFrame(enc, cmd_flags, object_id, pDecoder);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_DecoderEndFrame(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11VideoDecoder * pDecoder)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_DecoderEndFrame(ring, 0, object_id, pDecoder, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_DecoderEndFrame(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            ID3D11VideoDecoder * pDecoder)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_DecoderEndFrame(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_DecoderEndFrame_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::SubmitDecoderBuffers
 */

struct npt_command_ID3D11VideoContext_SubmitDecoderBuffers {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    UINT NumBuffers;
    const D3D11_VIDEO_DECODER_BUFFER_DESC * pBufferDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_SubmitDecoderBuffers(const ID3D11VideoDecoder * pDecoder,const UINT NumBuffers,const D3D11_VIDEO_DECODER_BUFFER_DESC * pBufferDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(pBufferDesc ? NumBuffers : 0);
    for (uint32_t _i = 0; _i < (pBufferDesc ? NumBuffers : 0); _i++)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_BUFFER_DESC(&pBufferDesc[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_SubmitDecoderBuffers(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11VideoDecoder * pDecoder,
                                                   UINT NumBuffers,
                                                   const D3D11_VIDEO_DECODER_BUFFER_DESC * pBufferDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_SubmitDecoderBuffers(pDecoder,NumBuffers,pBufferDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_SubmitDecoderBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    npt_encode_UINT(enc, &NumBuffers);
    if (pBufferDesc) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)(NumBuffers); _i++)
            npt_encode_D3D11_VIDEO_DECODER_BUFFER_DESC(enc, &pBufferDesc[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext_SubmitDecoderBuffers_reply(const ID3D11VideoDecoder * pDecoder,const UINT NumBuffers,const D3D11_VIDEO_DECODER_BUFFER_DESC * pBufferDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip NumBuffers (input only) */
/* skip pBufferDesc (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_SubmitDecoderBuffers_reply(struct npt_cs_decoder *dec,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_SubmitDecoderBuffers))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_SubmitDecoderBuffers reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_SubmitDecoderBuffers));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext_SubmitDecoderBuffers(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D11VideoDecoder * pDecoder,
                                                   UINT NumBuffers,
                                                   const D3D11_VIDEO_DECODER_BUFFER_DESC * pBufferDesc,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_SubmitDecoderBuffers(pDecoder,NumBuffers,pBufferDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_SubmitDecoderBuffers_reply(pDecoder,NumBuffers,pBufferDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_SubmitDecoderBuffers(enc, cmd_flags, object_id, pDecoder, NumBuffers, pBufferDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_SubmitDecoderBuffers(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D11VideoDecoder * pDecoder,
                                                  UINT NumBuffers,
                                                  const D3D11_VIDEO_DECODER_BUFFER_DESC * pBufferDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_SubmitDecoderBuffers(ring, 0, object_id, pDecoder, NumBuffers, pBufferDesc, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_SubmitDecoderBuffers(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11VideoDecoder * pDecoder,
                                                 UINT NumBuffers,
                                                 const D3D11_VIDEO_DECODER_BUFFER_DESC * pBufferDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_SubmitDecoderBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, NumBuffers, pBufferDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_SubmitDecoderBuffers_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::DecoderExtension
 */

struct npt_command_ID3D11VideoContext_DecoderExtension {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    const D3D11_VIDEO_DECODER_EXTENSION * pExtensionData;
    APP_DEPRECATED_HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_DecoderExtension(const ID3D11VideoDecoder * pDecoder,const D3D11_VIDEO_DECODER_EXTENSION * pExtensionData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pExtensionData);
    if (pExtensionData)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_EXTENSION(pExtensionData, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_DecoderExtension(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11VideoDecoder * pDecoder,
                                               const D3D11_VIDEO_DECODER_EXTENSION * pExtensionData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_DecoderExtension(pDecoder,pExtensionData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_DecoderExtension),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    if (npt_encode_simple_pointer(enc, pExtensionData))
        npt_encode_D3D11_VIDEO_DECODER_EXTENSION(enc, pExtensionData);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_DecoderExtension_reply(const ID3D11VideoDecoder * pDecoder,const D3D11_VIDEO_DECODER_EXTENSION * pExtensionData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip pExtensionData (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_DecoderExtension_reply(struct npt_cs_decoder *dec,
                                                     APP_DEPRECATED_HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_DecoderExtension))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_DecoderExtension reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_DecoderExtension));
        if (ret) *ret = (APP_DEPRECATED_HRESULT)0;
        return;
    }
    if (ret) *ret = (APP_DEPRECATED_HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext_DecoderExtension(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D11VideoDecoder * pDecoder,
                                               const D3D11_VIDEO_DECODER_EXTENSION * pExtensionData,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_DecoderExtension(pDecoder,pExtensionData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_DecoderExtension_reply(pDecoder,pExtensionData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_DecoderExtension(enc, cmd_flags, object_id, pDecoder, pExtensionData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_DecoderExtension(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11VideoDecoder * pDecoder,
                                              const D3D11_VIDEO_DECODER_EXTENSION * pExtensionData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_DecoderExtension(ring, 0, object_id, pDecoder, pExtensionData, &submit);
}

static inline APP_DEPRECATED_HRESULT
npt_call_ID3D11VideoContext_DecoderExtension(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D11VideoDecoder * pDecoder,
                                             const D3D11_VIDEO_DECODER_EXTENSION * pExtensionData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_DecoderExtension(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, pExtensionData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        APP_DEPRECATED_HRESULT _ret;
        npt_decode_ID3D11VideoContext_DecoderExtension_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (APP_DEPRECATED_HRESULT)0;
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputTargetRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputTargetRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL Enable;
    const RECT * pRect;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(const ID3D11VideoProcessor * pVideoProcessor,const BOOL Enable,const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pRect);
    if (pRect)
        cmd_size += npt_sizeof_RECT(pRect, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL Enable,
                                                                const RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(pVideoProcessor,Enable,pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputTargetRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_BOOL(enc, &Enable);
    if (npt_encode_simple_pointer(enc, pRect))
        npt_encode_RECT(enc, pRect);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputTargetRect_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL Enable,const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip Enable (input only) */
/* skip pRect (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL Enable,
                                                                const RECT * pRect,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(pVideoProcessor,Enable,pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputTargetRect_reply(pVideoProcessor,Enable,pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(enc, cmd_flags, object_id, pVideoProcessor, Enable, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               BOOL Enable,
                                                               const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(ring, 0, object_id, pVideoProcessor, Enable, pRect, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              BOOL Enable,
                                                              const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, Enable, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputBackgroundColor
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL YCbCr;
    const D3D11_VIDEO_COLOR * pColor;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(const ID3D11VideoProcessor * pVideoProcessor,const BOOL YCbCr,const D3D11_VIDEO_COLOR * pColor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_BOOL(&YCbCr, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pColor);
    if (pColor)
        cmd_size += npt_sizeof_D3D11_VIDEO_COLOR(pColor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(struct npt_cs_encoder *enc,
                                                                     uint32_t cmd_flags,
                                                                     npt_object_id object_id,
                                                                     ID3D11VideoProcessor * pVideoProcessor,
                                                                     BOOL YCbCr,
                                                                     const D3D11_VIDEO_COLOR * pColor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(pVideoProcessor,YCbCr,pColor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_BOOL(enc, &YCbCr);
    if (npt_encode_simple_pointer(enc, pColor))
        npt_encode_D3D11_VIDEO_COLOR(enc, pColor);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL YCbCr,const D3D11_VIDEO_COLOR * pColor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip YCbCr (input only) */
/* skip pColor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(struct npt_ring *ring,
                                                                     uint32_t cmd_flags,
                                                                     npt_object_id object_id,
                                                                     ID3D11VideoProcessor * pVideoProcessor,
                                                                     BOOL YCbCr,
                                                                     const D3D11_VIDEO_COLOR * pColor,
                                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(pVideoProcessor,YCbCr,pColor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor_reply(pVideoProcessor,YCbCr,pColor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(enc, cmd_flags, object_id, pVideoProcessor, YCbCr, pColor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    ID3D11VideoProcessor * pVideoProcessor,
                                                                    BOOL YCbCr,
                                                                    const D3D11_VIDEO_COLOR * pColor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(ring, 0, object_id, pVideoProcessor, YCbCr, pColor, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(struct npt_ring *ring,
                                                                   npt_object_id object_id,
                                                                   ID3D11VideoProcessor * pVideoProcessor,
                                                                   BOOL YCbCr,
                                                                   const D3D11_VIDEO_COLOR * pColor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, YCbCr, pColor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputColorSpace
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputColorSpace {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(const ID3D11VideoProcessor * pVideoProcessor,const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pColorSpace);
    if (pColorSpace)
        cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(pColorSpace, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(pVideoProcessor,pColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputColorSpace),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    if (npt_encode_simple_pointer(enc, pColorSpace))
        npt_encode_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(enc, pColorSpace);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputColorSpace_reply(const ID3D11VideoProcessor * pVideoProcessor,const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip pColorSpace (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(pVideoProcessor,pColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputColorSpace_reply(pVideoProcessor,pColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(enc, cmd_flags, object_id, pVideoProcessor, pColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(ring, 0, object_id, pVideoProcessor, pColorSpace, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputAlphaFillMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE AlphaFillMode;
    UINT StreamIndex;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(const ID3D11VideoProcessor * pVideoProcessor,const D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE AlphaFillMode,const UINT StreamIndex)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE(&AlphaFillMode, max_mode);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(struct npt_cs_encoder *enc,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   ID3D11VideoProcessor * pVideoProcessor,
                                                                   D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE AlphaFillMode,
                                                                   UINT StreamIndex)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(pVideoProcessor,AlphaFillMode,StreamIndex);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE(enc, &AlphaFillMode);
    npt_encode_UINT(enc, &StreamIndex);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode_reply(const ID3D11VideoProcessor * pVideoProcessor,const D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE AlphaFillMode,const UINT StreamIndex)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip AlphaFillMode (input only) */
/* skip StreamIndex (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(struct npt_ring *ring,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   ID3D11VideoProcessor * pVideoProcessor,
                                                                   D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE AlphaFillMode,
                                                                   UINT StreamIndex,
                                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(pVideoProcessor,AlphaFillMode,StreamIndex);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode_reply(pVideoProcessor,AlphaFillMode,StreamIndex)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(enc, cmd_flags, object_id, pVideoProcessor, AlphaFillMode, StreamIndex);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE AlphaFillMode,
                                                                  UINT StreamIndex)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(ring, 0, object_id, pVideoProcessor, AlphaFillMode, StreamIndex, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE AlphaFillMode,
                                                                 UINT StreamIndex)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, AlphaFillMode, StreamIndex, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputConstriction
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputConstriction {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL Enable;
    SIZE Size;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputConstriction(const ID3D11VideoProcessor * pVideoProcessor,const BOOL Enable,const SIZE Size)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_SIZE(&Size, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputConstriction(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  BOOL Enable,
                                                                  SIZE Size)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputConstriction(pVideoProcessor,Enable,Size);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputConstriction),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_BOOL(enc, &Enable);
    npt_encode_SIZE(enc, &Size);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputConstriction_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL Enable,const SIZE Size)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip Enable (input only) */
/* skip Size (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetOutputConstriction(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  BOOL Enable,
                                                                  SIZE Size,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputConstriction(pVideoProcessor,Enable,Size);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputConstriction_reply(pVideoProcessor,Enable,Size)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetOutputConstriction(enc, cmd_flags, object_id, pVideoProcessor, Enable, Size);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetOutputConstriction(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 BOOL Enable,
                                                                 SIZE Size)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputConstriction(ring, 0, object_id, pVideoProcessor, Enable, Size, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetOutputConstriction(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL Enable,
                                                                SIZE Size)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputConstriction(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, Enable, Size, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputStereoMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputStereoMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL Enable;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(const ID3D11VideoProcessor * pVideoProcessor,const BOOL Enable)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL Enable)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(pVideoProcessor,Enable);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputStereoMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_BOOL(enc, &Enable);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputStereoMode_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL Enable)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip Enable (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL Enable,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(pVideoProcessor,Enable);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputStereoMode_reply(pVideoProcessor,Enable)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(enc, cmd_flags, object_id, pVideoProcessor, Enable);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               BOOL Enable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(ring, 0, object_id, pVideoProcessor, Enable, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              BOOL Enable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, Enable, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputExtension
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputExtension {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    const GUID * pExtensionGuid;
    UINT DataSize;
    void * pData;
    APP_DEPRECATED_HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputExtension(const ID3D11VideoProcessor * pVideoProcessor,const GUID * pExtensionGuid,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pExtensionGuid);
    if (pExtensionGuid)
        cmd_size += npt_sizeof_GUID(pExtensionGuid, max_mode);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
    /* ERROR: pData (void) is not serializable */
    return 0;
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputExtension(struct npt_cs_encoder *enc,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               const GUID * pExtensionGuid,
                                                               UINT DataSize,
                                                               void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputExtension(pVideoProcessor,pExtensionGuid,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputExtension),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    if (npt_encode_simple_pointer(enc, pExtensionGuid))
        npt_encode_GUID(enc, pExtensionGuid);
    npt_encode_UINT(enc, &DataSize);
    /* ERROR: pData (void) is not serializable */
    return;
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputExtension_reply(const ID3D11VideoProcessor * pVideoProcessor,const GUID * pExtensionGuid,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip pExtensionGuid (input only) */
/* skip DataSize (input only) */
/* skip pData (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetOutputExtension_reply(struct npt_cs_decoder *dec,
                                                                     APP_DEPRECATED_HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputExtension))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorSetOutputExtension reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputExtension));
        if (ret) *ret = (APP_DEPRECATED_HRESULT)0;
        return;
    }
    if (ret) *ret = (APP_DEPRECATED_HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetOutputExtension(struct npt_ring *ring,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               const GUID * pExtensionGuid,
                                                               UINT DataSize,
                                                               void * pData,
                                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputExtension(pVideoProcessor,pExtensionGuid,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetOutputExtension_reply(pVideoProcessor,pExtensionGuid,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetOutputExtension(enc, cmd_flags, object_id, pVideoProcessor, pExtensionGuid, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetOutputExtension(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              const GUID * pExtensionGuid,
                                                              UINT DataSize,
                                                              void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputExtension(ring, 0, object_id, pVideoProcessor, pExtensionGuid, DataSize, pData, &submit);
}

static inline APP_DEPRECATED_HRESULT
npt_call_ID3D11VideoContext_VideoProcessorSetOutputExtension(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             const GUID * pExtensionGuid,
                                                             UINT DataSize,
                                                             void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetOutputExtension(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pExtensionGuid, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        APP_DEPRECATED_HRESULT _ret;
        npt_decode_ID3D11VideoContext_VideoProcessorSetOutputExtension_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (APP_DEPRECATED_HRESULT)0;
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputTargetRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputTargetRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * Enabled;
    RECT * pRect;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * Enabled,const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip Enabled (output only) */
/* skip pRect (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL * Enabled,
                                                                RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(pVideoProcessor,Enabled,pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputTargetRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
/* skip Enabled (output only) */
/* skip pRect (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * Enabled,const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_RECT(&(const RECT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_reply(struct npt_cs_decoder *dec,
                                                                      BOOL * Enabled,
                                                                      RECT * pRect)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputTargetRect))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetOutputTargetRect reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputTargetRect));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (Enabled)
            npt_decode_BOOL(dec, (BOOL *)Enabled);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pRect)
            npt_decode_RECT(dec, (RECT *)pRect);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL * Enabled,
                                                                RECT * pRect,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(pVideoProcessor,Enabled,pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_reply(pVideoProcessor,Enabled,pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(enc, cmd_flags, object_id, pVideoProcessor, Enabled, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               BOOL * Enabled,
                                                               RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(ring, 0, object_id, pVideoProcessor, Enabled, pRect, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              BOOL * Enabled,
                                                              RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, Enabled, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_reply(dec, Enabled, pRect);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputBackgroundColor
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * pYCbCr;
    D3D11_VIDEO_COLOR * pColor;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * pYCbCr,const D3D11_VIDEO_COLOR * pColor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pYCbCr (output only) */
/* skip pColor (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(struct npt_cs_encoder *enc,
                                                                     uint32_t cmd_flags,
                                                                     npt_object_id object_id,
                                                                     ID3D11VideoProcessor * pVideoProcessor,
                                                                     BOOL * pYCbCr,
                                                                     D3D11_VIDEO_COLOR * pColor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(pVideoProcessor,pYCbCr,pColor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
/* skip pYCbCr (output only) */
/* skip pColor (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * pYCbCr,const D3D11_VIDEO_COLOR * pColor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_COLOR(&(const D3D11_VIDEO_COLOR){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_reply(struct npt_cs_decoder *dec,
                                                                           BOOL * pYCbCr,
                                                                           D3D11_VIDEO_COLOR * pColor)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pYCbCr)
            npt_decode_BOOL(dec, (BOOL *)pYCbCr);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pColor)
            npt_decode_D3D11_VIDEO_COLOR(dec, (D3D11_VIDEO_COLOR *)pColor);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(struct npt_ring *ring,
                                                                     uint32_t cmd_flags,
                                                                     npt_object_id object_id,
                                                                     ID3D11VideoProcessor * pVideoProcessor,
                                                                     BOOL * pYCbCr,
                                                                     D3D11_VIDEO_COLOR * pColor,
                                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(pVideoProcessor,pYCbCr,pColor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_reply(pVideoProcessor,pYCbCr,pColor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(enc, cmd_flags, object_id, pVideoProcessor, pYCbCr, pColor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    ID3D11VideoProcessor * pVideoProcessor,
                                                                    BOOL * pYCbCr,
                                                                    D3D11_VIDEO_COLOR * pColor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(ring, 0, object_id, pVideoProcessor, pYCbCr, pColor, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(struct npt_ring *ring,
                                                                   npt_object_id object_id,
                                                                   ID3D11VideoProcessor * pVideoProcessor,
                                                                   BOOL * pYCbCr,
                                                                   D3D11_VIDEO_COLOR * pColor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pYCbCr, pColor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_reply(dec, pYCbCr, pColor);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputColorSpace
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputColorSpace {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(const ID3D11VideoProcessor * pVideoProcessor,const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pColorSpace (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(pVideoProcessor,pColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputColorSpace),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
/* skip pColorSpace (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_reply(const ID3D11VideoProcessor * pVideoProcessor,const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(&(const D3D11_VIDEO_PROCESSOR_COLOR_SPACE){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_reply(struct npt_cs_decoder *dec,
                                                                      D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputColorSpace))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetOutputColorSpace reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputColorSpace));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pColorSpace)
            npt_decode_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(dec, (D3D11_VIDEO_PROCESSOR_COLOR_SPACE *)pColorSpace);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(pVideoProcessor,pColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_reply(pVideoProcessor,pColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(enc, cmd_flags, object_id, pVideoProcessor, pColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(ring, 0, object_id, pVideoProcessor, pColorSpace, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_reply(dec, pColorSpace);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputAlphaFillMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE * pAlphaFillMode;
    UINT * pStreamIndex;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(const ID3D11VideoProcessor * pVideoProcessor,const D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE * pAlphaFillMode,const UINT * pStreamIndex)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pAlphaFillMode (output only) */
/* skip pStreamIndex (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(struct npt_cs_encoder *enc,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   ID3D11VideoProcessor * pVideoProcessor,
                                                                   D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE * pAlphaFillMode,
                                                                   UINT * pStreamIndex)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(pVideoProcessor,pAlphaFillMode,pStreamIndex);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
/* skip pAlphaFillMode (output only) */
/* skip pStreamIndex (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_reply(const ID3D11VideoProcessor * pVideoProcessor,const D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE * pAlphaFillMode,const UINT * pStreamIndex)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE(&(const D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_reply(struct npt_cs_decoder *dec,
                                                                         D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE * pAlphaFillMode,
                                                                         UINT * pStreamIndex)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pAlphaFillMode)
            npt_decode_D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE(dec, (D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE *)pAlphaFillMode);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pStreamIndex)
            npt_decode_UINT(dec, (UINT *)pStreamIndex);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(struct npt_ring *ring,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   ID3D11VideoProcessor * pVideoProcessor,
                                                                   D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE * pAlphaFillMode,
                                                                   UINT * pStreamIndex,
                                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(pVideoProcessor,pAlphaFillMode,pStreamIndex);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_reply(pVideoProcessor,pAlphaFillMode,pStreamIndex)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(enc, cmd_flags, object_id, pVideoProcessor, pAlphaFillMode, pStreamIndex);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE * pAlphaFillMode,
                                                                  UINT * pStreamIndex)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(ring, 0, object_id, pVideoProcessor, pAlphaFillMode, pStreamIndex, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE * pAlphaFillMode,
                                                                 UINT * pStreamIndex)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pAlphaFillMode, pStreamIndex, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_reply(dec, pAlphaFillMode, pStreamIndex);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputConstriction
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputConstriction {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * pEnabled;
    SIZE * pSize;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputConstriction(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * pEnabled,const SIZE * pSize)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pEnabled (output only) */
/* skip pSize (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputConstriction(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  BOOL * pEnabled,
                                                                  SIZE * pSize)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputConstriction(pVideoProcessor,pEnabled,pSize);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputConstriction),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
/* skip pEnabled (output only) */
/* skip pSize (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputConstriction_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * pEnabled,const SIZE * pSize)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_SIZE(&(const SIZE){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputConstriction_reply(struct npt_cs_decoder *dec,
                                                                        BOOL * pEnabled,
                                                                        SIZE * pSize)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputConstriction))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetOutputConstriction reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputConstriction));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnabled)
            npt_decode_BOOL(dec, (BOOL *)pEnabled);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pSize)
            npt_decode_SIZE(dec, (SIZE *)pSize);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetOutputConstriction(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  BOOL * pEnabled,
                                                                  SIZE * pSize,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputConstriction(pVideoProcessor,pEnabled,pSize);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputConstriction_reply(pVideoProcessor,pEnabled,pSize)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetOutputConstriction(enc, cmd_flags, object_id, pVideoProcessor, pEnabled, pSize);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetOutputConstriction(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 BOOL * pEnabled,
                                                                 SIZE * pSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputConstriction(ring, 0, object_id, pVideoProcessor, pEnabled, pSize, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetOutputConstriction(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL * pEnabled,
                                                                SIZE * pSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputConstriction(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pEnabled, pSize, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetOutputConstriction_reply(dec, pEnabled, pSize);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputStereoMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputStereoMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * pEnabled;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * pEnabled)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pEnabled (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL * pEnabled)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(pVideoProcessor,pEnabled);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputStereoMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
/* skip pEnabled (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * pEnabled)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_reply(struct npt_cs_decoder *dec,
                                                                      BOOL * pEnabled)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputStereoMode))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetOutputStereoMode reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputStereoMode));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnabled)
            npt_decode_BOOL(dec, (BOOL *)pEnabled);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL * pEnabled,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(pVideoProcessor,pEnabled);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_reply(pVideoProcessor,pEnabled)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(enc, cmd_flags, object_id, pVideoProcessor, pEnabled);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               BOOL * pEnabled)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(ring, 0, object_id, pVideoProcessor, pEnabled, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              BOOL * pEnabled)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pEnabled, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_reply(dec, pEnabled);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputExtension
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputExtension {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    const GUID * pExtensionGuid;
    UINT DataSize;
    void * pData;
    APP_DEPRECATED_HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputExtension(const ID3D11VideoProcessor * pVideoProcessor,const GUID * pExtensionGuid,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pExtensionGuid);
    if (pExtensionGuid)
        cmd_size += npt_sizeof_GUID(pExtensionGuid, max_mode);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
/* skip pData (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputExtension(struct npt_cs_encoder *enc,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               const GUID * pExtensionGuid,
                                                               UINT DataSize,
                                                               void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputExtension(pVideoProcessor,pExtensionGuid,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputExtension),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    if (npt_encode_simple_pointer(enc, pExtensionGuid))
        npt_encode_GUID(enc, pExtensionGuid);
    npt_encode_UINT(enc, &DataSize);
/* skip pData (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputExtension_reply(const ID3D11VideoProcessor * pVideoProcessor,const GUID * pExtensionGuid,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip pExtensionGuid (input only) */
/* skip DataSize (input only) */
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputExtension_reply(struct npt_cs_decoder *dec,
                                                                     void * pData,
                                                                     APP_DEPRECATED_HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputExtension))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetOutputExtension reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputExtension));
        if (ret) *ret = (APP_DEPRECATED_HRESULT)0;
        return;
    }
    if (ret) *ret = (APP_DEPRECATED_HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pData)
            npt_decode_blob_array(dec, (void *)pData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetOutputExtension(struct npt_ring *ring,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               const GUID * pExtensionGuid,
                                                               UINT DataSize,
                                                               void * pData,
                                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputExtension(pVideoProcessor,pExtensionGuid,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetOutputExtension_reply(pVideoProcessor,pExtensionGuid,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetOutputExtension(enc, cmd_flags, object_id, pVideoProcessor, pExtensionGuid, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetOutputExtension(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              const GUID * pExtensionGuid,
                                                              UINT DataSize,
                                                              void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputExtension(ring, 0, object_id, pVideoProcessor, pExtensionGuid, DataSize, pData, &submit);
}

static inline APP_DEPRECATED_HRESULT
npt_call_ID3D11VideoContext_VideoProcessorGetOutputExtension(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             const GUID * pExtensionGuid,
                                                             UINT DataSize,
                                                             void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetOutputExtension(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pExtensionGuid, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        APP_DEPRECATED_HRESULT _ret;
        npt_decode_ID3D11VideoContext_VideoProcessorGetOutputExtension_reply(dec, pData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (APP_DEPRECATED_HRESULT)0;
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamFrameFormat
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_FRAME_FORMAT FrameFormat;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_FRAME_FORMAT FrameFormat)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_D3D11_VIDEO_FRAME_FORMAT(&FrameFormat, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(struct npt_cs_encoder *enc,
                                                                 uint32_t cmd_flags,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 D3D11_VIDEO_FRAME_FORMAT FrameFormat)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(pVideoProcessor,StreamIndex,FrameFormat);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_D3D11_VIDEO_FRAME_FORMAT(enc, &FrameFormat);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_FRAME_FORMAT FrameFormat)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip FrameFormat (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(struct npt_ring *ring,
                                                                 uint32_t cmd_flags,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 D3D11_VIDEO_FRAME_FORMAT FrameFormat,
                                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(pVideoProcessor,StreamIndex,FrameFormat);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat_reply(pVideoProcessor,StreamIndex,FrameFormat)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, FrameFormat);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                D3D11_VIDEO_FRAME_FORMAT FrameFormat)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(ring, 0, object_id, pVideoProcessor, StreamIndex, FrameFormat, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               D3D11_VIDEO_FRAME_FORMAT FrameFormat)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, FrameFormat, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamColorSpace
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamColorSpace {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pColorSpace);
    if (pColorSpace)
        cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(pColorSpace, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(pVideoProcessor,StreamIndex,pColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamColorSpace),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    if (npt_encode_simple_pointer(enc, pColorSpace))
        npt_encode_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(enc, pColorSpace);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamColorSpace_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip pColorSpace (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(pVideoProcessor,StreamIndex,pColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamColorSpace_reply(pVideoProcessor,StreamIndex,pColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(ring, 0, object_id, pVideoProcessor, StreamIndex, pColorSpace, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamOutputRate
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamOutputRate {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_OUTPUT_RATE OutputRate;
    BOOL RepeatFrame;
    const DXGI_RATIONAL * pCustomRate;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_OUTPUT_RATE OutputRate,const BOOL RepeatFrame,const DXGI_RATIONAL * pCustomRate)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_OUTPUT_RATE(&OutputRate, max_mode);
    cmd_size += npt_sizeof_BOOL(&RepeatFrame, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pCustomRate);
    if (pCustomRate)
        cmd_size += npt_sizeof_DXGI_RATIONAL(pCustomRate, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                D3D11_VIDEO_PROCESSOR_OUTPUT_RATE OutputRate,
                                                                BOOL RepeatFrame,
                                                                const DXGI_RATIONAL * pCustomRate)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(pVideoProcessor,StreamIndex,OutputRate,RepeatFrame,pCustomRate);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamOutputRate),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_D3D11_VIDEO_PROCESSOR_OUTPUT_RATE(enc, &OutputRate);
    npt_encode_BOOL(enc, &RepeatFrame);
    if (npt_encode_simple_pointer(enc, pCustomRate))
        npt_encode_DXGI_RATIONAL(enc, pCustomRate);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamOutputRate_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_OUTPUT_RATE OutputRate,const BOOL RepeatFrame,const DXGI_RATIONAL * pCustomRate)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip OutputRate (input only) */
/* skip RepeatFrame (input only) */
/* skip pCustomRate (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                D3D11_VIDEO_PROCESSOR_OUTPUT_RATE OutputRate,
                                                                BOOL RepeatFrame,
                                                                const DXGI_RATIONAL * pCustomRate,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(pVideoProcessor,StreamIndex,OutputRate,RepeatFrame,pCustomRate);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamOutputRate_reply(pVideoProcessor,StreamIndex,OutputRate,RepeatFrame,pCustomRate)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, OutputRate, RepeatFrame, pCustomRate);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               D3D11_VIDEO_PROCESSOR_OUTPUT_RATE OutputRate,
                                                               BOOL RepeatFrame,
                                                               const DXGI_RATIONAL * pCustomRate)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(ring, 0, object_id, pVideoProcessor, StreamIndex, OutputRate, RepeatFrame, pCustomRate, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              D3D11_VIDEO_PROCESSOR_OUTPUT_RATE OutputRate,
                                                              BOOL RepeatFrame,
                                                              const DXGI_RATIONAL * pCustomRate)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, OutputRate, RepeatFrame, pCustomRate, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamSourceRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamSourceRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    const RECT * pRect;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pRect);
    if (pRect)
        cmd_size += npt_sizeof_RECT(pRect, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                BOOL Enable,
                                                                const RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(pVideoProcessor,StreamIndex,Enable,pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamSourceRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_BOOL(enc, &Enable);
    if (npt_encode_simple_pointer(enc, pRect))
        npt_encode_RECT(enc, pRect);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamSourceRect_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Enable (input only) */
/* skip pRect (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                BOOL Enable,
                                                                const RECT * pRect,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(pVideoProcessor,StreamIndex,Enable,pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamSourceRect_reply(pVideoProcessor,StreamIndex,Enable,pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Enable, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               BOOL Enable,
                                                               const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(ring, 0, object_id, pVideoProcessor, StreamIndex, Enable, pRect, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL Enable,
                                                              const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Enable, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamDestRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamDestRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    const RECT * pRect;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamDestRect(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pRect);
    if (pRect)
        cmd_size += npt_sizeof_RECT(pRect, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamDestRect(struct npt_cs_encoder *enc,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL Enable,
                                                              const RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamDestRect(pVideoProcessor,StreamIndex,Enable,pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamDestRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_BOOL(enc, &Enable);
    if (npt_encode_simple_pointer(enc, pRect))
        npt_encode_RECT(enc, pRect);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamDestRect_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Enable (input only) */
/* skip pRect (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamDestRect(struct npt_ring *ring,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL Enable,
                                                              const RECT * pRect,
                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamDestRect(pVideoProcessor,StreamIndex,Enable,pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamDestRect_reply(pVideoProcessor,StreamIndex,Enable,pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamDestRect(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Enable, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamDestRect(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL Enable,
                                                             const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamDestRect(ring, 0, object_id, pVideoProcessor, StreamIndex, Enable, pRect, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamDestRect(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            BOOL Enable,
                                                            const RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamDestRect(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Enable, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamAlpha
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAlpha {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    FLOAT Alpha;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAlpha(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const FLOAT Alpha)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_FLOAT(&Alpha, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamAlpha(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           BOOL Enable,
                                                           FLOAT Alpha)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAlpha(pVideoProcessor,StreamIndex,Enable,Alpha);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamAlpha),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_BOOL(enc, &Enable);
    npt_encode_FLOAT(enc, &Alpha);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAlpha_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const FLOAT Alpha)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Enable (input only) */
/* skip Alpha (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamAlpha(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           BOOL Enable,
                                                           FLOAT Alpha,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAlpha(pVideoProcessor,StreamIndex,Enable,Alpha);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAlpha_reply(pVideoProcessor,StreamIndex,Enable,Alpha)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamAlpha(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Enable, Alpha);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamAlpha(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D11VideoProcessor * pVideoProcessor,
                                                          UINT StreamIndex,
                                                          BOOL Enable,
                                                          FLOAT Alpha)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamAlpha(ring, 0, object_id, pVideoProcessor, StreamIndex, Enable, Alpha, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamAlpha(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         ID3D11VideoProcessor * pVideoProcessor,
                                                         UINT StreamIndex,
                                                         BOOL Enable,
                                                         FLOAT Alpha)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamAlpha(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Enable, Alpha, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamPalette
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPalette {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    UINT Count;
    const UINT * pEntries;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPalette(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const UINT Count,const UINT * pEntries)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_UINT(&Count, max_mode);
    cmd_size += npt_sizeof_array_count(pEntries ? Count : 0);
    if (pEntries)
        cmd_size += npt_sizeof_UINT_array(pEntries, Count);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamPalette(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             UINT Count,
                                                             const UINT * pEntries)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPalette(pVideoProcessor,StreamIndex,Count,pEntries);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamPalette),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_UINT(enc, &Count);
    if (pEntries) {
        npt_encode_array_count(enc, Count);
        npt_encode_UINT_array(enc, pEntries, Count);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPalette_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const UINT Count,const UINT * pEntries)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Count (input only) */
/* skip pEntries (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamPalette(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             UINT Count,
                                                             const UINT * pEntries,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPalette(pVideoProcessor,StreamIndex,Count,pEntries);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPalette_reply(pVideoProcessor,StreamIndex,Count,pEntries)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamPalette(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Count, pEntries);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamPalette(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            UINT Count,
                                                            const UINT * pEntries)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamPalette(ring, 0, object_id, pVideoProcessor, StreamIndex, Count, pEntries, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamPalette(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           UINT Count,
                                                           const UINT * pEntries)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamPalette(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Count, pEntries, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamPixelAspectRatio
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    const DXGI_RATIONAL * pSourceAspectRatio;
    const DXGI_RATIONAL * pDestinationAspectRatio;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const DXGI_RATIONAL * pSourceAspectRatio,const DXGI_RATIONAL * pDestinationAspectRatio)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSourceAspectRatio);
    if (pSourceAspectRatio)
        cmd_size += npt_sizeof_DXGI_RATIONAL(pSourceAspectRatio, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDestinationAspectRatio);
    if (pDestinationAspectRatio)
        cmd_size += npt_sizeof_DXGI_RATIONAL(pDestinationAspectRatio, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(struct npt_cs_encoder *enc,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      ID3D11VideoProcessor * pVideoProcessor,
                                                                      UINT StreamIndex,
                                                                      BOOL Enable,
                                                                      const DXGI_RATIONAL * pSourceAspectRatio,
                                                                      const DXGI_RATIONAL * pDestinationAspectRatio)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(pVideoProcessor,StreamIndex,Enable,pSourceAspectRatio,pDestinationAspectRatio);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_BOOL(enc, &Enable);
    if (npt_encode_simple_pointer(enc, pSourceAspectRatio))
        npt_encode_DXGI_RATIONAL(enc, pSourceAspectRatio);
    if (npt_encode_simple_pointer(enc, pDestinationAspectRatio))
        npt_encode_DXGI_RATIONAL(enc, pDestinationAspectRatio);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const DXGI_RATIONAL * pSourceAspectRatio,const DXGI_RATIONAL * pDestinationAspectRatio)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Enable (input only) */
/* skip pSourceAspectRatio (input only) */
/* skip pDestinationAspectRatio (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(struct npt_ring *ring,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      ID3D11VideoProcessor * pVideoProcessor,
                                                                      UINT StreamIndex,
                                                                      BOOL Enable,
                                                                      const DXGI_RATIONAL * pSourceAspectRatio,
                                                                      const DXGI_RATIONAL * pDestinationAspectRatio,
                                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(pVideoProcessor,StreamIndex,Enable,pSourceAspectRatio,pDestinationAspectRatio);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio_reply(pVideoProcessor,StreamIndex,Enable,pSourceAspectRatio,pDestinationAspectRatio)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Enable, pSourceAspectRatio, pDestinationAspectRatio);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     ID3D11VideoProcessor * pVideoProcessor,
                                                                     UINT StreamIndex,
                                                                     BOOL Enable,
                                                                     const DXGI_RATIONAL * pSourceAspectRatio,
                                                                     const DXGI_RATIONAL * pDestinationAspectRatio)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(ring, 0, object_id, pVideoProcessor, StreamIndex, Enable, pSourceAspectRatio, pDestinationAspectRatio, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    ID3D11VideoProcessor * pVideoProcessor,
                                                                    UINT StreamIndex,
                                                                    BOOL Enable,
                                                                    const DXGI_RATIONAL * pSourceAspectRatio,
                                                                    const DXGI_RATIONAL * pDestinationAspectRatio)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Enable, pSourceAspectRatio, pDestinationAspectRatio, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamLumaKey
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamLumaKey {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    FLOAT Lower;
    FLOAT Upper;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const FLOAT Lower,const FLOAT Upper)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_FLOAT(&Lower, max_mode);
    cmd_size += npt_sizeof_FLOAT(&Upper, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL Enable,
                                                             FLOAT Lower,
                                                             FLOAT Upper)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(pVideoProcessor,StreamIndex,Enable,Lower,Upper);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamLumaKey),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_BOOL(enc, &Enable);
    npt_encode_FLOAT(enc, &Lower);
    npt_encode_FLOAT(enc, &Upper);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamLumaKey_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const FLOAT Lower,const FLOAT Upper)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Enable (input only) */
/* skip Lower (input only) */
/* skip Upper (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL Enable,
                                                             FLOAT Lower,
                                                             FLOAT Upper,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(pVideoProcessor,StreamIndex,Enable,Lower,Upper);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamLumaKey_reply(pVideoProcessor,StreamIndex,Enable,Lower,Upper)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Enable, Lower, Upper);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            BOOL Enable,
                                                            FLOAT Lower,
                                                            FLOAT Upper)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(ring, 0, object_id, pVideoProcessor, StreamIndex, Enable, Lower, Upper, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           BOOL Enable,
                                                           FLOAT Lower,
                                                           FLOAT Upper)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Enable, Lower, Upper, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamStereoFormat
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    D3D11_VIDEO_PROCESSOR_STEREO_FORMAT Format;
    BOOL LeftViewFrame0;
    BOOL BaseViewFrame0;
    D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE FlipMode;
    int MonoOffset;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const D3D11_VIDEO_PROCESSOR_STEREO_FORMAT Format,const BOOL LeftViewFrame0,const BOOL BaseViewFrame0,const D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE FlipMode,const int MonoOffset)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_STEREO_FORMAT(&Format, max_mode);
    cmd_size += npt_sizeof_BOOL(&LeftViewFrame0, max_mode);
    cmd_size += npt_sizeof_BOOL(&BaseViewFrame0, max_mode);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE(&FlipMode, max_mode);
    cmd_size += npt_sizeof_int(&MonoOffset, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  BOOL Enable,
                                                                  D3D11_VIDEO_PROCESSOR_STEREO_FORMAT Format,
                                                                  BOOL LeftViewFrame0,
                                                                  BOOL BaseViewFrame0,
                                                                  D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE FlipMode,
                                                                  int MonoOffset)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(pVideoProcessor,StreamIndex,Enable,Format,LeftViewFrame0,BaseViewFrame0,FlipMode,MonoOffset);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_BOOL(enc, &Enable);
    npt_encode_D3D11_VIDEO_PROCESSOR_STEREO_FORMAT(enc, &Format);
    npt_encode_BOOL(enc, &LeftViewFrame0);
    npt_encode_BOOL(enc, &BaseViewFrame0);
    npt_encode_D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE(enc, &FlipMode);
    npt_encode_int(enc, &MonoOffset);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const D3D11_VIDEO_PROCESSOR_STEREO_FORMAT Format,const BOOL LeftViewFrame0,const BOOL BaseViewFrame0,const D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE FlipMode,const int MonoOffset)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Enable (input only) */
/* skip Format (input only) */
/* skip LeftViewFrame0 (input only) */
/* skip BaseViewFrame0 (input only) */
/* skip FlipMode (input only) */
/* skip MonoOffset (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  BOOL Enable,
                                                                  D3D11_VIDEO_PROCESSOR_STEREO_FORMAT Format,
                                                                  BOOL LeftViewFrame0,
                                                                  BOOL BaseViewFrame0,
                                                                  D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE FlipMode,
                                                                  int MonoOffset,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(pVideoProcessor,StreamIndex,Enable,Format,LeftViewFrame0,BaseViewFrame0,FlipMode,MonoOffset);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat_reply(pVideoProcessor,StreamIndex,Enable,Format,LeftViewFrame0,BaseViewFrame0,FlipMode,MonoOffset)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Enable, Format, LeftViewFrame0, BaseViewFrame0, FlipMode, MonoOffset);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 BOOL Enable,
                                                                 D3D11_VIDEO_PROCESSOR_STEREO_FORMAT Format,
                                                                 BOOL LeftViewFrame0,
                                                                 BOOL BaseViewFrame0,
                                                                 D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE FlipMode,
                                                                 int MonoOffset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(ring, 0, object_id, pVideoProcessor, StreamIndex, Enable, Format, LeftViewFrame0, BaseViewFrame0, FlipMode, MonoOffset, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                BOOL Enable,
                                                                D3D11_VIDEO_PROCESSOR_STEREO_FORMAT Format,
                                                                BOOL LeftViewFrame0,
                                                                BOOL BaseViewFrame0,
                                                                D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE FlipMode,
                                                                int MonoOffset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Enable, Format, LeftViewFrame0, BaseViewFrame0, FlipMode, MonoOffset, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamAutoProcessingMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(struct npt_cs_encoder *enc,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        ID3D11VideoProcessor * pVideoProcessor,
                                                                        UINT StreamIndex,
                                                                        BOOL Enable)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(pVideoProcessor,StreamIndex,Enable);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_BOOL(enc, &Enable);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Enable (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(struct npt_ring *ring,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        ID3D11VideoProcessor * pVideoProcessor,
                                                                        UINT StreamIndex,
                                                                        BOOL Enable,
                                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(pVideoProcessor,StreamIndex,Enable);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode_reply(pVideoProcessor,StreamIndex,Enable)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Enable);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(struct npt_ring *ring,
                                                                       npt_object_id object_id,
                                                                       ID3D11VideoProcessor * pVideoProcessor,
                                                                       UINT StreamIndex,
                                                                       BOOL Enable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(ring, 0, object_id, pVideoProcessor, StreamIndex, Enable, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      ID3D11VideoProcessor * pVideoProcessor,
                                                                      UINT StreamIndex,
                                                                      BOOL Enable)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Enable, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamFilter
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFilter {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_FILTER Filter;
    BOOL Enable;
    int Level;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFilter(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_FILTER Filter,const BOOL Enable,const int Level)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_FILTER(&Filter, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_int(&Level, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamFilter(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                            BOOL Enable,
                                                            int Level)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFilter(pVideoProcessor,StreamIndex,Filter,Enable,Level);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamFilter),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_D3D11_VIDEO_PROCESSOR_FILTER(enc, &Filter);
    npt_encode_BOOL(enc, &Enable);
    npt_encode_int(enc, &Level);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFilter_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_FILTER Filter,const BOOL Enable,const int Level)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Filter (input only) */
/* skip Enable (input only) */
/* skip Level (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamFilter(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                            BOOL Enable,
                                                            int Level,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFilter(pVideoProcessor,StreamIndex,Filter,Enable,Level);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamFilter_reply(pVideoProcessor,StreamIndex,Filter,Enable,Level)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamFilter(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Filter, Enable, Level);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamFilter(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                           BOOL Enable,
                                                           int Level)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamFilter(ring, 0, object_id, pVideoProcessor, StreamIndex, Filter, Enable, Level, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamFilter(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D11VideoProcessor * pVideoProcessor,
                                                          UINT StreamIndex,
                                                          D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                          BOOL Enable,
                                                          int Level)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamFilter(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Filter, Enable, Level, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamExtension
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamExtension {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    const GUID * pExtensionGuid;
    UINT DataSize;
    void * pData;
    APP_DEPRECATED_HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamExtension(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const GUID * pExtensionGuid,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pExtensionGuid);
    if (pExtensionGuid)
        cmd_size += npt_sizeof_GUID(pExtensionGuid, max_mode);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
    /* ERROR: pData (void) is not serializable */
    return 0;
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamExtension(struct npt_cs_encoder *enc,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               const GUID * pExtensionGuid,
                                                               UINT DataSize,
                                                               void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamExtension(pVideoProcessor,StreamIndex,pExtensionGuid,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamExtension),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    if (npt_encode_simple_pointer(enc, pExtensionGuid))
        npt_encode_GUID(enc, pExtensionGuid);
    npt_encode_UINT(enc, &DataSize);
    /* ERROR: pData (void) is not serializable */
    return;
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamExtension_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const GUID * pExtensionGuid,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip pExtensionGuid (input only) */
/* skip DataSize (input only) */
/* skip pData (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamExtension_reply(struct npt_cs_decoder *dec,
                                                                     APP_DEPRECATED_HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamExtension))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorSetStreamExtension reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamExtension));
        if (ret) *ret = (APP_DEPRECATED_HRESULT)0;
        return;
    }
    if (ret) *ret = (APP_DEPRECATED_HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamExtension(struct npt_ring *ring,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               const GUID * pExtensionGuid,
                                                               UINT DataSize,
                                                               void * pData,
                                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamExtension(pVideoProcessor,StreamIndex,pExtensionGuid,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamExtension_reply(pVideoProcessor,StreamIndex,pExtensionGuid,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamExtension(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pExtensionGuid, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamExtension(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              const GUID * pExtensionGuid,
                                                              UINT DataSize,
                                                              void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamExtension(ring, 0, object_id, pVideoProcessor, StreamIndex, pExtensionGuid, DataSize, pData, &submit);
}

static inline APP_DEPRECATED_HRESULT
npt_call_ID3D11VideoContext_VideoProcessorSetStreamExtension(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             const GUID * pExtensionGuid,
                                                             UINT DataSize,
                                                             void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamExtension(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pExtensionGuid, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        APP_DEPRECATED_HRESULT _ret;
        npt_decode_ID3D11VideoContext_VideoProcessorSetStreamExtension_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (APP_DEPRECATED_HRESULT)0;
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamFrameFormat
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_FRAME_FORMAT * pFrameFormat;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_FRAME_FORMAT * pFrameFormat)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pFrameFormat (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(struct npt_cs_encoder *enc,
                                                                 uint32_t cmd_flags,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 D3D11_VIDEO_FRAME_FORMAT * pFrameFormat)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(pVideoProcessor,StreamIndex,pFrameFormat);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pFrameFormat (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_FRAME_FORMAT * pFrameFormat)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_FRAME_FORMAT(&(const D3D11_VIDEO_FRAME_FORMAT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_reply(struct npt_cs_decoder *dec,
                                                                       D3D11_VIDEO_FRAME_FORMAT * pFrameFormat)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamFrameFormat reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pFrameFormat)
            npt_decode_D3D11_VIDEO_FRAME_FORMAT(dec, (D3D11_VIDEO_FRAME_FORMAT *)pFrameFormat);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(struct npt_ring *ring,
                                                                 uint32_t cmd_flags,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 D3D11_VIDEO_FRAME_FORMAT * pFrameFormat,
                                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(pVideoProcessor,StreamIndex,pFrameFormat);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_reply(pVideoProcessor,StreamIndex,pFrameFormat)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pFrameFormat);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                D3D11_VIDEO_FRAME_FORMAT * pFrameFormat)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(ring, 0, object_id, pVideoProcessor, StreamIndex, pFrameFormat, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               D3D11_VIDEO_FRAME_FORMAT * pFrameFormat)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pFrameFormat, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_reply(dec, pFrameFormat);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamColorSpace
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamColorSpace {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pColorSpace (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(pVideoProcessor,StreamIndex,pColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamColorSpace),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pColorSpace (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(&(const D3D11_VIDEO_PROCESSOR_COLOR_SPACE){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_reply(struct npt_cs_decoder *dec,
                                                                      D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamColorSpace))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamColorSpace reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamColorSpace));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pColorSpace)
            npt_decode_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(dec, (D3D11_VIDEO_PROCESSOR_COLOR_SPACE *)pColorSpace);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(pVideoProcessor,StreamIndex,pColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_reply(pVideoProcessor,StreamIndex,pColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(ring, 0, object_id, pVideoProcessor, StreamIndex, pColorSpace, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_reply(dec, pColorSpace);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamOutputRate
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamOutputRate {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_OUTPUT_RATE * pOutputRate;
    BOOL * pRepeatFrame;
    DXGI_RATIONAL * pCustomRate;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_OUTPUT_RATE * pOutputRate,const BOOL * pRepeatFrame,const DXGI_RATIONAL * pCustomRate)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pOutputRate (output only) */
/* skip pRepeatFrame (output only) */
/* skip pCustomRate (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                D3D11_VIDEO_PROCESSOR_OUTPUT_RATE * pOutputRate,
                                                                BOOL * pRepeatFrame,
                                                                DXGI_RATIONAL * pCustomRate)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(pVideoProcessor,StreamIndex,pOutputRate,pRepeatFrame,pCustomRate);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamOutputRate),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pOutputRate (output only) */
/* skip pRepeatFrame (output only) */
/* skip pCustomRate (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_OUTPUT_RATE * pOutputRate,const BOOL * pRepeatFrame,const DXGI_RATIONAL * pCustomRate)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_OUTPUT_RATE(&(const D3D11_VIDEO_PROCESSOR_OUTPUT_RATE){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_RATIONAL(&(const DXGI_RATIONAL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_reply(struct npt_cs_decoder *dec,
                                                                      D3D11_VIDEO_PROCESSOR_OUTPUT_RATE * pOutputRate,
                                                                      BOOL * pRepeatFrame,
                                                                      DXGI_RATIONAL * pCustomRate)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamOutputRate))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamOutputRate reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamOutputRate));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pOutputRate)
            npt_decode_D3D11_VIDEO_PROCESSOR_OUTPUT_RATE(dec, (D3D11_VIDEO_PROCESSOR_OUTPUT_RATE *)pOutputRate);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pRepeatFrame)
            npt_decode_BOOL(dec, (BOOL *)pRepeatFrame);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pCustomRate)
            npt_decode_DXGI_RATIONAL(dec, (DXGI_RATIONAL *)pCustomRate);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                D3D11_VIDEO_PROCESSOR_OUTPUT_RATE * pOutputRate,
                                                                BOOL * pRepeatFrame,
                                                                DXGI_RATIONAL * pCustomRate,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(pVideoProcessor,StreamIndex,pOutputRate,pRepeatFrame,pCustomRate);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_reply(pVideoProcessor,StreamIndex,pOutputRate,pRepeatFrame,pCustomRate)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pOutputRate, pRepeatFrame, pCustomRate);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               D3D11_VIDEO_PROCESSOR_OUTPUT_RATE * pOutputRate,
                                                               BOOL * pRepeatFrame,
                                                               DXGI_RATIONAL * pCustomRate)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(ring, 0, object_id, pVideoProcessor, StreamIndex, pOutputRate, pRepeatFrame, pCustomRate, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              D3D11_VIDEO_PROCESSOR_OUTPUT_RATE * pOutputRate,
                                                              BOOL * pRepeatFrame,
                                                              DXGI_RATIONAL * pCustomRate)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pOutputRate, pRepeatFrame, pCustomRate, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_reply(dec, pOutputRate, pRepeatFrame, pCustomRate);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamSourceRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamSourceRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    RECT * pRect;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pEnabled (output only) */
/* skip pRect (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                BOOL * pEnabled,
                                                                RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(pVideoProcessor,StreamIndex,pEnabled,pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamSourceRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pEnabled (output only) */
/* skip pRect (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_RECT(&(const RECT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_reply(struct npt_cs_decoder *dec,
                                                                      BOOL * pEnabled,
                                                                      RECT * pRect)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamSourceRect))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamSourceRect reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamSourceRect));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnabled)
            npt_decode_BOOL(dec, (BOOL *)pEnabled);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pRect)
            npt_decode_RECT(dec, (RECT *)pRect);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                BOOL * pEnabled,
                                                                RECT * pRect,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(pVideoProcessor,StreamIndex,pEnabled,pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_reply(pVideoProcessor,StreamIndex,pEnabled,pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pEnabled, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               BOOL * pEnabled,
                                                               RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(ring, 0, object_id, pVideoProcessor, StreamIndex, pEnabled, pRect, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL * pEnabled,
                                                              RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pEnabled, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_reply(dec, pEnabled, pRect);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamDestRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamDestRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    RECT * pRect;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamDestRect(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const RECT * pRect)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pEnabled (output only) */
/* skip pRect (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamDestRect(struct npt_cs_encoder *enc,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL * pEnabled,
                                                              RECT * pRect)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamDestRect(pVideoProcessor,StreamIndex,pEnabled,pRect);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamDestRect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pEnabled (output only) */
/* skip pRect (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamDestRect_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const RECT * pRect)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_RECT(&(const RECT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamDestRect_reply(struct npt_cs_decoder *dec,
                                                                    BOOL * pEnabled,
                                                                    RECT * pRect)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamDestRect))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamDestRect reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamDestRect));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnabled)
            npt_decode_BOOL(dec, (BOOL *)pEnabled);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pRect)
            npt_decode_RECT(dec, (RECT *)pRect);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamDestRect(struct npt_ring *ring,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL * pEnabled,
                                                              RECT * pRect,
                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamDestRect(pVideoProcessor,StreamIndex,pEnabled,pRect);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamDestRect_reply(pVideoProcessor,StreamIndex,pEnabled,pRect)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamDestRect(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pEnabled, pRect);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamDestRect(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL * pEnabled,
                                                             RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamDestRect(ring, 0, object_id, pVideoProcessor, StreamIndex, pEnabled, pRect, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamDestRect(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            BOOL * pEnabled,
                                                            RECT * pRect)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamDestRect(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pEnabled, pRect, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamDestRect_reply(dec, pEnabled, pRect);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamAlpha
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAlpha {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    FLOAT * pAlpha;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAlpha(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const FLOAT * pAlpha)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pEnabled (output only) */
/* skip pAlpha (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamAlpha(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           BOOL * pEnabled,
                                                           FLOAT * pAlpha)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAlpha(pVideoProcessor,StreamIndex,pEnabled,pAlpha);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamAlpha),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pEnabled (output only) */
/* skip pAlpha (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAlpha_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const FLOAT * pAlpha)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_FLOAT(&(const FLOAT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamAlpha_reply(struct npt_cs_decoder *dec,
                                                                 BOOL * pEnabled,
                                                                 FLOAT * pAlpha)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamAlpha))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamAlpha reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamAlpha));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnabled)
            npt_decode_BOOL(dec, (BOOL *)pEnabled);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pAlpha)
            npt_decode_FLOAT(dec, (FLOAT *)pAlpha);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamAlpha(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           BOOL * pEnabled,
                                                           FLOAT * pAlpha,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAlpha(pVideoProcessor,StreamIndex,pEnabled,pAlpha);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAlpha_reply(pVideoProcessor,StreamIndex,pEnabled,pAlpha)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamAlpha(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pEnabled, pAlpha);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamAlpha(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D11VideoProcessor * pVideoProcessor,
                                                          UINT StreamIndex,
                                                          BOOL * pEnabled,
                                                          FLOAT * pAlpha)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamAlpha(ring, 0, object_id, pVideoProcessor, StreamIndex, pEnabled, pAlpha, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamAlpha(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         ID3D11VideoProcessor * pVideoProcessor,
                                                         UINT StreamIndex,
                                                         BOOL * pEnabled,
                                                         FLOAT * pAlpha)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamAlpha(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pEnabled, pAlpha, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamAlpha_reply(dec, pEnabled, pAlpha);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamPalette
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPalette {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    UINT Count;
    UINT * pEntries;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPalette(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const UINT Count,const UINT * pEntries)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_UINT(&Count, max_mode);
/* skip pEntries (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamPalette(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             UINT Count,
                                                             UINT * pEntries)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPalette(pVideoProcessor,StreamIndex,Count,pEntries);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamPalette),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_UINT(enc, &Count);
/* skip pEntries (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPalette_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const UINT Count,const UINT * pEntries)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Count (input only) */
    cmd_size += npt_sizeof_array_count(pEntries ? Count : 0);
    if (pEntries)
        cmd_size += npt_sizeof_UINT_array(pEntries, Count);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamPalette_reply(struct npt_cs_decoder *dec,
                                                                   UINT * pEntries)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamPalette))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamPalette reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamPalette));
        return;
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pEntries)
            npt_decode_UINT_array(dec, (UINT *)pEntries, _count);
        else if (_count)
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */  
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamPalette(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             UINT Count,
                                                             UINT * pEntries,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPalette(pVideoProcessor,StreamIndex,Count,pEntries);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPalette_reply(pVideoProcessor,StreamIndex,Count,pEntries)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamPalette(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Count, pEntries);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamPalette(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            UINT Count,
                                                            UINT * pEntries)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamPalette(ring, 0, object_id, pVideoProcessor, StreamIndex, Count, pEntries, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamPalette(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           UINT Count,
                                                           UINT * pEntries)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamPalette(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Count, pEntries, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamPalette_reply(dec, pEntries);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamPixelAspectRatio
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    DXGI_RATIONAL * pSourceAspectRatio;
    DXGI_RATIONAL * pDestinationAspectRatio;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const DXGI_RATIONAL * pSourceAspectRatio,const DXGI_RATIONAL * pDestinationAspectRatio)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pEnabled (output only) */
/* skip pSourceAspectRatio (output only) */
/* skip pDestinationAspectRatio (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(struct npt_cs_encoder *enc,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      ID3D11VideoProcessor * pVideoProcessor,
                                                                      UINT StreamIndex,
                                                                      BOOL * pEnabled,
                                                                      DXGI_RATIONAL * pSourceAspectRatio,
                                                                      DXGI_RATIONAL * pDestinationAspectRatio)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(pVideoProcessor,StreamIndex,pEnabled,pSourceAspectRatio,pDestinationAspectRatio);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pEnabled (output only) */
/* skip pSourceAspectRatio (output only) */
/* skip pDestinationAspectRatio (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const DXGI_RATIONAL * pSourceAspectRatio,const DXGI_RATIONAL * pDestinationAspectRatio)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_RATIONAL(&(const DXGI_RATIONAL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_RATIONAL(&(const DXGI_RATIONAL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_reply(struct npt_cs_decoder *dec,
                                                                            BOOL * pEnabled,
                                                                            DXGI_RATIONAL * pSourceAspectRatio,
                                                                            DXGI_RATIONAL * pDestinationAspectRatio)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnabled)
            npt_decode_BOOL(dec, (BOOL *)pEnabled);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pSourceAspectRatio)
            npt_decode_DXGI_RATIONAL(dec, (DXGI_RATIONAL *)pSourceAspectRatio);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pDestinationAspectRatio)
            npt_decode_DXGI_RATIONAL(dec, (DXGI_RATIONAL *)pDestinationAspectRatio);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(struct npt_ring *ring,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      ID3D11VideoProcessor * pVideoProcessor,
                                                                      UINT StreamIndex,
                                                                      BOOL * pEnabled,
                                                                      DXGI_RATIONAL * pSourceAspectRatio,
                                                                      DXGI_RATIONAL * pDestinationAspectRatio,
                                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(pVideoProcessor,StreamIndex,pEnabled,pSourceAspectRatio,pDestinationAspectRatio);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_reply(pVideoProcessor,StreamIndex,pEnabled,pSourceAspectRatio,pDestinationAspectRatio)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pEnabled, pSourceAspectRatio, pDestinationAspectRatio);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     ID3D11VideoProcessor * pVideoProcessor,
                                                                     UINT StreamIndex,
                                                                     BOOL * pEnabled,
                                                                     DXGI_RATIONAL * pSourceAspectRatio,
                                                                     DXGI_RATIONAL * pDestinationAspectRatio)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(ring, 0, object_id, pVideoProcessor, StreamIndex, pEnabled, pSourceAspectRatio, pDestinationAspectRatio, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    ID3D11VideoProcessor * pVideoProcessor,
                                                                    UINT StreamIndex,
                                                                    BOOL * pEnabled,
                                                                    DXGI_RATIONAL * pSourceAspectRatio,
                                                                    DXGI_RATIONAL * pDestinationAspectRatio)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pEnabled, pSourceAspectRatio, pDestinationAspectRatio, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_reply(dec, pEnabled, pSourceAspectRatio, pDestinationAspectRatio);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamLumaKey
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamLumaKey {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    FLOAT * pLower;
    FLOAT * pUpper;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const FLOAT * pLower,const FLOAT * pUpper)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pEnabled (output only) */
/* skip pLower (output only) */
/* skip pUpper (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL * pEnabled,
                                                             FLOAT * pLower,
                                                             FLOAT * pUpper)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(pVideoProcessor,StreamIndex,pEnabled,pLower,pUpper);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamLumaKey),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pEnabled (output only) */
/* skip pLower (output only) */
/* skip pUpper (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled,const FLOAT * pLower,const FLOAT * pUpper)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_FLOAT(&(const FLOAT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_FLOAT(&(const FLOAT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_reply(struct npt_cs_decoder *dec,
                                                                   BOOL * pEnabled,
                                                                   FLOAT * pLower,
                                                                   FLOAT * pUpper)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamLumaKey))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamLumaKey reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamLumaKey));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnabled)
            npt_decode_BOOL(dec, (BOOL *)pEnabled);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pLower)
            npt_decode_FLOAT(dec, (FLOAT *)pLower);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pUpper)
            npt_decode_FLOAT(dec, (FLOAT *)pUpper);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL * pEnabled,
                                                             FLOAT * pLower,
                                                             FLOAT * pUpper,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(pVideoProcessor,StreamIndex,pEnabled,pLower,pUpper);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_reply(pVideoProcessor,StreamIndex,pEnabled,pLower,pUpper)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pEnabled, pLower, pUpper);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            BOOL * pEnabled,
                                                            FLOAT * pLower,
                                                            FLOAT * pUpper)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(ring, 0, object_id, pVideoProcessor, StreamIndex, pEnabled, pLower, pUpper, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           BOOL * pEnabled,
                                                           FLOAT * pLower,
                                                           FLOAT * pUpper)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pEnabled, pLower, pUpper, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_reply(dec, pEnabled, pLower, pUpper);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamStereoFormat
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnable;
    D3D11_VIDEO_PROCESSOR_STEREO_FORMAT * pFormat;
    BOOL * pLeftViewFrame0;
    BOOL * pBaseViewFrame0;
    D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE * pFlipMode;
    int * MonoOffset;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnable,const D3D11_VIDEO_PROCESSOR_STEREO_FORMAT * pFormat,const BOOL * pLeftViewFrame0,const BOOL * pBaseViewFrame0,const D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE * pFlipMode,const int * MonoOffset)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pEnable (output only) */
/* skip pFormat (output only) */
/* skip pLeftViewFrame0 (output only) */
/* skip pBaseViewFrame0 (output only) */
/* skip pFlipMode (output only) */
/* skip MonoOffset (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  BOOL * pEnable,
                                                                  D3D11_VIDEO_PROCESSOR_STEREO_FORMAT * pFormat,
                                                                  BOOL * pLeftViewFrame0,
                                                                  BOOL * pBaseViewFrame0,
                                                                  D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE * pFlipMode,
                                                                  int * MonoOffset)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(pVideoProcessor,StreamIndex,pEnable,pFormat,pLeftViewFrame0,pBaseViewFrame0,pFlipMode,MonoOffset);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pEnable (output only) */
/* skip pFormat (output only) */
/* skip pLeftViewFrame0 (output only) */
/* skip pBaseViewFrame0 (output only) */
/* skip pFlipMode (output only) */
/* skip MonoOffset (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnable,const D3D11_VIDEO_PROCESSOR_STEREO_FORMAT * pFormat,const BOOL * pLeftViewFrame0,const BOOL * pBaseViewFrame0,const D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE * pFlipMode,const int * MonoOffset)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_STEREO_FORMAT(&(const D3D11_VIDEO_PROCESSOR_STEREO_FORMAT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE(&(const D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_int(&(const int){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_reply(struct npt_cs_decoder *dec,
                                                                        BOOL * pEnable,
                                                                        D3D11_VIDEO_PROCESSOR_STEREO_FORMAT * pFormat,
                                                                        BOOL * pLeftViewFrame0,
                                                                        BOOL * pBaseViewFrame0,
                                                                        D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE * pFlipMode,
                                                                        int * MonoOffset)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamStereoFormat reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnable)
            npt_decode_BOOL(dec, (BOOL *)pEnable);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pFormat)
            npt_decode_D3D11_VIDEO_PROCESSOR_STEREO_FORMAT(dec, (D3D11_VIDEO_PROCESSOR_STEREO_FORMAT *)pFormat);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pLeftViewFrame0)
            npt_decode_BOOL(dec, (BOOL *)pLeftViewFrame0);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pBaseViewFrame0)
            npt_decode_BOOL(dec, (BOOL *)pBaseViewFrame0);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pFlipMode)
            npt_decode_D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE(dec, (D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE *)pFlipMode);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (MonoOffset)
            npt_decode_int(dec, (int *)MonoOffset);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  BOOL * pEnable,
                                                                  D3D11_VIDEO_PROCESSOR_STEREO_FORMAT * pFormat,
                                                                  BOOL * pLeftViewFrame0,
                                                                  BOOL * pBaseViewFrame0,
                                                                  D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE * pFlipMode,
                                                                  int * MonoOffset,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(pVideoProcessor,StreamIndex,pEnable,pFormat,pLeftViewFrame0,pBaseViewFrame0,pFlipMode,MonoOffset);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_reply(pVideoProcessor,StreamIndex,pEnable,pFormat,pLeftViewFrame0,pBaseViewFrame0,pFlipMode,MonoOffset)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pEnable, pFormat, pLeftViewFrame0, pBaseViewFrame0, pFlipMode, MonoOffset);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 BOOL * pEnable,
                                                                 D3D11_VIDEO_PROCESSOR_STEREO_FORMAT * pFormat,
                                                                 BOOL * pLeftViewFrame0,
                                                                 BOOL * pBaseViewFrame0,
                                                                 D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE * pFlipMode,
                                                                 int * MonoOffset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(ring, 0, object_id, pVideoProcessor, StreamIndex, pEnable, pFormat, pLeftViewFrame0, pBaseViewFrame0, pFlipMode, MonoOffset, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                BOOL * pEnable,
                                                                D3D11_VIDEO_PROCESSOR_STEREO_FORMAT * pFormat,
                                                                BOOL * pLeftViewFrame0,
                                                                BOOL * pBaseViewFrame0,
                                                                D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE * pFlipMode,
                                                                int * MonoOffset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pEnable, pFormat, pLeftViewFrame0, pBaseViewFrame0, pFlipMode, MonoOffset, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_reply(dec, pEnable, pFormat, pLeftViewFrame0, pBaseViewFrame0, pFlipMode, MonoOffset);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamAutoProcessingMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pEnabled (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(struct npt_cs_encoder *enc,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        ID3D11VideoProcessor * pVideoProcessor,
                                                                        UINT StreamIndex,
                                                                        BOOL * pEnabled)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(pVideoProcessor,StreamIndex,pEnabled);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pEnabled (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnabled)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_reply(struct npt_cs_decoder *dec,
                                                                              BOOL * pEnabled)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnabled)
            npt_decode_BOOL(dec, (BOOL *)pEnabled);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(struct npt_ring *ring,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        ID3D11VideoProcessor * pVideoProcessor,
                                                                        UINT StreamIndex,
                                                                        BOOL * pEnabled,
                                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(pVideoProcessor,StreamIndex,pEnabled);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_reply(pVideoProcessor,StreamIndex,pEnabled)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pEnabled);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(struct npt_ring *ring,
                                                                       npt_object_id object_id,
                                                                       ID3D11VideoProcessor * pVideoProcessor,
                                                                       UINT StreamIndex,
                                                                       BOOL * pEnabled)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(ring, 0, object_id, pVideoProcessor, StreamIndex, pEnabled, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      ID3D11VideoProcessor * pVideoProcessor,
                                                                      UINT StreamIndex,
                                                                      BOOL * pEnabled)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pEnabled, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_reply(dec, pEnabled);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamFilter
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFilter {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_FILTER Filter;
    BOOL * pEnabled;
    int * pLevel;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFilter(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_FILTER Filter,const BOOL * pEnabled,const int * pLevel)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_FILTER(&Filter, max_mode);
/* skip pEnabled (output only) */
/* skip pLevel (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamFilter(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                            BOOL * pEnabled,
                                                            int * pLevel)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFilter(pVideoProcessor,StreamIndex,Filter,pEnabled,pLevel);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamFilter),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_D3D11_VIDEO_PROCESSOR_FILTER(enc, &Filter);
/* skip pEnabled (output only) */
/* skip pLevel (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFilter_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const D3D11_VIDEO_PROCESSOR_FILTER Filter,const BOOL * pEnabled,const int * pLevel)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Filter (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_int(&(const int){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamFilter_reply(struct npt_cs_decoder *dec,
                                                                  BOOL * pEnabled,
                                                                  int * pLevel)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamFilter))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamFilter reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamFilter));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnabled)
            npt_decode_BOOL(dec, (BOOL *)pEnabled);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pLevel)
            npt_decode_int(dec, (int *)pLevel);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamFilter(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                            BOOL * pEnabled,
                                                            int * pLevel,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFilter(pVideoProcessor,StreamIndex,Filter,pEnabled,pLevel);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamFilter_reply(pVideoProcessor,StreamIndex,Filter,pEnabled,pLevel)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamFilter(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Filter, pEnabled, pLevel);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamFilter(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                           BOOL * pEnabled,
                                                           int * pLevel)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamFilter(ring, 0, object_id, pVideoProcessor, StreamIndex, Filter, pEnabled, pLevel, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamFilter(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D11VideoProcessor * pVideoProcessor,
                                                          UINT StreamIndex,
                                                          D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                          BOOL * pEnabled,
                                                          int * pLevel)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamFilter(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Filter, pEnabled, pLevel, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamFilter_reply(dec, pEnabled, pLevel);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamExtension
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamExtension {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    const GUID * pExtensionGuid;
    UINT DataSize;
    void * pData;
    APP_DEPRECATED_HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamExtension(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const GUID * pExtensionGuid,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pExtensionGuid);
    if (pExtensionGuid)
        cmd_size += npt_sizeof_GUID(pExtensionGuid, max_mode);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
/* skip pData (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamExtension(struct npt_cs_encoder *enc,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               const GUID * pExtensionGuid,
                                                               UINT DataSize,
                                                               void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamExtension(pVideoProcessor,StreamIndex,pExtensionGuid,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamExtension),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    if (npt_encode_simple_pointer(enc, pExtensionGuid))
        npt_encode_GUID(enc, pExtensionGuid);
    npt_encode_UINT(enc, &DataSize);
/* skip pData (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamExtension_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const GUID * pExtensionGuid,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip pExtensionGuid (input only) */
/* skip DataSize (input only) */
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamExtension_reply(struct npt_cs_decoder *dec,
                                                                     void * pData,
                                                                     APP_DEPRECATED_HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamExtension))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamExtension reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamExtension));
        if (ret) *ret = (APP_DEPRECATED_HRESULT)0;
        return;
    }
    if (ret) *ret = (APP_DEPRECATED_HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pData)
            npt_decode_blob_array(dec, (void *)pData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamExtension(struct npt_ring *ring,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D11VideoProcessor * pVideoProcessor,
                                                               UINT StreamIndex,
                                                               const GUID * pExtensionGuid,
                                                               UINT DataSize,
                                                               void * pData,
                                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamExtension(pVideoProcessor,StreamIndex,pExtensionGuid,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamExtension_reply(pVideoProcessor,StreamIndex,pExtensionGuid,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamExtension(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pExtensionGuid, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamExtension(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              const GUID * pExtensionGuid,
                                                              UINT DataSize,
                                                              void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamExtension(ring, 0, object_id, pVideoProcessor, StreamIndex, pExtensionGuid, DataSize, pData, &submit);
}

static inline APP_DEPRECATED_HRESULT
npt_call_ID3D11VideoContext_VideoProcessorGetStreamExtension(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             const GUID * pExtensionGuid,
                                                             UINT DataSize,
                                                             void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamExtension(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pExtensionGuid, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        APP_DEPRECATED_HRESULT _ret;
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamExtension_reply(dec, pData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (APP_DEPRECATED_HRESULT)0;
    }
}

/*
 * ID3D11VideoContext::VideoProcessorBlt
 */

struct npt_command_ID3D11VideoContext_VideoProcessorBlt {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    ID3D11VideoProcessorOutputView * pView;
    UINT OutputFrame;
    UINT StreamCount;
    const D3D11_VIDEO_PROCESSOR_STREAM * pStreams;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorBlt(const ID3D11VideoProcessor * pVideoProcessor,const ID3D11VideoProcessorOutputView * pView,const UINT OutputFrame,const UINT StreamCount,const D3D11_VIDEO_PROCESSOR_STREAM * pStreams)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&OutputFrame, max_mode);
    cmd_size += npt_sizeof_UINT(&StreamCount, max_mode);
    cmd_size += npt_sizeof_array_count(pStreams ? StreamCount : 0);
    for (uint32_t _i = 0; _i < (pStreams ? StreamCount : 0); _i++)
        cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_STREAM(&pStreams[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorBlt(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11VideoProcessor * pVideoProcessor,
                                                ID3D11VideoProcessorOutputView * pView,
                                                UINT OutputFrame,
                                                UINT StreamCount,
                                                const D3D11_VIDEO_PROCESSOR_STREAM * pStreams)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorBlt(pVideoProcessor,pView,OutputFrame,StreamCount,pStreams);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorBlt),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_com_handle(enc, npt_object_get_id(pView));
    npt_encode_UINT(enc, &OutputFrame);
    npt_encode_UINT(enc, &StreamCount);
    if (pStreams) {
        npt_encode_array_count(enc, StreamCount);
        for (uint32_t _i = 0; _i < (uint32_t)(StreamCount); _i++)
            npt_encode_D3D11_VIDEO_PROCESSOR_STREAM(enc, &pStreams[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorBlt_reply(const ID3D11VideoProcessor * pVideoProcessor,const ID3D11VideoProcessorOutputView * pView,const UINT OutputFrame,const UINT StreamCount,const D3D11_VIDEO_PROCESSOR_STREAM * pStreams)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip pView (input only) */
/* skip OutputFrame (input only) */
/* skip StreamCount (input only) */
/* skip pStreams (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorBlt_reply(struct npt_cs_decoder *dec,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorBlt))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorBlt reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorBlt));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorBlt(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D11VideoProcessor * pVideoProcessor,
                                                ID3D11VideoProcessorOutputView * pView,
                                                UINT OutputFrame,
                                                UINT StreamCount,
                                                const D3D11_VIDEO_PROCESSOR_STREAM * pStreams,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorBlt(pVideoProcessor,pView,OutputFrame,StreamCount,pStreams);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorBlt_reply(pVideoProcessor,pView,OutputFrame,StreamCount,pStreams)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorBlt(enc, cmd_flags, object_id, pVideoProcessor, pView, OutputFrame, StreamCount, pStreams);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorBlt(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D11VideoProcessor * pVideoProcessor,
                                               ID3D11VideoProcessorOutputView * pView,
                                               UINT OutputFrame,
                                               UINT StreamCount,
                                               const D3D11_VIDEO_PROCESSOR_STREAM * pStreams)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorBlt(ring, 0, object_id, pVideoProcessor, pView, OutputFrame, StreamCount, pStreams, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_VideoProcessorBlt(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D11VideoProcessor * pVideoProcessor,
                                              ID3D11VideoProcessorOutputView * pView,
                                              UINT OutputFrame,
                                              UINT StreamCount,
                                              const D3D11_VIDEO_PROCESSOR_STREAM * pStreams)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorBlt(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pView, OutputFrame, StreamCount, pStreams, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_VideoProcessorBlt_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::NegotiateCryptoSessionKeyExchange
 */

struct npt_command_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    UINT DataSize;
    void * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(const ID3D11CryptoSession * pCryptoSession,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11CryptoSession * pCryptoSession,
                                                                UINT DataSize,
                                                                void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(pCryptoSession,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCryptoSession));
    npt_encode_UINT(enc, &DataSize);
    if (pData) {
        npt_encode_array_count(enc, DataSize);
        npt_encode_blob_array(enc, pData, DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_reply(const ID3D11CryptoSession * pCryptoSession,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoSession (input only) */
/* skip DataSize (input only) */
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_reply(struct npt_cs_decoder *dec,
                                                                      void * pData,
                                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_NegotiateCryptoSessionKeyExchange reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pData)
            npt_decode_blob_array(dec, (void *)pData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                ID3D11CryptoSession * pCryptoSession,
                                                                UINT DataSize,
                                                                void * pData,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(pCryptoSession,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_reply(pCryptoSession,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(enc, cmd_flags, object_id, pCryptoSession, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               ID3D11CryptoSession * pCryptoSession,
                                                               UINT DataSize,
                                                               void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(ring, 0, object_id, pCryptoSession, DataSize, pData, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D11CryptoSession * pCryptoSession,
                                                              UINT DataSize,
                                                              void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoSession, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_reply(dec, pData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::EncryptionBlt
 */

struct npt_command_ID3D11VideoContext_EncryptionBlt {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    ID3D11Texture2D * pSrcSurface;
    ID3D11Texture2D * pDstSurface;
    UINT IVSize;
    void * pIV;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_EncryptionBlt(const ID3D11CryptoSession * pCryptoSession,const ID3D11Texture2D * pSrcSurface,const ID3D11Texture2D * pDstSurface,const UINT IVSize,const void * pIV)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&IVSize, max_mode);
    if (pIV) {
        cmd_size += npt_sizeof_array_count(IVSize);
        cmd_size += npt_sizeof_blob_array(pIV, IVSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_EncryptionBlt(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11CryptoSession * pCryptoSession,
                                            ID3D11Texture2D * pSrcSurface,
                                            ID3D11Texture2D * pDstSurface,
                                            UINT IVSize,
                                            void * pIV)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_EncryptionBlt(pCryptoSession,pSrcSurface,pDstSurface,IVSize,pIV);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_EncryptionBlt),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCryptoSession));
    npt_encode_com_handle(enc, npt_object_get_id(pSrcSurface));
    npt_encode_com_handle(enc, npt_object_get_id(pDstSurface));
    npt_encode_UINT(enc, &IVSize);
    if (pIV) {
        npt_encode_array_count(enc, IVSize);
        npt_encode_blob_array(enc, pIV, IVSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext_EncryptionBlt_reply(const ID3D11CryptoSession * pCryptoSession,const ID3D11Texture2D * pSrcSurface,const ID3D11Texture2D * pDstSurface,const UINT IVSize,const void * pIV)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoSession (input only) */
/* skip pSrcSurface (input only) */
/* skip pDstSurface (input only) */
/* skip IVSize (input only) */
    if (pIV) {
        cmd_size += npt_sizeof_array_count(IVSize);
        cmd_size += npt_sizeof_blob_array(pIV, IVSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_EncryptionBlt_reply(struct npt_cs_decoder *dec,
                                                  void * pIV)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_EncryptionBlt))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_EncryptionBlt reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_EncryptionBlt));
        return;
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pIV)
            npt_decode_blob_array(dec, (void *)pIV, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext_EncryptionBlt(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11CryptoSession * pCryptoSession,
                                            ID3D11Texture2D * pSrcSurface,
                                            ID3D11Texture2D * pDstSurface,
                                            UINT IVSize,
                                            void * pIV,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_EncryptionBlt(pCryptoSession,pSrcSurface,pDstSurface,IVSize,pIV);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_EncryptionBlt_reply(pCryptoSession,pSrcSurface,pDstSurface,IVSize,pIV)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_EncryptionBlt(enc, cmd_flags, object_id, pCryptoSession, pSrcSurface, pDstSurface, IVSize, pIV);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_EncryptionBlt(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D11CryptoSession * pCryptoSession,
                                           ID3D11Texture2D * pSrcSurface,
                                           ID3D11Texture2D * pDstSurface,
                                           UINT IVSize,
                                           void * pIV)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_EncryptionBlt(ring, 0, object_id, pCryptoSession, pSrcSurface, pDstSurface, IVSize, pIV, &submit);
}

static inline void
npt_call_ID3D11VideoContext_EncryptionBlt(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11CryptoSession * pCryptoSession,
                                          ID3D11Texture2D * pSrcSurface,
                                          ID3D11Texture2D * pDstSurface,
                                          UINT IVSize,
                                          void * pIV)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_EncryptionBlt(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoSession, pSrcSurface, pDstSurface, IVSize, pIV, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_EncryptionBlt_reply(dec, pIV);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::DecryptionBlt
 */

struct npt_command_ID3D11VideoContext_DecryptionBlt {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    ID3D11Texture2D * pSrcSurface;
    ID3D11Texture2D * pDstSurface;
    D3D11_ENCRYPTED_BLOCK_INFO * pEncryptedBlockInfo;
    UINT ContentKeySize;
    const void * pContentKey;
    UINT IVSize;
    void * pIV;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_DecryptionBlt(const ID3D11CryptoSession * pCryptoSession,const ID3D11Texture2D * pSrcSurface,const ID3D11Texture2D * pDstSurface,const D3D11_ENCRYPTED_BLOCK_INFO * pEncryptedBlockInfo,const UINT ContentKeySize,const void * pContentKey,const UINT IVSize,const void * pIV)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pEncryptedBlockInfo);
    if (pEncryptedBlockInfo)
        cmd_size += npt_sizeof_D3D11_ENCRYPTED_BLOCK_INFO(pEncryptedBlockInfo, max_mode);
    cmd_size += npt_sizeof_UINT(&ContentKeySize, max_mode);
    if (pContentKey) {
        cmd_size += npt_sizeof_array_count(ContentKeySize);
        cmd_size += npt_sizeof_blob_array(pContentKey, ContentKeySize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&IVSize, max_mode);
    if (pIV) {
        cmd_size += npt_sizeof_array_count(IVSize);
        cmd_size += npt_sizeof_blob_array(pIV, IVSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_DecryptionBlt(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11CryptoSession * pCryptoSession,
                                            ID3D11Texture2D * pSrcSurface,
                                            ID3D11Texture2D * pDstSurface,
                                            D3D11_ENCRYPTED_BLOCK_INFO * pEncryptedBlockInfo,
                                            UINT ContentKeySize,
                                            const void * pContentKey,
                                            UINT IVSize,
                                            void * pIV)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_DecryptionBlt(pCryptoSession,pSrcSurface,pDstSurface,pEncryptedBlockInfo,ContentKeySize,pContentKey,IVSize,pIV);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_DecryptionBlt),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCryptoSession));
    npt_encode_com_handle(enc, npt_object_get_id(pSrcSurface));
    npt_encode_com_handle(enc, npt_object_get_id(pDstSurface));
    if (npt_encode_simple_pointer(enc, pEncryptedBlockInfo))
        npt_encode_D3D11_ENCRYPTED_BLOCK_INFO(enc, pEncryptedBlockInfo);
    npt_encode_UINT(enc, &ContentKeySize);
    if (pContentKey) {
        npt_encode_array_count(enc, ContentKeySize);
        npt_encode_blob_array(enc, pContentKey, ContentKeySize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &IVSize);
    if (pIV) {
        npt_encode_array_count(enc, IVSize);
        npt_encode_blob_array(enc, pIV, IVSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext_DecryptionBlt_reply(const ID3D11CryptoSession * pCryptoSession,const ID3D11Texture2D * pSrcSurface,const ID3D11Texture2D * pDstSurface,const D3D11_ENCRYPTED_BLOCK_INFO * pEncryptedBlockInfo,const UINT ContentKeySize,const void * pContentKey,const UINT IVSize,const void * pIV)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoSession (input only) */
/* skip pSrcSurface (input only) */
/* skip pDstSurface (input only) */
/* skip pEncryptedBlockInfo (input only) */
/* skip ContentKeySize (input only) */
/* skip pContentKey (input only) */
/* skip IVSize (input only) */
    if (pIV) {
        cmd_size += npt_sizeof_array_count(IVSize);
        cmd_size += npt_sizeof_blob_array(pIV, IVSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_DecryptionBlt_reply(struct npt_cs_decoder *dec,
                                                  void * pIV)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_DecryptionBlt))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_DecryptionBlt reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_DecryptionBlt));
        return;
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pIV)
            npt_decode_blob_array(dec, (void *)pIV, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext_DecryptionBlt(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            ID3D11CryptoSession * pCryptoSession,
                                            ID3D11Texture2D * pSrcSurface,
                                            ID3D11Texture2D * pDstSurface,
                                            D3D11_ENCRYPTED_BLOCK_INFO * pEncryptedBlockInfo,
                                            UINT ContentKeySize,
                                            const void * pContentKey,
                                            UINT IVSize,
                                            void * pIV,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_DecryptionBlt(pCryptoSession,pSrcSurface,pDstSurface,pEncryptedBlockInfo,ContentKeySize,pContentKey,IVSize,pIV);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_DecryptionBlt_reply(pCryptoSession,pSrcSurface,pDstSurface,pEncryptedBlockInfo,ContentKeySize,pContentKey,IVSize,pIV)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_DecryptionBlt(enc, cmd_flags, object_id, pCryptoSession, pSrcSurface, pDstSurface, pEncryptedBlockInfo, ContentKeySize, pContentKey, IVSize, pIV);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_DecryptionBlt(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           ID3D11CryptoSession * pCryptoSession,
                                           ID3D11Texture2D * pSrcSurface,
                                           ID3D11Texture2D * pDstSurface,
                                           D3D11_ENCRYPTED_BLOCK_INFO * pEncryptedBlockInfo,
                                           UINT ContentKeySize,
                                           const void * pContentKey,
                                           UINT IVSize,
                                           void * pIV)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_DecryptionBlt(ring, 0, object_id, pCryptoSession, pSrcSurface, pDstSurface, pEncryptedBlockInfo, ContentKeySize, pContentKey, IVSize, pIV, &submit);
}

static inline void
npt_call_ID3D11VideoContext_DecryptionBlt(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D11CryptoSession * pCryptoSession,
                                          ID3D11Texture2D * pSrcSurface,
                                          ID3D11Texture2D * pDstSurface,
                                          D3D11_ENCRYPTED_BLOCK_INFO * pEncryptedBlockInfo,
                                          UINT ContentKeySize,
                                          const void * pContentKey,
                                          UINT IVSize,
                                          void * pIV)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_DecryptionBlt(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoSession, pSrcSurface, pDstSurface, pEncryptedBlockInfo, ContentKeySize, pContentKey, IVSize, pIV, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_DecryptionBlt_reply(dec, pIV);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::StartSessionKeyRefresh
 */

struct npt_command_ID3D11VideoContext_StartSessionKeyRefresh {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    UINT RandomNumberSize;
    void * pRandomNumber;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_StartSessionKeyRefresh(const ID3D11CryptoSession * pCryptoSession,const UINT RandomNumberSize,const void * pRandomNumber)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&RandomNumberSize, max_mode);
/* skip pRandomNumber (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_StartSessionKeyRefresh(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11CryptoSession * pCryptoSession,
                                                     UINT RandomNumberSize,
                                                     void * pRandomNumber)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_StartSessionKeyRefresh(pCryptoSession,RandomNumberSize,pRandomNumber);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_StartSessionKeyRefresh),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCryptoSession));
    npt_encode_UINT(enc, &RandomNumberSize);
/* skip pRandomNumber (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_StartSessionKeyRefresh_reply(const ID3D11CryptoSession * pCryptoSession,const UINT RandomNumberSize,const void * pRandomNumber)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoSession (input only) */
/* skip RandomNumberSize (input only) */
    if (pRandomNumber) {
        cmd_size += npt_sizeof_array_count(RandomNumberSize);
        cmd_size += npt_sizeof_blob_array(pRandomNumber, RandomNumberSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_StartSessionKeyRefresh_reply(struct npt_cs_decoder *dec,
                                                           void * pRandomNumber)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_StartSessionKeyRefresh))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_StartSessionKeyRefresh reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_StartSessionKeyRefresh));
        return;
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pRandomNumber)
            npt_decode_blob_array(dec, (void *)pRandomNumber, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext_StartSessionKeyRefresh(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11CryptoSession * pCryptoSession,
                                                     UINT RandomNumberSize,
                                                     void * pRandomNumber,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_StartSessionKeyRefresh(pCryptoSession,RandomNumberSize,pRandomNumber);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_StartSessionKeyRefresh_reply(pCryptoSession,RandomNumberSize,pRandomNumber)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_StartSessionKeyRefresh(enc, cmd_flags, object_id, pCryptoSession, RandomNumberSize, pRandomNumber);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_StartSessionKeyRefresh(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11CryptoSession * pCryptoSession,
                                                    UINT RandomNumberSize,
                                                    void * pRandomNumber)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_StartSessionKeyRefresh(ring, 0, object_id, pCryptoSession, RandomNumberSize, pRandomNumber, &submit);
}

static inline void
npt_call_ID3D11VideoContext_StartSessionKeyRefresh(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11CryptoSession * pCryptoSession,
                                                   UINT RandomNumberSize,
                                                   void * pRandomNumber)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_StartSessionKeyRefresh(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoSession, RandomNumberSize, pRandomNumber, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_StartSessionKeyRefresh_reply(dec, pRandomNumber);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::FinishSessionKeyRefresh
 */

struct npt_command_ID3D11VideoContext_FinishSessionKeyRefresh {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_FinishSessionKeyRefresh(const ID3D11CryptoSession * pCryptoSession)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_FinishSessionKeyRefresh(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11CryptoSession * pCryptoSession)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_FinishSessionKeyRefresh(pCryptoSession);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_FinishSessionKeyRefresh),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCryptoSession));
}

static inline size_t
npt_sizeof_ID3D11VideoContext_FinishSessionKeyRefresh_reply(const ID3D11CryptoSession * pCryptoSession)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoSession (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_FinishSessionKeyRefresh(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D11CryptoSession * pCryptoSession,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_FinishSessionKeyRefresh(pCryptoSession);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_FinishSessionKeyRefresh_reply(pCryptoSession)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_FinishSessionKeyRefresh(enc, cmd_flags, object_id, pCryptoSession);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_FinishSessionKeyRefresh(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3D11CryptoSession * pCryptoSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_FinishSessionKeyRefresh(ring, 0, object_id, pCryptoSession, &submit);
}

static inline void
npt_call_ID3D11VideoContext_FinishSessionKeyRefresh(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11CryptoSession * pCryptoSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_FinishSessionKeyRefresh(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoSession, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::GetEncryptionBltKey
 */

struct npt_command_ID3D11VideoContext_GetEncryptionBltKey {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    UINT KeySize;
    void * pReadbackKey;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_GetEncryptionBltKey(const ID3D11CryptoSession * pCryptoSession,const UINT KeySize,const void * pReadbackKey)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&KeySize, max_mode);
/* skip pReadbackKey (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_GetEncryptionBltKey(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11CryptoSession * pCryptoSession,
                                                  UINT KeySize,
                                                  void * pReadbackKey)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_GetEncryptionBltKey(pCryptoSession,KeySize,pReadbackKey);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_GetEncryptionBltKey),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCryptoSession));
    npt_encode_UINT(enc, &KeySize);
/* skip pReadbackKey (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_GetEncryptionBltKey_reply(const ID3D11CryptoSession * pCryptoSession,const UINT KeySize,const void * pReadbackKey)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoSession (input only) */
/* skip KeySize (input only) */
    if (pReadbackKey) {
        cmd_size += npt_sizeof_array_count(KeySize);
        cmd_size += npt_sizeof_blob_array(pReadbackKey, KeySize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_GetEncryptionBltKey_reply(struct npt_cs_decoder *dec,
                                                        void * pReadbackKey,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_GetEncryptionBltKey))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_GetEncryptionBltKey reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_GetEncryptionBltKey));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pReadbackKey)
            npt_decode_blob_array(dec, (void *)pReadbackKey, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext_GetEncryptionBltKey(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11CryptoSession * pCryptoSession,
                                                  UINT KeySize,
                                                  void * pReadbackKey,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_GetEncryptionBltKey(pCryptoSession,KeySize,pReadbackKey);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_GetEncryptionBltKey_reply(pCryptoSession,KeySize,pReadbackKey)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_GetEncryptionBltKey(enc, cmd_flags, object_id, pCryptoSession, KeySize, pReadbackKey);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_GetEncryptionBltKey(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11CryptoSession * pCryptoSession,
                                                 UINT KeySize,
                                                 void * pReadbackKey)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_GetEncryptionBltKey(ring, 0, object_id, pCryptoSession, KeySize, pReadbackKey, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_GetEncryptionBltKey(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11CryptoSession * pCryptoSession,
                                                UINT KeySize,
                                                void * pReadbackKey)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_GetEncryptionBltKey(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoSession, KeySize, pReadbackKey, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_GetEncryptionBltKey_reply(dec, pReadbackKey, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::NegotiateAuthenticatedChannelKeyExchange
 */

struct npt_command_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange {
    void *_self;
    ID3D11AuthenticatedChannel * pChannel;
    UINT DataSize;
    void * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(const ID3D11AuthenticatedChannel * pChannel,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(struct npt_cs_encoder *enc,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       ID3D11AuthenticatedChannel * pChannel,
                                                                       UINT DataSize,
                                                                       void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(pChannel,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pChannel));
    npt_encode_UINT(enc, &DataSize);
    if (pData) {
        npt_encode_array_count(enc, DataSize);
        npt_encode_blob_array(enc, pData, DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_reply(const ID3D11AuthenticatedChannel * pChannel,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pChannel (input only) */
/* skip DataSize (input only) */
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_reply(struct npt_cs_decoder *dec,
                                                                             void * pData,
                                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pData)
            npt_decode_blob_array(dec, (void *)pData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(struct npt_ring *ring,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       ID3D11AuthenticatedChannel * pChannel,
                                                                       UINT DataSize,
                                                                       void * pData,
                                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(pChannel,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_reply(pChannel,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(enc, cmd_flags, object_id, pChannel, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      ID3D11AuthenticatedChannel * pChannel,
                                                                      UINT DataSize,
                                                                      void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(ring, 0, object_id, pChannel, DataSize, pData, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     ID3D11AuthenticatedChannel * pChannel,
                                                                     UINT DataSize,
                                                                     void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(ring, NPT_CMD_FLAG_REPLY, object_id, pChannel, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_reply(dec, pData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::QueryAuthenticatedChannel
 */

struct npt_command_ID3D11VideoContext_QueryAuthenticatedChannel {
    void *_self;
    ID3D11AuthenticatedChannel * pChannel;
    UINT InputSize;
    const void * pInput;
    UINT OutputSize;
    void * pOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_QueryAuthenticatedChannel(const ID3D11AuthenticatedChannel * pChannel,const UINT InputSize,const void * pInput,const UINT OutputSize,const void * pOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&InputSize, max_mode);
    if (pInput) {
        cmd_size += npt_sizeof_array_count(InputSize);
        cmd_size += npt_sizeof_blob_array(pInput, InputSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&OutputSize, max_mode);
/* skip pOutput (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_QueryAuthenticatedChannel(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D11AuthenticatedChannel * pChannel,
                                                        UINT InputSize,
                                                        const void * pInput,
                                                        UINT OutputSize,
                                                        void * pOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_QueryAuthenticatedChannel(pChannel,InputSize,pInput,OutputSize,pOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_QueryAuthenticatedChannel),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pChannel));
    npt_encode_UINT(enc, &InputSize);
    if (pInput) {
        npt_encode_array_count(enc, InputSize);
        npt_encode_blob_array(enc, pInput, InputSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &OutputSize);
/* skip pOutput (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_QueryAuthenticatedChannel_reply(const ID3D11AuthenticatedChannel * pChannel,const UINT InputSize,const void * pInput,const UINT OutputSize,const void * pOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pChannel (input only) */
/* skip InputSize (input only) */
/* skip pInput (input only) */
/* skip OutputSize (input only) */
    if (pOutput) {
        cmd_size += npt_sizeof_array_count(OutputSize);
        cmd_size += npt_sizeof_blob_array(pOutput, OutputSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_QueryAuthenticatedChannel_reply(struct npt_cs_decoder *dec,
                                                              void * pOutput,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_QueryAuthenticatedChannel))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_QueryAuthenticatedChannel reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_QueryAuthenticatedChannel));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pOutput)
            npt_decode_blob_array(dec, (void *)pOutput, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext_QueryAuthenticatedChannel(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D11AuthenticatedChannel * pChannel,
                                                        UINT InputSize,
                                                        const void * pInput,
                                                        UINT OutputSize,
                                                        void * pOutput,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_QueryAuthenticatedChannel(pChannel,InputSize,pInput,OutputSize,pOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_QueryAuthenticatedChannel_reply(pChannel,InputSize,pInput,OutputSize,pOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_QueryAuthenticatedChannel(enc, cmd_flags, object_id, pChannel, InputSize, pInput, OutputSize, pOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_QueryAuthenticatedChannel(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D11AuthenticatedChannel * pChannel,
                                                       UINT InputSize,
                                                       const void * pInput,
                                                       UINT OutputSize,
                                                       void * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_QueryAuthenticatedChannel(ring, 0, object_id, pChannel, InputSize, pInput, OutputSize, pOutput, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_QueryAuthenticatedChannel(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      ID3D11AuthenticatedChannel * pChannel,
                                                      UINT InputSize,
                                                      const void * pInput,
                                                      UINT OutputSize,
                                                      void * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_QueryAuthenticatedChannel(ring, NPT_CMD_FLAG_REPLY, object_id, pChannel, InputSize, pInput, OutputSize, pOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_QueryAuthenticatedChannel_reply(dec, pOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::ConfigureAuthenticatedChannel
 */

struct npt_command_ID3D11VideoContext_ConfigureAuthenticatedChannel {
    void *_self;
    ID3D11AuthenticatedChannel * pChannel;
    UINT InputSize;
    const void * pInput;
    D3D11_AUTHENTICATED_CONFIGURE_OUTPUT * pOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_ConfigureAuthenticatedChannel(const ID3D11AuthenticatedChannel * pChannel,const UINT InputSize,const void * pInput,const D3D11_AUTHENTICATED_CONFIGURE_OUTPUT * pOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&InputSize, max_mode);
    if (pInput) {
        cmd_size += npt_sizeof_array_count(InputSize);
        cmd_size += npt_sizeof_blob_array(pInput, InputSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip pOutput (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_ConfigureAuthenticatedChannel(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11AuthenticatedChannel * pChannel,
                                                            UINT InputSize,
                                                            const void * pInput,
                                                            D3D11_AUTHENTICATED_CONFIGURE_OUTPUT * pOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_ConfigureAuthenticatedChannel(pChannel,InputSize,pInput,pOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_ConfigureAuthenticatedChannel),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pChannel));
    npt_encode_UINT(enc, &InputSize);
    if (pInput) {
        npt_encode_array_count(enc, InputSize);
        npt_encode_blob_array(enc, pInput, InputSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pOutput (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_ConfigureAuthenticatedChannel_reply(const ID3D11AuthenticatedChannel * pChannel,const UINT InputSize,const void * pInput,const D3D11_AUTHENTICATED_CONFIGURE_OUTPUT * pOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pChannel (input only) */
/* skip InputSize (input only) */
/* skip pInput (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_AUTHENTICATED_CONFIGURE_OUTPUT(&(const D3D11_AUTHENTICATED_CONFIGURE_OUTPUT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_ConfigureAuthenticatedChannel_reply(struct npt_cs_decoder *dec,
                                                                  D3D11_AUTHENTICATED_CONFIGURE_OUTPUT * pOutput,
                                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_ConfigureAuthenticatedChannel))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_ConfigureAuthenticatedChannel reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_ConfigureAuthenticatedChannel));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pOutput)
            npt_decode_D3D11_AUTHENTICATED_CONFIGURE_OUTPUT(dec, (D3D11_AUTHENTICATED_CONFIGURE_OUTPUT *)pOutput);
    }
}

static inline void
npt_submit_ID3D11VideoContext_ConfigureAuthenticatedChannel(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11AuthenticatedChannel * pChannel,
                                                            UINT InputSize,
                                                            const void * pInput,
                                                            D3D11_AUTHENTICATED_CONFIGURE_OUTPUT * pOutput,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_ConfigureAuthenticatedChannel(pChannel,InputSize,pInput,pOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_ConfigureAuthenticatedChannel_reply(pChannel,InputSize,pInput,pOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_ConfigureAuthenticatedChannel(enc, cmd_flags, object_id, pChannel, InputSize, pInput, pOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_ConfigureAuthenticatedChannel(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11AuthenticatedChannel * pChannel,
                                                           UINT InputSize,
                                                           const void * pInput,
                                                           D3D11_AUTHENTICATED_CONFIGURE_OUTPUT * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_ConfigureAuthenticatedChannel(ring, 0, object_id, pChannel, InputSize, pInput, pOutput, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext_ConfigureAuthenticatedChannel(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D11AuthenticatedChannel * pChannel,
                                                          UINT InputSize,
                                                          const void * pInput,
                                                          D3D11_AUTHENTICATED_CONFIGURE_OUTPUT * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_ConfigureAuthenticatedChannel(ring, NPT_CMD_FLAG_REPLY, object_id, pChannel, InputSize, pInput, pOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext_ConfigureAuthenticatedChannel_reply(dec, pOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamRotation
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamRotation {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    D3D11_VIDEO_PROCESSOR_ROTATION Rotation;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamRotation(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const D3D11_VIDEO_PROCESSOR_ROTATION Rotation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_ROTATION(&Rotation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamRotation(struct npt_cs_encoder *enc,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL Enable,
                                                              D3D11_VIDEO_PROCESSOR_ROTATION Rotation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamRotation(pVideoProcessor,StreamIndex,Enable,Rotation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamRotation),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_BOOL(enc, &Enable);
    npt_encode_D3D11_VIDEO_PROCESSOR_ROTATION(enc, &Rotation);
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamRotation_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const D3D11_VIDEO_PROCESSOR_ROTATION Rotation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Enable (input only) */
/* skip Rotation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorSetStreamRotation(struct npt_ring *ring,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL Enable,
                                                              D3D11_VIDEO_PROCESSOR_ROTATION Rotation,
                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamRotation(pVideoProcessor,StreamIndex,Enable,Rotation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorSetStreamRotation_reply(pVideoProcessor,StreamIndex,Enable,Rotation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorSetStreamRotation(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Enable, Rotation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorSetStreamRotation(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL Enable,
                                                             D3D11_VIDEO_PROCESSOR_ROTATION Rotation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamRotation(ring, 0, object_id, pVideoProcessor, StreamIndex, Enable, Rotation, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorSetStreamRotation(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            BOOL Enable,
                                                            D3D11_VIDEO_PROCESSOR_ROTATION Rotation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorSetStreamRotation(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Enable, Rotation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamRotation
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamRotation {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnable;
    D3D11_VIDEO_PROCESSOR_ROTATION * pRotation;
};

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamRotation(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnable,const D3D11_VIDEO_PROCESSOR_ROTATION * pRotation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pEnable (output only) */
/* skip pRotation (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamRotation(struct npt_cs_encoder *enc,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL * pEnable,
                                                              D3D11_VIDEO_PROCESSOR_ROTATION * pRotation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamRotation(pVideoProcessor,StreamIndex,pEnable,pRotation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamRotation),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pEnable (output only) */
/* skip pRotation (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamRotation_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnable,const D3D11_VIDEO_PROCESSOR_ROTATION * pRotation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_ROTATION(&(const D3D11_VIDEO_PROCESSOR_ROTATION){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamRotation_reply(struct npt_cs_decoder *dec,
                                                                    BOOL * pEnable,
                                                                    D3D11_VIDEO_PROCESSOR_ROTATION * pRotation)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                                  NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamRotation))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext_VideoProcessorGetStreamRotation reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                       NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamRotation));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnable)
            npt_decode_BOOL(dec, (BOOL *)pEnable);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pRotation)
            npt_decode_D3D11_VIDEO_PROCESSOR_ROTATION(dec, (D3D11_VIDEO_PROCESSOR_ROTATION *)pRotation);
    }
}

static inline void
npt_submit_ID3D11VideoContext_VideoProcessorGetStreamRotation(struct npt_ring *ring,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT StreamIndex,
                                                              BOOL * pEnable,
                                                              D3D11_VIDEO_PROCESSOR_ROTATION * pRotation,
                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamRotation(pVideoProcessor,StreamIndex,pEnable,pRotation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext_VideoProcessorGetStreamRotation_reply(pVideoProcessor,StreamIndex,pEnable,pRotation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext_VideoProcessorGetStreamRotation(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pEnable, pRotation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext_VideoProcessorGetStreamRotation(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL * pEnable,
                                                             D3D11_VIDEO_PROCESSOR_ROTATION * pRotation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamRotation(ring, 0, object_id, pVideoProcessor, StreamIndex, pEnable, pRotation, &submit);
}

static inline void
npt_call_ID3D11VideoContext_VideoProcessorGetStreamRotation(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            BOOL * pEnable,
                                                            D3D11_VIDEO_PROCESSOR_ROTATION * pRotation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext_VideoProcessorGetStreamRotation(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pEnable, pRotation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext_VideoProcessorGetStreamRotation_reply(dec, pEnable, pRotation);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext1::SubmitDecoderBuffers1
 */

struct npt_command_ID3D11VideoContext1_SubmitDecoderBuffers1 {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    UINT NumBuffers;
    const D3D11_VIDEO_DECODER_BUFFER_DESC1 * pBufferDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_SubmitDecoderBuffers1(const ID3D11VideoDecoder * pDecoder,const UINT NumBuffers,const D3D11_VIDEO_DECODER_BUFFER_DESC1 * pBufferDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(pBufferDesc ? NumBuffers : 0);
    for (uint32_t _i = 0; _i < (pBufferDesc ? NumBuffers : 0); _i++)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_BUFFER_DESC1(&pBufferDesc[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_SubmitDecoderBuffers1(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11VideoDecoder * pDecoder,
                                                     UINT NumBuffers,
                                                     const D3D11_VIDEO_DECODER_BUFFER_DESC1 * pBufferDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_SubmitDecoderBuffers1(pDecoder,NumBuffers,pBufferDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_SubmitDecoderBuffers1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    npt_encode_UINT(enc, &NumBuffers);
    if (pBufferDesc) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)(NumBuffers); _i++)
            npt_encode_D3D11_VIDEO_DECODER_BUFFER_DESC1(enc, &pBufferDesc[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_SubmitDecoderBuffers1_reply(const ID3D11VideoDecoder * pDecoder,const UINT NumBuffers,const D3D11_VIDEO_DECODER_BUFFER_DESC1 * pBufferDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip NumBuffers (input only) */
/* skip pBufferDesc (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_SubmitDecoderBuffers1_reply(struct npt_cs_decoder *dec,
                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_SubmitDecoderBuffers1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_SubmitDecoderBuffers1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_SubmitDecoderBuffers1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext1_SubmitDecoderBuffers1(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11VideoDecoder * pDecoder,
                                                     UINT NumBuffers,
                                                     const D3D11_VIDEO_DECODER_BUFFER_DESC1 * pBufferDesc,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_SubmitDecoderBuffers1(pDecoder,NumBuffers,pBufferDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_SubmitDecoderBuffers1_reply(pDecoder,NumBuffers,pBufferDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_SubmitDecoderBuffers1(enc, cmd_flags, object_id, pDecoder, NumBuffers, pBufferDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_SubmitDecoderBuffers1(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11VideoDecoder * pDecoder,
                                                    UINT NumBuffers,
                                                    const D3D11_VIDEO_DECODER_BUFFER_DESC1 * pBufferDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_SubmitDecoderBuffers1(ring, 0, object_id, pDecoder, NumBuffers, pBufferDesc, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext1_SubmitDecoderBuffers1(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11VideoDecoder * pDecoder,
                                                   UINT NumBuffers,
                                                   const D3D11_VIDEO_DECODER_BUFFER_DESC1 * pBufferDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_SubmitDecoderBuffers1(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, NumBuffers, pBufferDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext1_SubmitDecoderBuffers1_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext1::GetDataForNewHardwareKey
 */

struct npt_command_ID3D11VideoContext1_GetDataForNewHardwareKey {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    UINT PrivateInputSize;
    const void * pPrivatInputData;
    UINT64 * pPrivateOutputData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_GetDataForNewHardwareKey(const ID3D11CryptoSession * pCryptoSession,const UINT PrivateInputSize,const void * pPrivatInputData,const UINT64 * pPrivateOutputData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&PrivateInputSize, max_mode);
    if (pPrivatInputData) {
        cmd_size += npt_sizeof_array_count(PrivateInputSize);
        cmd_size += npt_sizeof_blob_array(pPrivatInputData, PrivateInputSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip pPrivateOutputData (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_GetDataForNewHardwareKey(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D11CryptoSession * pCryptoSession,
                                                        UINT PrivateInputSize,
                                                        const void * pPrivatInputData,
                                                        UINT64 * pPrivateOutputData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_GetDataForNewHardwareKey(pCryptoSession,PrivateInputSize,pPrivatInputData,pPrivateOutputData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_GetDataForNewHardwareKey),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCryptoSession));
    npt_encode_UINT(enc, &PrivateInputSize);
    if (pPrivatInputData) {
        npt_encode_array_count(enc, PrivateInputSize);
        npt_encode_blob_array(enc, pPrivatInputData, PrivateInputSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pPrivateOutputData (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_GetDataForNewHardwareKey_reply(const ID3D11CryptoSession * pCryptoSession,const UINT PrivateInputSize,const void * pPrivatInputData,const UINT64 * pPrivateOutputData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoSession (input only) */
/* skip PrivateInputSize (input only) */
/* skip pPrivatInputData (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT64(&(const UINT64){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_GetDataForNewHardwareKey_reply(struct npt_cs_decoder *dec,
                                                              UINT64 * pPrivateOutputData,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_GetDataForNewHardwareKey))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_GetDataForNewHardwareKey reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_GetDataForNewHardwareKey));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pPrivateOutputData)
            npt_decode_UINT64(dec, (UINT64 *)pPrivateOutputData);
    }
}

static inline void
npt_submit_ID3D11VideoContext1_GetDataForNewHardwareKey(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D11CryptoSession * pCryptoSession,
                                                        UINT PrivateInputSize,
                                                        const void * pPrivatInputData,
                                                        UINT64 * pPrivateOutputData,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_GetDataForNewHardwareKey(pCryptoSession,PrivateInputSize,pPrivatInputData,pPrivateOutputData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_GetDataForNewHardwareKey_reply(pCryptoSession,PrivateInputSize,pPrivatInputData,pPrivateOutputData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_GetDataForNewHardwareKey(enc, cmd_flags, object_id, pCryptoSession, PrivateInputSize, pPrivatInputData, pPrivateOutputData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_GetDataForNewHardwareKey(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D11CryptoSession * pCryptoSession,
                                                       UINT PrivateInputSize,
                                                       const void * pPrivatInputData,
                                                       UINT64 * pPrivateOutputData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_GetDataForNewHardwareKey(ring, 0, object_id, pCryptoSession, PrivateInputSize, pPrivatInputData, pPrivateOutputData, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext1_GetDataForNewHardwareKey(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      ID3D11CryptoSession * pCryptoSession,
                                                      UINT PrivateInputSize,
                                                      const void * pPrivatInputData,
                                                      UINT64 * pPrivateOutputData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_GetDataForNewHardwareKey(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoSession, PrivateInputSize, pPrivatInputData, pPrivateOutputData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext1_GetDataForNewHardwareKey_reply(dec, pPrivateOutputData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext1::CheckCryptoSessionStatus
 */

struct npt_command_ID3D11VideoContext1_CheckCryptoSessionStatus {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    D3D11_CRYPTO_SESSION_STATUS * pStatus;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_CheckCryptoSessionStatus(const ID3D11CryptoSession * pCryptoSession,const D3D11_CRYPTO_SESSION_STATUS * pStatus)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pStatus (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_CheckCryptoSessionStatus(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D11CryptoSession * pCryptoSession,
                                                        D3D11_CRYPTO_SESSION_STATUS * pStatus)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_CheckCryptoSessionStatus(pCryptoSession,pStatus);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_CheckCryptoSessionStatus),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCryptoSession));
/* skip pStatus (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_CheckCryptoSessionStatus_reply(const ID3D11CryptoSession * pCryptoSession,const D3D11_CRYPTO_SESSION_STATUS * pStatus)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoSession (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_CRYPTO_SESSION_STATUS(&(const D3D11_CRYPTO_SESSION_STATUS){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_CheckCryptoSessionStatus_reply(struct npt_cs_decoder *dec,
                                                              D3D11_CRYPTO_SESSION_STATUS * pStatus,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_CheckCryptoSessionStatus))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_CheckCryptoSessionStatus reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_CheckCryptoSessionStatus));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pStatus)
            npt_decode_D3D11_CRYPTO_SESSION_STATUS(dec, (D3D11_CRYPTO_SESSION_STATUS *)pStatus);
    }
}

static inline void
npt_submit_ID3D11VideoContext1_CheckCryptoSessionStatus(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D11CryptoSession * pCryptoSession,
                                                        D3D11_CRYPTO_SESSION_STATUS * pStatus,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_CheckCryptoSessionStatus(pCryptoSession,pStatus);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_CheckCryptoSessionStatus_reply(pCryptoSession,pStatus)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_CheckCryptoSessionStatus(enc, cmd_flags, object_id, pCryptoSession, pStatus);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_CheckCryptoSessionStatus(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D11CryptoSession * pCryptoSession,
                                                       D3D11_CRYPTO_SESSION_STATUS * pStatus)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_CheckCryptoSessionStatus(ring, 0, object_id, pCryptoSession, pStatus, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext1_CheckCryptoSessionStatus(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      ID3D11CryptoSession * pCryptoSession,
                                                      D3D11_CRYPTO_SESSION_STATUS * pStatus)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_CheckCryptoSessionStatus(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoSession, pStatus, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext1_CheckCryptoSessionStatus_reply(dec, pStatus, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext1::DecoderEnableDownsampling
 */

struct npt_command_ID3D11VideoContext1_DecoderEnableDownsampling {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    DXGI_COLOR_SPACE_TYPE InputColorSpace;
    const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc;
    UINT ReferenceFrameCount;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_DecoderEnableDownsampling(const ID3D11VideoDecoder * pDecoder,const DXGI_COLOR_SPACE_TYPE InputColorSpace,const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,const UINT ReferenceFrameCount)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&InputColorSpace, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOutputDesc);
    if (pOutputDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_SAMPLE_DESC(pOutputDesc, max_mode);
    cmd_size += npt_sizeof_UINT(&ReferenceFrameCount, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_DecoderEnableDownsampling(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D11VideoDecoder * pDecoder,
                                                         DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                         const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,
                                                         UINT ReferenceFrameCount)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_DecoderEnableDownsampling(pDecoder,InputColorSpace,pOutputDesc,ReferenceFrameCount);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_DecoderEnableDownsampling),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &InputColorSpace);
    if (npt_encode_simple_pointer(enc, pOutputDesc))
        npt_encode_D3D11_VIDEO_SAMPLE_DESC(enc, pOutputDesc);
    npt_encode_UINT(enc, &ReferenceFrameCount);
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_DecoderEnableDownsampling_reply(const ID3D11VideoDecoder * pDecoder,const DXGI_COLOR_SPACE_TYPE InputColorSpace,const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,const UINT ReferenceFrameCount)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip InputColorSpace (input only) */
/* skip pOutputDesc (input only) */
/* skip ReferenceFrameCount (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_DecoderEnableDownsampling_reply(struct npt_cs_decoder *dec,
                                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_DecoderEnableDownsampling))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_DecoderEnableDownsampling reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_DecoderEnableDownsampling));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext1_DecoderEnableDownsampling(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D11VideoDecoder * pDecoder,
                                                         DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                         const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,
                                                         UINT ReferenceFrameCount,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_DecoderEnableDownsampling(pDecoder,InputColorSpace,pOutputDesc,ReferenceFrameCount);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_DecoderEnableDownsampling_reply(pDecoder,InputColorSpace,pOutputDesc,ReferenceFrameCount)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_DecoderEnableDownsampling(enc, cmd_flags, object_id, pDecoder, InputColorSpace, pOutputDesc, ReferenceFrameCount);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_DecoderEnableDownsampling(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        ID3D11VideoDecoder * pDecoder,
                                                        DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                        const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,
                                                        UINT ReferenceFrameCount)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_DecoderEnableDownsampling(ring, 0, object_id, pDecoder, InputColorSpace, pOutputDesc, ReferenceFrameCount, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext1_DecoderEnableDownsampling(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D11VideoDecoder * pDecoder,
                                                       DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                       const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,
                                                       UINT ReferenceFrameCount)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_DecoderEnableDownsampling(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, InputColorSpace, pOutputDesc, ReferenceFrameCount, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext1_DecoderEnableDownsampling_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext1::DecoderUpdateDownsampling
 */

struct npt_command_ID3D11VideoContext1_DecoderUpdateDownsampling {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_DecoderUpdateDownsampling(const ID3D11VideoDecoder * pDecoder,const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pOutputDesc);
    if (pOutputDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_SAMPLE_DESC(pOutputDesc, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_DecoderUpdateDownsampling(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D11VideoDecoder * pDecoder,
                                                         const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_DecoderUpdateDownsampling(pDecoder,pOutputDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_DecoderUpdateDownsampling),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    if (npt_encode_simple_pointer(enc, pOutputDesc))
        npt_encode_D3D11_VIDEO_SAMPLE_DESC(enc, pOutputDesc);
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_DecoderUpdateDownsampling_reply(const ID3D11VideoDecoder * pDecoder,const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip pOutputDesc (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_DecoderUpdateDownsampling_reply(struct npt_cs_decoder *dec,
                                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_DecoderUpdateDownsampling))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_DecoderUpdateDownsampling reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_DecoderUpdateDownsampling));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext1_DecoderUpdateDownsampling(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D11VideoDecoder * pDecoder,
                                                         const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_DecoderUpdateDownsampling(pDecoder,pOutputDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_DecoderUpdateDownsampling_reply(pDecoder,pOutputDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_DecoderUpdateDownsampling(enc, cmd_flags, object_id, pDecoder, pOutputDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_DecoderUpdateDownsampling(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        ID3D11VideoDecoder * pDecoder,
                                                        const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_DecoderUpdateDownsampling(ring, 0, object_id, pDecoder, pOutputDesc, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext1_DecoderUpdateDownsampling(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D11VideoDecoder * pDecoder,
                                                       const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_DecoderUpdateDownsampling(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, pOutputDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext1_DecoderUpdateDownsampling_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext1::VideoProcessorSetOutputColorSpace1
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1 {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(const ID3D11VideoProcessor * pVideoProcessor,const DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&ColorSpace, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(pVideoProcessor,ColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &ColorSpace);
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1_reply(const ID3D11VideoProcessor * pVideoProcessor,const DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip ColorSpace (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(pVideoProcessor,ColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1_reply(pVideoProcessor,ColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(enc, cmd_flags, object_id, pVideoProcessor, ColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(ring, 0, object_id, pVideoProcessor, ColorSpace, &submit);
}

static inline void
npt_call_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, ColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext1::VideoProcessorSetOutputShaderUsage
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL ShaderUsage;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(const ID3D11VideoProcessor * pVideoProcessor,const BOOL ShaderUsage)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_BOOL(&ShaderUsage, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  BOOL ShaderUsage)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(pVideoProcessor,ShaderUsage);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_BOOL(enc, &ShaderUsage);
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL ShaderUsage)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip ShaderUsage (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  BOOL ShaderUsage,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(pVideoProcessor,ShaderUsage);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage_reply(pVideoProcessor,ShaderUsage)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(enc, cmd_flags, object_id, pVideoProcessor, ShaderUsage);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 BOOL ShaderUsage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(ring, 0, object_id, pVideoProcessor, ShaderUsage, &submit);
}

static inline void
npt_call_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL ShaderUsage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, ShaderUsage, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext1::VideoProcessorGetOutputColorSpace1
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1 {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    DXGI_COLOR_SPACE_TYPE * pColorSpace;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(const ID3D11VideoProcessor * pVideoProcessor,const DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pColorSpace (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(pVideoProcessor,pColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
/* skip pColorSpace (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_reply(const ID3D11VideoProcessor * pVideoProcessor,const DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&(const DXGI_COLOR_SPACE_TYPE){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_reply(struct npt_cs_decoder *dec,
                                                                        DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pColorSpace)
            npt_decode_DXGI_COLOR_SPACE_TYPE(dec, (DXGI_COLOR_SPACE_TYPE *)pColorSpace);
    }
}

static inline void
npt_submit_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  DXGI_COLOR_SPACE_TYPE * pColorSpace,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(pVideoProcessor,pColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_reply(pVideoProcessor,pColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(enc, cmd_flags, object_id, pVideoProcessor, pColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(ring, 0, object_id, pVideoProcessor, pColorSpace, &submit);
}

static inline void
npt_call_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_reply(dec, pColorSpace);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext1::VideoProcessorGetOutputShaderUsage
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * pShaderUsage;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * pShaderUsage)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pShaderUsage (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  BOOL * pShaderUsage)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(pVideoProcessor,pShaderUsage);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
/* skip pShaderUsage (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_reply(const ID3D11VideoProcessor * pVideoProcessor,const BOOL * pShaderUsage)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_reply(struct npt_cs_decoder *dec,
                                                                        BOOL * pShaderUsage)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pShaderUsage)
            npt_decode_BOOL(dec, (BOOL *)pShaderUsage);
    }
}

static inline void
npt_submit_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  BOOL * pShaderUsage,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(pVideoProcessor,pShaderUsage);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_reply(pVideoProcessor,pShaderUsage)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(enc, cmd_flags, object_id, pVideoProcessor, pShaderUsage);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 BOOL * pShaderUsage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(ring, 0, object_id, pVideoProcessor, pShaderUsage, &submit);
}

static inline void
npt_call_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                BOOL * pShaderUsage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pShaderUsage, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_reply(dec, pShaderUsage);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext1::VideoProcessorSetStreamColorSpace1
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1 {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&ColorSpace, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(pVideoProcessor,StreamIndex,ColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &ColorSpace);
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip ColorSpace (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  DXGI_COLOR_SPACE_TYPE ColorSpace,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(pVideoProcessor,StreamIndex,ColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1_reply(pVideoProcessor,StreamIndex,ColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, ColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(ring, 0, object_id, pVideoProcessor, StreamIndex, ColorSpace, &submit);
}

static inline void
npt_call_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                DXGI_COLOR_SPACE_TYPE ColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, ColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext1::VideoProcessorSetStreamMirror
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamMirror {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    BOOL FlipHorizontal;
    BOOL FlipVertical;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamMirror(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const BOOL FlipHorizontal,const BOOL FlipVertical)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_BOOL(&Enable, max_mode);
    cmd_size += npt_sizeof_BOOL(&FlipHorizontal, max_mode);
    cmd_size += npt_sizeof_BOOL(&FlipVertical, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorSetStreamMirror(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL Enable,
                                                             BOOL FlipHorizontal,
                                                             BOOL FlipVertical)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamMirror(pVideoProcessor,StreamIndex,Enable,FlipHorizontal,FlipVertical);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorSetStreamMirror),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_BOOL(enc, &Enable);
    npt_encode_BOOL(enc, &FlipHorizontal);
    npt_encode_BOOL(enc, &FlipVertical);
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamMirror_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL Enable,const BOOL FlipHorizontal,const BOOL FlipVertical)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Enable (input only) */
/* skip FlipHorizontal (input only) */
/* skip FlipVertical (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext1_VideoProcessorSetStreamMirror(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL Enable,
                                                             BOOL FlipHorizontal,
                                                             BOOL FlipVertical,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamMirror(pVideoProcessor,StreamIndex,Enable,FlipHorizontal,FlipVertical);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_VideoProcessorSetStreamMirror_reply(pVideoProcessor,StreamIndex,Enable,FlipHorizontal,FlipVertical)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_VideoProcessorSetStreamMirror(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Enable, FlipHorizontal, FlipVertical);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_VideoProcessorSetStreamMirror(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            BOOL Enable,
                                                            BOOL FlipHorizontal,
                                                            BOOL FlipVertical)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorSetStreamMirror(ring, 0, object_id, pVideoProcessor, StreamIndex, Enable, FlipHorizontal, FlipVertical, &submit);
}

static inline void
npt_call_ID3D11VideoContext1_VideoProcessorSetStreamMirror(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           BOOL Enable,
                                                           BOOL FlipHorizontal,
                                                           BOOL FlipVertical)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorSetStreamMirror(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Enable, FlipHorizontal, FlipVertical, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext1::VideoProcessorGetStreamColorSpace1
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1 {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    DXGI_COLOR_SPACE_TYPE * pColorSpace;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pColorSpace (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(pVideoProcessor,StreamIndex,pColorSpace);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pColorSpace (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&(const DXGI_COLOR_SPACE_TYPE){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_reply(struct npt_cs_decoder *dec,
                                                                        DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pColorSpace)
            npt_decode_DXGI_COLOR_SPACE_TYPE(dec, (DXGI_COLOR_SPACE_TYPE *)pColorSpace);
    }
}

static inline void
npt_submit_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  DXGI_COLOR_SPACE_TYPE * pColorSpace,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(pVideoProcessor,StreamIndex,pColorSpace);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_reply(pVideoProcessor,StreamIndex,pColorSpace)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pColorSpace);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(ring, 0, object_id, pVideoProcessor, StreamIndex, pColorSpace, &submit);
}

static inline void
npt_call_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                DXGI_COLOR_SPACE_TYPE * pColorSpace)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pColorSpace, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_reply(dec, pColorSpace);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext1::VideoProcessorGetStreamMirror
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamMirror {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnable;
    BOOL * pFlipHorizontal;
    BOOL * pFlipVertical;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamMirror(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnable,const BOOL * pFlipHorizontal,const BOOL * pFlipVertical)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pEnable (output only) */
/* skip pFlipHorizontal (output only) */
/* skip pFlipVertical (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetStreamMirror(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL * pEnable,
                                                             BOOL * pFlipHorizontal,
                                                             BOOL * pFlipVertical)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamMirror(pVideoProcessor,StreamIndex,pEnable,pFlipHorizontal,pFlipVertical);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetStreamMirror),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pEnable (output only) */
/* skip pFlipHorizontal (output only) */
/* skip pFlipVertical (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamMirror_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const BOOL * pEnable,const BOOL * pFlipHorizontal,const BOOL * pFlipVertical)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetStreamMirror_reply(struct npt_cs_decoder *dec,
                                                                   BOOL * pEnable,
                                                                   BOOL * pFlipHorizontal,
                                                                   BOOL * pFlipVertical)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetStreamMirror))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_VideoProcessorGetStreamMirror reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetStreamMirror));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pEnable)
            npt_decode_BOOL(dec, (BOOL *)pEnable);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pFlipHorizontal)
            npt_decode_BOOL(dec, (BOOL *)pFlipHorizontal);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pFlipVertical)
            npt_decode_BOOL(dec, (BOOL *)pFlipVertical);
    }
}

static inline void
npt_submit_ID3D11VideoContext1_VideoProcessorGetStreamMirror(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT StreamIndex,
                                                             BOOL * pEnable,
                                                             BOOL * pFlipHorizontal,
                                                             BOOL * pFlipVertical,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamMirror(pVideoProcessor,StreamIndex,pEnable,pFlipHorizontal,pFlipVertical);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_VideoProcessorGetStreamMirror_reply(pVideoProcessor,StreamIndex,pEnable,pFlipHorizontal,pFlipVertical)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_VideoProcessorGetStreamMirror(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pEnable, pFlipHorizontal, pFlipVertical);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_VideoProcessorGetStreamMirror(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT StreamIndex,
                                                            BOOL * pEnable,
                                                            BOOL * pFlipHorizontal,
                                                            BOOL * pFlipVertical)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetStreamMirror(ring, 0, object_id, pVideoProcessor, StreamIndex, pEnable, pFlipHorizontal, pFlipVertical, &submit);
}

static inline void
npt_call_ID3D11VideoContext1_VideoProcessorGetStreamMirror(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11VideoProcessor * pVideoProcessor,
                                                           UINT StreamIndex,
                                                           BOOL * pEnable,
                                                           BOOL * pFlipHorizontal,
                                                           BOOL * pFlipVertical)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetStreamMirror(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pEnable, pFlipHorizontal, pFlipVertical, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext1_VideoProcessorGetStreamMirror_reply(dec, pEnable, pFlipHorizontal, pFlipVertical);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext1::VideoProcessorGetBehaviorHints
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetBehaviorHints {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT OutputWidth;
    UINT OutputHeight;
    DXGI_FORMAT OutputFormat;
    UINT StreamCount;
    const D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT * pStreams;
    UINT * pBehaviorHints;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(const ID3D11VideoProcessor * pVideoProcessor,const UINT OutputWidth,const UINT OutputHeight,const DXGI_FORMAT OutputFormat,const UINT StreamCount,const D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT * pStreams,const UINT * pBehaviorHints)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&OutputWidth, max_mode);
    cmd_size += npt_sizeof_UINT(&OutputHeight, max_mode);
    cmd_size += npt_sizeof_DXGI_FORMAT(&OutputFormat, max_mode);
    cmd_size += npt_sizeof_UINT(&StreamCount, max_mode);
    cmd_size += npt_sizeof_array_count(pStreams ? StreamCount : 0);
    for (uint32_t _i = 0; _i < (pStreams ? StreamCount : 0); _i++)
        cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT(&pStreams[_i], max_mode);
/* skip pBehaviorHints (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(struct npt_cs_encoder *enc,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT OutputWidth,
                                                              UINT OutputHeight,
                                                              DXGI_FORMAT OutputFormat,
                                                              UINT StreamCount,
                                                              const D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT * pStreams,
                                                              UINT * pBehaviorHints)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(pVideoProcessor,OutputWidth,OutputHeight,OutputFormat,StreamCount,pStreams,pBehaviorHints);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetBehaviorHints),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &OutputWidth);
    npt_encode_UINT(enc, &OutputHeight);
    npt_encode_DXGI_FORMAT(enc, &OutputFormat);
    npt_encode_UINT(enc, &StreamCount);
    if (pStreams) {
        npt_encode_array_count(enc, StreamCount);
        for (uint32_t _i = 0; _i < (uint32_t)(StreamCount); _i++)
            npt_encode_D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT(enc, &pStreams[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
/* skip pBehaviorHints (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT OutputWidth,const UINT OutputHeight,const DXGI_FORMAT OutputFormat,const UINT StreamCount,const D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT * pStreams,const UINT * pBehaviorHints)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip OutputWidth (input only) */
/* skip OutputHeight (input only) */
/* skip OutputFormat (input only) */
/* skip StreamCount (input only) */
/* skip pStreams (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_reply(struct npt_cs_decoder *dec,
                                                                    UINT * pBehaviorHints,
                                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                                  NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetBehaviorHints))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext1_VideoProcessorGetBehaviorHints reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                       NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetBehaviorHints));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pBehaviorHints)
            npt_decode_UINT(dec, (UINT *)pBehaviorHints);
    }
}

static inline void
npt_submit_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(struct npt_ring *ring,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D11VideoProcessor * pVideoProcessor,
                                                              UINT OutputWidth,
                                                              UINT OutputHeight,
                                                              DXGI_FORMAT OutputFormat,
                                                              UINT StreamCount,
                                                              const D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT * pStreams,
                                                              UINT * pBehaviorHints,
                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(pVideoProcessor,OutputWidth,OutputHeight,OutputFormat,StreamCount,pStreams,pBehaviorHints);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_reply(pVideoProcessor,OutputWidth,OutputHeight,OutputFormat,StreamCount,pStreams,pBehaviorHints)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(enc, cmd_flags, object_id, pVideoProcessor, OutputWidth, OutputHeight, OutputFormat, StreamCount, pStreams, pBehaviorHints);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D11VideoProcessor * pVideoProcessor,
                                                             UINT OutputWidth,
                                                             UINT OutputHeight,
                                                             DXGI_FORMAT OutputFormat,
                                                             UINT StreamCount,
                                                             const D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT * pStreams,
                                                             UINT * pBehaviorHints)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(ring, 0, object_id, pVideoProcessor, OutputWidth, OutputHeight, OutputFormat, StreamCount, pStreams, pBehaviorHints, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D11VideoProcessor * pVideoProcessor,
                                                            UINT OutputWidth,
                                                            UINT OutputHeight,
                                                            DXGI_FORMAT OutputFormat,
                                                            UINT StreamCount,
                                                            const D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT * pStreams,
                                                            UINT * pBehaviorHints)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, OutputWidth, OutputHeight, OutputFormat, StreamCount, pStreams, pBehaviorHints, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_reply(dec, pBehaviorHints, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext2::VideoProcessorSetOutputHDRMetaData
 */

struct npt_command_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    DXGI_HDR_METADATA_TYPE Type;
    UINT Size;
    const void * pHDRMetaData;
};

static inline size_t
npt_sizeof_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(const ID3D11VideoProcessor * pVideoProcessor,const DXGI_HDR_METADATA_TYPE Type,const UINT Size,const void * pHDRMetaData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_DXGI_HDR_METADATA_TYPE(&Type, max_mode);
    cmd_size += npt_sizeof_UINT(&Size, max_mode);
    if (pHDRMetaData) {
        cmd_size += npt_sizeof_array_count(Size);
        cmd_size += npt_sizeof_blob_array(pHDRMetaData, Size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  DXGI_HDR_METADATA_TYPE Type,
                                                                  UINT Size,
                                                                  const void * pHDRMetaData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(pVideoProcessor,Type,Size,pHDRMetaData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                 NPT_METHOD_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_DXGI_HDR_METADATA_TYPE(enc, &Type);
    npt_encode_UINT(enc, &Size);
    if (pHDRMetaData) {
        npt_encode_array_count(enc, Size);
        npt_encode_blob_array(enc, pHDRMetaData, Size);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData_reply(const ID3D11VideoProcessor * pVideoProcessor,const DXGI_HDR_METADATA_TYPE Type,const UINT Size,const void * pHDRMetaData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip Type (input only) */
/* skip Size (input only) */
/* skip pHDRMetaData (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  DXGI_HDR_METADATA_TYPE Type,
                                                                  UINT Size,
                                                                  const void * pHDRMetaData,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(pVideoProcessor,Type,Size,pHDRMetaData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData_reply(pVideoProcessor,Type,Size,pHDRMetaData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(enc, cmd_flags, object_id, pVideoProcessor, Type, Size, pHDRMetaData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 DXGI_HDR_METADATA_TYPE Type,
                                                                 UINT Size,
                                                                 const void * pHDRMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(ring, 0, object_id, pVideoProcessor, Type, Size, pHDRMetaData, &submit);
}

static inline void
npt_call_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                DXGI_HDR_METADATA_TYPE Type,
                                                                UINT Size,
                                                                const void * pHDRMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, Type, Size, pHDRMetaData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext2::VideoProcessorGetOutputHDRMetaData
 */

struct npt_command_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    DXGI_HDR_METADATA_TYPE * pType;
    UINT Size;
    void * pMetaData;
};

static inline size_t
npt_sizeof_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(const ID3D11VideoProcessor * pVideoProcessor,const DXGI_HDR_METADATA_TYPE * pType,const UINT Size,const void * pMetaData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pType (output only) */
    cmd_size += npt_sizeof_UINT(&Size, max_mode);
/* skip pMetaData (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  DXGI_HDR_METADATA_TYPE * pType,
                                                                  UINT Size,
                                                                  void * pMetaData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(pVideoProcessor,pType,Size,pMetaData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                 NPT_METHOD_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
/* skip pType (output only) */
    npt_encode_UINT(enc, &Size);
/* skip pMetaData (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_reply(const ID3D11VideoProcessor * pVideoProcessor,const DXGI_HDR_METADATA_TYPE * pType,const UINT Size,const void * pMetaData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_HDR_METADATA_TYPE(&(const DXGI_HDR_METADATA_TYPE){0}, 1);
/* skip Size (input only) */
    if (pMetaData) {
        cmd_size += npt_sizeof_array_count(Size);
        cmd_size += npt_sizeof_blob_array(pMetaData, Size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_reply(struct npt_cs_decoder *dec,
                                                                        DXGI_HDR_METADATA_TYPE * pType,
                                                                        void * pMetaData)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                                  NPT_METHOD_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                       NPT_METHOD_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pType)
            npt_decode_DXGI_HDR_METADATA_TYPE(dec, (DXGI_HDR_METADATA_TYPE *)pType);
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pMetaData)
            npt_decode_blob_array(dec, (void *)pMetaData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  DXGI_HDR_METADATA_TYPE * pType,
                                                                  UINT Size,
                                                                  void * pMetaData,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(pVideoProcessor,pType,Size,pMetaData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_reply(pVideoProcessor,pType,Size,pMetaData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(enc, cmd_flags, object_id, pVideoProcessor, pType, Size, pMetaData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 DXGI_HDR_METADATA_TYPE * pType,
                                                                 UINT Size,
                                                                 void * pMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(ring, 0, object_id, pVideoProcessor, pType, Size, pMetaData, &submit);
}

static inline void
npt_call_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                DXGI_HDR_METADATA_TYPE * pType,
                                                                UINT Size,
                                                                void * pMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, pType, Size, pMetaData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_reply(dec, pType, pMetaData);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext2::VideoProcessorSetStreamHDRMetaData
 */

struct npt_command_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    DXGI_HDR_METADATA_TYPE Type;
    UINT Size;
    const void * pHDRMetaData;
};

static inline size_t
npt_sizeof_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const DXGI_HDR_METADATA_TYPE Type,const UINT Size,const void * pHDRMetaData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
    cmd_size += npt_sizeof_DXGI_HDR_METADATA_TYPE(&Type, max_mode);
    cmd_size += npt_sizeof_UINT(&Size, max_mode);
    if (pHDRMetaData) {
        cmd_size += npt_sizeof_array_count(Size);
        cmd_size += npt_sizeof_blob_array(pHDRMetaData, Size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  DXGI_HDR_METADATA_TYPE Type,
                                                                  UINT Size,
                                                                  const void * pHDRMetaData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(pVideoProcessor,StreamIndex,Type,Size,pHDRMetaData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                 NPT_METHOD_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
    npt_encode_DXGI_HDR_METADATA_TYPE(enc, &Type);
    npt_encode_UINT(enc, &Size);
    if (pHDRMetaData) {
        npt_encode_array_count(enc, Size);
        npt_encode_blob_array(enc, pHDRMetaData, Size);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const DXGI_HDR_METADATA_TYPE Type,const UINT Size,const void * pHDRMetaData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
/* skip Type (input only) */
/* skip Size (input only) */
/* skip pHDRMetaData (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  DXGI_HDR_METADATA_TYPE Type,
                                                                  UINT Size,
                                                                  const void * pHDRMetaData,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(pVideoProcessor,StreamIndex,Type,Size,pHDRMetaData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData_reply(pVideoProcessor,StreamIndex,Type,Size,pHDRMetaData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, Type, Size, pHDRMetaData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 DXGI_HDR_METADATA_TYPE Type,
                                                                 UINT Size,
                                                                 const void * pHDRMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(ring, 0, object_id, pVideoProcessor, StreamIndex, Type, Size, pHDRMetaData, &submit);
}

static inline void
npt_call_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                DXGI_HDR_METADATA_TYPE Type,
                                                                UINT Size,
                                                                const void * pHDRMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, Type, Size, pHDRMetaData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext2::VideoProcessorGetStreamHDRMetaData
 */

struct npt_command_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    DXGI_HDR_METADATA_TYPE * pType;
    UINT Size;
    void * pMetaData;
};

static inline size_t
npt_sizeof_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const DXGI_HDR_METADATA_TYPE * pType,const UINT Size,const void * pMetaData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&StreamIndex, max_mode);
/* skip pType (output only) */
    cmd_size += npt_sizeof_UINT(&Size, max_mode);
/* skip pMetaData (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  DXGI_HDR_METADATA_TYPE * pType,
                                                                  UINT Size,
                                                                  void * pMetaData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(pVideoProcessor,StreamIndex,pType,Size,pMetaData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                 NPT_METHOD_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pVideoProcessor));
    npt_encode_UINT(enc, &StreamIndex);
/* skip pType (output only) */
    npt_encode_UINT(enc, &Size);
/* skip pMetaData (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_reply(const ID3D11VideoProcessor * pVideoProcessor,const UINT StreamIndex,const DXGI_HDR_METADATA_TYPE * pType,const UINT Size,const void * pMetaData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoProcessor (input only) */
/* skip StreamIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DXGI_HDR_METADATA_TYPE(&(const DXGI_HDR_METADATA_TYPE){0}, 1);
/* skip Size (input only) */
    if (pMetaData) {
        cmd_size += npt_sizeof_array_count(Size);
        cmd_size += npt_sizeof_blob_array(pMetaData, Size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_reply(struct npt_cs_decoder *dec,
                                                                        DXGI_HDR_METADATA_TYPE * pType,
                                                                        void * pMetaData)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                                  NPT_METHOD_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                       NPT_METHOD_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData));
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pType)
            npt_decode_DXGI_HDR_METADATA_TYPE(dec, (DXGI_HDR_METADATA_TYPE *)pType);
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pMetaData)
            npt_decode_blob_array(dec, (void *)pMetaData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11VideoProcessor * pVideoProcessor,
                                                                  UINT StreamIndex,
                                                                  DXGI_HDR_METADATA_TYPE * pType,
                                                                  UINT Size,
                                                                  void * pMetaData,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(pVideoProcessor,StreamIndex,pType,Size,pMetaData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_reply(pVideoProcessor,StreamIndex,pType,Size,pMetaData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(enc, cmd_flags, object_id, pVideoProcessor, StreamIndex, pType, Size, pMetaData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11VideoProcessor * pVideoProcessor,
                                                                 UINT StreamIndex,
                                                                 DXGI_HDR_METADATA_TYPE * pType,
                                                                 UINT Size,
                                                                 void * pMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(ring, 0, object_id, pVideoProcessor, StreamIndex, pType, Size, pMetaData, &submit);
}

static inline void
npt_call_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11VideoProcessor * pVideoProcessor,
                                                                UINT StreamIndex,
                                                                DXGI_HDR_METADATA_TYPE * pType,
                                                                UINT Size,
                                                                void * pMetaData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoProcessor, StreamIndex, pType, Size, pMetaData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_decode_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_reply(dec, pType, pMetaData);
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D11VideoContext3::DecoderBeginFrame1
 */

struct npt_command_ID3D11VideoContext3_DecoderBeginFrame1 {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    ID3D11VideoDecoderOutputView * pView;
    UINT ContentKeySize;
    const void * pContentKey;
    UINT NumComponentHistograms;
    const UINT * pHistogramOffsets;
    ID3D11Buffer ** ppHistogramBuffers;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext3_DecoderBeginFrame1(const ID3D11VideoDecoder * pDecoder,const ID3D11VideoDecoderOutputView * pView,const UINT ContentKeySize,const void * pContentKey,const UINT NumComponentHistograms,const UINT * pHistogramOffsets,ID3D11Buffer ** ppHistogramBuffers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&ContentKeySize, max_mode);
    if (pContentKey) {
        cmd_size += npt_sizeof_array_count(ContentKeySize);
        cmd_size += npt_sizeof_blob_array(pContentKey, ContentKeySize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&NumComponentHistograms, max_mode);
    cmd_size += npt_sizeof_array_count(pHistogramOffsets ? NumComponentHistograms : 0);
    if (pHistogramOffsets)
        cmd_size += npt_sizeof_UINT_array(pHistogramOffsets, NumComponentHistograms);
    cmd_size += npt_sizeof_array_count(NumComponentHistograms);
    cmd_size += sizeof(uint64_t) * NumComponentHistograms;
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext3_DecoderBeginFrame1(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11VideoDecoder * pDecoder,
                                                  ID3D11VideoDecoderOutputView * pView,
                                                  UINT ContentKeySize,
                                                  const void * pContentKey,
                                                  UINT NumComponentHistograms,
                                                  const UINT * pHistogramOffsets,
                                                  ID3D11Buffer ** ppHistogramBuffers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext3_DecoderBeginFrame1(pDecoder,pView,ContentKeySize,pContentKey,NumComponentHistograms,pHistogramOffsets,ppHistogramBuffers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext3,
                                 NPT_METHOD_ID3D11VideoContext3_DecoderBeginFrame1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    npt_encode_com_handle(enc, npt_object_get_id(pView));
    npt_encode_UINT(enc, &ContentKeySize);
    if (pContentKey) {
        npt_encode_array_count(enc, ContentKeySize);
        npt_encode_blob_array(enc, pContentKey, ContentKeySize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumComponentHistograms);
    if (pHistogramOffsets) {
        npt_encode_array_count(enc, NumComponentHistograms);
        npt_encode_UINT_array(enc, pHistogramOffsets, NumComponentHistograms);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (ppHistogramBuffers) {
        npt_encode_array_count(enc, NumComponentHistograms);
        for (uint32_t _i = 0; _i < (uint32_t)NumComponentHistograms; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppHistogramBuffers[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext3_DecoderBeginFrame1_reply(const ID3D11VideoDecoder * pDecoder,const ID3D11VideoDecoderOutputView * pView,const UINT ContentKeySize,const void * pContentKey,const UINT NumComponentHistograms,const UINT * pHistogramOffsets,ID3D11Buffer ** ppHistogramBuffers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip pView (input only) */
/* skip ContentKeySize (input only) */
/* skip pContentKey (input only) */
/* skip NumComponentHistograms (input only) */
/* skip pHistogramOffsets (input only) */
/* skip ppHistogramBuffers (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext3_DecoderBeginFrame1_reply(struct npt_cs_decoder *dec,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext3,
                                                  NPT_METHOD_ID3D11VideoContext3_DecoderBeginFrame1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext3_DecoderBeginFrame1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext3,
                                       NPT_METHOD_ID3D11VideoContext3_DecoderBeginFrame1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext3_DecoderBeginFrame1(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11VideoDecoder * pDecoder,
                                                  ID3D11VideoDecoderOutputView * pView,
                                                  UINT ContentKeySize,
                                                  const void * pContentKey,
                                                  UINT NumComponentHistograms,
                                                  const UINT * pHistogramOffsets,
                                                  ID3D11Buffer ** ppHistogramBuffers,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext3_DecoderBeginFrame1(pDecoder,pView,ContentKeySize,pContentKey,NumComponentHistograms,pHistogramOffsets,ppHistogramBuffers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext3_DecoderBeginFrame1_reply(pDecoder,pView,ContentKeySize,pContentKey,NumComponentHistograms,pHistogramOffsets,ppHistogramBuffers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext3_DecoderBeginFrame1(enc, cmd_flags, object_id, pDecoder, pView, ContentKeySize, pContentKey, NumComponentHistograms, pHistogramOffsets, ppHistogramBuffers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext3_DecoderBeginFrame1(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11VideoDecoder * pDecoder,
                                                 ID3D11VideoDecoderOutputView * pView,
                                                 UINT ContentKeySize,
                                                 const void * pContentKey,
                                                 UINT NumComponentHistograms,
                                                 const UINT * pHistogramOffsets,
                                                 ID3D11Buffer ** ppHistogramBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext3_DecoderBeginFrame1(ring, 0, object_id, pDecoder, pView, ContentKeySize, pContentKey, NumComponentHistograms, pHistogramOffsets, ppHistogramBuffers, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext3_DecoderBeginFrame1(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11VideoDecoder * pDecoder,
                                                ID3D11VideoDecoderOutputView * pView,
                                                UINT ContentKeySize,
                                                const void * pContentKey,
                                                UINT NumComponentHistograms,
                                                const UINT * pHistogramOffsets,
                                                ID3D11Buffer ** ppHistogramBuffers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext3_DecoderBeginFrame1(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, pView, ContentKeySize, pContentKey, NumComponentHistograms, pHistogramOffsets, ppHistogramBuffers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext3_DecoderBeginFrame1_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoContext3::SubmitDecoderBuffers2
 */

struct npt_command_ID3D11VideoContext3_SubmitDecoderBuffers2 {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    UINT NumBuffers;
    const D3D11_VIDEO_DECODER_BUFFER_DESC2 * pBufferDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoContext3_SubmitDecoderBuffers2(const ID3D11VideoDecoder * pDecoder,const UINT NumBuffers,const D3D11_VIDEO_DECODER_BUFFER_DESC2 * pBufferDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&NumBuffers, max_mode);
    cmd_size += npt_sizeof_array_count(pBufferDesc ? NumBuffers : 0);
    for (uint32_t _i = 0; _i < (pBufferDesc ? NumBuffers : 0); _i++)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_BUFFER_DESC2(&pBufferDesc[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoContext3_SubmitDecoderBuffers2(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11VideoDecoder * pDecoder,
                                                     UINT NumBuffers,
                                                     const D3D11_VIDEO_DECODER_BUFFER_DESC2 * pBufferDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoContext3_SubmitDecoderBuffers2(pDecoder,NumBuffers,pBufferDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext3,
                                 NPT_METHOD_ID3D11VideoContext3_SubmitDecoderBuffers2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDecoder));
    npt_encode_UINT(enc, &NumBuffers);
    if (pBufferDesc) {
        npt_encode_array_count(enc, NumBuffers);
        for (uint32_t _i = 0; _i < (uint32_t)(NumBuffers); _i++)
            npt_encode_D3D11_VIDEO_DECODER_BUFFER_DESC2(enc, &pBufferDesc[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoContext3_SubmitDecoderBuffers2_reply(const ID3D11VideoDecoder * pDecoder,const UINT NumBuffers,const D3D11_VIDEO_DECODER_BUFFER_DESC2 * pBufferDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoder (input only) */
/* skip NumBuffers (input only) */
/* skip pBufferDesc (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoContext3_SubmitDecoderBuffers2_reply(struct npt_cs_decoder *dec,
                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext3,
                                                  NPT_METHOD_ID3D11VideoContext3_SubmitDecoderBuffers2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoContext3_SubmitDecoderBuffers2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext3,
                                       NPT_METHOD_ID3D11VideoContext3_SubmitDecoderBuffers2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoContext3_SubmitDecoderBuffers2(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D11VideoDecoder * pDecoder,
                                                     UINT NumBuffers,
                                                     const D3D11_VIDEO_DECODER_BUFFER_DESC2 * pBufferDesc,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoContext3_SubmitDecoderBuffers2(pDecoder,NumBuffers,pBufferDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoContext3_SubmitDecoderBuffers2_reply(pDecoder,NumBuffers,pBufferDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoContext3_SubmitDecoderBuffers2(enc, cmd_flags, object_id, pDecoder, NumBuffers, pBufferDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoContext3_SubmitDecoderBuffers2(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D11VideoDecoder * pDecoder,
                                                    UINT NumBuffers,
                                                    const D3D11_VIDEO_DECODER_BUFFER_DESC2 * pBufferDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext3_SubmitDecoderBuffers2(ring, 0, object_id, pDecoder, NumBuffers, pBufferDesc, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoContext3_SubmitDecoderBuffers2(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D11VideoDecoder * pDecoder,
                                                   UINT NumBuffers,
                                                   const D3D11_VIDEO_DECODER_BUFFER_DESC2 * pBufferDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoContext3_SubmitDecoderBuffers2(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoder, NumBuffers, pBufferDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoContext3_SubmitDecoderBuffers2_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11VIDEOCONTEXT_H */
