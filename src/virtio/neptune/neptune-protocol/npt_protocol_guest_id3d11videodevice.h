/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11videodevice.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11VIDEODEVICE_H
#define NPT_PROTOCOL_GUEST_ID3D11VIDEODEVICE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11VideoDevice::CreateVideoDecoder
 */

struct npt_command_ID3D11VideoDevice_CreateVideoDecoder {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pVideoDesc;
    const D3D11_VIDEO_DECODER_CONFIG * pConfig;
    ID3D11VideoDecoder ** ppDecoder;
    /* Shadow: the guest-allocated id for ppDecoder arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDecoder -> *ppDecoder} in the
     * context object table. */
    uint64_t _guest_id_ppDecoder;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoDecoder(const D3D11_VIDEO_DECODER_DESC * pVideoDesc,const D3D11_VIDEO_DECODER_CONFIG * pConfig,ID3D11VideoDecoder ** ppDecoder)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pVideoDesc);
    if (pVideoDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_DESC(pVideoDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pConfig);
    if (pConfig)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_CONFIG(pConfig, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDecoder: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoDecoder(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D11_VIDEO_DECODER_DESC * pVideoDesc,
                                                const D3D11_VIDEO_DECODER_CONFIG * pConfig,
                                                ID3D11VideoDecoder ** ppDecoder)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CreateVideoDecoder(pVideoDesc,pConfig,ppDecoder);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoDecoder),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pVideoDesc))
        npt_encode_D3D11_VIDEO_DECODER_DESC(enc, pVideoDesc);
    if (npt_encode_simple_pointer(enc, pConfig))
        npt_encode_D3D11_VIDEO_DECODER_CONFIG(enc, pConfig);
{
    uint64_t _gid_ppDecoder = 0;
    if (ppDecoder) {
        _gid_ppDecoder = npt_com_allocate_next_id();
        *ppDecoder = (ID3D11VideoDecoder *)(uintptr_t)_gid_ppDecoder;
    }
    npt_encode_uint64_t(enc, &_gid_ppDecoder);
}
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoDecoder_reply(const D3D11_VIDEO_DECODER_DESC * pVideoDesc,const D3D11_VIDEO_DECODER_CONFIG * pConfig,ID3D11VideoDecoder ** ppDecoder)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pVideoDesc (input only) */
/* skip pConfig (input only) */
/* skip ppDecoder (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoDecoder_reply(struct npt_cs_decoder *dec,
                                                      ID3D11VideoDecoder ** ppDecoder,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CreateVideoDecoder))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CreateVideoDecoder reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CreateVideoDecoder));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDecoder (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11VideoDevice_CreateVideoDecoder(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                const D3D11_VIDEO_DECODER_DESC * pVideoDesc,
                                                const D3D11_VIDEO_DECODER_CONFIG * pConfig,
                                                ID3D11VideoDecoder ** ppDecoder,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CreateVideoDecoder(pVideoDesc,pConfig,ppDecoder);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CreateVideoDecoder_reply(pVideoDesc,pConfig,ppDecoder)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CreateVideoDecoder(enc, cmd_flags, object_id, pVideoDesc, pConfig, ppDecoder);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CreateVideoDecoder(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const D3D11_VIDEO_DECODER_DESC * pVideoDesc,
                                               const D3D11_VIDEO_DECODER_CONFIG * pConfig,
                                               ID3D11VideoDecoder ** ppDecoder)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoDecoder(ring, 0, object_id, pVideoDesc, pConfig, ppDecoder, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CreateVideoDecoder(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              const D3D11_VIDEO_DECODER_DESC * pVideoDesc,
                                              const D3D11_VIDEO_DECODER_CONFIG * pConfig,
                                              ID3D11VideoDecoder ** ppDecoder)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoDecoder(ring, NPT_CMD_FLAG_REPLY, object_id, pVideoDesc, pConfig, ppDecoder, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CreateVideoDecoder_reply(dec, ppDecoder, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::CreateVideoProcessor
 */

struct npt_command_ID3D11VideoDevice_CreateVideoProcessor {
    void *_self;
    ID3D11VideoProcessorEnumerator * pEnum;
    UINT RateConversionIndex;
    ID3D11VideoProcessor ** ppVideoProcessor;
    /* Shadow: the guest-allocated id for ppVideoProcessor arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVideoProcessor -> *ppVideoProcessor} in the
     * context object table. */
    uint64_t _guest_id_ppVideoProcessor;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoProcessor(const ID3D11VideoProcessorEnumerator * pEnum,const UINT RateConversionIndex,ID3D11VideoProcessor ** ppVideoProcessor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&RateConversionIndex, max_mode);
cmd_size += sizeof(uint64_t);  /* ppVideoProcessor: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoProcessor(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11VideoProcessorEnumerator * pEnum,
                                                  UINT RateConversionIndex,
                                                  ID3D11VideoProcessor ** ppVideoProcessor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CreateVideoProcessor(pEnum,RateConversionIndex,ppVideoProcessor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessor),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pEnum));
    npt_encode_UINT(enc, &RateConversionIndex);
{
    uint64_t _gid_ppVideoProcessor = 0;
    if (ppVideoProcessor) {
        _gid_ppVideoProcessor = npt_com_allocate_next_id();
        *ppVideoProcessor = (ID3D11VideoProcessor *)(uintptr_t)_gid_ppVideoProcessor;
    }
    npt_encode_uint64_t(enc, &_gid_ppVideoProcessor);
}
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoProcessor_reply(const ID3D11VideoProcessorEnumerator * pEnum,const UINT RateConversionIndex,ID3D11VideoProcessor ** ppVideoProcessor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pEnum (input only) */
/* skip RateConversionIndex (input only) */
/* skip ppVideoProcessor (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoProcessor_reply(struct npt_cs_decoder *dec,
                                                        ID3D11VideoProcessor ** ppVideoProcessor,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessor))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CreateVideoProcessor reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessor));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppVideoProcessor (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11VideoDevice_CreateVideoProcessor(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D11VideoProcessorEnumerator * pEnum,
                                                  UINT RateConversionIndex,
                                                  ID3D11VideoProcessor ** ppVideoProcessor,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CreateVideoProcessor(pEnum,RateConversionIndex,ppVideoProcessor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CreateVideoProcessor_reply(pEnum,RateConversionIndex,ppVideoProcessor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CreateVideoProcessor(enc, cmd_flags, object_id, pEnum, RateConversionIndex, ppVideoProcessor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CreateVideoProcessor(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D11VideoProcessorEnumerator * pEnum,
                                                 UINT RateConversionIndex,
                                                 ID3D11VideoProcessor ** ppVideoProcessor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoProcessor(ring, 0, object_id, pEnum, RateConversionIndex, ppVideoProcessor, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CreateVideoProcessor(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D11VideoProcessorEnumerator * pEnum,
                                                UINT RateConversionIndex,
                                                ID3D11VideoProcessor ** ppVideoProcessor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoProcessor(ring, NPT_CMD_FLAG_REPLY, object_id, pEnum, RateConversionIndex, ppVideoProcessor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CreateVideoProcessor_reply(dec, ppVideoProcessor, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::CreateAuthenticatedChannel
 */

struct npt_command_ID3D11VideoDevice_CreateAuthenticatedChannel {
    void *_self;
    D3D11_AUTHENTICATED_CHANNEL_TYPE ChannelType;
    ID3D11AuthenticatedChannel ** ppAuthenticatedChannel;
    /* Shadow: the guest-allocated id for ppAuthenticatedChannel arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppAuthenticatedChannel -> *ppAuthenticatedChannel} in the
     * context object table. */
    uint64_t _guest_id_ppAuthenticatedChannel;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateAuthenticatedChannel(const D3D11_AUTHENTICATED_CHANNEL_TYPE ChannelType,ID3D11AuthenticatedChannel ** ppAuthenticatedChannel)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D11_AUTHENTICATED_CHANNEL_TYPE(&ChannelType, max_mode);
cmd_size += sizeof(uint64_t);  /* ppAuthenticatedChannel: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CreateAuthenticatedChannel(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        D3D11_AUTHENTICATED_CHANNEL_TYPE ChannelType,
                                                        ID3D11AuthenticatedChannel ** ppAuthenticatedChannel)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CreateAuthenticatedChannel(ChannelType,ppAuthenticatedChannel);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateAuthenticatedChannel),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D11_AUTHENTICATED_CHANNEL_TYPE(enc, &ChannelType);
{
    uint64_t _gid_ppAuthenticatedChannel = 0;
    if (ppAuthenticatedChannel) {
        _gid_ppAuthenticatedChannel = npt_com_allocate_next_id();
        *ppAuthenticatedChannel = (ID3D11AuthenticatedChannel *)(uintptr_t)_gid_ppAuthenticatedChannel;
    }
    npt_encode_uint64_t(enc, &_gid_ppAuthenticatedChannel);
}
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateAuthenticatedChannel_reply(const D3D11_AUTHENTICATED_CHANNEL_TYPE ChannelType,ID3D11AuthenticatedChannel ** ppAuthenticatedChannel)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ChannelType (input only) */
/* skip ppAuthenticatedChannel (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CreateAuthenticatedChannel_reply(struct npt_cs_decoder *dec,
                                                              ID3D11AuthenticatedChannel ** ppAuthenticatedChannel,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CreateAuthenticatedChannel))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CreateAuthenticatedChannel reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CreateAuthenticatedChannel));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppAuthenticatedChannel (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11VideoDevice_CreateAuthenticatedChannel(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        D3D11_AUTHENTICATED_CHANNEL_TYPE ChannelType,
                                                        ID3D11AuthenticatedChannel ** ppAuthenticatedChannel,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CreateAuthenticatedChannel(ChannelType,ppAuthenticatedChannel);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CreateAuthenticatedChannel_reply(ChannelType,ppAuthenticatedChannel)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CreateAuthenticatedChannel(enc, cmd_flags, object_id, ChannelType, ppAuthenticatedChannel);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CreateAuthenticatedChannel(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       D3D11_AUTHENTICATED_CHANNEL_TYPE ChannelType,
                                                       ID3D11AuthenticatedChannel ** ppAuthenticatedChannel)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateAuthenticatedChannel(ring, 0, object_id, ChannelType, ppAuthenticatedChannel, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CreateAuthenticatedChannel(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      D3D11_AUTHENTICATED_CHANNEL_TYPE ChannelType,
                                                      ID3D11AuthenticatedChannel ** ppAuthenticatedChannel)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateAuthenticatedChannel(ring, NPT_CMD_FLAG_REPLY, object_id, ChannelType, ppAuthenticatedChannel, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CreateAuthenticatedChannel_reply(dec, ppAuthenticatedChannel, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::CreateCryptoSession
 */

struct npt_command_ID3D11VideoDevice_CreateCryptoSession {
    void *_self;
    const GUID * pCryptoType;
    const GUID * pDecoderProfile;
    const GUID * pKeyExchangeType;
    ID3D11CryptoSession ** ppCryptoSession;
    /* Shadow: the guest-allocated id for ppCryptoSession arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCryptoSession -> *ppCryptoSession} in the
     * context object table. */
    uint64_t _guest_id_ppCryptoSession;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateCryptoSession(const GUID * pCryptoType,const GUID * pDecoderProfile,const GUID * pKeyExchangeType,ID3D11CryptoSession ** ppCryptoSession)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pCryptoType);
    if (pCryptoType)
        cmd_size += npt_sizeof_GUID(pCryptoType, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDecoderProfile);
    if (pDecoderProfile)
        cmd_size += npt_sizeof_GUID(pDecoderProfile, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pKeyExchangeType);
    if (pKeyExchangeType)
        cmd_size += npt_sizeof_GUID(pKeyExchangeType, max_mode);
cmd_size += sizeof(uint64_t);  /* ppCryptoSession: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CreateCryptoSession(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 const GUID * pCryptoType,
                                                 const GUID * pDecoderProfile,
                                                 const GUID * pKeyExchangeType,
                                                 ID3D11CryptoSession ** ppCryptoSession)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CreateCryptoSession(pCryptoType,pDecoderProfile,pKeyExchangeType,ppCryptoSession);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateCryptoSession),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pCryptoType))
        npt_encode_GUID(enc, pCryptoType);
    if (npt_encode_simple_pointer(enc, pDecoderProfile))
        npt_encode_GUID(enc, pDecoderProfile);
    if (npt_encode_simple_pointer(enc, pKeyExchangeType))
        npt_encode_GUID(enc, pKeyExchangeType);
{
    uint64_t _gid_ppCryptoSession = 0;
    if (ppCryptoSession) {
        _gid_ppCryptoSession = npt_com_allocate_next_id();
        *ppCryptoSession = (ID3D11CryptoSession *)(uintptr_t)_gid_ppCryptoSession;
    }
    npt_encode_uint64_t(enc, &_gid_ppCryptoSession);
}
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateCryptoSession_reply(const GUID * pCryptoType,const GUID * pDecoderProfile,const GUID * pKeyExchangeType,ID3D11CryptoSession ** ppCryptoSession)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoType (input only) */
/* skip pDecoderProfile (input only) */
/* skip pKeyExchangeType (input only) */
/* skip ppCryptoSession (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CreateCryptoSession_reply(struct npt_cs_decoder *dec,
                                                       ID3D11CryptoSession ** ppCryptoSession,
                                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CreateCryptoSession))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CreateCryptoSession reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CreateCryptoSession));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppCryptoSession (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11VideoDevice_CreateCryptoSession(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 const GUID * pCryptoType,
                                                 const GUID * pDecoderProfile,
                                                 const GUID * pKeyExchangeType,
                                                 ID3D11CryptoSession ** ppCryptoSession,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CreateCryptoSession(pCryptoType,pDecoderProfile,pKeyExchangeType,ppCryptoSession);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CreateCryptoSession_reply(pCryptoType,pDecoderProfile,pKeyExchangeType,ppCryptoSession)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CreateCryptoSession(enc, cmd_flags, object_id, pCryptoType, pDecoderProfile, pKeyExchangeType, ppCryptoSession);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CreateCryptoSession(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const GUID * pCryptoType,
                                                const GUID * pDecoderProfile,
                                                const GUID * pKeyExchangeType,
                                                ID3D11CryptoSession ** ppCryptoSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateCryptoSession(ring, 0, object_id, pCryptoType, pDecoderProfile, pKeyExchangeType, ppCryptoSession, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CreateCryptoSession(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               const GUID * pCryptoType,
                                               const GUID * pDecoderProfile,
                                               const GUID * pKeyExchangeType,
                                               ID3D11CryptoSession ** ppCryptoSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateCryptoSession(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoType, pDecoderProfile, pKeyExchangeType, ppCryptoSession, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CreateCryptoSession_reply(dec, ppCryptoSession, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::CreateVideoDecoderOutputView
 */

struct npt_command_ID3D11VideoDevice_CreateVideoDecoderOutputView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC * pDesc;
    ID3D11VideoDecoderOutputView ** ppVDOVView;
    /* Shadow: the guest-allocated id for ppVDOVView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVDOVView -> *ppVDOVView} in the
     * context object table. */
    uint64_t _guest_id_ppVDOVView;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoDecoderOutputView(const ID3D11Resource * pResource,const D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC * pDesc,ID3D11VideoDecoderOutputView ** ppVDOVView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppVDOVView: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoDecoderOutputView(struct npt_cs_encoder *enc,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          ID3D11Resource * pResource,
                                                          const D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC * pDesc,
                                                          ID3D11VideoDecoderOutputView ** ppVDOVView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CreateVideoDecoderOutputView(pResource,pDesc,ppVDOVView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoDecoderOutputView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC(enc, pDesc);
{
    uint64_t _gid_ppVDOVView = 0;
    if (ppVDOVView) {
        _gid_ppVDOVView = npt_com_allocate_next_id();
        *ppVDOVView = (ID3D11VideoDecoderOutputView *)(uintptr_t)_gid_ppVDOVView;
    }
    npt_encode_uint64_t(enc, &_gid_ppVDOVView);
}
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoDecoderOutputView_reply(const ID3D11Resource * pResource,const D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC * pDesc,ID3D11VideoDecoderOutputView ** ppVDOVView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pDesc (input only) */
/* skip ppVDOVView (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoDecoderOutputView_reply(struct npt_cs_decoder *dec,
                                                                ID3D11VideoDecoderOutputView ** ppVDOVView,
                                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CreateVideoDecoderOutputView))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CreateVideoDecoderOutputView reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CreateVideoDecoderOutputView));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppVDOVView (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11VideoDevice_CreateVideoDecoderOutputView(struct npt_ring *ring,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          ID3D11Resource * pResource,
                                                          const D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC * pDesc,
                                                          ID3D11VideoDecoderOutputView ** ppVDOVView,
                                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CreateVideoDecoderOutputView(pResource,pDesc,ppVDOVView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CreateVideoDecoderOutputView_reply(pResource,pDesc,ppVDOVView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CreateVideoDecoderOutputView(enc, cmd_flags, object_id, pResource, pDesc, ppVDOVView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CreateVideoDecoderOutputView(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         ID3D11Resource * pResource,
                                                         const D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC * pDesc,
                                                         ID3D11VideoDecoderOutputView ** ppVDOVView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoDecoderOutputView(ring, 0, object_id, pResource, pDesc, ppVDOVView, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CreateVideoDecoderOutputView(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        ID3D11Resource * pResource,
                                                        const D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC * pDesc,
                                                        ID3D11VideoDecoderOutputView ** ppVDOVView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoDecoderOutputView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pDesc, ppVDOVView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CreateVideoDecoderOutputView_reply(dec, ppVDOVView, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::CreateVideoProcessorInputView
 */

struct npt_command_ID3D11VideoDevice_CreateVideoProcessorInputView {
    void *_self;
    ID3D11Resource * pResource;
    ID3D11VideoProcessorEnumerator * pEnum;
    const D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC * pDesc;
    ID3D11VideoProcessorInputView ** ppVPIView;
    /* Shadow: the guest-allocated id for ppVPIView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVPIView -> *ppVPIView} in the
     * context object table. */
    uint64_t _guest_id_ppVPIView;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorInputView(const ID3D11Resource * pResource,const ID3D11VideoProcessorEnumerator * pEnum,const D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC * pDesc,ID3D11VideoProcessorInputView ** ppVPIView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppVPIView: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoProcessorInputView(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D11Resource * pResource,
                                                           ID3D11VideoProcessorEnumerator * pEnum,
                                                           const D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC * pDesc,
                                                           ID3D11VideoProcessorInputView ** ppVPIView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorInputView(pResource,pEnum,pDesc,ppVPIView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorInputView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    npt_encode_com_handle(enc, npt_object_get_id(pEnum));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC(enc, pDesc);
{
    uint64_t _gid_ppVPIView = 0;
    if (ppVPIView) {
        _gid_ppVPIView = npt_com_allocate_next_id();
        *ppVPIView = (ID3D11VideoProcessorInputView *)(uintptr_t)_gid_ppVPIView;
    }
    npt_encode_uint64_t(enc, &_gid_ppVPIView);
}
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorInputView_reply(const ID3D11Resource * pResource,const ID3D11VideoProcessorEnumerator * pEnum,const D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC * pDesc,ID3D11VideoProcessorInputView ** ppVPIView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pEnum (input only) */
/* skip pDesc (input only) */
/* skip ppVPIView (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoProcessorInputView_reply(struct npt_cs_decoder *dec,
                                                                 ID3D11VideoProcessorInputView ** ppVPIView,
                                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorInputView))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CreateVideoProcessorInputView reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorInputView));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppVPIView (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11VideoDevice_CreateVideoProcessorInputView(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D11Resource * pResource,
                                                           ID3D11VideoProcessorEnumerator * pEnum,
                                                           const D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC * pDesc,
                                                           ID3D11VideoProcessorInputView ** ppVPIView,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorInputView(pResource,pEnum,pDesc,ppVPIView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorInputView_reply(pResource,pEnum,pDesc,ppVPIView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CreateVideoProcessorInputView(enc, cmd_flags, object_id, pResource, pEnum, pDesc, ppVPIView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CreateVideoProcessorInputView(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D11Resource * pResource,
                                                          ID3D11VideoProcessorEnumerator * pEnum,
                                                          const D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC * pDesc,
                                                          ID3D11VideoProcessorInputView ** ppVPIView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoProcessorInputView(ring, 0, object_id, pResource, pEnum, pDesc, ppVPIView, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CreateVideoProcessorInputView(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         ID3D11Resource * pResource,
                                                         ID3D11VideoProcessorEnumerator * pEnum,
                                                         const D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC * pDesc,
                                                         ID3D11VideoProcessorInputView ** ppVPIView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoProcessorInputView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pEnum, pDesc, ppVPIView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CreateVideoProcessorInputView_reply(dec, ppVPIView, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::CreateVideoProcessorOutputView
 */

struct npt_command_ID3D11VideoDevice_CreateVideoProcessorOutputView {
    void *_self;
    ID3D11Resource * pResource;
    ID3D11VideoProcessorEnumerator * pEnum;
    const D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC * pDesc;
    ID3D11VideoProcessorOutputView ** ppVPOView;
    /* Shadow: the guest-allocated id for ppVPOView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVPOView -> *ppVPOView} in the
     * context object table. */
    uint64_t _guest_id_ppVPOView;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorOutputView(const ID3D11Resource * pResource,const ID3D11VideoProcessorEnumerator * pEnum,const D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC * pDesc,ID3D11VideoProcessorOutputView ** ppVPOView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppVPOView: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoProcessorOutputView(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11Resource * pResource,
                                                            ID3D11VideoProcessorEnumerator * pEnum,
                                                            const D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC * pDesc,
                                                            ID3D11VideoProcessorOutputView ** ppVPOView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorOutputView(pResource,pEnum,pDesc,ppVPOView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorOutputView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    npt_encode_com_handle(enc, npt_object_get_id(pEnum));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC(enc, pDesc);
{
    uint64_t _gid_ppVPOView = 0;
    if (ppVPOView) {
        _gid_ppVPOView = npt_com_allocate_next_id();
        *ppVPOView = (ID3D11VideoProcessorOutputView *)(uintptr_t)_gid_ppVPOView;
    }
    npt_encode_uint64_t(enc, &_gid_ppVPOView);
}
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorOutputView_reply(const ID3D11Resource * pResource,const ID3D11VideoProcessorEnumerator * pEnum,const D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC * pDesc,ID3D11VideoProcessorOutputView ** ppVPOView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pEnum (input only) */
/* skip pDesc (input only) */
/* skip ppVPOView (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoProcessorOutputView_reply(struct npt_cs_decoder *dec,
                                                                  ID3D11VideoProcessorOutputView ** ppVPOView,
                                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorOutputView))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CreateVideoProcessorOutputView reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorOutputView));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppVPOView (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11VideoDevice_CreateVideoProcessorOutputView(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D11Resource * pResource,
                                                            ID3D11VideoProcessorEnumerator * pEnum,
                                                            const D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC * pDesc,
                                                            ID3D11VideoProcessorOutputView ** ppVPOView,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorOutputView(pResource,pEnum,pDesc,ppVPOView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorOutputView_reply(pResource,pEnum,pDesc,ppVPOView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CreateVideoProcessorOutputView(enc, cmd_flags, object_id, pResource, pEnum, pDesc, ppVPOView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CreateVideoProcessorOutputView(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D11Resource * pResource,
                                                           ID3D11VideoProcessorEnumerator * pEnum,
                                                           const D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC * pDesc,
                                                           ID3D11VideoProcessorOutputView ** ppVPOView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoProcessorOutputView(ring, 0, object_id, pResource, pEnum, pDesc, ppVPOView, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CreateVideoProcessorOutputView(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D11Resource * pResource,
                                                          ID3D11VideoProcessorEnumerator * pEnum,
                                                          const D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC * pDesc,
                                                          ID3D11VideoProcessorOutputView ** ppVPOView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoProcessorOutputView(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pEnum, pDesc, ppVPOView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CreateVideoProcessorOutputView_reply(dec, ppVPOView, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::CreateVideoProcessorEnumerator
 */

struct npt_command_ID3D11VideoDevice_CreateVideoProcessorEnumerator {
    void *_self;
    const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pDesc;
    ID3D11VideoProcessorEnumerator ** ppEnum;
    /* Shadow: the guest-allocated id for ppEnum arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppEnum -> *ppEnum} in the
     * context object table. */
    uint64_t _guest_id_ppEnum;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorEnumerator(const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pDesc,ID3D11VideoProcessorEnumerator ** ppEnum)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_CONTENT_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppEnum: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoProcessorEnumerator(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pDesc,
                                                            ID3D11VideoProcessorEnumerator ** ppEnum)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorEnumerator(pDesc,ppEnum);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorEnumerator),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_VIDEO_PROCESSOR_CONTENT_DESC(enc, pDesc);
{
    uint64_t _gid_ppEnum = 0;
    if (ppEnum) {
        _gid_ppEnum = npt_com_allocate_next_id();
        *ppEnum = (ID3D11VideoProcessorEnumerator *)(uintptr_t)_gid_ppEnum;
    }
    npt_encode_uint64_t(enc, &_gid_ppEnum);
}
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorEnumerator_reply(const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pDesc,ID3D11VideoProcessorEnumerator ** ppEnum)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip ppEnum (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoProcessorEnumerator_reply(struct npt_cs_decoder *dec,
                                                                  ID3D11VideoProcessorEnumerator ** ppEnum,
                                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorEnumerator))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CreateVideoProcessorEnumerator reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorEnumerator));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppEnum (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D11VideoDevice_CreateVideoProcessorEnumerator(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pDesc,
                                                            ID3D11VideoProcessorEnumerator ** ppEnum,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorEnumerator(pDesc,ppEnum);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CreateVideoProcessorEnumerator_reply(pDesc,ppEnum)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CreateVideoProcessorEnumerator(enc, cmd_flags, object_id, pDesc, ppEnum);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CreateVideoProcessorEnumerator(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pDesc,
                                                           ID3D11VideoProcessorEnumerator ** ppEnum)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoProcessorEnumerator(ring, 0, object_id, pDesc, ppEnum, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CreateVideoProcessorEnumerator(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pDesc,
                                                          ID3D11VideoProcessorEnumerator ** ppEnum)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CreateVideoProcessorEnumerator(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, ppEnum, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CreateVideoProcessorEnumerator_reply(dec, ppEnum, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::GetVideoDecoderProfileCount
 */

struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfileCount {
    void *_self;
    UINT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfileCount(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_GetVideoDecoderProfileCount(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfileCount();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderProfileCount),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfileCount_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_GetVideoDecoderProfileCount_reply(struct npt_cs_decoder *dec,
                                                               UINT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderProfileCount))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_GetVideoDecoderProfileCount reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderProfileCount));
        if (ret) *ret = (UINT)0;
        return;
    }
    if (ret) *ret = (UINT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoDevice_GetVideoDecoderProfileCount(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfileCount();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfileCount_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_GetVideoDecoderProfileCount(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_GetVideoDecoderProfileCount(struct npt_ring *ring,
                                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetVideoDecoderProfileCount(ring, 0, object_id, &submit);
}

static inline UINT
npt_call_ID3D11VideoDevice_GetVideoDecoderProfileCount(struct npt_ring *ring,
                                                       npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetVideoDecoderProfileCount(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT _ret;
        npt_decode_ID3D11VideoDevice_GetVideoDecoderProfileCount_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT)0;
    }
}

/*
 * ID3D11VideoDevice::GetVideoDecoderProfile
 */

struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfile {
    void *_self;
    UINT Index;
    GUID * pDecoderProfile;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfile(const UINT Index,const GUID * pDecoderProfile)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Index, max_mode);
/* skip pDecoderProfile (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_GetVideoDecoderProfile(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT Index,
                                                    GUID * pDecoderProfile)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfile(Index,pDecoderProfile);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderProfile),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Index);
/* skip pDecoderProfile (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfile_reply(const UINT Index,const GUID * pDecoderProfile)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Index (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_GUID(&(const GUID){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_GetVideoDecoderProfile_reply(struct npt_cs_decoder *dec,
                                                          GUID * pDecoderProfile,
                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderProfile))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_GetVideoDecoderProfile reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderProfile));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDecoderProfile)
            npt_decode_GUID(dec, (GUID *)pDecoderProfile);
    }
}

static inline void
npt_submit_ID3D11VideoDevice_GetVideoDecoderProfile(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT Index,
                                                    GUID * pDecoderProfile,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfile(Index,pDecoderProfile);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_GetVideoDecoderProfile_reply(Index,pDecoderProfile)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_GetVideoDecoderProfile(enc, cmd_flags, object_id, Index, pDecoderProfile);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_GetVideoDecoderProfile(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT Index,
                                                   GUID * pDecoderProfile)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetVideoDecoderProfile(ring, 0, object_id, Index, pDecoderProfile, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_GetVideoDecoderProfile(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT Index,
                                                  GUID * pDecoderProfile)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetVideoDecoderProfile(ring, NPT_CMD_FLAG_REPLY, object_id, Index, pDecoderProfile, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_GetVideoDecoderProfile_reply(dec, pDecoderProfile, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::CheckVideoDecoderFormat
 */

struct npt_command_ID3D11VideoDevice_CheckVideoDecoderFormat {
    void *_self;
    const GUID * pDecoderProfile;
    DXGI_FORMAT Format;
    BOOL * pSupported;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CheckVideoDecoderFormat(const GUID * pDecoderProfile,const DXGI_FORMAT Format,const BOOL * pSupported)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDecoderProfile);
    if (pDecoderProfile)
        cmd_size += npt_sizeof_GUID(pDecoderProfile, max_mode);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
/* skip pSupported (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CheckVideoDecoderFormat(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const GUID * pDecoderProfile,
                                                     DXGI_FORMAT Format,
                                                     BOOL * pSupported)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CheckVideoDecoderFormat(pDecoderProfile,Format,pSupported);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CheckVideoDecoderFormat),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDecoderProfile))
        npt_encode_GUID(enc, pDecoderProfile);
    npt_encode_DXGI_FORMAT(enc, &Format);
/* skip pSupported (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CheckVideoDecoderFormat_reply(const GUID * pDecoderProfile,const DXGI_FORMAT Format,const BOOL * pSupported)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoderProfile (input only) */
/* skip Format (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CheckVideoDecoderFormat_reply(struct npt_cs_decoder *dec,
                                                           BOOL * pSupported,
                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CheckVideoDecoderFormat))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CheckVideoDecoderFormat reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CheckVideoDecoderFormat));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pSupported)
            npt_decode_BOOL(dec, (BOOL *)pSupported);
    }
}

static inline void
npt_submit_ID3D11VideoDevice_CheckVideoDecoderFormat(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const GUID * pDecoderProfile,
                                                     DXGI_FORMAT Format,
                                                     BOOL * pSupported,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CheckVideoDecoderFormat(pDecoderProfile,Format,pSupported);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CheckVideoDecoderFormat_reply(pDecoderProfile,Format,pSupported)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CheckVideoDecoderFormat(enc, cmd_flags, object_id, pDecoderProfile, Format, pSupported);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CheckVideoDecoderFormat(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    const GUID * pDecoderProfile,
                                                    DXGI_FORMAT Format,
                                                    BOOL * pSupported)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CheckVideoDecoderFormat(ring, 0, object_id, pDecoderProfile, Format, pSupported, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CheckVideoDecoderFormat(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   const GUID * pDecoderProfile,
                                                   DXGI_FORMAT Format,
                                                   BOOL * pSupported)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CheckVideoDecoderFormat(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoderProfile, Format, pSupported, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CheckVideoDecoderFormat_reply(dec, pSupported, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::GetVideoDecoderConfigCount
 */

struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfigCount {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pDesc;
    UINT * pCount;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfigCount(const D3D11_VIDEO_DECODER_DESC * pDesc,const UINT * pCount)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_DESC(pDesc, max_mode);
/* skip pCount (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_GetVideoDecoderConfigCount(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        const D3D11_VIDEO_DECODER_DESC * pDesc,
                                                        UINT * pCount)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfigCount(pDesc,pCount);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderConfigCount),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_VIDEO_DECODER_DESC(enc, pDesc);
/* skip pCount (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfigCount_reply(const D3D11_VIDEO_DECODER_DESC * pDesc,const UINT * pCount)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_GetVideoDecoderConfigCount_reply(struct npt_cs_decoder *dec,
                                                              UINT * pCount,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderConfigCount))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_GetVideoDecoderConfigCount reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderConfigCount));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pCount)
            npt_decode_UINT(dec, (UINT *)pCount);
    }
}

static inline void
npt_submit_ID3D11VideoDevice_GetVideoDecoderConfigCount(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        const D3D11_VIDEO_DECODER_DESC * pDesc,
                                                        UINT * pCount,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfigCount(pDesc,pCount);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfigCount_reply(pDesc,pCount)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_GetVideoDecoderConfigCount(enc, cmd_flags, object_id, pDesc, pCount);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_GetVideoDecoderConfigCount(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       const D3D11_VIDEO_DECODER_DESC * pDesc,
                                                       UINT * pCount)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetVideoDecoderConfigCount(ring, 0, object_id, pDesc, pCount, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_GetVideoDecoderConfigCount(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      const D3D11_VIDEO_DECODER_DESC * pDesc,
                                                      UINT * pCount)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetVideoDecoderConfigCount(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, pCount, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_GetVideoDecoderConfigCount_reply(dec, pCount, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::GetVideoDecoderConfig
 */

struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfig {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pDesc;
    UINT Index;
    D3D11_VIDEO_DECODER_CONFIG * pConfig;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfig(const D3D11_VIDEO_DECODER_DESC * pDesc,const UINT Index,const D3D11_VIDEO_DECODER_CONFIG * pConfig)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_UINT(&Index, max_mode);
/* skip pConfig (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_GetVideoDecoderConfig(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   const D3D11_VIDEO_DECODER_DESC * pDesc,
                                                   UINT Index,
                                                   D3D11_VIDEO_DECODER_CONFIG * pConfig)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfig(pDesc,Index,pConfig);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderConfig),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D11_VIDEO_DECODER_DESC(enc, pDesc);
    npt_encode_UINT(enc, &Index);
/* skip pConfig (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfig_reply(const D3D11_VIDEO_DECODER_DESC * pDesc,const UINT Index,const D3D11_VIDEO_DECODER_CONFIG * pConfig)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip Index (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_CONFIG(&(const D3D11_VIDEO_DECODER_CONFIG){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_GetVideoDecoderConfig_reply(struct npt_cs_decoder *dec,
                                                         D3D11_VIDEO_DECODER_CONFIG * pConfig,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderConfig))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_GetVideoDecoderConfig reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderConfig));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pConfig)
            npt_decode_D3D11_VIDEO_DECODER_CONFIG(dec, (D3D11_VIDEO_DECODER_CONFIG *)pConfig);
    }
}

static inline void
npt_submit_ID3D11VideoDevice_GetVideoDecoderConfig(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   const D3D11_VIDEO_DECODER_DESC * pDesc,
                                                   UINT Index,
                                                   D3D11_VIDEO_DECODER_CONFIG * pConfig,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfig(pDesc,Index,pConfig);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_GetVideoDecoderConfig_reply(pDesc,Index,pConfig)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_GetVideoDecoderConfig(enc, cmd_flags, object_id, pDesc, Index, pConfig);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_GetVideoDecoderConfig(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  const D3D11_VIDEO_DECODER_DESC * pDesc,
                                                  UINT Index,
                                                  D3D11_VIDEO_DECODER_CONFIG * pConfig)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetVideoDecoderConfig(ring, 0, object_id, pDesc, Index, pConfig, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_GetVideoDecoderConfig(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const D3D11_VIDEO_DECODER_DESC * pDesc,
                                                 UINT Index,
                                                 D3D11_VIDEO_DECODER_CONFIG * pConfig)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetVideoDecoderConfig(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, Index, pConfig, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_GetVideoDecoderConfig_reply(dec, pConfig, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::GetContentProtectionCaps
 */

struct npt_command_ID3D11VideoDevice_GetContentProtectionCaps {
    void *_self;
    const GUID * pCryptoType;
    const GUID * pDecoderProfile;
    D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetContentProtectionCaps(const GUID * pCryptoType,const GUID * pDecoderProfile,const D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pCryptoType);
    if (pCryptoType)
        cmd_size += npt_sizeof_GUID(pCryptoType, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDecoderProfile);
    if (pDecoderProfile)
        cmd_size += npt_sizeof_GUID(pDecoderProfile, max_mode);
/* skip pCaps (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_GetContentProtectionCaps(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      const GUID * pCryptoType,
                                                      const GUID * pDecoderProfile,
                                                      D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_GetContentProtectionCaps(pCryptoType,pDecoderProfile,pCaps);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetContentProtectionCaps),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pCryptoType))
        npt_encode_GUID(enc, pCryptoType);
    if (npt_encode_simple_pointer(enc, pDecoderProfile))
        npt_encode_GUID(enc, pDecoderProfile);
/* skip pCaps (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_GetContentProtectionCaps_reply(const GUID * pCryptoType,const GUID * pDecoderProfile,const D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoType (input only) */
/* skip pDecoderProfile (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_CONTENT_PROTECTION_CAPS(&(const D3D11_VIDEO_CONTENT_PROTECTION_CAPS){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_GetContentProtectionCaps_reply(struct npt_cs_decoder *dec,
                                                            D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_GetContentProtectionCaps))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_GetContentProtectionCaps reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_GetContentProtectionCaps));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pCaps)
            npt_decode_D3D11_VIDEO_CONTENT_PROTECTION_CAPS(dec, (D3D11_VIDEO_CONTENT_PROTECTION_CAPS *)pCaps);
    }
}

static inline void
npt_submit_ID3D11VideoDevice_GetContentProtectionCaps(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      const GUID * pCryptoType,
                                                      const GUID * pDecoderProfile,
                                                      D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_GetContentProtectionCaps(pCryptoType,pDecoderProfile,pCaps);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_GetContentProtectionCaps_reply(pCryptoType,pDecoderProfile,pCaps)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_GetContentProtectionCaps(enc, cmd_flags, object_id, pCryptoType, pDecoderProfile, pCaps);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_GetContentProtectionCaps(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     const GUID * pCryptoType,
                                                     const GUID * pDecoderProfile,
                                                     D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetContentProtectionCaps(ring, 0, object_id, pCryptoType, pDecoderProfile, pCaps, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_GetContentProtectionCaps(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    const GUID * pCryptoType,
                                                    const GUID * pDecoderProfile,
                                                    D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_GetContentProtectionCaps(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoType, pDecoderProfile, pCaps, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_GetContentProtectionCaps_reply(dec, pCaps, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::CheckCryptoKeyExchange
 */

struct npt_command_ID3D11VideoDevice_CheckCryptoKeyExchange {
    void *_self;
    const GUID * pCryptoType;
    const GUID * pDecoderProfile;
    UINT Index;
    GUID * pKeyExchangeType;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_CheckCryptoKeyExchange(const GUID * pCryptoType,const GUID * pDecoderProfile,const UINT Index,const GUID * pKeyExchangeType)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pCryptoType);
    if (pCryptoType)
        cmd_size += npt_sizeof_GUID(pCryptoType, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDecoderProfile);
    if (pDecoderProfile)
        cmd_size += npt_sizeof_GUID(pDecoderProfile, max_mode);
    cmd_size += npt_sizeof_UINT(&Index, max_mode);
/* skip pKeyExchangeType (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_CheckCryptoKeyExchange(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    const GUID * pCryptoType,
                                                    const GUID * pDecoderProfile,
                                                    UINT Index,
                                                    GUID * pKeyExchangeType)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_CheckCryptoKeyExchange(pCryptoType,pDecoderProfile,Index,pKeyExchangeType);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CheckCryptoKeyExchange),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pCryptoType))
        npt_encode_GUID(enc, pCryptoType);
    if (npt_encode_simple_pointer(enc, pDecoderProfile))
        npt_encode_GUID(enc, pDecoderProfile);
    npt_encode_UINT(enc, &Index);
/* skip pKeyExchangeType (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_CheckCryptoKeyExchange_reply(const GUID * pCryptoType,const GUID * pDecoderProfile,const UINT Index,const GUID * pKeyExchangeType)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoType (input only) */
/* skip pDecoderProfile (input only) */
/* skip Index (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_GUID(&(const GUID){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_CheckCryptoKeyExchange_reply(struct npt_cs_decoder *dec,
                                                          GUID * pKeyExchangeType,
                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_CheckCryptoKeyExchange))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_CheckCryptoKeyExchange reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_CheckCryptoKeyExchange));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pKeyExchangeType)
            npt_decode_GUID(dec, (GUID *)pKeyExchangeType);
    }
}

static inline void
npt_submit_ID3D11VideoDevice_CheckCryptoKeyExchange(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    const GUID * pCryptoType,
                                                    const GUID * pDecoderProfile,
                                                    UINT Index,
                                                    GUID * pKeyExchangeType,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_CheckCryptoKeyExchange(pCryptoType,pDecoderProfile,Index,pKeyExchangeType);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_CheckCryptoKeyExchange_reply(pCryptoType,pDecoderProfile,Index,pKeyExchangeType)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_CheckCryptoKeyExchange(enc, cmd_flags, object_id, pCryptoType, pDecoderProfile, Index, pKeyExchangeType);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_CheckCryptoKeyExchange(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   const GUID * pCryptoType,
                                                   const GUID * pDecoderProfile,
                                                   UINT Index,
                                                   GUID * pKeyExchangeType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CheckCryptoKeyExchange(ring, 0, object_id, pCryptoType, pDecoderProfile, Index, pKeyExchangeType, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_CheckCryptoKeyExchange(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  const GUID * pCryptoType,
                                                  const GUID * pDecoderProfile,
                                                  UINT Index,
                                                  GUID * pKeyExchangeType)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_CheckCryptoKeyExchange(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoType, pDecoderProfile, Index, pKeyExchangeType, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_CheckCryptoKeyExchange_reply(dec, pKeyExchangeType, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::SetPrivateData
 */

struct npt_command_ID3D11VideoDevice_SetPrivateData {
    void *_self;
    const GUID * guid;
    UINT DataSize;
    const void * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_SetPrivateData(const GUID * guid,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(guid);
    if (guid)
        cmd_size += npt_sizeof_GUID(guid, max_mode);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_SetPrivateData(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            const GUID * guid,
                                            UINT DataSize,
                                            const void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_SetPrivateData(guid,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_SetPrivateData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, guid))
        npt_encode_GUID(enc, guid);
    npt_encode_UINT(enc, &DataSize);
    if (pData) {
        npt_encode_array_count(enc, DataSize);
        npt_encode_blob_array(enc, pData, DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_SetPrivateData_reply(const GUID * guid,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip guid (input only) */
/* skip DataSize (input only) */
/* skip pData (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_SetPrivateData_reply(struct npt_cs_decoder *dec,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_SetPrivateData))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_SetPrivateData reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_SetPrivateData));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoDevice_SetPrivateData(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            const GUID * guid,
                                            UINT DataSize,
                                            const void * pData,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_SetPrivateData(guid,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_SetPrivateData_reply(guid,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_SetPrivateData(enc, cmd_flags, object_id, guid, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_SetPrivateData(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           const GUID * guid,
                                           UINT DataSize,
                                           const void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_SetPrivateData(ring, 0, object_id, guid, DataSize, pData, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_SetPrivateData(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          const GUID * guid,
                                          UINT DataSize,
                                          const void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_SetPrivateData(ring, NPT_CMD_FLAG_REPLY, object_id, guid, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_SetPrivateData_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice::SetPrivateDataInterface
 */

struct npt_command_ID3D11VideoDevice_SetPrivateDataInterface {
    void *_self;
    const GUID * guid;
    const IUnknown * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice_SetPrivateDataInterface(const GUID * guid,const IUnknown * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(guid);
    if (guid)
        cmd_size += npt_sizeof_GUID(guid, max_mode);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice_SetPrivateDataInterface(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const GUID * guid,
                                                     const IUnknown * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice_SetPrivateDataInterface(guid,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_SetPrivateDataInterface),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, guid))
        npt_encode_GUID(enc, guid);
    npt_encode_com_handle(enc, npt_object_get_id(pData));
}

static inline size_t
npt_sizeof_ID3D11VideoDevice_SetPrivateDataInterface_reply(const GUID * guid,const IUnknown * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip guid (input only) */
/* skip pData (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice_SetPrivateDataInterface_reply(struct npt_cs_decoder *dec,
                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                                  NPT_METHOD_ID3D11VideoDevice_SetPrivateDataInterface))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice_SetPrivateDataInterface reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                       NPT_METHOD_ID3D11VideoDevice_SetPrivateDataInterface));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D11VideoDevice_SetPrivateDataInterface(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const GUID * guid,
                                                     const IUnknown * pData,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice_SetPrivateDataInterface(guid,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice_SetPrivateDataInterface_reply(guid,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice_SetPrivateDataInterface(enc, cmd_flags, object_id, guid, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice_SetPrivateDataInterface(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    const GUID * guid,
                                                    const IUnknown * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_SetPrivateDataInterface(ring, 0, object_id, guid, pData, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice_SetPrivateDataInterface(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   const GUID * guid,
                                                   const IUnknown * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice_SetPrivateDataInterface(ring, NPT_CMD_FLAG_REPLY, object_id, guid, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice_SetPrivateDataInterface_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice1::GetCryptoSessionPrivateDataSize
 */

struct npt_command_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize {
    void *_self;
    const GUID * pCryptoType;
    const GUID * pDecoderProfile;
    const GUID * pKeyExchangeType;
    UINT * pPrivateInputSize;
    UINT * pPrivateOutputSize;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(const GUID * pCryptoType,const GUID * pDecoderProfile,const GUID * pKeyExchangeType,const UINT * pPrivateInputSize,const UINT * pPrivateOutputSize)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pCryptoType);
    if (pCryptoType)
        cmd_size += npt_sizeof_GUID(pCryptoType, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDecoderProfile);
    if (pDecoderProfile)
        cmd_size += npt_sizeof_GUID(pDecoderProfile, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pKeyExchangeType);
    if (pKeyExchangeType)
        cmd_size += npt_sizeof_GUID(pKeyExchangeType, max_mode);
/* skip pPrivateInputSize (output only) */
/* skip pPrivateOutputSize (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(struct npt_cs_encoder *enc,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              const GUID * pCryptoType,
                                                              const GUID * pDecoderProfile,
                                                              const GUID * pKeyExchangeType,
                                                              UINT * pPrivateInputSize,
                                                              UINT * pPrivateOutputSize)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(pCryptoType,pDecoderProfile,pKeyExchangeType,pPrivateInputSize,pPrivateOutputSize);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                 NPT_METHOD_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pCryptoType))
        npt_encode_GUID(enc, pCryptoType);
    if (npt_encode_simple_pointer(enc, pDecoderProfile))
        npt_encode_GUID(enc, pDecoderProfile);
    if (npt_encode_simple_pointer(enc, pKeyExchangeType))
        npt_encode_GUID(enc, pKeyExchangeType);
/* skip pPrivateInputSize (output only) */
/* skip pPrivateOutputSize (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_reply(const GUID * pCryptoType,const GUID * pDecoderProfile,const GUID * pKeyExchangeType,const UINT * pPrivateInputSize,const UINT * pPrivateOutputSize)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoType (input only) */
/* skip pDecoderProfile (input only) */
/* skip pKeyExchangeType (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_reply(struct npt_cs_decoder *dec,
                                                                    UINT * pPrivateInputSize,
                                                                    UINT * pPrivateOutputSize,
                                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                                  NPT_METHOD_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                       NPT_METHOD_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pPrivateInputSize)
            npt_decode_UINT(dec, (UINT *)pPrivateInputSize);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pPrivateOutputSize)
            npt_decode_UINT(dec, (UINT *)pPrivateOutputSize);
    }
}

static inline void
npt_submit_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(struct npt_ring *ring,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              const GUID * pCryptoType,
                                                              const GUID * pDecoderProfile,
                                                              const GUID * pKeyExchangeType,
                                                              UINT * pPrivateInputSize,
                                                              UINT * pPrivateOutputSize,
                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(pCryptoType,pDecoderProfile,pKeyExchangeType,pPrivateInputSize,pPrivateOutputSize);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_reply(pCryptoType,pDecoderProfile,pKeyExchangeType,pPrivateInputSize,pPrivateOutputSize)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(enc, cmd_flags, object_id, pCryptoType, pDecoderProfile, pKeyExchangeType, pPrivateInputSize, pPrivateOutputSize);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             const GUID * pCryptoType,
                                                             const GUID * pDecoderProfile,
                                                             const GUID * pKeyExchangeType,
                                                             UINT * pPrivateInputSize,
                                                             UINT * pPrivateOutputSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(ring, 0, object_id, pCryptoType, pDecoderProfile, pKeyExchangeType, pPrivateInputSize, pPrivateOutputSize, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            const GUID * pCryptoType,
                                                            const GUID * pDecoderProfile,
                                                            const GUID * pKeyExchangeType,
                                                            UINT * pPrivateInputSize,
                                                            UINT * pPrivateOutputSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoType, pDecoderProfile, pKeyExchangeType, pPrivateInputSize, pPrivateOutputSize, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_reply(dec, pPrivateInputSize, pPrivateOutputSize, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice1::GetVideoDecoderCaps
 */

struct npt_command_ID3D11VideoDevice1_GetVideoDecoderCaps {
    void *_self;
    const GUID * pDecoderProfile;
    UINT SampleWidth;
    UINT SampleHeight;
    const DXGI_RATIONAL * pFrameRate;
    UINT BitRate;
    const GUID * pCryptoType;
    UINT * pDecoderCaps;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice1_GetVideoDecoderCaps(const GUID * pDecoderProfile,const UINT SampleWidth,const UINT SampleHeight,const DXGI_RATIONAL * pFrameRate,const UINT BitRate,const GUID * pCryptoType,const UINT * pDecoderCaps)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDecoderProfile);
    if (pDecoderProfile)
        cmd_size += npt_sizeof_GUID(pDecoderProfile, max_mode);
    cmd_size += npt_sizeof_UINT(&SampleWidth, max_mode);
    cmd_size += npt_sizeof_UINT(&SampleHeight, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pFrameRate);
    if (pFrameRate)
        cmd_size += npt_sizeof_DXGI_RATIONAL(pFrameRate, max_mode);
    cmd_size += npt_sizeof_UINT(&BitRate, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pCryptoType);
    if (pCryptoType)
        cmd_size += npt_sizeof_GUID(pCryptoType, max_mode);
/* skip pDecoderCaps (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice1_GetVideoDecoderCaps(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const GUID * pDecoderProfile,
                                                  UINT SampleWidth,
                                                  UINT SampleHeight,
                                                  const DXGI_RATIONAL * pFrameRate,
                                                  UINT BitRate,
                                                  const GUID * pCryptoType,
                                                  UINT * pDecoderCaps)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice1_GetVideoDecoderCaps(pDecoderProfile,SampleWidth,SampleHeight,pFrameRate,BitRate,pCryptoType,pDecoderCaps);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                 NPT_METHOD_ID3D11VideoDevice1_GetVideoDecoderCaps),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDecoderProfile))
        npt_encode_GUID(enc, pDecoderProfile);
    npt_encode_UINT(enc, &SampleWidth);
    npt_encode_UINT(enc, &SampleHeight);
    if (npt_encode_simple_pointer(enc, pFrameRate))
        npt_encode_DXGI_RATIONAL(enc, pFrameRate);
    npt_encode_UINT(enc, &BitRate);
    if (npt_encode_simple_pointer(enc, pCryptoType))
        npt_encode_GUID(enc, pCryptoType);
/* skip pDecoderCaps (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice1_GetVideoDecoderCaps_reply(const GUID * pDecoderProfile,const UINT SampleWidth,const UINT SampleHeight,const DXGI_RATIONAL * pFrameRate,const UINT BitRate,const GUID * pCryptoType,const UINT * pDecoderCaps)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDecoderProfile (input only) */
/* skip SampleWidth (input only) */
/* skip SampleHeight (input only) */
/* skip pFrameRate (input only) */
/* skip BitRate (input only) */
/* skip pCryptoType (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice1_GetVideoDecoderCaps_reply(struct npt_cs_decoder *dec,
                                                        UINT * pDecoderCaps,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                                  NPT_METHOD_ID3D11VideoDevice1_GetVideoDecoderCaps))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice1_GetVideoDecoderCaps reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                       NPT_METHOD_ID3D11VideoDevice1_GetVideoDecoderCaps));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pDecoderCaps)
            npt_decode_UINT(dec, (UINT *)pDecoderCaps);
    }
}

static inline void
npt_submit_ID3D11VideoDevice1_GetVideoDecoderCaps(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const GUID * pDecoderProfile,
                                                  UINT SampleWidth,
                                                  UINT SampleHeight,
                                                  const DXGI_RATIONAL * pFrameRate,
                                                  UINT BitRate,
                                                  const GUID * pCryptoType,
                                                  UINT * pDecoderCaps,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice1_GetVideoDecoderCaps(pDecoderProfile,SampleWidth,SampleHeight,pFrameRate,BitRate,pCryptoType,pDecoderCaps);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice1_GetVideoDecoderCaps_reply(pDecoderProfile,SampleWidth,SampleHeight,pFrameRate,BitRate,pCryptoType,pDecoderCaps)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice1_GetVideoDecoderCaps(enc, cmd_flags, object_id, pDecoderProfile, SampleWidth, SampleHeight, pFrameRate, BitRate, pCryptoType, pDecoderCaps);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice1_GetVideoDecoderCaps(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const GUID * pDecoderProfile,
                                                 UINT SampleWidth,
                                                 UINT SampleHeight,
                                                 const DXGI_RATIONAL * pFrameRate,
                                                 UINT BitRate,
                                                 const GUID * pCryptoType,
                                                 UINT * pDecoderCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice1_GetVideoDecoderCaps(ring, 0, object_id, pDecoderProfile, SampleWidth, SampleHeight, pFrameRate, BitRate, pCryptoType, pDecoderCaps, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice1_GetVideoDecoderCaps(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const GUID * pDecoderProfile,
                                                UINT SampleWidth,
                                                UINT SampleHeight,
                                                const DXGI_RATIONAL * pFrameRate,
                                                UINT BitRate,
                                                const GUID * pCryptoType,
                                                UINT * pDecoderCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice1_GetVideoDecoderCaps(ring, NPT_CMD_FLAG_REPLY, object_id, pDecoderProfile, SampleWidth, SampleHeight, pFrameRate, BitRate, pCryptoType, pDecoderCaps, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice1_GetVideoDecoderCaps_reply(dec, pDecoderCaps, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice1::CheckVideoDecoderDownsampling
 */

struct npt_command_ID3D11VideoDevice1_CheckVideoDecoderDownsampling {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pInputDesc;
    DXGI_COLOR_SPACE_TYPE InputColorSpace;
    const D3D11_VIDEO_DECODER_CONFIG * pInputConfig;
    const DXGI_RATIONAL * pFrameRate;
    const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc;
    BOOL * pSupported;
    BOOL * pRealTimeHint;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(const D3D11_VIDEO_DECODER_DESC * pInputDesc,const DXGI_COLOR_SPACE_TYPE InputColorSpace,const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,const DXGI_RATIONAL * pFrameRate,const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,const BOOL * pSupported,const BOOL * pRealTimeHint)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pInputDesc);
    if (pInputDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_DESC(pInputDesc, max_mode);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&InputColorSpace, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pInputConfig);
    if (pInputConfig)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_CONFIG(pInputConfig, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pFrameRate);
    if (pFrameRate)
        cmd_size += npt_sizeof_DXGI_RATIONAL(pFrameRate, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pOutputDesc);
    if (pOutputDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_SAMPLE_DESC(pOutputDesc, max_mode);
/* skip pSupported (output only) */
/* skip pRealTimeHint (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            const D3D11_VIDEO_DECODER_DESC * pInputDesc,
                                                            DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                            const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,
                                                            const DXGI_RATIONAL * pFrameRate,
                                                            const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,
                                                            BOOL * pSupported,
                                                            BOOL * pRealTimeHint)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(pInputDesc,InputColorSpace,pInputConfig,pFrameRate,pOutputDesc,pSupported,pRealTimeHint);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                 NPT_METHOD_ID3D11VideoDevice1_CheckVideoDecoderDownsampling),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pInputDesc))
        npt_encode_D3D11_VIDEO_DECODER_DESC(enc, pInputDesc);
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &InputColorSpace);
    if (npt_encode_simple_pointer(enc, pInputConfig))
        npt_encode_D3D11_VIDEO_DECODER_CONFIG(enc, pInputConfig);
    if (npt_encode_simple_pointer(enc, pFrameRate))
        npt_encode_DXGI_RATIONAL(enc, pFrameRate);
    if (npt_encode_simple_pointer(enc, pOutputDesc))
        npt_encode_D3D11_VIDEO_SAMPLE_DESC(enc, pOutputDesc);
/* skip pSupported (output only) */
/* skip pRealTimeHint (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_reply(const D3D11_VIDEO_DECODER_DESC * pInputDesc,const DXGI_COLOR_SPACE_TYPE InputColorSpace,const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,const DXGI_RATIONAL * pFrameRate,const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,const BOOL * pSupported,const BOOL * pRealTimeHint)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pInputDesc (input only) */
/* skip InputColorSpace (input only) */
/* skip pInputConfig (input only) */
/* skip pFrameRate (input only) */
/* skip pOutputDesc (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_reply(struct npt_cs_decoder *dec,
                                                                  BOOL * pSupported,
                                                                  BOOL * pRealTimeHint,
                                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                                  NPT_METHOD_ID3D11VideoDevice1_CheckVideoDecoderDownsampling))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice1_CheckVideoDecoderDownsampling reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                       NPT_METHOD_ID3D11VideoDevice1_CheckVideoDecoderDownsampling));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pSupported)
            npt_decode_BOOL(dec, (BOOL *)pSupported);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pRealTimeHint)
            npt_decode_BOOL(dec, (BOOL *)pRealTimeHint);
    }
}

static inline void
npt_submit_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            const D3D11_VIDEO_DECODER_DESC * pInputDesc,
                                                            DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                            const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,
                                                            const DXGI_RATIONAL * pFrameRate,
                                                            const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,
                                                            BOOL * pSupported,
                                                            BOOL * pRealTimeHint,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(pInputDesc,InputColorSpace,pInputConfig,pFrameRate,pOutputDesc,pSupported,pRealTimeHint);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_reply(pInputDesc,InputColorSpace,pInputConfig,pFrameRate,pOutputDesc,pSupported,pRealTimeHint)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(enc, cmd_flags, object_id, pInputDesc, InputColorSpace, pInputConfig, pFrameRate, pOutputDesc, pSupported, pRealTimeHint);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           const D3D11_VIDEO_DECODER_DESC * pInputDesc,
                                                           DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                           const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,
                                                           const DXGI_RATIONAL * pFrameRate,
                                                           const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,
                                                           BOOL * pSupported,
                                                           BOOL * pRealTimeHint)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(ring, 0, object_id, pInputDesc, InputColorSpace, pInputConfig, pFrameRate, pOutputDesc, pSupported, pRealTimeHint, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          const D3D11_VIDEO_DECODER_DESC * pInputDesc,
                                                          DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                          const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,
                                                          const DXGI_RATIONAL * pFrameRate,
                                                          const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc,
                                                          BOOL * pSupported,
                                                          BOOL * pRealTimeHint)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(ring, NPT_CMD_FLAG_REPLY, object_id, pInputDesc, InputColorSpace, pInputConfig, pFrameRate, pOutputDesc, pSupported, pRealTimeHint, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_reply(dec, pSupported, pRealTimeHint, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice1::RecommendVideoDecoderDownsampleParameters
 */

struct npt_command_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pInputDesc;
    DXGI_COLOR_SPACE_TYPE InputColorSpace;
    const D3D11_VIDEO_DECODER_CONFIG * pInputConfig;
    const DXGI_RATIONAL * pFrameRate;
    D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(const D3D11_VIDEO_DECODER_DESC * pInputDesc,const DXGI_COLOR_SPACE_TYPE InputColorSpace,const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,const DXGI_RATIONAL * pFrameRate,const D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pInputDesc);
    if (pInputDesc)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_DESC(pInputDesc, max_mode);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&InputColorSpace, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pInputConfig);
    if (pInputConfig)
        cmd_size += npt_sizeof_D3D11_VIDEO_DECODER_CONFIG(pInputConfig, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pFrameRate);
    if (pFrameRate)
        cmd_size += npt_sizeof_DXGI_RATIONAL(pFrameRate, max_mode);
/* skip pRecommendedOutputDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(struct npt_cs_encoder *enc,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        const D3D11_VIDEO_DECODER_DESC * pInputDesc,
                                                                        DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                                        const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,
                                                                        const DXGI_RATIONAL * pFrameRate,
                                                                        D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(pInputDesc,InputColorSpace,pInputConfig,pFrameRate,pRecommendedOutputDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                 NPT_METHOD_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pInputDesc))
        npt_encode_D3D11_VIDEO_DECODER_DESC(enc, pInputDesc);
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &InputColorSpace);
    if (npt_encode_simple_pointer(enc, pInputConfig))
        npt_encode_D3D11_VIDEO_DECODER_CONFIG(enc, pInputConfig);
    if (npt_encode_simple_pointer(enc, pFrameRate))
        npt_encode_DXGI_RATIONAL(enc, pFrameRate);
/* skip pRecommendedOutputDesc (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_reply(const D3D11_VIDEO_DECODER_DESC * pInputDesc,const DXGI_COLOR_SPACE_TYPE InputColorSpace,const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,const DXGI_RATIONAL * pFrameRate,const D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pInputDesc (input only) */
/* skip InputColorSpace (input only) */
/* skip pInputConfig (input only) */
/* skip pFrameRate (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_SAMPLE_DESC(&(const D3D11_VIDEO_SAMPLE_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_reply(struct npt_cs_decoder *dec,
                                                                              D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc,
                                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                                  NPT_METHOD_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                       NPT_METHOD_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pRecommendedOutputDesc)
            npt_decode_D3D11_VIDEO_SAMPLE_DESC(dec, (D3D11_VIDEO_SAMPLE_DESC *)pRecommendedOutputDesc);
    }
}

static inline void
npt_submit_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(struct npt_ring *ring,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        const D3D11_VIDEO_DECODER_DESC * pInputDesc,
                                                                        DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                                        const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,
                                                                        const DXGI_RATIONAL * pFrameRate,
                                                                        D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc,
                                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(pInputDesc,InputColorSpace,pInputConfig,pFrameRate,pRecommendedOutputDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_reply(pInputDesc,InputColorSpace,pInputConfig,pFrameRate,pRecommendedOutputDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(enc, cmd_flags, object_id, pInputDesc, InputColorSpace, pInputConfig, pFrameRate, pRecommendedOutputDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(struct npt_ring *ring,
                                                                       npt_object_id object_id,
                                                                       const D3D11_VIDEO_DECODER_DESC * pInputDesc,
                                                                       DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                                       const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,
                                                                       const DXGI_RATIONAL * pFrameRate,
                                                                       D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(ring, 0, object_id, pInputDesc, InputColorSpace, pInputConfig, pFrameRate, pRecommendedOutputDesc, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      const D3D11_VIDEO_DECODER_DESC * pInputDesc,
                                                                      DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                                      const D3D11_VIDEO_DECODER_CONFIG * pInputConfig,
                                                                      const DXGI_RATIONAL * pFrameRate,
                                                                      D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(ring, NPT_CMD_FLAG_REPLY, object_id, pInputDesc, InputColorSpace, pInputConfig, pFrameRate, pRecommendedOutputDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_reply(dec, pRecommendedOutputDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice2::CheckFeatureSupport
 */

struct npt_command_ID3D11VideoDevice2_CheckFeatureSupport {
    void *_self;
    D3D11_FEATURE_VIDEO Feature;
    void * pFeatureSupportData;
    UINT FeatureSupportDataSize;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice2_CheckFeatureSupport(const D3D11_FEATURE_VIDEO Feature,const void * pFeatureSupportData,const UINT FeatureSupportDataSize)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D11_FEATURE_VIDEO(&Feature, max_mode);
/* skip pFeatureSupportData (output only) */
    cmd_size += npt_sizeof_UINT(&FeatureSupportDataSize, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice2_CheckFeatureSupport(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  D3D11_FEATURE_VIDEO Feature,
                                                  void * pFeatureSupportData,
                                                  UINT FeatureSupportDataSize)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice2_CheckFeatureSupport(Feature,pFeatureSupportData,FeatureSupportDataSize);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice2,
                                 NPT_METHOD_ID3D11VideoDevice2_CheckFeatureSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D11_FEATURE_VIDEO(enc, &Feature);
/* skip pFeatureSupportData (output only) */
    npt_encode_UINT(enc, &FeatureSupportDataSize);
}

static inline size_t
npt_sizeof_ID3D11VideoDevice2_CheckFeatureSupport_reply(const D3D11_FEATURE_VIDEO Feature,const void * pFeatureSupportData,const UINT FeatureSupportDataSize)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Feature (input only) */
    if (pFeatureSupportData) {
        cmd_size += npt_sizeof_array_count(FeatureSupportDataSize);
        cmd_size += npt_sizeof_blob_array(pFeatureSupportData, FeatureSupportDataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip FeatureSupportDataSize (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice2_CheckFeatureSupport_reply(struct npt_cs_decoder *dec,
                                                        void * pFeatureSupportData,
                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice2,
                                                  NPT_METHOD_ID3D11VideoDevice2_CheckFeatureSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice2_CheckFeatureSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice2,
                                       NPT_METHOD_ID3D11VideoDevice2_CheckFeatureSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pFeatureSupportData)
            npt_decode_blob_array(dec, (void *)pFeatureSupportData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoDevice2_CheckFeatureSupport(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  D3D11_FEATURE_VIDEO Feature,
                                                  void * pFeatureSupportData,
                                                  UINT FeatureSupportDataSize,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice2_CheckFeatureSupport(Feature,pFeatureSupportData,FeatureSupportDataSize);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice2_CheckFeatureSupport_reply(Feature,pFeatureSupportData,FeatureSupportDataSize)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice2_CheckFeatureSupport(enc, cmd_flags, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice2_CheckFeatureSupport(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 D3D11_FEATURE_VIDEO Feature,
                                                 void * pFeatureSupportData,
                                                 UINT FeatureSupportDataSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice2_CheckFeatureSupport(ring, 0, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice2_CheckFeatureSupport(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                D3D11_FEATURE_VIDEO Feature,
                                                void * pFeatureSupportData,
                                                UINT FeatureSupportDataSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice2_CheckFeatureSupport(ring, NPT_CMD_FLAG_REPLY, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice2_CheckFeatureSupport_reply(dec, pFeatureSupportData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoDevice2::NegotiateCryptoSessionKeyExchangeMT
 */

struct npt_command_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS flags;
    UINT DataSize;
    void * pData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(const ID3D11CryptoSession * pCryptoSession,const D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS flags,const UINT DataSize,const void * pData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS(&flags, max_mode);
    cmd_size += npt_sizeof_UINT(&DataSize, max_mode);
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11CryptoSession * pCryptoSession,
                                                                  D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS flags,
                                                                  UINT DataSize,
                                                                  void * pData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(pCryptoSession,flags,DataSize,pData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice2,
                                 NPT_METHOD_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCryptoSession));
    npt_encode_D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS(enc, &flags);
    npt_encode_UINT(enc, &DataSize);
    if (pData) {
        npt_encode_array_count(enc, DataSize);
        npt_encode_blob_array(enc, pData, DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_reply(const ID3D11CryptoSession * pCryptoSession,const D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS flags,const UINT DataSize,const void * pData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCryptoSession (input only) */
/* skip flags (input only) */
/* skip DataSize (input only) */
    if (pData) {
        cmd_size += npt_sizeof_array_count(DataSize);
        cmd_size += npt_sizeof_blob_array(pData, DataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_reply(struct npt_cs_decoder *dec,
                                                                        void * pData,
                                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice2,
                                                  NPT_METHOD_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice2,
                                       NPT_METHOD_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pData)
            npt_decode_blob_array(dec, (void *)pData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D11CryptoSession * pCryptoSession,
                                                                  D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS flags,
                                                                  UINT DataSize,
                                                                  void * pData,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(pCryptoSession,flags,DataSize,pData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_reply(pCryptoSession,flags,DataSize,pData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(enc, cmd_flags, object_id, pCryptoSession, flags, DataSize, pData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D11CryptoSession * pCryptoSession,
                                                                 D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS flags,
                                                                 UINT DataSize,
                                                                 void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(ring, 0, object_id, pCryptoSession, flags, DataSize, pData, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D11CryptoSession * pCryptoSession,
                                                                D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS flags,
                                                                UINT DataSize,
                                                                void * pData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(ring, NPT_CMD_FLAG_REPLY, object_id, pCryptoSession, flags, DataSize, pData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_reply(dec, pData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11VIDEODEVICE_H */
