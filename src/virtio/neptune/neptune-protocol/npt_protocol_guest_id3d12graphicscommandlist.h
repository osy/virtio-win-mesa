/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12graphicscommandlist.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12GRAPHICSCOMMANDLIST_H
#define NPT_PROTOCOL_GUEST_ID3D12GRAPHICSCOMMANDLIST_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12GraphicsCommandList::Close
 */

struct npt_command_ID3D12GraphicsCommandList_Close {
    void *_self;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_Close(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_Close(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_Close();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_Close),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_Close_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D12GraphicsCommandList_Close_reply(struct npt_cs_decoder *dec,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                                  NPT_METHOD_ID3D12GraphicsCommandList_Close))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12GraphicsCommandList_Close reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                       NPT_METHOD_ID3D12GraphicsCommandList_Close));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_Close(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_Close();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_Close_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_Close(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_Close(struct npt_ring *ring,
                                          npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_Close(ring, 0, object_id, &submit);
}

static inline HRESULT
npt_call_ID3D12GraphicsCommandList_Close(struct npt_ring *ring,
                                         npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_Close(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12GraphicsCommandList_Close_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12GraphicsCommandList::Reset
 */

struct npt_command_ID3D12GraphicsCommandList_Reset {
    void *_self;
    ID3D12CommandAllocator * pAllocator;
    ID3D12PipelineState * pInitialState;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_Reset(const ID3D12CommandAllocator * pAllocator,const ID3D12PipelineState * pInitialState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_Reset(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D12CommandAllocator * pAllocator,
                                           ID3D12PipelineState * pInitialState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_Reset(pAllocator,pInitialState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_Reset),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pAllocator));
    npt_encode_com_handle(enc, npt_object_get_id(pInitialState));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_Reset_reply(const ID3D12CommandAllocator * pAllocator,const ID3D12PipelineState * pInitialState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAllocator (input only) */
/* skip pInitialState (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12GraphicsCommandList_Reset_reply(struct npt_cs_decoder *dec,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                                  NPT_METHOD_ID3D12GraphicsCommandList_Reset))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12GraphicsCommandList_Reset reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                       NPT_METHOD_ID3D12GraphicsCommandList_Reset));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_Reset(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           ID3D12CommandAllocator * pAllocator,
                                           ID3D12PipelineState * pInitialState,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_Reset(pAllocator,pInitialState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_Reset_reply(pAllocator,pInitialState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_Reset(enc, cmd_flags, object_id, pAllocator, pInitialState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_Reset(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          ID3D12CommandAllocator * pAllocator,
                                          ID3D12PipelineState * pInitialState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_Reset(ring, 0, object_id, pAllocator, pInitialState, &submit);
}

static inline HRESULT
npt_call_ID3D12GraphicsCommandList_Reset(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         ID3D12CommandAllocator * pAllocator,
                                         ID3D12PipelineState * pInitialState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_Reset(ring, NPT_CMD_FLAG_REPLY, object_id, pAllocator, pInitialState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12GraphicsCommandList_Reset_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12GraphicsCommandList::ClearState
 */

struct npt_command_ID3D12GraphicsCommandList_ClearState {
    void *_self;
    ID3D12PipelineState * pPipelineState;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearState(const ID3D12PipelineState * pPipelineState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearState(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D12PipelineState * pPipelineState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ClearState(pPipelineState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pPipelineState));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearState_reply(const ID3D12PipelineState * pPipelineState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pPipelineState (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ClearState(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D12PipelineState * pPipelineState,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ClearState(pPipelineState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ClearState_reply(pPipelineState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ClearState(enc, cmd_flags, object_id, pPipelineState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ClearState(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D12PipelineState * pPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearState(ring, 0, object_id, pPipelineState, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ClearState(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D12PipelineState * pPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearState(ring, NPT_CMD_FLAG_REPLY, object_id, pPipelineState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::DrawInstanced
 */

struct npt_command_ID3D12GraphicsCommandList_DrawInstanced {
    void *_self;
    UINT VertexCountPerInstance;
    UINT InstanceCount;
    UINT StartVertexLocation;
    UINT StartInstanceLocation;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_DrawInstanced(const UINT VertexCountPerInstance,const UINT InstanceCount,const UINT StartVertexLocation,const UINT StartInstanceLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&VertexCountPerInstance, max_mode);
    cmd_size += npt_sizeof_UINT(&InstanceCount, max_mode);
    cmd_size += npt_sizeof_UINT(&StartVertexLocation, max_mode);
    cmd_size += npt_sizeof_UINT(&StartInstanceLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_DrawInstanced(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT VertexCountPerInstance,
                                                   UINT InstanceCount,
                                                   UINT StartVertexLocation,
                                                   UINT StartInstanceLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_DrawInstanced(VertexCountPerInstance,InstanceCount,StartVertexLocation,StartInstanceLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_DrawInstanced),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &VertexCountPerInstance);
    npt_encode_UINT(enc, &InstanceCount);
    npt_encode_UINT(enc, &StartVertexLocation);
    npt_encode_UINT(enc, &StartInstanceLocation);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_DrawInstanced_reply(const UINT VertexCountPerInstance,const UINT InstanceCount,const UINT StartVertexLocation,const UINT StartInstanceLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip VertexCountPerInstance (input only) */
/* skip InstanceCount (input only) */
/* skip StartVertexLocation (input only) */
/* skip StartInstanceLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_DrawInstanced(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT VertexCountPerInstance,
                                                   UINT InstanceCount,
                                                   UINT StartVertexLocation,
                                                   UINT StartInstanceLocation,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_DrawInstanced(VertexCountPerInstance,InstanceCount,StartVertexLocation,StartInstanceLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_DrawInstanced_reply(VertexCountPerInstance,InstanceCount,StartVertexLocation,StartInstanceLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_DrawInstanced(enc, cmd_flags, object_id, VertexCountPerInstance, InstanceCount, StartVertexLocation, StartInstanceLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_DrawInstanced(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT VertexCountPerInstance,
                                                  UINT InstanceCount,
                                                  UINT StartVertexLocation,
                                                  UINT StartInstanceLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_DrawInstanced(ring, 0, object_id, VertexCountPerInstance, InstanceCount, StartVertexLocation, StartInstanceLocation, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_DrawInstanced(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT VertexCountPerInstance,
                                                 UINT InstanceCount,
                                                 UINT StartVertexLocation,
                                                 UINT StartInstanceLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_DrawInstanced(ring, NPT_CMD_FLAG_REPLY, object_id, VertexCountPerInstance, InstanceCount, StartVertexLocation, StartInstanceLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::DrawIndexedInstanced
 */

struct npt_command_ID3D12GraphicsCommandList_DrawIndexedInstanced {
    void *_self;
    UINT IndexCountPerInstance;
    UINT InstanceCount;
    UINT StartIndexLocation;
    INT BaseVertexLocation;
    UINT StartInstanceLocation;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_DrawIndexedInstanced(const UINT IndexCountPerInstance,const UINT InstanceCount,const UINT StartIndexLocation,const INT BaseVertexLocation,const UINT StartInstanceLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&IndexCountPerInstance, max_mode);
    cmd_size += npt_sizeof_UINT(&InstanceCount, max_mode);
    cmd_size += npt_sizeof_UINT(&StartIndexLocation, max_mode);
    cmd_size += npt_sizeof_INT(&BaseVertexLocation, max_mode);
    cmd_size += npt_sizeof_UINT(&StartInstanceLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_DrawIndexedInstanced(struct npt_cs_encoder *enc,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          UINT IndexCountPerInstance,
                                                          UINT InstanceCount,
                                                          UINT StartIndexLocation,
                                                          INT BaseVertexLocation,
                                                          UINT StartInstanceLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_DrawIndexedInstanced(IndexCountPerInstance,InstanceCount,StartIndexLocation,BaseVertexLocation,StartInstanceLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_DrawIndexedInstanced),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &IndexCountPerInstance);
    npt_encode_UINT(enc, &InstanceCount);
    npt_encode_UINT(enc, &StartIndexLocation);
    npt_encode_INT(enc, &BaseVertexLocation);
    npt_encode_UINT(enc, &StartInstanceLocation);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_DrawIndexedInstanced_reply(const UINT IndexCountPerInstance,const UINT InstanceCount,const UINT StartIndexLocation,const INT BaseVertexLocation,const UINT StartInstanceLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip IndexCountPerInstance (input only) */
/* skip InstanceCount (input only) */
/* skip StartIndexLocation (input only) */
/* skip BaseVertexLocation (input only) */
/* skip StartInstanceLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_DrawIndexedInstanced(struct npt_ring *ring,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          UINT IndexCountPerInstance,
                                                          UINT InstanceCount,
                                                          UINT StartIndexLocation,
                                                          INT BaseVertexLocation,
                                                          UINT StartInstanceLocation,
                                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_DrawIndexedInstanced(IndexCountPerInstance,InstanceCount,StartIndexLocation,BaseVertexLocation,StartInstanceLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_DrawIndexedInstanced_reply(IndexCountPerInstance,InstanceCount,StartIndexLocation,BaseVertexLocation,StartInstanceLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_DrawIndexedInstanced(enc, cmd_flags, object_id, IndexCountPerInstance, InstanceCount, StartIndexLocation, BaseVertexLocation, StartInstanceLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_DrawIndexedInstanced(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         UINT IndexCountPerInstance,
                                                         UINT InstanceCount,
                                                         UINT StartIndexLocation,
                                                         INT BaseVertexLocation,
                                                         UINT StartInstanceLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_DrawIndexedInstanced(ring, 0, object_id, IndexCountPerInstance, InstanceCount, StartIndexLocation, BaseVertexLocation, StartInstanceLocation, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_DrawIndexedInstanced(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        UINT IndexCountPerInstance,
                                                        UINT InstanceCount,
                                                        UINT StartIndexLocation,
                                                        INT BaseVertexLocation,
                                                        UINT StartInstanceLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_DrawIndexedInstanced(ring, NPT_CMD_FLAG_REPLY, object_id, IndexCountPerInstance, InstanceCount, StartIndexLocation, BaseVertexLocation, StartInstanceLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::Dispatch
 */

struct npt_command_ID3D12GraphicsCommandList_Dispatch {
    void *_self;
    UINT ThreadGroupCountX;
    UINT ThreadGroupCountY;
    UINT ThreadGroupCountZ;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_Dispatch(const UINT ThreadGroupCountX,const UINT ThreadGroupCountY,const UINT ThreadGroupCountZ)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&ThreadGroupCountX, max_mode);
    cmd_size += npt_sizeof_UINT(&ThreadGroupCountY, max_mode);
    cmd_size += npt_sizeof_UINT(&ThreadGroupCountZ, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_Dispatch(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT ThreadGroupCountX,
                                              UINT ThreadGroupCountY,
                                              UINT ThreadGroupCountZ)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_Dispatch(ThreadGroupCountX,ThreadGroupCountY,ThreadGroupCountZ);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_Dispatch),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &ThreadGroupCountX);
    npt_encode_UINT(enc, &ThreadGroupCountY);
    npt_encode_UINT(enc, &ThreadGroupCountZ);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_Dispatch_reply(const UINT ThreadGroupCountX,const UINT ThreadGroupCountY,const UINT ThreadGroupCountZ)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ThreadGroupCountX (input only) */
/* skip ThreadGroupCountY (input only) */
/* skip ThreadGroupCountZ (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_Dispatch(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT ThreadGroupCountX,
                                              UINT ThreadGroupCountY,
                                              UINT ThreadGroupCountZ,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_Dispatch(ThreadGroupCountX,ThreadGroupCountY,ThreadGroupCountZ);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_Dispatch_reply(ThreadGroupCountX,ThreadGroupCountY,ThreadGroupCountZ)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_Dispatch(enc, cmd_flags, object_id, ThreadGroupCountX, ThreadGroupCountY, ThreadGroupCountZ);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_Dispatch(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT ThreadGroupCountX,
                                             UINT ThreadGroupCountY,
                                             UINT ThreadGroupCountZ)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_Dispatch(ring, 0, object_id, ThreadGroupCountX, ThreadGroupCountY, ThreadGroupCountZ, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_Dispatch(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT ThreadGroupCountX,
                                            UINT ThreadGroupCountY,
                                            UINT ThreadGroupCountZ)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_Dispatch(ring, NPT_CMD_FLAG_REPLY, object_id, ThreadGroupCountX, ThreadGroupCountY, ThreadGroupCountZ, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::CopyBufferRegion
 */

struct npt_command_ID3D12GraphicsCommandList_CopyBufferRegion {
    void *_self;
    ID3D12Resource * pDstBuffer;
    UINT64 DstOffset;
    ID3D12Resource * pSrcBuffer;
    UINT64 SrcOffset;
    UINT64 NumBytes;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_CopyBufferRegion(const ID3D12Resource * pDstBuffer,const UINT64 DstOffset,const ID3D12Resource * pSrcBuffer,const UINT64 SrcOffset,const UINT64 NumBytes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&DstOffset, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&SrcOffset, max_mode);
    cmd_size += npt_sizeof_UINT64(&NumBytes, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_CopyBufferRegion(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D12Resource * pDstBuffer,
                                                      UINT64 DstOffset,
                                                      ID3D12Resource * pSrcBuffer,
                                                      UINT64 SrcOffset,
                                                      UINT64 NumBytes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_CopyBufferRegion(pDstBuffer,DstOffset,pSrcBuffer,SrcOffset,NumBytes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_CopyBufferRegion),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstBuffer));
    npt_encode_UINT64(enc, &DstOffset);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcBuffer));
    npt_encode_UINT64(enc, &SrcOffset);
    npt_encode_UINT64(enc, &NumBytes);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_CopyBufferRegion_reply(const ID3D12Resource * pDstBuffer,const UINT64 DstOffset,const ID3D12Resource * pSrcBuffer,const UINT64 SrcOffset,const UINT64 NumBytes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstBuffer (input only) */
/* skip DstOffset (input only) */
/* skip pSrcBuffer (input only) */
/* skip SrcOffset (input only) */
/* skip NumBytes (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_CopyBufferRegion(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D12Resource * pDstBuffer,
                                                      UINT64 DstOffset,
                                                      ID3D12Resource * pSrcBuffer,
                                                      UINT64 SrcOffset,
                                                      UINT64 NumBytes,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_CopyBufferRegion(pDstBuffer,DstOffset,pSrcBuffer,SrcOffset,NumBytes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_CopyBufferRegion_reply(pDstBuffer,DstOffset,pSrcBuffer,SrcOffset,NumBytes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_CopyBufferRegion(enc, cmd_flags, object_id, pDstBuffer, DstOffset, pSrcBuffer, SrcOffset, NumBytes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_CopyBufferRegion(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3D12Resource * pDstBuffer,
                                                     UINT64 DstOffset,
                                                     ID3D12Resource * pSrcBuffer,
                                                     UINT64 SrcOffset,
                                                     UINT64 NumBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_CopyBufferRegion(ring, 0, object_id, pDstBuffer, DstOffset, pSrcBuffer, SrcOffset, NumBytes, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_CopyBufferRegion(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D12Resource * pDstBuffer,
                                                    UINT64 DstOffset,
                                                    ID3D12Resource * pSrcBuffer,
                                                    UINT64 SrcOffset,
                                                    UINT64 NumBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_CopyBufferRegion(ring, NPT_CMD_FLAG_REPLY, object_id, pDstBuffer, DstOffset, pSrcBuffer, SrcOffset, NumBytes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::CopyTextureRegion
 */

struct npt_command_ID3D12GraphicsCommandList_CopyTextureRegion {
    void *_self;
    const D3D12_TEXTURE_COPY_LOCATION * pDst;
    UINT DstX;
    UINT DstY;
    UINT DstZ;
    const D3D12_TEXTURE_COPY_LOCATION * pSrc;
    const D3D12_BOX * pSrcBox;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_CopyTextureRegion(const D3D12_TEXTURE_COPY_LOCATION * pDst,const UINT DstX,const UINT DstY,const UINT DstZ,const D3D12_TEXTURE_COPY_LOCATION * pSrc,const D3D12_BOX * pSrcBox)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDst);
    if (pDst)
        cmd_size += npt_sizeof_D3D12_TEXTURE_COPY_LOCATION(pDst, max_mode);
    cmd_size += npt_sizeof_UINT(&DstX, max_mode);
    cmd_size += npt_sizeof_UINT(&DstY, max_mode);
    cmd_size += npt_sizeof_UINT(&DstZ, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSrc);
    if (pSrc)
        cmd_size += npt_sizeof_D3D12_TEXTURE_COPY_LOCATION(pSrc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSrcBox);
    if (pSrcBox)
        cmd_size += npt_sizeof_D3D12_BOX(pSrcBox, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_CopyTextureRegion(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       const D3D12_TEXTURE_COPY_LOCATION * pDst,
                                                       UINT DstX,
                                                       UINT DstY,
                                                       UINT DstZ,
                                                       const D3D12_TEXTURE_COPY_LOCATION * pSrc,
                                                       const D3D12_BOX * pSrcBox)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_CopyTextureRegion(pDst,DstX,DstY,DstZ,pSrc,pSrcBox);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_CopyTextureRegion),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDst))
        npt_encode_D3D12_TEXTURE_COPY_LOCATION(enc, pDst);
    npt_encode_UINT(enc, &DstX);
    npt_encode_UINT(enc, &DstY);
    npt_encode_UINT(enc, &DstZ);
    if (npt_encode_simple_pointer(enc, pSrc))
        npt_encode_D3D12_TEXTURE_COPY_LOCATION(enc, pSrc);
    if (npt_encode_simple_pointer(enc, pSrcBox))
        npt_encode_D3D12_BOX(enc, pSrcBox);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_CopyTextureRegion_reply(const D3D12_TEXTURE_COPY_LOCATION * pDst,const UINT DstX,const UINT DstY,const UINT DstZ,const D3D12_TEXTURE_COPY_LOCATION * pSrc,const D3D12_BOX * pSrcBox)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDst (input only) */
/* skip DstX (input only) */
/* skip DstY (input only) */
/* skip DstZ (input only) */
/* skip pSrc (input only) */
/* skip pSrcBox (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_CopyTextureRegion(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       const D3D12_TEXTURE_COPY_LOCATION * pDst,
                                                       UINT DstX,
                                                       UINT DstY,
                                                       UINT DstZ,
                                                       const D3D12_TEXTURE_COPY_LOCATION * pSrc,
                                                       const D3D12_BOX * pSrcBox,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_CopyTextureRegion(pDst,DstX,DstY,DstZ,pSrc,pSrcBox);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_CopyTextureRegion_reply(pDst,DstX,DstY,DstZ,pSrc,pSrcBox)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_CopyTextureRegion(enc, cmd_flags, object_id, pDst, DstX, DstY, DstZ, pSrc, pSrcBox);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_CopyTextureRegion(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      const D3D12_TEXTURE_COPY_LOCATION * pDst,
                                                      UINT DstX,
                                                      UINT DstY,
                                                      UINT DstZ,
                                                      const D3D12_TEXTURE_COPY_LOCATION * pSrc,
                                                      const D3D12_BOX * pSrcBox)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_CopyTextureRegion(ring, 0, object_id, pDst, DstX, DstY, DstZ, pSrc, pSrcBox, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_CopyTextureRegion(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     const D3D12_TEXTURE_COPY_LOCATION * pDst,
                                                     UINT DstX,
                                                     UINT DstY,
                                                     UINT DstZ,
                                                     const D3D12_TEXTURE_COPY_LOCATION * pSrc,
                                                     const D3D12_BOX * pSrcBox)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_CopyTextureRegion(ring, NPT_CMD_FLAG_REPLY, object_id, pDst, DstX, DstY, DstZ, pSrc, pSrcBox, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::CopyResource
 */

struct npt_command_ID3D12GraphicsCommandList_CopyResource {
    void *_self;
    ID3D12Resource * pDstResource;
    ID3D12Resource * pSrcResource;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_CopyResource(const ID3D12Resource * pDstResource,const ID3D12Resource * pSrcResource)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_CopyResource(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D12Resource * pDstResource,
                                                  ID3D12Resource * pSrcResource)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_CopyResource(pDstResource,pSrcResource);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_CopyResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_com_handle(enc, npt_object_get_id(pSrcResource));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_CopyResource_reply(const ID3D12Resource * pDstResource,const ID3D12Resource * pSrcResource)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip pSrcResource (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_CopyResource(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  ID3D12Resource * pDstResource,
                                                  ID3D12Resource * pSrcResource,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_CopyResource(pDstResource,pSrcResource);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_CopyResource_reply(pDstResource,pSrcResource)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_CopyResource(enc, cmd_flags, object_id, pDstResource, pSrcResource);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_CopyResource(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D12Resource * pDstResource,
                                                 ID3D12Resource * pSrcResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_CopyResource(ring, 0, object_id, pDstResource, pSrcResource, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_CopyResource(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                ID3D12Resource * pDstResource,
                                                ID3D12Resource * pSrcResource)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_CopyResource(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, pSrcResource, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::CopyTiles
 */

struct npt_command_ID3D12GraphicsCommandList_CopyTiles {
    void *_self;
    ID3D12Resource * pTiledResource;
    const D3D12_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate;
    const D3D12_TILE_REGION_SIZE * pTileRegionSize;
    ID3D12Resource * pBuffer;
    UINT64 BufferStartOffsetInBytes;
    D3D12_TILE_COPY_FLAGS Flags;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_CopyTiles(const ID3D12Resource * pTiledResource,const D3D12_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,const D3D12_TILE_REGION_SIZE * pTileRegionSize,const ID3D12Resource * pBuffer,const UINT64 BufferStartOffsetInBytes,const D3D12_TILE_COPY_FLAGS Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pTileRegionStartCoordinate);
    if (pTileRegionStartCoordinate)
        cmd_size += npt_sizeof_D3D12_TILED_RESOURCE_COORDINATE(pTileRegionStartCoordinate, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pTileRegionSize);
    if (pTileRegionSize)
        cmd_size += npt_sizeof_D3D12_TILE_REGION_SIZE(pTileRegionSize, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&BufferStartOffsetInBytes, max_mode);
    cmd_size += npt_sizeof_D3D12_TILE_COPY_FLAGS(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_CopyTiles(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12Resource * pTiledResource,
                                               const D3D12_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,
                                               const D3D12_TILE_REGION_SIZE * pTileRegionSize,
                                               ID3D12Resource * pBuffer,
                                               UINT64 BufferStartOffsetInBytes,
                                               D3D12_TILE_COPY_FLAGS Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_CopyTiles(pTiledResource,pTileRegionStartCoordinate,pTileRegionSize,pBuffer,BufferStartOffsetInBytes,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_CopyTiles),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pTiledResource));
    if (npt_encode_simple_pointer(enc, pTileRegionStartCoordinate))
        npt_encode_D3D12_TILED_RESOURCE_COORDINATE(enc, pTileRegionStartCoordinate);
    if (npt_encode_simple_pointer(enc, pTileRegionSize))
        npt_encode_D3D12_TILE_REGION_SIZE(enc, pTileRegionSize);
    npt_encode_com_handle(enc, npt_object_get_id(pBuffer));
    npt_encode_UINT64(enc, &BufferStartOffsetInBytes);
    npt_encode_D3D12_TILE_COPY_FLAGS(enc, &Flags);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_CopyTiles_reply(const ID3D12Resource * pTiledResource,const D3D12_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,const D3D12_TILE_REGION_SIZE * pTileRegionSize,const ID3D12Resource * pBuffer,const UINT64 BufferStartOffsetInBytes,const D3D12_TILE_COPY_FLAGS Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pTiledResource (input only) */
/* skip pTileRegionStartCoordinate (input only) */
/* skip pTileRegionSize (input only) */
/* skip pBuffer (input only) */
/* skip BufferStartOffsetInBytes (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_CopyTiles(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               ID3D12Resource * pTiledResource,
                                               const D3D12_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,
                                               const D3D12_TILE_REGION_SIZE * pTileRegionSize,
                                               ID3D12Resource * pBuffer,
                                               UINT64 BufferStartOffsetInBytes,
                                               D3D12_TILE_COPY_FLAGS Flags,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_CopyTiles(pTiledResource,pTileRegionStartCoordinate,pTileRegionSize,pBuffer,BufferStartOffsetInBytes,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_CopyTiles_reply(pTiledResource,pTileRegionStartCoordinate,pTileRegionSize,pBuffer,BufferStartOffsetInBytes,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_CopyTiles(enc, cmd_flags, object_id, pTiledResource, pTileRegionStartCoordinate, pTileRegionSize, pBuffer, BufferStartOffsetInBytes, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_CopyTiles(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D12Resource * pTiledResource,
                                              const D3D12_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,
                                              const D3D12_TILE_REGION_SIZE * pTileRegionSize,
                                              ID3D12Resource * pBuffer,
                                              UINT64 BufferStartOffsetInBytes,
                                              D3D12_TILE_COPY_FLAGS Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_CopyTiles(ring, 0, object_id, pTiledResource, pTileRegionStartCoordinate, pTileRegionSize, pBuffer, BufferStartOffsetInBytes, Flags, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_CopyTiles(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D12Resource * pTiledResource,
                                             const D3D12_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate,
                                             const D3D12_TILE_REGION_SIZE * pTileRegionSize,
                                             ID3D12Resource * pBuffer,
                                             UINT64 BufferStartOffsetInBytes,
                                             D3D12_TILE_COPY_FLAGS Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_CopyTiles(ring, NPT_CMD_FLAG_REPLY, object_id, pTiledResource, pTileRegionStartCoordinate, pTileRegionSize, pBuffer, BufferStartOffsetInBytes, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::ResolveSubresource
 */

struct npt_command_ID3D12GraphicsCommandList_ResolveSubresource {
    void *_self;
    ID3D12Resource * pDstResource;
    UINT DstSubresource;
    ID3D12Resource * pSrcResource;
    UINT SrcSubresource;
    DXGI_FORMAT Format;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ResolveSubresource(const ID3D12Resource * pDstResource,const UINT DstSubresource,const ID3D12Resource * pSrcResource,const UINT SrcSubresource,const DXGI_FORMAT Format)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DstSubresource, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&SrcSubresource, max_mode);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ResolveSubresource(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D12Resource * pDstResource,
                                                        UINT DstSubresource,
                                                        ID3D12Resource * pSrcResource,
                                                        UINT SrcSubresource,
                                                        DXGI_FORMAT Format)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ResolveSubresource(pDstResource,DstSubresource,pSrcResource,SrcSubresource,Format);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ResolveSubresource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_UINT(enc, &DstSubresource);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcResource));
    npt_encode_UINT(enc, &SrcSubresource);
    npt_encode_DXGI_FORMAT(enc, &Format);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ResolveSubresource_reply(const ID3D12Resource * pDstResource,const UINT DstSubresource,const ID3D12Resource * pSrcResource,const UINT SrcSubresource,const DXGI_FORMAT Format)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip DstSubresource (input only) */
/* skip pSrcResource (input only) */
/* skip SrcSubresource (input only) */
/* skip Format (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ResolveSubresource(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D12Resource * pDstResource,
                                                        UINT DstSubresource,
                                                        ID3D12Resource * pSrcResource,
                                                        UINT SrcSubresource,
                                                        DXGI_FORMAT Format,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ResolveSubresource(pDstResource,DstSubresource,pSrcResource,SrcSubresource,Format);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ResolveSubresource_reply(pDstResource,DstSubresource,pSrcResource,SrcSubresource,Format)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ResolveSubresource(enc, cmd_flags, object_id, pDstResource, DstSubresource, pSrcResource, SrcSubresource, Format);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ResolveSubresource(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D12Resource * pDstResource,
                                                       UINT DstSubresource,
                                                       ID3D12Resource * pSrcResource,
                                                       UINT SrcSubresource,
                                                       DXGI_FORMAT Format)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ResolveSubresource(ring, 0, object_id, pDstResource, DstSubresource, pSrcResource, SrcSubresource, Format, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ResolveSubresource(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      ID3D12Resource * pDstResource,
                                                      UINT DstSubresource,
                                                      ID3D12Resource * pSrcResource,
                                                      UINT SrcSubresource,
                                                      DXGI_FORMAT Format)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ResolveSubresource(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, DstSubresource, pSrcResource, SrcSubresource, Format, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::IASetPrimitiveTopology
 */

struct npt_command_ID3D12GraphicsCommandList_IASetPrimitiveTopology {
    void *_self;
    D3D12_PRIMITIVE_TOPOLOGY PrimitiveTopology;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_IASetPrimitiveTopology(const D3D12_PRIMITIVE_TOPOLOGY PrimitiveTopology)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_PRIMITIVE_TOPOLOGY(&PrimitiveTopology, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_IASetPrimitiveTopology(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            D3D12_PRIMITIVE_TOPOLOGY PrimitiveTopology)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_IASetPrimitiveTopology(PrimitiveTopology);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_IASetPrimitiveTopology),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_PRIMITIVE_TOPOLOGY(enc, &PrimitiveTopology);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_IASetPrimitiveTopology_reply(const D3D12_PRIMITIVE_TOPOLOGY PrimitiveTopology)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip PrimitiveTopology (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_IASetPrimitiveTopology(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            D3D12_PRIMITIVE_TOPOLOGY PrimitiveTopology,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_IASetPrimitiveTopology(PrimitiveTopology);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_IASetPrimitiveTopology_reply(PrimitiveTopology)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_IASetPrimitiveTopology(enc, cmd_flags, object_id, PrimitiveTopology);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_IASetPrimitiveTopology(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           D3D12_PRIMITIVE_TOPOLOGY PrimitiveTopology)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_IASetPrimitiveTopology(ring, 0, object_id, PrimitiveTopology, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_IASetPrimitiveTopology(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          D3D12_PRIMITIVE_TOPOLOGY PrimitiveTopology)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_IASetPrimitiveTopology(ring, NPT_CMD_FLAG_REPLY, object_id, PrimitiveTopology, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::RSSetViewports
 */

struct npt_command_ID3D12GraphicsCommandList_RSSetViewports {
    void *_self;
    UINT NumViewports;
    const D3D12_VIEWPORT * pViewports;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_RSSetViewports(const UINT NumViewports,const D3D12_VIEWPORT * pViewports)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumViewports, max_mode);
    cmd_size += npt_sizeof_array_count(pViewports ? NumViewports : 0);
    for (uint32_t _i = 0; _i < (pViewports ? NumViewports : 0); _i++)
        cmd_size += npt_sizeof_D3D12_VIEWPORT(&pViewports[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_RSSetViewports(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT NumViewports,
                                                    const D3D12_VIEWPORT * pViewports)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_RSSetViewports(NumViewports,pViewports);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_RSSetViewports),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumViewports);
    if (pViewports) {
        npt_encode_array_count(enc, NumViewports);
        for (uint32_t _i = 0; _i < (uint32_t)(NumViewports); _i++)
            npt_encode_D3D12_VIEWPORT(enc, &pViewports[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_RSSetViewports_reply(const UINT NumViewports,const D3D12_VIEWPORT * pViewports)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumViewports (input only) */
/* skip pViewports (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_RSSetViewports(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT NumViewports,
                                                    const D3D12_VIEWPORT * pViewports,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_RSSetViewports(NumViewports,pViewports);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_RSSetViewports_reply(NumViewports,pViewports)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_RSSetViewports(enc, cmd_flags, object_id, NumViewports, pViewports);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_RSSetViewports(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT NumViewports,
                                                   const D3D12_VIEWPORT * pViewports)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_RSSetViewports(ring, 0, object_id, NumViewports, pViewports, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_RSSetViewports(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT NumViewports,
                                                  const D3D12_VIEWPORT * pViewports)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_RSSetViewports(ring, NPT_CMD_FLAG_REPLY, object_id, NumViewports, pViewports, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::RSSetScissorRects
 */

struct npt_command_ID3D12GraphicsCommandList_RSSetScissorRects {
    void *_self;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_RSSetScissorRects(const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumRects, max_mode);
    cmd_size += npt_sizeof_array_count(pRects ? NumRects : 0);
    for (uint32_t _i = 0; _i < (pRects ? NumRects : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RECT(&pRects[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_RSSetScissorRects(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       UINT NumRects,
                                                       const D3D12_RECT * pRects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_RSSetScissorRects(NumRects,pRects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_RSSetScissorRects),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumRects);
    if (pRects) {
        npt_encode_array_count(enc, NumRects);
        for (uint32_t _i = 0; _i < (uint32_t)(NumRects); _i++)
            npt_encode_D3D12_RECT(enc, &pRects[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_RSSetScissorRects_reply(const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumRects (input only) */
/* skip pRects (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_RSSetScissorRects(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       UINT NumRects,
                                                       const D3D12_RECT * pRects,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_RSSetScissorRects(NumRects,pRects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_RSSetScissorRects_reply(NumRects,pRects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_RSSetScissorRects(enc, cmd_flags, object_id, NumRects, pRects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_RSSetScissorRects(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      UINT NumRects,
                                                      const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_RSSetScissorRects(ring, 0, object_id, NumRects, pRects, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_RSSetScissorRects(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT NumRects,
                                                     const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_RSSetScissorRects(ring, NPT_CMD_FLAG_REPLY, object_id, NumRects, pRects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::OMSetBlendFactor
 */

struct npt_command_ID3D12GraphicsCommandList_OMSetBlendFactor {
    void *_self;
    const FLOAT * BlendFactor;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_OMSetBlendFactor(const FLOAT * BlendFactor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_array_count(BlendFactor ? 4 : 0);
    if (BlendFactor)
        cmd_size += npt_sizeof_FLOAT_array((const FLOAT *)BlendFactor, 4);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_OMSetBlendFactor(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      const FLOAT * BlendFactor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_OMSetBlendFactor(BlendFactor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_OMSetBlendFactor),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (BlendFactor) {
        npt_encode_array_count(enc, 4);
        npt_encode_FLOAT_array(enc, (const FLOAT *)BlendFactor, 4);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_OMSetBlendFactor_reply(const FLOAT * BlendFactor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip BlendFactor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_OMSetBlendFactor(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      const FLOAT * BlendFactor,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_OMSetBlendFactor(BlendFactor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_OMSetBlendFactor_reply(BlendFactor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_OMSetBlendFactor(enc, cmd_flags, object_id, BlendFactor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_OMSetBlendFactor(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     const FLOAT * BlendFactor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_OMSetBlendFactor(ring, 0, object_id, BlendFactor, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_OMSetBlendFactor(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    const FLOAT * BlendFactor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_OMSetBlendFactor(ring, NPT_CMD_FLAG_REPLY, object_id, BlendFactor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::OMSetStencilRef
 */

struct npt_command_ID3D12GraphicsCommandList_OMSetStencilRef {
    void *_self;
    UINT StencilRef;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_OMSetStencilRef(const UINT StencilRef)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StencilRef, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_OMSetStencilRef(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     UINT StencilRef)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_OMSetStencilRef(StencilRef);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_OMSetStencilRef),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StencilRef);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_OMSetStencilRef_reply(const UINT StencilRef)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StencilRef (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_OMSetStencilRef(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     UINT StencilRef,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_OMSetStencilRef(StencilRef);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_OMSetStencilRef_reply(StencilRef)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_OMSetStencilRef(enc, cmd_flags, object_id, StencilRef);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_OMSetStencilRef(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT StencilRef)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_OMSetStencilRef(ring, 0, object_id, StencilRef, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_OMSetStencilRef(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT StencilRef)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_OMSetStencilRef(ring, NPT_CMD_FLAG_REPLY, object_id, StencilRef, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetPipelineState
 */

struct npt_command_ID3D12GraphicsCommandList_SetPipelineState {
    void *_self;
    ID3D12PipelineState * pPipelineState;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetPipelineState(const ID3D12PipelineState * pPipelineState)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetPipelineState(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D12PipelineState * pPipelineState)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetPipelineState(pPipelineState);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetPipelineState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pPipelineState));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetPipelineState_reply(const ID3D12PipelineState * pPipelineState)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pPipelineState (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetPipelineState(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D12PipelineState * pPipelineState,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetPipelineState(pPipelineState);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetPipelineState_reply(pPipelineState)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetPipelineState(enc, cmd_flags, object_id, pPipelineState);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetPipelineState(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3D12PipelineState * pPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetPipelineState(ring, 0, object_id, pPipelineState, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetPipelineState(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D12PipelineState * pPipelineState)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetPipelineState(ring, NPT_CMD_FLAG_REPLY, object_id, pPipelineState, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::ResourceBarrier
 */

struct npt_command_ID3D12GraphicsCommandList_ResourceBarrier {
    void *_self;
    UINT NumBarriers;
    const D3D12_RESOURCE_BARRIER * pBarriers;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ResourceBarrier(const UINT NumBarriers,const D3D12_RESOURCE_BARRIER * pBarriers)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumBarriers, max_mode);
    cmd_size += npt_sizeof_array_count(pBarriers ? NumBarriers : 0);
    for (uint32_t _i = 0; _i < (pBarriers ? NumBarriers : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RESOURCE_BARRIER(&pBarriers[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ResourceBarrier(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     UINT NumBarriers,
                                                     const D3D12_RESOURCE_BARRIER * pBarriers)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ResourceBarrier(NumBarriers,pBarriers);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ResourceBarrier),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumBarriers);
    if (pBarriers) {
        npt_encode_array_count(enc, NumBarriers);
        for (uint32_t _i = 0; _i < (uint32_t)(NumBarriers); _i++)
            npt_encode_D3D12_RESOURCE_BARRIER(enc, &pBarriers[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ResourceBarrier_reply(const UINT NumBarriers,const D3D12_RESOURCE_BARRIER * pBarriers)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumBarriers (input only) */
/* skip pBarriers (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ResourceBarrier(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     UINT NumBarriers,
                                                     const D3D12_RESOURCE_BARRIER * pBarriers,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ResourceBarrier(NumBarriers,pBarriers);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ResourceBarrier_reply(NumBarriers,pBarriers)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ResourceBarrier(enc, cmd_flags, object_id, NumBarriers, pBarriers);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ResourceBarrier(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT NumBarriers,
                                                    const D3D12_RESOURCE_BARRIER * pBarriers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ResourceBarrier(ring, 0, object_id, NumBarriers, pBarriers, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ResourceBarrier(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT NumBarriers,
                                                   const D3D12_RESOURCE_BARRIER * pBarriers)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ResourceBarrier(ring, NPT_CMD_FLAG_REPLY, object_id, NumBarriers, pBarriers, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::ExecuteBundle
 */

struct npt_command_ID3D12GraphicsCommandList_ExecuteBundle {
    void *_self;
    ID3D12GraphicsCommandList * pCommandList;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ExecuteBundle(const ID3D12GraphicsCommandList * pCommandList)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ExecuteBundle(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D12GraphicsCommandList * pCommandList)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ExecuteBundle(pCommandList);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ExecuteBundle),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCommandList));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ExecuteBundle_reply(const ID3D12GraphicsCommandList * pCommandList)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCommandList (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ExecuteBundle(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   ID3D12GraphicsCommandList * pCommandList,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ExecuteBundle(pCommandList);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ExecuteBundle_reply(pCommandList)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ExecuteBundle(enc, cmd_flags, object_id, pCommandList);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ExecuteBundle(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D12GraphicsCommandList * pCommandList)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ExecuteBundle(ring, 0, object_id, pCommandList, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ExecuteBundle(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 ID3D12GraphicsCommandList * pCommandList)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ExecuteBundle(ring, NPT_CMD_FLAG_REPLY, object_id, pCommandList, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetDescriptorHeaps
 */

struct npt_command_ID3D12GraphicsCommandList_SetDescriptorHeaps {
    void *_self;
    UINT NumDescriptorHeaps;
    ID3D12DescriptorHeap ** ppDescriptorHeaps;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetDescriptorHeaps(const UINT NumDescriptorHeaps,ID3D12DescriptorHeap ** ppDescriptorHeaps)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumDescriptorHeaps, max_mode);
    cmd_size += npt_sizeof_array_count(NumDescriptorHeaps);
    cmd_size += sizeof(uint64_t) * NumDescriptorHeaps;
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetDescriptorHeaps(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        UINT NumDescriptorHeaps,
                                                        ID3D12DescriptorHeap ** ppDescriptorHeaps)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetDescriptorHeaps(NumDescriptorHeaps,ppDescriptorHeaps);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetDescriptorHeaps),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumDescriptorHeaps);
    if (ppDescriptorHeaps) {
        npt_encode_array_count(enc, NumDescriptorHeaps);
        for (uint32_t _i = 0; _i < (uint32_t)NumDescriptorHeaps; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppDescriptorHeaps[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetDescriptorHeaps_reply(const UINT NumDescriptorHeaps,ID3D12DescriptorHeap ** ppDescriptorHeaps)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumDescriptorHeaps (input only) */
/* skip ppDescriptorHeaps (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetDescriptorHeaps(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        UINT NumDescriptorHeaps,
                                                        ID3D12DescriptorHeap ** ppDescriptorHeaps,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetDescriptorHeaps(NumDescriptorHeaps,ppDescriptorHeaps);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetDescriptorHeaps_reply(NumDescriptorHeaps,ppDescriptorHeaps)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetDescriptorHeaps(enc, cmd_flags, object_id, NumDescriptorHeaps, ppDescriptorHeaps);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetDescriptorHeaps(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       UINT NumDescriptorHeaps,
                                                       ID3D12DescriptorHeap ** ppDescriptorHeaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetDescriptorHeaps(ring, 0, object_id, NumDescriptorHeaps, ppDescriptorHeaps, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetDescriptorHeaps(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      UINT NumDescriptorHeaps,
                                                      ID3D12DescriptorHeap ** ppDescriptorHeaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetDescriptorHeaps(ring, NPT_CMD_FLAG_REPLY, object_id, NumDescriptorHeaps, ppDescriptorHeaps, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootSignature
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootSignature {
    void *_self;
    ID3D12RootSignature * pRootSignature;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootSignature(const ID3D12RootSignature * pRootSignature)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootSignature(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D12RootSignature * pRootSignature)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootSignature(pRootSignature);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootSignature),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pRootSignature));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootSignature_reply(const ID3D12RootSignature * pRootSignature)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRootSignature (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetComputeRootSignature(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D12RootSignature * pRootSignature,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootSignature(pRootSignature);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootSignature_reply(pRootSignature)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetComputeRootSignature(enc, cmd_flags, object_id, pRootSignature);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetComputeRootSignature(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D12RootSignature * pRootSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootSignature(ring, 0, object_id, pRootSignature, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetComputeRootSignature(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D12RootSignature * pRootSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootSignature(ring, NPT_CMD_FLAG_REPLY, object_id, pRootSignature, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootSignature
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootSignature {
    void *_self;
    ID3D12RootSignature * pRootSignature;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootSignature(const ID3D12RootSignature * pRootSignature)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootSignature(struct npt_cs_encoder *enc,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D12RootSignature * pRootSignature)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootSignature(pRootSignature);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootSignature),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pRootSignature));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootSignature_reply(const ID3D12RootSignature * pRootSignature)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRootSignature (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootSignature(struct npt_ring *ring,
                                                              uint32_t cmd_flags,
                                                              npt_object_id object_id,
                                                              ID3D12RootSignature * pRootSignature,
                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootSignature(pRootSignature);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootSignature_reply(pRootSignature)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootSignature(enc, cmd_flags, object_id, pRootSignature);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetGraphicsRootSignature(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D12RootSignature * pRootSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootSignature(ring, 0, object_id, pRootSignature, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetGraphicsRootSignature(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D12RootSignature * pRootSignature)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootSignature(ring, NPT_CMD_FLAG_REPLY, object_id, pRootSignature, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootDescriptorTable
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(const UINT RootParameterIndex,const D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_D3D12_GPU_DESCRIPTOR_HANDLE(&BaseDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(struct npt_cs_encoder *enc,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   UINT RootParameterIndex,
                                                                   D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(RootParameterIndex,BaseDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_D3D12_GPU_DESCRIPTOR_HANDLE(enc, &BaseDescriptor);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable_reply(const UINT RootParameterIndex,const D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip BaseDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(struct npt_ring *ring,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   UINT RootParameterIndex,
                                                                   D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor,
                                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(RootParameterIndex,BaseDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable_reply(RootParameterIndex,BaseDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(enc, cmd_flags, object_id, RootParameterIndex, BaseDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  UINT RootParameterIndex,
                                                                  D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(ring, 0, object_id, RootParameterIndex, BaseDescriptor, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 UINT RootParameterIndex,
                                                                 D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, BaseDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootDescriptorTable
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(const UINT RootParameterIndex,const D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_D3D12_GPU_DESCRIPTOR_HANDLE(&BaseDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(struct npt_cs_encoder *enc,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    UINT RootParameterIndex,
                                                                    D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(RootParameterIndex,BaseDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_D3D12_GPU_DESCRIPTOR_HANDLE(enc, &BaseDescriptor);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable_reply(const UINT RootParameterIndex,const D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip BaseDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(struct npt_ring *ring,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    UINT RootParameterIndex,
                                                                    D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor,
                                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(RootParameterIndex,BaseDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable_reply(RootParameterIndex,BaseDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(enc, cmd_flags, object_id, RootParameterIndex, BaseDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(struct npt_ring *ring,
                                                                   npt_object_id object_id,
                                                                   UINT RootParameterIndex,
                                                                   D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(ring, 0, object_id, RootParameterIndex, BaseDescriptor, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  UINT RootParameterIndex,
                                                                  D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, BaseDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetComputeRoot32BitConstant
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant {
    void *_self;
    UINT RootParameterIndex;
    UINT SrcData;
    UINT DestOffsetIn32BitValues;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(const UINT RootParameterIndex,const UINT SrcData,const UINT DestOffsetIn32BitValues)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_UINT(&SrcData, max_mode);
    cmd_size += npt_sizeof_UINT(&DestOffsetIn32BitValues, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(struct npt_cs_encoder *enc,
                                                                 uint32_t cmd_flags,
                                                                 npt_object_id object_id,
                                                                 UINT RootParameterIndex,
                                                                 UINT SrcData,
                                                                 UINT DestOffsetIn32BitValues)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(RootParameterIndex,SrcData,DestOffsetIn32BitValues);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_UINT(enc, &SrcData);
    npt_encode_UINT(enc, &DestOffsetIn32BitValues);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant_reply(const UINT RootParameterIndex,const UINT SrcData,const UINT DestOffsetIn32BitValues)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip SrcData (input only) */
/* skip DestOffsetIn32BitValues (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(struct npt_ring *ring,
                                                                 uint32_t cmd_flags,
                                                                 npt_object_id object_id,
                                                                 UINT RootParameterIndex,
                                                                 UINT SrcData,
                                                                 UINT DestOffsetIn32BitValues,
                                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(RootParameterIndex,SrcData,DestOffsetIn32BitValues);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant_reply(RootParameterIndex,SrcData,DestOffsetIn32BitValues)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(enc, cmd_flags, object_id, RootParameterIndex, SrcData, DestOffsetIn32BitValues);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                UINT RootParameterIndex,
                                                                UINT SrcData,
                                                                UINT DestOffsetIn32BitValues)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(ring, 0, object_id, RootParameterIndex, SrcData, DestOffsetIn32BitValues, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               UINT RootParameterIndex,
                                                               UINT SrcData,
                                                               UINT DestOffsetIn32BitValues)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, SrcData, DestOffsetIn32BitValues, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRoot32BitConstant
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant {
    void *_self;
    UINT RootParameterIndex;
    UINT SrcData;
    UINT DestOffsetIn32BitValues;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(const UINT RootParameterIndex,const UINT SrcData,const UINT DestOffsetIn32BitValues)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_UINT(&SrcData, max_mode);
    cmd_size += npt_sizeof_UINT(&DestOffsetIn32BitValues, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  UINT RootParameterIndex,
                                                                  UINT SrcData,
                                                                  UINT DestOffsetIn32BitValues)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(RootParameterIndex,SrcData,DestOffsetIn32BitValues);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_UINT(enc, &SrcData);
    npt_encode_UINT(enc, &DestOffsetIn32BitValues);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant_reply(const UINT RootParameterIndex,const UINT SrcData,const UINT DestOffsetIn32BitValues)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip SrcData (input only) */
/* skip DestOffsetIn32BitValues (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  UINT RootParameterIndex,
                                                                  UINT SrcData,
                                                                  UINT DestOffsetIn32BitValues,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(RootParameterIndex,SrcData,DestOffsetIn32BitValues);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant_reply(RootParameterIndex,SrcData,DestOffsetIn32BitValues)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(enc, cmd_flags, object_id, RootParameterIndex, SrcData, DestOffsetIn32BitValues);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 UINT RootParameterIndex,
                                                                 UINT SrcData,
                                                                 UINT DestOffsetIn32BitValues)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(ring, 0, object_id, RootParameterIndex, SrcData, DestOffsetIn32BitValues, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                UINT RootParameterIndex,
                                                                UINT SrcData,
                                                                UINT DestOffsetIn32BitValues)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, SrcData, DestOffsetIn32BitValues, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetComputeRoot32BitConstants
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants {
    void *_self;
    UINT RootParameterIndex;
    UINT Num32BitValuesToSet;
    const void * pSrcData;
    UINT DestOffsetIn32BitValues;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(const UINT RootParameterIndex,const UINT Num32BitValuesToSet,const void * pSrcData,const UINT DestOffsetIn32BitValues)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_UINT(&Num32BitValuesToSet, max_mode);
    if (pSrcData) {
        cmd_size += npt_sizeof_array_count(Num32BitValuesToSet * sizeof(UINT));
        cmd_size += npt_sizeof_blob_array(pSrcData, Num32BitValuesToSet * sizeof(UINT));
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&DestOffsetIn32BitValues, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  UINT RootParameterIndex,
                                                                  UINT Num32BitValuesToSet,
                                                                  const void * pSrcData,
                                                                  UINT DestOffsetIn32BitValues)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(RootParameterIndex,Num32BitValuesToSet,pSrcData,DestOffsetIn32BitValues);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_UINT(enc, &Num32BitValuesToSet);
    if (pSrcData) {
        npt_encode_array_count(enc, Num32BitValuesToSet * sizeof(UINT));
        npt_encode_blob_array(enc, pSrcData, Num32BitValuesToSet * sizeof(UINT));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &DestOffsetIn32BitValues);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants_reply(const UINT RootParameterIndex,const UINT Num32BitValuesToSet,const void * pSrcData,const UINT DestOffsetIn32BitValues)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip Num32BitValuesToSet (input only) */
/* skip pSrcData (input only) */
/* skip DestOffsetIn32BitValues (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  UINT RootParameterIndex,
                                                                  UINT Num32BitValuesToSet,
                                                                  const void * pSrcData,
                                                                  UINT DestOffsetIn32BitValues,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(RootParameterIndex,Num32BitValuesToSet,pSrcData,DestOffsetIn32BitValues);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants_reply(RootParameterIndex,Num32BitValuesToSet,pSrcData,DestOffsetIn32BitValues)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(enc, cmd_flags, object_id, RootParameterIndex, Num32BitValuesToSet, pSrcData, DestOffsetIn32BitValues);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 UINT RootParameterIndex,
                                                                 UINT Num32BitValuesToSet,
                                                                 const void * pSrcData,
                                                                 UINT DestOffsetIn32BitValues)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(ring, 0, object_id, RootParameterIndex, Num32BitValuesToSet, pSrcData, DestOffsetIn32BitValues, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                UINT RootParameterIndex,
                                                                UINT Num32BitValuesToSet,
                                                                const void * pSrcData,
                                                                UINT DestOffsetIn32BitValues)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, Num32BitValuesToSet, pSrcData, DestOffsetIn32BitValues, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRoot32BitConstants
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants {
    void *_self;
    UINT RootParameterIndex;
    UINT Num32BitValuesToSet;
    const void * pSrcData;
    UINT DestOffsetIn32BitValues;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(const UINT RootParameterIndex,const UINT Num32BitValuesToSet,const void * pSrcData,const UINT DestOffsetIn32BitValues)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_UINT(&Num32BitValuesToSet, max_mode);
    if (pSrcData) {
        cmd_size += npt_sizeof_array_count(Num32BitValuesToSet * sizeof(UINT));
        cmd_size += npt_sizeof_blob_array(pSrcData, Num32BitValuesToSet * sizeof(UINT));
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&DestOffsetIn32BitValues, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(struct npt_cs_encoder *enc,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   UINT RootParameterIndex,
                                                                   UINT Num32BitValuesToSet,
                                                                   const void * pSrcData,
                                                                   UINT DestOffsetIn32BitValues)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(RootParameterIndex,Num32BitValuesToSet,pSrcData,DestOffsetIn32BitValues);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_UINT(enc, &Num32BitValuesToSet);
    if (pSrcData) {
        npt_encode_array_count(enc, Num32BitValuesToSet * sizeof(UINT));
        npt_encode_blob_array(enc, pSrcData, Num32BitValuesToSet * sizeof(UINT));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &DestOffsetIn32BitValues);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants_reply(const UINT RootParameterIndex,const UINT Num32BitValuesToSet,const void * pSrcData,const UINT DestOffsetIn32BitValues)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip Num32BitValuesToSet (input only) */
/* skip pSrcData (input only) */
/* skip DestOffsetIn32BitValues (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(struct npt_ring *ring,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   UINT RootParameterIndex,
                                                                   UINT Num32BitValuesToSet,
                                                                   const void * pSrcData,
                                                                   UINT DestOffsetIn32BitValues,
                                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(RootParameterIndex,Num32BitValuesToSet,pSrcData,DestOffsetIn32BitValues);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants_reply(RootParameterIndex,Num32BitValuesToSet,pSrcData,DestOffsetIn32BitValues)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(enc, cmd_flags, object_id, RootParameterIndex, Num32BitValuesToSet, pSrcData, DestOffsetIn32BitValues);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  UINT RootParameterIndex,
                                                                  UINT Num32BitValuesToSet,
                                                                  const void * pSrcData,
                                                                  UINT DestOffsetIn32BitValues)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(ring, 0, object_id, RootParameterIndex, Num32BitValuesToSet, pSrcData, DestOffsetIn32BitValues, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 UINT RootParameterIndex,
                                                                 UINT Num32BitValuesToSet,
                                                                 const void * pSrcData,
                                                                 UINT DestOffsetIn32BitValues)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, Num32BitValuesToSet, pSrcData, DestOffsetIn32BitValues, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootConstantBufferView
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS(&BufferLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(struct npt_cs_encoder *enc,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      UINT RootParameterIndex,
                                                                      D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(RootParameterIndex,BufferLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_D3D12_GPU_VIRTUAL_ADDRESS(enc, &BufferLocation);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView_reply(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip BufferLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(struct npt_ring *ring,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      UINT RootParameterIndex,
                                                                      D3D12_GPU_VIRTUAL_ADDRESS BufferLocation,
                                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(RootParameterIndex,BufferLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView_reply(RootParameterIndex,BufferLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(enc, cmd_flags, object_id, RootParameterIndex, BufferLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     UINT RootParameterIndex,
                                                                     D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(ring, 0, object_id, RootParameterIndex, BufferLocation, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    UINT RootParameterIndex,
                                                                    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, BufferLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootConstantBufferView
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS(&BufferLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(struct npt_cs_encoder *enc,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       UINT RootParameterIndex,
                                                                       D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(RootParameterIndex,BufferLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_D3D12_GPU_VIRTUAL_ADDRESS(enc, &BufferLocation);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView_reply(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip BufferLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(struct npt_ring *ring,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       UINT RootParameterIndex,
                                                                       D3D12_GPU_VIRTUAL_ADDRESS BufferLocation,
                                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(RootParameterIndex,BufferLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView_reply(RootParameterIndex,BufferLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(enc, cmd_flags, object_id, RootParameterIndex, BufferLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      UINT RootParameterIndex,
                                                                      D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(ring, 0, object_id, RootParameterIndex, BufferLocation, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     UINT RootParameterIndex,
                                                                     D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, BufferLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootShaderResourceView
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS(&BufferLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(struct npt_cs_encoder *enc,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      UINT RootParameterIndex,
                                                                      D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(RootParameterIndex,BufferLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_D3D12_GPU_VIRTUAL_ADDRESS(enc, &BufferLocation);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView_reply(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip BufferLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(struct npt_ring *ring,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      UINT RootParameterIndex,
                                                                      D3D12_GPU_VIRTUAL_ADDRESS BufferLocation,
                                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(RootParameterIndex,BufferLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView_reply(RootParameterIndex,BufferLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(enc, cmd_flags, object_id, RootParameterIndex, BufferLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     UINT RootParameterIndex,
                                                                     D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(ring, 0, object_id, RootParameterIndex, BufferLocation, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    UINT RootParameterIndex,
                                                                    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, BufferLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootShaderResourceView
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS(&BufferLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(struct npt_cs_encoder *enc,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       UINT RootParameterIndex,
                                                                       D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(RootParameterIndex,BufferLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_D3D12_GPU_VIRTUAL_ADDRESS(enc, &BufferLocation);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView_reply(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip BufferLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(struct npt_ring *ring,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       UINT RootParameterIndex,
                                                                       D3D12_GPU_VIRTUAL_ADDRESS BufferLocation,
                                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(RootParameterIndex,BufferLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView_reply(RootParameterIndex,BufferLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(enc, cmd_flags, object_id, RootParameterIndex, BufferLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      UINT RootParameterIndex,
                                                                      D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(ring, 0, object_id, RootParameterIndex, BufferLocation, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     UINT RootParameterIndex,
                                                                     D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, BufferLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootUnorderedAccessView
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS(&BufferLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(struct npt_cs_encoder *enc,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       UINT RootParameterIndex,
                                                                       D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(RootParameterIndex,BufferLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_D3D12_GPU_VIRTUAL_ADDRESS(enc, &BufferLocation);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView_reply(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip BufferLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(struct npt_ring *ring,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       UINT RootParameterIndex,
                                                                       D3D12_GPU_VIRTUAL_ADDRESS BufferLocation,
                                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(RootParameterIndex,BufferLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView_reply(RootParameterIndex,BufferLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(enc, cmd_flags, object_id, RootParameterIndex, BufferLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      UINT RootParameterIndex,
                                                                      D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(ring, 0, object_id, RootParameterIndex, BufferLocation, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     UINT RootParameterIndex,
                                                                     D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, BufferLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootUnorderedAccessView
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&RootParameterIndex, max_mode);
    cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS(&BufferLocation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(struct npt_cs_encoder *enc,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        UINT RootParameterIndex,
                                                                        D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(RootParameterIndex,BufferLocation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &RootParameterIndex);
    npt_encode_D3D12_GPU_VIRTUAL_ADDRESS(enc, &BufferLocation);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView_reply(const UINT RootParameterIndex,const D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RootParameterIndex (input only) */
/* skip BufferLocation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(struct npt_ring *ring,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        UINT RootParameterIndex,
                                                                        D3D12_GPU_VIRTUAL_ADDRESS BufferLocation,
                                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(RootParameterIndex,BufferLocation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView_reply(RootParameterIndex,BufferLocation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(enc, cmd_flags, object_id, RootParameterIndex, BufferLocation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(struct npt_ring *ring,
                                                                       npt_object_id object_id,
                                                                       UINT RootParameterIndex,
                                                                       D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(ring, 0, object_id, RootParameterIndex, BufferLocation, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      UINT RootParameterIndex,
                                                                      D3D12_GPU_VIRTUAL_ADDRESS BufferLocation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(ring, NPT_CMD_FLAG_REPLY, object_id, RootParameterIndex, BufferLocation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::IASetIndexBuffer
 */

struct npt_command_ID3D12GraphicsCommandList_IASetIndexBuffer {
    void *_self;
    const D3D12_INDEX_BUFFER_VIEW * pView;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_IASetIndexBuffer(const D3D12_INDEX_BUFFER_VIEW * pView)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pView);
    if (pView)
        cmd_size += npt_sizeof_D3D12_INDEX_BUFFER_VIEW(pView, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_IASetIndexBuffer(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      const D3D12_INDEX_BUFFER_VIEW * pView)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_IASetIndexBuffer(pView);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_IASetIndexBuffer),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pView))
        npt_encode_D3D12_INDEX_BUFFER_VIEW(enc, pView);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_IASetIndexBuffer_reply(const D3D12_INDEX_BUFFER_VIEW * pView)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pView (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_IASetIndexBuffer(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      const D3D12_INDEX_BUFFER_VIEW * pView,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_IASetIndexBuffer(pView);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_IASetIndexBuffer_reply(pView)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_IASetIndexBuffer(enc, cmd_flags, object_id, pView);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_IASetIndexBuffer(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     const D3D12_INDEX_BUFFER_VIEW * pView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_IASetIndexBuffer(ring, 0, object_id, pView, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_IASetIndexBuffer(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    const D3D12_INDEX_BUFFER_VIEW * pView)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_IASetIndexBuffer(ring, NPT_CMD_FLAG_REPLY, object_id, pView, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::IASetVertexBuffers
 */

struct npt_command_ID3D12GraphicsCommandList_IASetVertexBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    const D3D12_VERTEX_BUFFER_VIEW * pViews;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_IASetVertexBuffers(const UINT StartSlot,const UINT NumViews,const D3D12_VERTEX_BUFFER_VIEW * pViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
    cmd_size += npt_sizeof_array_count(pViews ? NumViews : 0);
    for (uint32_t _i = 0; _i < (pViews ? NumViews : 0); _i++)
        cmd_size += npt_sizeof_D3D12_VERTEX_BUFFER_VIEW(&pViews[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_IASetVertexBuffers(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        UINT StartSlot,
                                                        UINT NumViews,
                                                        const D3D12_VERTEX_BUFFER_VIEW * pViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_IASetVertexBuffers(StartSlot,NumViews,pViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_IASetVertexBuffers),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
    if (pViews) {
        npt_encode_array_count(enc, NumViews);
        for (uint32_t _i = 0; _i < (uint32_t)(NumViews); _i++)
            npt_encode_D3D12_VERTEX_BUFFER_VIEW(enc, &pViews[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_IASetVertexBuffers_reply(const UINT StartSlot,const UINT NumViews,const D3D12_VERTEX_BUFFER_VIEW * pViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip pViews (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_IASetVertexBuffers(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        UINT StartSlot,
                                                        UINT NumViews,
                                                        const D3D12_VERTEX_BUFFER_VIEW * pViews,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_IASetVertexBuffers(StartSlot,NumViews,pViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_IASetVertexBuffers_reply(StartSlot,NumViews,pViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_IASetVertexBuffers(enc, cmd_flags, object_id, StartSlot, NumViews, pViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_IASetVertexBuffers(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       UINT StartSlot,
                                                       UINT NumViews,
                                                       const D3D12_VERTEX_BUFFER_VIEW * pViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_IASetVertexBuffers(ring, 0, object_id, StartSlot, NumViews, pViews, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_IASetVertexBuffers(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      UINT StartSlot,
                                                      UINT NumViews,
                                                      const D3D12_VERTEX_BUFFER_VIEW * pViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_IASetVertexBuffers(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, pViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SOSetTargets
 */

struct npt_command_ID3D12GraphicsCommandList_SOSetTargets {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    const D3D12_STREAM_OUTPUT_BUFFER_VIEW * pViews;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SOSetTargets(const UINT StartSlot,const UINT NumViews,const D3D12_STREAM_OUTPUT_BUFFER_VIEW * pViews)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&StartSlot, max_mode);
    cmd_size += npt_sizeof_UINT(&NumViews, max_mode);
    cmd_size += npt_sizeof_array_count(pViews ? NumViews : 0);
    for (uint32_t _i = 0; _i < (pViews ? NumViews : 0); _i++)
        cmd_size += npt_sizeof_D3D12_STREAM_OUTPUT_BUFFER_VIEW(&pViews[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SOSetTargets(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  const D3D12_STREAM_OUTPUT_BUFFER_VIEW * pViews)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SOSetTargets(StartSlot,NumViews,pViews);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SOSetTargets),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &StartSlot);
    npt_encode_UINT(enc, &NumViews);
    if (pViews) {
        npt_encode_array_count(enc, NumViews);
        for (uint32_t _i = 0; _i < (uint32_t)(NumViews); _i++)
            npt_encode_D3D12_STREAM_OUTPUT_BUFFER_VIEW(enc, &pViews[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SOSetTargets_reply(const UINT StartSlot,const UINT NumViews,const D3D12_STREAM_OUTPUT_BUFFER_VIEW * pViews)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip StartSlot (input only) */
/* skip NumViews (input only) */
/* skip pViews (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SOSetTargets(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  UINT StartSlot,
                                                  UINT NumViews,
                                                  const D3D12_STREAM_OUTPUT_BUFFER_VIEW * pViews,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SOSetTargets(StartSlot,NumViews,pViews);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SOSetTargets_reply(StartSlot,NumViews,pViews)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SOSetTargets(enc, cmd_flags, object_id, StartSlot, NumViews, pViews);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SOSetTargets(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT StartSlot,
                                                 UINT NumViews,
                                                 const D3D12_STREAM_OUTPUT_BUFFER_VIEW * pViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SOSetTargets(ring, 0, object_id, StartSlot, NumViews, pViews, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SOSetTargets(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                UINT StartSlot,
                                                UINT NumViews,
                                                const D3D12_STREAM_OUTPUT_BUFFER_VIEW * pViews)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SOSetTargets(ring, NPT_CMD_FLAG_REPLY, object_id, StartSlot, NumViews, pViews, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::OMSetRenderTargets
 */

struct npt_command_ID3D12GraphicsCommandList_OMSetRenderTargets {
    void *_self;
    UINT NumRenderTargetDescriptors;
    const D3D12_CPU_DESCRIPTOR_HANDLE * pRenderTargetDescriptors;
    BOOL RTsSingleHandleToDescriptorRange;
    const D3D12_CPU_DESCRIPTOR_HANDLE * pDepthStencilDescriptor;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_OMSetRenderTargets(const UINT NumRenderTargetDescriptors,const D3D12_CPU_DESCRIPTOR_HANDLE * pRenderTargetDescriptors,const BOOL RTsSingleHandleToDescriptorRange,const D3D12_CPU_DESCRIPTOR_HANDLE * pDepthStencilDescriptor)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumRenderTargetDescriptors, max_mode);
    cmd_size += npt_sizeof_array_count(pRenderTargetDescriptors ? RTsSingleHandleToDescriptorRange ? 1 : NumRenderTargetDescriptors : 0);
    for (uint32_t _i = 0; _i < (pRenderTargetDescriptors ? RTsSingleHandleToDescriptorRange ? 1 : NumRenderTargetDescriptors : 0); _i++)
        cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&pRenderTargetDescriptors[_i], max_mode);
    cmd_size += npt_sizeof_BOOL(&RTsSingleHandleToDescriptorRange, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDepthStencilDescriptor);
    if (pDepthStencilDescriptor)
        cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(pDepthStencilDescriptor, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_OMSetRenderTargets(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        UINT NumRenderTargetDescriptors,
                                                        const D3D12_CPU_DESCRIPTOR_HANDLE * pRenderTargetDescriptors,
                                                        BOOL RTsSingleHandleToDescriptorRange,
                                                        const D3D12_CPU_DESCRIPTOR_HANDLE * pDepthStencilDescriptor)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_OMSetRenderTargets(NumRenderTargetDescriptors,pRenderTargetDescriptors,RTsSingleHandleToDescriptorRange,pDepthStencilDescriptor);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_OMSetRenderTargets),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumRenderTargetDescriptors);
    if (pRenderTargetDescriptors) {
        npt_encode_array_count(enc, RTsSingleHandleToDescriptorRange ? 1 : NumRenderTargetDescriptors);
        for (uint32_t _i = 0; _i < (uint32_t)(RTsSingleHandleToDescriptorRange ? 1 : NumRenderTargetDescriptors); _i++)
            npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &pRenderTargetDescriptors[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_BOOL(enc, &RTsSingleHandleToDescriptorRange);
    if (npt_encode_simple_pointer(enc, pDepthStencilDescriptor))
        npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, pDepthStencilDescriptor);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_OMSetRenderTargets_reply(const UINT NumRenderTargetDescriptors,const D3D12_CPU_DESCRIPTOR_HANDLE * pRenderTargetDescriptors,const BOOL RTsSingleHandleToDescriptorRange,const D3D12_CPU_DESCRIPTOR_HANDLE * pDepthStencilDescriptor)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumRenderTargetDescriptors (input only) */
/* skip pRenderTargetDescriptors (input only) */
/* skip RTsSingleHandleToDescriptorRange (input only) */
/* skip pDepthStencilDescriptor (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_OMSetRenderTargets(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        UINT NumRenderTargetDescriptors,
                                                        const D3D12_CPU_DESCRIPTOR_HANDLE * pRenderTargetDescriptors,
                                                        BOOL RTsSingleHandleToDescriptorRange,
                                                        const D3D12_CPU_DESCRIPTOR_HANDLE * pDepthStencilDescriptor,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_OMSetRenderTargets(NumRenderTargetDescriptors,pRenderTargetDescriptors,RTsSingleHandleToDescriptorRange,pDepthStencilDescriptor);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_OMSetRenderTargets_reply(NumRenderTargetDescriptors,pRenderTargetDescriptors,RTsSingleHandleToDescriptorRange,pDepthStencilDescriptor)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_OMSetRenderTargets(enc, cmd_flags, object_id, NumRenderTargetDescriptors, pRenderTargetDescriptors, RTsSingleHandleToDescriptorRange, pDepthStencilDescriptor);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_OMSetRenderTargets(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       UINT NumRenderTargetDescriptors,
                                                       const D3D12_CPU_DESCRIPTOR_HANDLE * pRenderTargetDescriptors,
                                                       BOOL RTsSingleHandleToDescriptorRange,
                                                       const D3D12_CPU_DESCRIPTOR_HANDLE * pDepthStencilDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_OMSetRenderTargets(ring, 0, object_id, NumRenderTargetDescriptors, pRenderTargetDescriptors, RTsSingleHandleToDescriptorRange, pDepthStencilDescriptor, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_OMSetRenderTargets(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      UINT NumRenderTargetDescriptors,
                                                      const D3D12_CPU_DESCRIPTOR_HANDLE * pRenderTargetDescriptors,
                                                      BOOL RTsSingleHandleToDescriptorRange,
                                                      const D3D12_CPU_DESCRIPTOR_HANDLE * pDepthStencilDescriptor)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_OMSetRenderTargets(ring, NPT_CMD_FLAG_REPLY, object_id, NumRenderTargetDescriptors, pRenderTargetDescriptors, RTsSingleHandleToDescriptorRange, pDepthStencilDescriptor, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::ClearDepthStencilView
 */

struct npt_command_ID3D12GraphicsCommandList_ClearDepthStencilView {
    void *_self;
    D3D12_CPU_DESCRIPTOR_HANDLE DepthStencilView;
    D3D12_CLEAR_FLAGS ClearFlags;
    FLOAT Depth;
    UINT8 Stencil;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearDepthStencilView(const D3D12_CPU_DESCRIPTOR_HANDLE DepthStencilView,const D3D12_CLEAR_FLAGS ClearFlags,const FLOAT Depth,const UINT8 Stencil,const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&DepthStencilView, max_mode);
    cmd_size += npt_sizeof_D3D12_CLEAR_FLAGS(&ClearFlags, max_mode);
    cmd_size += npt_sizeof_FLOAT(&Depth, max_mode);
    cmd_size += npt_sizeof_UINT8(&Stencil, max_mode);
    cmd_size += npt_sizeof_UINT(&NumRects, max_mode);
    cmd_size += npt_sizeof_array_count(pRects ? NumRects : 0);
    for (uint32_t _i = 0; _i < (pRects ? NumRects : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RECT(&pRects[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearDepthStencilView(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           D3D12_CPU_DESCRIPTOR_HANDLE DepthStencilView,
                                                           D3D12_CLEAR_FLAGS ClearFlags,
                                                           FLOAT Depth,
                                                           UINT8 Stencil,
                                                           UINT NumRects,
                                                           const D3D12_RECT * pRects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ClearDepthStencilView(DepthStencilView,ClearFlags,Depth,Stencil,NumRects,pRects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearDepthStencilView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &DepthStencilView);
    npt_encode_D3D12_CLEAR_FLAGS(enc, &ClearFlags);
    npt_encode_FLOAT(enc, &Depth);
    npt_encode_UINT8(enc, &Stencil);
    npt_encode_UINT(enc, &NumRects);
    if (pRects) {
        npt_encode_array_count(enc, NumRects);
        for (uint32_t _i = 0; _i < (uint32_t)(NumRects); _i++)
            npt_encode_D3D12_RECT(enc, &pRects[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearDepthStencilView_reply(const D3D12_CPU_DESCRIPTOR_HANDLE DepthStencilView,const D3D12_CLEAR_FLAGS ClearFlags,const FLOAT Depth,const UINT8 Stencil,const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip DepthStencilView (input only) */
/* skip ClearFlags (input only) */
/* skip Depth (input only) */
/* skip Stencil (input only) */
/* skip NumRects (input only) */
/* skip pRects (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ClearDepthStencilView(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           D3D12_CPU_DESCRIPTOR_HANDLE DepthStencilView,
                                                           D3D12_CLEAR_FLAGS ClearFlags,
                                                           FLOAT Depth,
                                                           UINT8 Stencil,
                                                           UINT NumRects,
                                                           const D3D12_RECT * pRects,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ClearDepthStencilView(DepthStencilView,ClearFlags,Depth,Stencil,NumRects,pRects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ClearDepthStencilView_reply(DepthStencilView,ClearFlags,Depth,Stencil,NumRects,pRects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ClearDepthStencilView(enc, cmd_flags, object_id, DepthStencilView, ClearFlags, Depth, Stencil, NumRects, pRects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ClearDepthStencilView(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          D3D12_CPU_DESCRIPTOR_HANDLE DepthStencilView,
                                                          D3D12_CLEAR_FLAGS ClearFlags,
                                                          FLOAT Depth,
                                                          UINT8 Stencil,
                                                          UINT NumRects,
                                                          const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearDepthStencilView(ring, 0, object_id, DepthStencilView, ClearFlags, Depth, Stencil, NumRects, pRects, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ClearDepthStencilView(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         D3D12_CPU_DESCRIPTOR_HANDLE DepthStencilView,
                                                         D3D12_CLEAR_FLAGS ClearFlags,
                                                         FLOAT Depth,
                                                         UINT8 Stencil,
                                                         UINT NumRects,
                                                         const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearDepthStencilView(ring, NPT_CMD_FLAG_REPLY, object_id, DepthStencilView, ClearFlags, Depth, Stencil, NumRects, pRects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::ClearRenderTargetView
 */

struct npt_command_ID3D12GraphicsCommandList_ClearRenderTargetView {
    void *_self;
    D3D12_CPU_DESCRIPTOR_HANDLE RenderTargetView;
    const FLOAT * ColorRGBA;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearRenderTargetView(const D3D12_CPU_DESCRIPTOR_HANDLE RenderTargetView,const FLOAT * ColorRGBA,const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&RenderTargetView, max_mode);
    cmd_size += npt_sizeof_array_count(4);
    cmd_size += npt_sizeof_FLOAT_array((const FLOAT *)ColorRGBA, 4);
    cmd_size += npt_sizeof_UINT(&NumRects, max_mode);
    cmd_size += npt_sizeof_array_count(pRects ? NumRects : 0);
    for (uint32_t _i = 0; _i < (pRects ? NumRects : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RECT(&pRects[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearRenderTargetView(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           D3D12_CPU_DESCRIPTOR_HANDLE RenderTargetView,
                                                           const FLOAT * ColorRGBA,
                                                           UINT NumRects,
                                                           const D3D12_RECT * pRects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ClearRenderTargetView(RenderTargetView,ColorRGBA,NumRects,pRects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearRenderTargetView),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &RenderTargetView);
    npt_encode_array_count(enc, 4);
    npt_encode_FLOAT_array(enc, (const FLOAT *)ColorRGBA, 4);
    npt_encode_UINT(enc, &NumRects);
    if (pRects) {
        npt_encode_array_count(enc, NumRects);
        for (uint32_t _i = 0; _i < (uint32_t)(NumRects); _i++)
            npt_encode_D3D12_RECT(enc, &pRects[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearRenderTargetView_reply(const D3D12_CPU_DESCRIPTOR_HANDLE RenderTargetView,const FLOAT * ColorRGBA,const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip RenderTargetView (input only) */
/* skip ColorRGBA (input only) */
/* skip NumRects (input only) */
/* skip pRects (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ClearRenderTargetView(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           D3D12_CPU_DESCRIPTOR_HANDLE RenderTargetView,
                                                           const FLOAT * ColorRGBA,
                                                           UINT NumRects,
                                                           const D3D12_RECT * pRects,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ClearRenderTargetView(RenderTargetView,ColorRGBA,NumRects,pRects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ClearRenderTargetView_reply(RenderTargetView,ColorRGBA,NumRects,pRects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ClearRenderTargetView(enc, cmd_flags, object_id, RenderTargetView, ColorRGBA, NumRects, pRects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ClearRenderTargetView(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          D3D12_CPU_DESCRIPTOR_HANDLE RenderTargetView,
                                                          const FLOAT * ColorRGBA,
                                                          UINT NumRects,
                                                          const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearRenderTargetView(ring, 0, object_id, RenderTargetView, ColorRGBA, NumRects, pRects, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ClearRenderTargetView(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         D3D12_CPU_DESCRIPTOR_HANDLE RenderTargetView,
                                                         const FLOAT * ColorRGBA,
                                                         UINT NumRects,
                                                         const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearRenderTargetView(ring, NPT_CMD_FLAG_REPLY, object_id, RenderTargetView, ColorRGBA, NumRects, pRects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::ClearUnorderedAccessViewUint
 */

struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint {
    void *_self;
    D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap;
    D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle;
    ID3D12Resource * pResource;
    const UINT * Values;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(const D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,const D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,const ID3D12Resource * pResource,const UINT * Values,const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_GPU_DESCRIPTOR_HANDLE(&ViewGPUHandleInCurrentHeap, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&ViewCPUHandle, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(4);
    cmd_size += npt_sizeof_UINT_array((const UINT *)Values, 4);
    cmd_size += npt_sizeof_UINT(&NumRects, max_mode);
    cmd_size += npt_sizeof_array_count(pRects ? NumRects : 0);
    for (uint32_t _i = 0; _i < (pRects ? NumRects : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RECT(&pRects[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,
                                                                  D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,
                                                                  ID3D12Resource * pResource,
                                                                  const UINT * Values,
                                                                  UINT NumRects,
                                                                  const D3D12_RECT * pRects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(ViewGPUHandleInCurrentHeap,ViewCPUHandle,pResource,Values,NumRects,pRects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_GPU_DESCRIPTOR_HANDLE(enc, &ViewGPUHandleInCurrentHeap);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &ViewCPUHandle);
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    npt_encode_array_count(enc, 4);
    npt_encode_UINT_array(enc, (const UINT *)Values, 4);
    npt_encode_UINT(enc, &NumRects);
    if (pRects) {
        npt_encode_array_count(enc, NumRects);
        for (uint32_t _i = 0; _i < (uint32_t)(NumRects); _i++)
            npt_encode_D3D12_RECT(enc, &pRects[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint_reply(const D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,const D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,const ID3D12Resource * pResource,const UINT * Values,const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ViewGPUHandleInCurrentHeap (input only) */
/* skip ViewCPUHandle (input only) */
/* skip pResource (input only) */
/* skip Values (input only) */
/* skip NumRects (input only) */
/* skip pRects (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,
                                                                  D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,
                                                                  ID3D12Resource * pResource,
                                                                  const UINT * Values,
                                                                  UINT NumRects,
                                                                  const D3D12_RECT * pRects,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(ViewGPUHandleInCurrentHeap,ViewCPUHandle,pResource,Values,NumRects,pRects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint_reply(ViewGPUHandleInCurrentHeap,ViewCPUHandle,pResource,Values,NumRects,pRects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(enc, cmd_flags, object_id, ViewGPUHandleInCurrentHeap, ViewCPUHandle, pResource, Values, NumRects, pRects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,
                                                                 D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,
                                                                 ID3D12Resource * pResource,
                                                                 const UINT * Values,
                                                                 UINT NumRects,
                                                                 const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(ring, 0, object_id, ViewGPUHandleInCurrentHeap, ViewCPUHandle, pResource, Values, NumRects, pRects, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,
                                                                D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,
                                                                ID3D12Resource * pResource,
                                                                const UINT * Values,
                                                                UINT NumRects,
                                                                const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(ring, NPT_CMD_FLAG_REPLY, object_id, ViewGPUHandleInCurrentHeap, ViewCPUHandle, pResource, Values, NumRects, pRects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::ClearUnorderedAccessViewFloat
 */

struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat {
    void *_self;
    D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap;
    D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle;
    ID3D12Resource * pResource;
    const FLOAT * Values;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(const D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,const D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,const ID3D12Resource * pResource,const FLOAT * Values,const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_GPU_DESCRIPTOR_HANDLE(&ViewGPUHandleInCurrentHeap, max_mode);
    cmd_size += npt_sizeof_D3D12_CPU_DESCRIPTOR_HANDLE(&ViewCPUHandle, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_array_count(4);
    cmd_size += npt_sizeof_FLOAT_array((const FLOAT *)Values, 4);
    cmd_size += npt_sizeof_UINT(&NumRects, max_mode);
    cmd_size += npt_sizeof_array_count(pRects ? NumRects : 0);
    for (uint32_t _i = 0; _i < (pRects ? NumRects : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RECT(&pRects[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(struct npt_cs_encoder *enc,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,
                                                                   D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,
                                                                   ID3D12Resource * pResource,
                                                                   const FLOAT * Values,
                                                                   UINT NumRects,
                                                                   const D3D12_RECT * pRects)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(ViewGPUHandleInCurrentHeap,ViewCPUHandle,pResource,Values,NumRects,pRects);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_GPU_DESCRIPTOR_HANDLE(enc, &ViewGPUHandleInCurrentHeap);
    npt_encode_D3D12_CPU_DESCRIPTOR_HANDLE(enc, &ViewCPUHandle);
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    npt_encode_array_count(enc, 4);
    npt_encode_FLOAT_array(enc, (const FLOAT *)Values, 4);
    npt_encode_UINT(enc, &NumRects);
    if (pRects) {
        npt_encode_array_count(enc, NumRects);
        for (uint32_t _i = 0; _i < (uint32_t)(NumRects); _i++)
            npt_encode_D3D12_RECT(enc, &pRects[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat_reply(const D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,const D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,const ID3D12Resource * pResource,const FLOAT * Values,const UINT NumRects,const D3D12_RECT * pRects)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ViewGPUHandleInCurrentHeap (input only) */
/* skip ViewCPUHandle (input only) */
/* skip pResource (input only) */
/* skip Values (input only) */
/* skip NumRects (input only) */
/* skip pRects (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(struct npt_ring *ring,
                                                                   uint32_t cmd_flags,
                                                                   npt_object_id object_id,
                                                                   D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,
                                                                   D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,
                                                                   ID3D12Resource * pResource,
                                                                   const FLOAT * Values,
                                                                   UINT NumRects,
                                                                   const D3D12_RECT * pRects,
                                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(ViewGPUHandleInCurrentHeap,ViewCPUHandle,pResource,Values,NumRects,pRects);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat_reply(ViewGPUHandleInCurrentHeap,ViewCPUHandle,pResource,Values,NumRects,pRects)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(enc, cmd_flags, object_id, ViewGPUHandleInCurrentHeap, ViewCPUHandle, pResource, Values, NumRects, pRects);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,
                                                                  D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,
                                                                  ID3D12Resource * pResource,
                                                                  const FLOAT * Values,
                                                                  UINT NumRects,
                                                                  const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(ring, 0, object_id, ViewGPUHandleInCurrentHeap, ViewCPUHandle, pResource, Values, NumRects, pRects, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap,
                                                                 D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle,
                                                                 ID3D12Resource * pResource,
                                                                 const FLOAT * Values,
                                                                 UINT NumRects,
                                                                 const D3D12_RECT * pRects)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(ring, NPT_CMD_FLAG_REPLY, object_id, ViewGPUHandleInCurrentHeap, ViewCPUHandle, pResource, Values, NumRects, pRects, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::DiscardResource
 */

struct npt_command_ID3D12GraphicsCommandList_DiscardResource {
    void *_self;
    ID3D12Resource * pResource;
    const D3D12_DISCARD_REGION * pRegion;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_DiscardResource(const ID3D12Resource * pResource,const D3D12_DISCARD_REGION * pRegion)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pRegion);
    if (pRegion)
        cmd_size += npt_sizeof_D3D12_DISCARD_REGION(pRegion, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_DiscardResource(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D12Resource * pResource,
                                                     const D3D12_DISCARD_REGION * pRegion)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_DiscardResource(pResource,pRegion);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_DiscardResource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pResource));
    if (npt_encode_simple_pointer(enc, pRegion))
        npt_encode_D3D12_DISCARD_REGION(enc, pRegion);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_DiscardResource_reply(const ID3D12Resource * pResource,const D3D12_DISCARD_REGION * pRegion)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pResource (input only) */
/* skip pRegion (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_DiscardResource(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D12Resource * pResource,
                                                     const D3D12_DISCARD_REGION * pRegion,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_DiscardResource(pResource,pRegion);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_DiscardResource_reply(pResource,pRegion)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_DiscardResource(enc, cmd_flags, object_id, pResource, pRegion);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_DiscardResource(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D12Resource * pResource,
                                                    const D3D12_DISCARD_REGION * pRegion)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_DiscardResource(ring, 0, object_id, pResource, pRegion, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_DiscardResource(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D12Resource * pResource,
                                                   const D3D12_DISCARD_REGION * pRegion)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_DiscardResource(ring, NPT_CMD_FLAG_REPLY, object_id, pResource, pRegion, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::BeginQuery
 */

struct npt_command_ID3D12GraphicsCommandList_BeginQuery {
    void *_self;
    ID3D12QueryHeap * pQueryHeap;
    D3D12_QUERY_TYPE Type;
    UINT Index;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_BeginQuery(const ID3D12QueryHeap * pQueryHeap,const D3D12_QUERY_TYPE Type,const UINT Index)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D12_QUERY_TYPE(&Type, max_mode);
    cmd_size += npt_sizeof_UINT(&Index, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_BeginQuery(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D12QueryHeap * pQueryHeap,
                                                D3D12_QUERY_TYPE Type,
                                                UINT Index)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_BeginQuery(pQueryHeap,Type,Index);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_BeginQuery),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pQueryHeap));
    npt_encode_D3D12_QUERY_TYPE(enc, &Type);
    npt_encode_UINT(enc, &Index);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_BeginQuery_reply(const ID3D12QueryHeap * pQueryHeap,const D3D12_QUERY_TYPE Type,const UINT Index)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pQueryHeap (input only) */
/* skip Type (input only) */
/* skip Index (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_BeginQuery(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                ID3D12QueryHeap * pQueryHeap,
                                                D3D12_QUERY_TYPE Type,
                                                UINT Index,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_BeginQuery(pQueryHeap,Type,Index);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_BeginQuery_reply(pQueryHeap,Type,Index)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_BeginQuery(enc, cmd_flags, object_id, pQueryHeap, Type, Index);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_BeginQuery(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               ID3D12QueryHeap * pQueryHeap,
                                               D3D12_QUERY_TYPE Type,
                                               UINT Index)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_BeginQuery(ring, 0, object_id, pQueryHeap, Type, Index, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_BeginQuery(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              ID3D12QueryHeap * pQueryHeap,
                                              D3D12_QUERY_TYPE Type,
                                              UINT Index)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_BeginQuery(ring, NPT_CMD_FLAG_REPLY, object_id, pQueryHeap, Type, Index, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::EndQuery
 */

struct npt_command_ID3D12GraphicsCommandList_EndQuery {
    void *_self;
    ID3D12QueryHeap * pQueryHeap;
    D3D12_QUERY_TYPE Type;
    UINT Index;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_EndQuery(const ID3D12QueryHeap * pQueryHeap,const D3D12_QUERY_TYPE Type,const UINT Index)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D12_QUERY_TYPE(&Type, max_mode);
    cmd_size += npt_sizeof_UINT(&Index, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_EndQuery(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D12QueryHeap * pQueryHeap,
                                              D3D12_QUERY_TYPE Type,
                                              UINT Index)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_EndQuery(pQueryHeap,Type,Index);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_EndQuery),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pQueryHeap));
    npt_encode_D3D12_QUERY_TYPE(enc, &Type);
    npt_encode_UINT(enc, &Index);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_EndQuery_reply(const ID3D12QueryHeap * pQueryHeap,const D3D12_QUERY_TYPE Type,const UINT Index)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pQueryHeap (input only) */
/* skip Type (input only) */
/* skip Index (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_EndQuery(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              ID3D12QueryHeap * pQueryHeap,
                                              D3D12_QUERY_TYPE Type,
                                              UINT Index,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_EndQuery(pQueryHeap,Type,Index);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_EndQuery_reply(pQueryHeap,Type,Index)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_EndQuery(enc, cmd_flags, object_id, pQueryHeap, Type, Index);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_EndQuery(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             ID3D12QueryHeap * pQueryHeap,
                                             D3D12_QUERY_TYPE Type,
                                             UINT Index)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_EndQuery(ring, 0, object_id, pQueryHeap, Type, Index, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_EndQuery(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            ID3D12QueryHeap * pQueryHeap,
                                            D3D12_QUERY_TYPE Type,
                                            UINT Index)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_EndQuery(ring, NPT_CMD_FLAG_REPLY, object_id, pQueryHeap, Type, Index, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::ResolveQueryData
 */

struct npt_command_ID3D12GraphicsCommandList_ResolveQueryData {
    void *_self;
    ID3D12QueryHeap * pQueryHeap;
    D3D12_QUERY_TYPE Type;
    UINT StartIndex;
    UINT NumQueries;
    ID3D12Resource * pDestinationBuffer;
    UINT64 AlignedDestinationBufferOffset;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ResolveQueryData(const ID3D12QueryHeap * pQueryHeap,const D3D12_QUERY_TYPE Type,const UINT StartIndex,const UINT NumQueries,const ID3D12Resource * pDestinationBuffer,const UINT64 AlignedDestinationBufferOffset)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D12_QUERY_TYPE(&Type, max_mode);
    cmd_size += npt_sizeof_UINT(&StartIndex, max_mode);
    cmd_size += npt_sizeof_UINT(&NumQueries, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&AlignedDestinationBufferOffset, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ResolveQueryData(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D12QueryHeap * pQueryHeap,
                                                      D3D12_QUERY_TYPE Type,
                                                      UINT StartIndex,
                                                      UINT NumQueries,
                                                      ID3D12Resource * pDestinationBuffer,
                                                      UINT64 AlignedDestinationBufferOffset)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ResolveQueryData(pQueryHeap,Type,StartIndex,NumQueries,pDestinationBuffer,AlignedDestinationBufferOffset);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ResolveQueryData),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pQueryHeap));
    npt_encode_D3D12_QUERY_TYPE(enc, &Type);
    npt_encode_UINT(enc, &StartIndex);
    npt_encode_UINT(enc, &NumQueries);
    npt_encode_com_handle(enc, npt_object_get_id(pDestinationBuffer));
    npt_encode_UINT64(enc, &AlignedDestinationBufferOffset);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ResolveQueryData_reply(const ID3D12QueryHeap * pQueryHeap,const D3D12_QUERY_TYPE Type,const UINT StartIndex,const UINT NumQueries,const ID3D12Resource * pDestinationBuffer,const UINT64 AlignedDestinationBufferOffset)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pQueryHeap (input only) */
/* skip Type (input only) */
/* skip StartIndex (input only) */
/* skip NumQueries (input only) */
/* skip pDestinationBuffer (input only) */
/* skip AlignedDestinationBufferOffset (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ResolveQueryData(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      ID3D12QueryHeap * pQueryHeap,
                                                      D3D12_QUERY_TYPE Type,
                                                      UINT StartIndex,
                                                      UINT NumQueries,
                                                      ID3D12Resource * pDestinationBuffer,
                                                      UINT64 AlignedDestinationBufferOffset,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ResolveQueryData(pQueryHeap,Type,StartIndex,NumQueries,pDestinationBuffer,AlignedDestinationBufferOffset);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ResolveQueryData_reply(pQueryHeap,Type,StartIndex,NumQueries,pDestinationBuffer,AlignedDestinationBufferOffset)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ResolveQueryData(enc, cmd_flags, object_id, pQueryHeap, Type, StartIndex, NumQueries, pDestinationBuffer, AlignedDestinationBufferOffset);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ResolveQueryData(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     ID3D12QueryHeap * pQueryHeap,
                                                     D3D12_QUERY_TYPE Type,
                                                     UINT StartIndex,
                                                     UINT NumQueries,
                                                     ID3D12Resource * pDestinationBuffer,
                                                     UINT64 AlignedDestinationBufferOffset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ResolveQueryData(ring, 0, object_id, pQueryHeap, Type, StartIndex, NumQueries, pDestinationBuffer, AlignedDestinationBufferOffset, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ResolveQueryData(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D12QueryHeap * pQueryHeap,
                                                    D3D12_QUERY_TYPE Type,
                                                    UINT StartIndex,
                                                    UINT NumQueries,
                                                    ID3D12Resource * pDestinationBuffer,
                                                    UINT64 AlignedDestinationBufferOffset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ResolveQueryData(ring, NPT_CMD_FLAG_REPLY, object_id, pQueryHeap, Type, StartIndex, NumQueries, pDestinationBuffer, AlignedDestinationBufferOffset, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetPredication
 */

struct npt_command_ID3D12GraphicsCommandList_SetPredication {
    void *_self;
    ID3D12Resource * pBuffer;
    UINT64 AlignedBufferOffset;
    D3D12_PREDICATION_OP Operation;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetPredication(const ID3D12Resource * pBuffer,const UINT64 AlignedBufferOffset,const D3D12_PREDICATION_OP Operation)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&AlignedBufferOffset, max_mode);
    cmd_size += npt_sizeof_D3D12_PREDICATION_OP(&Operation, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetPredication(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    ID3D12Resource * pBuffer,
                                                    UINT64 AlignedBufferOffset,
                                                    D3D12_PREDICATION_OP Operation)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetPredication(pBuffer,AlignedBufferOffset,Operation);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetPredication),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pBuffer));
    npt_encode_UINT64(enc, &AlignedBufferOffset);
    npt_encode_D3D12_PREDICATION_OP(enc, &Operation);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetPredication_reply(const ID3D12Resource * pBuffer,const UINT64 AlignedBufferOffset,const D3D12_PREDICATION_OP Operation)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pBuffer (input only) */
/* skip AlignedBufferOffset (input only) */
/* skip Operation (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetPredication(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    ID3D12Resource * pBuffer,
                                                    UINT64 AlignedBufferOffset,
                                                    D3D12_PREDICATION_OP Operation,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetPredication(pBuffer,AlignedBufferOffset,Operation);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetPredication_reply(pBuffer,AlignedBufferOffset,Operation)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetPredication(enc, cmd_flags, object_id, pBuffer, AlignedBufferOffset, Operation);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetPredication(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D12Resource * pBuffer,
                                                   UINT64 AlignedBufferOffset,
                                                   D3D12_PREDICATION_OP Operation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetPredication(ring, 0, object_id, pBuffer, AlignedBufferOffset, Operation, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetPredication(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  ID3D12Resource * pBuffer,
                                                  UINT64 AlignedBufferOffset,
                                                  D3D12_PREDICATION_OP Operation)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetPredication(ring, NPT_CMD_FLAG_REPLY, object_id, pBuffer, AlignedBufferOffset, Operation, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::SetMarker
 */

struct npt_command_ID3D12GraphicsCommandList_SetMarker {
    void *_self;
    UINT Metadata;
    const void * pData;
    UINT Size;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetMarker(const UINT Metadata,const void * pData,const UINT Size)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Metadata, max_mode);
    if (pData) {
        cmd_size += npt_sizeof_array_count(Size);
        cmd_size += npt_sizeof_blob_array(pData, Size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&Size, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetMarker(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               UINT Metadata,
                                               const void * pData,
                                               UINT Size)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_SetMarker(Metadata,pData,Size);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetMarker),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Metadata);
    if (pData) {
        npt_encode_array_count(enc, Size);
        npt_encode_blob_array(enc, pData, Size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &Size);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_SetMarker_reply(const UINT Metadata,const void * pData,const UINT Size)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Metadata (input only) */
/* skip pData (input only) */
/* skip Size (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_SetMarker(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               UINT Metadata,
                                               const void * pData,
                                               UINT Size,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_SetMarker(Metadata,pData,Size);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_SetMarker_reply(Metadata,pData,Size)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_SetMarker(enc, cmd_flags, object_id, Metadata, pData, Size);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_SetMarker(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT Metadata,
                                              const void * pData,
                                              UINT Size)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetMarker(ring, 0, object_id, Metadata, pData, Size, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_SetMarker(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT Metadata,
                                             const void * pData,
                                             UINT Size)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_SetMarker(ring, NPT_CMD_FLAG_REPLY, object_id, Metadata, pData, Size, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::BeginEvent
 */

struct npt_command_ID3D12GraphicsCommandList_BeginEvent {
    void *_self;
    UINT Metadata;
    const void * pData;
    UINT Size;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_BeginEvent(const UINT Metadata,const void * pData,const UINT Size)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Metadata, max_mode);
    if (pData) {
        cmd_size += npt_sizeof_array_count(Size);
        cmd_size += npt_sizeof_blob_array(pData, Size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&Size, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_BeginEvent(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT Metadata,
                                                const void * pData,
                                                UINT Size)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_BeginEvent(Metadata,pData,Size);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_BeginEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Metadata);
    if (pData) {
        npt_encode_array_count(enc, Size);
        npt_encode_blob_array(enc, pData, Size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &Size);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_BeginEvent_reply(const UINT Metadata,const void * pData,const UINT Size)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Metadata (input only) */
/* skip pData (input only) */
/* skip Size (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_BeginEvent(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                UINT Metadata,
                                                const void * pData,
                                                UINT Size,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_BeginEvent(Metadata,pData,Size);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_BeginEvent_reply(Metadata,pData,Size)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_BeginEvent(enc, cmd_flags, object_id, Metadata, pData, Size);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_BeginEvent(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               UINT Metadata,
                                               const void * pData,
                                               UINT Size)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_BeginEvent(ring, 0, object_id, Metadata, pData, Size, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_BeginEvent(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              UINT Metadata,
                                              const void * pData,
                                              UINT Size)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_BeginEvent(ring, NPT_CMD_FLAG_REPLY, object_id, Metadata, pData, Size, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::EndEvent
 */

struct npt_command_ID3D12GraphicsCommandList_EndEvent {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_EndEvent(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_EndEvent(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_EndEvent();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_EndEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_EndEvent_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_EndEvent(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_EndEvent();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_EndEvent_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_EndEvent(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_EndEvent(struct npt_ring *ring,
                                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_EndEvent(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_EndEvent(struct npt_ring *ring,
                                            npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_EndEvent(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList::ExecuteIndirect
 */

struct npt_command_ID3D12GraphicsCommandList_ExecuteIndirect {
    void *_self;
    ID3D12CommandSignature * pCommandSignature;
    UINT MaxCommandCount;
    ID3D12Resource * pArgumentBuffer;
    UINT64 ArgumentBufferOffset;
    ID3D12Resource * pCountBuffer;
    UINT64 CountBufferOffset;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ExecuteIndirect(const ID3D12CommandSignature * pCommandSignature,const UINT MaxCommandCount,const ID3D12Resource * pArgumentBuffer,const UINT64 ArgumentBufferOffset,const ID3D12Resource * pCountBuffer,const UINT64 CountBufferOffset)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&MaxCommandCount, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&ArgumentBufferOffset, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&CountBufferOffset, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ExecuteIndirect(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D12CommandSignature * pCommandSignature,
                                                     UINT MaxCommandCount,
                                                     ID3D12Resource * pArgumentBuffer,
                                                     UINT64 ArgumentBufferOffset,
                                                     ID3D12Resource * pCountBuffer,
                                                     UINT64 CountBufferOffset)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList_ExecuteIndirect(pCommandSignature,MaxCommandCount,pArgumentBuffer,ArgumentBufferOffset,pCountBuffer,CountBufferOffset);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ExecuteIndirect),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pCommandSignature));
    npt_encode_UINT(enc, &MaxCommandCount);
    npt_encode_com_handle(enc, npt_object_get_id(pArgumentBuffer));
    npt_encode_UINT64(enc, &ArgumentBufferOffset);
    npt_encode_com_handle(enc, npt_object_get_id(pCountBuffer));
    npt_encode_UINT64(enc, &CountBufferOffset);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList_ExecuteIndirect_reply(const ID3D12CommandSignature * pCommandSignature,const UINT MaxCommandCount,const ID3D12Resource * pArgumentBuffer,const UINT64 ArgumentBufferOffset,const ID3D12Resource * pCountBuffer,const UINT64 CountBufferOffset)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pCommandSignature (input only) */
/* skip MaxCommandCount (input only) */
/* skip pArgumentBuffer (input only) */
/* skip ArgumentBufferOffset (input only) */
/* skip pCountBuffer (input only) */
/* skip CountBufferOffset (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList_ExecuteIndirect(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     ID3D12CommandSignature * pCommandSignature,
                                                     UINT MaxCommandCount,
                                                     ID3D12Resource * pArgumentBuffer,
                                                     UINT64 ArgumentBufferOffset,
                                                     ID3D12Resource * pCountBuffer,
                                                     UINT64 CountBufferOffset,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList_ExecuteIndirect(pCommandSignature,MaxCommandCount,pArgumentBuffer,ArgumentBufferOffset,pCountBuffer,CountBufferOffset);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList_ExecuteIndirect_reply(pCommandSignature,MaxCommandCount,pArgumentBuffer,ArgumentBufferOffset,pCountBuffer,CountBufferOffset)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList_ExecuteIndirect(enc, cmd_flags, object_id, pCommandSignature, MaxCommandCount, pArgumentBuffer, ArgumentBufferOffset, pCountBuffer, CountBufferOffset);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList_ExecuteIndirect(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    ID3D12CommandSignature * pCommandSignature,
                                                    UINT MaxCommandCount,
                                                    ID3D12Resource * pArgumentBuffer,
                                                    UINT64 ArgumentBufferOffset,
                                                    ID3D12Resource * pCountBuffer,
                                                    UINT64 CountBufferOffset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ExecuteIndirect(ring, 0, object_id, pCommandSignature, MaxCommandCount, pArgumentBuffer, ArgumentBufferOffset, pCountBuffer, CountBufferOffset, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList_ExecuteIndirect(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   ID3D12CommandSignature * pCommandSignature,
                                                   UINT MaxCommandCount,
                                                   ID3D12Resource * pArgumentBuffer,
                                                   UINT64 ArgumentBufferOffset,
                                                   ID3D12Resource * pCountBuffer,
                                                   UINT64 CountBufferOffset)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList_ExecuteIndirect(ring, NPT_CMD_FLAG_REPLY, object_id, pCommandSignature, MaxCommandCount, pArgumentBuffer, ArgumentBufferOffset, pCountBuffer, CountBufferOffset, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList1::AtomicCopyBufferUINT
 */

struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT {
    void *_self;
    ID3D12Resource * pDstBuffer;
    UINT64 DstOffset;
    ID3D12Resource * pSrcBuffer;
    UINT64 SrcOffset;
    UINT Dependencies;
    ID3D12Resource ** ppDependentResources;
    const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(const ID3D12Resource * pDstBuffer,const UINT64 DstOffset,const ID3D12Resource * pSrcBuffer,const UINT64 SrcOffset,const UINT Dependencies,ID3D12Resource ** ppDependentResources,const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&DstOffset, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&SrcOffset, max_mode);
    cmd_size += npt_sizeof_UINT(&Dependencies, max_mode);
    cmd_size += npt_sizeof_array_count(Dependencies);
    cmd_size += sizeof(uint64_t) * Dependencies;
    cmd_size += npt_sizeof_array_count(pDependentSubresourceRanges ? Dependencies : 0);
    for (uint32_t _i = 0; _i < (pDependentSubresourceRanges ? Dependencies : 0); _i++)
        cmd_size += npt_sizeof_D3D12_SUBRESOURCE_RANGE_UINT64(&pDependentSubresourceRanges[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D12Resource * pDstBuffer,
                                                           UINT64 DstOffset,
                                                           ID3D12Resource * pSrcBuffer,
                                                           UINT64 SrcOffset,
                                                           UINT Dependencies,
                                                           ID3D12Resource ** ppDependentResources,
                                                           const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(pDstBuffer,DstOffset,pSrcBuffer,SrcOffset,Dependencies,ppDependentResources,pDependentSubresourceRanges);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstBuffer));
    npt_encode_UINT64(enc, &DstOffset);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcBuffer));
    npt_encode_UINT64(enc, &SrcOffset);
    npt_encode_UINT(enc, &Dependencies);
    if (ppDependentResources) {
        npt_encode_array_count(enc, Dependencies);
        for (uint32_t _i = 0; _i < (uint32_t)Dependencies; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppDependentResources[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pDependentSubresourceRanges) {
        npt_encode_array_count(enc, Dependencies);
        for (uint32_t _i = 0; _i < (uint32_t)(Dependencies); _i++)
            npt_encode_D3D12_SUBRESOURCE_RANGE_UINT64(enc, &pDependentSubresourceRanges[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT_reply(const ID3D12Resource * pDstBuffer,const UINT64 DstOffset,const ID3D12Resource * pSrcBuffer,const UINT64 SrcOffset,const UINT Dependencies,ID3D12Resource ** ppDependentResources,const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstBuffer (input only) */
/* skip DstOffset (input only) */
/* skip pSrcBuffer (input only) */
/* skip SrcOffset (input only) */
/* skip Dependencies (input only) */
/* skip ppDependentResources (input only) */
/* skip pDependentSubresourceRanges (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           ID3D12Resource * pDstBuffer,
                                                           UINT64 DstOffset,
                                                           ID3D12Resource * pSrcBuffer,
                                                           UINT64 SrcOffset,
                                                           UINT Dependencies,
                                                           ID3D12Resource ** ppDependentResources,
                                                           const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(pDstBuffer,DstOffset,pSrcBuffer,SrcOffset,Dependencies,ppDependentResources,pDependentSubresourceRanges);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT_reply(pDstBuffer,DstOffset,pSrcBuffer,SrcOffset,Dependencies,ppDependentResources,pDependentSubresourceRanges)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(enc, cmd_flags, object_id, pDstBuffer, DstOffset, pSrcBuffer, SrcOffset, Dependencies, ppDependentResources, pDependentSubresourceRanges);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D12Resource * pDstBuffer,
                                                          UINT64 DstOffset,
                                                          ID3D12Resource * pSrcBuffer,
                                                          UINT64 SrcOffset,
                                                          UINT Dependencies,
                                                          ID3D12Resource ** ppDependentResources,
                                                          const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(ring, 0, object_id, pDstBuffer, DstOffset, pSrcBuffer, SrcOffset, Dependencies, ppDependentResources, pDependentSubresourceRanges, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         ID3D12Resource * pDstBuffer,
                                                         UINT64 DstOffset,
                                                         ID3D12Resource * pSrcBuffer,
                                                         UINT64 SrcOffset,
                                                         UINT Dependencies,
                                                         ID3D12Resource ** ppDependentResources,
                                                         const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(ring, NPT_CMD_FLAG_REPLY, object_id, pDstBuffer, DstOffset, pSrcBuffer, SrcOffset, Dependencies, ppDependentResources, pDependentSubresourceRanges, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList1::AtomicCopyBufferUINT64
 */

struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64 {
    void *_self;
    ID3D12Resource * pDstBuffer;
    UINT64 DstOffset;
    ID3D12Resource * pSrcBuffer;
    UINT64 SrcOffset;
    UINT Dependencies;
    ID3D12Resource ** ppDependentResources;
    const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(const ID3D12Resource * pDstBuffer,const UINT64 DstOffset,const ID3D12Resource * pSrcBuffer,const UINT64 SrcOffset,const UINT Dependencies,ID3D12Resource ** ppDependentResources,const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&DstOffset, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT64(&SrcOffset, max_mode);
    cmd_size += npt_sizeof_UINT(&Dependencies, max_mode);
    cmd_size += npt_sizeof_array_count(Dependencies);
    cmd_size += sizeof(uint64_t) * Dependencies;
    cmd_size += npt_sizeof_array_count(pDependentSubresourceRanges ? Dependencies : 0);
    for (uint32_t _i = 0; _i < (pDependentSubresourceRanges ? Dependencies : 0); _i++)
        cmd_size += npt_sizeof_D3D12_SUBRESOURCE_RANGE_UINT64(&pDependentSubresourceRanges[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(struct npt_cs_encoder *enc,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D12Resource * pDstBuffer,
                                                             UINT64 DstOffset,
                                                             ID3D12Resource * pSrcBuffer,
                                                             UINT64 SrcOffset,
                                                             UINT Dependencies,
                                                             ID3D12Resource ** ppDependentResources,
                                                             const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(pDstBuffer,DstOffset,pSrcBuffer,SrcOffset,Dependencies,ppDependentResources,pDependentSubresourceRanges);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstBuffer));
    npt_encode_UINT64(enc, &DstOffset);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcBuffer));
    npt_encode_UINT64(enc, &SrcOffset);
    npt_encode_UINT(enc, &Dependencies);
    if (ppDependentResources) {
        npt_encode_array_count(enc, Dependencies);
        for (uint32_t _i = 0; _i < (uint32_t)Dependencies; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppDependentResources[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pDependentSubresourceRanges) {
        npt_encode_array_count(enc, Dependencies);
        for (uint32_t _i = 0; _i < (uint32_t)(Dependencies); _i++)
            npt_encode_D3D12_SUBRESOURCE_RANGE_UINT64(enc, &pDependentSubresourceRanges[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64_reply(const ID3D12Resource * pDstBuffer,const UINT64 DstOffset,const ID3D12Resource * pSrcBuffer,const UINT64 SrcOffset,const UINT Dependencies,ID3D12Resource ** ppDependentResources,const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstBuffer (input only) */
/* skip DstOffset (input only) */
/* skip pSrcBuffer (input only) */
/* skip SrcOffset (input only) */
/* skip Dependencies (input only) */
/* skip ppDependentResources (input only) */
/* skip pDependentSubresourceRanges (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(struct npt_ring *ring,
                                                             uint32_t cmd_flags,
                                                             npt_object_id object_id,
                                                             ID3D12Resource * pDstBuffer,
                                                             UINT64 DstOffset,
                                                             ID3D12Resource * pSrcBuffer,
                                                             UINT64 SrcOffset,
                                                             UINT Dependencies,
                                                             ID3D12Resource ** ppDependentResources,
                                                             const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges,
                                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(pDstBuffer,DstOffset,pSrcBuffer,SrcOffset,Dependencies,ppDependentResources,pDependentSubresourceRanges);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64_reply(pDstBuffer,DstOffset,pSrcBuffer,SrcOffset,Dependencies,ppDependentResources,pDependentSubresourceRanges)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(enc, cmd_flags, object_id, pDstBuffer, DstOffset, pSrcBuffer, SrcOffset, Dependencies, ppDependentResources, pDependentSubresourceRanges);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(struct npt_ring *ring,
                                                            npt_object_id object_id,
                                                            ID3D12Resource * pDstBuffer,
                                                            UINT64 DstOffset,
                                                            ID3D12Resource * pSrcBuffer,
                                                            UINT64 SrcOffset,
                                                            UINT Dependencies,
                                                            ID3D12Resource ** ppDependentResources,
                                                            const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(ring, 0, object_id, pDstBuffer, DstOffset, pSrcBuffer, SrcOffset, Dependencies, ppDependentResources, pDependentSubresourceRanges, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D12Resource * pDstBuffer,
                                                           UINT64 DstOffset,
                                                           ID3D12Resource * pSrcBuffer,
                                                           UINT64 SrcOffset,
                                                           UINT Dependencies,
                                                           ID3D12Resource ** ppDependentResources,
                                                           const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(ring, NPT_CMD_FLAG_REPLY, object_id, pDstBuffer, DstOffset, pSrcBuffer, SrcOffset, Dependencies, ppDependentResources, pDependentSubresourceRanges, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList1::OMSetDepthBounds
 */

struct npt_command_ID3D12GraphicsCommandList1_OMSetDepthBounds {
    void *_self;
    FLOAT Min;
    FLOAT Max;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_OMSetDepthBounds(const FLOAT Min,const FLOAT Max)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_FLOAT(&Min, max_mode);
    cmd_size += npt_sizeof_FLOAT(&Max, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_OMSetDepthBounds(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       FLOAT Min,
                                                       FLOAT Max)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList1_OMSetDepthBounds(Min,Max);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_OMSetDepthBounds),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_FLOAT(enc, &Min);
    npt_encode_FLOAT(enc, &Max);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_OMSetDepthBounds_reply(const FLOAT Min,const FLOAT Max)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Min (input only) */
/* skip Max (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList1_OMSetDepthBounds(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       FLOAT Min,
                                                       FLOAT Max,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList1_OMSetDepthBounds(Min,Max);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList1_OMSetDepthBounds_reply(Min,Max)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList1_OMSetDepthBounds(enc, cmd_flags, object_id, Min, Max);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList1_OMSetDepthBounds(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      FLOAT Min,
                                                      FLOAT Max)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_OMSetDepthBounds(ring, 0, object_id, Min, Max, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList1_OMSetDepthBounds(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     FLOAT Min,
                                                     FLOAT Max)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_OMSetDepthBounds(ring, NPT_CMD_FLAG_REPLY, object_id, Min, Max, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList1::SetSamplePositions
 */

struct npt_command_ID3D12GraphicsCommandList1_SetSamplePositions {
    void *_self;
    UINT NumSamplesPerPixel;
    UINT NumPixels;
    D3D12_SAMPLE_POSITION * pSamplePositions;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_SetSamplePositions(const UINT NumSamplesPerPixel,const UINT NumPixels,const D3D12_SAMPLE_POSITION * pSamplePositions)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumSamplesPerPixel, max_mode);
    cmd_size += npt_sizeof_UINT(&NumPixels, max_mode);
    cmd_size += npt_sizeof_array_count(pSamplePositions ? NumSamplesPerPixel * NumPixels : 0);
    for (uint32_t _i = 0; _i < (pSamplePositions ? NumSamplesPerPixel * NumPixels : 0); _i++)
        cmd_size += npt_sizeof_D3D12_SAMPLE_POSITION(&pSamplePositions[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_SetSamplePositions(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         UINT NumSamplesPerPixel,
                                                         UINT NumPixels,
                                                         D3D12_SAMPLE_POSITION * pSamplePositions)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList1_SetSamplePositions(NumSamplesPerPixel,NumPixels,pSamplePositions);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_SetSamplePositions),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumSamplesPerPixel);
    npt_encode_UINT(enc, &NumPixels);
    if (pSamplePositions) {
        npt_encode_array_count(enc, NumSamplesPerPixel * NumPixels);
        for (uint32_t _i = 0; _i < (uint32_t)(NumSamplesPerPixel * NumPixels); _i++)
            npt_encode_D3D12_SAMPLE_POSITION(enc, &pSamplePositions[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_SetSamplePositions_reply(const UINT NumSamplesPerPixel,const UINT NumPixels,const D3D12_SAMPLE_POSITION * pSamplePositions)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumSamplesPerPixel (input only) */
/* skip NumPixels (input only) */
/* skip pSamplePositions (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList1_SetSamplePositions(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         UINT NumSamplesPerPixel,
                                                         UINT NumPixels,
                                                         D3D12_SAMPLE_POSITION * pSamplePositions,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList1_SetSamplePositions(NumSamplesPerPixel,NumPixels,pSamplePositions);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList1_SetSamplePositions_reply(NumSamplesPerPixel,NumPixels,pSamplePositions)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList1_SetSamplePositions(enc, cmd_flags, object_id, NumSamplesPerPixel, NumPixels, pSamplePositions);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList1_SetSamplePositions(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        UINT NumSamplesPerPixel,
                                                        UINT NumPixels,
                                                        D3D12_SAMPLE_POSITION * pSamplePositions)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_SetSamplePositions(ring, 0, object_id, NumSamplesPerPixel, NumPixels, pSamplePositions, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList1_SetSamplePositions(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       UINT NumSamplesPerPixel,
                                                       UINT NumPixels,
                                                       D3D12_SAMPLE_POSITION * pSamplePositions)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_SetSamplePositions(ring, NPT_CMD_FLAG_REPLY, object_id, NumSamplesPerPixel, NumPixels, pSamplePositions, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList1::ResolveSubresourceRegion
 */

struct npt_command_ID3D12GraphicsCommandList1_ResolveSubresourceRegion {
    void *_self;
    ID3D12Resource * pDstResource;
    UINT DstSubresource;
    UINT DstX;
    UINT DstY;
    ID3D12Resource * pSrcResource;
    UINT SrcSubresource;
    D3D12_RECT * pSrcRect;
    DXGI_FORMAT Format;
    D3D12_RESOLVE_MODE ResolveMode;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(const ID3D12Resource * pDstResource,const UINT DstSubresource,const UINT DstX,const UINT DstY,const ID3D12Resource * pSrcResource,const UINT SrcSubresource,const D3D12_RECT * pSrcRect,const DXGI_FORMAT Format,const D3D12_RESOLVE_MODE ResolveMode)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&DstSubresource, max_mode);
    cmd_size += npt_sizeof_UINT(&DstX, max_mode);
    cmd_size += npt_sizeof_UINT(&DstY, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&SrcSubresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSrcRect);
    if (pSrcRect)
        cmd_size += npt_sizeof_D3D12_RECT(pSrcRect, max_mode);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
    cmd_size += npt_sizeof_D3D12_RESOLVE_MODE(&ResolveMode, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(struct npt_cs_encoder *enc,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D12Resource * pDstResource,
                                                               UINT DstSubresource,
                                                               UINT DstX,
                                                               UINT DstY,
                                                               ID3D12Resource * pSrcResource,
                                                               UINT SrcSubresource,
                                                               D3D12_RECT * pSrcRect,
                                                               DXGI_FORMAT Format,
                                                               D3D12_RESOLVE_MODE ResolveMode)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(pDstResource,DstSubresource,DstX,DstY,pSrcResource,SrcSubresource,pSrcRect,Format,ResolveMode);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_ResolveSubresourceRegion),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDstResource));
    npt_encode_UINT(enc, &DstSubresource);
    npt_encode_UINT(enc, &DstX);
    npt_encode_UINT(enc, &DstY);
    npt_encode_com_handle(enc, npt_object_get_id(pSrcResource));
    npt_encode_UINT(enc, &SrcSubresource);
    if (npt_encode_simple_pointer(enc, pSrcRect))
        npt_encode_D3D12_RECT(enc, pSrcRect);
    npt_encode_DXGI_FORMAT(enc, &Format);
    npt_encode_D3D12_RESOLVE_MODE(enc, &ResolveMode);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_ResolveSubresourceRegion_reply(const ID3D12Resource * pDstResource,const UINT DstSubresource,const UINT DstX,const UINT DstY,const ID3D12Resource * pSrcResource,const UINT SrcSubresource,const D3D12_RECT * pSrcRect,const DXGI_FORMAT Format,const D3D12_RESOLVE_MODE ResolveMode)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDstResource (input only) */
/* skip DstSubresource (input only) */
/* skip DstX (input only) */
/* skip DstY (input only) */
/* skip pSrcResource (input only) */
/* skip SrcSubresource (input only) */
/* skip pSrcRect (input only) */
/* skip Format (input only) */
/* skip ResolveMode (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(struct npt_ring *ring,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               ID3D12Resource * pDstResource,
                                                               UINT DstSubresource,
                                                               UINT DstX,
                                                               UINT DstY,
                                                               ID3D12Resource * pSrcResource,
                                                               UINT SrcSubresource,
                                                               D3D12_RECT * pSrcRect,
                                                               DXGI_FORMAT Format,
                                                               D3D12_RESOLVE_MODE ResolveMode,
                                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(pDstResource,DstSubresource,DstX,DstY,pSrcResource,SrcSubresource,pSrcRect,Format,ResolveMode);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList1_ResolveSubresourceRegion_reply(pDstResource,DstSubresource,DstX,DstY,pSrcResource,SrcSubresource,pSrcRect,Format,ResolveMode)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(enc, cmd_flags, object_id, pDstResource, DstSubresource, DstX, DstY, pSrcResource, SrcSubresource, pSrcRect, Format, ResolveMode);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              ID3D12Resource * pDstResource,
                                                              UINT DstSubresource,
                                                              UINT DstX,
                                                              UINT DstY,
                                                              ID3D12Resource * pSrcResource,
                                                              UINT SrcSubresource,
                                                              D3D12_RECT * pSrcRect,
                                                              DXGI_FORMAT Format,
                                                              D3D12_RESOLVE_MODE ResolveMode)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(ring, 0, object_id, pDstResource, DstSubresource, DstX, DstY, pSrcResource, SrcSubresource, pSrcRect, Format, ResolveMode, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             ID3D12Resource * pDstResource,
                                                             UINT DstSubresource,
                                                             UINT DstX,
                                                             UINT DstY,
                                                             ID3D12Resource * pSrcResource,
                                                             UINT SrcSubresource,
                                                             D3D12_RECT * pSrcRect,
                                                             DXGI_FORMAT Format,
                                                             D3D12_RESOLVE_MODE ResolveMode)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(ring, NPT_CMD_FLAG_REPLY, object_id, pDstResource, DstSubresource, DstX, DstY, pSrcResource, SrcSubresource, pSrcRect, Format, ResolveMode, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList1::SetViewInstanceMask
 */

struct npt_command_ID3D12GraphicsCommandList1_SetViewInstanceMask {
    void *_self;
    UINT Mask;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_SetViewInstanceMask(const UINT Mask)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Mask, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_SetViewInstanceMask(struct npt_cs_encoder *enc,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          UINT Mask)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList1_SetViewInstanceMask(Mask);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_SetViewInstanceMask),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Mask);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList1_SetViewInstanceMask_reply(const UINT Mask)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Mask (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList1_SetViewInstanceMask(struct npt_ring *ring,
                                                          uint32_t cmd_flags,
                                                          npt_object_id object_id,
                                                          UINT Mask,
                                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList1_SetViewInstanceMask(Mask);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList1_SetViewInstanceMask_reply(Mask)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList1_SetViewInstanceMask(enc, cmd_flags, object_id, Mask);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList1_SetViewInstanceMask(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         UINT Mask)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_SetViewInstanceMask(ring, 0, object_id, Mask, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList1_SetViewInstanceMask(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        UINT Mask)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList1_SetViewInstanceMask(ring, NPT_CMD_FLAG_REPLY, object_id, Mask, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList2::WriteBufferImmediate
 */

struct npt_command_ID3D12GraphicsCommandList2_WriteBufferImmediate {
    void *_self;
    UINT Count;
    const D3D12_WRITEBUFFERIMMEDIATE_PARAMETER * pParams;
    const D3D12_WRITEBUFFERIMMEDIATE_MODE * pModes;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList2_WriteBufferImmediate(const UINT Count,const D3D12_WRITEBUFFERIMMEDIATE_PARAMETER * pParams,const D3D12_WRITEBUFFERIMMEDIATE_MODE * pModes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Count, max_mode);
    cmd_size += npt_sizeof_array_count(pParams ? Count : 0);
    for (uint32_t _i = 0; _i < (pParams ? Count : 0); _i++)
        cmd_size += npt_sizeof_D3D12_WRITEBUFFERIMMEDIATE_PARAMETER(&pParams[_i], max_mode);
    cmd_size += npt_sizeof_array_count(pModes ? Count : 0);
    if (pModes)
        cmd_size += npt_sizeof_D3D12_WRITEBUFFERIMMEDIATE_MODE_array(pModes, Count);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList2_WriteBufferImmediate(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           UINT Count,
                                                           const D3D12_WRITEBUFFERIMMEDIATE_PARAMETER * pParams,
                                                           const D3D12_WRITEBUFFERIMMEDIATE_MODE * pModes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList2_WriteBufferImmediate(Count,pParams,pModes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList2,
                                 NPT_METHOD_ID3D12GraphicsCommandList2_WriteBufferImmediate),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Count);
    if (pParams) {
        npt_encode_array_count(enc, Count);
        for (uint32_t _i = 0; _i < (uint32_t)(Count); _i++)
            npt_encode_D3D12_WRITEBUFFERIMMEDIATE_PARAMETER(enc, &pParams[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (pModes) {
        npt_encode_array_count(enc, Count);
        npt_encode_D3D12_WRITEBUFFERIMMEDIATE_MODE_array(enc, pModes, Count);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList2_WriteBufferImmediate_reply(const UINT Count,const D3D12_WRITEBUFFERIMMEDIATE_PARAMETER * pParams,const D3D12_WRITEBUFFERIMMEDIATE_MODE * pModes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Count (input only) */
/* skip pParams (input only) */
/* skip pModes (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList2_WriteBufferImmediate(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           UINT Count,
                                                           const D3D12_WRITEBUFFERIMMEDIATE_PARAMETER * pParams,
                                                           const D3D12_WRITEBUFFERIMMEDIATE_MODE * pModes,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList2_WriteBufferImmediate(Count,pParams,pModes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList2_WriteBufferImmediate_reply(Count,pParams,pModes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList2_WriteBufferImmediate(enc, cmd_flags, object_id, Count, pParams, pModes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList2_WriteBufferImmediate(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          UINT Count,
                                                          const D3D12_WRITEBUFFERIMMEDIATE_PARAMETER * pParams,
                                                          const D3D12_WRITEBUFFERIMMEDIATE_MODE * pModes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList2_WriteBufferImmediate(ring, 0, object_id, Count, pParams, pModes, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList2_WriteBufferImmediate(struct npt_ring *ring,
                                                         npt_object_id object_id,
                                                         UINT Count,
                                                         const D3D12_WRITEBUFFERIMMEDIATE_PARAMETER * pParams,
                                                         const D3D12_WRITEBUFFERIMMEDIATE_MODE * pModes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList2_WriteBufferImmediate(ring, NPT_CMD_FLAG_REPLY, object_id, Count, pParams, pModes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList3::SetProtectedResourceSession
 */

struct npt_command_ID3D12GraphicsCommandList3_SetProtectedResourceSession {
    void *_self;
    ID3D12ProtectedResourceSession * pProtectedResourceSession;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList3_SetProtectedResourceSession(const ID3D12ProtectedResourceSession * pProtectedResourceSession)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList3_SetProtectedResourceSession(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D12ProtectedResourceSession * pProtectedResourceSession)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList3_SetProtectedResourceSession(pProtectedResourceSession);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList3,
                                 NPT_METHOD_ID3D12GraphicsCommandList3_SetProtectedResourceSession),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pProtectedResourceSession));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList3_SetProtectedResourceSession_reply(const ID3D12ProtectedResourceSession * pProtectedResourceSession)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pProtectedResourceSession (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList3_SetProtectedResourceSession(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  ID3D12ProtectedResourceSession * pProtectedResourceSession,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList3_SetProtectedResourceSession(pProtectedResourceSession);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList3_SetProtectedResourceSession_reply(pProtectedResourceSession)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList3_SetProtectedResourceSession(enc, cmd_flags, object_id, pProtectedResourceSession);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList3_SetProtectedResourceSession(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 ID3D12ProtectedResourceSession * pProtectedResourceSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList3_SetProtectedResourceSession(ring, 0, object_id, pProtectedResourceSession, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList3_SetProtectedResourceSession(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                ID3D12ProtectedResourceSession * pProtectedResourceSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList3_SetProtectedResourceSession(ring, NPT_CMD_FLAG_REPLY, object_id, pProtectedResourceSession, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList4::BeginRenderPass
 */

struct npt_command_ID3D12GraphicsCommandList4_BeginRenderPass {
    void *_self;
    UINT NumRenderTargets;
    const D3D12_RENDER_PASS_RENDER_TARGET_DESC * pRenderTargets;
    const D3D12_RENDER_PASS_DEPTH_STENCIL_DESC * pDepthStencil;
    D3D12_RENDER_PASS_FLAGS Flags;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_BeginRenderPass(const UINT NumRenderTargets,const D3D12_RENDER_PASS_RENDER_TARGET_DESC * pRenderTargets,const D3D12_RENDER_PASS_DEPTH_STENCIL_DESC * pDepthStencil,const D3D12_RENDER_PASS_FLAGS Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&NumRenderTargets, max_mode);
    cmd_size += npt_sizeof_array_count(pRenderTargets ? NumRenderTargets : 0);
    for (uint32_t _i = 0; _i < (pRenderTargets ? NumRenderTargets : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RENDER_PASS_RENDER_TARGET_DESC(&pRenderTargets[_i], max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDepthStencil);
    if (pDepthStencil)
        cmd_size += npt_sizeof_D3D12_RENDER_PASS_DEPTH_STENCIL_DESC(pDepthStencil, max_mode);
    cmd_size += npt_sizeof_D3D12_RENDER_PASS_FLAGS(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_BeginRenderPass(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT NumRenderTargets,
                                                      const D3D12_RENDER_PASS_RENDER_TARGET_DESC * pRenderTargets,
                                                      const D3D12_RENDER_PASS_DEPTH_STENCIL_DESC * pDepthStencil,
                                                      D3D12_RENDER_PASS_FLAGS Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList4_BeginRenderPass(NumRenderTargets,pRenderTargets,pDepthStencil,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_BeginRenderPass),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &NumRenderTargets);
    if (pRenderTargets) {
        npt_encode_array_count(enc, NumRenderTargets);
        for (uint32_t _i = 0; _i < (uint32_t)(NumRenderTargets); _i++)
            npt_encode_D3D12_RENDER_PASS_RENDER_TARGET_DESC(enc, &pRenderTargets[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, pDepthStencil))
        npt_encode_D3D12_RENDER_PASS_DEPTH_STENCIL_DESC(enc, pDepthStencil);
    npt_encode_D3D12_RENDER_PASS_FLAGS(enc, &Flags);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_BeginRenderPass_reply(const UINT NumRenderTargets,const D3D12_RENDER_PASS_RENDER_TARGET_DESC * pRenderTargets,const D3D12_RENDER_PASS_DEPTH_STENCIL_DESC * pDepthStencil,const D3D12_RENDER_PASS_FLAGS Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumRenderTargets (input only) */
/* skip pRenderTargets (input only) */
/* skip pDepthStencil (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList4_BeginRenderPass(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      UINT NumRenderTargets,
                                                      const D3D12_RENDER_PASS_RENDER_TARGET_DESC * pRenderTargets,
                                                      const D3D12_RENDER_PASS_DEPTH_STENCIL_DESC * pDepthStencil,
                                                      D3D12_RENDER_PASS_FLAGS Flags,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList4_BeginRenderPass(NumRenderTargets,pRenderTargets,pDepthStencil,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList4_BeginRenderPass_reply(NumRenderTargets,pRenderTargets,pDepthStencil,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList4_BeginRenderPass(enc, cmd_flags, object_id, NumRenderTargets, pRenderTargets, pDepthStencil, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList4_BeginRenderPass(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     UINT NumRenderTargets,
                                                     const D3D12_RENDER_PASS_RENDER_TARGET_DESC * pRenderTargets,
                                                     const D3D12_RENDER_PASS_DEPTH_STENCIL_DESC * pDepthStencil,
                                                     D3D12_RENDER_PASS_FLAGS Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_BeginRenderPass(ring, 0, object_id, NumRenderTargets, pRenderTargets, pDepthStencil, Flags, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList4_BeginRenderPass(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    UINT NumRenderTargets,
                                                    const D3D12_RENDER_PASS_RENDER_TARGET_DESC * pRenderTargets,
                                                    const D3D12_RENDER_PASS_DEPTH_STENCIL_DESC * pDepthStencil,
                                                    D3D12_RENDER_PASS_FLAGS Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_BeginRenderPass(ring, NPT_CMD_FLAG_REPLY, object_id, NumRenderTargets, pRenderTargets, pDepthStencil, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList4::EndRenderPass
 */

struct npt_command_ID3D12GraphicsCommandList4_EndRenderPass {
    void *_self;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_EndRenderPass(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_EndRenderPass(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList4_EndRenderPass();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_EndRenderPass),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_EndRenderPass_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList4_EndRenderPass(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList4_EndRenderPass();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList4_EndRenderPass_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList4_EndRenderPass(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList4_EndRenderPass(struct npt_ring *ring,
                                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_EndRenderPass(ring, 0, object_id, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList4_EndRenderPass(struct npt_ring *ring,
                                                  npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_EndRenderPass(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList4::InitializeMetaCommand
 */

struct npt_command_ID3D12GraphicsCommandList4_InitializeMetaCommand {
    void *_self;
    ID3D12MetaCommand * pMetaCommand;
    const void * pInitializationParametersData;
    SIZE_T InitializationParametersDataSizeInBytes;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_InitializeMetaCommand(const ID3D12MetaCommand * pMetaCommand,const void * pInitializationParametersData,const SIZE_T InitializationParametersDataSizeInBytes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    if (pInitializationParametersData) {
        cmd_size += npt_sizeof_array_count(InitializationParametersDataSizeInBytes);
        cmd_size += npt_sizeof_blob_array(pInitializationParametersData, InitializationParametersDataSizeInBytes);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&InitializationParametersDataSizeInBytes, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_InitializeMetaCommand(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D12MetaCommand * pMetaCommand,
                                                            const void * pInitializationParametersData,
                                                            SIZE_T InitializationParametersDataSizeInBytes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList4_InitializeMetaCommand(pMetaCommand,pInitializationParametersData,InitializationParametersDataSizeInBytes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_InitializeMetaCommand),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pMetaCommand));
    if (pInitializationParametersData) {
        npt_encode_array_count(enc, InitializationParametersDataSizeInBytes);
        npt_encode_blob_array(enc, pInitializationParametersData, InitializationParametersDataSizeInBytes);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &InitializationParametersDataSizeInBytes);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_InitializeMetaCommand_reply(const ID3D12MetaCommand * pMetaCommand,const void * pInitializationParametersData,const SIZE_T InitializationParametersDataSizeInBytes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pMetaCommand (input only) */
/* skip pInitializationParametersData (input only) */
/* skip InitializationParametersDataSizeInBytes (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList4_InitializeMetaCommand(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D12MetaCommand * pMetaCommand,
                                                            const void * pInitializationParametersData,
                                                            SIZE_T InitializationParametersDataSizeInBytes,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList4_InitializeMetaCommand(pMetaCommand,pInitializationParametersData,InitializationParametersDataSizeInBytes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList4_InitializeMetaCommand_reply(pMetaCommand,pInitializationParametersData,InitializationParametersDataSizeInBytes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList4_InitializeMetaCommand(enc, cmd_flags, object_id, pMetaCommand, pInitializationParametersData, InitializationParametersDataSizeInBytes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList4_InitializeMetaCommand(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D12MetaCommand * pMetaCommand,
                                                           const void * pInitializationParametersData,
                                                           SIZE_T InitializationParametersDataSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_InitializeMetaCommand(ring, 0, object_id, pMetaCommand, pInitializationParametersData, InitializationParametersDataSizeInBytes, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList4_InitializeMetaCommand(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D12MetaCommand * pMetaCommand,
                                                          const void * pInitializationParametersData,
                                                          SIZE_T InitializationParametersDataSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_InitializeMetaCommand(ring, NPT_CMD_FLAG_REPLY, object_id, pMetaCommand, pInitializationParametersData, InitializationParametersDataSizeInBytes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList4::ExecuteMetaCommand
 */

struct npt_command_ID3D12GraphicsCommandList4_ExecuteMetaCommand {
    void *_self;
    ID3D12MetaCommand * pMetaCommand;
    const void * pExecutionParametersData;
    SIZE_T ExecutionParametersDataSizeInBytes;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_ExecuteMetaCommand(const ID3D12MetaCommand * pMetaCommand,const void * pExecutionParametersData,const SIZE_T ExecutionParametersDataSizeInBytes)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    if (pExecutionParametersData) {
        cmd_size += npt_sizeof_array_count(ExecutionParametersDataSizeInBytes);
        cmd_size += npt_sizeof_blob_array(pExecutionParametersData, ExecutionParametersDataSizeInBytes);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&ExecutionParametersDataSizeInBytes, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_ExecuteMetaCommand(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D12MetaCommand * pMetaCommand,
                                                         const void * pExecutionParametersData,
                                                         SIZE_T ExecutionParametersDataSizeInBytes)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList4_ExecuteMetaCommand(pMetaCommand,pExecutionParametersData,ExecutionParametersDataSizeInBytes);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_ExecuteMetaCommand),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pMetaCommand));
    if (pExecutionParametersData) {
        npt_encode_array_count(enc, ExecutionParametersDataSizeInBytes);
        npt_encode_blob_array(enc, pExecutionParametersData, ExecutionParametersDataSizeInBytes);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &ExecutionParametersDataSizeInBytes);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_ExecuteMetaCommand_reply(const ID3D12MetaCommand * pMetaCommand,const void * pExecutionParametersData,const SIZE_T ExecutionParametersDataSizeInBytes)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pMetaCommand (input only) */
/* skip pExecutionParametersData (input only) */
/* skip ExecutionParametersDataSizeInBytes (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList4_ExecuteMetaCommand(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         npt_object_id object_id,
                                                         ID3D12MetaCommand * pMetaCommand,
                                                         const void * pExecutionParametersData,
                                                         SIZE_T ExecutionParametersDataSizeInBytes,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList4_ExecuteMetaCommand(pMetaCommand,pExecutionParametersData,ExecutionParametersDataSizeInBytes);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList4_ExecuteMetaCommand_reply(pMetaCommand,pExecutionParametersData,ExecutionParametersDataSizeInBytes)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList4_ExecuteMetaCommand(enc, cmd_flags, object_id, pMetaCommand, pExecutionParametersData, ExecutionParametersDataSizeInBytes);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList4_ExecuteMetaCommand(struct npt_ring *ring,
                                                        npt_object_id object_id,
                                                        ID3D12MetaCommand * pMetaCommand,
                                                        const void * pExecutionParametersData,
                                                        SIZE_T ExecutionParametersDataSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_ExecuteMetaCommand(ring, 0, object_id, pMetaCommand, pExecutionParametersData, ExecutionParametersDataSizeInBytes, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList4_ExecuteMetaCommand(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D12MetaCommand * pMetaCommand,
                                                       const void * pExecutionParametersData,
                                                       SIZE_T ExecutionParametersDataSizeInBytes)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_ExecuteMetaCommand(ring, NPT_CMD_FLAG_REPLY, object_id, pMetaCommand, pExecutionParametersData, ExecutionParametersDataSizeInBytes, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList4::BuildRaytracingAccelerationStructure
 */

struct npt_command_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure {
    void *_self;
    const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC * pDesc;
    UINT NumPostbuildInfoDescs;
    const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pPostbuildInfoDescs;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC * pDesc,const UINT NumPostbuildInfoDescs,const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pPostbuildInfoDescs)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_UINT(&NumPostbuildInfoDescs, max_mode);
    cmd_size += npt_sizeof_array_count(pPostbuildInfoDescs ? NumPostbuildInfoDescs : 0);
    for (uint32_t _i = 0; _i < (pPostbuildInfoDescs ? NumPostbuildInfoDescs : 0); _i++)
        cmd_size += npt_sizeof_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC(&pPostbuildInfoDescs[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(struct npt_cs_encoder *enc,
                                                                           uint32_t cmd_flags,
                                                                           npt_object_id object_id,
                                                                           const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC * pDesc,
                                                                           UINT NumPostbuildInfoDescs,
                                                                           const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pPostbuildInfoDescs)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(pDesc,NumPostbuildInfoDescs,pPostbuildInfoDescs);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC(enc, pDesc);
    npt_encode_UINT(enc, &NumPostbuildInfoDescs);
    if (pPostbuildInfoDescs) {
        npt_encode_array_count(enc, NumPostbuildInfoDescs);
        for (uint32_t _i = 0; _i < (uint32_t)(NumPostbuildInfoDescs); _i++)
            npt_encode_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC(enc, &pPostbuildInfoDescs[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure_reply(const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC * pDesc,const UINT NumPostbuildInfoDescs,const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pPostbuildInfoDescs)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip NumPostbuildInfoDescs (input only) */
/* skip pPostbuildInfoDescs (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(struct npt_ring *ring,
                                                                           uint32_t cmd_flags,
                                                                           npt_object_id object_id,
                                                                           const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC * pDesc,
                                                                           UINT NumPostbuildInfoDescs,
                                                                           const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pPostbuildInfoDescs,
                                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(pDesc,NumPostbuildInfoDescs,pPostbuildInfoDescs);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure_reply(pDesc,NumPostbuildInfoDescs,pPostbuildInfoDescs)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(enc, cmd_flags, object_id, pDesc, NumPostbuildInfoDescs, pPostbuildInfoDescs);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(struct npt_ring *ring,
                                                                          npt_object_id object_id,
                                                                          const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC * pDesc,
                                                                          UINT NumPostbuildInfoDescs,
                                                                          const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pPostbuildInfoDescs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(ring, 0, object_id, pDesc, NumPostbuildInfoDescs, pPostbuildInfoDescs, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(struct npt_ring *ring,
                                                                         npt_object_id object_id,
                                                                         const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC * pDesc,
                                                                         UINT NumPostbuildInfoDescs,
                                                                         const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pPostbuildInfoDescs)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, NumPostbuildInfoDescs, pPostbuildInfoDescs, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList4::EmitRaytracingAccelerationStructurePostbuildInfo
 */

struct npt_command_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo {
    void *_self;
    const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pDesc;
    UINT NumSourceAccelerationStructures;
    const D3D12_GPU_VIRTUAL_ADDRESS * pSourceAccelerationStructureData;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pDesc,const UINT NumSourceAccelerationStructures,const D3D12_GPU_VIRTUAL_ADDRESS * pSourceAccelerationStructureData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC(pDesc, max_mode);
    cmd_size += npt_sizeof_UINT(&NumSourceAccelerationStructures, max_mode);
    cmd_size += npt_sizeof_array_count(pSourceAccelerationStructureData ? NumSourceAccelerationStructures : 0);
    if (pSourceAccelerationStructureData)
        cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS_array(pSourceAccelerationStructureData, NumSourceAccelerationStructures);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(struct npt_cs_encoder *enc,
                                                                                       uint32_t cmd_flags,
                                                                                       npt_object_id object_id,
                                                                                       const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pDesc,
                                                                                       UINT NumSourceAccelerationStructures,
                                                                                       const D3D12_GPU_VIRTUAL_ADDRESS * pSourceAccelerationStructureData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(pDesc,NumSourceAccelerationStructures,pSourceAccelerationStructureData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC(enc, pDesc);
    npt_encode_UINT(enc, &NumSourceAccelerationStructures);
    if (pSourceAccelerationStructureData) {
        npt_encode_array_count(enc, NumSourceAccelerationStructures);
        npt_encode_D3D12_GPU_VIRTUAL_ADDRESS_array(enc, pSourceAccelerationStructureData, NumSourceAccelerationStructures);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo_reply(const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pDesc,const UINT NumSourceAccelerationStructures,const D3D12_GPU_VIRTUAL_ADDRESS * pSourceAccelerationStructureData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip NumSourceAccelerationStructures (input only) */
/* skip pSourceAccelerationStructureData (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(struct npt_ring *ring,
                                                                                       uint32_t cmd_flags,
                                                                                       npt_object_id object_id,
                                                                                       const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pDesc,
                                                                                       UINT NumSourceAccelerationStructures,
                                                                                       const D3D12_GPU_VIRTUAL_ADDRESS * pSourceAccelerationStructureData,
                                                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(pDesc,NumSourceAccelerationStructures,pSourceAccelerationStructureData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo_reply(pDesc,NumSourceAccelerationStructures,pSourceAccelerationStructureData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(enc, cmd_flags, object_id, pDesc, NumSourceAccelerationStructures, pSourceAccelerationStructureData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(struct npt_ring *ring,
                                                                                      npt_object_id object_id,
                                                                                      const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pDesc,
                                                                                      UINT NumSourceAccelerationStructures,
                                                                                      const D3D12_GPU_VIRTUAL_ADDRESS * pSourceAccelerationStructureData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(ring, 0, object_id, pDesc, NumSourceAccelerationStructures, pSourceAccelerationStructureData, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(struct npt_ring *ring,
                                                                                     npt_object_id object_id,
                                                                                     const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pDesc,
                                                                                     UINT NumSourceAccelerationStructures,
                                                                                     const D3D12_GPU_VIRTUAL_ADDRESS * pSourceAccelerationStructureData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, NumSourceAccelerationStructures, pSourceAccelerationStructureData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList4::CopyRaytracingAccelerationStructure
 */

struct npt_command_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure {
    void *_self;
    D3D12_GPU_VIRTUAL_ADDRESS DestAccelerationStructureData;
    D3D12_GPU_VIRTUAL_ADDRESS SourceAccelerationStructureData;
    D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE Mode;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(const D3D12_GPU_VIRTUAL_ADDRESS DestAccelerationStructureData,const D3D12_GPU_VIRTUAL_ADDRESS SourceAccelerationStructureData,const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE Mode)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS(&DestAccelerationStructureData, max_mode);
    cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS(&SourceAccelerationStructureData, max_mode);
    cmd_size += npt_sizeof_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE(&Mode, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(struct npt_cs_encoder *enc,
                                                                          uint32_t cmd_flags,
                                                                          npt_object_id object_id,
                                                                          D3D12_GPU_VIRTUAL_ADDRESS DestAccelerationStructureData,
                                                                          D3D12_GPU_VIRTUAL_ADDRESS SourceAccelerationStructureData,
                                                                          D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE Mode)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(DestAccelerationStructureData,SourceAccelerationStructureData,Mode);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_GPU_VIRTUAL_ADDRESS(enc, &DestAccelerationStructureData);
    npt_encode_D3D12_GPU_VIRTUAL_ADDRESS(enc, &SourceAccelerationStructureData);
    npt_encode_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE(enc, &Mode);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure_reply(const D3D12_GPU_VIRTUAL_ADDRESS DestAccelerationStructureData,const D3D12_GPU_VIRTUAL_ADDRESS SourceAccelerationStructureData,const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE Mode)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip DestAccelerationStructureData (input only) */
/* skip SourceAccelerationStructureData (input only) */
/* skip Mode (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(struct npt_ring *ring,
                                                                          uint32_t cmd_flags,
                                                                          npt_object_id object_id,
                                                                          D3D12_GPU_VIRTUAL_ADDRESS DestAccelerationStructureData,
                                                                          D3D12_GPU_VIRTUAL_ADDRESS SourceAccelerationStructureData,
                                                                          D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE Mode,
                                                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(DestAccelerationStructureData,SourceAccelerationStructureData,Mode);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure_reply(DestAccelerationStructureData,SourceAccelerationStructureData,Mode)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(enc, cmd_flags, object_id, DestAccelerationStructureData, SourceAccelerationStructureData, Mode);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(struct npt_ring *ring,
                                                                         npt_object_id object_id,
                                                                         D3D12_GPU_VIRTUAL_ADDRESS DestAccelerationStructureData,
                                                                         D3D12_GPU_VIRTUAL_ADDRESS SourceAccelerationStructureData,
                                                                         D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE Mode)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(ring, 0, object_id, DestAccelerationStructureData, SourceAccelerationStructureData, Mode, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(struct npt_ring *ring,
                                                                        npt_object_id object_id,
                                                                        D3D12_GPU_VIRTUAL_ADDRESS DestAccelerationStructureData,
                                                                        D3D12_GPU_VIRTUAL_ADDRESS SourceAccelerationStructureData,
                                                                        D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE Mode)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(ring, NPT_CMD_FLAG_REPLY, object_id, DestAccelerationStructureData, SourceAccelerationStructureData, Mode, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList4::SetPipelineState1
 */

struct npt_command_ID3D12GraphicsCommandList4_SetPipelineState1 {
    void *_self;
    ID3D12StateObject * pStateObject;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_SetPipelineState1(const ID3D12StateObject * pStateObject)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_SetPipelineState1(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D12StateObject * pStateObject)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList4_SetPipelineState1(pStateObject);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_SetPipelineState1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pStateObject));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_SetPipelineState1_reply(const ID3D12StateObject * pStateObject)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pStateObject (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList4_SetPipelineState1(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        ID3D12StateObject * pStateObject,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList4_SetPipelineState1(pStateObject);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList4_SetPipelineState1_reply(pStateObject)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList4_SetPipelineState1(enc, cmd_flags, object_id, pStateObject);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList4_SetPipelineState1(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       ID3D12StateObject * pStateObject)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_SetPipelineState1(ring, 0, object_id, pStateObject, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList4_SetPipelineState1(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      ID3D12StateObject * pStateObject)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_SetPipelineState1(ring, NPT_CMD_FLAG_REPLY, object_id, pStateObject, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList4::DispatchRays
 */

struct npt_command_ID3D12GraphicsCommandList4_DispatchRays {
    void *_self;
    const D3D12_DISPATCH_RAYS_DESC * pDesc;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_DispatchRays(const D3D12_DISPATCH_RAYS_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_DISPATCH_RAYS_DESC(pDesc, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_DispatchRays(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   const D3D12_DISPATCH_RAYS_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList4_DispatchRays(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_DispatchRays),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_DISPATCH_RAYS_DESC(enc, pDesc);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList4_DispatchRays_reply(const D3D12_DISPATCH_RAYS_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList4_DispatchRays(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   const D3D12_DISPATCH_RAYS_DESC * pDesc,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList4_DispatchRays(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList4_DispatchRays_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList4_DispatchRays(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList4_DispatchRays(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  const D3D12_DISPATCH_RAYS_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_DispatchRays(ring, 0, object_id, pDesc, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList4_DispatchRays(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const D3D12_DISPATCH_RAYS_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList4_DispatchRays(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList5::RSSetShadingRate
 */

struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRate {
    void *_self;
    D3D12_SHADING_RATE baseShadingRate;
    const D3D12_SHADING_RATE_COMBINER * combiners;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRate(const D3D12_SHADING_RATE baseShadingRate,const D3D12_SHADING_RATE_COMBINER * combiners)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_SHADING_RATE(&baseShadingRate, max_mode);
    cmd_size += npt_sizeof_array_count(combiners ? D3D12_RS_SET_SHADING_RATE_COMBINER_COUNT : 0);
    if (combiners)
        cmd_size += npt_sizeof_D3D12_SHADING_RATE_COMBINER_array(combiners, D3D12_RS_SET_SHADING_RATE_COMBINER_COUNT);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList5_RSSetShadingRate(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       D3D12_SHADING_RATE baseShadingRate,
                                                       const D3D12_SHADING_RATE_COMBINER * combiners)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRate(baseShadingRate,combiners);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList5,
                                 NPT_METHOD_ID3D12GraphicsCommandList5_RSSetShadingRate),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_SHADING_RATE(enc, &baseShadingRate);
    if (combiners) {
        npt_encode_array_count(enc, D3D12_RS_SET_SHADING_RATE_COMBINER_COUNT);
        npt_encode_D3D12_SHADING_RATE_COMBINER_array(enc, combiners, D3D12_RS_SET_SHADING_RATE_COMBINER_COUNT);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRate_reply(const D3D12_SHADING_RATE baseShadingRate,const D3D12_SHADING_RATE_COMBINER * combiners)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip baseShadingRate (input only) */
/* skip combiners (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList5_RSSetShadingRate(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       D3D12_SHADING_RATE baseShadingRate,
                                                       const D3D12_SHADING_RATE_COMBINER * combiners,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRate(baseShadingRate,combiners);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRate_reply(baseShadingRate,combiners)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList5_RSSetShadingRate(enc, cmd_flags, object_id, baseShadingRate, combiners);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList5_RSSetShadingRate(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      D3D12_SHADING_RATE baseShadingRate,
                                                      const D3D12_SHADING_RATE_COMBINER * combiners)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList5_RSSetShadingRate(ring, 0, object_id, baseShadingRate, combiners, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList5_RSSetShadingRate(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     D3D12_SHADING_RATE baseShadingRate,
                                                     const D3D12_SHADING_RATE_COMBINER * combiners)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList5_RSSetShadingRate(ring, NPT_CMD_FLAG_REPLY, object_id, baseShadingRate, combiners, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList5::RSSetShadingRateImage
 */

struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRateImage {
    void *_self;
    ID3D12Resource * shadingRateImage;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRateImage(const ID3D12Resource * shadingRateImage)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList5_RSSetShadingRateImage(struct npt_cs_encoder *enc,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D12Resource * shadingRateImage)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRateImage(shadingRateImage);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList5,
                                 NPT_METHOD_ID3D12GraphicsCommandList5_RSSetShadingRateImage),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(shadingRateImage));
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRateImage_reply(const ID3D12Resource * shadingRateImage)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip shadingRateImage (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList5_RSSetShadingRateImage(struct npt_ring *ring,
                                                            uint32_t cmd_flags,
                                                            npt_object_id object_id,
                                                            ID3D12Resource * shadingRateImage,
                                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRateImage(shadingRateImage);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList5_RSSetShadingRateImage_reply(shadingRateImage)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList5_RSSetShadingRateImage(enc, cmd_flags, object_id, shadingRateImage);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList5_RSSetShadingRateImage(struct npt_ring *ring,
                                                           npt_object_id object_id,
                                                           ID3D12Resource * shadingRateImage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList5_RSSetShadingRateImage(ring, 0, object_id, shadingRateImage, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList5_RSSetShadingRateImage(struct npt_ring *ring,
                                                          npt_object_id object_id,
                                                          ID3D12Resource * shadingRateImage)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList5_RSSetShadingRateImage(ring, NPT_CMD_FLAG_REPLY, object_id, shadingRateImage, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList6::DispatchMesh
 */

struct npt_command_ID3D12GraphicsCommandList6_DispatchMesh {
    void *_self;
    UINT ThreadGroupCountX;
    UINT ThreadGroupCountY;
    UINT ThreadGroupCountZ;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList6_DispatchMesh(const UINT ThreadGroupCountX,const UINT ThreadGroupCountY,const UINT ThreadGroupCountZ)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&ThreadGroupCountX, max_mode);
    cmd_size += npt_sizeof_UINT(&ThreadGroupCountY, max_mode);
    cmd_size += npt_sizeof_UINT(&ThreadGroupCountZ, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList6_DispatchMesh(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT ThreadGroupCountX,
                                                   UINT ThreadGroupCountY,
                                                   UINT ThreadGroupCountZ)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList6_DispatchMesh(ThreadGroupCountX,ThreadGroupCountY,ThreadGroupCountZ);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList6,
                                 NPT_METHOD_ID3D12GraphicsCommandList6_DispatchMesh),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &ThreadGroupCountX);
    npt_encode_UINT(enc, &ThreadGroupCountY);
    npt_encode_UINT(enc, &ThreadGroupCountZ);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList6_DispatchMesh_reply(const UINT ThreadGroupCountX,const UINT ThreadGroupCountY,const UINT ThreadGroupCountZ)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip ThreadGroupCountX (input only) */
/* skip ThreadGroupCountY (input only) */
/* skip ThreadGroupCountZ (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList6_DispatchMesh(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   UINT ThreadGroupCountX,
                                                   UINT ThreadGroupCountY,
                                                   UINT ThreadGroupCountZ,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList6_DispatchMesh(ThreadGroupCountX,ThreadGroupCountY,ThreadGroupCountZ);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList6_DispatchMesh_reply(ThreadGroupCountX,ThreadGroupCountY,ThreadGroupCountZ)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList6_DispatchMesh(enc, cmd_flags, object_id, ThreadGroupCountX, ThreadGroupCountY, ThreadGroupCountZ);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList6_DispatchMesh(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT ThreadGroupCountX,
                                                  UINT ThreadGroupCountY,
                                                  UINT ThreadGroupCountZ)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList6_DispatchMesh(ring, 0, object_id, ThreadGroupCountX, ThreadGroupCountY, ThreadGroupCountZ, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList6_DispatchMesh(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 UINT ThreadGroupCountX,
                                                 UINT ThreadGroupCountY,
                                                 UINT ThreadGroupCountZ)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList6_DispatchMesh(ring, NPT_CMD_FLAG_REPLY, object_id, ThreadGroupCountX, ThreadGroupCountY, ThreadGroupCountZ, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList7::Barrier
 */

struct npt_command_ID3D12GraphicsCommandList7_Barrier {
    void *_self;
    UINT32 NumBarrierGroups;
    const D3D12_BARRIER_GROUP * pBarrierGroups;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList7_Barrier(const UINT32 NumBarrierGroups,const D3D12_BARRIER_GROUP * pBarrierGroups)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT32(&NumBarrierGroups, max_mode);
    cmd_size += npt_sizeof_array_count(pBarrierGroups ? NumBarrierGroups : 0);
    for (uint32_t _i = 0; _i < (pBarrierGroups ? NumBarrierGroups : 0); _i++)
        cmd_size += npt_sizeof_D3D12_BARRIER_GROUP(&pBarrierGroups[_i], max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList7_Barrier(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT32 NumBarrierGroups,
                                              const D3D12_BARRIER_GROUP * pBarrierGroups)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList7_Barrier(NumBarrierGroups,pBarrierGroups);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList7,
                                 NPT_METHOD_ID3D12GraphicsCommandList7_Barrier),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT32(enc, &NumBarrierGroups);
    if (pBarrierGroups) {
        npt_encode_array_count(enc, NumBarrierGroups);
        for (uint32_t _i = 0; _i < (uint32_t)(NumBarrierGroups); _i++)
            npt_encode_D3D12_BARRIER_GROUP(enc, &pBarrierGroups[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList7_Barrier_reply(const UINT32 NumBarrierGroups,const D3D12_BARRIER_GROUP * pBarrierGroups)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip NumBarrierGroups (input only) */
/* skip pBarrierGroups (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList7_Barrier(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              UINT32 NumBarrierGroups,
                                              const D3D12_BARRIER_GROUP * pBarrierGroups,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList7_Barrier(NumBarrierGroups,pBarrierGroups);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList7_Barrier_reply(NumBarrierGroups,pBarrierGroups)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList7_Barrier(enc, cmd_flags, object_id, NumBarrierGroups, pBarrierGroups);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList7_Barrier(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             UINT32 NumBarrierGroups,
                                             const D3D12_BARRIER_GROUP * pBarrierGroups)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList7_Barrier(ring, 0, object_id, NumBarrierGroups, pBarrierGroups, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList7_Barrier(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT32 NumBarrierGroups,
                                            const D3D12_BARRIER_GROUP * pBarrierGroups)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList7_Barrier(ring, NPT_CMD_FLAG_REPLY, object_id, NumBarrierGroups, pBarrierGroups, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList8::OMSetFrontAndBackStencilRef
 */

struct npt_command_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef {
    void *_self;
    UINT FrontStencilRef;
    UINT BackStencilRef;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(const UINT FrontStencilRef,const UINT BackStencilRef)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&FrontStencilRef, max_mode);
    cmd_size += npt_sizeof_UINT(&BackStencilRef, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(struct npt_cs_encoder *enc,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  UINT FrontStencilRef,
                                                                  UINT BackStencilRef)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(FrontStencilRef,BackStencilRef);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList8,
                                 NPT_METHOD_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &FrontStencilRef);
    npt_encode_UINT(enc, &BackStencilRef);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef_reply(const UINT FrontStencilRef,const UINT BackStencilRef)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip FrontStencilRef (input only) */
/* skip BackStencilRef (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(struct npt_ring *ring,
                                                                  uint32_t cmd_flags,
                                                                  npt_object_id object_id,
                                                                  UINT FrontStencilRef,
                                                                  UINT BackStencilRef,
                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(FrontStencilRef,BackStencilRef);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef_reply(FrontStencilRef,BackStencilRef)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(enc, cmd_flags, object_id, FrontStencilRef, BackStencilRef);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(struct npt_ring *ring,
                                                                 npt_object_id object_id,
                                                                 UINT FrontStencilRef,
                                                                 UINT BackStencilRef)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(ring, 0, object_id, FrontStencilRef, BackStencilRef, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                UINT FrontStencilRef,
                                                                UINT BackStencilRef)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(ring, NPT_CMD_FLAG_REPLY, object_id, FrontStencilRef, BackStencilRef, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList9::RSSetDepthBias
 */

struct npt_command_ID3D12GraphicsCommandList9_RSSetDepthBias {
    void *_self;
    FLOAT DepthBias;
    FLOAT DepthBiasClamp;
    FLOAT SlopeScaledDepthBias;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList9_RSSetDepthBias(const FLOAT DepthBias,const FLOAT DepthBiasClamp,const FLOAT SlopeScaledDepthBias)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_FLOAT(&DepthBias, max_mode);
    cmd_size += npt_sizeof_FLOAT(&DepthBiasClamp, max_mode);
    cmd_size += npt_sizeof_FLOAT(&SlopeScaledDepthBias, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList9_RSSetDepthBias(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     FLOAT DepthBias,
                                                     FLOAT DepthBiasClamp,
                                                     FLOAT SlopeScaledDepthBias)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList9_RSSetDepthBias(DepthBias,DepthBiasClamp,SlopeScaledDepthBias);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList9,
                                 NPT_METHOD_ID3D12GraphicsCommandList9_RSSetDepthBias),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_FLOAT(enc, &DepthBias);
    npt_encode_FLOAT(enc, &DepthBiasClamp);
    npt_encode_FLOAT(enc, &SlopeScaledDepthBias);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList9_RSSetDepthBias_reply(const FLOAT DepthBias,const FLOAT DepthBiasClamp,const FLOAT SlopeScaledDepthBias)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip DepthBias (input only) */
/* skip DepthBiasClamp (input only) */
/* skip SlopeScaledDepthBias (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList9_RSSetDepthBias(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     FLOAT DepthBias,
                                                     FLOAT DepthBiasClamp,
                                                     FLOAT SlopeScaledDepthBias,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList9_RSSetDepthBias(DepthBias,DepthBiasClamp,SlopeScaledDepthBias);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList9_RSSetDepthBias_reply(DepthBias,DepthBiasClamp,SlopeScaledDepthBias)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList9_RSSetDepthBias(enc, cmd_flags, object_id, DepthBias, DepthBiasClamp, SlopeScaledDepthBias);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList9_RSSetDepthBias(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    FLOAT DepthBias,
                                                    FLOAT DepthBiasClamp,
                                                    FLOAT SlopeScaledDepthBias)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList9_RSSetDepthBias(ring, 0, object_id, DepthBias, DepthBiasClamp, SlopeScaledDepthBias, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList9_RSSetDepthBias(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   FLOAT DepthBias,
                                                   FLOAT DepthBiasClamp,
                                                   FLOAT SlopeScaledDepthBias)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList9_RSSetDepthBias(ring, NPT_CMD_FLAG_REPLY, object_id, DepthBias, DepthBiasClamp, SlopeScaledDepthBias, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList9::IASetIndexBufferStripCutValue
 */

struct npt_command_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue {
    void *_self;
    D3D12_INDEX_BUFFER_STRIP_CUT_VALUE IBStripCutValue;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(const D3D12_INDEX_BUFFER_STRIP_CUT_VALUE IBStripCutValue)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D12_INDEX_BUFFER_STRIP_CUT_VALUE(&IBStripCutValue, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(struct npt_cs_encoder *enc,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    D3D12_INDEX_BUFFER_STRIP_CUT_VALUE IBStripCutValue)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(IBStripCutValue);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList9,
                                 NPT_METHOD_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D12_INDEX_BUFFER_STRIP_CUT_VALUE(enc, &IBStripCutValue);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue_reply(const D3D12_INDEX_BUFFER_STRIP_CUT_VALUE IBStripCutValue)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip IBStripCutValue (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(struct npt_ring *ring,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    D3D12_INDEX_BUFFER_STRIP_CUT_VALUE IBStripCutValue,
                                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(IBStripCutValue);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue_reply(IBStripCutValue)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(enc, cmd_flags, object_id, IBStripCutValue);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(struct npt_ring *ring,
                                                                   npt_object_id object_id,
                                                                   D3D12_INDEX_BUFFER_STRIP_CUT_VALUE IBStripCutValue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(ring, 0, object_id, IBStripCutValue, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  D3D12_INDEX_BUFFER_STRIP_CUT_VALUE IBStripCutValue)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(ring, NPT_CMD_FLAG_REPLY, object_id, IBStripCutValue, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList10::SetProgram
 */

struct npt_command_ID3D12GraphicsCommandList10_SetProgram {
    void *_self;
    const D3D12_SET_PROGRAM_DESC * pDesc;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList10_SetProgram(const D3D12_SET_PROGRAM_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_SET_PROGRAM_DESC(pDesc, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList10_SetProgram(struct npt_cs_encoder *enc,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_SET_PROGRAM_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList10_SetProgram(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList10,
                                 NPT_METHOD_ID3D12GraphicsCommandList10_SetProgram),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_SET_PROGRAM_DESC(enc, pDesc);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList10_SetProgram_reply(const D3D12_SET_PROGRAM_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList10_SetProgram(struct npt_ring *ring,
                                                  uint32_t cmd_flags,
                                                  npt_object_id object_id,
                                                  const D3D12_SET_PROGRAM_DESC * pDesc,
                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList10_SetProgram(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList10_SetProgram_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList10_SetProgram(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList10_SetProgram(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 const D3D12_SET_PROGRAM_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList10_SetProgram(ring, 0, object_id, pDesc, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList10_SetProgram(struct npt_ring *ring,
                                                npt_object_id object_id,
                                                const D3D12_SET_PROGRAM_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList10_SetProgram(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12GraphicsCommandList10::DispatchGraph
 */

struct npt_command_ID3D12GraphicsCommandList10_DispatchGraph {
    void *_self;
    const D3D12_DISPATCH_GRAPH_DESC * pDesc;
};

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList10_DispatchGraph(const D3D12_DISPATCH_GRAPH_DESC * pDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_DISPATCH_GRAPH_DESC(pDesc, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12GraphicsCommandList10_DispatchGraph(struct npt_cs_encoder *enc,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const D3D12_DISPATCH_GRAPH_DESC * pDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12GraphicsCommandList10_DispatchGraph(pDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList10,
                                 NPT_METHOD_ID3D12GraphicsCommandList10_DispatchGraph),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_DISPATCH_GRAPH_DESC(enc, pDesc);
}

static inline size_t
npt_sizeof_ID3D12GraphicsCommandList10_DispatchGraph_reply(const D3D12_DISPATCH_GRAPH_DESC * pDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12GraphicsCommandList10_DispatchGraph(struct npt_ring *ring,
                                                     uint32_t cmd_flags,
                                                     npt_object_id object_id,
                                                     const D3D12_DISPATCH_GRAPH_DESC * pDesc,
                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12GraphicsCommandList10_DispatchGraph(pDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12GraphicsCommandList10_DispatchGraph_reply(pDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12GraphicsCommandList10_DispatchGraph(enc, cmd_flags, object_id, pDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12GraphicsCommandList10_DispatchGraph(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    const D3D12_DISPATCH_GRAPH_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList10_DispatchGraph(ring, 0, object_id, pDesc, &submit);
}

static inline void
npt_call_ID3D12GraphicsCommandList10_DispatchGraph(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   const D3D12_DISPATCH_GRAPH_DESC * pDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12GraphicsCommandList10_DispatchGraph(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12GRAPHICSCOMMANDLIST_H */
