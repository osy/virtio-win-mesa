/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for toplevel.
 */

#ifndef NPT_PROTOCOL_GUEST_TOPLEVEL_H
#define NPT_PROTOCOL_GUEST_TOPLEVEL_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * CreateDXGIFactory (group 1, id 0)
 */

struct npt_command_CreateDXGIFactory {
    const IID * riid;
    void ** ppFactory;
    /* Shadow: guest-allocated id for ppFactory. */
    uint64_t _guest_id_ppFactory;
    HRESULT ret;
};

static inline size_t
npt_sizeof_CreateDXGIFactory(const IID * riid,void ** ppFactory)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppFactory: guest id */
    return cmd_size;
}

static inline void
npt_encode_CreateDXGIFactory(struct npt_cs_encoder *enc,
                             uint32_t cmd_flags,
                             const IID * riid,
                             void ** ppFactory)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_CreateDXGIFactory(riid,ppFactory);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(1, 0),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppFactory = 0;
    if (ppFactory) {
        _gid_ppFactory = npt_com_allocate_next_id();
        *ppFactory = (void *)(uintptr_t)_gid_ppFactory;
    }
    npt_encode_uint64_t(enc, &_gid_ppFactory);
}
}

static inline size_t
npt_sizeof_CreateDXGIFactory_reply(const IID * riid,void ** ppFactory)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip riid (input only) */
/* skip ppFactory (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_CreateDXGIFactory_reply(struct npt_cs_decoder *dec,
                                   void ** ppFactory,
                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(1, 0))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("CreateDXGIFactory reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(1, 0));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppFactory (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_CreateDXGIFactory(struct npt_ring *ring,
                             uint32_t cmd_flags,
                             const IID * riid,
                             void ** ppFactory,
                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_CreateDXGIFactory(riid,ppFactory);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_CreateDXGIFactory_reply(riid,ppFactory)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_CreateDXGIFactory(enc, cmd_flags, riid, ppFactory);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_CreateDXGIFactory(struct npt_ring *ring,
                            const IID * riid,
                            void ** ppFactory)
{
    struct npt_ring_submit_command submit;
    npt_submit_CreateDXGIFactory(ring, 0, riid, ppFactory, &submit);
}

static inline HRESULT
npt_call_CreateDXGIFactory(struct npt_ring *ring,
                           const IID * riid,
                           void ** ppFactory)
{
    struct npt_ring_submit_command submit;
    npt_submit_CreateDXGIFactory(ring, NPT_CMD_FLAG_REPLY, riid, ppFactory, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_CreateDXGIFactory_reply(dec, ppFactory, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * CreateDXGIFactory1 (group 1, id 1)
 */

struct npt_command_CreateDXGIFactory1 {
    const IID * riid;
    void ** ppFactory;
    /* Shadow: guest-allocated id for ppFactory. */
    uint64_t _guest_id_ppFactory;
    HRESULT ret;
};

static inline size_t
npt_sizeof_CreateDXGIFactory1(const IID * riid,void ** ppFactory)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppFactory: guest id */
    return cmd_size;
}

static inline void
npt_encode_CreateDXGIFactory1(struct npt_cs_encoder *enc,
                              uint32_t cmd_flags,
                              const IID * riid,
                              void ** ppFactory)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_CreateDXGIFactory1(riid,ppFactory);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(1, 1),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppFactory = 0;
    if (ppFactory) {
        _gid_ppFactory = npt_com_allocate_next_id();
        *ppFactory = (void *)(uintptr_t)_gid_ppFactory;
    }
    npt_encode_uint64_t(enc, &_gid_ppFactory);
}
}

static inline size_t
npt_sizeof_CreateDXGIFactory1_reply(const IID * riid,void ** ppFactory)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip riid (input only) */
/* skip ppFactory (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_CreateDXGIFactory1_reply(struct npt_cs_decoder *dec,
                                    void ** ppFactory,
                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(1, 1))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("CreateDXGIFactory1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(1, 1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppFactory (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_CreateDXGIFactory1(struct npt_ring *ring,
                              uint32_t cmd_flags,
                              const IID * riid,
                              void ** ppFactory,
                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_CreateDXGIFactory1(riid,ppFactory);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_CreateDXGIFactory1_reply(riid,ppFactory)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_CreateDXGIFactory1(enc, cmd_flags, riid, ppFactory);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_CreateDXGIFactory1(struct npt_ring *ring,
                             const IID * riid,
                             void ** ppFactory)
{
    struct npt_ring_submit_command submit;
    npt_submit_CreateDXGIFactory1(ring, 0, riid, ppFactory, &submit);
}

static inline HRESULT
npt_call_CreateDXGIFactory1(struct npt_ring *ring,
                            const IID * riid,
                            void ** ppFactory)
{
    struct npt_ring_submit_command submit;
    npt_submit_CreateDXGIFactory1(ring, NPT_CMD_FLAG_REPLY, riid, ppFactory, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_CreateDXGIFactory1_reply(dec, ppFactory, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * CreateDXGIFactory2 (group 1, id 2)
 */

struct npt_command_CreateDXGIFactory2 {
    UINT Flags;
    const IID * riid;
    void ** ppFactory;
    /* Shadow: guest-allocated id for ppFactory. */
    uint64_t _guest_id_ppFactory;
    HRESULT ret;
};

static inline size_t
npt_sizeof_CreateDXGIFactory2(const UINT Flags,const IID * riid,void ** ppFactory)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppFactory: guest id */
    return cmd_size;
}

static inline void
npt_encode_CreateDXGIFactory2(struct npt_cs_encoder *enc,
                              uint32_t cmd_flags,
                              UINT Flags,
                              const IID * riid,
                              void ** ppFactory)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_CreateDXGIFactory2(Flags,riid,ppFactory);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(1, 2),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Flags);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppFactory = 0;
    if (ppFactory) {
        _gid_ppFactory = npt_com_allocate_next_id();
        *ppFactory = (void *)(uintptr_t)_gid_ppFactory;
    }
    npt_encode_uint64_t(enc, &_gid_ppFactory);
}
}

static inline size_t
npt_sizeof_CreateDXGIFactory2_reply(const UINT Flags,const IID * riid,void ** ppFactory)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Flags (input only) */
/* skip riid (input only) */
/* skip ppFactory (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_CreateDXGIFactory2_reply(struct npt_cs_decoder *dec,
                                    void ** ppFactory,
                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(1, 2))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("CreateDXGIFactory2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(1, 2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppFactory (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_CreateDXGIFactory2(struct npt_ring *ring,
                              uint32_t cmd_flags,
                              UINT Flags,
                              const IID * riid,
                              void ** ppFactory,
                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_CreateDXGIFactory2(Flags,riid,ppFactory);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_CreateDXGIFactory2_reply(Flags,riid,ppFactory)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_CreateDXGIFactory2(enc, cmd_flags, Flags, riid, ppFactory);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_CreateDXGIFactory2(struct npt_ring *ring,
                             UINT Flags,
                             const IID * riid,
                             void ** ppFactory)
{
    struct npt_ring_submit_command submit;
    npt_submit_CreateDXGIFactory2(ring, 0, Flags, riid, ppFactory, &submit);
}

static inline HRESULT
npt_call_CreateDXGIFactory2(struct npt_ring *ring,
                            UINT Flags,
                            const IID * riid,
                            void ** ppFactory)
{
    struct npt_ring_submit_command submit;
    npt_submit_CreateDXGIFactory2(ring, NPT_CMD_FLAG_REPLY, Flags, riid, ppFactory, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_CreateDXGIFactory2_reply(dec, ppFactory, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * DXGIDeclareAdapterRemovalSupport (group 1, id 3)
 */

struct npt_command_DXGIDeclareAdapterRemovalSupport {
    HRESULT ret;
};

static inline size_t
npt_sizeof_DXGIDeclareAdapterRemovalSupport(void)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    return cmd_size;
}

static inline void
npt_encode_DXGIDeclareAdapterRemovalSupport(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_DXGIDeclareAdapterRemovalSupport();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(1, 3),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_DXGIDeclareAdapterRemovalSupport_reply(void)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_DXGIDeclareAdapterRemovalSupport_reply(struct npt_cs_decoder *dec,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(1, 3))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("DXGIDeclareAdapterRemovalSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(1, 3));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_DXGIDeclareAdapterRemovalSupport(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_DXGIDeclareAdapterRemovalSupport();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_DXGIDeclareAdapterRemovalSupport_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_DXGIDeclareAdapterRemovalSupport(enc, cmd_flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_DXGIDeclareAdapterRemovalSupport(struct npt_ring *ring)
{
    struct npt_ring_submit_command submit;
    npt_submit_DXGIDeclareAdapterRemovalSupport(ring, 0, &submit);
}

static inline HRESULT
npt_call_DXGIDeclareAdapterRemovalSupport(struct npt_ring *ring)
{
    struct npt_ring_submit_command submit;
    npt_submit_DXGIDeclareAdapterRemovalSupport(ring, NPT_CMD_FLAG_REPLY, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_DXGIDeclareAdapterRemovalSupport_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * D3D11CreateDevice (group 2, id 0)
 */

struct npt_command_D3D11CreateDevice {
    IDXGIAdapter * pAdapter;
    D3D_DRIVER_TYPE DriverType;
    HMODULE Software;
    UINT Flags;
    const D3D_FEATURE_LEVEL * pFeatureLevels;
    UINT FeatureLevels;
    UINT SDKVersion;
    ID3D11Device ** ppDevice;
    D3D_FEATURE_LEVEL * pFeatureLevel;
    ID3D11DeviceContext ** ppImmediateContext;
    /* Shadow: guest-allocated id for ppDevice. */
    uint64_t _guest_id_ppDevice;
    /* Shadow: guest-allocated id for ppImmediateContext. */
    uint64_t _guest_id_ppImmediateContext;
    HRESULT ret;
};

static inline size_t
npt_sizeof_D3D11CreateDevice(const IDXGIAdapter * pAdapter,const D3D_DRIVER_TYPE DriverType,const HMODULE Software,const UINT Flags,const D3D_FEATURE_LEVEL * pFeatureLevels,const UINT FeatureLevels,const UINT SDKVersion,ID3D11Device ** ppDevice,const D3D_FEATURE_LEVEL * pFeatureLevel,ID3D11DeviceContext ** ppImmediateContext)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D_DRIVER_TYPE(&DriverType, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    cmd_size += npt_sizeof_array_count(pFeatureLevels ? FeatureLevels : 0);
    if (pFeatureLevels)
        cmd_size += npt_sizeof_D3D_FEATURE_LEVEL_array(pFeatureLevels, FeatureLevels);
    cmd_size += npt_sizeof_UINT(&FeatureLevels, max_mode);
    cmd_size += npt_sizeof_UINT(&SDKVersion, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDevice: guest id */
/* skip pFeatureLevel (output only) */
cmd_size += sizeof(uint64_t);  /* ppImmediateContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_D3D11CreateDevice(struct npt_cs_encoder *enc,
                             uint32_t cmd_flags,
                             IDXGIAdapter * pAdapter,
                             D3D_DRIVER_TYPE DriverType,
                             HMODULE Software,
                             UINT Flags,
                             const D3D_FEATURE_LEVEL * pFeatureLevels,
                             UINT FeatureLevels,
                             UINT SDKVersion,
                             ID3D11Device ** ppDevice,
                             D3D_FEATURE_LEVEL * pFeatureLevel,
                             ID3D11DeviceContext ** ppImmediateContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_D3D11CreateDevice(pAdapter,DriverType,Software,Flags,pFeatureLevels,FeatureLevels,SDKVersion,ppDevice,pFeatureLevel,ppImmediateContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(2, 0),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pAdapter));
    npt_encode_D3D_DRIVER_TYPE(enc, &DriverType);
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)Software));
    npt_encode_UINT(enc, &Flags);
    if (pFeatureLevels) {
        npt_encode_array_count(enc, FeatureLevels);
        npt_encode_D3D_FEATURE_LEVEL_array(enc, pFeatureLevels, FeatureLevels);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &FeatureLevels);
    npt_encode_UINT(enc, &SDKVersion);
{
    uint64_t _gid_ppDevice = 0;
    if (ppDevice) {
        _gid_ppDevice = npt_com_allocate_next_id();
        *ppDevice = (ID3D11Device *)(uintptr_t)_gid_ppDevice;
    }
    npt_encode_uint64_t(enc, &_gid_ppDevice);
}
/* skip pFeatureLevel (output only) */
{
    uint64_t _gid_ppImmediateContext = 0;
    if (ppImmediateContext) {
        _gid_ppImmediateContext = npt_com_allocate_next_id();
        *ppImmediateContext = (ID3D11DeviceContext *)(uintptr_t)_gid_ppImmediateContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppImmediateContext);
}
}

static inline size_t
npt_sizeof_D3D11CreateDevice_reply(const IDXGIAdapter * pAdapter,const D3D_DRIVER_TYPE DriverType,const HMODULE Software,const UINT Flags,const D3D_FEATURE_LEVEL * pFeatureLevels,const UINT FeatureLevels,const UINT SDKVersion,ID3D11Device ** ppDevice,const D3D_FEATURE_LEVEL * pFeatureLevel,ID3D11DeviceContext ** ppImmediateContext)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAdapter (input only) */
/* skip DriverType (input only) */
/* skip Software (input only) */
/* skip Flags (input only) */
/* skip pFeatureLevels (input only) */
/* skip FeatureLevels (input only) */
/* skip SDKVersion (input only) */
/* skip ppDevice (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D_FEATURE_LEVEL(&(const D3D_FEATURE_LEVEL){0}, 1);
/* skip ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_D3D11CreateDevice_reply(struct npt_cs_decoder *dec,
                                   ID3D11Device ** ppDevice,
                                   D3D_FEATURE_LEVEL * pFeatureLevel,
                                   ID3D11DeviceContext ** ppImmediateContext,
                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(2, 0))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("D3D11CreateDevice reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(2, 0));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDevice (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pFeatureLevel)
            npt_decode_D3D_FEATURE_LEVEL(dec, (D3D_FEATURE_LEVEL *)pFeatureLevel);
    }
/* skip ppImmediateContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_D3D11CreateDevice(struct npt_ring *ring,
                             uint32_t cmd_flags,
                             IDXGIAdapter * pAdapter,
                             D3D_DRIVER_TYPE DriverType,
                             HMODULE Software,
                             UINT Flags,
                             const D3D_FEATURE_LEVEL * pFeatureLevels,
                             UINT FeatureLevels,
                             UINT SDKVersion,
                             ID3D11Device ** ppDevice,
                             D3D_FEATURE_LEVEL * pFeatureLevel,
                             ID3D11DeviceContext ** ppImmediateContext,
                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_D3D11CreateDevice(pAdapter,DriverType,Software,Flags,pFeatureLevels,FeatureLevels,SDKVersion,ppDevice,pFeatureLevel,ppImmediateContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_D3D11CreateDevice_reply(pAdapter,DriverType,Software,Flags,pFeatureLevels,FeatureLevels,SDKVersion,ppDevice,pFeatureLevel,ppImmediateContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_D3D11CreateDevice(enc, cmd_flags, pAdapter, DriverType, Software, Flags, pFeatureLevels, FeatureLevels, SDKVersion, ppDevice, pFeatureLevel, ppImmediateContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_D3D11CreateDevice(struct npt_ring *ring,
                            IDXGIAdapter * pAdapter,
                            D3D_DRIVER_TYPE DriverType,
                            HMODULE Software,
                            UINT Flags,
                            const D3D_FEATURE_LEVEL * pFeatureLevels,
                            UINT FeatureLevels,
                            UINT SDKVersion,
                            ID3D11Device ** ppDevice,
                            D3D_FEATURE_LEVEL * pFeatureLevel,
                            ID3D11DeviceContext ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D11CreateDevice(ring, 0, pAdapter, DriverType, Software, Flags, pFeatureLevels, FeatureLevels, SDKVersion, ppDevice, pFeatureLevel, ppImmediateContext, &submit);
}

static inline HRESULT
npt_call_D3D11CreateDevice(struct npt_ring *ring,
                           IDXGIAdapter * pAdapter,
                           D3D_DRIVER_TYPE DriverType,
                           HMODULE Software,
                           UINT Flags,
                           const D3D_FEATURE_LEVEL * pFeatureLevels,
                           UINT FeatureLevels,
                           UINT SDKVersion,
                           ID3D11Device ** ppDevice,
                           D3D_FEATURE_LEVEL * pFeatureLevel,
                           ID3D11DeviceContext ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D11CreateDevice(ring, NPT_CMD_FLAG_REPLY, pAdapter, DriverType, Software, Flags, pFeatureLevels, FeatureLevels, SDKVersion, ppDevice, pFeatureLevel, ppImmediateContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_D3D11CreateDevice_reply(dec, ppDevice, pFeatureLevel, ppImmediateContext, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * D3D11CreateDeviceAndSwapChain (group 2, id 1)
 */

struct npt_command_D3D11CreateDeviceAndSwapChain {
    IDXGIAdapter * pAdapter;
    D3D_DRIVER_TYPE DriverType;
    HMODULE Software;
    UINT Flags;
    const D3D_FEATURE_LEVEL * pFeatureLevels;
    UINT FeatureLevels;
    UINT SDKVersion;
    const DXGI_SWAP_CHAIN_DESC * pSwapChainDesc;
    IDXGISwapChain ** ppSwapChain;
    ID3D11Device ** ppDevice;
    D3D_FEATURE_LEVEL * pFeatureLevel;
    ID3D11DeviceContext ** ppImmediateContext;
    /* Shadow: guest-allocated id for ppSwapChain. */
    uint64_t _guest_id_ppSwapChain;
    /* Shadow: guest-allocated id for ppDevice. */
    uint64_t _guest_id_ppDevice;
    /* Shadow: guest-allocated id for ppImmediateContext. */
    uint64_t _guest_id_ppImmediateContext;
    HRESULT ret;
};

static inline size_t
npt_sizeof_D3D11CreateDeviceAndSwapChain(const IDXGIAdapter * pAdapter,const D3D_DRIVER_TYPE DriverType,const HMODULE Software,const UINT Flags,const D3D_FEATURE_LEVEL * pFeatureLevels,const UINT FeatureLevels,const UINT SDKVersion,const DXGI_SWAP_CHAIN_DESC * pSwapChainDesc,IDXGISwapChain ** ppSwapChain,ID3D11Device ** ppDevice,const D3D_FEATURE_LEVEL * pFeatureLevel,ID3D11DeviceContext ** ppImmediateContext)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D_DRIVER_TYPE(&DriverType, max_mode);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    cmd_size += npt_sizeof_array_count(pFeatureLevels ? FeatureLevels : 0);
    if (pFeatureLevels)
        cmd_size += npt_sizeof_D3D_FEATURE_LEVEL_array(pFeatureLevels, FeatureLevels);
    cmd_size += npt_sizeof_UINT(&FeatureLevels, max_mode);
    cmd_size += npt_sizeof_UINT(&SDKVersion, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSwapChainDesc);
    if (pSwapChainDesc)
        cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_DESC(pSwapChainDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppSwapChain: guest id */
cmd_size += sizeof(uint64_t);  /* ppDevice: guest id */
/* skip pFeatureLevel (output only) */
cmd_size += sizeof(uint64_t);  /* ppImmediateContext: guest id */
    return cmd_size;
}

static inline void
npt_encode_D3D11CreateDeviceAndSwapChain(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         IDXGIAdapter * pAdapter,
                                         D3D_DRIVER_TYPE DriverType,
                                         HMODULE Software,
                                         UINT Flags,
                                         const D3D_FEATURE_LEVEL * pFeatureLevels,
                                         UINT FeatureLevels,
                                         UINT SDKVersion,
                                         const DXGI_SWAP_CHAIN_DESC * pSwapChainDesc,
                                         IDXGISwapChain ** ppSwapChain,
                                         ID3D11Device ** ppDevice,
                                         D3D_FEATURE_LEVEL * pFeatureLevel,
                                         ID3D11DeviceContext ** ppImmediateContext)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_D3D11CreateDeviceAndSwapChain(pAdapter,DriverType,Software,Flags,pFeatureLevels,FeatureLevels,SDKVersion,pSwapChainDesc,ppSwapChain,ppDevice,pFeatureLevel,ppImmediateContext);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(2, 1),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pAdapter));
    npt_encode_D3D_DRIVER_TYPE(enc, &DriverType);
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)Software));
    npt_encode_UINT(enc, &Flags);
    if (pFeatureLevels) {
        npt_encode_array_count(enc, FeatureLevels);
        npt_encode_D3D_FEATURE_LEVEL_array(enc, pFeatureLevels, FeatureLevels);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &FeatureLevels);
    npt_encode_UINT(enc, &SDKVersion);
    if (npt_encode_simple_pointer(enc, pSwapChainDesc))
        npt_encode_DXGI_SWAP_CHAIN_DESC(enc, pSwapChainDesc);
{
    uint64_t _gid_ppSwapChain = 0;
    if (ppSwapChain) {
        _gid_ppSwapChain = npt_com_allocate_next_id();
        *ppSwapChain = (IDXGISwapChain *)(uintptr_t)_gid_ppSwapChain;
    }
    npt_encode_uint64_t(enc, &_gid_ppSwapChain);
}
{
    uint64_t _gid_ppDevice = 0;
    if (ppDevice) {
        _gid_ppDevice = npt_com_allocate_next_id();
        *ppDevice = (ID3D11Device *)(uintptr_t)_gid_ppDevice;
    }
    npt_encode_uint64_t(enc, &_gid_ppDevice);
}
/* skip pFeatureLevel (output only) */
{
    uint64_t _gid_ppImmediateContext = 0;
    if (ppImmediateContext) {
        _gid_ppImmediateContext = npt_com_allocate_next_id();
        *ppImmediateContext = (ID3D11DeviceContext *)(uintptr_t)_gid_ppImmediateContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppImmediateContext);
}
}

static inline size_t
npt_sizeof_D3D11CreateDeviceAndSwapChain_reply(const IDXGIAdapter * pAdapter,const D3D_DRIVER_TYPE DriverType,const HMODULE Software,const UINT Flags,const D3D_FEATURE_LEVEL * pFeatureLevels,const UINT FeatureLevels,const UINT SDKVersion,const DXGI_SWAP_CHAIN_DESC * pSwapChainDesc,IDXGISwapChain ** ppSwapChain,ID3D11Device ** ppDevice,const D3D_FEATURE_LEVEL * pFeatureLevel,ID3D11DeviceContext ** ppImmediateContext)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAdapter (input only) */
/* skip DriverType (input only) */
/* skip Software (input only) */
/* skip Flags (input only) */
/* skip pFeatureLevels (input only) */
/* skip FeatureLevels (input only) */
/* skip SDKVersion (input only) */
/* skip pSwapChainDesc (input only) */
/* skip ppSwapChain (guest-allocated id, registered host-side; not in reply) */
/* skip ppDevice (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D_FEATURE_LEVEL(&(const D3D_FEATURE_LEVEL){0}, 1);
/* skip ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_D3D11CreateDeviceAndSwapChain_reply(struct npt_cs_decoder *dec,
                                               IDXGISwapChain ** ppSwapChain,
                                               ID3D11Device ** ppDevice,
                                               D3D_FEATURE_LEVEL * pFeatureLevel,
                                               ID3D11DeviceContext ** ppImmediateContext,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(2, 1))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("D3D11CreateDeviceAndSwapChain reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(2, 1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSwapChain (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppDevice (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pFeatureLevel)
            npt_decode_D3D_FEATURE_LEVEL(dec, (D3D_FEATURE_LEVEL *)pFeatureLevel);
    }
/* skip ppImmediateContext (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_D3D11CreateDeviceAndSwapChain(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         IDXGIAdapter * pAdapter,
                                         D3D_DRIVER_TYPE DriverType,
                                         HMODULE Software,
                                         UINT Flags,
                                         const D3D_FEATURE_LEVEL * pFeatureLevels,
                                         UINT FeatureLevels,
                                         UINT SDKVersion,
                                         const DXGI_SWAP_CHAIN_DESC * pSwapChainDesc,
                                         IDXGISwapChain ** ppSwapChain,
                                         ID3D11Device ** ppDevice,
                                         D3D_FEATURE_LEVEL * pFeatureLevel,
                                         ID3D11DeviceContext ** ppImmediateContext,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_D3D11CreateDeviceAndSwapChain(pAdapter,DriverType,Software,Flags,pFeatureLevels,FeatureLevels,SDKVersion,pSwapChainDesc,ppSwapChain,ppDevice,pFeatureLevel,ppImmediateContext);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_D3D11CreateDeviceAndSwapChain_reply(pAdapter,DriverType,Software,Flags,pFeatureLevels,FeatureLevels,SDKVersion,pSwapChainDesc,ppSwapChain,ppDevice,pFeatureLevel,ppImmediateContext)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_D3D11CreateDeviceAndSwapChain(enc, cmd_flags, pAdapter, DriverType, Software, Flags, pFeatureLevels, FeatureLevels, SDKVersion, pSwapChainDesc, ppSwapChain, ppDevice, pFeatureLevel, ppImmediateContext);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_D3D11CreateDeviceAndSwapChain(struct npt_ring *ring,
                                        IDXGIAdapter * pAdapter,
                                        D3D_DRIVER_TYPE DriverType,
                                        HMODULE Software,
                                        UINT Flags,
                                        const D3D_FEATURE_LEVEL * pFeatureLevels,
                                        UINT FeatureLevels,
                                        UINT SDKVersion,
                                        const DXGI_SWAP_CHAIN_DESC * pSwapChainDesc,
                                        IDXGISwapChain ** ppSwapChain,
                                        ID3D11Device ** ppDevice,
                                        D3D_FEATURE_LEVEL * pFeatureLevel,
                                        ID3D11DeviceContext ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D11CreateDeviceAndSwapChain(ring, 0, pAdapter, DriverType, Software, Flags, pFeatureLevels, FeatureLevels, SDKVersion, pSwapChainDesc, ppSwapChain, ppDevice, pFeatureLevel, ppImmediateContext, &submit);
}

static inline HRESULT
npt_call_D3D11CreateDeviceAndSwapChain(struct npt_ring *ring,
                                       IDXGIAdapter * pAdapter,
                                       D3D_DRIVER_TYPE DriverType,
                                       HMODULE Software,
                                       UINT Flags,
                                       const D3D_FEATURE_LEVEL * pFeatureLevels,
                                       UINT FeatureLevels,
                                       UINT SDKVersion,
                                       const DXGI_SWAP_CHAIN_DESC * pSwapChainDesc,
                                       IDXGISwapChain ** ppSwapChain,
                                       ID3D11Device ** ppDevice,
                                       D3D_FEATURE_LEVEL * pFeatureLevel,
                                       ID3D11DeviceContext ** ppImmediateContext)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D11CreateDeviceAndSwapChain(ring, NPT_CMD_FLAG_REPLY, pAdapter, DriverType, Software, Flags, pFeatureLevels, FeatureLevels, SDKVersion, pSwapChainDesc, ppSwapChain, ppDevice, pFeatureLevel, ppImmediateContext, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_D3D11CreateDeviceAndSwapChain_reply(dec, ppSwapChain, ppDevice, pFeatureLevel, ppImmediateContext, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * D3D11On12CreateDevice (group 2, id 2)
 */

struct npt_command_D3D11On12CreateDevice {
    IUnknown * pDevice;
    UINT Flags;
    const D3D_FEATURE_LEVEL * pFeatureLevels;
    UINT FeatureLevels;
    const IUnknown ** ppCommandQueues;
    UINT NumQueues;
    UINT NodeMask;
    ID3D11Device ** ppDevice;
    ID3D11DeviceContext ** ppImmediateContext;
    D3D_FEATURE_LEVEL * pChosenFeatureLevel;
    /* Shadow: guest-allocated id for ppDevice. */
    uint64_t _guest_id_ppDevice;
    /* Shadow: guest-allocated id for ppImmediateContext. */
    uint64_t _guest_id_ppImmediateContext;
    HRESULT ret;
};

static inline size_t
npt_sizeof_D3D11On12CreateDevice(const IUnknown * pDevice,const UINT Flags,const D3D_FEATURE_LEVEL * pFeatureLevels,const UINT FeatureLevels,const IUnknown ** ppCommandQueues,const UINT NumQueues,const UINT NodeMask,ID3D11Device ** ppDevice,ID3D11DeviceContext ** ppImmediateContext,const D3D_FEATURE_LEVEL * pChosenFeatureLevel)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    cmd_size += npt_sizeof_array_count(pFeatureLevels ? FeatureLevels : 0);
    if (pFeatureLevels)
        cmd_size += npt_sizeof_D3D_FEATURE_LEVEL_array(pFeatureLevels, FeatureLevels);
    cmd_size += npt_sizeof_UINT(&FeatureLevels, max_mode);
    cmd_size += npt_sizeof_array_count(NumQueues);
    cmd_size += sizeof(uint64_t) * NumQueues;
    cmd_size += npt_sizeof_UINT(&NumQueues, max_mode);
    cmd_size += npt_sizeof_UINT(&NodeMask, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDevice: guest id */
cmd_size += sizeof(uint64_t);  /* ppImmediateContext: guest id */
/* skip pChosenFeatureLevel (output only) */
    return cmd_size;
}

static inline void
npt_encode_D3D11On12CreateDevice(struct npt_cs_encoder *enc,
                                 uint32_t cmd_flags,
                                 IUnknown * pDevice,
                                 UINT Flags,
                                 const D3D_FEATURE_LEVEL * pFeatureLevels,
                                 UINT FeatureLevels,
                                 const IUnknown ** ppCommandQueues,
                                 UINT NumQueues,
                                 UINT NodeMask,
                                 ID3D11Device ** ppDevice,
                                 ID3D11DeviceContext ** ppImmediateContext,
                                 D3D_FEATURE_LEVEL * pChosenFeatureLevel)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_D3D11On12CreateDevice(pDevice,Flags,pFeatureLevels,FeatureLevels,ppCommandQueues,NumQueues,NodeMask,ppDevice,ppImmediateContext,pChosenFeatureLevel);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(2, 2),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
    npt_encode_UINT(enc, &Flags);
    if (pFeatureLevels) {
        npt_encode_array_count(enc, FeatureLevels);
        npt_encode_D3D_FEATURE_LEVEL_array(enc, pFeatureLevels, FeatureLevels);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &FeatureLevels);
    if (ppCommandQueues) {
        npt_encode_array_count(enc, NumQueues);
        for (uint32_t _i = 0; _i < (uint32_t)NumQueues; _i++)
            npt_encode_com_handle(enc, npt_object_get_id(ppCommandQueues[_i]));
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &NumQueues);
    npt_encode_UINT(enc, &NodeMask);
{
    uint64_t _gid_ppDevice = 0;
    if (ppDevice) {
        _gid_ppDevice = npt_com_allocate_next_id();
        *ppDevice = (ID3D11Device *)(uintptr_t)_gid_ppDevice;
    }
    npt_encode_uint64_t(enc, &_gid_ppDevice);
}
{
    uint64_t _gid_ppImmediateContext = 0;
    if (ppImmediateContext) {
        _gid_ppImmediateContext = npt_com_allocate_next_id();
        *ppImmediateContext = (ID3D11DeviceContext *)(uintptr_t)_gid_ppImmediateContext;
    }
    npt_encode_uint64_t(enc, &_gid_ppImmediateContext);
}
/* skip pChosenFeatureLevel (output only) */
}

static inline size_t
npt_sizeof_D3D11On12CreateDevice_reply(const IUnknown * pDevice,const UINT Flags,const D3D_FEATURE_LEVEL * pFeatureLevels,const UINT FeatureLevels,const IUnknown ** ppCommandQueues,const UINT NumQueues,const UINT NodeMask,ID3D11Device ** ppDevice,ID3D11DeviceContext ** ppImmediateContext,const D3D_FEATURE_LEVEL * pChosenFeatureLevel)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip Flags (input only) */
/* skip pFeatureLevels (input only) */
/* skip FeatureLevels (input only) */
/* skip ppCommandQueues (input only) */
/* skip NumQueues (input only) */
/* skip NodeMask (input only) */
/* skip ppDevice (guest-allocated id, registered host-side; not in reply) */
/* skip ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D_FEATURE_LEVEL(&(const D3D_FEATURE_LEVEL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_D3D11On12CreateDevice_reply(struct npt_cs_decoder *dec,
                                       ID3D11Device ** ppDevice,
                                       ID3D11DeviceContext ** ppImmediateContext,
                                       D3D_FEATURE_LEVEL * pChosenFeatureLevel,
                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(2, 2))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("D3D11On12CreateDevice reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(2, 2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDevice (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppImmediateContext (guest-allocated id, wrapper built on client thunk; not in reply) */
    if (npt_decode_simple_pointer(dec)) {
        if (pChosenFeatureLevel)
            npt_decode_D3D_FEATURE_LEVEL(dec, (D3D_FEATURE_LEVEL *)pChosenFeatureLevel);
    }
}

static inline void
npt_submit_D3D11On12CreateDevice(struct npt_ring *ring,
                                 uint32_t cmd_flags,
                                 IUnknown * pDevice,
                                 UINT Flags,
                                 const D3D_FEATURE_LEVEL * pFeatureLevels,
                                 UINT FeatureLevels,
                                 const IUnknown ** ppCommandQueues,
                                 UINT NumQueues,
                                 UINT NodeMask,
                                 ID3D11Device ** ppDevice,
                                 ID3D11DeviceContext ** ppImmediateContext,
                                 D3D_FEATURE_LEVEL * pChosenFeatureLevel,
                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_D3D11On12CreateDevice(pDevice,Flags,pFeatureLevels,FeatureLevels,ppCommandQueues,NumQueues,NodeMask,ppDevice,ppImmediateContext,pChosenFeatureLevel);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_D3D11On12CreateDevice_reply(pDevice,Flags,pFeatureLevels,FeatureLevels,ppCommandQueues,NumQueues,NodeMask,ppDevice,ppImmediateContext,pChosenFeatureLevel)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_D3D11On12CreateDevice(enc, cmd_flags, pDevice, Flags, pFeatureLevels, FeatureLevels, ppCommandQueues, NumQueues, NodeMask, ppDevice, ppImmediateContext, pChosenFeatureLevel);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_D3D11On12CreateDevice(struct npt_ring *ring,
                                IUnknown * pDevice,
                                UINT Flags,
                                const D3D_FEATURE_LEVEL * pFeatureLevels,
                                UINT FeatureLevels,
                                const IUnknown ** ppCommandQueues,
                                UINT NumQueues,
                                UINT NodeMask,
                                ID3D11Device ** ppDevice,
                                ID3D11DeviceContext ** ppImmediateContext,
                                D3D_FEATURE_LEVEL * pChosenFeatureLevel)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D11On12CreateDevice(ring, 0, pDevice, Flags, pFeatureLevels, FeatureLevels, ppCommandQueues, NumQueues, NodeMask, ppDevice, ppImmediateContext, pChosenFeatureLevel, &submit);
}

static inline HRESULT
npt_call_D3D11On12CreateDevice(struct npt_ring *ring,
                               IUnknown * pDevice,
                               UINT Flags,
                               const D3D_FEATURE_LEVEL * pFeatureLevels,
                               UINT FeatureLevels,
                               const IUnknown ** ppCommandQueues,
                               UINT NumQueues,
                               UINT NodeMask,
                               ID3D11Device ** ppDevice,
                               ID3D11DeviceContext ** ppImmediateContext,
                               D3D_FEATURE_LEVEL * pChosenFeatureLevel)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D11On12CreateDevice(ring, NPT_CMD_FLAG_REPLY, pDevice, Flags, pFeatureLevels, FeatureLevels, ppCommandQueues, NumQueues, NodeMask, ppDevice, ppImmediateContext, pChosenFeatureLevel, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_D3D11On12CreateDevice_reply(dec, ppDevice, ppImmediateContext, pChosenFeatureLevel, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * D3D12CreateDevice (group 3, id 0)
 */

struct npt_command_D3D12CreateDevice {
    IUnknown * pAdapter;
    D3D_FEATURE_LEVEL MinimumFeatureLevel;
    const IID * riid;
    void ** ppDevice;
    /* Shadow: guest-allocated id for ppDevice. */
    uint64_t _guest_id_ppDevice;
    HRESULT ret;
};

static inline size_t
npt_sizeof_D3D12CreateDevice(const IUnknown * pAdapter,const D3D_FEATURE_LEVEL MinimumFeatureLevel,const IID * riid,void ** ppDevice)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_D3D_FEATURE_LEVEL(&MinimumFeatureLevel, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppDevice: guest id */
    return cmd_size;
}

static inline void
npt_encode_D3D12CreateDevice(struct npt_cs_encoder *enc,
                             uint32_t cmd_flags,
                             IUnknown * pAdapter,
                             D3D_FEATURE_LEVEL MinimumFeatureLevel,
                             const IID * riid,
                             void ** ppDevice)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_D3D12CreateDevice(pAdapter,MinimumFeatureLevel,riid,ppDevice);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 0),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pAdapter));
    npt_encode_D3D_FEATURE_LEVEL(enc, &MinimumFeatureLevel);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppDevice = 0;
    if (ppDevice) {
        _gid_ppDevice = npt_com_allocate_next_id();
        *ppDevice = (void *)(uintptr_t)_gid_ppDevice;
    }
    npt_encode_uint64_t(enc, &_gid_ppDevice);
}
}

static inline size_t
npt_sizeof_D3D12CreateDevice_reply(const IUnknown * pAdapter,const D3D_FEATURE_LEVEL MinimumFeatureLevel,const IID * riid,void ** ppDevice)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pAdapter (input only) */
/* skip MinimumFeatureLevel (input only) */
/* skip riid (input only) */
/* skip ppDevice (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_D3D12CreateDevice_reply(struct npt_cs_decoder *dec,
                                   void ** ppDevice,
                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(3, 0))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("D3D12CreateDevice reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(3, 0));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppDevice (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_D3D12CreateDevice(struct npt_ring *ring,
                             uint32_t cmd_flags,
                             IUnknown * pAdapter,
                             D3D_FEATURE_LEVEL MinimumFeatureLevel,
                             const IID * riid,
                             void ** ppDevice,
                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_D3D12CreateDevice(pAdapter,MinimumFeatureLevel,riid,ppDevice);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_D3D12CreateDevice_reply(pAdapter,MinimumFeatureLevel,riid,ppDevice)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_D3D12CreateDevice(enc, cmd_flags, pAdapter, MinimumFeatureLevel, riid, ppDevice);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_D3D12CreateDevice(struct npt_ring *ring,
                            IUnknown * pAdapter,
                            D3D_FEATURE_LEVEL MinimumFeatureLevel,
                            const IID * riid,
                            void ** ppDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12CreateDevice(ring, 0, pAdapter, MinimumFeatureLevel, riid, ppDevice, &submit);
}

static inline HRESULT
npt_call_D3D12CreateDevice(struct npt_ring *ring,
                           IUnknown * pAdapter,
                           D3D_FEATURE_LEVEL MinimumFeatureLevel,
                           const IID * riid,
                           void ** ppDevice)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12CreateDevice(ring, NPT_CMD_FLAG_REPLY, pAdapter, MinimumFeatureLevel, riid, ppDevice, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_D3D12CreateDevice_reply(dec, ppDevice, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * D3D12CreateRootSignatureDeserializer (group 3, id 1)
 */

struct npt_command_D3D12CreateRootSignatureDeserializer {
    const VOID * pSrcData;
    SIZE_T SrcDataSizeInBytes;
    const IID * pRootSignatureDeserializerInterface;
    void ** ppRootSignatureDeserializer;
    HRESULT ret;
};

static inline size_t
npt_sizeof_D3D12CreateRootSignatureDeserializer(const VOID * pSrcData,const SIZE_T SrcDataSizeInBytes,const IID * pRootSignatureDeserializerInterface,void ** ppRootSignatureDeserializer)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    if (pSrcData) {
        cmd_size += npt_sizeof_array_count(SrcDataSizeInBytes);
        cmd_size += npt_sizeof_blob_array(pSrcData, SrcDataSizeInBytes);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&SrcDataSizeInBytes, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pRootSignatureDeserializerInterface);
    if (pRootSignatureDeserializerInterface)
        cmd_size += npt_sizeof_IID(pRootSignatureDeserializerInterface, max_mode);
/* skip ppRootSignatureDeserializer (output only) */
    return cmd_size;
}

static inline void
npt_encode_D3D12CreateRootSignatureDeserializer(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                const VOID * pSrcData,
                                                SIZE_T SrcDataSizeInBytes,
                                                const IID * pRootSignatureDeserializerInterface,
                                                void ** ppRootSignatureDeserializer)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_D3D12CreateRootSignatureDeserializer(pSrcData,SrcDataSizeInBytes,pRootSignatureDeserializerInterface,ppRootSignatureDeserializer);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 1),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pSrcData) {
        npt_encode_array_count(enc, SrcDataSizeInBytes);
        npt_encode_blob_array(enc, pSrcData, SrcDataSizeInBytes);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &SrcDataSizeInBytes);
    if (npt_encode_simple_pointer(enc, pRootSignatureDeserializerInterface))
        npt_encode_IID(enc, pRootSignatureDeserializerInterface);
/* skip ppRootSignatureDeserializer (output only) */
}

static inline size_t
npt_sizeof_D3D12CreateRootSignatureDeserializer_reply(const VOID * pSrcData,const SIZE_T SrcDataSizeInBytes,const IID * pRootSignatureDeserializerInterface,void ** ppRootSignatureDeserializer)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pSrcData (input only) */
/* skip SrcDataSizeInBytes (input only) */
/* skip pRootSignatureDeserializerInterface (input only) */
    /* ERROR: ppRootSignatureDeserializer (void) is not serializable */
    return 0;
    return cmd_size;
}

static inline void
npt_decode_D3D12CreateRootSignatureDeserializer_reply(struct npt_cs_decoder *dec,
                                                      void ** ppRootSignatureDeserializer,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(3, 1))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("D3D12CreateRootSignatureDeserializer reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(3, 1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    /* ERROR: ppRootSignatureDeserializer (void) is not serializable */
    return;
}

static inline void
npt_submit_D3D12CreateRootSignatureDeserializer(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                const VOID * pSrcData,
                                                SIZE_T SrcDataSizeInBytes,
                                                const IID * pRootSignatureDeserializerInterface,
                                                void ** ppRootSignatureDeserializer,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_D3D12CreateRootSignatureDeserializer(pSrcData,SrcDataSizeInBytes,pRootSignatureDeserializerInterface,ppRootSignatureDeserializer);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_D3D12CreateRootSignatureDeserializer_reply(pSrcData,SrcDataSizeInBytes,pRootSignatureDeserializerInterface,ppRootSignatureDeserializer)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_D3D12CreateRootSignatureDeserializer(enc, cmd_flags, pSrcData, SrcDataSizeInBytes, pRootSignatureDeserializerInterface, ppRootSignatureDeserializer);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_D3D12CreateRootSignatureDeserializer(struct npt_ring *ring,
                                               const VOID * pSrcData,
                                               SIZE_T SrcDataSizeInBytes,
                                               const IID * pRootSignatureDeserializerInterface,
                                               void ** ppRootSignatureDeserializer)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12CreateRootSignatureDeserializer(ring, 0, pSrcData, SrcDataSizeInBytes, pRootSignatureDeserializerInterface, ppRootSignatureDeserializer, &submit);
}

static inline HRESULT
npt_call_D3D12CreateRootSignatureDeserializer(struct npt_ring *ring,
                                              const VOID * pSrcData,
                                              SIZE_T SrcDataSizeInBytes,
                                              const IID * pRootSignatureDeserializerInterface,
                                              void ** ppRootSignatureDeserializer)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12CreateRootSignatureDeserializer(ring, NPT_CMD_FLAG_REPLY, pSrcData, SrcDataSizeInBytes, pRootSignatureDeserializerInterface, ppRootSignatureDeserializer, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_D3D12CreateRootSignatureDeserializer_reply(dec, ppRootSignatureDeserializer, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * D3D12CreateVersionedRootSignatureDeserializer (group 3, id 2)
 */

struct npt_command_D3D12CreateVersionedRootSignatureDeserializer {
    const VOID * pSrcData;
    SIZE_T SrcDataSizeInBytes;
    const IID * pRootSignatureDeserializerInterface;
    void ** ppRootSignatureDeserializer;
    HRESULT ret;
};

static inline size_t
npt_sizeof_D3D12CreateVersionedRootSignatureDeserializer(const VOID * pSrcData,const SIZE_T SrcDataSizeInBytes,const IID * pRootSignatureDeserializerInterface,void ** ppRootSignatureDeserializer)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    if (pSrcData) {
        cmd_size += npt_sizeof_array_count(SrcDataSizeInBytes);
        cmd_size += npt_sizeof_blob_array(pSrcData, SrcDataSizeInBytes);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&SrcDataSizeInBytes, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pRootSignatureDeserializerInterface);
    if (pRootSignatureDeserializerInterface)
        cmd_size += npt_sizeof_IID(pRootSignatureDeserializerInterface, max_mode);
/* skip ppRootSignatureDeserializer (output only) */
    return cmd_size;
}

static inline void
npt_encode_D3D12CreateVersionedRootSignatureDeserializer(struct npt_cs_encoder *enc,
                                                         uint32_t cmd_flags,
                                                         const VOID * pSrcData,
                                                         SIZE_T SrcDataSizeInBytes,
                                                         const IID * pRootSignatureDeserializerInterface,
                                                         void ** ppRootSignatureDeserializer)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_D3D12CreateVersionedRootSignatureDeserializer(pSrcData,SrcDataSizeInBytes,pRootSignatureDeserializerInterface,ppRootSignatureDeserializer);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 2),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pSrcData) {
        npt_encode_array_count(enc, SrcDataSizeInBytes);
        npt_encode_blob_array(enc, pSrcData, SrcDataSizeInBytes);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &SrcDataSizeInBytes);
    if (npt_encode_simple_pointer(enc, pRootSignatureDeserializerInterface))
        npt_encode_IID(enc, pRootSignatureDeserializerInterface);
/* skip ppRootSignatureDeserializer (output only) */
}

static inline size_t
npt_sizeof_D3D12CreateVersionedRootSignatureDeserializer_reply(const VOID * pSrcData,const SIZE_T SrcDataSizeInBytes,const IID * pRootSignatureDeserializerInterface,void ** ppRootSignatureDeserializer)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pSrcData (input only) */
/* skip SrcDataSizeInBytes (input only) */
/* skip pRootSignatureDeserializerInterface (input only) */
    /* ERROR: ppRootSignatureDeserializer (void) is not serializable */
    return 0;
    return cmd_size;
}

static inline void
npt_decode_D3D12CreateVersionedRootSignatureDeserializer_reply(struct npt_cs_decoder *dec,
                                                               void ** ppRootSignatureDeserializer,
                                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(3, 2))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("D3D12CreateVersionedRootSignatureDeserializer reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(3, 2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    /* ERROR: ppRootSignatureDeserializer (void) is not serializable */
    return;
}

static inline void
npt_submit_D3D12CreateVersionedRootSignatureDeserializer(struct npt_ring *ring,
                                                         uint32_t cmd_flags,
                                                         const VOID * pSrcData,
                                                         SIZE_T SrcDataSizeInBytes,
                                                         const IID * pRootSignatureDeserializerInterface,
                                                         void ** ppRootSignatureDeserializer,
                                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_D3D12CreateVersionedRootSignatureDeserializer(pSrcData,SrcDataSizeInBytes,pRootSignatureDeserializerInterface,ppRootSignatureDeserializer);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_D3D12CreateVersionedRootSignatureDeserializer_reply(pSrcData,SrcDataSizeInBytes,pRootSignatureDeserializerInterface,ppRootSignatureDeserializer)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_D3D12CreateVersionedRootSignatureDeserializer(enc, cmd_flags, pSrcData, SrcDataSizeInBytes, pRootSignatureDeserializerInterface, ppRootSignatureDeserializer);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_D3D12CreateVersionedRootSignatureDeserializer(struct npt_ring *ring,
                                                        const VOID * pSrcData,
                                                        SIZE_T SrcDataSizeInBytes,
                                                        const IID * pRootSignatureDeserializerInterface,
                                                        void ** ppRootSignatureDeserializer)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12CreateVersionedRootSignatureDeserializer(ring, 0, pSrcData, SrcDataSizeInBytes, pRootSignatureDeserializerInterface, ppRootSignatureDeserializer, &submit);
}

static inline HRESULT
npt_call_D3D12CreateVersionedRootSignatureDeserializer(struct npt_ring *ring,
                                                       const VOID * pSrcData,
                                                       SIZE_T SrcDataSizeInBytes,
                                                       const IID * pRootSignatureDeserializerInterface,
                                                       void ** ppRootSignatureDeserializer)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12CreateVersionedRootSignatureDeserializer(ring, NPT_CMD_FLAG_REPLY, pSrcData, SrcDataSizeInBytes, pRootSignatureDeserializerInterface, ppRootSignatureDeserializer, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_D3D12CreateVersionedRootSignatureDeserializer_reply(dec, ppRootSignatureDeserializer, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * D3D12SerializeRootSignature (group 3, id 3)
 */

struct npt_command_D3D12SerializeRootSignature {
    const D3D12_ROOT_SIGNATURE_DESC * pRootSignature;
    D3D_ROOT_SIGNATURE_VERSION Version;
    ID3DBlob ** ppBlob;
    ID3DBlob ** ppErrorBlob;
    /* Shadow: guest-allocated id for ppBlob. */
    uint64_t _guest_id_ppBlob;
    /* Shadow: guest-allocated id for ppErrorBlob. */
    uint64_t _guest_id_ppErrorBlob;
    HRESULT ret;
};

static inline size_t
npt_sizeof_D3D12SerializeRootSignature(const D3D12_ROOT_SIGNATURE_DESC * pRootSignature,const D3D_ROOT_SIGNATURE_VERSION Version,ID3DBlob ** ppBlob,ID3DBlob ** ppErrorBlob)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    cmd_size += npt_sizeof_simple_pointer(pRootSignature);
    if (pRootSignature)
        cmd_size += npt_sizeof_D3D12_ROOT_SIGNATURE_DESC(pRootSignature, max_mode);
    cmd_size += npt_sizeof_D3D_ROOT_SIGNATURE_VERSION(&Version, max_mode);
cmd_size += sizeof(uint64_t);  /* ppBlob: guest id */
cmd_size += sizeof(uint64_t);  /* ppErrorBlob: guest id */
    return cmd_size;
}

static inline void
npt_encode_D3D12SerializeRootSignature(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       const D3D12_ROOT_SIGNATURE_DESC * pRootSignature,
                                       D3D_ROOT_SIGNATURE_VERSION Version,
                                       ID3DBlob ** ppBlob,
                                       ID3DBlob ** ppErrorBlob)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_D3D12SerializeRootSignature(pRootSignature,Version,ppBlob,ppErrorBlob);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 3),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pRootSignature))
        npt_encode_D3D12_ROOT_SIGNATURE_DESC(enc, pRootSignature);
    npt_encode_D3D_ROOT_SIGNATURE_VERSION(enc, &Version);
{
    uint64_t _gid_ppBlob = 0;
    if (ppBlob) {
        _gid_ppBlob = npt_com_allocate_next_id();
        *ppBlob = (ID3DBlob *)(uintptr_t)_gid_ppBlob;
    }
    npt_encode_uint64_t(enc, &_gid_ppBlob);
}
{
    uint64_t _gid_ppErrorBlob = 0;
    if (ppErrorBlob) {
        _gid_ppErrorBlob = npt_com_allocate_next_id();
        *ppErrorBlob = (ID3DBlob *)(uintptr_t)_gid_ppErrorBlob;
    }
    npt_encode_uint64_t(enc, &_gid_ppErrorBlob);
}
}

static inline size_t
npt_sizeof_D3D12SerializeRootSignature_reply(const D3D12_ROOT_SIGNATURE_DESC * pRootSignature,const D3D_ROOT_SIGNATURE_VERSION Version,ID3DBlob ** ppBlob,ID3DBlob ** ppErrorBlob)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRootSignature (input only) */
/* skip Version (input only) */
/* skip ppBlob (guest-allocated id, registered host-side; not in reply) */
/* skip ppErrorBlob (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_D3D12SerializeRootSignature_reply(struct npt_cs_decoder *dec,
                                             ID3DBlob ** ppBlob,
                                             ID3DBlob ** ppErrorBlob,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(3, 3))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("D3D12SerializeRootSignature reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(3, 3));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppBlob (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppErrorBlob (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_D3D12SerializeRootSignature(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       const D3D12_ROOT_SIGNATURE_DESC * pRootSignature,
                                       D3D_ROOT_SIGNATURE_VERSION Version,
                                       ID3DBlob ** ppBlob,
                                       ID3DBlob ** ppErrorBlob,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_D3D12SerializeRootSignature(pRootSignature,Version,ppBlob,ppErrorBlob);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_D3D12SerializeRootSignature_reply(pRootSignature,Version,ppBlob,ppErrorBlob)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_D3D12SerializeRootSignature(enc, cmd_flags, pRootSignature, Version, ppBlob, ppErrorBlob);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_D3D12SerializeRootSignature(struct npt_ring *ring,
                                      const D3D12_ROOT_SIGNATURE_DESC * pRootSignature,
                                      D3D_ROOT_SIGNATURE_VERSION Version,
                                      ID3DBlob ** ppBlob,
                                      ID3DBlob ** ppErrorBlob)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12SerializeRootSignature(ring, 0, pRootSignature, Version, ppBlob, ppErrorBlob, &submit);
}

static inline HRESULT
npt_call_D3D12SerializeRootSignature(struct npt_ring *ring,
                                     const D3D12_ROOT_SIGNATURE_DESC * pRootSignature,
                                     D3D_ROOT_SIGNATURE_VERSION Version,
                                     ID3DBlob ** ppBlob,
                                     ID3DBlob ** ppErrorBlob)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12SerializeRootSignature(ring, NPT_CMD_FLAG_REPLY, pRootSignature, Version, ppBlob, ppErrorBlob, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_D3D12SerializeRootSignature_reply(dec, ppBlob, ppErrorBlob, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * D3D12SerializeVersionedRootSignature (group 3, id 4)
 */

struct npt_command_D3D12SerializeVersionedRootSignature {
    const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pRootSignature;
    ID3DBlob ** ppBlob;
    ID3DBlob ** ppErrorBlob;
    /* Shadow: guest-allocated id for ppBlob. */
    uint64_t _guest_id_ppBlob;
    /* Shadow: guest-allocated id for ppErrorBlob. */
    uint64_t _guest_id_ppErrorBlob;
    HRESULT ret;
};

static inline size_t
npt_sizeof_D3D12SerializeVersionedRootSignature(const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pRootSignature,ID3DBlob ** ppBlob,ID3DBlob ** ppErrorBlob)
{
    const int max_mode = 0;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_command_header); /* header only, no UUID */
    cmd_size += npt_sizeof_simple_pointer(pRootSignature);
    if (pRootSignature)
        cmd_size += npt_sizeof_D3D12_VERSIONED_ROOT_SIGNATURE_DESC(pRootSignature, max_mode);
cmd_size += sizeof(uint64_t);  /* ppBlob: guest id */
cmd_size += sizeof(uint64_t);  /* ppErrorBlob: guest id */
    return cmd_size;
}

static inline void
npt_encode_D3D12SerializeVersionedRootSignature(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pRootSignature,
                                                ID3DBlob ** ppBlob,
                                                ID3DBlob ** ppErrorBlob)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_D3D12SerializeVersionedRootSignature(pRootSignature,ppBlob,ppErrorBlob);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 4),
        .cmd_flags = cmd_flags,
        .object_id = 0,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pRootSignature))
        npt_encode_D3D12_VERSIONED_ROOT_SIGNATURE_DESC(enc, pRootSignature);
{
    uint64_t _gid_ppBlob = 0;
    if (ppBlob) {
        _gid_ppBlob = npt_com_allocate_next_id();
        *ppBlob = (ID3DBlob *)(uintptr_t)_gid_ppBlob;
    }
    npt_encode_uint64_t(enc, &_gid_ppBlob);
}
{
    uint64_t _gid_ppErrorBlob = 0;
    if (ppErrorBlob) {
        _gid_ppErrorBlob = npt_com_allocate_next_id();
        *ppErrorBlob = (ID3DBlob *)(uintptr_t)_gid_ppErrorBlob;
    }
    npt_encode_uint64_t(enc, &_gid_ppErrorBlob);
}
}

static inline size_t
npt_sizeof_D3D12SerializeVersionedRootSignature_reply(const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pRootSignature,ID3DBlob ** ppBlob,ID3DBlob ** ppErrorBlob)
{
    const int max_mode = 1;
    (void)max_mode;
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pRootSignature (input only) */
/* skip ppBlob (guest-allocated id, registered host-side; not in reply) */
/* skip ppErrorBlob (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_D3D12SerializeVersionedRootSignature_reply(struct npt_cs_decoder *dec,
                                                      ID3DBlob ** ppBlob,
                                                      ID3DBlob ** ppErrorBlob,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE_TOPLEVEL(3, 4))) {
        /* Host didn't write a reply.  See per-interface equivalent
         * handler above for the reasoning. */
        npt_log("D3D12SerializeVersionedRootSignature reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE_TOPLEVEL(3, 4));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppBlob (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppErrorBlob (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_D3D12SerializeVersionedRootSignature(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pRootSignature,
                                                ID3DBlob ** ppBlob,
                                                ID3DBlob ** ppErrorBlob,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_D3D12SerializeVersionedRootSignature(pRootSignature,ppBlob,ppErrorBlob);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_D3D12SerializeVersionedRootSignature_reply(pRootSignature,ppBlob,ppErrorBlob)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_D3D12SerializeVersionedRootSignature(enc, cmd_flags, pRootSignature, ppBlob, ppErrorBlob);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_D3D12SerializeVersionedRootSignature(struct npt_ring *ring,
                                               const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pRootSignature,
                                               ID3DBlob ** ppBlob,
                                               ID3DBlob ** ppErrorBlob)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12SerializeVersionedRootSignature(ring, 0, pRootSignature, ppBlob, ppErrorBlob, &submit);
}

static inline HRESULT
npt_call_D3D12SerializeVersionedRootSignature(struct npt_ring *ring,
                                              const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pRootSignature,
                                              ID3DBlob ** ppBlob,
                                              ID3DBlob ** ppErrorBlob)
{
    struct npt_ring_submit_command submit;
    npt_submit_D3D12SerializeVersionedRootSignature(ring, NPT_CMD_FLAG_REPLY, pRootSignature, ppBlob, ppErrorBlob, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_D3D12SerializeVersionedRootSignature_reply(dec, ppBlob, ppErrorBlob, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_TOPLEVEL_H */
