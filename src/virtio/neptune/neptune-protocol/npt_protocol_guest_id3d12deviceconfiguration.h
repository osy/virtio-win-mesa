/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12deviceconfiguration.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12DEVICECONFIGURATION_H
#define NPT_PROTOCOL_GUEST_ID3D12DEVICECONFIGURATION_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12DeviceConfiguration::GetDesc
 */

struct npt_command_ID3D12DeviceConfiguration_GetDesc {
    void *_self;
    D3D12_DEVICE_CONFIGURATION_DESC ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration_GetDesc(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceConfiguration_GetDesc(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceConfiguration_GetDesc();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                 NPT_METHOD_ID3D12DeviceConfiguration_GetDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration_GetDesc_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_DEVICE_CONFIGURATION_DESC(&(const D3D12_DEVICE_CONFIGURATION_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceConfiguration_GetDesc_reply(struct npt_cs_decoder *dec,
                                                   D3D12_DEVICE_CONFIGURATION_DESC *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                                  NPT_METHOD_ID3D12DeviceConfiguration_GetDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceConfiguration_GetDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                       NPT_METHOD_ID3D12DeviceConfiguration_GetDesc));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_DEVICE_CONFIGURATION_DESC(dec, ret);
}

static inline void
npt_submit_ID3D12DeviceConfiguration_GetDesc(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceConfiguration_GetDesc();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceConfiguration_GetDesc_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceConfiguration_GetDesc(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceConfiguration_GetDesc(struct npt_ring *ring,
                                            npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration_GetDesc(ring, 0, object_id, &submit);
}

static inline D3D12_DEVICE_CONFIGURATION_DESC
npt_call_ID3D12DeviceConfiguration_GetDesc(struct npt_ring *ring,
                                           npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration_GetDesc(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_DEVICE_CONFIGURATION_DESC _ret;
        npt_decode_ID3D12DeviceConfiguration_GetDesc_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_DEVICE_CONFIGURATION_DESC){0};
    }
}

/*
 * ID3D12DeviceConfiguration::GetEnabledExperimentalFeatures
 */

struct npt_command_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures {
    void *_self;
    GUID * pGuids;
    UINT NumGuids;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(const GUID * pGuids,const UINT NumGuids)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pGuids (output only) */
    cmd_size += npt_sizeof_UINT(&NumGuids, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(struct npt_cs_encoder *enc,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    GUID * pGuids,
                                                                    UINT NumGuids)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(pGuids,NumGuids);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                 NPT_METHOD_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pGuids (output only) */
    npt_encode_UINT(enc, &NumGuids);
}

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures_reply(const GUID * pGuids,const UINT NumGuids)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_array_count(pGuids ? NumGuids : 0);
    if (pGuids)
        cmd_size += (size_t)(NumGuids) * npt_sizeof_GUID(&(const GUID){0}, 1);
/* skip NumGuids (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures_reply(struct npt_cs_decoder *dec,
                                                                          GUID * pGuids,
                                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                                  NPT_METHOD_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                       NPT_METHOD_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count && pGuids) {
            for (uint32_t _i = 0; _i < (uint32_t)_count; _i++)
                npt_decode_GUID(dec, (GUID *)&pGuids[_i]);
        } else if (_count) {
            npt_cs_decoder_set_fatal(dec); /* data sent but no buffer */
        }
    }
}

static inline void
npt_submit_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(struct npt_ring *ring,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    GUID * pGuids,
                                                                    UINT NumGuids,
                                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(pGuids,NumGuids);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures_reply(pGuids,NumGuids)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(enc, cmd_flags, object_id, pGuids, NumGuids);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(struct npt_ring *ring,
                                                                   npt_object_id object_id,
                                                                   GUID * pGuids,
                                                                   UINT NumGuids)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(ring, 0, object_id, pGuids, NumGuids, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  GUID * pGuids,
                                                                  UINT NumGuids)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures(ring, NPT_CMD_FLAG_REPLY, object_id, pGuids, NumGuids, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceConfiguration_GetEnabledExperimentalFeatures_reply(dec, pGuids, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceConfiguration::SerializeVersionedRootSignature
 */

struct npt_command_ID3D12DeviceConfiguration_SerializeVersionedRootSignature {
    void *_self;
    const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pDesc;
    ID3DBlob ** ppResult;
    ID3DBlob ** ppError;
    /* Shadow: the guest-allocated id for ppResult arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResult -> *ppResult} in the
     * context object table. */
    uint64_t _guest_id_ppResult;
    /* Shadow: the guest-allocated id for ppError arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppError -> *ppError} in the
     * context object table. */
    uint64_t _guest_id_ppError;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pDesc,ID3DBlob ** ppResult,ID3DBlob ** ppError)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_D3D12_VERSIONED_ROOT_SIGNATURE_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppResult: guest id */
cmd_size += sizeof(uint64_t);  /* ppError: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(struct npt_cs_encoder *enc,
                                                                     uint32_t cmd_flags,
                                                                     npt_object_id object_id,
                                                                     const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pDesc,
                                                                     ID3DBlob ** ppResult,
                                                                     ID3DBlob ** ppError)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(pDesc,ppResult,ppError);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                 NPT_METHOD_ID3D12DeviceConfiguration_SerializeVersionedRootSignature),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_D3D12_VERSIONED_ROOT_SIGNATURE_DESC(enc, pDesc);
{
    uint64_t _gid_ppResult = 0;
    if (ppResult) {
        _gid_ppResult = npt_com_allocate_next_id();
        *ppResult = (ID3DBlob *)(uintptr_t)_gid_ppResult;
    }
    npt_encode_uint64_t(enc, &_gid_ppResult);
}
{
    uint64_t _gid_ppError = 0;
    if (ppError) {
        _gid_ppError = npt_com_allocate_next_id();
        *ppError = (ID3DBlob *)(uintptr_t)_gid_ppError;
    }
    npt_encode_uint64_t(enc, &_gid_ppError);
}
}

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration_SerializeVersionedRootSignature_reply(const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pDesc,ID3DBlob ** ppResult,ID3DBlob ** ppError)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDesc (input only) */
/* skip ppResult (guest-allocated id, registered host-side; not in reply) */
/* skip ppError (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceConfiguration_SerializeVersionedRootSignature_reply(struct npt_cs_decoder *dec,
                                                                           ID3DBlob ** ppResult,
                                                                           ID3DBlob ** ppError,
                                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                                  NPT_METHOD_ID3D12DeviceConfiguration_SerializeVersionedRootSignature))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceConfiguration_SerializeVersionedRootSignature reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                       NPT_METHOD_ID3D12DeviceConfiguration_SerializeVersionedRootSignature));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppResult (guest-allocated id, wrapper built on client thunk; not in reply) */
/* skip ppError (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(struct npt_ring *ring,
                                                                     uint32_t cmd_flags,
                                                                     npt_object_id object_id,
                                                                     const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pDesc,
                                                                     ID3DBlob ** ppResult,
                                                                     ID3DBlob ** ppError,
                                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(pDesc,ppResult,ppError);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceConfiguration_SerializeVersionedRootSignature_reply(pDesc,ppResult,ppError)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(enc, cmd_flags, object_id, pDesc, ppResult, ppError);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pDesc,
                                                                    ID3DBlob ** ppResult,
                                                                    ID3DBlob ** ppError)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(ring, 0, object_id, pDesc, ppResult, ppError, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(struct npt_ring *ring,
                                                                   npt_object_id object_id,
                                                                   const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pDesc,
                                                                   ID3DBlob ** ppResult,
                                                                   ID3DBlob ** ppError)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration_SerializeVersionedRootSignature(ring, NPT_CMD_FLAG_REPLY, object_id, pDesc, ppResult, ppError, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceConfiguration_SerializeVersionedRootSignature_reply(dec, ppResult, ppError, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceConfiguration::CreateVersionedRootSignatureDeserializer
 */

struct npt_command_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer {
    void *_self;
    const void * pBlob;
    SIZE_T Size;
    const IID * riid;
    void ** ppvDeserializer;
    /* Shadow: the guest-allocated id for ppvDeserializer arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvDeserializer -> *ppvDeserializer} in the
     * context object table. */
    uint64_t _guest_id_ppvDeserializer;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(const void * pBlob,const SIZE_T Size,const IID * riid,void ** ppvDeserializer)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pBlob) {
        cmd_size += npt_sizeof_array_count(Size);
        cmd_size += npt_sizeof_blob_array(pBlob, Size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&Size, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvDeserializer: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(struct npt_cs_encoder *enc,
                                                                              uint32_t cmd_flags,
                                                                              npt_object_id object_id,
                                                                              const void * pBlob,
                                                                              SIZE_T Size,
                                                                              const IID * riid,
                                                                              void ** ppvDeserializer)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(pBlob,Size,riid,ppvDeserializer);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                 NPT_METHOD_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pBlob) {
        npt_encode_array_count(enc, Size);
        npt_encode_blob_array(enc, pBlob, Size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &Size);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvDeserializer = 0;
    if (ppvDeserializer) {
        _gid_ppvDeserializer = npt_com_allocate_next_id();
        *ppvDeserializer = (void *)(uintptr_t)_gid_ppvDeserializer;
    }
    npt_encode_uint64_t(enc, &_gid_ppvDeserializer);
}
}

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer_reply(const void * pBlob,const SIZE_T Size,const IID * riid,void ** ppvDeserializer)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pBlob (input only) */
/* skip Size (input only) */
/* skip riid (input only) */
/* skip ppvDeserializer (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer_reply(struct npt_cs_decoder *dec,
                                                                                    void ** ppvDeserializer,
                                                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                                  NPT_METHOD_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration,
                                       NPT_METHOD_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvDeserializer (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(struct npt_ring *ring,
                                                                              uint32_t cmd_flags,
                                                                              npt_object_id object_id,
                                                                              const void * pBlob,
                                                                              SIZE_T Size,
                                                                              const IID * riid,
                                                                              void ** ppvDeserializer,
                                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(pBlob,Size,riid,ppvDeserializer);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer_reply(pBlob,Size,riid,ppvDeserializer)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(enc, cmd_flags, object_id, pBlob, Size, riid, ppvDeserializer);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(struct npt_ring *ring,
                                                                             npt_object_id object_id,
                                                                             const void * pBlob,
                                                                             SIZE_T Size,
                                                                             const IID * riid,
                                                                             void ** ppvDeserializer)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(ring, 0, object_id, pBlob, Size, riid, ppvDeserializer, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(struct npt_ring *ring,
                                                                            npt_object_id object_id,
                                                                            const void * pBlob,
                                                                            SIZE_T Size,
                                                                            const IID * riid,
                                                                            void ** ppvDeserializer)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer(ring, NPT_CMD_FLAG_REPLY, object_id, pBlob, Size, riid, ppvDeserializer, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceConfiguration_CreateVersionedRootSignatureDeserializer_reply(dec, ppvDeserializer, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceConfiguration1::CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary
 */

struct npt_command_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary {
    void *_self;
    const void * pLibraryBlob;
    SIZE_T Size;
    const WCHAR * RootSignatureSubobjectName;
    const IID * riid;
    void ** ppvDeserializer;
    /* Shadow: the guest-allocated id for ppvDeserializer arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvDeserializer -> *ppvDeserializer} in the
     * context object table. */
    uint64_t _guest_id_ppvDeserializer;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(const void * pLibraryBlob,const SIZE_T Size,const WCHAR * RootSignatureSubobjectName,const IID * riid,void ** ppvDeserializer)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    if (pLibraryBlob) {
        cmd_size += npt_sizeof_array_count(Size);
        cmd_size += npt_sizeof_blob_array(pLibraryBlob, Size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_SIZE_T(&Size, max_mode);
    if (RootSignatureSubobjectName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)RootSignatureSubobjectName) + 1) * sizeof(WCHAR);
        cmd_size += npt_sizeof_array_count(_wstr_size);
        cmd_size += npt_sizeof_blob_array(RootSignatureSubobjectName, _wstr_size);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvDeserializer: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(struct npt_cs_encoder *enc,
                                                                                                     uint32_t cmd_flags,
                                                                                                     npt_object_id object_id,
                                                                                                     const void * pLibraryBlob,
                                                                                                     SIZE_T Size,
                                                                                                     const WCHAR * RootSignatureSubobjectName,
                                                                                                     const IID * riid,
                                                                                                     void ** ppvDeserializer)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(pLibraryBlob,Size,RootSignatureSubobjectName,riid,ppvDeserializer);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration1,
                                 NPT_METHOD_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (pLibraryBlob) {
        npt_encode_array_count(enc, Size);
        npt_encode_blob_array(enc, pLibraryBlob, Size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_SIZE_T(enc, &Size);
    if (RootSignatureSubobjectName) {
        const size_t _wstr_size = (npt_wcslen((const WCHAR *)RootSignatureSubobjectName) + 1) * sizeof(WCHAR);
        npt_encode_array_count(enc, _wstr_size);
        npt_encode_blob_array(enc, RootSignatureSubobjectName, _wstr_size);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvDeserializer = 0;
    if (ppvDeserializer) {
        _gid_ppvDeserializer = npt_com_allocate_next_id();
        *ppvDeserializer = (void *)(uintptr_t)_gid_ppvDeserializer;
    }
    npt_encode_uint64_t(enc, &_gid_ppvDeserializer);
}
}

static inline size_t
npt_sizeof_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary_reply(const void * pLibraryBlob,const SIZE_T Size,const WCHAR * RootSignatureSubobjectName,const IID * riid,void ** ppvDeserializer)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pLibraryBlob (input only) */
/* skip Size (input only) */
/* skip RootSignatureSubobjectName (input only) */
/* skip riid (input only) */
/* skip ppvDeserializer (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary_reply(struct npt_cs_decoder *dec,
                                                                                                           void ** ppvDeserializer,
                                                                                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration1,
                                                  NPT_METHOD_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceConfiguration1,
                                       NPT_METHOD_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvDeserializer (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(struct npt_ring *ring,
                                                                                                     uint32_t cmd_flags,
                                                                                                     npt_object_id object_id,
                                                                                                     const void * pLibraryBlob,
                                                                                                     SIZE_T Size,
                                                                                                     const WCHAR * RootSignatureSubobjectName,
                                                                                                     const IID * riid,
                                                                                                     void ** ppvDeserializer,
                                                                                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(pLibraryBlob,Size,RootSignatureSubobjectName,riid,ppvDeserializer);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary_reply(pLibraryBlob,Size,RootSignatureSubobjectName,riid,ppvDeserializer)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(enc, cmd_flags, object_id, pLibraryBlob, Size, RootSignatureSubobjectName, riid, ppvDeserializer);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(struct npt_ring *ring,
                                                                                                    npt_object_id object_id,
                                                                                                    const void * pLibraryBlob,
                                                                                                    SIZE_T Size,
                                                                                                    const WCHAR * RootSignatureSubobjectName,
                                                                                                    const IID * riid,
                                                                                                    void ** ppvDeserializer)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(ring, 0, object_id, pLibraryBlob, Size, RootSignatureSubobjectName, riid, ppvDeserializer, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(struct npt_ring *ring,
                                                                                                   npt_object_id object_id,
                                                                                                   const void * pLibraryBlob,
                                                                                                   SIZE_T Size,
                                                                                                   const WCHAR * RootSignatureSubobjectName,
                                                                                                   const IID * riid,
                                                                                                   void ** ppvDeserializer)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary(ring, NPT_CMD_FLAG_REPLY, object_id, pLibraryBlob, Size, RootSignatureSubobjectName, riid, ppvDeserializer, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceConfiguration1_CreateVersionedRootSignatureDeserializerFromSubobjectInLibrary_reply(dec, ppvDeserializer, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12DEVICECONFIGURATION_H */
