/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3ddestructionnotifier.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3DDESTRUCTIONNOTIFIER_H
#define NPT_PROTOCOL_GUEST_ID3DDESTRUCTIONNOTIFIER_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3DDestructionNotifier::RegisterDestructionCallback
 */

struct npt_command_ID3DDestructionNotifier_RegisterDestructionCallback {
    void *_self;
    PFN_DESTRUCTION_CALLBACK callbackFn;
    void * pData;
    UINT * pCallbackID;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3DDestructionNotifier_RegisterDestructionCallback(const PFN_DESTRUCTION_CALLBACK callbackFn,const void * pData,const UINT * pCallbackID)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    /* ERROR: callbackFn (PFN_DESTRUCTION_CALLBACK) is not serializable */
    return 0;
    /* ERROR: pData (void) is not serializable */
    return 0;
/* skip pCallbackID (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3DDestructionNotifier_RegisterDestructionCallback(struct npt_cs_encoder *enc,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               PFN_DESTRUCTION_CALLBACK callbackFn,
                                                               void * pData,
                                                               UINT * pCallbackID)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3DDestructionNotifier_RegisterDestructionCallback(callbackFn,pData,pCallbackID);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3DDestructionNotifier,
                                 NPT_METHOD_ID3DDestructionNotifier_RegisterDestructionCallback),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    /* ERROR: callbackFn (PFN_DESTRUCTION_CALLBACK) is not serializable */
    return;
    /* ERROR: pData (void) is not serializable */
    return;
/* skip pCallbackID (output only) */
}

static inline size_t
npt_sizeof_ID3DDestructionNotifier_RegisterDestructionCallback_reply(const PFN_DESTRUCTION_CALLBACK callbackFn,const void * pData,const UINT * pCallbackID)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip callbackFn (input only) */
/* skip pData (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3DDestructionNotifier_RegisterDestructionCallback_reply(struct npt_cs_decoder *dec,
                                                                     UINT * pCallbackID,
                                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3DDestructionNotifier,
                                                  NPT_METHOD_ID3DDestructionNotifier_RegisterDestructionCallback))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3DDestructionNotifier_RegisterDestructionCallback reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3DDestructionNotifier,
                                       NPT_METHOD_ID3DDestructionNotifier_RegisterDestructionCallback));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pCallbackID)
            npt_decode_UINT(dec, (UINT *)pCallbackID);
    }
}

static inline void
npt_submit_ID3DDestructionNotifier_RegisterDestructionCallback(struct npt_ring *ring,
                                                               uint32_t cmd_flags,
                                                               npt_object_id object_id,
                                                               PFN_DESTRUCTION_CALLBACK callbackFn,
                                                               void * pData,
                                                               UINT * pCallbackID,
                                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3DDestructionNotifier_RegisterDestructionCallback(callbackFn,pData,pCallbackID);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3DDestructionNotifier_RegisterDestructionCallback_reply(callbackFn,pData,pCallbackID)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3DDestructionNotifier_RegisterDestructionCallback(enc, cmd_flags, object_id, callbackFn, pData, pCallbackID);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3DDestructionNotifier_RegisterDestructionCallback(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              PFN_DESTRUCTION_CALLBACK callbackFn,
                                                              void * pData,
                                                              UINT * pCallbackID)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3DDestructionNotifier_RegisterDestructionCallback(ring, 0, object_id, callbackFn, pData, pCallbackID, &submit);
}

static inline HRESULT
npt_call_ID3DDestructionNotifier_RegisterDestructionCallback(struct npt_ring *ring,
                                                             npt_object_id object_id,
                                                             PFN_DESTRUCTION_CALLBACK callbackFn,
                                                             void * pData,
                                                             UINT * pCallbackID)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3DDestructionNotifier_RegisterDestructionCallback(ring, NPT_CMD_FLAG_REPLY, object_id, callbackFn, pData, pCallbackID, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3DDestructionNotifier_RegisterDestructionCallback_reply(dec, pCallbackID, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3DDestructionNotifier::UnregisterDestructionCallback
 */

struct npt_command_ID3DDestructionNotifier_UnregisterDestructionCallback {
    void *_self;
    UINT callbackID;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3DDestructionNotifier_UnregisterDestructionCallback(const UINT callbackID)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&callbackID, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3DDestructionNotifier_UnregisterDestructionCallback(struct npt_cs_encoder *enc,
                                                                 uint32_t cmd_flags,
                                                                 npt_object_id object_id,
                                                                 UINT callbackID)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3DDestructionNotifier_UnregisterDestructionCallback(callbackID);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3DDestructionNotifier,
                                 NPT_METHOD_ID3DDestructionNotifier_UnregisterDestructionCallback),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &callbackID);
}

static inline size_t
npt_sizeof_ID3DDestructionNotifier_UnregisterDestructionCallback_reply(const UINT callbackID)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip callbackID (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3DDestructionNotifier_UnregisterDestructionCallback_reply(struct npt_cs_decoder *dec,
                                                                       HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3DDestructionNotifier,
                                                  NPT_METHOD_ID3DDestructionNotifier_UnregisterDestructionCallback))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3DDestructionNotifier_UnregisterDestructionCallback reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3DDestructionNotifier,
                                       NPT_METHOD_ID3DDestructionNotifier_UnregisterDestructionCallback));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3DDestructionNotifier_UnregisterDestructionCallback(struct npt_ring *ring,
                                                                 uint32_t cmd_flags,
                                                                 npt_object_id object_id,
                                                                 UINT callbackID,
                                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3DDestructionNotifier_UnregisterDestructionCallback(callbackID);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3DDestructionNotifier_UnregisterDestructionCallback_reply(callbackID)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3DDestructionNotifier_UnregisterDestructionCallback(enc, cmd_flags, object_id, callbackID);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3DDestructionNotifier_UnregisterDestructionCallback(struct npt_ring *ring,
                                                                npt_object_id object_id,
                                                                UINT callbackID)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3DDestructionNotifier_UnregisterDestructionCallback(ring, 0, object_id, callbackID, &submit);
}

static inline HRESULT
npt_call_ID3DDestructionNotifier_UnregisterDestructionCallback(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               UINT callbackID)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3DDestructionNotifier_UnregisterDestructionCallback(ring, NPT_CMD_FLAG_REPLY, object_id, callbackID, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3DDestructionNotifier_UnregisterDestructionCallback_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3DDESTRUCTIONNOTIFIER_H */
