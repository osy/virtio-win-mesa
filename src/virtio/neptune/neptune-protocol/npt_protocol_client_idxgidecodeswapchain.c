/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for idxgidecodeswapchain: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_idxgidecodeswapchain.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* IDXGIDecodeSwapChain                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_idxgidecodeswapchain_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_IDXGIDecodeSwapChain,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_idxgidecodeswapchain_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Pre-allocate the guest id and speculatively build the wrapper
     * BEFORE the host round-trip.  On E_NOINTERFACE the wrapper is
     * never used (the host rejected the QI, so its id is unregistered)
     * and we must Release the speculative wrapper to keep the refcount
     * straight.  The host round-trip carries just HRESULT. */
    uint64_t _guest_id = npt_com_allocate_next_id();
    /* Pass `self` as parent so the new wrapper inherits its instance_ring
     * (DC/SC shared ring pinning).  QI typically returns the same host
     * pointer as `self`, so the cache hit short-circuits before any new
     * wrapper is created -- but on the cache-miss path the new wrapper
     * must inherit the same ring or generated methods on the QI'd
     * interface would scatter onto a different host dispatch thread
     * than every other method on the same underlying object. */
    void *wrapper = npt_com_get_or_wrap(
        npt_com_self_device(self), riid, _guest_id,
        (struct npt_com_base *)self);
    if (!wrapper) {
        if (ppvObject) *ppvObject = NULL;
        return NPT_E_OUTOFMEMORY;
    }
    HRESULT host_hr = npt_com_send_query_interface(
        npt_com_self_device(self),
        npt_com_self_id(self),
        riid, _guest_id);
    if (host_hr == NPT_S_OK) {
        if (ppvObject)
            *ppvObject = wrapper;
        return NPT_S_OK;
    }
    /* QI failed host-side: the guest_id stays unregistered.  Release the
     * speculative wrapper so the guest-side refcount drains cleanly.
     * (npt_com_default_release still issues COM_RELEASE to the host,
     * which is a no-op there because the id was never registered.) */
    struct npt_com_base *com = wrapper;
    typedef ULONG (NPT_STDMETHODCALLTYPE *release_fn_t)(void *);
    ((release_fn_t)com->lpVtbl[2])(com);
    if (ppvObject) *ppvObject = NULL;
    return host_hr;
}

ULONG NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_PresentBuffer(void *self, UINT BufferToPresent, UINT SyncInterval, UINT Flags)
{
    npt_async_IDXGIDecodeSwapChain_PresentBuffer(npt_com_self_ring(self),npt_com_self_id(self),BufferToPresent,SyncInterval,Flags);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_SetSourceRect(void *self, const RECT * pRect)
{
    npt_async_IDXGIDecodeSwapChain_SetSourceRect(npt_com_self_ring(self),npt_com_self_id(self),pRect);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_SetTargetRect(void *self, const RECT * pRect)
{
    npt_async_IDXGIDecodeSwapChain_SetTargetRect(npt_com_self_ring(self),npt_com_self_id(self),pRect);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_SetDestSize(void *self, UINT Width, UINT Height)
{
    npt_async_IDXGIDecodeSwapChain_SetDestSize(npt_com_self_ring(self),npt_com_self_id(self),Width,Height);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_GetSourceRect(void *self, RECT * pRect)
{
    HRESULT _ret = npt_call_IDXGIDecodeSwapChain_GetSourceRect(npt_com_self_ring(self),npt_com_self_id(self),pRect);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_GetTargetRect(void *self, RECT * pRect)
{
    HRESULT _ret = npt_call_IDXGIDecodeSwapChain_GetTargetRect(npt_com_self_ring(self),npt_com_self_id(self),pRect);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_GetDestSize(void *self, UINT * pWidth, UINT * pHeight)
{
    HRESULT _ret = npt_call_IDXGIDecodeSwapChain_GetDestSize(npt_com_self_ring(self),npt_com_self_id(self),pWidth,pHeight);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_SetColorSpace(void *self, DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ColorSpace)
{
    npt_async_IDXGIDecodeSwapChain_SetColorSpace(npt_com_self_ring(self),npt_com_self_id(self),ColorSpace);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS NPT_STDMETHODCALLTYPE
npt_idxgidecodeswapchain_default_GetColorSpace(void *self)
{
    DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS _ret = npt_call_IDXGIDecodeSwapChain_GetColorSpace(npt_com_self_ring(self),npt_com_self_id(self));
    return _ret;
}


void
npt_idxgidecodeswapchain_fill_default_vtbl(struct npt_idxgidecodeswapchain_client_vtbl *dst)
{
    dst->QueryInterface = npt_idxgidecodeswapchain_default_QueryInterface;
    dst->AddRef         = npt_idxgidecodeswapchain_default_AddRef;
    dst->Release        = npt_idxgidecodeswapchain_default_Release;
    dst->PresentBuffer = npt_idxgidecodeswapchain_default_PresentBuffer;
    dst->SetSourceRect = npt_idxgidecodeswapchain_default_SetSourceRect;
    dst->SetTargetRect = npt_idxgidecodeswapchain_default_SetTargetRect;
    dst->SetDestSize = npt_idxgidecodeswapchain_default_SetDestSize;
    dst->GetSourceRect = npt_idxgidecodeswapchain_default_GetSourceRect;
    dst->GetTargetRect = npt_idxgidecodeswapchain_default_GetTargetRect;
    dst->GetDestSize = npt_idxgidecodeswapchain_default_GetDestSize;
    dst->SetColorSpace = npt_idxgidecodeswapchain_default_SetColorSpace;
    dst->GetColorSpace = npt_idxgidecodeswapchain_default_GetColorSpace;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_idxgidecodeswapchain_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_idxgidecodeswapchain_client_vtbl npt_idxgidecodeswapchain_default_vtbl_storage;

void
npt_idxgidecodeswapchain_install_default_vtbl(void)
{
    npt_idxgidecodeswapchain_fill_default_vtbl(&npt_idxgidecodeswapchain_default_vtbl_storage);
}

void *
npt_idxgidecodeswapchain_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_idxgidecodeswapchain_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
