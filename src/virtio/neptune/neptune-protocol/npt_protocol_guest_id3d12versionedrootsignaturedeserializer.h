/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12versionedrootsignaturedeserializer.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12VERSIONEDROOTSIGNATUREDESERIALIZER_H
#define NPT_PROTOCOL_GUEST_ID3D12VERSIONEDROOTSIGNATUREDESERIALIZER_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12VersionedRootSignatureDeserializer::GetRootSignatureDescAtVersion
 */

struct npt_command_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion {
    void *_self;
    D3D_ROOT_SIGNATURE_VERSION convertToVersion;
    const D3D12_VERSIONED_ROOT_SIGNATURE_DESC ** ppDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(const D3D_ROOT_SIGNATURE_VERSION convertToVersion,const D3D12_VERSIONED_ROOT_SIGNATURE_DESC ** ppDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D_ROOT_SIGNATURE_VERSION(&convertToVersion, max_mode);
/* skip ppDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(struct npt_cs_encoder *enc,
                                                                                  uint32_t cmd_flags,
                                                                                  npt_object_id object_id,
                                                                                  D3D_ROOT_SIGNATURE_VERSION convertToVersion,
                                                                                  const D3D12_VERSIONED_ROOT_SIGNATURE_DESC ** ppDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(convertToVersion,ppDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VersionedRootSignatureDeserializer,
                                 NPT_METHOD_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D_ROOT_SIGNATURE_VERSION(enc, &convertToVersion);
/* skip ppDesc (output only) */
}

static inline size_t
npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion_reply(const D3D_ROOT_SIGNATURE_VERSION convertToVersion,const D3D12_VERSIONED_ROOT_SIGNATURE_DESC ** ppDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion_reply(struct npt_cs_decoder *dec,
                                                                                        const D3D12_VERSIONED_ROOT_SIGNATURE_DESC ** ppDesc,
                                                                                        HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VersionedRootSignatureDeserializer,
                                                  NPT_METHOD_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VersionedRootSignatureDeserializer,
                                       NPT_METHOD_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    /* ERROR: ppDesc is unsized and not optional */
    npt_cs_decoder_set_fatal(dec);
    return;
}

static inline void
npt_submit_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(struct npt_ring *ring,
                                                                                  uint32_t cmd_flags,
                                                                                  npt_object_id object_id,
                                                                                  D3D_ROOT_SIGNATURE_VERSION convertToVersion,
                                                                                  const D3D12_VERSIONED_ROOT_SIGNATURE_DESC ** ppDesc,
                                                                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(convertToVersion,ppDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion_reply(convertToVersion,ppDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(enc, cmd_flags, object_id, convertToVersion, ppDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(struct npt_ring *ring,
                                                                                 npt_object_id object_id,
                                                                                 D3D_ROOT_SIGNATURE_VERSION convertToVersion,
                                                                                 const D3D12_VERSIONED_ROOT_SIGNATURE_DESC ** ppDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(ring, 0, object_id, convertToVersion, ppDesc, &submit);
}

static inline HRESULT
npt_call_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(struct npt_ring *ring,
                                                                                npt_object_id object_id,
                                                                                D3D_ROOT_SIGNATURE_VERSION convertToVersion,
                                                                                const D3D12_VERSIONED_ROOT_SIGNATURE_DESC ** ppDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion(ring, NPT_CMD_FLAG_REPLY, object_id, convertToVersion, ppDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12VersionedRootSignatureDeserializer_GetRootSignatureDescAtVersion_reply(dec, ppDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12VersionedRootSignatureDeserializer::GetUnconvertedRootSignatureDesc
 */

struct npt_command_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc {
    void *_self;
    D3D12_VERSIONED_ROOT_SIGNATURE_DESC ret;
};

static inline size_t
npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc(struct npt_cs_encoder *enc,
                                                                                    uint32_t cmd_flags,
                                                                                    npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VersionedRootSignatureDeserializer,
                                 NPT_METHOD_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc_reply(struct npt_cs_decoder *dec,
                                                                                          D3D12_VERSIONED_ROOT_SIGNATURE_DESC *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VersionedRootSignatureDeserializer,
                                                  NPT_METHOD_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12VersionedRootSignatureDeserializer,
                                       NPT_METHOD_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_VERSIONED_ROOT_SIGNATURE_DESC(dec, ret);
}

static inline void
npt_submit_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc(struct npt_ring *ring,
                                                                                    uint32_t cmd_flags,
                                                                                    npt_object_id object_id,
                                                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc(struct npt_ring *ring,
                                                                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc(ring, 0, object_id, &submit);
}

static inline D3D12_VERSIONED_ROOT_SIGNATURE_DESC
npt_call_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc(struct npt_ring *ring,
                                                                                  npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_VERSIONED_ROOT_SIGNATURE_DESC _ret;
        npt_decode_ID3D12VersionedRootSignatureDeserializer_GetUnconvertedRootSignatureDesc_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_VERSIONED_ROOT_SIGNATURE_DESC){0};
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12VERSIONEDROOTSIGNATUREDESERIALIZER_H */
