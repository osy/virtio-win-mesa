/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgifactory.
 */

#ifndef NPT_PROTOCOL_GUEST_IDXGIFACTORY_H
#define NPT_PROTOCOL_GUEST_IDXGIFACTORY_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIFactory::EnumAdapters
 */

struct npt_command_IDXGIFactory_EnumAdapters {
    void *_self;
    UINT Adapter;
    IDXGIAdapter ** ppAdapter;
    /* Shadow: the guest-allocated id for ppAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppAdapter -> *ppAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppAdapter;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory_EnumAdapters(const UINT Adapter,IDXGIAdapter ** ppAdapter)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Adapter, max_mode);
cmd_size += sizeof(uint64_t);  /* ppAdapter: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory_EnumAdapters(struct npt_cs_encoder *enc,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     UINT Adapter,
                                     IDXGIAdapter ** ppAdapter)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory_EnumAdapters(Adapter,ppAdapter);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_EnumAdapters),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Adapter);
{
    uint64_t _gid_ppAdapter = 0;
    if (ppAdapter) {
        _gid_ppAdapter = npt_com_allocate_next_id();
        *ppAdapter = (IDXGIAdapter *)(uintptr_t)_gid_ppAdapter;
    }
    npt_encode_uint64_t(enc, &_gid_ppAdapter);
}
}

static inline size_t
npt_sizeof_IDXGIFactory_EnumAdapters_reply(const UINT Adapter,IDXGIAdapter ** ppAdapter)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Adapter (input only) */
/* skip ppAdapter (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory_EnumAdapters_reply(struct npt_cs_decoder *dec,
                                           IDXGIAdapter ** ppAdapter,
                                           HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                                  NPT_METHOD_IDXGIFactory_EnumAdapters))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory_EnumAdapters reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                       NPT_METHOD_IDXGIFactory_EnumAdapters));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppAdapter (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory_EnumAdapters(struct npt_ring *ring,
                                     uint32_t cmd_flags,
                                     npt_object_id object_id,
                                     UINT Adapter,
                                     IDXGIAdapter ** ppAdapter,
                                     struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory_EnumAdapters(Adapter,ppAdapter);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory_EnumAdapters_reply(Adapter,ppAdapter)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory_EnumAdapters(enc, cmd_flags, object_id, Adapter, ppAdapter);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory_EnumAdapters(struct npt_ring *ring,
                                    npt_object_id object_id,
                                    UINT Adapter,
                                    IDXGIAdapter ** ppAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_EnumAdapters(ring, 0, object_id, Adapter, ppAdapter, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory_EnumAdapters(struct npt_ring *ring,
                                   npt_object_id object_id,
                                   UINT Adapter,
                                   IDXGIAdapter ** ppAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_EnumAdapters(ring, NPT_CMD_FLAG_REPLY, object_id, Adapter, ppAdapter, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory_EnumAdapters_reply(dec, ppAdapter, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory::MakeWindowAssociation
 */

struct npt_command_IDXGIFactory_MakeWindowAssociation {
    void *_self;
    HWND WindowHandle;
    UINT Flags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory_MakeWindowAssociation(const HWND WindowHandle,const UINT Flags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&Flags, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory_MakeWindowAssociation(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              HWND WindowHandle,
                                              UINT Flags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory_MakeWindowAssociation(WindowHandle,Flags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_MakeWindowAssociation),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)WindowHandle));
    npt_encode_UINT(enc, &Flags);
}

static inline size_t
npt_sizeof_IDXGIFactory_MakeWindowAssociation_reply(const HWND WindowHandle,const UINT Flags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip WindowHandle (input only) */
/* skip Flags (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory_MakeWindowAssociation_reply(struct npt_cs_decoder *dec,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                                  NPT_METHOD_IDXGIFactory_MakeWindowAssociation))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory_MakeWindowAssociation reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                       NPT_METHOD_IDXGIFactory_MakeWindowAssociation));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIFactory_MakeWindowAssociation(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              HWND WindowHandle,
                                              UINT Flags,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory_MakeWindowAssociation(WindowHandle,Flags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory_MakeWindowAssociation_reply(WindowHandle,Flags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory_MakeWindowAssociation(enc, cmd_flags, object_id, WindowHandle, Flags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory_MakeWindowAssociation(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             HWND WindowHandle,
                                             UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_MakeWindowAssociation(ring, 0, object_id, WindowHandle, Flags, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory_MakeWindowAssociation(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            HWND WindowHandle,
                                            UINT Flags)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_MakeWindowAssociation(ring, NPT_CMD_FLAG_REPLY, object_id, WindowHandle, Flags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory_MakeWindowAssociation_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory::GetWindowAssociation
 */

struct npt_command_IDXGIFactory_GetWindowAssociation {
    void *_self;
    HWND * pWindowHandle;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory_GetWindowAssociation(const HWND * pWindowHandle)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pWindowHandle (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory_GetWindowAssociation(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             HWND * pWindowHandle)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory_GetWindowAssociation(pWindowHandle);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_GetWindowAssociation),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pWindowHandle (output only) */
}

static inline size_t
npt_sizeof_IDXGIFactory_GetWindowAssociation_reply(const HWND * pWindowHandle)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += sizeof(uint64_t);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory_GetWindowAssociation_reply(struct npt_cs_decoder *dec,
                                                   HWND * pWindowHandle,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                                  NPT_METHOD_IDXGIFactory_GetWindowAssociation))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory_GetWindowAssociation reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                       NPT_METHOD_IDXGIFactory_GetWindowAssociation));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        if (pWindowHandle)
            *pWindowHandle = (HWND)(uintptr_t)npt_win32_handle_from_id(_id);
    }
}

static inline void
npt_submit_IDXGIFactory_GetWindowAssociation(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             HWND * pWindowHandle,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory_GetWindowAssociation(pWindowHandle);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory_GetWindowAssociation_reply(pWindowHandle)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory_GetWindowAssociation(enc, cmd_flags, object_id, pWindowHandle);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory_GetWindowAssociation(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            HWND * pWindowHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_GetWindowAssociation(ring, 0, object_id, pWindowHandle, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory_GetWindowAssociation(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           HWND * pWindowHandle)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_GetWindowAssociation(ring, NPT_CMD_FLAG_REPLY, object_id, pWindowHandle, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory_GetWindowAssociation_reply(dec, pWindowHandle, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory::CreateSwapChain
 */

struct npt_command_IDXGIFactory_CreateSwapChain {
    void *_self;
    IUnknown * pDevice;
    DXGI_SWAP_CHAIN_DESC * pDesc;
    IDXGISwapChain ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory_CreateSwapChain(const IUnknown * pDevice,const DXGI_SWAP_CHAIN_DESC * pDesc,IDXGISwapChain ** ppSwapChain)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_DESC(pDesc, max_mode);
cmd_size += sizeof(uint64_t);  /* ppSwapChain: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory_CreateSwapChain(struct npt_cs_encoder *enc,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        IUnknown * pDevice,
                                        DXGI_SWAP_CHAIN_DESC * pDesc,
                                        IDXGISwapChain ** ppSwapChain)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory_CreateSwapChain(pDevice,pDesc,ppSwapChain);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_CreateSwapChain),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_DXGI_SWAP_CHAIN_DESC(enc, pDesc);
{
    uint64_t _gid_ppSwapChain = 0;
    if (ppSwapChain) {
        _gid_ppSwapChain = npt_com_allocate_next_id();
        *ppSwapChain = (IDXGISwapChain *)(uintptr_t)_gid_ppSwapChain;
    }
    npt_encode_uint64_t(enc, &_gid_ppSwapChain);
}
}

static inline size_t
npt_sizeof_IDXGIFactory_CreateSwapChain_reply(const IUnknown * pDevice,const DXGI_SWAP_CHAIN_DESC * pDesc,IDXGISwapChain ** ppSwapChain)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip pDesc (input only) */
/* skip ppSwapChain (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory_CreateSwapChain_reply(struct npt_cs_decoder *dec,
                                              IDXGISwapChain ** ppSwapChain,
                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                                  NPT_METHOD_IDXGIFactory_CreateSwapChain))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory_CreateSwapChain reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                       NPT_METHOD_IDXGIFactory_CreateSwapChain));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSwapChain (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory_CreateSwapChain(struct npt_ring *ring,
                                        uint32_t cmd_flags,
                                        npt_object_id object_id,
                                        IUnknown * pDevice,
                                        DXGI_SWAP_CHAIN_DESC * pDesc,
                                        IDXGISwapChain ** ppSwapChain,
                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory_CreateSwapChain(pDevice,pDesc,ppSwapChain);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory_CreateSwapChain_reply(pDevice,pDesc,ppSwapChain)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory_CreateSwapChain(enc, cmd_flags, object_id, pDevice, pDesc, ppSwapChain);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory_CreateSwapChain(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       IUnknown * pDevice,
                                       DXGI_SWAP_CHAIN_DESC * pDesc,
                                       IDXGISwapChain ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_CreateSwapChain(ring, 0, object_id, pDevice, pDesc, ppSwapChain, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory_CreateSwapChain(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      IUnknown * pDevice,
                                      DXGI_SWAP_CHAIN_DESC * pDesc,
                                      IDXGISwapChain ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_CreateSwapChain(ring, NPT_CMD_FLAG_REPLY, object_id, pDevice, pDesc, ppSwapChain, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory_CreateSwapChain_reply(dec, ppSwapChain, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory::CreateSoftwareAdapter
 */

struct npt_command_IDXGIFactory_CreateSoftwareAdapter {
    void *_self;
    HMODULE Module;
    IDXGIAdapter ** ppAdapter;
    /* Shadow: the guest-allocated id for ppAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppAdapter -> *ppAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppAdapter;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory_CreateSoftwareAdapter(const HMODULE Module,IDXGIAdapter ** ppAdapter)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppAdapter: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory_CreateSoftwareAdapter(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              HMODULE Module,
                                              IDXGIAdapter ** ppAdapter)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory_CreateSoftwareAdapter(Module,ppAdapter);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_CreateSoftwareAdapter),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)Module));
{
    uint64_t _gid_ppAdapter = 0;
    if (ppAdapter) {
        _gid_ppAdapter = npt_com_allocate_next_id();
        *ppAdapter = (IDXGIAdapter *)(uintptr_t)_gid_ppAdapter;
    }
    npt_encode_uint64_t(enc, &_gid_ppAdapter);
}
}

static inline size_t
npt_sizeof_IDXGIFactory_CreateSoftwareAdapter_reply(const HMODULE Module,IDXGIAdapter ** ppAdapter)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Module (input only) */
/* skip ppAdapter (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory_CreateSoftwareAdapter_reply(struct npt_cs_decoder *dec,
                                                    IDXGIAdapter ** ppAdapter,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                                  NPT_METHOD_IDXGIFactory_CreateSoftwareAdapter))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory_CreateSoftwareAdapter reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                       NPT_METHOD_IDXGIFactory_CreateSoftwareAdapter));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppAdapter (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory_CreateSoftwareAdapter(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              HMODULE Module,
                                              IDXGIAdapter ** ppAdapter,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory_CreateSoftwareAdapter(Module,ppAdapter);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory_CreateSoftwareAdapter_reply(Module,ppAdapter)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory_CreateSoftwareAdapter(enc, cmd_flags, object_id, Module, ppAdapter);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory_CreateSoftwareAdapter(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             HMODULE Module,
                                             IDXGIAdapter ** ppAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_CreateSoftwareAdapter(ring, 0, object_id, Module, ppAdapter, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory_CreateSoftwareAdapter(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            HMODULE Module,
                                            IDXGIAdapter ** ppAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory_CreateSoftwareAdapter(ring, NPT_CMD_FLAG_REPLY, object_id, Module, ppAdapter, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory_CreateSoftwareAdapter_reply(dec, ppAdapter, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory1::EnumAdapters1
 */

struct npt_command_IDXGIFactory1_EnumAdapters1 {
    void *_self;
    UINT Adapter;
    IDXGIAdapter1 ** ppAdapter;
    /* Shadow: the guest-allocated id for ppAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppAdapter -> *ppAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppAdapter;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory1_EnumAdapters1(const UINT Adapter,IDXGIAdapter1 ** ppAdapter)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Adapter, max_mode);
cmd_size += sizeof(uint64_t);  /* ppAdapter: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory1_EnumAdapters1(struct npt_cs_encoder *enc,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       UINT Adapter,
                                       IDXGIAdapter1 ** ppAdapter)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory1_EnumAdapters1(Adapter,ppAdapter);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory1,
                                 NPT_METHOD_IDXGIFactory1_EnumAdapters1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Adapter);
{
    uint64_t _gid_ppAdapter = 0;
    if (ppAdapter) {
        _gid_ppAdapter = npt_com_allocate_next_id();
        *ppAdapter = (IDXGIAdapter1 *)(uintptr_t)_gid_ppAdapter;
    }
    npt_encode_uint64_t(enc, &_gid_ppAdapter);
}
}

static inline size_t
npt_sizeof_IDXGIFactory1_EnumAdapters1_reply(const UINT Adapter,IDXGIAdapter1 ** ppAdapter)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Adapter (input only) */
/* skip ppAdapter (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory1_EnumAdapters1_reply(struct npt_cs_decoder *dec,
                                             IDXGIAdapter1 ** ppAdapter,
                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory1,
                                                  NPT_METHOD_IDXGIFactory1_EnumAdapters1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory1_EnumAdapters1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory1,
                                       NPT_METHOD_IDXGIFactory1_EnumAdapters1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppAdapter (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory1_EnumAdapters1(struct npt_ring *ring,
                                       uint32_t cmd_flags,
                                       npt_object_id object_id,
                                       UINT Adapter,
                                       IDXGIAdapter1 ** ppAdapter,
                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory1_EnumAdapters1(Adapter,ppAdapter);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory1_EnumAdapters1_reply(Adapter,ppAdapter)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory1_EnumAdapters1(enc, cmd_flags, object_id, Adapter, ppAdapter);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory1_EnumAdapters1(struct npt_ring *ring,
                                      npt_object_id object_id,
                                      UINT Adapter,
                                      IDXGIAdapter1 ** ppAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory1_EnumAdapters1(ring, 0, object_id, Adapter, ppAdapter, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory1_EnumAdapters1(struct npt_ring *ring,
                                     npt_object_id object_id,
                                     UINT Adapter,
                                     IDXGIAdapter1 ** ppAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory1_EnumAdapters1(ring, NPT_CMD_FLAG_REPLY, object_id, Adapter, ppAdapter, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory1_EnumAdapters1_reply(dec, ppAdapter, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory1::IsCurrent
 */

struct npt_command_IDXGIFactory1_IsCurrent {
    void *_self;
    BOOL ret;
};

static inline size_t
npt_sizeof_IDXGIFactory1_IsCurrent(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory1_IsCurrent(struct npt_cs_encoder *enc,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory1_IsCurrent();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory1,
                                 NPT_METHOD_IDXGIFactory1_IsCurrent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIFactory1_IsCurrent_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory1_IsCurrent_reply(struct npt_cs_decoder *dec,
                                         BOOL *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory1,
                                                  NPT_METHOD_IDXGIFactory1_IsCurrent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory1_IsCurrent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory1,
                                       NPT_METHOD_IDXGIFactory1_IsCurrent));
        if (ret) *ret = (BOOL)0;
        return;
    }
    if (ret) *ret = (BOOL)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIFactory1_IsCurrent(struct npt_ring *ring,
                                   uint32_t cmd_flags,
                                   npt_object_id object_id,
                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory1_IsCurrent();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory1_IsCurrent_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory1_IsCurrent(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory1_IsCurrent(struct npt_ring *ring,
                                  npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory1_IsCurrent(ring, 0, object_id, &submit);
}

static inline BOOL
npt_call_IDXGIFactory1_IsCurrent(struct npt_ring *ring,
                                 npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory1_IsCurrent(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        BOOL _ret;
        npt_decode_IDXGIFactory1_IsCurrent_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (BOOL)0;
    }
}

/*
 * IDXGIFactory2::IsWindowedStereoEnabled
 */

struct npt_command_IDXGIFactory2_IsWindowedStereoEnabled {
    void *_self;
    BOOL ret;
};

static inline size_t
npt_sizeof_IDXGIFactory2_IsWindowedStereoEnabled(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_IsWindowedStereoEnabled(struct npt_cs_encoder *enc,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_IsWindowedStereoEnabled();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_IsWindowedStereoEnabled),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIFactory2_IsWindowedStereoEnabled_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory2_IsWindowedStereoEnabled_reply(struct npt_cs_decoder *dec,
                                                       BOOL *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                                  NPT_METHOD_IDXGIFactory2_IsWindowedStereoEnabled))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory2_IsWindowedStereoEnabled reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                       NPT_METHOD_IDXGIFactory2_IsWindowedStereoEnabled));
        if (ret) *ret = (BOOL)0;
        return;
    }
    if (ret) *ret = (BOOL)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIFactory2_IsWindowedStereoEnabled(struct npt_ring *ring,
                                                 uint32_t cmd_flags,
                                                 npt_object_id object_id,
                                                 struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_IsWindowedStereoEnabled();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_IsWindowedStereoEnabled_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_IsWindowedStereoEnabled(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_IsWindowedStereoEnabled(struct npt_ring *ring,
                                                npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_IsWindowedStereoEnabled(ring, 0, object_id, &submit);
}

static inline BOOL
npt_call_IDXGIFactory2_IsWindowedStereoEnabled(struct npt_ring *ring,
                                               npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_IsWindowedStereoEnabled(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        BOOL _ret;
        npt_decode_IDXGIFactory2_IsWindowedStereoEnabled_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (BOOL)0;
    }
}

/*
 * IDXGIFactory2::CreateSwapChainForHwnd
 */

struct npt_command_IDXGIFactory2_CreateSwapChainForHwnd {
    void *_self;
    IUnknown * pDevice;
    HWND hWnd;
    const DXGI_SWAP_CHAIN_DESC1 * pDesc;
    const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pFullscreenDesc;
    IDXGIOutput * pRestrictToOutput;
    IDXGISwapChain1 ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory2_CreateSwapChainForHwnd(const IUnknown * pDevice,const HWND hWnd,const DXGI_SWAP_CHAIN_DESC1 * pDesc,const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pFullscreenDesc,const IDXGIOutput * pRestrictToOutput,IDXGISwapChain1 ** ppSwapChain)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_DESC1(pDesc, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pFullscreenDesc);
    if (pFullscreenDesc)
        cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_FULLSCREEN_DESC(pFullscreenDesc, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppSwapChain: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_CreateSwapChainForHwnd(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                IUnknown * pDevice,
                                                HWND hWnd,
                                                const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pFullscreenDesc,
                                                IDXGIOutput * pRestrictToOutput,
                                                IDXGISwapChain1 ** ppSwapChain)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_CreateSwapChainForHwnd(pDevice,hWnd,pDesc,pFullscreenDesc,pRestrictToOutput,ppSwapChain);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_CreateSwapChainForHwnd),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hWnd));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_DXGI_SWAP_CHAIN_DESC1(enc, pDesc);
    if (npt_encode_simple_pointer(enc, pFullscreenDesc))
        npt_encode_DXGI_SWAP_CHAIN_FULLSCREEN_DESC(enc, pFullscreenDesc);
    npt_encode_com_handle(enc, npt_object_get_id(pRestrictToOutput));
{
    uint64_t _gid_ppSwapChain = 0;
    if (ppSwapChain) {
        _gid_ppSwapChain = npt_com_allocate_next_id();
        *ppSwapChain = (IDXGISwapChain1 *)(uintptr_t)_gid_ppSwapChain;
    }
    npt_encode_uint64_t(enc, &_gid_ppSwapChain);
}
}

static inline size_t
npt_sizeof_IDXGIFactory2_CreateSwapChainForHwnd_reply(const IUnknown * pDevice,const HWND hWnd,const DXGI_SWAP_CHAIN_DESC1 * pDesc,const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pFullscreenDesc,const IDXGIOutput * pRestrictToOutput,IDXGISwapChain1 ** ppSwapChain)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip hWnd (input only) */
/* skip pDesc (input only) */
/* skip pFullscreenDesc (input only) */
/* skip pRestrictToOutput (input only) */
/* skip ppSwapChain (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory2_CreateSwapChainForHwnd_reply(struct npt_cs_decoder *dec,
                                                      IDXGISwapChain1 ** ppSwapChain,
                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                                  NPT_METHOD_IDXGIFactory2_CreateSwapChainForHwnd))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory2_CreateSwapChainForHwnd reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                       NPT_METHOD_IDXGIFactory2_CreateSwapChainForHwnd));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSwapChain (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory2_CreateSwapChainForHwnd(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                IUnknown * pDevice,
                                                HWND hWnd,
                                                const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pFullscreenDesc,
                                                IDXGIOutput * pRestrictToOutput,
                                                IDXGISwapChain1 ** ppSwapChain,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_CreateSwapChainForHwnd(pDevice,hWnd,pDesc,pFullscreenDesc,pRestrictToOutput,ppSwapChain);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_CreateSwapChainForHwnd_reply(pDevice,hWnd,pDesc,pFullscreenDesc,pRestrictToOutput,ppSwapChain)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_CreateSwapChainForHwnd(enc, cmd_flags, object_id, pDevice, hWnd, pDesc, pFullscreenDesc, pRestrictToOutput, ppSwapChain);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_CreateSwapChainForHwnd(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               IUnknown * pDevice,
                                               HWND hWnd,
                                               const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                               const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pFullscreenDesc,
                                               IDXGIOutput * pRestrictToOutput,
                                               IDXGISwapChain1 ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_CreateSwapChainForHwnd(ring, 0, object_id, pDevice, hWnd, pDesc, pFullscreenDesc, pRestrictToOutput, ppSwapChain, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory2_CreateSwapChainForHwnd(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              IUnknown * pDevice,
                                              HWND hWnd,
                                              const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                              const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pFullscreenDesc,
                                              IDXGIOutput * pRestrictToOutput,
                                              IDXGISwapChain1 ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_CreateSwapChainForHwnd(ring, NPT_CMD_FLAG_REPLY, object_id, pDevice, hWnd, pDesc, pFullscreenDesc, pRestrictToOutput, ppSwapChain, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory2_CreateSwapChainForHwnd_reply(dec, ppSwapChain, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory2::CreateSwapChainForCoreWindow
 */

struct npt_command_IDXGIFactory2_CreateSwapChainForCoreWindow {
    void *_self;
    IUnknown * pDevice;
    IUnknown * pWindow;
    const DXGI_SWAP_CHAIN_DESC1 * pDesc;
    IDXGIOutput * pRestrictToOutput;
    IDXGISwapChain1 ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory2_CreateSwapChainForCoreWindow(const IUnknown * pDevice,const IUnknown * pWindow,const DXGI_SWAP_CHAIN_DESC1 * pDesc,const IDXGIOutput * pRestrictToOutput,IDXGISwapChain1 ** ppSwapChain)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_DESC1(pDesc, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppSwapChain: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_CreateSwapChainForCoreWindow(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      IUnknown * pDevice,
                                                      IUnknown * pWindow,
                                                      const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                      IDXGIOutput * pRestrictToOutput,
                                                      IDXGISwapChain1 ** ppSwapChain)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_CreateSwapChainForCoreWindow(pDevice,pWindow,pDesc,pRestrictToOutput,ppSwapChain);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_CreateSwapChainForCoreWindow),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
    npt_encode_com_handle(enc, npt_object_get_id(pWindow));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_DXGI_SWAP_CHAIN_DESC1(enc, pDesc);
    npt_encode_com_handle(enc, npt_object_get_id(pRestrictToOutput));
{
    uint64_t _gid_ppSwapChain = 0;
    if (ppSwapChain) {
        _gid_ppSwapChain = npt_com_allocate_next_id();
        *ppSwapChain = (IDXGISwapChain1 *)(uintptr_t)_gid_ppSwapChain;
    }
    npt_encode_uint64_t(enc, &_gid_ppSwapChain);
}
}

static inline size_t
npt_sizeof_IDXGIFactory2_CreateSwapChainForCoreWindow_reply(const IUnknown * pDevice,const IUnknown * pWindow,const DXGI_SWAP_CHAIN_DESC1 * pDesc,const IDXGIOutput * pRestrictToOutput,IDXGISwapChain1 ** ppSwapChain)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip pWindow (input only) */
/* skip pDesc (input only) */
/* skip pRestrictToOutput (input only) */
/* skip ppSwapChain (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory2_CreateSwapChainForCoreWindow_reply(struct npt_cs_decoder *dec,
                                                            IDXGISwapChain1 ** ppSwapChain,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                                  NPT_METHOD_IDXGIFactory2_CreateSwapChainForCoreWindow))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory2_CreateSwapChainForCoreWindow reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                       NPT_METHOD_IDXGIFactory2_CreateSwapChainForCoreWindow));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSwapChain (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory2_CreateSwapChainForCoreWindow(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      IUnknown * pDevice,
                                                      IUnknown * pWindow,
                                                      const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                      IDXGIOutput * pRestrictToOutput,
                                                      IDXGISwapChain1 ** ppSwapChain,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_CreateSwapChainForCoreWindow(pDevice,pWindow,pDesc,pRestrictToOutput,ppSwapChain);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_CreateSwapChainForCoreWindow_reply(pDevice,pWindow,pDesc,pRestrictToOutput,ppSwapChain)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_CreateSwapChainForCoreWindow(enc, cmd_flags, object_id, pDevice, pWindow, pDesc, pRestrictToOutput, ppSwapChain);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_CreateSwapChainForCoreWindow(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     IUnknown * pDevice,
                                                     IUnknown * pWindow,
                                                     const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                     IDXGIOutput * pRestrictToOutput,
                                                     IDXGISwapChain1 ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_CreateSwapChainForCoreWindow(ring, 0, object_id, pDevice, pWindow, pDesc, pRestrictToOutput, ppSwapChain, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory2_CreateSwapChainForCoreWindow(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    IUnknown * pDevice,
                                                    IUnknown * pWindow,
                                                    const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                    IDXGIOutput * pRestrictToOutput,
                                                    IDXGISwapChain1 ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_CreateSwapChainForCoreWindow(ring, NPT_CMD_FLAG_REPLY, object_id, pDevice, pWindow, pDesc, pRestrictToOutput, ppSwapChain, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory2_CreateSwapChainForCoreWindow_reply(dec, ppSwapChain, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory2::GetSharedResourceAdapterLuid
 */

struct npt_command_IDXGIFactory2_GetSharedResourceAdapterLuid {
    void *_self;
    HANDLE hResource;
    LUID * pLuid;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory2_GetSharedResourceAdapterLuid(const HANDLE hResource,const LUID * pLuid)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pLuid (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_GetSharedResourceAdapterLuid(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      HANDLE hResource,
                                                      LUID * pLuid)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_GetSharedResourceAdapterLuid(hResource,pLuid);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_GetSharedResourceAdapterLuid),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hResource));
/* skip pLuid (output only) */
}

static inline size_t
npt_sizeof_IDXGIFactory2_GetSharedResourceAdapterLuid_reply(const HANDLE hResource,const LUID * pLuid)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hResource (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_LUID(&(const LUID){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory2_GetSharedResourceAdapterLuid_reply(struct npt_cs_decoder *dec,
                                                            LUID * pLuid,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                                  NPT_METHOD_IDXGIFactory2_GetSharedResourceAdapterLuid))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory2_GetSharedResourceAdapterLuid reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                       NPT_METHOD_IDXGIFactory2_GetSharedResourceAdapterLuid));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pLuid)
            npt_decode_LUID(dec, (LUID *)pLuid);
    }
}

static inline void
npt_submit_IDXGIFactory2_GetSharedResourceAdapterLuid(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      HANDLE hResource,
                                                      LUID * pLuid,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_GetSharedResourceAdapterLuid(hResource,pLuid);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_GetSharedResourceAdapterLuid_reply(hResource,pLuid)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_GetSharedResourceAdapterLuid(enc, cmd_flags, object_id, hResource, pLuid);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_GetSharedResourceAdapterLuid(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     HANDLE hResource,
                                                     LUID * pLuid)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_GetSharedResourceAdapterLuid(ring, 0, object_id, hResource, pLuid, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory2_GetSharedResourceAdapterLuid(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    HANDLE hResource,
                                                    LUID * pLuid)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_GetSharedResourceAdapterLuid(ring, NPT_CMD_FLAG_REPLY, object_id, hResource, pLuid, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory2_GetSharedResourceAdapterLuid_reply(dec, pLuid, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory2::RegisterStereoStatusWindow
 */

struct npt_command_IDXGIFactory2_RegisterStereoStatusWindow {
    void *_self;
    HWND WindowHandle;
    UINT wMsg;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory2_RegisterStereoStatusWindow(const HWND WindowHandle,const UINT wMsg,const DWORD * pdwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&wMsg, max_mode);
/* skip pdwCookie (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_RegisterStereoStatusWindow(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    HWND WindowHandle,
                                                    UINT wMsg,
                                                    DWORD * pdwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_RegisterStereoStatusWindow(WindowHandle,wMsg,pdwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_RegisterStereoStatusWindow),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)WindowHandle));
    npt_encode_UINT(enc, &wMsg);
/* skip pdwCookie (output only) */
}

static inline size_t
npt_sizeof_IDXGIFactory2_RegisterStereoStatusWindow_reply(const HWND WindowHandle,const UINT wMsg,const DWORD * pdwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip WindowHandle (input only) */
/* skip wMsg (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DWORD(&(const DWORD){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory2_RegisterStereoStatusWindow_reply(struct npt_cs_decoder *dec,
                                                          DWORD * pdwCookie,
                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                                  NPT_METHOD_IDXGIFactory2_RegisterStereoStatusWindow))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory2_RegisterStereoStatusWindow reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                       NPT_METHOD_IDXGIFactory2_RegisterStereoStatusWindow));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pdwCookie)
            npt_decode_DWORD(dec, (DWORD *)pdwCookie);
    }
}

static inline void
npt_submit_IDXGIFactory2_RegisterStereoStatusWindow(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    HWND WindowHandle,
                                                    UINT wMsg,
                                                    DWORD * pdwCookie,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_RegisterStereoStatusWindow(WindowHandle,wMsg,pdwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_RegisterStereoStatusWindow_reply(WindowHandle,wMsg,pdwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_RegisterStereoStatusWindow(enc, cmd_flags, object_id, WindowHandle, wMsg, pdwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_RegisterStereoStatusWindow(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   HWND WindowHandle,
                                                   UINT wMsg,
                                                   DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_RegisterStereoStatusWindow(ring, 0, object_id, WindowHandle, wMsg, pdwCookie, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory2_RegisterStereoStatusWindow(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  HWND WindowHandle,
                                                  UINT wMsg,
                                                  DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_RegisterStereoStatusWindow(ring, NPT_CMD_FLAG_REPLY, object_id, WindowHandle, wMsg, pdwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory2_RegisterStereoStatusWindow_reply(dec, pdwCookie, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory2::RegisterStereoStatusEvent
 */

struct npt_command_IDXGIFactory2_RegisterStereoStatusEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory2_RegisterStereoStatusEvent(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pdwCookie (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_RegisterStereoStatusEvent(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   HANDLE hEvent,
                                                   DWORD * pdwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_RegisterStereoStatusEvent(hEvent,pdwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_RegisterStereoStatusEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
/* skip pdwCookie (output only) */
}

static inline size_t
npt_sizeof_IDXGIFactory2_RegisterStereoStatusEvent_reply(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hEvent (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DWORD(&(const DWORD){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory2_RegisterStereoStatusEvent_reply(struct npt_cs_decoder *dec,
                                                         DWORD * pdwCookie,
                                                         HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                                  NPT_METHOD_IDXGIFactory2_RegisterStereoStatusEvent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory2_RegisterStereoStatusEvent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                       NPT_METHOD_IDXGIFactory2_RegisterStereoStatusEvent));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pdwCookie)
            npt_decode_DWORD(dec, (DWORD *)pdwCookie);
    }
}

static inline void
npt_submit_IDXGIFactory2_RegisterStereoStatusEvent(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   HANDLE hEvent,
                                                   DWORD * pdwCookie,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_RegisterStereoStatusEvent(hEvent,pdwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_RegisterStereoStatusEvent_reply(hEvent,pdwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_RegisterStereoStatusEvent(enc, cmd_flags, object_id, hEvent, pdwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_RegisterStereoStatusEvent(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  HANDLE hEvent,
                                                  DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_RegisterStereoStatusEvent(ring, 0, object_id, hEvent, pdwCookie, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory2_RegisterStereoStatusEvent(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 HANDLE hEvent,
                                                 DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_RegisterStereoStatusEvent(ring, NPT_CMD_FLAG_REPLY, object_id, hEvent, pdwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory2_RegisterStereoStatusEvent_reply(dec, pdwCookie, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory2::UnregisterStereoStatus
 */

struct npt_command_IDXGIFactory2_UnregisterStereoStatus {
    void *_self;
    DWORD dwCookie;
};

static inline size_t
npt_sizeof_IDXGIFactory2_UnregisterStereoStatus(const DWORD dwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DWORD(&dwCookie, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_UnregisterStereoStatus(struct npt_cs_encoder *enc,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                DWORD dwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_UnregisterStereoStatus(dwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_UnregisterStereoStatus),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DWORD(enc, &dwCookie);
}

static inline size_t
npt_sizeof_IDXGIFactory2_UnregisterStereoStatus_reply(const DWORD dwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip dwCookie (input only) */
    return cmd_size;
}

static inline void
npt_submit_IDXGIFactory2_UnregisterStereoStatus(struct npt_ring *ring,
                                                uint32_t cmd_flags,
                                                npt_object_id object_id,
                                                DWORD dwCookie,
                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_UnregisterStereoStatus(dwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_UnregisterStereoStatus_reply(dwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_UnregisterStereoStatus(enc, cmd_flags, object_id, dwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_UnregisterStereoStatus(struct npt_ring *ring,
                                               npt_object_id object_id,
                                               DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_UnregisterStereoStatus(ring, 0, object_id, dwCookie, &submit);
}

static inline void
npt_call_IDXGIFactory2_UnregisterStereoStatus(struct npt_ring *ring,
                                              npt_object_id object_id,
                                              DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_UnregisterStereoStatus(ring, NPT_CMD_FLAG_REPLY, object_id, dwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * IDXGIFactory2::RegisterOcclusionStatusWindow
 */

struct npt_command_IDXGIFactory2_RegisterOcclusionStatusWindow {
    void *_self;
    HWND WindowHandle;
    UINT wMsg;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusWindow(const HWND WindowHandle,const UINT wMsg,const DWORD * pdwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_UINT(&wMsg, max_mode);
/* skip pdwCookie (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_RegisterOcclusionStatusWindow(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       HWND WindowHandle,
                                                       UINT wMsg,
                                                       DWORD * pdwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusWindow(WindowHandle,wMsg,pdwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_RegisterOcclusionStatusWindow),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)WindowHandle));
    npt_encode_UINT(enc, &wMsg);
/* skip pdwCookie (output only) */
}

static inline size_t
npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusWindow_reply(const HWND WindowHandle,const UINT wMsg,const DWORD * pdwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip WindowHandle (input only) */
/* skip wMsg (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DWORD(&(const DWORD){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory2_RegisterOcclusionStatusWindow_reply(struct npt_cs_decoder *dec,
                                                             DWORD * pdwCookie,
                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                                  NPT_METHOD_IDXGIFactory2_RegisterOcclusionStatusWindow))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory2_RegisterOcclusionStatusWindow reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                       NPT_METHOD_IDXGIFactory2_RegisterOcclusionStatusWindow));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pdwCookie)
            npt_decode_DWORD(dec, (DWORD *)pdwCookie);
    }
}

static inline void
npt_submit_IDXGIFactory2_RegisterOcclusionStatusWindow(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       HWND WindowHandle,
                                                       UINT wMsg,
                                                       DWORD * pdwCookie,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusWindow(WindowHandle,wMsg,pdwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusWindow_reply(WindowHandle,wMsg,pdwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_RegisterOcclusionStatusWindow(enc, cmd_flags, object_id, WindowHandle, wMsg, pdwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_RegisterOcclusionStatusWindow(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      HWND WindowHandle,
                                                      UINT wMsg,
                                                      DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_RegisterOcclusionStatusWindow(ring, 0, object_id, WindowHandle, wMsg, pdwCookie, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory2_RegisterOcclusionStatusWindow(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     HWND WindowHandle,
                                                     UINT wMsg,
                                                     DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_RegisterOcclusionStatusWindow(ring, NPT_CMD_FLAG_REPLY, object_id, WindowHandle, wMsg, pdwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory2_RegisterOcclusionStatusWindow_reply(dec, pdwCookie, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory2::RegisterOcclusionStatusEvent
 */

struct npt_command_IDXGIFactory2_RegisterOcclusionStatusEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusEvent(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pdwCookie (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_RegisterOcclusionStatusEvent(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      HANDLE hEvent,
                                                      DWORD * pdwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusEvent(hEvent,pdwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_RegisterOcclusionStatusEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
/* skip pdwCookie (output only) */
}

static inline size_t
npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusEvent_reply(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hEvent (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DWORD(&(const DWORD){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory2_RegisterOcclusionStatusEvent_reply(struct npt_cs_decoder *dec,
                                                            DWORD * pdwCookie,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                                  NPT_METHOD_IDXGIFactory2_RegisterOcclusionStatusEvent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory2_RegisterOcclusionStatusEvent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                       NPT_METHOD_IDXGIFactory2_RegisterOcclusionStatusEvent));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pdwCookie)
            npt_decode_DWORD(dec, (DWORD *)pdwCookie);
    }
}

static inline void
npt_submit_IDXGIFactory2_RegisterOcclusionStatusEvent(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      HANDLE hEvent,
                                                      DWORD * pdwCookie,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusEvent(hEvent,pdwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_RegisterOcclusionStatusEvent_reply(hEvent,pdwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_RegisterOcclusionStatusEvent(enc, cmd_flags, object_id, hEvent, pdwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_RegisterOcclusionStatusEvent(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     HANDLE hEvent,
                                                     DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_RegisterOcclusionStatusEvent(ring, 0, object_id, hEvent, pdwCookie, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory2_RegisterOcclusionStatusEvent(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    HANDLE hEvent,
                                                    DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_RegisterOcclusionStatusEvent(ring, NPT_CMD_FLAG_REPLY, object_id, hEvent, pdwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory2_RegisterOcclusionStatusEvent_reply(dec, pdwCookie, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory2::UnregisterOcclusionStatus
 */

struct npt_command_IDXGIFactory2_UnregisterOcclusionStatus {
    void *_self;
    DWORD dwCookie;
};

static inline size_t
npt_sizeof_IDXGIFactory2_UnregisterOcclusionStatus(const DWORD dwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DWORD(&dwCookie, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_UnregisterOcclusionStatus(struct npt_cs_encoder *enc,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   DWORD dwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_UnregisterOcclusionStatus(dwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_UnregisterOcclusionStatus),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DWORD(enc, &dwCookie);
}

static inline size_t
npt_sizeof_IDXGIFactory2_UnregisterOcclusionStatus_reply(const DWORD dwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip dwCookie (input only) */
    return cmd_size;
}

static inline void
npt_submit_IDXGIFactory2_UnregisterOcclusionStatus(struct npt_ring *ring,
                                                   uint32_t cmd_flags,
                                                   npt_object_id object_id,
                                                   DWORD dwCookie,
                                                   struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_UnregisterOcclusionStatus(dwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_UnregisterOcclusionStatus_reply(dwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_UnregisterOcclusionStatus(enc, cmd_flags, object_id, dwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_UnregisterOcclusionStatus(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_UnregisterOcclusionStatus(ring, 0, object_id, dwCookie, &submit);
}

static inline void
npt_call_IDXGIFactory2_UnregisterOcclusionStatus(struct npt_ring *ring,
                                                 npt_object_id object_id,
                                                 DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_UnregisterOcclusionStatus(ring, NPT_CMD_FLAG_REPLY, object_id, dwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * IDXGIFactory2::CreateSwapChainForComposition
 */

struct npt_command_IDXGIFactory2_CreateSwapChainForComposition {
    void *_self;
    IUnknown * pDevice;
    const DXGI_SWAP_CHAIN_DESC1 * pDesc;
    IDXGIOutput * pRestrictToOutput;
    IDXGISwapChain1 ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory2_CreateSwapChainForComposition(const IUnknown * pDevice,const DXGI_SWAP_CHAIN_DESC1 * pDesc,const IDXGIOutput * pRestrictToOutput,IDXGISwapChain1 ** ppSwapChain)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
    cmd_size += npt_sizeof_simple_pointer(pDesc);
    if (pDesc)
        cmd_size += npt_sizeof_DXGI_SWAP_CHAIN_DESC1(pDesc, max_mode);
    cmd_size += sizeof(uint64_t);
cmd_size += sizeof(uint64_t);  /* ppSwapChain: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory2_CreateSwapChainForComposition(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       IUnknown * pDevice,
                                                       const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                       IDXGIOutput * pRestrictToOutput,
                                                       IDXGISwapChain1 ** ppSwapChain)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory2_CreateSwapChainForComposition(pDevice,pDesc,pRestrictToOutput,ppSwapChain);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_CreateSwapChainForComposition),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_com_handle(enc, npt_object_get_id(pDevice));
    if (npt_encode_simple_pointer(enc, pDesc))
        npt_encode_DXGI_SWAP_CHAIN_DESC1(enc, pDesc);
    npt_encode_com_handle(enc, npt_object_get_id(pRestrictToOutput));
{
    uint64_t _gid_ppSwapChain = 0;
    if (ppSwapChain) {
        _gid_ppSwapChain = npt_com_allocate_next_id();
        *ppSwapChain = (IDXGISwapChain1 *)(uintptr_t)_gid_ppSwapChain;
    }
    npt_encode_uint64_t(enc, &_gid_ppSwapChain);
}
}

static inline size_t
npt_sizeof_IDXGIFactory2_CreateSwapChainForComposition_reply(const IUnknown * pDevice,const DXGI_SWAP_CHAIN_DESC1 * pDesc,const IDXGIOutput * pRestrictToOutput,IDXGISwapChain1 ** ppSwapChain)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip pDevice (input only) */
/* skip pDesc (input only) */
/* skip pRestrictToOutput (input only) */
/* skip ppSwapChain (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory2_CreateSwapChainForComposition_reply(struct npt_cs_decoder *dec,
                                                             IDXGISwapChain1 ** ppSwapChain,
                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                                  NPT_METHOD_IDXGIFactory2_CreateSwapChainForComposition))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory2_CreateSwapChainForComposition reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                       NPT_METHOD_IDXGIFactory2_CreateSwapChainForComposition));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppSwapChain (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory2_CreateSwapChainForComposition(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       IUnknown * pDevice,
                                                       const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                       IDXGIOutput * pRestrictToOutput,
                                                       IDXGISwapChain1 ** ppSwapChain,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory2_CreateSwapChainForComposition(pDevice,pDesc,pRestrictToOutput,ppSwapChain);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory2_CreateSwapChainForComposition_reply(pDevice,pDesc,pRestrictToOutput,ppSwapChain)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory2_CreateSwapChainForComposition(enc, cmd_flags, object_id, pDevice, pDesc, pRestrictToOutput, ppSwapChain);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory2_CreateSwapChainForComposition(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      IUnknown * pDevice,
                                                      const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                      IDXGIOutput * pRestrictToOutput,
                                                      IDXGISwapChain1 ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_CreateSwapChainForComposition(ring, 0, object_id, pDevice, pDesc, pRestrictToOutput, ppSwapChain, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory2_CreateSwapChainForComposition(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     IUnknown * pDevice,
                                                     const DXGI_SWAP_CHAIN_DESC1 * pDesc,
                                                     IDXGIOutput * pRestrictToOutput,
                                                     IDXGISwapChain1 ** ppSwapChain)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory2_CreateSwapChainForComposition(ring, NPT_CMD_FLAG_REPLY, object_id, pDevice, pDesc, pRestrictToOutput, ppSwapChain, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory2_CreateSwapChainForComposition_reply(dec, ppSwapChain, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory3::GetCreationFlags
 */

struct npt_command_IDXGIFactory3_GetCreationFlags {
    void *_self;
    UINT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory3_GetCreationFlags(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory3_GetCreationFlags(struct npt_cs_encoder *enc,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory3_GetCreationFlags();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory3,
                                 NPT_METHOD_IDXGIFactory3_GetCreationFlags),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_IDXGIFactory3_GetCreationFlags_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory3_GetCreationFlags_reply(struct npt_cs_decoder *dec,
                                                UINT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory3,
                                                  NPT_METHOD_IDXGIFactory3_GetCreationFlags))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory3_GetCreationFlags reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory3,
                                       NPT_METHOD_IDXGIFactory3_GetCreationFlags));
        if (ret) *ret = (UINT)0;
        return;
    }
    if (ret) *ret = (UINT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIFactory3_GetCreationFlags(struct npt_ring *ring,
                                          uint32_t cmd_flags,
                                          npt_object_id object_id,
                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory3_GetCreationFlags();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory3_GetCreationFlags_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory3_GetCreationFlags(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory3_GetCreationFlags(struct npt_ring *ring,
                                         npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory3_GetCreationFlags(ring, 0, object_id, &submit);
}

static inline UINT
npt_call_IDXGIFactory3_GetCreationFlags(struct npt_ring *ring,
                                        npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory3_GetCreationFlags(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        UINT _ret;
        npt_decode_IDXGIFactory3_GetCreationFlags_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (UINT)0;
    }
}

/*
 * IDXGIFactory4::EnumAdapterByLuid
 */

struct npt_command_IDXGIFactory4_EnumAdapterByLuid {
    void *_self;
    LUID AdapterLuid;
    const IID * riid;
    void ** ppvAdapter;
    /* Shadow: the guest-allocated id for ppvAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvAdapter -> *ppvAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppvAdapter;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory4_EnumAdapterByLuid(const LUID AdapterLuid,const IID * riid,void ** ppvAdapter)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_LUID(&AdapterLuid, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvAdapter: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory4_EnumAdapterByLuid(struct npt_cs_encoder *enc,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           LUID AdapterLuid,
                                           const IID * riid,
                                           void ** ppvAdapter)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory4_EnumAdapterByLuid(AdapterLuid,riid,ppvAdapter);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory4,
                                 NPT_METHOD_IDXGIFactory4_EnumAdapterByLuid),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_LUID(enc, &AdapterLuid);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvAdapter = 0;
    if (ppvAdapter) {
        _gid_ppvAdapter = npt_com_allocate_next_id();
        *ppvAdapter = (void *)(uintptr_t)_gid_ppvAdapter;
    }
    npt_encode_uint64_t(enc, &_gid_ppvAdapter);
}
}

static inline size_t
npt_sizeof_IDXGIFactory4_EnumAdapterByLuid_reply(const LUID AdapterLuid,const IID * riid,void ** ppvAdapter)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip AdapterLuid (input only) */
/* skip riid (input only) */
/* skip ppvAdapter (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory4_EnumAdapterByLuid_reply(struct npt_cs_decoder *dec,
                                                 void ** ppvAdapter,
                                                 HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory4,
                                                  NPT_METHOD_IDXGIFactory4_EnumAdapterByLuid))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory4_EnumAdapterByLuid reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory4,
                                       NPT_METHOD_IDXGIFactory4_EnumAdapterByLuid));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvAdapter (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory4_EnumAdapterByLuid(struct npt_ring *ring,
                                           uint32_t cmd_flags,
                                           npt_object_id object_id,
                                           LUID AdapterLuid,
                                           const IID * riid,
                                           void ** ppvAdapter,
                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory4_EnumAdapterByLuid(AdapterLuid,riid,ppvAdapter);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory4_EnumAdapterByLuid_reply(AdapterLuid,riid,ppvAdapter)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory4_EnumAdapterByLuid(enc, cmd_flags, object_id, AdapterLuid, riid, ppvAdapter);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory4_EnumAdapterByLuid(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          LUID AdapterLuid,
                                          const IID * riid,
                                          void ** ppvAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory4_EnumAdapterByLuid(ring, 0, object_id, AdapterLuid, riid, ppvAdapter, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory4_EnumAdapterByLuid(struct npt_ring *ring,
                                         npt_object_id object_id,
                                         LUID AdapterLuid,
                                         const IID * riid,
                                         void ** ppvAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory4_EnumAdapterByLuid(ring, NPT_CMD_FLAG_REPLY, object_id, AdapterLuid, riid, ppvAdapter, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory4_EnumAdapterByLuid_reply(dec, ppvAdapter, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory4::EnumWarpAdapter
 */

struct npt_command_IDXGIFactory4_EnumWarpAdapter {
    void *_self;
    const IID * riid;
    void ** ppvAdapter;
    /* Shadow: the guest-allocated id for ppvAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvAdapter -> *ppvAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppvAdapter;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory4_EnumWarpAdapter(const IID * riid,void ** ppvAdapter)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvAdapter: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory4_EnumWarpAdapter(struct npt_cs_encoder *enc,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const IID * riid,
                                         void ** ppvAdapter)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory4_EnumWarpAdapter(riid,ppvAdapter);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory4,
                                 NPT_METHOD_IDXGIFactory4_EnumWarpAdapter),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvAdapter = 0;
    if (ppvAdapter) {
        _gid_ppvAdapter = npt_com_allocate_next_id();
        *ppvAdapter = (void *)(uintptr_t)_gid_ppvAdapter;
    }
    npt_encode_uint64_t(enc, &_gid_ppvAdapter);
}
}

static inline size_t
npt_sizeof_IDXGIFactory4_EnumWarpAdapter_reply(const IID * riid,void ** ppvAdapter)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip riid (input only) */
/* skip ppvAdapter (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory4_EnumWarpAdapter_reply(struct npt_cs_decoder *dec,
                                               void ** ppvAdapter,
                                               HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory4,
                                                  NPT_METHOD_IDXGIFactory4_EnumWarpAdapter))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory4_EnumWarpAdapter reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory4,
                                       NPT_METHOD_IDXGIFactory4_EnumWarpAdapter));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvAdapter (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory4_EnumWarpAdapter(struct npt_ring *ring,
                                         uint32_t cmd_flags,
                                         npt_object_id object_id,
                                         const IID * riid,
                                         void ** ppvAdapter,
                                         struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory4_EnumWarpAdapter(riid,ppvAdapter);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory4_EnumWarpAdapter_reply(riid,ppvAdapter)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory4_EnumWarpAdapter(enc, cmd_flags, object_id, riid, ppvAdapter);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory4_EnumWarpAdapter(struct npt_ring *ring,
                                        npt_object_id object_id,
                                        const IID * riid,
                                        void ** ppvAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory4_EnumWarpAdapter(ring, 0, object_id, riid, ppvAdapter, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory4_EnumWarpAdapter(struct npt_ring *ring,
                                       npt_object_id object_id,
                                       const IID * riid,
                                       void ** ppvAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory4_EnumWarpAdapter(ring, NPT_CMD_FLAG_REPLY, object_id, riid, ppvAdapter, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory4_EnumWarpAdapter_reply(dec, ppvAdapter, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory5::CheckFeatureSupport
 */

struct npt_command_IDXGIFactory5_CheckFeatureSupport {
    void *_self;
    DXGI_FEATURE Feature;
    void * pFeatureSupportData;
    UINT FeatureSupportDataSize;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory5_CheckFeatureSupport(const DXGI_FEATURE Feature,const void * pFeatureSupportData,const UINT FeatureSupportDataSize)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FEATURE(&Feature, max_mode);
    if (pFeatureSupportData) {
        cmd_size += npt_sizeof_array_count(FeatureSupportDataSize);
        cmd_size += npt_sizeof_blob_array(pFeatureSupportData, FeatureSupportDataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
    cmd_size += npt_sizeof_UINT(&FeatureSupportDataSize, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory5_CheckFeatureSupport(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             DXGI_FEATURE Feature,
                                             void * pFeatureSupportData,
                                             UINT FeatureSupportDataSize)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory5_CheckFeatureSupport(Feature,pFeatureSupportData,FeatureSupportDataSize);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory5,
                                 NPT_METHOD_IDXGIFactory5_CheckFeatureSupport),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FEATURE(enc, &Feature);
    if (pFeatureSupportData) {
        npt_encode_array_count(enc, FeatureSupportDataSize);
        npt_encode_blob_array(enc, pFeatureSupportData, FeatureSupportDataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    npt_encode_UINT(enc, &FeatureSupportDataSize);
}

static inline size_t
npt_sizeof_IDXGIFactory5_CheckFeatureSupport_reply(const DXGI_FEATURE Feature,const void * pFeatureSupportData,const UINT FeatureSupportDataSize)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Feature (input only) */
    if (pFeatureSupportData) {
        cmd_size += npt_sizeof_array_count(FeatureSupportDataSize);
        cmd_size += npt_sizeof_blob_array(pFeatureSupportData, FeatureSupportDataSize);
    } else {
        cmd_size += npt_sizeof_array_count(0);
    }
/* skip FeatureSupportDataSize (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory5_CheckFeatureSupport_reply(struct npt_cs_decoder *dec,
                                                   void * pFeatureSupportData,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory5,
                                                  NPT_METHOD_IDXGIFactory5_CheckFeatureSupport))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory5_CheckFeatureSupport reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory5,
                                       NPT_METHOD_IDXGIFactory5_CheckFeatureSupport));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size && pFeatureSupportData)
            npt_decode_blob_array(dec, (void *)pFeatureSupportData, _blob_size);
        else if (_blob_size)
            npt_cs_decoder_read(dec, ((_blob_size + 3) & ~3), NULL, 0);
    }
}

static inline void
npt_submit_IDXGIFactory5_CheckFeatureSupport(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             DXGI_FEATURE Feature,
                                             void * pFeatureSupportData,
                                             UINT FeatureSupportDataSize,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory5_CheckFeatureSupport(Feature,pFeatureSupportData,FeatureSupportDataSize);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory5_CheckFeatureSupport_reply(Feature,pFeatureSupportData,FeatureSupportDataSize)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory5_CheckFeatureSupport(enc, cmd_flags, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory5_CheckFeatureSupport(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            DXGI_FEATURE Feature,
                                            void * pFeatureSupportData,
                                            UINT FeatureSupportDataSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory5_CheckFeatureSupport(ring, 0, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory5_CheckFeatureSupport(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           DXGI_FEATURE Feature,
                                           void * pFeatureSupportData,
                                           UINT FeatureSupportDataSize)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory5_CheckFeatureSupport(ring, NPT_CMD_FLAG_REPLY, object_id, Feature, pFeatureSupportData, FeatureSupportDataSize, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory5_CheckFeatureSupport_reply(dec, pFeatureSupportData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory6::EnumAdapterByGpuPreference
 */

struct npt_command_IDXGIFactory6_EnumAdapterByGpuPreference {
    void *_self;
    UINT Adapter;
    DXGI_GPU_PREFERENCE GpuPreference;
    const IID * riid;
    void ** ppvAdapter;
    /* Shadow: the guest-allocated id for ppvAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvAdapter -> *ppvAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppvAdapter;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory6_EnumAdapterByGpuPreference(const UINT Adapter,const DXGI_GPU_PREFERENCE GpuPreference,const IID * riid,void ** ppvAdapter)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Adapter, max_mode);
    cmd_size += npt_sizeof_DXGI_GPU_PREFERENCE(&GpuPreference, max_mode);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppvAdapter: guest id */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory6_EnumAdapterByGpuPreference(struct npt_cs_encoder *enc,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT Adapter,
                                                    DXGI_GPU_PREFERENCE GpuPreference,
                                                    const IID * riid,
                                                    void ** ppvAdapter)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory6_EnumAdapterByGpuPreference(Adapter,GpuPreference,riid,ppvAdapter);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory6,
                                 NPT_METHOD_IDXGIFactory6_EnumAdapterByGpuPreference),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Adapter);
    npt_encode_DXGI_GPU_PREFERENCE(enc, &GpuPreference);
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppvAdapter = 0;
    if (ppvAdapter) {
        _gid_ppvAdapter = npt_com_allocate_next_id();
        *ppvAdapter = (void *)(uintptr_t)_gid_ppvAdapter;
    }
    npt_encode_uint64_t(enc, &_gid_ppvAdapter);
}
}

static inline size_t
npt_sizeof_IDXGIFactory6_EnumAdapterByGpuPreference_reply(const UINT Adapter,const DXGI_GPU_PREFERENCE GpuPreference,const IID * riid,void ** ppvAdapter)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Adapter (input only) */
/* skip GpuPreference (input only) */
/* skip riid (input only) */
/* skip ppvAdapter (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory6_EnumAdapterByGpuPreference_reply(struct npt_cs_decoder *dec,
                                                          void ** ppvAdapter,
                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory6,
                                                  NPT_METHOD_IDXGIFactory6_EnumAdapterByGpuPreference))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory6_EnumAdapterByGpuPreference reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory6,
                                       NPT_METHOD_IDXGIFactory6_EnumAdapterByGpuPreference));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppvAdapter (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_IDXGIFactory6_EnumAdapterByGpuPreference(struct npt_ring *ring,
                                                    uint32_t cmd_flags,
                                                    npt_object_id object_id,
                                                    UINT Adapter,
                                                    DXGI_GPU_PREFERENCE GpuPreference,
                                                    const IID * riid,
                                                    void ** ppvAdapter,
                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory6_EnumAdapterByGpuPreference(Adapter,GpuPreference,riid,ppvAdapter);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory6_EnumAdapterByGpuPreference_reply(Adapter,GpuPreference,riid,ppvAdapter)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory6_EnumAdapterByGpuPreference(enc, cmd_flags, object_id, Adapter, GpuPreference, riid, ppvAdapter);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory6_EnumAdapterByGpuPreference(struct npt_ring *ring,
                                                   npt_object_id object_id,
                                                   UINT Adapter,
                                                   DXGI_GPU_PREFERENCE GpuPreference,
                                                   const IID * riid,
                                                   void ** ppvAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory6_EnumAdapterByGpuPreference(ring, 0, object_id, Adapter, GpuPreference, riid, ppvAdapter, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory6_EnumAdapterByGpuPreference(struct npt_ring *ring,
                                                  npt_object_id object_id,
                                                  UINT Adapter,
                                                  DXGI_GPU_PREFERENCE GpuPreference,
                                                  const IID * riid,
                                                  void ** ppvAdapter)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory6_EnumAdapterByGpuPreference(ring, NPT_CMD_FLAG_REPLY, object_id, Adapter, GpuPreference, riid, ppvAdapter, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory6_EnumAdapterByGpuPreference_reply(dec, ppvAdapter, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory7::RegisterAdaptersChangedEvent
 */

struct npt_command_IDXGIFactory7_RegisterAdaptersChangedEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory7_RegisterAdaptersChangedEvent(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += sizeof(uint64_t);
/* skip pdwCookie (output only) */
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory7_RegisterAdaptersChangedEvent(struct npt_cs_encoder *enc,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      HANDLE hEvent,
                                                      DWORD * pdwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory7_RegisterAdaptersChangedEvent(hEvent,pdwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory7,
                                 NPT_METHOD_IDXGIFactory7_RegisterAdaptersChangedEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)hEvent));
/* skip pdwCookie (output only) */
}

static inline size_t
npt_sizeof_IDXGIFactory7_RegisterAdaptersChangedEvent_reply(const HANDLE hEvent,const DWORD * pdwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip hEvent (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_DWORD(&(const DWORD){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory7_RegisterAdaptersChangedEvent_reply(struct npt_cs_decoder *dec,
                                                            DWORD * pdwCookie,
                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory7,
                                                  NPT_METHOD_IDXGIFactory7_RegisterAdaptersChangedEvent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory7_RegisterAdaptersChangedEvent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory7,
                                       NPT_METHOD_IDXGIFactory7_RegisterAdaptersChangedEvent));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pdwCookie)
            npt_decode_DWORD(dec, (DWORD *)pdwCookie);
    }
}

static inline void
npt_submit_IDXGIFactory7_RegisterAdaptersChangedEvent(struct npt_ring *ring,
                                                      uint32_t cmd_flags,
                                                      npt_object_id object_id,
                                                      HANDLE hEvent,
                                                      DWORD * pdwCookie,
                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory7_RegisterAdaptersChangedEvent(hEvent,pdwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory7_RegisterAdaptersChangedEvent_reply(hEvent,pdwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory7_RegisterAdaptersChangedEvent(enc, cmd_flags, object_id, hEvent, pdwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory7_RegisterAdaptersChangedEvent(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     HANDLE hEvent,
                                                     DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory7_RegisterAdaptersChangedEvent(ring, 0, object_id, hEvent, pdwCookie, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory7_RegisterAdaptersChangedEvent(struct npt_ring *ring,
                                                    npt_object_id object_id,
                                                    HANDLE hEvent,
                                                    DWORD * pdwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory7_RegisterAdaptersChangedEvent(ring, NPT_CMD_FLAG_REPLY, object_id, hEvent, pdwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory7_RegisterAdaptersChangedEvent_reply(dec, pdwCookie, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * IDXGIFactory7::UnregisterAdaptersChangedEvent
 */

struct npt_command_IDXGIFactory7_UnregisterAdaptersChangedEvent {
    void *_self;
    DWORD dwCookie;
    HRESULT ret;
};

static inline size_t
npt_sizeof_IDXGIFactory7_UnregisterAdaptersChangedEvent(const DWORD dwCookie)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DWORD(&dwCookie, max_mode);
    return cmd_size;
}

static inline void
npt_encode_IDXGIFactory7_UnregisterAdaptersChangedEvent(struct npt_cs_encoder *enc,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        DWORD dwCookie)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_IDXGIFactory7_UnregisterAdaptersChangedEvent(dwCookie);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory7,
                                 NPT_METHOD_IDXGIFactory7_UnregisterAdaptersChangedEvent),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DWORD(enc, &dwCookie);
}

static inline size_t
npt_sizeof_IDXGIFactory7_UnregisterAdaptersChangedEvent_reply(const DWORD dwCookie)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip dwCookie (input only) */
    return cmd_size;
}

static inline void
npt_decode_IDXGIFactory7_UnregisterAdaptersChangedEvent_reply(struct npt_cs_decoder *dec,
                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory7,
                                                  NPT_METHOD_IDXGIFactory7_UnregisterAdaptersChangedEvent))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("IDXGIFactory7_UnregisterAdaptersChangedEvent reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory7,
                                       NPT_METHOD_IDXGIFactory7_UnregisterAdaptersChangedEvent));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_IDXGIFactory7_UnregisterAdaptersChangedEvent(struct npt_ring *ring,
                                                        uint32_t cmd_flags,
                                                        npt_object_id object_id,
                                                        DWORD dwCookie,
                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_IDXGIFactory7_UnregisterAdaptersChangedEvent(dwCookie);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_IDXGIFactory7_UnregisterAdaptersChangedEvent_reply(dwCookie)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_IDXGIFactory7_UnregisterAdaptersChangedEvent(enc, cmd_flags, object_id, dwCookie);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_IDXGIFactory7_UnregisterAdaptersChangedEvent(struct npt_ring *ring,
                                                       npt_object_id object_id,
                                                       DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory7_UnregisterAdaptersChangedEvent(ring, 0, object_id, dwCookie, &submit);
}

static inline HRESULT
npt_call_IDXGIFactory7_UnregisterAdaptersChangedEvent(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      DWORD dwCookie)
{
    struct npt_ring_submit_command submit;
    npt_submit_IDXGIFactory7_UnregisterAdaptersChangedEvent(ring, NPT_CMD_FLAG_REPLY, object_id, dwCookie, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_IDXGIFactory7_UnregisterAdaptersChangedEvent_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_IDXGIFACTORY_H */
