/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11videoprocessorenumerator.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D11VIDEOPROCESSORENUMERATOR_H
#define NPT_PROTOCOL_GUEST_ID3D11VIDEOPROCESSORENUMERATOR_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorContentDesc
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc {
    void *_self;
    D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pContentDesc;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pContentDesc)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pContentDesc (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(struct npt_cs_encoder *enc,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pContentDesc)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(pContentDesc);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pContentDesc (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_reply(const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pContentDesc)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_CONTENT_DESC(&(const D3D11_VIDEO_PROCESSOR_CONTENT_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_reply(struct npt_cs_decoder *dec,
                                                                             D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pContentDesc,
                                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                                  NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                       NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pContentDesc)
            npt_decode_D3D11_VIDEO_PROCESSOR_CONTENT_DESC(dec, (D3D11_VIDEO_PROCESSOR_CONTENT_DESC *)pContentDesc);
    }
}

static inline void
npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(struct npt_ring *ring,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pContentDesc,
                                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(pContentDesc);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_reply(pContentDesc)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(enc, cmd_flags, object_id, pContentDesc);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pContentDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(ring, 0, object_id, pContentDesc, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pContentDesc)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(ring, NPT_CMD_FLAG_REPLY, object_id, pContentDesc, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_reply(dec, pContentDesc, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoProcessorEnumerator::CheckVideoProcessorFormat
 */

struct npt_command_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat {
    void *_self;
    DXGI_FORMAT Format;
    UINT * pFlags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(const DXGI_FORMAT Format,const UINT * pFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FORMAT(&Format, max_mode);
/* skip pFlags (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(struct npt_cs_encoder *enc,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    DXGI_FORMAT Format,
                                                                    UINT * pFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(Format,pFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FORMAT(enc, &Format);
/* skip pFlags (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_reply(const DXGI_FORMAT Format,const UINT * pFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Format (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_UINT(&(const UINT){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_reply(struct npt_cs_decoder *dec,
                                                                          UINT * pFlags,
                                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                                  NPT_METHOD_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                       NPT_METHOD_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pFlags)
            npt_decode_UINT(dec, (UINT *)pFlags);
    }
}

static inline void
npt_submit_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(struct npt_ring *ring,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    DXGI_FORMAT Format,
                                                                    UINT * pFlags,
                                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(Format,pFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_reply(Format,pFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(enc, cmd_flags, object_id, Format, pFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(struct npt_ring *ring,
                                                                   npt_object_id object_id,
                                                                   DXGI_FORMAT Format,
                                                                   UINT * pFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(ring, 0, object_id, Format, pFlags, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  DXGI_FORMAT Format,
                                                                  UINT * pFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(ring, NPT_CMD_FLAG_REPLY, object_id, Format, pFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_reply(dec, pFlags, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorCaps
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps {
    void *_self;
    D3D11_VIDEO_PROCESSOR_CAPS * pCaps;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(const D3D11_VIDEO_PROCESSOR_CAPS * pCaps)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pCaps (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(struct npt_cs_encoder *enc,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                D3D11_VIDEO_PROCESSOR_CAPS * pCaps)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(pCaps);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pCaps (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_reply(const D3D11_VIDEO_PROCESSOR_CAPS * pCaps)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_CAPS(&(const D3D11_VIDEO_PROCESSOR_CAPS){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_reply(struct npt_cs_decoder *dec,
                                                                      D3D11_VIDEO_PROCESSOR_CAPS * pCaps,
                                                                      HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                                  NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                       NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pCaps)
            npt_decode_D3D11_VIDEO_PROCESSOR_CAPS(dec, (D3D11_VIDEO_PROCESSOR_CAPS *)pCaps);
    }
}

static inline void
npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(struct npt_ring *ring,
                                                                uint32_t cmd_flags,
                                                                npt_object_id object_id,
                                                                D3D11_VIDEO_PROCESSOR_CAPS * pCaps,
                                                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(pCaps);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_reply(pCaps)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(enc, cmd_flags, object_id, pCaps);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(struct npt_ring *ring,
                                                               npt_object_id object_id,
                                                               D3D11_VIDEO_PROCESSOR_CAPS * pCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(ring, 0, object_id, pCaps, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(struct npt_ring *ring,
                                                              npt_object_id object_id,
                                                              D3D11_VIDEO_PROCESSOR_CAPS * pCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(ring, NPT_CMD_FLAG_REPLY, object_id, pCaps, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_reply(dec, pCaps, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorRateConversionCaps
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps {
    void *_self;
    UINT TypeIndex;
    D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS * pCaps;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(const UINT TypeIndex,const D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS * pCaps)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&TypeIndex, max_mode);
/* skip pCaps (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(struct npt_cs_encoder *enc,
                                                                              uint32_t cmd_flags,
                                                                              npt_object_id object_id,
                                                                              UINT TypeIndex,
                                                                              D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS * pCaps)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(TypeIndex,pCaps);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &TypeIndex);
/* skip pCaps (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_reply(const UINT TypeIndex,const D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS * pCaps)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip TypeIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS(&(const D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_reply(struct npt_cs_decoder *dec,
                                                                                    D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS * pCaps,
                                                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                                  NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                       NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pCaps)
            npt_decode_D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS(dec, (D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS *)pCaps);
    }
}

static inline void
npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(struct npt_ring *ring,
                                                                              uint32_t cmd_flags,
                                                                              npt_object_id object_id,
                                                                              UINT TypeIndex,
                                                                              D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS * pCaps,
                                                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(TypeIndex,pCaps);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_reply(TypeIndex,pCaps)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(enc, cmd_flags, object_id, TypeIndex, pCaps);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(struct npt_ring *ring,
                                                                             npt_object_id object_id,
                                                                             UINT TypeIndex,
                                                                             D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS * pCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(ring, 0, object_id, TypeIndex, pCaps, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(struct npt_ring *ring,
                                                                            npt_object_id object_id,
                                                                            UINT TypeIndex,
                                                                            D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS * pCaps)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(ring, NPT_CMD_FLAG_REPLY, object_id, TypeIndex, pCaps, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_reply(dec, pCaps, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorCustomRate
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate {
    void *_self;
    UINT TypeIndex;
    UINT CustomRateIndex;
    D3D11_VIDEO_PROCESSOR_CUSTOM_RATE * pRate;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(const UINT TypeIndex,const UINT CustomRateIndex,const D3D11_VIDEO_PROCESSOR_CUSTOM_RATE * pRate)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&TypeIndex, max_mode);
    cmd_size += npt_sizeof_UINT(&CustomRateIndex, max_mode);
/* skip pRate (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(struct npt_cs_encoder *enc,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      UINT TypeIndex,
                                                                      UINT CustomRateIndex,
                                                                      D3D11_VIDEO_PROCESSOR_CUSTOM_RATE * pRate)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(TypeIndex,CustomRateIndex,pRate);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &TypeIndex);
    npt_encode_UINT(enc, &CustomRateIndex);
/* skip pRate (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_reply(const UINT TypeIndex,const UINT CustomRateIndex,const D3D11_VIDEO_PROCESSOR_CUSTOM_RATE * pRate)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip TypeIndex (input only) */
/* skip CustomRateIndex (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_CUSTOM_RATE(&(const D3D11_VIDEO_PROCESSOR_CUSTOM_RATE){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_reply(struct npt_cs_decoder *dec,
                                                                            D3D11_VIDEO_PROCESSOR_CUSTOM_RATE * pRate,
                                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                                  NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                       NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pRate)
            npt_decode_D3D11_VIDEO_PROCESSOR_CUSTOM_RATE(dec, (D3D11_VIDEO_PROCESSOR_CUSTOM_RATE *)pRate);
    }
}

static inline void
npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(struct npt_ring *ring,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      UINT TypeIndex,
                                                                      UINT CustomRateIndex,
                                                                      D3D11_VIDEO_PROCESSOR_CUSTOM_RATE * pRate,
                                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(TypeIndex,CustomRateIndex,pRate);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_reply(TypeIndex,CustomRateIndex,pRate)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(enc, cmd_flags, object_id, TypeIndex, CustomRateIndex, pRate);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     UINT TypeIndex,
                                                                     UINT CustomRateIndex,
                                                                     D3D11_VIDEO_PROCESSOR_CUSTOM_RATE * pRate)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(ring, 0, object_id, TypeIndex, CustomRateIndex, pRate, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    UINT TypeIndex,
                                                                    UINT CustomRateIndex,
                                                                    D3D11_VIDEO_PROCESSOR_CUSTOM_RATE * pRate)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(ring, NPT_CMD_FLAG_REPLY, object_id, TypeIndex, CustomRateIndex, pRate, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_reply(dec, pRate, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorFilterRange
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange {
    void *_self;
    D3D11_VIDEO_PROCESSOR_FILTER Filter;
    D3D11_VIDEO_PROCESSOR_FILTER_RANGE * pRange;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(const D3D11_VIDEO_PROCESSOR_FILTER Filter,const D3D11_VIDEO_PROCESSOR_FILTER_RANGE * pRange)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_FILTER(&Filter, max_mode);
/* skip pRange (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(struct npt_cs_encoder *enc,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                                       D3D11_VIDEO_PROCESSOR_FILTER_RANGE * pRange)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(Filter,pRange);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_D3D11_VIDEO_PROCESSOR_FILTER(enc, &Filter);
/* skip pRange (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_reply(const D3D11_VIDEO_PROCESSOR_FILTER Filter,const D3D11_VIDEO_PROCESSOR_FILTER_RANGE * pRange)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Filter (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D11_VIDEO_PROCESSOR_FILTER_RANGE(&(const D3D11_VIDEO_PROCESSOR_FILTER_RANGE){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_reply(struct npt_cs_decoder *dec,
                                                                             D3D11_VIDEO_PROCESSOR_FILTER_RANGE * pRange,
                                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                                  NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                       NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pRange)
            npt_decode_D3D11_VIDEO_PROCESSOR_FILTER_RANGE(dec, (D3D11_VIDEO_PROCESSOR_FILTER_RANGE *)pRange);
    }
}

static inline void
npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(struct npt_ring *ring,
                                                                       uint32_t cmd_flags,
                                                                       npt_object_id object_id,
                                                                       D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                                       D3D11_VIDEO_PROCESSOR_FILTER_RANGE * pRange,
                                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(Filter,pRange);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_reply(Filter,pRange)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(enc, cmd_flags, object_id, Filter, pRange);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                                      D3D11_VIDEO_PROCESSOR_FILTER_RANGE * pRange)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(ring, 0, object_id, Filter, pRange, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     D3D11_VIDEO_PROCESSOR_FILTER Filter,
                                                                     D3D11_VIDEO_PROCESSOR_FILTER_RANGE * pRange)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(ring, NPT_CMD_FLAG_REPLY, object_id, Filter, pRange, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_reply(dec, pRange, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D11VideoProcessorEnumerator1::CheckVideoProcessorFormatConversion
 */

struct npt_command_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion {
    void *_self;
    DXGI_FORMAT InputFormat;
    DXGI_COLOR_SPACE_TYPE InputColorSpace;
    DXGI_FORMAT OutputFormat;
    DXGI_COLOR_SPACE_TYPE OutputColorSpace;
    BOOL * pSupported;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(const DXGI_FORMAT InputFormat,const DXGI_COLOR_SPACE_TYPE InputColorSpace,const DXGI_FORMAT OutputFormat,const DXGI_COLOR_SPACE_TYPE OutputColorSpace,const BOOL * pSupported)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_DXGI_FORMAT(&InputFormat, max_mode);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&InputColorSpace, max_mode);
    cmd_size += npt_sizeof_DXGI_FORMAT(&OutputFormat, max_mode);
    cmd_size += npt_sizeof_DXGI_COLOR_SPACE_TYPE(&OutputColorSpace, max_mode);
/* skip pSupported (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(struct npt_cs_encoder *enc,
                                                                               uint32_t cmd_flags,
                                                                               npt_object_id object_id,
                                                                               DXGI_FORMAT InputFormat,
                                                                               DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                                               DXGI_FORMAT OutputFormat,
                                                                               DXGI_COLOR_SPACE_TYPE OutputColorSpace,
                                                                               BOOL * pSupported)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(InputFormat,InputColorSpace,OutputFormat,OutputColorSpace,pSupported);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator1,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_DXGI_FORMAT(enc, &InputFormat);
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &InputColorSpace);
    npt_encode_DXGI_FORMAT(enc, &OutputFormat);
    npt_encode_DXGI_COLOR_SPACE_TYPE(enc, &OutputColorSpace);
/* skip pSupported (output only) */
}

static inline size_t
npt_sizeof_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_reply(const DXGI_FORMAT InputFormat,const DXGI_COLOR_SPACE_TYPE InputColorSpace,const DXGI_FORMAT OutputFormat,const DXGI_COLOR_SPACE_TYPE OutputColorSpace,const BOOL * pSupported)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip InputFormat (input only) */
/* skip InputColorSpace (input only) */
/* skip OutputFormat (input only) */
/* skip OutputColorSpace (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_BOOL(&(const BOOL){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_reply(struct npt_cs_decoder *dec,
                                                                                     BOOL * pSupported,
                                                                                     HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator1,
                                                  NPT_METHOD_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator1,
                                       NPT_METHOD_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pSupported)
            npt_decode_BOOL(dec, (BOOL *)pSupported);
    }
}

static inline void
npt_submit_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(struct npt_ring *ring,
                                                                               uint32_t cmd_flags,
                                                                               npt_object_id object_id,
                                                                               DXGI_FORMAT InputFormat,
                                                                               DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                                               DXGI_FORMAT OutputFormat,
                                                                               DXGI_COLOR_SPACE_TYPE OutputColorSpace,
                                                                               BOOL * pSupported,
                                                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(InputFormat,InputColorSpace,OutputFormat,OutputColorSpace,pSupported);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_reply(InputFormat,InputColorSpace,OutputFormat,OutputColorSpace,pSupported)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(enc, cmd_flags, object_id, InputFormat, InputColorSpace, OutputFormat, OutputColorSpace, pSupported);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(struct npt_ring *ring,
                                                                              npt_object_id object_id,
                                                                              DXGI_FORMAT InputFormat,
                                                                              DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                                              DXGI_FORMAT OutputFormat,
                                                                              DXGI_COLOR_SPACE_TYPE OutputColorSpace,
                                                                              BOOL * pSupported)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(ring, 0, object_id, InputFormat, InputColorSpace, OutputFormat, OutputColorSpace, pSupported, &submit);
}

static inline HRESULT
npt_call_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(struct npt_ring *ring,
                                                                             npt_object_id object_id,
                                                                             DXGI_FORMAT InputFormat,
                                                                             DXGI_COLOR_SPACE_TYPE InputColorSpace,
                                                                             DXGI_FORMAT OutputFormat,
                                                                             DXGI_COLOR_SPACE_TYPE OutputColorSpace,
                                                                             BOOL * pSupported)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(ring, NPT_CMD_FLAG_REPLY, object_id, InputFormat, InputColorSpace, OutputFormat, OutputColorSpace, pSupported, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_reply(dec, pSupported, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D11VIDEOPROCESSORENUMERATOR_H */
