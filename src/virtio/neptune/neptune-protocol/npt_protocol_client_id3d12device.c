/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM implementation for id3d12device: out-of-line default
 * thunks, default vtable populators, and default constructors for every
 * interface in this family.
 */

#include "npt_protocol_client_id3d12device.h"
#include "npt_com.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wunused-variable"



/* ================================================================== */
/* ID3D12Device                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device_default_Release(void *self)
{
    return npt_com_default_release(self);
}

UINT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_GetNodeCount(void *self)
{
    UINT _ret = npt_call_ID3D12Device_GetNodeCount(npt_com_self_ring(self),npt_com_self_id(self));
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateCommandQueue(void *self, const D3D12_COMMAND_QUEUE_DESC * pDesc, const IID * riid, void ** ppCommandQueue)
{
    void *_raw_ppCommandQueue = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppCommandQueue = ppCommandQueue;
    if (ppCommandQueue)
        ppCommandQueue = (void **)&_raw_ppCommandQueue;
    npt_async_ID3D12Device_CreateCommandQueue(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppCommandQueue);
    if (_orig_ppCommandQueue) {
        if (_raw_ppCommandQueue)
            *_orig_ppCommandQueue = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppCommandQueue,
                (struct npt_com_base *)self);
        else
            *_orig_ppCommandQueue = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateCommandAllocator(void *self, D3D12_COMMAND_LIST_TYPE type, const IID * riid, void ** ppCommandAllocator)
{
    void *_raw_ppCommandAllocator = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppCommandAllocator = ppCommandAllocator;
    if (ppCommandAllocator)
        ppCommandAllocator = (void **)&_raw_ppCommandAllocator;
    npt_async_ID3D12Device_CreateCommandAllocator(npt_com_self_ring(self),npt_com_self_id(self),type,riid,ppCommandAllocator);
    if (_orig_ppCommandAllocator) {
        if (_raw_ppCommandAllocator)
            *_orig_ppCommandAllocator = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppCommandAllocator,
                (struct npt_com_base *)self);
        else
            *_orig_ppCommandAllocator = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateGraphicsPipelineState(void *self, const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc, const IID * riid, void ** ppPipelineState)
{
    void *_raw_ppPipelineState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppPipelineState = ppPipelineState;
    if (ppPipelineState)
        ppPipelineState = (void **)&_raw_ppPipelineState;
    npt_async_ID3D12Device_CreateGraphicsPipelineState(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppPipelineState);
    if (_orig_ppPipelineState) {
        if (_raw_ppPipelineState)
            *_orig_ppPipelineState = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppPipelineState,
                (struct npt_com_base *)self);
        else
            *_orig_ppPipelineState = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateComputePipelineState(void *self, const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc, const IID * riid, void ** ppPipelineState)
{
    void *_raw_ppPipelineState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppPipelineState = ppPipelineState;
    if (ppPipelineState)
        ppPipelineState = (void **)&_raw_ppPipelineState;
    npt_async_ID3D12Device_CreateComputePipelineState(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppPipelineState);
    if (_orig_ppPipelineState) {
        if (_raw_ppPipelineState)
            *_orig_ppPipelineState = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppPipelineState,
                (struct npt_com_base *)self);
        else
            *_orig_ppPipelineState = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateCommandList(void *self, UINT nodeMask, D3D12_COMMAND_LIST_TYPE type, ID3D12CommandAllocator * pCommandAllocator, ID3D12PipelineState * pInitialState, const IID * riid, void ** ppCommandList)
{
    void *_raw_ppCommandList = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppCommandList = ppCommandList;
    if (ppCommandList)
        ppCommandList = (void **)&_raw_ppCommandList;
    npt_async_ID3D12Device_CreateCommandList(npt_com_self_ring(self),npt_com_self_id(self),nodeMask,type,pCommandAllocator,pInitialState,riid,ppCommandList);
    if (_orig_ppCommandList) {
        if (_raw_ppCommandList)
            *_orig_ppCommandList = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppCommandList,
                (struct npt_com_base *)self);
        else
            *_orig_ppCommandList = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

/* skip_default: ID3D12Device::CheckFeatureSupport -- override required.
 * The default body aborts loudly so a missing override is a runtime
 * failure rather than a silent data drop. */
HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CheckFeatureSupport(void *self, D3D12_FEATURE Feature, void * pFeatureSupportData, UINT FeatureSupportDataSize)
{
    (void)self;
    (void)Feature;
    (void)pFeatureSupportData;
    (void)FeatureSupportDataSize;
    npt_com_assert_overridden("ID3D12Device", "CheckFeatureSupport");
    return (HRESULT)0x80004005 /* E_FAIL */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateDescriptorHeap(void *self, const D3D12_DESCRIPTOR_HEAP_DESC * pDescriptorHeapDesc, const IID * riid, void ** ppvHeap)
{
    void *_raw_ppvHeap = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvHeap = ppvHeap;
    if (ppvHeap)
        ppvHeap = (void **)&_raw_ppvHeap;
    npt_async_ID3D12Device_CreateDescriptorHeap(npt_com_self_ring(self),npt_com_self_id(self),pDescriptorHeapDesc,riid,ppvHeap);
    if (_orig_ppvHeap) {
        if (_raw_ppvHeap)
            *_orig_ppvHeap = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvHeap,
                (struct npt_com_base *)self);
        else
            *_orig_ppvHeap = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

UINT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_GetDescriptorHandleIncrementSize(void *self, D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapType)
{
    UINT _ret = npt_call_ID3D12Device_GetDescriptorHandleIncrementSize(npt_com_self_ring(self),npt_com_self_id(self),DescriptorHeapType);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateRootSignature(void *self, UINT nodeMask, const void * pBlobWithRootSignature, SIZE_T blobLengthInBytes, const IID * riid, void ** ppvRootSignature)
{
    void *_raw_ppvRootSignature = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvRootSignature = ppvRootSignature;
    if (ppvRootSignature)
        ppvRootSignature = (void **)&_raw_ppvRootSignature;
    npt_async_ID3D12Device_CreateRootSignature(npt_com_self_ring(self),npt_com_self_id(self),nodeMask,pBlobWithRootSignature,blobLengthInBytes,riid,ppvRootSignature);
    if (_orig_ppvRootSignature) {
        if (_raw_ppvRootSignature)
            *_orig_ppvRootSignature = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvRootSignature,
                (struct npt_com_base *)self);
        else
            *_orig_ppvRootSignature = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateConstantBufferView(void *self, const D3D12_CONSTANT_BUFFER_VIEW_DESC * pDesc, D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    npt_async_ID3D12Device_CreateConstantBufferView(npt_com_self_ring(self),npt_com_self_id(self),pDesc,DestDescriptor);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateShaderResourceView(void *self, ID3D12Resource * pResource, const D3D12_SHADER_RESOURCE_VIEW_DESC * pDesc, D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    npt_async_ID3D12Device_CreateShaderResourceView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,DestDescriptor);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateUnorderedAccessView(void *self, ID3D12Resource * pResource, ID3D12Resource * pCounterResource, const D3D12_UNORDERED_ACCESS_VIEW_DESC * pDesc, D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    npt_async_ID3D12Device_CreateUnorderedAccessView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pCounterResource,pDesc,DestDescriptor);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateRenderTargetView(void *self, ID3D12Resource * pResource, const D3D12_RENDER_TARGET_VIEW_DESC * pDesc, D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    npt_async_ID3D12Device_CreateRenderTargetView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,DestDescriptor);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateDepthStencilView(void *self, ID3D12Resource * pResource, const D3D12_DEPTH_STENCIL_VIEW_DESC * pDesc, D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    npt_async_ID3D12Device_CreateDepthStencilView(npt_com_self_ring(self),npt_com_self_id(self),pResource,pDesc,DestDescriptor);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateSampler(void *self, const D3D12_SAMPLER_DESC * pDesc, D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    npt_async_ID3D12Device_CreateSampler(npt_com_self_ring(self),npt_com_self_id(self),pDesc,DestDescriptor);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CopyDescriptors(void *self, UINT NumDestDescriptorRanges, const D3D12_CPU_DESCRIPTOR_HANDLE * pDestDescriptorRangeStarts, const UINT * pDestDescriptorRangeSizes, UINT NumSrcDescriptorRanges, const D3D12_CPU_DESCRIPTOR_HANDLE * pSrcDescriptorRangeStarts, const UINT * pSrcDescriptorRangeSizes, D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    npt_async_ID3D12Device_CopyDescriptors(npt_com_self_ring(self),npt_com_self_id(self),NumDestDescriptorRanges,pDestDescriptorRangeStarts,pDestDescriptorRangeSizes,NumSrcDescriptorRanges,pSrcDescriptorRangeStarts,pSrcDescriptorRangeSizes,DescriptorHeapsType);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CopyDescriptorsSimple(void *self, UINT NumDescriptors, D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptorRangeStart, D3D12_CPU_DESCRIPTOR_HANDLE SrcDescriptorRangeStart, D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType)
{
    npt_async_ID3D12Device_CopyDescriptorsSimple(npt_com_self_ring(self),npt_com_self_id(self),NumDescriptors,DestDescriptorRangeStart,SrcDescriptorRangeStart,DescriptorHeapsType);
}

D3D12_RESOURCE_ALLOCATION_INFO * NPT_STDMETHODCALLTYPE
npt_id3d12device_default_GetResourceAllocationInfo(void *self, D3D12_RESOURCE_ALLOCATION_INFO *_ret_out, UINT visibleMask, UINT numResourceDescs, const D3D12_RESOURCE_DESC * pResourceDescs)
{
    D3D12_RESOURCE_ALLOCATION_INFO _ret = npt_call_ID3D12Device_GetResourceAllocationInfo(npt_com_self_ring(self),npt_com_self_id(self),visibleMask,numResourceDescs,pResourceDescs);
    /* COM x64 aggregate-return ABI: copy into the caller's hidden
     * return slot and hand the pointer back. */
    if (_ret_out) *_ret_out = _ret;
    return _ret_out;
}

D3D12_HEAP_PROPERTIES * NPT_STDMETHODCALLTYPE
npt_id3d12device_default_GetCustomHeapProperties(void *self, D3D12_HEAP_PROPERTIES *_ret_out, UINT nodeMask, D3D12_HEAP_TYPE heapType)
{
    D3D12_HEAP_PROPERTIES _ret = npt_call_ID3D12Device_GetCustomHeapProperties(npt_com_self_ring(self),npt_com_self_id(self),nodeMask,heapType);
    /* COM x64 aggregate-return ABI: copy into the caller's hidden
     * return slot and hand the pointer back. */
    if (_ret_out) *_ret_out = _ret;
    return _ret_out;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateCommittedResource(void *self, const D3D12_HEAP_PROPERTIES * pHeapProperties, D3D12_HEAP_FLAGS HeapFlags, const D3D12_RESOURCE_DESC * pDesc, D3D12_RESOURCE_STATES InitialResourceState, const D3D12_CLEAR_VALUE * pOptimizedClearValue, const IID * riidResource, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device_CreateCommittedResource(npt_com_self_ring(self),npt_com_self_id(self),pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,riidResource,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riidResource,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateHeap(void *self, const D3D12_HEAP_DESC * pDesc, const IID * riid, void ** ppvHeap)
{
    void *_raw_ppvHeap = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvHeap = ppvHeap;
    if (ppvHeap)
        ppvHeap = (void **)&_raw_ppvHeap;
    npt_async_ID3D12Device_CreateHeap(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppvHeap);
    if (_orig_ppvHeap) {
        if (_raw_ppvHeap)
            *_orig_ppvHeap = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvHeap,
                (struct npt_com_base *)self);
        else
            *_orig_ppvHeap = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreatePlacedResource(void *self, ID3D12Heap * pHeap, UINT64 HeapOffset, const D3D12_RESOURCE_DESC * pDesc, D3D12_RESOURCE_STATES InitialState, const D3D12_CLEAR_VALUE * pOptimizedClearValue, const IID * riid, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device_CreatePlacedResource(npt_com_self_ring(self),npt_com_self_id(self),pHeap,HeapOffset,pDesc,InitialState,pOptimizedClearValue,riid,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateReservedResource(void *self, const D3D12_RESOURCE_DESC * pDesc, D3D12_RESOURCE_STATES InitialState, const D3D12_CLEAR_VALUE * pOptimizedClearValue, const IID * riid, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device_CreateReservedResource(npt_com_self_ring(self),npt_com_self_id(self),pDesc,InitialState,pOptimizedClearValue,riid,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateSharedHandle(void *self, ID3D12DeviceChild * pObject, const SECURITY_ATTRIBUTES * pAttributes, DWORD Access, const WCHAR * Name, HANDLE * pHandle)
{
    HRESULT _ret = npt_call_ID3D12Device_CreateSharedHandle(npt_com_self_ring(self),npt_com_self_id(self),pObject,pAttributes,Access,Name,pHandle);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_OpenSharedHandle(void *self, HANDLE NTHandle, const IID * riid, void ** ppvObj)
{
    void *_raw_ppvObj = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvObj = ppvObj;
    if (ppvObj)
        ppvObj = (void **)&_raw_ppvObj;
    npt_async_ID3D12Device_OpenSharedHandle(npt_com_self_ring(self),npt_com_self_id(self),NTHandle,riid,ppvObj);
    if (_orig_ppvObj) {
        if (_raw_ppvObj)
            *_orig_ppvObj = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvObj,
                (struct npt_com_base *)self);
        else
            *_orig_ppvObj = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_OpenSharedHandleByName(void *self, const WCHAR * Name, DWORD Access, HANDLE * pNTHandle)
{
    HRESULT _ret = npt_call_ID3D12Device_OpenSharedHandleByName(npt_com_self_ring(self),npt_com_self_id(self),Name,Access,pNTHandle);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_MakeResident(void *self, UINT NumObjects, ID3D12Pageable ** ppObjects)
{
    npt_async_ID3D12Device_MakeResident(npt_com_self_ring(self),npt_com_self_id(self),NumObjects,ppObjects);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_Evict(void *self, UINT NumObjects, ID3D12Pageable ** ppObjects)
{
    npt_async_ID3D12Device_Evict(npt_com_self_ring(self),npt_com_self_id(self),NumObjects,ppObjects);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateFence(void *self, UINT64 InitialValue, D3D12_FENCE_FLAGS Flags, const IID * riid, void ** ppFence)
{
    void *_raw_ppFence = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppFence = ppFence;
    if (ppFence)
        ppFence = (void **)&_raw_ppFence;
    npt_async_ID3D12Device_CreateFence(npt_com_self_ring(self),npt_com_self_id(self),InitialValue,Flags,riid,ppFence);
    if (_orig_ppFence) {
        if (_raw_ppFence)
            *_orig_ppFence = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppFence,
                (struct npt_com_base *)self);
        else
            *_orig_ppFence = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_GetDeviceRemovedReason(void *self)
{
    npt_async_ID3D12Device_GetDeviceRemovedReason(npt_com_self_ring(self),npt_com_self_id(self));
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_GetCopyableFootprints(void *self, const D3D12_RESOURCE_DESC * pResourceDesc, UINT FirstSubresource, UINT NumSubresources, UINT64 BaseOffset, D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts, UINT * pNumRows, UINT64 * pRowSizeInBytes, UINT64 * pTotalBytes)
{
    npt_call_ID3D12Device_GetCopyableFootprints(npt_com_self_ring(self),npt_com_self_id(self),pResourceDesc,FirstSubresource,NumSubresources,BaseOffset,pLayouts,pNumRows,pRowSizeInBytes,pTotalBytes);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateQueryHeap(void *self, const D3D12_QUERY_HEAP_DESC * pDesc, const IID * riid, void ** ppvHeap)
{
    void *_raw_ppvHeap = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvHeap = ppvHeap;
    if (ppvHeap)
        ppvHeap = (void **)&_raw_ppvHeap;
    npt_async_ID3D12Device_CreateQueryHeap(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppvHeap);
    if (_orig_ppvHeap) {
        if (_raw_ppvHeap)
            *_orig_ppvHeap = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvHeap,
                (struct npt_com_base *)self);
        else
            *_orig_ppvHeap = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_SetStablePowerState(void *self, BOOL Enable)
{
    npt_async_ID3D12Device_SetStablePowerState(npt_com_self_ring(self),npt_com_self_id(self),Enable);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device_default_CreateCommandSignature(void *self, const D3D12_COMMAND_SIGNATURE_DESC * pDesc, ID3D12RootSignature * pRootSignature, const IID * riid, void ** ppvCommandSignature)
{
    void *_raw_ppvCommandSignature = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvCommandSignature = ppvCommandSignature;
    if (ppvCommandSignature)
        ppvCommandSignature = (void **)&_raw_ppvCommandSignature;
    npt_async_ID3D12Device_CreateCommandSignature(npt_com_self_ring(self),npt_com_self_id(self),pDesc,pRootSignature,riid,ppvCommandSignature);
    if (_orig_ppvCommandSignature) {
        if (_raw_ppvCommandSignature)
            *_orig_ppvCommandSignature = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvCommandSignature,
                (struct npt_com_base *)self);
        else
            *_orig_ppvCommandSignature = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device_default_GetResourceTiling(void *self, ID3D12Resource * pTiledResource, UINT * pNumTilesForEntireResource, D3D12_PACKED_MIP_INFO * pPackedMipDesc, D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips, UINT * pNumSubresourceTilings, UINT FirstSubresourceTilingToGet, D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips)
{
    npt_call_ID3D12Device_GetResourceTiling(npt_com_self_ring(self),npt_com_self_id(self),pTiledResource,pNumTilesForEntireResource,pPackedMipDesc,pStandardTileShapeForNonPackedMips,pNumSubresourceTilings,FirstSubresourceTilingToGet,pSubresourceTilingsForNonPackedMips);
}

LUID * NPT_STDMETHODCALLTYPE
npt_id3d12device_default_GetAdapterLuid(void *self, LUID *_ret_out)
{
    LUID _ret = npt_call_ID3D12Device_GetAdapterLuid(npt_com_self_ring(self),npt_com_self_id(self));
    /* COM x64 aggregate-return ABI: copy into the caller's hidden
     * return slot and hand the pointer back. */
    if (_ret_out) *_ret_out = _ret;
    return _ret_out;
}


void
npt_id3d12device_fill_default_vtbl(struct npt_id3d12device_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device_default_QueryInterface;
    dst->AddRef         = npt_id3d12device_default_AddRef;
    dst->Release        = npt_id3d12device_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device_client_vtbl npt_id3d12device_default_vtbl_storage;

void
npt_id3d12device_install_default_vtbl(void)
{
    npt_id3d12device_fill_default_vtbl(&npt_id3d12device_default_vtbl_storage);
}

void *
npt_id3d12device_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device1                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device1_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device1_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device1_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device1_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device1_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device1_default_CreatePipelineLibrary(void *self, const void * pLibraryBlob, SIZE_T BlobLength, const IID * riid, void ** ppPipelineLibrary)
{
    void *_raw_ppPipelineLibrary = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppPipelineLibrary = ppPipelineLibrary;
    if (ppPipelineLibrary)
        ppPipelineLibrary = (void **)&_raw_ppPipelineLibrary;
    npt_async_ID3D12Device1_CreatePipelineLibrary(npt_com_self_ring(self),npt_com_self_id(self),pLibraryBlob,BlobLength,riid,ppPipelineLibrary);
    if (_orig_ppPipelineLibrary) {
        if (_raw_ppPipelineLibrary)
            *_orig_ppPipelineLibrary = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppPipelineLibrary,
                (struct npt_com_base *)self);
        else
            *_orig_ppPipelineLibrary = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device1_default_SetEventOnMultipleFenceCompletion(void *self, ID3D12Fence ** ppFences, const UINT64 * pFenceValues, UINT NumFences, D3D12_MULTIPLE_FENCE_WAIT_FLAGS Flags, HANDLE hEvent)
{
    npt_async_ID3D12Device1_SetEventOnMultipleFenceCompletion(npt_com_self_ring(self),npt_com_self_id(self),ppFences,pFenceValues,NumFences,Flags,hEvent);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device1_default_SetResidencyPriority(void *self, UINT NumObjects, ID3D12Pageable ** ppObjects, const D3D12_RESIDENCY_PRIORITY * pPriorities)
{
    npt_async_ID3D12Device1_SetResidencyPriority(npt_com_self_ring(self),npt_com_self_id(self),NumObjects,ppObjects,pPriorities);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d12device1_fill_default_vtbl(struct npt_id3d12device1_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device1_default_QueryInterface;
    dst->AddRef         = npt_id3d12device1_default_AddRef;
    dst->Release        = npt_id3d12device1_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device1_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device1_client_vtbl npt_id3d12device1_default_vtbl_storage;

void
npt_id3d12device1_install_default_vtbl(void)
{
    npt_id3d12device1_fill_default_vtbl(&npt_id3d12device1_default_vtbl_storage);
}

void *
npt_id3d12device1_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device1_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device2                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device2_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device2_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device2_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device2_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device2_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device2_default_CreatePipelineState(void *self, const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc, const IID * riid, void ** ppPipelineState)
{
    void *_raw_ppPipelineState = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppPipelineState = ppPipelineState;
    if (ppPipelineState)
        ppPipelineState = (void **)&_raw_ppPipelineState;
    npt_async_ID3D12Device2_CreatePipelineState(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppPipelineState);
    if (_orig_ppPipelineState) {
        if (_raw_ppPipelineState)
            *_orig_ppPipelineState = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppPipelineState,
                (struct npt_com_base *)self);
        else
            *_orig_ppPipelineState = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d12device2_fill_default_vtbl(struct npt_id3d12device2_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device2_default_QueryInterface;
    dst->AddRef         = npt_id3d12device2_default_AddRef;
    dst->Release        = npt_id3d12device2_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device2_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device2_client_vtbl npt_id3d12device2_default_vtbl_storage;

void
npt_id3d12device2_install_default_vtbl(void)
{
    npt_id3d12device2_fill_default_vtbl(&npt_id3d12device2_default_vtbl_storage);
}

void *
npt_id3d12device2_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device2_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device3                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device3_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device3_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device3_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device3_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device3_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device3_default_OpenExistingHeapFromAddress(void *self, const void * pAddress, const IID * riid, void ** ppvHeap)
{
    void *_raw_ppvHeap = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvHeap = ppvHeap;
    if (ppvHeap)
        ppvHeap = (void **)&_raw_ppvHeap;
    npt_async_ID3D12Device3_OpenExistingHeapFromAddress(npt_com_self_ring(self),npt_com_self_id(self),pAddress,riid,ppvHeap);
    if (_orig_ppvHeap) {
        if (_raw_ppvHeap)
            *_orig_ppvHeap = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvHeap,
                (struct npt_com_base *)self);
        else
            *_orig_ppvHeap = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device3_default_OpenExistingHeapFromFileMapping(void *self, HANDLE hFileMapping, const IID * riid, void ** ppvHeap)
{
    void *_raw_ppvHeap = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvHeap = ppvHeap;
    if (ppvHeap)
        ppvHeap = (void **)&_raw_ppvHeap;
    npt_async_ID3D12Device3_OpenExistingHeapFromFileMapping(npt_com_self_ring(self),npt_com_self_id(self),hFileMapping,riid,ppvHeap);
    if (_orig_ppvHeap) {
        if (_raw_ppvHeap)
            *_orig_ppvHeap = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvHeap,
                (struct npt_com_base *)self);
        else
            *_orig_ppvHeap = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device3_default_EnqueueMakeResident(void *self, D3D12_RESIDENCY_FLAGS Flags, UINT NumObjects, ID3D12Pageable ** ppObjects, ID3D12Fence * pFenceToSignal, UINT64 FenceValueToSignal)
{
    npt_async_ID3D12Device3_EnqueueMakeResident(npt_com_self_ring(self),npt_com_self_id(self),Flags,NumObjects,ppObjects,pFenceToSignal,FenceValueToSignal);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d12device3_fill_default_vtbl(struct npt_id3d12device3_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device3_default_QueryInterface;
    dst->AddRef         = npt_id3d12device3_default_AddRef;
    dst->Release        = npt_id3d12device3_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device3_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device3_client_vtbl npt_id3d12device3_default_vtbl_storage;

void
npt_id3d12device3_install_default_vtbl(void)
{
    npt_id3d12device3_fill_default_vtbl(&npt_id3d12device3_default_vtbl_storage);
}

void *
npt_id3d12device3_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device3_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device4                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device4_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device4_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device4_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device4_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device4_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device4_default_CreateCommandList1(void *self, UINT nodeMask, D3D12_COMMAND_LIST_TYPE type, D3D12_COMMAND_LIST_FLAGS flags, const IID * riid, void ** ppCommandList)
{
    void *_raw_ppCommandList = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppCommandList = ppCommandList;
    if (ppCommandList)
        ppCommandList = (void **)&_raw_ppCommandList;
    npt_async_ID3D12Device4_CreateCommandList1(npt_com_self_ring(self),npt_com_self_id(self),nodeMask,type,flags,riid,ppCommandList);
    if (_orig_ppCommandList) {
        if (_raw_ppCommandList)
            *_orig_ppCommandList = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppCommandList,
                (struct npt_com_base *)self);
        else
            *_orig_ppCommandList = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device4_default_CreateProtectedResourceSession(void *self, const D3D12_PROTECTED_RESOURCE_SESSION_DESC * pDesc, const IID * riid, void ** ppSession)
{
    void *_raw_ppSession = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppSession = ppSession;
    if (ppSession)
        ppSession = (void **)&_raw_ppSession;
    npt_async_ID3D12Device4_CreateProtectedResourceSession(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppSession);
    if (_orig_ppSession) {
        if (_raw_ppSession)
            *_orig_ppSession = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppSession,
                (struct npt_com_base *)self);
        else
            *_orig_ppSession = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device4_default_CreateCommittedResource1(void *self, const D3D12_HEAP_PROPERTIES * pHeapProperties, D3D12_HEAP_FLAGS HeapFlags, const D3D12_RESOURCE_DESC * pDesc, D3D12_RESOURCE_STATES InitialResourceState, const D3D12_CLEAR_VALUE * pOptimizedClearValue, ID3D12ProtectedResourceSession * pProtectedSession, const IID * riidResource, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device4_CreateCommittedResource1(npt_com_self_ring(self),npt_com_self_id(self),pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,pProtectedSession,riidResource,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riidResource,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device4_default_CreateHeap1(void *self, const D3D12_HEAP_DESC * pDesc, ID3D12ProtectedResourceSession * pProtectedSession, const IID * riid, void ** ppvHeap)
{
    void *_raw_ppvHeap = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvHeap = ppvHeap;
    if (ppvHeap)
        ppvHeap = (void **)&_raw_ppvHeap;
    npt_async_ID3D12Device4_CreateHeap1(npt_com_self_ring(self),npt_com_self_id(self),pDesc,pProtectedSession,riid,ppvHeap);
    if (_orig_ppvHeap) {
        if (_raw_ppvHeap)
            *_orig_ppvHeap = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvHeap,
                (struct npt_com_base *)self);
        else
            *_orig_ppvHeap = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device4_default_CreateReservedResource1(void *self, const D3D12_RESOURCE_DESC * pDesc, D3D12_RESOURCE_STATES InitialState, const D3D12_CLEAR_VALUE * pOptimizedClearValue, ID3D12ProtectedResourceSession * pProtectedSession, const IID * riid, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device4_CreateReservedResource1(npt_com_self_ring(self),npt_com_self_id(self),pDesc,InitialState,pOptimizedClearValue,pProtectedSession,riid,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

D3D12_RESOURCE_ALLOCATION_INFO * NPT_STDMETHODCALLTYPE
npt_id3d12device4_default_GetResourceAllocationInfo1(void *self, D3D12_RESOURCE_ALLOCATION_INFO *_ret_out, UINT visibleMask, UINT numResourceDescs, const D3D12_RESOURCE_DESC * pResourceDescs, D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    D3D12_RESOURCE_ALLOCATION_INFO _ret = npt_call_ID3D12Device4_GetResourceAllocationInfo1(npt_com_self_ring(self),npt_com_self_id(self),visibleMask,numResourceDescs,pResourceDescs,pResourceAllocationInfo1);
    /* COM x64 aggregate-return ABI: copy into the caller's hidden
     * return slot and hand the pointer back. */
    if (_ret_out) *_ret_out = _ret;
    return _ret_out;
}


void
npt_id3d12device4_fill_default_vtbl(struct npt_id3d12device4_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device4_default_QueryInterface;
    dst->AddRef         = npt_id3d12device4_default_AddRef;
    dst->Release        = npt_id3d12device4_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device4_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device4_client_vtbl npt_id3d12device4_default_vtbl_storage;

void
npt_id3d12device4_install_default_vtbl(void)
{
    npt_id3d12device4_fill_default_vtbl(&npt_id3d12device4_default_vtbl_storage);
}

void *
npt_id3d12device4_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device4_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device5                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device5_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device5_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_CreateLifetimeTracker(void *self, ID3D12LifetimeOwner * pOwner, const IID * riid, void ** ppvTracker)
{
    void *_raw_ppvTracker = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvTracker = ppvTracker;
    if (ppvTracker)
        ppvTracker = (void **)&_raw_ppvTracker;
    npt_async_ID3D12Device5_CreateLifetimeTracker(npt_com_self_ring(self),npt_com_self_id(self),pOwner,riid,ppvTracker);
    if (_orig_ppvTracker) {
        if (_raw_ppvTracker)
            *_orig_ppvTracker = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvTracker,
                (struct npt_com_base *)self);
        else
            *_orig_ppvTracker = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_RemoveDevice(void *self)
{
    npt_async_ID3D12Device5_RemoveDevice(npt_com_self_ring(self),npt_com_self_id(self));
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_EnumerateMetaCommands(void *self, UINT * pNumMetaCommands, D3D12_META_COMMAND_DESC * pDescs)
{
    HRESULT _ret = npt_call_ID3D12Device5_EnumerateMetaCommands(npt_com_self_ring(self),npt_com_self_id(self),pNumMetaCommands,pDescs);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_EnumerateMetaCommandParameters(void *self, const GUID * CommandId, D3D12_META_COMMAND_PARAMETER_STAGE Stage, UINT * pTotalStructureSizeInBytes, UINT * pParameterCount, D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs)
{
    HRESULT _ret = npt_call_ID3D12Device5_EnumerateMetaCommandParameters(npt_com_self_ring(self),npt_com_self_id(self),CommandId,Stage,pTotalStructureSizeInBytes,pParameterCount,pParameterDescs);
    return _ret;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_CreateMetaCommand(void *self, const GUID * CommandId, UINT NodeMask, const void * pCreationParametersData, SIZE_T CreationParametersDataSizeInBytes, const IID * riid, void ** ppMetaCommand)
{
    void *_raw_ppMetaCommand = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppMetaCommand = ppMetaCommand;
    if (ppMetaCommand)
        ppMetaCommand = (void **)&_raw_ppMetaCommand;
    npt_async_ID3D12Device5_CreateMetaCommand(npt_com_self_ring(self),npt_com_self_id(self),CommandId,NodeMask,pCreationParametersData,CreationParametersDataSizeInBytes,riid,ppMetaCommand);
    if (_orig_ppMetaCommand) {
        if (_raw_ppMetaCommand)
            *_orig_ppMetaCommand = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppMetaCommand,
                (struct npt_com_base *)self);
        else
            *_orig_ppMetaCommand = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_CreateStateObject(void *self, const D3D12_STATE_OBJECT_DESC * pDesc, const IID * riid, void ** ppStateObject)
{
    void *_raw_ppStateObject = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppStateObject = ppStateObject;
    if (ppStateObject)
        ppStateObject = (void **)&_raw_ppStateObject;
    npt_async_ID3D12Device5_CreateStateObject(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppStateObject);
    if (_orig_ppStateObject) {
        if (_raw_ppStateObject)
            *_orig_ppStateObject = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppStateObject,
                (struct npt_com_base *)self);
        else
            *_orig_ppStateObject = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo(void *self, const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS * pDesc, D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo)
{
    npt_call_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(npt_com_self_ring(self),npt_com_self_id(self),pDesc,pInfo);
}

D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS NPT_STDMETHODCALLTYPE
npt_id3d12device5_default_CheckDriverMatchingIdentifier(void *self, D3D12_SERIALIZED_DATA_TYPE SerializedDataType, const D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER * pIdentifierToCheck)
{
    D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS _ret = npt_call_ID3D12Device5_CheckDriverMatchingIdentifier(npt_com_self_ring(self),npt_com_self_id(self),SerializedDataType,pIdentifierToCheck);
    return _ret;
}


void
npt_id3d12device5_fill_default_vtbl(struct npt_id3d12device5_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device5_default_QueryInterface;
    dst->AddRef         = npt_id3d12device5_default_AddRef;
    dst->Release        = npt_id3d12device5_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device5_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device5_client_vtbl npt_id3d12device5_default_vtbl_storage;

void
npt_id3d12device5_install_default_vtbl(void)
{
    npt_id3d12device5_fill_default_vtbl(&npt_id3d12device5_default_vtbl_storage);
}

void *
npt_id3d12device5_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device5_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device6                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device6_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    &NPT_IID_ID3D12Device6,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device6_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device6_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device6_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device6_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device6_default_SetBackgroundProcessingMode(void *self, D3D12_BACKGROUND_PROCESSING_MODE Mode, D3D12_MEASUREMENTS_ACTION MeasurementsAction, HANDLE hEventToSignalUponCompletion, BOOL * pbFurtherMeasurementsDesired)
{
    HRESULT _ret = npt_call_ID3D12Device6_SetBackgroundProcessingMode(npt_com_self_ring(self),npt_com_self_id(self),Mode,MeasurementsAction,hEventToSignalUponCompletion,pbFurtherMeasurementsDesired);
    return _ret;
}


void
npt_id3d12device6_fill_default_vtbl(struct npt_id3d12device6_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device6_default_QueryInterface;
    dst->AddRef         = npt_id3d12device6_default_AddRef;
    dst->Release        = npt_id3d12device6_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
    dst->SetBackgroundProcessingMode = npt_id3d12device6_default_SetBackgroundProcessingMode;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device6_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device6_client_vtbl npt_id3d12device6_default_vtbl_storage;

void
npt_id3d12device6_install_default_vtbl(void)
{
    npt_id3d12device6_fill_default_vtbl(&npt_id3d12device6_default_vtbl_storage);
}

void *
npt_id3d12device6_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device6_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device7                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device7_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    &NPT_IID_ID3D12Device6,
    &NPT_IID_ID3D12Device7,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device7_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device7_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device7_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device7_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device7_default_AddToStateObject(void *self, const D3D12_STATE_OBJECT_DESC * pAddition, ID3D12StateObject * pStateObjectToGrowFrom, const IID * riid, void ** ppNewStateObject)
{
    void *_raw_ppNewStateObject = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppNewStateObject = ppNewStateObject;
    if (ppNewStateObject)
        ppNewStateObject = (void **)&_raw_ppNewStateObject;
    npt_async_ID3D12Device7_AddToStateObject(npt_com_self_ring(self),npt_com_self_id(self),pAddition,pStateObjectToGrowFrom,riid,ppNewStateObject);
    if (_orig_ppNewStateObject) {
        if (_raw_ppNewStateObject)
            *_orig_ppNewStateObject = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppNewStateObject,
                (struct npt_com_base *)self);
        else
            *_orig_ppNewStateObject = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device7_default_CreateProtectedResourceSession1(void *self, const D3D12_PROTECTED_RESOURCE_SESSION_DESC1 * pDesc, const IID * riid, void ** ppSession)
{
    void *_raw_ppSession = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppSession = ppSession;
    if (ppSession)
        ppSession = (void **)&_raw_ppSession;
    npt_async_ID3D12Device7_CreateProtectedResourceSession1(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppSession);
    if (_orig_ppSession) {
        if (_raw_ppSession)
            *_orig_ppSession = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppSession,
                (struct npt_com_base *)self);
        else
            *_orig_ppSession = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d12device7_fill_default_vtbl(struct npt_id3d12device7_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device7_default_QueryInterface;
    dst->AddRef         = npt_id3d12device7_default_AddRef;
    dst->Release        = npt_id3d12device7_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
    dst->SetBackgroundProcessingMode = npt_id3d12device6_default_SetBackgroundProcessingMode;
    dst->AddToStateObject = npt_id3d12device7_default_AddToStateObject;
    dst->CreateProtectedResourceSession1 = npt_id3d12device7_default_CreateProtectedResourceSession1;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device7_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device7_client_vtbl npt_id3d12device7_default_vtbl_storage;

void
npt_id3d12device7_install_default_vtbl(void)
{
    npt_id3d12device7_fill_default_vtbl(&npt_id3d12device7_default_vtbl_storage);
}

void *
npt_id3d12device7_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device7_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device8                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device8_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    &NPT_IID_ID3D12Device6,
    &NPT_IID_ID3D12Device7,
    &NPT_IID_ID3D12Device8,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device8_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device8_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device8_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device8_default_Release(void *self)
{
    return npt_com_default_release(self);
}

D3D12_RESOURCE_ALLOCATION_INFO * NPT_STDMETHODCALLTYPE
npt_id3d12device8_default_GetResourceAllocationInfo2(void *self, D3D12_RESOURCE_ALLOCATION_INFO *_ret_out, UINT visibleMask, UINT numResourceDescs, const D3D12_RESOURCE_DESC1 * pResourceDescs, D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    D3D12_RESOURCE_ALLOCATION_INFO _ret = npt_call_ID3D12Device8_GetResourceAllocationInfo2(npt_com_self_ring(self),npt_com_self_id(self),visibleMask,numResourceDescs,pResourceDescs,pResourceAllocationInfo1);
    /* COM x64 aggregate-return ABI: copy into the caller's hidden
     * return slot and hand the pointer back. */
    if (_ret_out) *_ret_out = _ret;
    return _ret_out;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device8_default_CreateCommittedResource2(void *self, const D3D12_HEAP_PROPERTIES * pHeapProperties, D3D12_HEAP_FLAGS HeapFlags, const D3D12_RESOURCE_DESC1 * pDesc, D3D12_RESOURCE_STATES InitialResourceState, const D3D12_CLEAR_VALUE * pOptimizedClearValue, ID3D12ProtectedResourceSession * pProtectedSession, const IID * riidResource, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device8_CreateCommittedResource2(npt_com_self_ring(self),npt_com_self_id(self),pHeapProperties,HeapFlags,pDesc,InitialResourceState,pOptimizedClearValue,pProtectedSession,riidResource,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riidResource,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device8_default_CreatePlacedResource1(void *self, ID3D12Heap * pHeap, UINT64 HeapOffset, const D3D12_RESOURCE_DESC1 * pDesc, D3D12_RESOURCE_STATES InitialState, const D3D12_CLEAR_VALUE * pOptimizedClearValue, const IID * riid, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device8_CreatePlacedResource1(npt_com_self_ring(self),npt_com_self_id(self),pHeap,HeapOffset,pDesc,InitialState,pOptimizedClearValue,riid,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device8_default_CreateSamplerFeedbackUnorderedAccessView(void *self, ID3D12Resource * pTargetedResource, ID3D12Resource * pFeedbackResource, D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    npt_async_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(npt_com_self_ring(self),npt_com_self_id(self),pTargetedResource,pFeedbackResource,DestDescriptor);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device8_default_GetCopyableFootprints1(void *self, const D3D12_RESOURCE_DESC1 * pResourceDesc, UINT FirstSubresource, UINT NumSubresources, UINT64 BaseOffset, D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts, UINT * pNumRows, UINT64 * pRowSizeInBytes, UINT64 * pTotalBytes)
{
    npt_call_ID3D12Device8_GetCopyableFootprints1(npt_com_self_ring(self),npt_com_self_id(self),pResourceDesc,FirstSubresource,NumSubresources,BaseOffset,pLayouts,pNumRows,pRowSizeInBytes,pTotalBytes);
}


void
npt_id3d12device8_fill_default_vtbl(struct npt_id3d12device8_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device8_default_QueryInterface;
    dst->AddRef         = npt_id3d12device8_default_AddRef;
    dst->Release        = npt_id3d12device8_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
    dst->SetBackgroundProcessingMode = npt_id3d12device6_default_SetBackgroundProcessingMode;
    dst->AddToStateObject = npt_id3d12device7_default_AddToStateObject;
    dst->CreateProtectedResourceSession1 = npt_id3d12device7_default_CreateProtectedResourceSession1;
    dst->GetResourceAllocationInfo2 = npt_id3d12device8_default_GetResourceAllocationInfo2;
    dst->CreateCommittedResource2 = npt_id3d12device8_default_CreateCommittedResource2;
    dst->CreatePlacedResource1 = npt_id3d12device8_default_CreatePlacedResource1;
    dst->CreateSamplerFeedbackUnorderedAccessView = npt_id3d12device8_default_CreateSamplerFeedbackUnorderedAccessView;
    dst->GetCopyableFootprints1 = npt_id3d12device8_default_GetCopyableFootprints1;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device8_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device8_client_vtbl npt_id3d12device8_default_vtbl_storage;

void
npt_id3d12device8_install_default_vtbl(void)
{
    npt_id3d12device8_fill_default_vtbl(&npt_id3d12device8_default_vtbl_storage);
}

void *
npt_id3d12device8_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device8_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device9                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device9_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    &NPT_IID_ID3D12Device6,
    &NPT_IID_ID3D12Device7,
    &NPT_IID_ID3D12Device8,
    &NPT_IID_ID3D12Device9,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device9_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device9_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device9_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device9_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device9_default_CreateShaderCacheSession(void *self, const D3D12_SHADER_CACHE_SESSION_DESC * pDesc, const IID * riid, void ** ppvSession)
{
    void *_raw_ppvSession = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvSession = ppvSession;
    if (ppvSession)
        ppvSession = (void **)&_raw_ppvSession;
    npt_async_ID3D12Device9_CreateShaderCacheSession(npt_com_self_ring(self),npt_com_self_id(self),pDesc,riid,ppvSession);
    if (_orig_ppvSession) {
        if (_raw_ppvSession)
            *_orig_ppvSession = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvSession,
                (struct npt_com_base *)self);
        else
            *_orig_ppvSession = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device9_default_ShaderCacheControl(void *self, D3D12_SHADER_CACHE_KIND_FLAGS Kinds, D3D12_SHADER_CACHE_CONTROL_FLAGS Control)
{
    npt_async_ID3D12Device9_ShaderCacheControl(npt_com_self_ring(self),npt_com_self_id(self),Kinds,Control);
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device9_default_CreateCommandQueue1(void *self, const D3D12_COMMAND_QUEUE_DESC * pDesc, const IID * CreatorID, const IID * riid, void ** ppCommandQueue)
{
    void *_raw_ppCommandQueue = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppCommandQueue = ppCommandQueue;
    if (ppCommandQueue)
        ppCommandQueue = (void **)&_raw_ppCommandQueue;
    npt_async_ID3D12Device9_CreateCommandQueue1(npt_com_self_ring(self),npt_com_self_id(self),pDesc,CreatorID,riid,ppCommandQueue);
    if (_orig_ppCommandQueue) {
        if (_raw_ppCommandQueue)
            *_orig_ppCommandQueue = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                CreatorID,
                (uint64_t)(uintptr_t)_raw_ppCommandQueue,
                (struct npt_com_base *)self);
        else
            *_orig_ppCommandQueue = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d12device9_fill_default_vtbl(struct npt_id3d12device9_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device9_default_QueryInterface;
    dst->AddRef         = npt_id3d12device9_default_AddRef;
    dst->Release        = npt_id3d12device9_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
    dst->SetBackgroundProcessingMode = npt_id3d12device6_default_SetBackgroundProcessingMode;
    dst->AddToStateObject = npt_id3d12device7_default_AddToStateObject;
    dst->CreateProtectedResourceSession1 = npt_id3d12device7_default_CreateProtectedResourceSession1;
    dst->GetResourceAllocationInfo2 = npt_id3d12device8_default_GetResourceAllocationInfo2;
    dst->CreateCommittedResource2 = npt_id3d12device8_default_CreateCommittedResource2;
    dst->CreatePlacedResource1 = npt_id3d12device8_default_CreatePlacedResource1;
    dst->CreateSamplerFeedbackUnorderedAccessView = npt_id3d12device8_default_CreateSamplerFeedbackUnorderedAccessView;
    dst->GetCopyableFootprints1 = npt_id3d12device8_default_GetCopyableFootprints1;
    dst->CreateShaderCacheSession = npt_id3d12device9_default_CreateShaderCacheSession;
    dst->ShaderCacheControl = npt_id3d12device9_default_ShaderCacheControl;
    dst->CreateCommandQueue1 = npt_id3d12device9_default_CreateCommandQueue1;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device9_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device9_client_vtbl npt_id3d12device9_default_vtbl_storage;

void
npt_id3d12device9_install_default_vtbl(void)
{
    npt_id3d12device9_fill_default_vtbl(&npt_id3d12device9_default_vtbl_storage);
}

void *
npt_id3d12device9_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device9_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device10                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device10_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    &NPT_IID_ID3D12Device6,
    &NPT_IID_ID3D12Device7,
    &NPT_IID_ID3D12Device8,
    &NPT_IID_ID3D12Device9,
    &NPT_IID_ID3D12Device10,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device10_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device10_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device10_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device10_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device10_default_CreateCommittedResource3(void *self, const D3D12_HEAP_PROPERTIES * pHeapProperties, D3D12_HEAP_FLAGS HeapFlags, const D3D12_RESOURCE_DESC1 * pDesc, D3D12_BARRIER_LAYOUT InitialLayout, const D3D12_CLEAR_VALUE * pOptimizedClearValue, ID3D12ProtectedResourceSession * pProtectedSession, UINT32 NumCastableFormats, const DXGI_FORMAT * pCastableFormats, const IID * riidResource, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device10_CreateCommittedResource3(npt_com_self_ring(self),npt_com_self_id(self),pHeapProperties,HeapFlags,pDesc,InitialLayout,pOptimizedClearValue,pProtectedSession,NumCastableFormats,pCastableFormats,riidResource,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riidResource,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device10_default_CreatePlacedResource2(void *self, ID3D12Heap * pHeap, UINT64 HeapOffset, const D3D12_RESOURCE_DESC1 * pDesc, D3D12_BARRIER_LAYOUT InitialLayout, const D3D12_CLEAR_VALUE * pOptimizedClearValue, UINT32 NumCastableFormats, const DXGI_FORMAT * pCastableFormats, const IID * riid, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device10_CreatePlacedResource2(npt_com_self_ring(self),npt_com_self_id(self),pHeap,HeapOffset,pDesc,InitialLayout,pOptimizedClearValue,NumCastableFormats,pCastableFormats,riid,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device10_default_CreateReservedResource2(void *self, const D3D12_RESOURCE_DESC * pDesc, D3D12_BARRIER_LAYOUT InitialLayout, const D3D12_CLEAR_VALUE * pOptimizedClearValue, ID3D12ProtectedResourceSession * pProtectedSession, UINT32 NumCastableFormats, const DXGI_FORMAT * pCastableFormats, const IID * riid, void ** ppvResource)
{
    void *_raw_ppvResource = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvResource = ppvResource;
    if (ppvResource)
        ppvResource = (void **)&_raw_ppvResource;
    npt_async_ID3D12Device10_CreateReservedResource2(npt_com_self_ring(self),npt_com_self_id(self),pDesc,InitialLayout,pOptimizedClearValue,pProtectedSession,NumCastableFormats,pCastableFormats,riid,ppvResource);
    if (_orig_ppvResource) {
        if (_raw_ppvResource)
            *_orig_ppvResource = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvResource,
                (struct npt_com_base *)self);
        else
            *_orig_ppvResource = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d12device10_fill_default_vtbl(struct npt_id3d12device10_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device10_default_QueryInterface;
    dst->AddRef         = npt_id3d12device10_default_AddRef;
    dst->Release        = npt_id3d12device10_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
    dst->SetBackgroundProcessingMode = npt_id3d12device6_default_SetBackgroundProcessingMode;
    dst->AddToStateObject = npt_id3d12device7_default_AddToStateObject;
    dst->CreateProtectedResourceSession1 = npt_id3d12device7_default_CreateProtectedResourceSession1;
    dst->GetResourceAllocationInfo2 = npt_id3d12device8_default_GetResourceAllocationInfo2;
    dst->CreateCommittedResource2 = npt_id3d12device8_default_CreateCommittedResource2;
    dst->CreatePlacedResource1 = npt_id3d12device8_default_CreatePlacedResource1;
    dst->CreateSamplerFeedbackUnorderedAccessView = npt_id3d12device8_default_CreateSamplerFeedbackUnorderedAccessView;
    dst->GetCopyableFootprints1 = npt_id3d12device8_default_GetCopyableFootprints1;
    dst->CreateShaderCacheSession = npt_id3d12device9_default_CreateShaderCacheSession;
    dst->ShaderCacheControl = npt_id3d12device9_default_ShaderCacheControl;
    dst->CreateCommandQueue1 = npt_id3d12device9_default_CreateCommandQueue1;
    dst->CreateCommittedResource3 = npt_id3d12device10_default_CreateCommittedResource3;
    dst->CreatePlacedResource2 = npt_id3d12device10_default_CreatePlacedResource2;
    dst->CreateReservedResource2 = npt_id3d12device10_default_CreateReservedResource2;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device10_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device10_client_vtbl npt_id3d12device10_default_vtbl_storage;

void
npt_id3d12device10_install_default_vtbl(void)
{
    npt_id3d12device10_fill_default_vtbl(&npt_id3d12device10_default_vtbl_storage);
}

void *
npt_id3d12device10_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device10_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device11                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device11_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    &NPT_IID_ID3D12Device6,
    &NPT_IID_ID3D12Device7,
    &NPT_IID_ID3D12Device8,
    &NPT_IID_ID3D12Device9,
    &NPT_IID_ID3D12Device10,
    &NPT_IID_ID3D12Device11,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device11_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device11_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device11_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device11_default_Release(void *self)
{
    return npt_com_default_release(self);
}

void NPT_STDMETHODCALLTYPE
npt_id3d12device11_default_CreateSampler2(void *self, const D3D12_SAMPLER_DESC2 * pDesc, D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor)
{
    npt_async_ID3D12Device11_CreateSampler2(npt_com_self_ring(self),npt_com_self_id(self),pDesc,DestDescriptor);
}


void
npt_id3d12device11_fill_default_vtbl(struct npt_id3d12device11_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device11_default_QueryInterface;
    dst->AddRef         = npt_id3d12device11_default_AddRef;
    dst->Release        = npt_id3d12device11_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
    dst->SetBackgroundProcessingMode = npt_id3d12device6_default_SetBackgroundProcessingMode;
    dst->AddToStateObject = npt_id3d12device7_default_AddToStateObject;
    dst->CreateProtectedResourceSession1 = npt_id3d12device7_default_CreateProtectedResourceSession1;
    dst->GetResourceAllocationInfo2 = npt_id3d12device8_default_GetResourceAllocationInfo2;
    dst->CreateCommittedResource2 = npt_id3d12device8_default_CreateCommittedResource2;
    dst->CreatePlacedResource1 = npt_id3d12device8_default_CreatePlacedResource1;
    dst->CreateSamplerFeedbackUnorderedAccessView = npt_id3d12device8_default_CreateSamplerFeedbackUnorderedAccessView;
    dst->GetCopyableFootprints1 = npt_id3d12device8_default_GetCopyableFootprints1;
    dst->CreateShaderCacheSession = npt_id3d12device9_default_CreateShaderCacheSession;
    dst->ShaderCacheControl = npt_id3d12device9_default_ShaderCacheControl;
    dst->CreateCommandQueue1 = npt_id3d12device9_default_CreateCommandQueue1;
    dst->CreateCommittedResource3 = npt_id3d12device10_default_CreateCommittedResource3;
    dst->CreatePlacedResource2 = npt_id3d12device10_default_CreatePlacedResource2;
    dst->CreateReservedResource2 = npt_id3d12device10_default_CreateReservedResource2;
    dst->CreateSampler2 = npt_id3d12device11_default_CreateSampler2;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device11_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device11_client_vtbl npt_id3d12device11_default_vtbl_storage;

void
npt_id3d12device11_install_default_vtbl(void)
{
    npt_id3d12device11_fill_default_vtbl(&npt_id3d12device11_default_vtbl_storage);
}

void *
npt_id3d12device11_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device11_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device12                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device12_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    &NPT_IID_ID3D12Device6,
    &NPT_IID_ID3D12Device7,
    &NPT_IID_ID3D12Device8,
    &NPT_IID_ID3D12Device9,
    &NPT_IID_ID3D12Device10,
    &NPT_IID_ID3D12Device11,
    &NPT_IID_ID3D12Device12,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device12_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device12_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device12_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device12_default_Release(void *self)
{
    return npt_com_default_release(self);
}

D3D12_RESOURCE_ALLOCATION_INFO * NPT_STDMETHODCALLTYPE
npt_id3d12device12_default_GetResourceAllocationInfo3(void *self, D3D12_RESOURCE_ALLOCATION_INFO *_ret_out, UINT visibleMask, UINT numResourceDescs, const D3D12_RESOURCE_DESC1 * pResourceDescs, const UINT32 * pNumCastableFormats, const DXGI_FORMAT ** ppCastableFormats, D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1)
{
    D3D12_RESOURCE_ALLOCATION_INFO _ret = npt_call_ID3D12Device12_GetResourceAllocationInfo3(npt_com_self_ring(self),npt_com_self_id(self),visibleMask,numResourceDescs,pResourceDescs,pNumCastableFormats,ppCastableFormats,pResourceAllocationInfo1);
    /* COM x64 aggregate-return ABI: copy into the caller's hidden
     * return slot and hand the pointer back. */
    if (_ret_out) *_ret_out = _ret;
    return _ret_out;
}


void
npt_id3d12device12_fill_default_vtbl(struct npt_id3d12device12_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device12_default_QueryInterface;
    dst->AddRef         = npt_id3d12device12_default_AddRef;
    dst->Release        = npt_id3d12device12_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
    dst->SetBackgroundProcessingMode = npt_id3d12device6_default_SetBackgroundProcessingMode;
    dst->AddToStateObject = npt_id3d12device7_default_AddToStateObject;
    dst->CreateProtectedResourceSession1 = npt_id3d12device7_default_CreateProtectedResourceSession1;
    dst->GetResourceAllocationInfo2 = npt_id3d12device8_default_GetResourceAllocationInfo2;
    dst->CreateCommittedResource2 = npt_id3d12device8_default_CreateCommittedResource2;
    dst->CreatePlacedResource1 = npt_id3d12device8_default_CreatePlacedResource1;
    dst->CreateSamplerFeedbackUnorderedAccessView = npt_id3d12device8_default_CreateSamplerFeedbackUnorderedAccessView;
    dst->GetCopyableFootprints1 = npt_id3d12device8_default_GetCopyableFootprints1;
    dst->CreateShaderCacheSession = npt_id3d12device9_default_CreateShaderCacheSession;
    dst->ShaderCacheControl = npt_id3d12device9_default_ShaderCacheControl;
    dst->CreateCommandQueue1 = npt_id3d12device9_default_CreateCommandQueue1;
    dst->CreateCommittedResource3 = npt_id3d12device10_default_CreateCommittedResource3;
    dst->CreatePlacedResource2 = npt_id3d12device10_default_CreatePlacedResource2;
    dst->CreateReservedResource2 = npt_id3d12device10_default_CreateReservedResource2;
    dst->CreateSampler2 = npt_id3d12device11_default_CreateSampler2;
    dst->GetResourceAllocationInfo3 = npt_id3d12device12_default_GetResourceAllocationInfo3;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device12_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device12_client_vtbl npt_id3d12device12_default_vtbl_storage;

void
npt_id3d12device12_install_default_vtbl(void)
{
    npt_id3d12device12_fill_default_vtbl(&npt_id3d12device12_default_vtbl_storage);
}

void *
npt_id3d12device12_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device12_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device13                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device13_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    &NPT_IID_ID3D12Device6,
    &NPT_IID_ID3D12Device7,
    &NPT_IID_ID3D12Device8,
    &NPT_IID_ID3D12Device9,
    &NPT_IID_ID3D12Device10,
    &NPT_IID_ID3D12Device11,
    &NPT_IID_ID3D12Device12,
    &NPT_IID_ID3D12Device13,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device13_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device13_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device13_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device13_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device13_default_OpenExistingHeapFromAddress1(void *self, const void * pAddress, SIZE_T size, const IID * riid, void ** ppvHeap)
{
    void *_raw_ppvHeap = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvHeap = ppvHeap;
    if (ppvHeap)
        ppvHeap = (void **)&_raw_ppvHeap;
    npt_async_ID3D12Device13_OpenExistingHeapFromAddress1(npt_com_self_ring(self),npt_com_self_id(self),pAddress,size,riid,ppvHeap);
    if (_orig_ppvHeap) {
        if (_raw_ppvHeap)
            *_orig_ppvHeap = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvHeap,
                (struct npt_com_base *)self);
        else
            *_orig_ppvHeap = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d12device13_fill_default_vtbl(struct npt_id3d12device13_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device13_default_QueryInterface;
    dst->AddRef         = npt_id3d12device13_default_AddRef;
    dst->Release        = npt_id3d12device13_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
    dst->SetBackgroundProcessingMode = npt_id3d12device6_default_SetBackgroundProcessingMode;
    dst->AddToStateObject = npt_id3d12device7_default_AddToStateObject;
    dst->CreateProtectedResourceSession1 = npt_id3d12device7_default_CreateProtectedResourceSession1;
    dst->GetResourceAllocationInfo2 = npt_id3d12device8_default_GetResourceAllocationInfo2;
    dst->CreateCommittedResource2 = npt_id3d12device8_default_CreateCommittedResource2;
    dst->CreatePlacedResource1 = npt_id3d12device8_default_CreatePlacedResource1;
    dst->CreateSamplerFeedbackUnorderedAccessView = npt_id3d12device8_default_CreateSamplerFeedbackUnorderedAccessView;
    dst->GetCopyableFootprints1 = npt_id3d12device8_default_GetCopyableFootprints1;
    dst->CreateShaderCacheSession = npt_id3d12device9_default_CreateShaderCacheSession;
    dst->ShaderCacheControl = npt_id3d12device9_default_ShaderCacheControl;
    dst->CreateCommandQueue1 = npt_id3d12device9_default_CreateCommandQueue1;
    dst->CreateCommittedResource3 = npt_id3d12device10_default_CreateCommittedResource3;
    dst->CreatePlacedResource2 = npt_id3d12device10_default_CreatePlacedResource2;
    dst->CreateReservedResource2 = npt_id3d12device10_default_CreateReservedResource2;
    dst->CreateSampler2 = npt_id3d12device11_default_CreateSampler2;
    dst->GetResourceAllocationInfo3 = npt_id3d12device12_default_GetResourceAllocationInfo3;
    dst->OpenExistingHeapFromAddress1 = npt_id3d12device13_default_OpenExistingHeapFromAddress1;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device13_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device13_client_vtbl npt_id3d12device13_default_vtbl_storage;

void
npt_id3d12device13_install_default_vtbl(void)
{
    npt_id3d12device13_fill_default_vtbl(&npt_id3d12device13_default_vtbl_storage);
}

void *
npt_id3d12device13_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device13_default_vtbl_storage,
        dev, host_id);
    return p;
}


/* ================================================================== */
/* ID3D12Device14                                                       */
/* ================================================================== */

/* Parent IID chain for QueryInterface fast-path.  NULL-terminated list of
 * IID pointers; QI returns self for any IID in this list. */
static const GUID *const npt_id3d12device14_parent_iids[] = {
    &NPT_IID_IUnknown,
    &NPT_IID_ID3D12Object,
    &NPT_IID_ID3D12Device,
    &NPT_IID_ID3D12Device1,
    &NPT_IID_ID3D12Device2,
    &NPT_IID_ID3D12Device3,
    &NPT_IID_ID3D12Device4,
    &NPT_IID_ID3D12Device5,
    &NPT_IID_ID3D12Device6,
    &NPT_IID_ID3D12Device7,
    &NPT_IID_ID3D12Device8,
    &NPT_IID_ID3D12Device9,
    &NPT_IID_ID3D12Device10,
    &NPT_IID_ID3D12Device11,
    &NPT_IID_ID3D12Device12,
    &NPT_IID_ID3D12Device13,
    &NPT_IID_ID3D12Device14,
    NULL,
};

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device14_default_QueryInterface(void *self, REFIID riid, void **ppvObject)
{
    HRESULT hr = npt_com_default_query_interface_chain(
        self, riid, ppvObject, npt_id3d12device14_parent_iids);
    if (hr == NPT_S_OK)
        return hr;
    /* Derived/sibling IIDs are resolved against the host object, one
     * wire round trip per (object, IID) lifetime: the runtime memoizes
     * the verdict and the wrapper on `self` (npt_com.c). */
    return npt_com_query_interface_host(self, riid, ppvObject);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device14_default_AddRef(void *self)
{
    return npt_com_default_addref(self);
}

ULONG NPT_STDMETHODCALLTYPE
npt_id3d12device14_default_Release(void *self)
{
    return npt_com_default_release(self);
}

HRESULT NPT_STDMETHODCALLTYPE
npt_id3d12device14_default_CreateRootSignatureFromSubobjectInLibrary(void *self, UINT nodeMask, const void * pLibraryBlob, SIZE_T blobLengthInBytes, const WCHAR * subobjectName, const IID * riid, void ** ppvRootSignature)
{
    void *_raw_ppvRootSignature = NULL;
    /* Redirect each single-output pointer to a local _raw_ slot so the
     * encoder's stashed guest_id lands somewhere we can read after the
     * submit returns; the original caller-side pointers are restored
     * with the wrapped result at the end. */
    void **_orig_ppvRootSignature = ppvRootSignature;
    if (ppvRootSignature)
        ppvRootSignature = (void **)&_raw_ppvRootSignature;
    npt_async_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(npt_com_self_ring(self),npt_com_self_id(self),nodeMask,pLibraryBlob,blobLengthInBytes,subobjectName,riid,ppvRootSignature);
    if (_orig_ppvRootSignature) {
        if (_raw_ppvRootSignature)
            *_orig_ppvRootSignature = (void *)npt_com_get_or_wrap(
                npt_com_self_device(self),
                riid,
                (uint64_t)(uintptr_t)_raw_ppvRootSignature,
                (struct npt_com_base *)self);
        else
            *_orig_ppvRootSignature = NULL;
    }
    /* Async under the deferred-fatal model: assume success.  Host-side
     * failures (OOM, bad arg) leave the guest_id unregistered, so the
     * first method call against the wrapper trips decoder_fatal and
     * unwinds the context cleanly. */
    return (HRESULT)0 /* S_OK */;
}


void
npt_id3d12device14_fill_default_vtbl(struct npt_id3d12device14_client_vtbl *dst)
{
    dst->QueryInterface = npt_id3d12device14_default_QueryInterface;
    dst->AddRef         = npt_id3d12device14_default_AddRef;
    dst->Release        = npt_id3d12device14_default_Release;
    dst->GetPrivateData = npt_id3d12object_default_GetPrivateData;
    dst->SetPrivateData = npt_id3d12object_default_SetPrivateData;
    dst->SetPrivateDataInterface = npt_id3d12object_default_SetPrivateDataInterface;
    dst->SetName = npt_id3d12object_default_SetName;
    dst->GetNodeCount = npt_id3d12device_default_GetNodeCount;
    dst->CreateCommandQueue = npt_id3d12device_default_CreateCommandQueue;
    dst->CreateCommandAllocator = npt_id3d12device_default_CreateCommandAllocator;
    dst->CreateGraphicsPipelineState = npt_id3d12device_default_CreateGraphicsPipelineState;
    dst->CreateComputePipelineState = npt_id3d12device_default_CreateComputePipelineState;
    dst->CreateCommandList = npt_id3d12device_default_CreateCommandList;
    dst->CheckFeatureSupport = npt_id3d12device_default_CheckFeatureSupport;
    dst->CreateDescriptorHeap = npt_id3d12device_default_CreateDescriptorHeap;
    dst->GetDescriptorHandleIncrementSize = npt_id3d12device_default_GetDescriptorHandleIncrementSize;
    dst->CreateRootSignature = npt_id3d12device_default_CreateRootSignature;
    dst->CreateConstantBufferView = npt_id3d12device_default_CreateConstantBufferView;
    dst->CreateShaderResourceView = npt_id3d12device_default_CreateShaderResourceView;
    dst->CreateUnorderedAccessView = npt_id3d12device_default_CreateUnorderedAccessView;
    dst->CreateRenderTargetView = npt_id3d12device_default_CreateRenderTargetView;
    dst->CreateDepthStencilView = npt_id3d12device_default_CreateDepthStencilView;
    dst->CreateSampler = npt_id3d12device_default_CreateSampler;
    dst->CopyDescriptors = npt_id3d12device_default_CopyDescriptors;
    dst->CopyDescriptorsSimple = npt_id3d12device_default_CopyDescriptorsSimple;
    dst->GetResourceAllocationInfo = npt_id3d12device_default_GetResourceAllocationInfo;
    dst->GetCustomHeapProperties = npt_id3d12device_default_GetCustomHeapProperties;
    dst->CreateCommittedResource = npt_id3d12device_default_CreateCommittedResource;
    dst->CreateHeap = npt_id3d12device_default_CreateHeap;
    dst->CreatePlacedResource = npt_id3d12device_default_CreatePlacedResource;
    dst->CreateReservedResource = npt_id3d12device_default_CreateReservedResource;
    dst->CreateSharedHandle = npt_id3d12device_default_CreateSharedHandle;
    dst->OpenSharedHandle = npt_id3d12device_default_OpenSharedHandle;
    dst->OpenSharedHandleByName = npt_id3d12device_default_OpenSharedHandleByName;
    dst->MakeResident = npt_id3d12device_default_MakeResident;
    dst->Evict = npt_id3d12device_default_Evict;
    dst->CreateFence = npt_id3d12device_default_CreateFence;
    dst->GetDeviceRemovedReason = npt_id3d12device_default_GetDeviceRemovedReason;
    dst->GetCopyableFootprints = npt_id3d12device_default_GetCopyableFootprints;
    dst->CreateQueryHeap = npt_id3d12device_default_CreateQueryHeap;
    dst->SetStablePowerState = npt_id3d12device_default_SetStablePowerState;
    dst->CreateCommandSignature = npt_id3d12device_default_CreateCommandSignature;
    dst->GetResourceTiling = npt_id3d12device_default_GetResourceTiling;
    dst->GetAdapterLuid = npt_id3d12device_default_GetAdapterLuid;
    dst->CreatePipelineLibrary = npt_id3d12device1_default_CreatePipelineLibrary;
    dst->SetEventOnMultipleFenceCompletion = npt_id3d12device1_default_SetEventOnMultipleFenceCompletion;
    dst->SetResidencyPriority = npt_id3d12device1_default_SetResidencyPriority;
    dst->CreatePipelineState = npt_id3d12device2_default_CreatePipelineState;
    dst->OpenExistingHeapFromAddress = npt_id3d12device3_default_OpenExistingHeapFromAddress;
    dst->OpenExistingHeapFromFileMapping = npt_id3d12device3_default_OpenExistingHeapFromFileMapping;
    dst->EnqueueMakeResident = npt_id3d12device3_default_EnqueueMakeResident;
    dst->CreateCommandList1 = npt_id3d12device4_default_CreateCommandList1;
    dst->CreateProtectedResourceSession = npt_id3d12device4_default_CreateProtectedResourceSession;
    dst->CreateCommittedResource1 = npt_id3d12device4_default_CreateCommittedResource1;
    dst->CreateHeap1 = npt_id3d12device4_default_CreateHeap1;
    dst->CreateReservedResource1 = npt_id3d12device4_default_CreateReservedResource1;
    dst->GetResourceAllocationInfo1 = npt_id3d12device4_default_GetResourceAllocationInfo1;
    dst->CreateLifetimeTracker = npt_id3d12device5_default_CreateLifetimeTracker;
    dst->RemoveDevice = npt_id3d12device5_default_RemoveDevice;
    dst->EnumerateMetaCommands = npt_id3d12device5_default_EnumerateMetaCommands;
    dst->EnumerateMetaCommandParameters = npt_id3d12device5_default_EnumerateMetaCommandParameters;
    dst->CreateMetaCommand = npt_id3d12device5_default_CreateMetaCommand;
    dst->CreateStateObject = npt_id3d12device5_default_CreateStateObject;
    dst->GetRaytracingAccelerationStructurePrebuildInfo = npt_id3d12device5_default_GetRaytracingAccelerationStructurePrebuildInfo;
    dst->CheckDriverMatchingIdentifier = npt_id3d12device5_default_CheckDriverMatchingIdentifier;
    dst->SetBackgroundProcessingMode = npt_id3d12device6_default_SetBackgroundProcessingMode;
    dst->AddToStateObject = npt_id3d12device7_default_AddToStateObject;
    dst->CreateProtectedResourceSession1 = npt_id3d12device7_default_CreateProtectedResourceSession1;
    dst->GetResourceAllocationInfo2 = npt_id3d12device8_default_GetResourceAllocationInfo2;
    dst->CreateCommittedResource2 = npt_id3d12device8_default_CreateCommittedResource2;
    dst->CreatePlacedResource1 = npt_id3d12device8_default_CreatePlacedResource1;
    dst->CreateSamplerFeedbackUnorderedAccessView = npt_id3d12device8_default_CreateSamplerFeedbackUnorderedAccessView;
    dst->GetCopyableFootprints1 = npt_id3d12device8_default_GetCopyableFootprints1;
    dst->CreateShaderCacheSession = npt_id3d12device9_default_CreateShaderCacheSession;
    dst->ShaderCacheControl = npt_id3d12device9_default_ShaderCacheControl;
    dst->CreateCommandQueue1 = npt_id3d12device9_default_CreateCommandQueue1;
    dst->CreateCommittedResource3 = npt_id3d12device10_default_CreateCommittedResource3;
    dst->CreatePlacedResource2 = npt_id3d12device10_default_CreatePlacedResource2;
    dst->CreateReservedResource2 = npt_id3d12device10_default_CreateReservedResource2;
    dst->CreateSampler2 = npt_id3d12device11_default_CreateSampler2;
    dst->GetResourceAllocationInfo3 = npt_id3d12device12_default_GetResourceAllocationInfo3;
    dst->OpenExistingHeapFromAddress1 = npt_id3d12device13_default_OpenExistingHeapFromAddress1;
    dst->CreateRootSignatureFromSubobjectInLibrary = npt_id3d12device14_default_CreateRootSignatureFromSubobjectInLibrary;
}

/*
 * Per-interface default vtbl storage.  Populated by
 * npt_id3d12device14_install_default_vtbl(), which is called once at
 * runtime init time from the global client_table init function.
 */
struct npt_id3d12device14_client_vtbl npt_id3d12device14_default_vtbl_storage;

void
npt_id3d12device14_install_default_vtbl(void)
{
    npt_id3d12device14_fill_default_vtbl(&npt_id3d12device14_default_vtbl_storage);
}

void *
npt_id3d12device14_client_create(struct npt_device *dev, uint64_t host_id)
{
    struct npt_com_base *p = (struct npt_com_base *)npt_alloc(sizeof(*p));
    if (!p)
        return NULL;
    npt_com_base_init(p,
        (const void **)&npt_id3d12device14_default_vtbl_storage,
        dev, host_id);
    return p;
}


#pragma GCC diagnostic pop
