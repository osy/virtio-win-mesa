/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12deviceremovedextendeddata.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12DEVICEREMOVEDEXTENDEDDATA_H
#define NPT_PROTOCOL_GUEST_ID3D12DEVICEREMOVEDEXTENDEDDATA_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12DeviceRemovedExtendedData::GetAutoBreadcrumbsOutput
 */

struct npt_command_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput {
    void *_self;
    D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(const D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pOutput (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(struct npt_cs_encoder *enc,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(pOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pOutput (output only) */
}

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput_reply(const D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput_reply(struct npt_cs_decoder *dec,
                                                                          D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput,
                                                                          HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData,
                                                  NPT_METHOD_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData,
                                       NPT_METHOD_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pOutput)
            npt_decode_D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT(dec, (D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT *)pOutput);
    }
}

static inline void
npt_submit_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(struct npt_ring *ring,
                                                                    uint32_t cmd_flags,
                                                                    npt_object_id object_id,
                                                                    D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput,
                                                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(pOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput_reply(pOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(enc, cmd_flags, object_id, pOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(struct npt_ring *ring,
                                                                   npt_object_id object_id,
                                                                   D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(ring, 0, object_id, pOutput, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(struct npt_ring *ring,
                                                                  npt_object_id object_id,
                                                                  D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput(ring, NPT_CMD_FLAG_REPLY, object_id, pOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceRemovedExtendedData_GetAutoBreadcrumbsOutput_reply(dec, pOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceRemovedExtendedData::GetPageFaultAllocationOutput
 */

struct npt_command_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput {
    void *_self;
    D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(const D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pOutput (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(struct npt_cs_encoder *enc,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(pOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pOutput (output only) */
}

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput_reply(const D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput_reply(struct npt_cs_decoder *dec,
                                                                              D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput,
                                                                              HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData,
                                                  NPT_METHOD_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData,
                                       NPT_METHOD_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pOutput)
            npt_decode_D3D12_DRED_PAGE_FAULT_OUTPUT(dec, (D3D12_DRED_PAGE_FAULT_OUTPUT *)pOutput);
    }
}

static inline void
npt_submit_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(struct npt_ring *ring,
                                                                        uint32_t cmd_flags,
                                                                        npt_object_id object_id,
                                                                        D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput,
                                                                        struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(pOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput_reply(pOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(enc, cmd_flags, object_id, pOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(struct npt_ring *ring,
                                                                       npt_object_id object_id,
                                                                       D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(ring, 0, object_id, pOutput, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(struct npt_ring *ring,
                                                                      npt_object_id object_id,
                                                                      D3D12_DRED_PAGE_FAULT_OUTPUT * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput(ring, NPT_CMD_FLAG_REPLY, object_id, pOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceRemovedExtendedData_GetPageFaultAllocationOutput_reply(dec, pOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceRemovedExtendedData1::GetAutoBreadcrumbsOutput1
 */

struct npt_command_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1 {
    void *_self;
    D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(const D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pOutput (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(struct npt_cs_encoder *enc,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(pOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData1,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pOutput (output only) */
}

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1_reply(const D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1_reply(struct npt_cs_decoder *dec,
                                                                            D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput,
                                                                            HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData1,
                                                  NPT_METHOD_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData1,
                                       NPT_METHOD_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pOutput)
            npt_decode_D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1(dec, (D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 *)pOutput);
    }
}

static inline void
npt_submit_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(struct npt_ring *ring,
                                                                      uint32_t cmd_flags,
                                                                      npt_object_id object_id,
                                                                      D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput,
                                                                      struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(pOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1_reply(pOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(enc, cmd_flags, object_id, pOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(struct npt_ring *ring,
                                                                     npt_object_id object_id,
                                                                     D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(ring, 0, object_id, pOutput, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(struct npt_ring *ring,
                                                                    npt_object_id object_id,
                                                                    D3D12_DRED_AUTO_BREADCRUMBS_OUTPUT1 * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1(ring, NPT_CMD_FLAG_REPLY, object_id, pOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceRemovedExtendedData1_GetAutoBreadcrumbsOutput1_reply(dec, pOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceRemovedExtendedData1::GetPageFaultAllocationOutput1
 */

struct npt_command_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1 {
    void *_self;
    D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(const D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pOutput (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(struct npt_cs_encoder *enc,
                                                                          uint32_t cmd_flags,
                                                                          npt_object_id object_id,
                                                                          D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(pOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData1,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pOutput (output only) */
}

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1_reply(const D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1_reply(struct npt_cs_decoder *dec,
                                                                                D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput,
                                                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData1,
                                                  NPT_METHOD_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData1,
                                       NPT_METHOD_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pOutput)
            npt_decode_D3D12_DRED_PAGE_FAULT_OUTPUT1(dec, (D3D12_DRED_PAGE_FAULT_OUTPUT1 *)pOutput);
    }
}

static inline void
npt_submit_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(struct npt_ring *ring,
                                                                          uint32_t cmd_flags,
                                                                          npt_object_id object_id,
                                                                          D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput,
                                                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(pOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1_reply(pOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(enc, cmd_flags, object_id, pOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(struct npt_ring *ring,
                                                                         npt_object_id object_id,
                                                                         D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(ring, 0, object_id, pOutput, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(struct npt_ring *ring,
                                                                        npt_object_id object_id,
                                                                        D3D12_DRED_PAGE_FAULT_OUTPUT1 * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1(ring, NPT_CMD_FLAG_REPLY, object_id, pOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceRemovedExtendedData1_GetPageFaultAllocationOutput1_reply(dec, pOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceRemovedExtendedData2::GetPageFaultAllocationOutput2
 */

struct npt_command_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2 {
    void *_self;
    D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(const D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pOutput (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(struct npt_cs_encoder *enc,
                                                                          uint32_t cmd_flags,
                                                                          npt_object_id object_id,
                                                                          D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(pOutput);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData2,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pOutput (output only) */
}

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2_reply(const D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* Reply has runtime-unbounded wire size; reserve only the header.
     * Host encode_reply sets the encoder fatal flag and the guest
     * decode path returns the documented default. */
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2_reply(struct npt_cs_decoder *dec,
                                                                                D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput,
                                                                                HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData2,
                                                  NPT_METHOD_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData2,
                                       NPT_METHOD_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pOutput)
            npt_decode_D3D12_DRED_PAGE_FAULT_OUTPUT2(dec, (D3D12_DRED_PAGE_FAULT_OUTPUT2 *)pOutput);
    }
}

static inline void
npt_submit_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(struct npt_ring *ring,
                                                                          uint32_t cmd_flags,
                                                                          npt_object_id object_id,
                                                                          D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput,
                                                                          struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(pOutput);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2_reply(pOutput)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(enc, cmd_flags, object_id, pOutput);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(struct npt_ring *ring,
                                                                         npt_object_id object_id,
                                                                         D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(ring, 0, object_id, pOutput, &submit);
}

static inline HRESULT
npt_call_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(struct npt_ring *ring,
                                                                        npt_object_id object_id,
                                                                        D3D12_DRED_PAGE_FAULT_OUTPUT2 * pOutput)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2(ring, NPT_CMD_FLAG_REPLY, object_id, pOutput, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12DeviceRemovedExtendedData2_GetPageFaultAllocationOutput2_reply(dec, pOutput, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12DeviceRemovedExtendedData2::GetDeviceState
 */

struct npt_command_ID3D12DeviceRemovedExtendedData2_GetDeviceState {
    void *_self;
    D3D12_DRED_DEVICE_STATE ret;
};

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetDeviceState(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedData2_GetDeviceState(struct npt_cs_encoder *enc,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetDeviceState();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData2,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedData2_GetDeviceState),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetDeviceState_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    return cmd_size;
}

static inline void
npt_decode_ID3D12DeviceRemovedExtendedData2_GetDeviceState_reply(struct npt_cs_decoder *dec,
                                                                 D3D12_DRED_DEVICE_STATE *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData2,
                                                  NPT_METHOD_ID3D12DeviceRemovedExtendedData2_GetDeviceState))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12DeviceRemovedExtendedData2_GetDeviceState reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedData2,
                                       NPT_METHOD_ID3D12DeviceRemovedExtendedData2_GetDeviceState));
        if (ret) *ret = (D3D12_DRED_DEVICE_STATE)0;
        return;
    }
    if (ret) *ret = (D3D12_DRED_DEVICE_STATE)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12DeviceRemovedExtendedData2_GetDeviceState(struct npt_ring *ring,
                                                           uint32_t cmd_flags,
                                                           npt_object_id object_id,
                                                           struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetDeviceState();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12DeviceRemovedExtendedData2_GetDeviceState_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12DeviceRemovedExtendedData2_GetDeviceState(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12DeviceRemovedExtendedData2_GetDeviceState(struct npt_ring *ring,
                                                          npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData2_GetDeviceState(ring, 0, object_id, &submit);
}

static inline D3D12_DRED_DEVICE_STATE
npt_call_ID3D12DeviceRemovedExtendedData2_GetDeviceState(struct npt_ring *ring,
                                                         npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12DeviceRemovedExtendedData2_GetDeviceState(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_DRED_DEVICE_STATE _ret;
        npt_decode_ID3D12DeviceRemovedExtendedData2_GetDeviceState_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_DRED_DEVICE_STATE)0;
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12DEVICEREMOVEDEXTENDEDDATA_H */
