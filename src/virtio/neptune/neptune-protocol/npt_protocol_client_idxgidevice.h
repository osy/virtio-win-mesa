/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM declarations for idxgidevice: per-interface vtable
 * structs, default thunk prototypes, default vtable populators, and default
 * constructors.
 *
 * The thunk bodies, populators, and constructors live in the matching .c
 * file.  This split keeps the headers small and prevents N copies of the
 * thunk code from landing in every TU that pulls in this family.
 */

#ifndef NPT_PROTOCOL_CLIENT_IDXGIDEVICE_H
#define NPT_PROTOCOL_CLIENT_IDXGIDEVICE_H

#include "npt_protocol_guest_idxgidevice.h"
#include "npt_protocol_client_idxgiobject.h"

/* Forward-declare struct npt_device so the client_create prototypes don't
 * trigger the "declared inside parameter list" warning.  The full
 * definition lives in the consumer's npt_device.h, which the .c file pulls
 * in via npt_com.h. */
struct npt_device;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"



/* ================================================================== */
/* IDXGIDevice                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIDeviceVtbl, by construction.
 */
struct npt_idxgidevice_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIDevice::GetAdapter */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAdapter)(void *self, IDXGIAdapter ** pAdapter);
    /* slot 8: IDXGIDevice::CreateSurface */
    HRESULT (NPT_STDMETHODCALLTYPE *CreateSurface)(void *self, const DXGI_SURFACE_DESC * pDesc, UINT NumSurfaces, DXGI_USAGE Usage, const DXGI_SHARED_RESOURCE * pSharedResource, IDXGISurface ** ppSurface);
    /* slot 9: IDXGIDevice::QueryResourceResidency */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryResourceResidency)(void *self, IUnknown ** ppResources, DXGI_RESIDENCY * pResidencyStatus, UINT NumResources);
    /* slot 10: IDXGIDevice::SetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *SetGPUThreadPriority)(void *self, INT Priority);
    /* slot 11: IDXGIDevice::GetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *GetGPUThreadPriority)(void *self, INT * pPriority);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice_default_GetAdapter(void *self, IDXGIAdapter ** pAdapter);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice_default_CreateSurface(void *self, const DXGI_SURFACE_DESC * pDesc, UINT NumSurfaces, DXGI_USAGE Usage, const DXGI_SHARED_RESOURCE * pSharedResource, IDXGISurface ** ppSurface);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice_default_QueryResourceResidency(void *self, IUnknown ** ppResources, DXGI_RESIDENCY * pResidencyStatus, UINT NumResources);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice_default_SetGPUThreadPriority(void *self, INT Priority);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice_default_GetGPUThreadPriority(void *self, INT * pPriority);

/* Default IUnknown thunks for IDXGIDevice.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgidevice_fill_default_vtbl(struct npt_idxgidevice_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgidevice_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgidevice_client_vtbl npt_idxgidevice_default_vtbl_storage;
extern void
npt_idxgidevice_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgidevice_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* IDXGIDevice1                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIDevice1Vtbl, by construction.
 */
struct npt_idxgidevice1_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIDevice::GetAdapter */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAdapter)(void *self, IDXGIAdapter ** pAdapter);
    /* slot 8: IDXGIDevice::CreateSurface */
    HRESULT (NPT_STDMETHODCALLTYPE *CreateSurface)(void *self, const DXGI_SURFACE_DESC * pDesc, UINT NumSurfaces, DXGI_USAGE Usage, const DXGI_SHARED_RESOURCE * pSharedResource, IDXGISurface ** ppSurface);
    /* slot 9: IDXGIDevice::QueryResourceResidency */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryResourceResidency)(void *self, IUnknown ** ppResources, DXGI_RESIDENCY * pResidencyStatus, UINT NumResources);
    /* slot 10: IDXGIDevice::SetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *SetGPUThreadPriority)(void *self, INT Priority);
    /* slot 11: IDXGIDevice::GetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *GetGPUThreadPriority)(void *self, INT * pPriority);
    /* slot 12: IDXGIDevice1::SetMaximumFrameLatency */
    HRESULT (NPT_STDMETHODCALLTYPE *SetMaximumFrameLatency)(void *self, UINT MaxLatency);
    /* slot 13: IDXGIDevice1::GetMaximumFrameLatency */
    HRESULT (NPT_STDMETHODCALLTYPE *GetMaximumFrameLatency)(void *self, UINT * pMaxLatency);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice1_default_SetMaximumFrameLatency(void *self, UINT MaxLatency);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice1_default_GetMaximumFrameLatency(void *self, UINT * pMaxLatency);

/* Default IUnknown thunks for IDXGIDevice1.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice1_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice1_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice1_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgidevice1_fill_default_vtbl(struct npt_idxgidevice1_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgidevice1_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgidevice1_client_vtbl npt_idxgidevice1_default_vtbl_storage;
extern void
npt_idxgidevice1_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgidevice1_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* IDXGIDevice2                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIDevice2Vtbl, by construction.
 */
struct npt_idxgidevice2_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIDevice::GetAdapter */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAdapter)(void *self, IDXGIAdapter ** pAdapter);
    /* slot 8: IDXGIDevice::CreateSurface */
    HRESULT (NPT_STDMETHODCALLTYPE *CreateSurface)(void *self, const DXGI_SURFACE_DESC * pDesc, UINT NumSurfaces, DXGI_USAGE Usage, const DXGI_SHARED_RESOURCE * pSharedResource, IDXGISurface ** ppSurface);
    /* slot 9: IDXGIDevice::QueryResourceResidency */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryResourceResidency)(void *self, IUnknown ** ppResources, DXGI_RESIDENCY * pResidencyStatus, UINT NumResources);
    /* slot 10: IDXGIDevice::SetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *SetGPUThreadPriority)(void *self, INT Priority);
    /* slot 11: IDXGIDevice::GetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *GetGPUThreadPriority)(void *self, INT * pPriority);
    /* slot 12: IDXGIDevice1::SetMaximumFrameLatency */
    HRESULT (NPT_STDMETHODCALLTYPE *SetMaximumFrameLatency)(void *self, UINT MaxLatency);
    /* slot 13: IDXGIDevice1::GetMaximumFrameLatency */
    HRESULT (NPT_STDMETHODCALLTYPE *GetMaximumFrameLatency)(void *self, UINT * pMaxLatency);
    /* slot 14: IDXGIDevice2::OfferResources */
    HRESULT (NPT_STDMETHODCALLTYPE *OfferResources)(void *self, UINT NumResources, IDXGIResource ** ppResources, DXGI_OFFER_RESOURCE_PRIORITY Priority);
    /* slot 15: IDXGIDevice2::ReclaimResources */
    HRESULT (NPT_STDMETHODCALLTYPE *ReclaimResources)(void *self, UINT NumResources, IDXGIResource ** ppResources, BOOL * pDiscarded);
    /* slot 16: IDXGIDevice2::EnqueueSetEvent */
    HRESULT (NPT_STDMETHODCALLTYPE *EnqueueSetEvent)(void *self, HANDLE hEvent);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice2_default_OfferResources(void *self, UINT NumResources, IDXGIResource ** ppResources, DXGI_OFFER_RESOURCE_PRIORITY Priority);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice2_default_ReclaimResources(void *self, UINT NumResources, IDXGIResource ** ppResources, BOOL * pDiscarded);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice2_default_EnqueueSetEvent(void *self, HANDLE hEvent);

/* Default IUnknown thunks for IDXGIDevice2.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice2_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice2_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice2_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgidevice2_fill_default_vtbl(struct npt_idxgidevice2_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgidevice2_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgidevice2_client_vtbl npt_idxgidevice2_default_vtbl_storage;
extern void
npt_idxgidevice2_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgidevice2_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* IDXGIDevice3                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIDevice3Vtbl, by construction.
 */
struct npt_idxgidevice3_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIDevice::GetAdapter */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAdapter)(void *self, IDXGIAdapter ** pAdapter);
    /* slot 8: IDXGIDevice::CreateSurface */
    HRESULT (NPT_STDMETHODCALLTYPE *CreateSurface)(void *self, const DXGI_SURFACE_DESC * pDesc, UINT NumSurfaces, DXGI_USAGE Usage, const DXGI_SHARED_RESOURCE * pSharedResource, IDXGISurface ** ppSurface);
    /* slot 9: IDXGIDevice::QueryResourceResidency */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryResourceResidency)(void *self, IUnknown ** ppResources, DXGI_RESIDENCY * pResidencyStatus, UINT NumResources);
    /* slot 10: IDXGIDevice::SetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *SetGPUThreadPriority)(void *self, INT Priority);
    /* slot 11: IDXGIDevice::GetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *GetGPUThreadPriority)(void *self, INT * pPriority);
    /* slot 12: IDXGIDevice1::SetMaximumFrameLatency */
    HRESULT (NPT_STDMETHODCALLTYPE *SetMaximumFrameLatency)(void *self, UINT MaxLatency);
    /* slot 13: IDXGIDevice1::GetMaximumFrameLatency */
    HRESULT (NPT_STDMETHODCALLTYPE *GetMaximumFrameLatency)(void *self, UINT * pMaxLatency);
    /* slot 14: IDXGIDevice2::OfferResources */
    HRESULT (NPT_STDMETHODCALLTYPE *OfferResources)(void *self, UINT NumResources, IDXGIResource ** ppResources, DXGI_OFFER_RESOURCE_PRIORITY Priority);
    /* slot 15: IDXGIDevice2::ReclaimResources */
    HRESULT (NPT_STDMETHODCALLTYPE *ReclaimResources)(void *self, UINT NumResources, IDXGIResource ** ppResources, BOOL * pDiscarded);
    /* slot 16: IDXGIDevice2::EnqueueSetEvent */
    HRESULT (NPT_STDMETHODCALLTYPE *EnqueueSetEvent)(void *self, HANDLE hEvent);
    /* slot 17: IDXGIDevice3::Trim */
    void (NPT_STDMETHODCALLTYPE *Trim)(void *self);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern void NPT_STDMETHODCALLTYPE
npt_idxgidevice3_default_Trim(void *self);

/* Default IUnknown thunks for IDXGIDevice3.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice3_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice3_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice3_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgidevice3_fill_default_vtbl(struct npt_idxgidevice3_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgidevice3_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgidevice3_client_vtbl npt_idxgidevice3_default_vtbl_storage;
extern void
npt_idxgidevice3_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgidevice3_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* IDXGIDevice4                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIDevice4Vtbl, by construction.
 */
struct npt_idxgidevice4_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIDevice::GetAdapter */
    HRESULT (NPT_STDMETHODCALLTYPE *GetAdapter)(void *self, IDXGIAdapter ** pAdapter);
    /* slot 8: IDXGIDevice::CreateSurface */
    HRESULT (NPT_STDMETHODCALLTYPE *CreateSurface)(void *self, const DXGI_SURFACE_DESC * pDesc, UINT NumSurfaces, DXGI_USAGE Usage, const DXGI_SHARED_RESOURCE * pSharedResource, IDXGISurface ** ppSurface);
    /* slot 9: IDXGIDevice::QueryResourceResidency */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryResourceResidency)(void *self, IUnknown ** ppResources, DXGI_RESIDENCY * pResidencyStatus, UINT NumResources);
    /* slot 10: IDXGIDevice::SetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *SetGPUThreadPriority)(void *self, INT Priority);
    /* slot 11: IDXGIDevice::GetGPUThreadPriority */
    HRESULT (NPT_STDMETHODCALLTYPE *GetGPUThreadPriority)(void *self, INT * pPriority);
    /* slot 12: IDXGIDevice1::SetMaximumFrameLatency */
    HRESULT (NPT_STDMETHODCALLTYPE *SetMaximumFrameLatency)(void *self, UINT MaxLatency);
    /* slot 13: IDXGIDevice1::GetMaximumFrameLatency */
    HRESULT (NPT_STDMETHODCALLTYPE *GetMaximumFrameLatency)(void *self, UINT * pMaxLatency);
    /* slot 14: IDXGIDevice2::OfferResources */
    HRESULT (NPT_STDMETHODCALLTYPE *OfferResources)(void *self, UINT NumResources, IDXGIResource ** ppResources, DXGI_OFFER_RESOURCE_PRIORITY Priority);
    /* slot 15: IDXGIDevice2::ReclaimResources */
    HRESULT (NPT_STDMETHODCALLTYPE *ReclaimResources)(void *self, UINT NumResources, IDXGIResource ** ppResources, BOOL * pDiscarded);
    /* slot 16: IDXGIDevice2::EnqueueSetEvent */
    HRESULT (NPT_STDMETHODCALLTYPE *EnqueueSetEvent)(void *self, HANDLE hEvent);
    /* slot 17: IDXGIDevice3::Trim */
    void (NPT_STDMETHODCALLTYPE *Trim)(void *self);
    /* slot 18: IDXGIDevice4::OfferResources1 */
    HRESULT (NPT_STDMETHODCALLTYPE *OfferResources1)(void *self, UINT NumResources, IDXGIResource ** ppResources, DXGI_OFFER_RESOURCE_PRIORITY Priority, UINT Flags);
    /* slot 19: IDXGIDevice4::ReclaimResources1 */
    HRESULT (NPT_STDMETHODCALLTYPE *ReclaimResources1)(void *self, UINT NumResources, IDXGIResource ** ppResources, DXGI_RECLAIM_RESOURCE_RESULTS * pResults);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice4_default_OfferResources1(void *self, UINT NumResources, IDXGIResource ** ppResources, DXGI_OFFER_RESOURCE_PRIORITY Priority, UINT Flags);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice4_default_ReclaimResources1(void *self, UINT NumResources, IDXGIResource ** ppResources, DXGI_RECLAIM_RESOURCE_RESULTS * pResults);

/* Default IUnknown thunks for IDXGIDevice4.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgidevice4_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice4_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgidevice4_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgidevice4_fill_default_vtbl(struct npt_idxgidevice4_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgidevice4_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgidevice4_client_vtbl npt_idxgidevice4_default_vtbl_storage;
extern void
npt_idxgidevice4_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgidevice4_client_create(struct npt_device *dev, uint64_t host_id);


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_CLIENT_IDXGIDEVICE_H */
