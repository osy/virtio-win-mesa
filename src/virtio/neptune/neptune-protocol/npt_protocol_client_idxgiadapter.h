/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Client-side COM declarations for idxgiadapter: per-interface vtable
 * structs, default thunk prototypes, default vtable populators, and default
 * constructors.
 *
 * The thunk bodies, populators, and constructors live in the matching .c
 * file.  This split keeps the headers small and prevents N copies of the
 * thunk code from landing in every TU that pulls in this family.
 */

#ifndef NPT_PROTOCOL_CLIENT_IDXGIADAPTER_H
#define NPT_PROTOCOL_CLIENT_IDXGIADAPTER_H

#include "npt_protocol_guest_idxgiadapter.h"
#include "npt_protocol_client_idxgiobject.h"

/* Forward-declare struct npt_device so the client_create prototypes don't
 * trigger the "declared inside parameter list" warning.  The full
 * definition lives in the consumer's npt_device.h, which the .c file pulls
 * in via npt_com.h. */
struct npt_device;

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"



/* ================================================================== */
/* IDXGIAdapter                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIAdapterVtbl, by construction.
 */
struct npt_idxgiadapter_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIAdapter::EnumOutputs */
    HRESULT (NPT_STDMETHODCALLTYPE *EnumOutputs)(void *self, UINT Output, IDXGIOutput ** ppOutput);
    /* slot 8: IDXGIAdapter::GetDesc */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc)(void *self, DXGI_ADAPTER_DESC * pDesc);
    /* slot 9: IDXGIAdapter::CheckInterfaceSupport */
    HRESULT (NPT_STDMETHODCALLTYPE *CheckInterfaceSupport)(void *self, const GUID * InterfaceName, LARGE_INTEGER * pUMDVersion);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter_default_EnumOutputs(void *self, UINT Output, IDXGIOutput ** ppOutput);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter_default_GetDesc(void *self, DXGI_ADAPTER_DESC * pDesc);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter_default_CheckInterfaceSupport(void *self, const GUID * InterfaceName, LARGE_INTEGER * pUMDVersion);

/* Default IUnknown thunks for IDXGIAdapter.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgiadapter_fill_default_vtbl(struct npt_idxgiadapter_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgiadapter_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgiadapter_client_vtbl npt_idxgiadapter_default_vtbl_storage;
extern void
npt_idxgiadapter_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgiadapter_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* IDXGIAdapter1                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIAdapter1Vtbl, by construction.
 */
struct npt_idxgiadapter1_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIAdapter::EnumOutputs */
    HRESULT (NPT_STDMETHODCALLTYPE *EnumOutputs)(void *self, UINT Output, IDXGIOutput ** ppOutput);
    /* slot 8: IDXGIAdapter::GetDesc */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc)(void *self, DXGI_ADAPTER_DESC * pDesc);
    /* slot 9: IDXGIAdapter::CheckInterfaceSupport */
    HRESULT (NPT_STDMETHODCALLTYPE *CheckInterfaceSupport)(void *self, const GUID * InterfaceName, LARGE_INTEGER * pUMDVersion);
    /* slot 10: IDXGIAdapter1::GetDesc1 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc1)(void *self, DXGI_ADAPTER_DESC1 * pDesc);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter1_default_GetDesc1(void *self, DXGI_ADAPTER_DESC1 * pDesc);

/* Default IUnknown thunks for IDXGIAdapter1.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter1_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter1_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter1_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgiadapter1_fill_default_vtbl(struct npt_idxgiadapter1_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgiadapter1_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgiadapter1_client_vtbl npt_idxgiadapter1_default_vtbl_storage;
extern void
npt_idxgiadapter1_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgiadapter1_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* IDXGIAdapter2                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIAdapter2Vtbl, by construction.
 */
struct npt_idxgiadapter2_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIAdapter::EnumOutputs */
    HRESULT (NPT_STDMETHODCALLTYPE *EnumOutputs)(void *self, UINT Output, IDXGIOutput ** ppOutput);
    /* slot 8: IDXGIAdapter::GetDesc */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc)(void *self, DXGI_ADAPTER_DESC * pDesc);
    /* slot 9: IDXGIAdapter::CheckInterfaceSupport */
    HRESULT (NPT_STDMETHODCALLTYPE *CheckInterfaceSupport)(void *self, const GUID * InterfaceName, LARGE_INTEGER * pUMDVersion);
    /* slot 10: IDXGIAdapter1::GetDesc1 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc1)(void *self, DXGI_ADAPTER_DESC1 * pDesc);
    /* slot 11: IDXGIAdapter2::GetDesc2 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc2)(void *self, DXGI_ADAPTER_DESC2 * pDesc);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter2_default_GetDesc2(void *self, DXGI_ADAPTER_DESC2 * pDesc);

/* Default IUnknown thunks for IDXGIAdapter2.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter2_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter2_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter2_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgiadapter2_fill_default_vtbl(struct npt_idxgiadapter2_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgiadapter2_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgiadapter2_client_vtbl npt_idxgiadapter2_default_vtbl_storage;
extern void
npt_idxgiadapter2_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgiadapter2_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* IDXGIAdapter3                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIAdapter3Vtbl, by construction.
 */
struct npt_idxgiadapter3_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIAdapter::EnumOutputs */
    HRESULT (NPT_STDMETHODCALLTYPE *EnumOutputs)(void *self, UINT Output, IDXGIOutput ** ppOutput);
    /* slot 8: IDXGIAdapter::GetDesc */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc)(void *self, DXGI_ADAPTER_DESC * pDesc);
    /* slot 9: IDXGIAdapter::CheckInterfaceSupport */
    HRESULT (NPT_STDMETHODCALLTYPE *CheckInterfaceSupport)(void *self, const GUID * InterfaceName, LARGE_INTEGER * pUMDVersion);
    /* slot 10: IDXGIAdapter1::GetDesc1 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc1)(void *self, DXGI_ADAPTER_DESC1 * pDesc);
    /* slot 11: IDXGIAdapter2::GetDesc2 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc2)(void *self, DXGI_ADAPTER_DESC2 * pDesc);
    /* slot 12: IDXGIAdapter3::RegisterHardwareContentProtectionTeardownStatusEvent */
    HRESULT (NPT_STDMETHODCALLTYPE *RegisterHardwareContentProtectionTeardownStatusEvent)(void *self, HANDLE hEvent, DWORD * pdwCookie);
    /* slot 13: IDXGIAdapter3::UnregisterHardwareContentProtectionTeardownStatus */
    void (NPT_STDMETHODCALLTYPE *UnregisterHardwareContentProtectionTeardownStatus)(void *self, DWORD dwCookie);
    /* slot 14: IDXGIAdapter3::QueryVideoMemoryInfo */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryVideoMemoryInfo)(void *self, UINT NodeIndex, DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup, DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo);
    /* slot 15: IDXGIAdapter3::SetVideoMemoryReservation */
    HRESULT (NPT_STDMETHODCALLTYPE *SetVideoMemoryReservation)(void *self, UINT NodeIndex, DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup, UINT64 Reservation);
    /* slot 16: IDXGIAdapter3::RegisterVideoMemoryBudgetChangeNotificationEvent */
    HRESULT (NPT_STDMETHODCALLTYPE *RegisterVideoMemoryBudgetChangeNotificationEvent)(void *self, HANDLE hEvent, DWORD * pdwCookie);
    /* slot 17: IDXGIAdapter3::UnregisterVideoMemoryBudgetChangeNotification */
    void (NPT_STDMETHODCALLTYPE *UnregisterVideoMemoryBudgetChangeNotification)(void *self, DWORD dwCookie);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter3_default_RegisterHardwareContentProtectionTeardownStatusEvent(void *self, HANDLE hEvent, DWORD * pdwCookie);
extern void NPT_STDMETHODCALLTYPE
npt_idxgiadapter3_default_UnregisterHardwareContentProtectionTeardownStatus(void *self, DWORD dwCookie);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter3_default_QueryVideoMemoryInfo(void *self, UINT NodeIndex, DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup, DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter3_default_SetVideoMemoryReservation(void *self, UINT NodeIndex, DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup, UINT64 Reservation);
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter3_default_RegisterVideoMemoryBudgetChangeNotificationEvent(void *self, HANDLE hEvent, DWORD * pdwCookie);
extern void NPT_STDMETHODCALLTYPE
npt_idxgiadapter3_default_UnregisterVideoMemoryBudgetChangeNotification(void *self, DWORD dwCookie);

/* Default IUnknown thunks for IDXGIAdapter3.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter3_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter3_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter3_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgiadapter3_fill_default_vtbl(struct npt_idxgiadapter3_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgiadapter3_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgiadapter3_client_vtbl npt_idxgiadapter3_default_vtbl_storage;
extern void
npt_idxgiadapter3_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgiadapter3_client_create(struct npt_device *dev, uint64_t host_id);


/* ================================================================== */
/* IDXGIAdapter4                                                       */
/* ================================================================== */

/*
 * Vtable struct in COM ABI order: slots 0-2 are IUnknown, then the
 * inherited methods from each ancestor (topmost first), then this
 * interface's own methods.  Layout-compatible with the Windows SDK's
 * IDXGIAdapter4Vtbl, by construction.
 */
struct npt_idxgiadapter4_client_vtbl {
    /* IUnknown (slots 0-2) */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryInterface)(void *self, REFIID riid, void **ppvObject);
    ULONG   (NPT_STDMETHODCALLTYPE *AddRef)(void *self);
    ULONG   (NPT_STDMETHODCALLTYPE *Release)(void *self);
    /* slot 3: IDXGIObject::SetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateData)(void *self, const GUID * Name, UINT DataSize, const void * pData);
    /* slot 4: IDXGIObject::SetPrivateDataInterface */
    HRESULT (NPT_STDMETHODCALLTYPE *SetPrivateDataInterface)(void *self, const GUID * Name, const IUnknown * pUnknown);
    /* slot 5: IDXGIObject::GetPrivateData */
    HRESULT (NPT_STDMETHODCALLTYPE *GetPrivateData)(void *self, const GUID * Name, UINT * pDataSize, void * pData);
    /* slot 6: IDXGIObject::GetParent */
    HRESULT (NPT_STDMETHODCALLTYPE *GetParent)(void *self, const IID * riid, void ** ppParent);
    /* slot 7: IDXGIAdapter::EnumOutputs */
    HRESULT (NPT_STDMETHODCALLTYPE *EnumOutputs)(void *self, UINT Output, IDXGIOutput ** ppOutput);
    /* slot 8: IDXGIAdapter::GetDesc */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc)(void *self, DXGI_ADAPTER_DESC * pDesc);
    /* slot 9: IDXGIAdapter::CheckInterfaceSupport */
    HRESULT (NPT_STDMETHODCALLTYPE *CheckInterfaceSupport)(void *self, const GUID * InterfaceName, LARGE_INTEGER * pUMDVersion);
    /* slot 10: IDXGIAdapter1::GetDesc1 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc1)(void *self, DXGI_ADAPTER_DESC1 * pDesc);
    /* slot 11: IDXGIAdapter2::GetDesc2 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc2)(void *self, DXGI_ADAPTER_DESC2 * pDesc);
    /* slot 12: IDXGIAdapter3::RegisterHardwareContentProtectionTeardownStatusEvent */
    HRESULT (NPT_STDMETHODCALLTYPE *RegisterHardwareContentProtectionTeardownStatusEvent)(void *self, HANDLE hEvent, DWORD * pdwCookie);
    /* slot 13: IDXGIAdapter3::UnregisterHardwareContentProtectionTeardownStatus */
    void (NPT_STDMETHODCALLTYPE *UnregisterHardwareContentProtectionTeardownStatus)(void *self, DWORD dwCookie);
    /* slot 14: IDXGIAdapter3::QueryVideoMemoryInfo */
    HRESULT (NPT_STDMETHODCALLTYPE *QueryVideoMemoryInfo)(void *self, UINT NodeIndex, DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup, DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo);
    /* slot 15: IDXGIAdapter3::SetVideoMemoryReservation */
    HRESULT (NPT_STDMETHODCALLTYPE *SetVideoMemoryReservation)(void *self, UINT NodeIndex, DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup, UINT64 Reservation);
    /* slot 16: IDXGIAdapter3::RegisterVideoMemoryBudgetChangeNotificationEvent */
    HRESULT (NPT_STDMETHODCALLTYPE *RegisterVideoMemoryBudgetChangeNotificationEvent)(void *self, HANDLE hEvent, DWORD * pdwCookie);
    /* slot 17: IDXGIAdapter3::UnregisterVideoMemoryBudgetChangeNotification */
    void (NPT_STDMETHODCALLTYPE *UnregisterVideoMemoryBudgetChangeNotification)(void *self, DWORD dwCookie);
    /* slot 18: IDXGIAdapter4::GetDesc3 */
    HRESULT (NPT_STDMETHODCALLTYPE *GetDesc3)(void *self, DXGI_ADAPTER_DESC3 * pDesc);
};

/* Default thunk prototypes for the methods OWNED by this interface
 * (inherited methods are declared by their owning interface's client
 * header, included above).  Bodies live in the matching .c file.  Methods
 * marked skip_default get a generated stub that aborts at runtime via
 * npt_com_assert_overridden; the consumer must patch the vtable slot
 * with a real implementation before the method is called. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter4_default_GetDesc3(void *self, DXGI_ADAPTER_DESC3 * pDesc);

/* Default IUnknown thunks for IDXGIAdapter4.  AddRef/Release route through
 * the runtime helpers; QueryInterface walks this interface's parent IID
 * chain, falling through to a host COM_QUERY_INTERFACE round-trip on miss. */
extern HRESULT NPT_STDMETHODCALLTYPE
npt_idxgiadapter4_default_QueryInterface(void *self, REFIID riid, void **ppvObject);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter4_default_AddRef(void *self);
extern ULONG NPT_STDMETHODCALLTYPE
npt_idxgiadapter4_default_Release(void *self);

/* Populate `dst` with all default thunks (own + inherited via the
 * extern decls from the parent client header).  Hand-written code can
 * then patch individual slots before installing the vtable. */
extern void
npt_idxgiadapter4_fill_default_vtbl(struct npt_idxgiadapter4_client_vtbl *dst);

/* Per-interface default vtbl storage shared by all client instances of
 * this interface.  Populated once at runtime init by
 * npt_idxgiadapter4_install_default_vtbl() (called from the global
 * npt_com_init_default_ctors populator). */
extern struct npt_idxgiadapter4_client_vtbl npt_idxgiadapter4_default_vtbl_storage;
extern void
npt_idxgiadapter4_install_default_vtbl(void);

/* Default constructor for the wrapper cache.  Allocates a bare
 * `struct npt_com_base`, installs the default vtable, and returns it. */
extern void *
npt_idxgiadapter4_client_create(struct npt_device *dev, uint64_t host_id);


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_CLIENT_IDXGIADAPTER_H */
