/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Common command stream interface, shared by guest and host.
 *
 * The consuming project must provide "npt_cs.h" with implementations of
 * the types and functions listed below.
 */

#ifndef NPT_PROTOCOL_COMMON_CS_H
#define NPT_PROTOCOL_COMMON_CS_H

#include <assert.h>
#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>
#include <wchar.h>

/*
 * Required types/functions from the consuming project's "npt_cs.h":
 *
 *   -- Types --
 *   struct npt_cs_encoder
 *   struct npt_cs_decoder
 *   typedef uint64_t npt_object_id
 *   typedef int npt_object_type
 *
 *   -- Encoder --
 *   void npt_cs_encoder_write(struct npt_cs_encoder *enc,
 *                             size_t size, const void *val, size_t val_size)
 *   void npt_cs_encoder_set_fatal(const struct npt_cs_encoder *enc)
 *
 *   -- Decoder --
 *   void npt_cs_decoder_read(struct npt_cs_decoder *dec,
 *                            size_t size, void *val, size_t val_size)
 *   void npt_cs_decoder_peek(const struct npt_cs_decoder *dec,
 *                            size_t size, void *val, size_t val_size)
 *   void npt_cs_decoder_set_fatal(const struct npt_cs_decoder *dec)
 *
 *   -- Object/handle management --
 *   npt_object_id npt_object_get_id(const void *handle)
 *   void *npt_object_from_id(npt_object_id id)
 *   npt_object_id npt_win32_handle_get_id(const void *handle)
 *   void *npt_win32_handle_from_id(npt_object_id id)
 */
#include "npt_cs.h"

#endif /* NPT_PROTOCOL_COMMON_CS_H */
