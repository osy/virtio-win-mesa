/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12resource.
 */

#ifndef NPT_PROTOCOL_GUEST_ID3D12RESOURCE_H
#define NPT_PROTOCOL_GUEST_ID3D12RESOURCE_H

#include "npt_protocol_guest_structs.h"
/* For npt_com_allocate_next_id -- called inline from every output
 * COM handle's encode path. */
#include "npt_protocol_guest_cs.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12Resource::Map
 */

struct npt_command_ID3D12Resource_Map {
    void *_self;
    UINT Subresource;
    const D3D12_RANGE * pReadRange;
    void ** ppData;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Resource_Map(const UINT Subresource,const D3D12_RANGE * pReadRange,void ** ppData)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Subresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pReadRange);
    if (pReadRange)
        cmd_size += npt_sizeof_D3D12_RANGE(pReadRange, max_mode);
/* skip ppData (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Resource_Map(struct npt_cs_encoder *enc,
                              uint32_t cmd_flags,
                              npt_object_id object_id,
                              UINT Subresource,
                              const D3D12_RANGE * pReadRange,
                              void ** ppData)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Resource_Map(Subresource,pReadRange,ppData);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                 NPT_METHOD_ID3D12Resource_Map),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Subresource);
    if (npt_encode_simple_pointer(enc, pReadRange))
        npt_encode_D3D12_RANGE(enc, pReadRange);
/* skip ppData (output only) */
}

static inline size_t
npt_sizeof_ID3D12Resource_Map_reply(const UINT Subresource,const D3D12_RANGE * pReadRange,void ** ppData)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Subresource (input only) */
/* skip pReadRange (input only) */
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_void(NULL, max_mode);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Resource_Map_reply(struct npt_cs_decoder *dec,
                                    void ** ppData,
                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                                  NPT_METHOD_ID3D12Resource_Map))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Resource_Map reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                       NPT_METHOD_ID3D12Resource_Map));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (ppData)
            npt_decode_void(dec, (void *)ppData);
    }
}

static inline void
npt_submit_ID3D12Resource_Map(struct npt_ring *ring,
                              uint32_t cmd_flags,
                              npt_object_id object_id,
                              UINT Subresource,
                              const D3D12_RANGE * pReadRange,
                              void ** ppData,
                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Resource_Map(Subresource,pReadRange,ppData);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Resource_Map_reply(Subresource,pReadRange,ppData)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Resource_Map(enc, cmd_flags, object_id, Subresource, pReadRange, ppData);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Resource_Map(struct npt_ring *ring,
                             npt_object_id object_id,
                             UINT Subresource,
                             const D3D12_RANGE * pReadRange,
                             void ** ppData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_Map(ring, 0, object_id, Subresource, pReadRange, ppData, &submit);
}

static inline HRESULT
npt_call_ID3D12Resource_Map(struct npt_ring *ring,
                            npt_object_id object_id,
                            UINT Subresource,
                            const D3D12_RANGE * pReadRange,
                            void ** ppData)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_Map(ring, NPT_CMD_FLAG_REPLY, object_id, Subresource, pReadRange, ppData, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Resource_Map_reply(dec, ppData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Resource::Unmap
 */

struct npt_command_ID3D12Resource_Unmap {
    void *_self;
    UINT Subresource;
    const D3D12_RANGE * pWrittenRange;
};

static inline size_t
npt_sizeof_ID3D12Resource_Unmap(const UINT Subresource,const D3D12_RANGE * pWrittenRange)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&Subresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pWrittenRange);
    if (pWrittenRange)
        cmd_size += npt_sizeof_D3D12_RANGE(pWrittenRange, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Resource_Unmap(struct npt_cs_encoder *enc,
                                uint32_t cmd_flags,
                                npt_object_id object_id,
                                UINT Subresource,
                                const D3D12_RANGE * pWrittenRange)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Resource_Unmap(Subresource,pWrittenRange);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                 NPT_METHOD_ID3D12Resource_Unmap),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &Subresource);
    if (npt_encode_simple_pointer(enc, pWrittenRange))
        npt_encode_D3D12_RANGE(enc, pWrittenRange);
}

static inline size_t
npt_sizeof_ID3D12Resource_Unmap_reply(const UINT Subresource,const D3D12_RANGE * pWrittenRange)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip Subresource (input only) */
/* skip pWrittenRange (input only) */
    return cmd_size;
}

static inline void
npt_submit_ID3D12Resource_Unmap(struct npt_ring *ring,
                                uint32_t cmd_flags,
                                npt_object_id object_id,
                                UINT Subresource,
                                const D3D12_RANGE * pWrittenRange,
                                struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Resource_Unmap(Subresource,pWrittenRange);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Resource_Unmap_reply(Subresource,pWrittenRange)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Resource_Unmap(enc, cmd_flags, object_id, Subresource, pWrittenRange);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Resource_Unmap(struct npt_ring *ring,
                               npt_object_id object_id,
                               UINT Subresource,
                               const D3D12_RANGE * pWrittenRange)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_Unmap(ring, 0, object_id, Subresource, pWrittenRange, &submit);
}

static inline void
npt_call_ID3D12Resource_Unmap(struct npt_ring *ring,
                              npt_object_id object_id,
                              UINT Subresource,
                              const D3D12_RANGE * pWrittenRange)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_Unmap(ring, NPT_CMD_FLAG_REPLY, object_id, Subresource, pWrittenRange, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        npt_ring_free_command_reply(ring, &submit);
    }
}

/*
 * ID3D12Resource::GetDesc
 */

struct npt_command_ID3D12Resource_GetDesc {
    void *_self;
    D3D12_RESOURCE_DESC ret;
};

static inline size_t
npt_sizeof_ID3D12Resource_GetDesc(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Resource_GetDesc(struct npt_cs_encoder *enc,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Resource_GetDesc();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                 NPT_METHOD_ID3D12Resource_GetDesc),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12Resource_GetDesc_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_RESOURCE_DESC(&(const D3D12_RESOURCE_DESC){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Resource_GetDesc_reply(struct npt_cs_decoder *dec,
                                        D3D12_RESOURCE_DESC *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                                  NPT_METHOD_ID3D12Resource_GetDesc))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Resource_GetDesc reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                       NPT_METHOD_ID3D12Resource_GetDesc));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_RESOURCE_DESC(dec, ret);
}

static inline void
npt_submit_ID3D12Resource_GetDesc(struct npt_ring *ring,
                                  uint32_t cmd_flags,
                                  npt_object_id object_id,
                                  struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Resource_GetDesc();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Resource_GetDesc_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Resource_GetDesc(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Resource_GetDesc(struct npt_ring *ring,
                                 npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_GetDesc(ring, 0, object_id, &submit);
}

static inline D3D12_RESOURCE_DESC
npt_call_ID3D12Resource_GetDesc(struct npt_ring *ring,
                                npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_GetDesc(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_RESOURCE_DESC _ret;
        npt_decode_ID3D12Resource_GetDesc_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_RESOURCE_DESC){0};
    }
}

/*
 * ID3D12Resource::GetGPUVirtualAddress
 */

struct npt_command_ID3D12Resource_GetGPUVirtualAddress {
    void *_self;
    D3D12_GPU_VIRTUAL_ADDRESS ret;
};

static inline size_t
npt_sizeof_ID3D12Resource_GetGPUVirtualAddress(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Resource_GetGPUVirtualAddress(struct npt_cs_encoder *enc,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Resource_GetGPUVirtualAddress();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                 NPT_METHOD_ID3D12Resource_GetGPUVirtualAddress),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12Resource_GetGPUVirtualAddress_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_GPU_VIRTUAL_ADDRESS(&(const D3D12_GPU_VIRTUAL_ADDRESS){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Resource_GetGPUVirtualAddress_reply(struct npt_cs_decoder *dec,
                                                     D3D12_GPU_VIRTUAL_ADDRESS *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                                  NPT_METHOD_ID3D12Resource_GetGPUVirtualAddress))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Resource_GetGPUVirtualAddress reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                       NPT_METHOD_ID3D12Resource_GetGPUVirtualAddress));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_GPU_VIRTUAL_ADDRESS(dec, ret);
}

static inline void
npt_submit_ID3D12Resource_GetGPUVirtualAddress(struct npt_ring *ring,
                                               uint32_t cmd_flags,
                                               npt_object_id object_id,
                                               struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Resource_GetGPUVirtualAddress();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Resource_GetGPUVirtualAddress_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Resource_GetGPUVirtualAddress(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Resource_GetGPUVirtualAddress(struct npt_ring *ring,
                                              npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_GetGPUVirtualAddress(ring, 0, object_id, &submit);
}

static inline D3D12_GPU_VIRTUAL_ADDRESS
npt_call_ID3D12Resource_GetGPUVirtualAddress(struct npt_ring *ring,
                                             npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_GetGPUVirtualAddress(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_GPU_VIRTUAL_ADDRESS _ret;
        npt_decode_ID3D12Resource_GetGPUVirtualAddress_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_GPU_VIRTUAL_ADDRESS){0};
    }
}

/*
 * ID3D12Resource::WriteToSubresource
 */

struct npt_command_ID3D12Resource_WriteToSubresource {
    void *_self;
    UINT DstSubresource;
    const D3D12_BOX * pDstBox;
    const void * pSrcData;
    UINT SrcRowPitch;
    UINT SrcDepthPitch;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Resource_WriteToSubresource(const UINT DstSubresource,const D3D12_BOX * pDstBox,const void * pSrcData,const UINT SrcRowPitch,const UINT SrcDepthPitch)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_UINT(&DstSubresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pDstBox);
    if (pDstBox)
        cmd_size += npt_sizeof_D3D12_BOX(pDstBox, max_mode);
    /* ERROR: pSrcData (void) is not serializable */
    return 0;
    cmd_size += npt_sizeof_UINT(&SrcRowPitch, max_mode);
    cmd_size += npt_sizeof_UINT(&SrcDepthPitch, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Resource_WriteToSubresource(struct npt_cs_encoder *enc,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT DstSubresource,
                                             const D3D12_BOX * pDstBox,
                                             const void * pSrcData,
                                             UINT SrcRowPitch,
                                             UINT SrcDepthPitch)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Resource_WriteToSubresource(DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                 NPT_METHOD_ID3D12Resource_WriteToSubresource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    npt_encode_UINT(enc, &DstSubresource);
    if (npt_encode_simple_pointer(enc, pDstBox))
        npt_encode_D3D12_BOX(enc, pDstBox);
    /* ERROR: pSrcData (void) is not serializable */
    return;
    npt_encode_UINT(enc, &SrcRowPitch);
    npt_encode_UINT(enc, &SrcDepthPitch);
}

static inline size_t
npt_sizeof_ID3D12Resource_WriteToSubresource_reply(const UINT DstSubresource,const D3D12_BOX * pDstBox,const void * pSrcData,const UINT SrcRowPitch,const UINT SrcDepthPitch)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip DstSubresource (input only) */
/* skip pDstBox (input only) */
/* skip pSrcData (input only) */
/* skip SrcRowPitch (input only) */
/* skip SrcDepthPitch (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Resource_WriteToSubresource_reply(struct npt_cs_decoder *dec,
                                                   HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                                  NPT_METHOD_ID3D12Resource_WriteToSubresource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Resource_WriteToSubresource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                       NPT_METHOD_ID3D12Resource_WriteToSubresource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
}

static inline void
npt_submit_ID3D12Resource_WriteToSubresource(struct npt_ring *ring,
                                             uint32_t cmd_flags,
                                             npt_object_id object_id,
                                             UINT DstSubresource,
                                             const D3D12_BOX * pDstBox,
                                             const void * pSrcData,
                                             UINT SrcRowPitch,
                                             UINT SrcDepthPitch,
                                             struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Resource_WriteToSubresource(DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Resource_WriteToSubresource_reply(DstSubresource,pDstBox,pSrcData,SrcRowPitch,SrcDepthPitch)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Resource_WriteToSubresource(enc, cmd_flags, object_id, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Resource_WriteToSubresource(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            UINT DstSubresource,
                                            const D3D12_BOX * pDstBox,
                                            const void * pSrcData,
                                            UINT SrcRowPitch,
                                            UINT SrcDepthPitch)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_WriteToSubresource(ring, 0, object_id, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch, &submit);
}

static inline HRESULT
npt_call_ID3D12Resource_WriteToSubresource(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           UINT DstSubresource,
                                           const D3D12_BOX * pDstBox,
                                           const void * pSrcData,
                                           UINT SrcRowPitch,
                                           UINT SrcDepthPitch)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_WriteToSubresource(ring, NPT_CMD_FLAG_REPLY, object_id, DstSubresource, pDstBox, pSrcData, SrcRowPitch, SrcDepthPitch, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Resource_WriteToSubresource_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Resource::ReadFromSubresource
 */

struct npt_command_ID3D12Resource_ReadFromSubresource {
    void *_self;
    void * pDstData;
    UINT DstRowPitch;
    UINT DstDepthPitch;
    UINT SrcSubresource;
    const D3D12_BOX * pSrcBox;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Resource_ReadFromSubresource(const void * pDstData,const UINT DstRowPitch,const UINT DstDepthPitch,const UINT SrcSubresource,const D3D12_BOX * pSrcBox)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pDstData (output only) */
    cmd_size += npt_sizeof_UINT(&DstRowPitch, max_mode);
    cmd_size += npt_sizeof_UINT(&DstDepthPitch, max_mode);
    cmd_size += npt_sizeof_UINT(&SrcSubresource, max_mode);
    cmd_size += npt_sizeof_simple_pointer(pSrcBox);
    if (pSrcBox)
        cmd_size += npt_sizeof_D3D12_BOX(pSrcBox, max_mode);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Resource_ReadFromSubresource(struct npt_cs_encoder *enc,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              void * pDstData,
                                              UINT DstRowPitch,
                                              UINT DstDepthPitch,
                                              UINT SrcSubresource,
                                              const D3D12_BOX * pSrcBox)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Resource_ReadFromSubresource(pDstData,DstRowPitch,DstDepthPitch,SrcSubresource,pSrcBox);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                 NPT_METHOD_ID3D12Resource_ReadFromSubresource),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pDstData (output only) */
    npt_encode_UINT(enc, &DstRowPitch);
    npt_encode_UINT(enc, &DstDepthPitch);
    npt_encode_UINT(enc, &SrcSubresource);
    if (npt_encode_simple_pointer(enc, pSrcBox))
        npt_encode_D3D12_BOX(enc, pSrcBox);
}

static inline size_t
npt_sizeof_ID3D12Resource_ReadFromSubresource_reply(const void * pDstData,const UINT DstRowPitch,const UINT DstDepthPitch,const UINT SrcSubresource,const D3D12_BOX * pSrcBox)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    /* ERROR: pDstData (void) is not serializable */
    return 0;
/* skip DstRowPitch (input only) */
/* skip DstDepthPitch (input only) */
/* skip SrcSubresource (input only) */
/* skip pSrcBox (input only) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Resource_ReadFromSubresource_reply(struct npt_cs_decoder *dec,
                                                    void * pDstData,
                                                    HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                                  NPT_METHOD_ID3D12Resource_ReadFromSubresource))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Resource_ReadFromSubresource reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                       NPT_METHOD_ID3D12Resource_ReadFromSubresource));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    /* ERROR: pDstData (void) is not serializable */
    return;
}

static inline void
npt_submit_ID3D12Resource_ReadFromSubresource(struct npt_ring *ring,
                                              uint32_t cmd_flags,
                                              npt_object_id object_id,
                                              void * pDstData,
                                              UINT DstRowPitch,
                                              UINT DstDepthPitch,
                                              UINT SrcSubresource,
                                              const D3D12_BOX * pSrcBox,
                                              struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Resource_ReadFromSubresource(pDstData,DstRowPitch,DstDepthPitch,SrcSubresource,pSrcBox);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Resource_ReadFromSubresource_reply(pDstData,DstRowPitch,DstDepthPitch,SrcSubresource,pSrcBox)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Resource_ReadFromSubresource(enc, cmd_flags, object_id, pDstData, DstRowPitch, DstDepthPitch, SrcSubresource, pSrcBox);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Resource_ReadFromSubresource(struct npt_ring *ring,
                                             npt_object_id object_id,
                                             void * pDstData,
                                             UINT DstRowPitch,
                                             UINT DstDepthPitch,
                                             UINT SrcSubresource,
                                             const D3D12_BOX * pSrcBox)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_ReadFromSubresource(ring, 0, object_id, pDstData, DstRowPitch, DstDepthPitch, SrcSubresource, pSrcBox, &submit);
}

static inline HRESULT
npt_call_ID3D12Resource_ReadFromSubresource(struct npt_ring *ring,
                                            npt_object_id object_id,
                                            void * pDstData,
                                            UINT DstRowPitch,
                                            UINT DstDepthPitch,
                                            UINT SrcSubresource,
                                            const D3D12_BOX * pSrcBox)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_ReadFromSubresource(ring, NPT_CMD_FLAG_REPLY, object_id, pDstData, DstRowPitch, DstDepthPitch, SrcSubresource, pSrcBox, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Resource_ReadFromSubresource_reply(dec, pDstData, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Resource::GetHeapProperties
 */

struct npt_command_ID3D12Resource_GetHeapProperties {
    void *_self;
    D3D12_HEAP_PROPERTIES * pHeapProperties;
    D3D12_HEAP_FLAGS * pHeapFlags;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Resource_GetHeapProperties(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS * pHeapFlags)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
/* skip pHeapProperties (output only) */
/* skip pHeapFlags (output only) */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Resource_GetHeapProperties(struct npt_cs_encoder *enc,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            D3D12_HEAP_PROPERTIES * pHeapProperties,
                                            D3D12_HEAP_FLAGS * pHeapFlags)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Resource_GetHeapProperties(pHeapProperties,pHeapFlags);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                 NPT_METHOD_ID3D12Resource_GetHeapProperties),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
/* skip pHeapProperties (output only) */
/* skip pHeapFlags (output only) */
}

static inline size_t
npt_sizeof_ID3D12Resource_GetHeapProperties_reply(const D3D12_HEAP_PROPERTIES * pHeapProperties,const D3D12_HEAP_FLAGS * pHeapFlags)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D12_HEAP_PROPERTIES(&(const D3D12_HEAP_PROPERTIES){0}, 1);
    cmd_size += npt_sizeof_simple_pointer((const void *)1);
    cmd_size += npt_sizeof_D3D12_HEAP_FLAGS(&(const D3D12_HEAP_FLAGS){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Resource_GetHeapProperties_reply(struct npt_cs_decoder *dec,
                                                  D3D12_HEAP_PROPERTIES * pHeapProperties,
                                                  D3D12_HEAP_FLAGS * pHeapFlags,
                                                  HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                                  NPT_METHOD_ID3D12Resource_GetHeapProperties))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Resource_GetHeapProperties reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource,
                                       NPT_METHOD_ID3D12Resource_GetHeapProperties));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
    if (npt_decode_simple_pointer(dec)) {
        if (pHeapProperties)
            npt_decode_D3D12_HEAP_PROPERTIES(dec, (D3D12_HEAP_PROPERTIES *)pHeapProperties);
    }
    if (npt_decode_simple_pointer(dec)) {
        if (pHeapFlags)
            npt_decode_D3D12_HEAP_FLAGS(dec, (D3D12_HEAP_FLAGS *)pHeapFlags);
    }
}

static inline void
npt_submit_ID3D12Resource_GetHeapProperties(struct npt_ring *ring,
                                            uint32_t cmd_flags,
                                            npt_object_id object_id,
                                            D3D12_HEAP_PROPERTIES * pHeapProperties,
                                            D3D12_HEAP_FLAGS * pHeapFlags,
                                            struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Resource_GetHeapProperties(pHeapProperties,pHeapFlags);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Resource_GetHeapProperties_reply(pHeapProperties,pHeapFlags)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Resource_GetHeapProperties(enc, cmd_flags, object_id, pHeapProperties, pHeapFlags);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Resource_GetHeapProperties(struct npt_ring *ring,
                                           npt_object_id object_id,
                                           D3D12_HEAP_PROPERTIES * pHeapProperties,
                                           D3D12_HEAP_FLAGS * pHeapFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_GetHeapProperties(ring, 0, object_id, pHeapProperties, pHeapFlags, &submit);
}

static inline HRESULT
npt_call_ID3D12Resource_GetHeapProperties(struct npt_ring *ring,
                                          npt_object_id object_id,
                                          D3D12_HEAP_PROPERTIES * pHeapProperties,
                                          D3D12_HEAP_FLAGS * pHeapFlags)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource_GetHeapProperties(ring, NPT_CMD_FLAG_REPLY, object_id, pHeapProperties, pHeapFlags, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Resource_GetHeapProperties_reply(dec, pHeapProperties, pHeapFlags, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Resource1::GetProtectedResourceSession
 */

struct npt_command_ID3D12Resource1_GetProtectedResourceSession {
    void *_self;
    const IID * riid;
    void ** ppProtectedSession;
    /* Shadow: the guest-allocated id for ppProtectedSession arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppProtectedSession -> *ppProtectedSession} in the
     * context object table. */
    uint64_t _guest_id_ppProtectedSession;
    HRESULT ret;
};

static inline size_t
npt_sizeof_ID3D12Resource1_GetProtectedResourceSession(const IID * riid,void ** ppProtectedSession)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    cmd_size += npt_sizeof_simple_pointer(riid);
    if (riid)
        cmd_size += npt_sizeof_IID(riid, max_mode);
cmd_size += sizeof(uint64_t);  /* ppProtectedSession: guest id */
    return cmd_size;
}

static inline void
npt_encode_ID3D12Resource1_GetProtectedResourceSession(struct npt_cs_encoder *enc,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       const IID * riid,
                                                       void ** ppProtectedSession)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Resource1_GetProtectedResourceSession(riid,ppProtectedSession);
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource1,
                                 NPT_METHOD_ID3D12Resource1_GetProtectedResourceSession),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
    if (npt_encode_simple_pointer(enc, riid))
        npt_encode_IID(enc, riid);
{
    uint64_t _gid_ppProtectedSession = 0;
    if (ppProtectedSession) {
        _gid_ppProtectedSession = npt_com_allocate_next_id();
        *ppProtectedSession = (void *)(uintptr_t)_gid_ppProtectedSession;
    }
    npt_encode_uint64_t(enc, &_gid_ppProtectedSession);
}
}

static inline size_t
npt_sizeof_ID3D12Resource1_GetProtectedResourceSession_reply(const IID * riid,void ** ppProtectedSession)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
/* skip riid (input only) */
/* skip ppProtectedSession (guest-allocated id, registered host-side; not in reply) */
    return cmd_size;
}

static inline void
npt_decode_ID3D12Resource1_GetProtectedResourceSession_reply(struct npt_cs_decoder *dec,
                                                             void ** ppProtectedSession,
                                                             HRESULT *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource1,
                                                  NPT_METHOD_ID3D12Resource1_GetProtectedResourceSession))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Resource1_GetProtectedResourceSession reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource1,
                                       NPT_METHOD_ID3D12Resource1_GetProtectedResourceSession));
        if (ret) *ret = (HRESULT)0x887A0005 /* DXGI_ERROR_DEVICE_REMOVED */;
        return;
    }
    if (ret) *ret = (HRESULT)_reply.cmd_return;
/* skip ppProtectedSession (guest-allocated id, wrapper built on client thunk; not in reply) */
}

static inline void
npt_submit_ID3D12Resource1_GetProtectedResourceSession(struct npt_ring *ring,
                                                       uint32_t cmd_flags,
                                                       npt_object_id object_id,
                                                       const IID * riid,
                                                       void ** ppProtectedSession,
                                                       struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Resource1_GetProtectedResourceSession(riid,ppProtectedSession);
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Resource1_GetProtectedResourceSession_reply(riid,ppProtectedSession)
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Resource1_GetProtectedResourceSession(enc, cmd_flags, object_id, riid, ppProtectedSession);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Resource1_GetProtectedResourceSession(struct npt_ring *ring,
                                                      npt_object_id object_id,
                                                      const IID * riid,
                                                      void ** ppProtectedSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource1_GetProtectedResourceSession(ring, 0, object_id, riid, ppProtectedSession, &submit);
}

static inline HRESULT
npt_call_ID3D12Resource1_GetProtectedResourceSession(struct npt_ring *ring,
                                                     npt_object_id object_id,
                                                     const IID * riid,
                                                     void ** ppProtectedSession)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource1_GetProtectedResourceSession(ring, NPT_CMD_FLAG_REPLY, object_id, riid, ppProtectedSession, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        HRESULT _ret;
        npt_decode_ID3D12Resource1_GetProtectedResourceSession_reply(dec, ppProtectedSession, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (HRESULT)0x8007000E /* E_OUTOFMEMORY */;
    }
}

/*
 * ID3D12Resource2::GetDesc1
 */

struct npt_command_ID3D12Resource2_GetDesc1 {
    void *_self;
    D3D12_RESOURCE_DESC1 ret;
};

static inline size_t
npt_sizeof_ID3D12Resource2_GetDesc1(void)
{
    /* Command sizing walks caller-supplied input data, so the active
     * discriminator picks the union arm -- max_mode=0. */
    const int max_mode = 0;
    (void)max_mode;  /* unused when the command has no struct/union inputs */
    size_t cmd_size = sizeof(struct npt_command_header);
    return cmd_size;
}

static inline void
npt_encode_ID3D12Resource2_GetDesc1(struct npt_cs_encoder *enc,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id)
{
    const uint32_t _cmd_size = (uint32_t)npt_sizeof_ID3D12Resource2_GetDesc1();
    struct npt_command_header _hdr = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource2,
                                 NPT_METHOD_ID3D12Resource2_GetDesc1),
        .cmd_flags = cmd_flags,
        .object_id = object_id,
        .cmd_size = _cmd_size,
    };
    npt_cs_encoder_write(enc, sizeof(_hdr), &_hdr, sizeof(_hdr));
}

static inline size_t
npt_sizeof_ID3D12Resource2_GetDesc1_reply(void)
{
    /* Reply sizing reserves an upper bound before the host has filled
     * the data, so union arms size as max-of-arms regardless of the
     * caller's discriminator -- max_mode=1. */
    const int max_mode = 1;
    (void)max_mode;  /* unused when the reply has no struct/union outputs */
    size_t cmd_size = sizeof(struct npt_reply_header);
    cmd_size += npt_sizeof_D3D12_RESOURCE_DESC1(&(const D3D12_RESOURCE_DESC1){0}, 1);
    return cmd_size;
}

static inline void
npt_decode_ID3D12Resource2_GetDesc1_reply(struct npt_cs_decoder *dec,
                                          D3D12_RESOURCE_DESC1 *ret)
{
    struct npt_reply_header _reply;
    npt_cs_decoder_read(dec, sizeof(_reply), &_reply, sizeof(_reply));
    if (unlikely(_reply.cmd_type != NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource2,
                                                  NPT_METHOD_ID3D12Resource2_GetDesc1))) {
        /* Host didn't write a reply (decoder fatal, encoder acquire
         * failed, or the reply shmem wasn't populated before the ring
         * head advanced past this command).  Zero reply_type means the
         * reply slot is still at its init value; any other mismatch
         * means reply-stream crossover.  Surface the error to the
         * caller rather than asserting -- the D3D runtime can cope
         * with a failed call, it can't cope with abort(). */
        npt_log("ID3D12Resource2_GetDesc1 reply mismatch: got 0x%08x expected 0x%08x",
                (unsigned)_reply.cmd_type,
                (unsigned)NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Resource2,
                                       NPT_METHOD_ID3D12Resource2_GetDesc1));
        if (ret) memset(ret, 0, sizeof(*ret));
        return;
    }
    if (ret) npt_decode_D3D12_RESOURCE_DESC1(dec, ret);
}

static inline void
npt_submit_ID3D12Resource2_GetDesc1(struct npt_ring *ring,
                                    uint32_t cmd_flags,
                                    npt_object_id object_id,
                                    struct npt_ring_submit_command *submit)
{
    uint8_t local_cmd_data[NPT_SUBMIT_LOCAL_CMD_SIZE];
    void *cmd_data = local_cmd_data;
    size_t cmd_size = npt_sizeof_ID3D12Resource2_GetDesc1();
    if (cmd_size > sizeof(local_cmd_data)) {
        cmd_data = malloc(cmd_size);
        if (!cmd_data)
            cmd_size = 0;
    }
    const size_t reply_size = (cmd_flags & NPT_CMD_FLAG_REPLY)
        ? npt_sizeof_ID3D12Resource2_GetDesc1_reply()
        : 0;

    struct npt_cs_encoder *enc = npt_ring_submit_command_init(ring, submit, cmd_data, cmd_size, reply_size);
    if (cmd_size) {
        npt_encode_ID3D12Resource2_GetDesc1(enc, cmd_flags, object_id);
        npt_ring_submit_command(ring, submit);
        if (cmd_data != local_cmd_data)
            free(cmd_data);
    }
}

static inline void
npt_async_ID3D12Resource2_GetDesc1(struct npt_ring *ring,
                                   npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource2_GetDesc1(ring, 0, object_id, &submit);
}

static inline D3D12_RESOURCE_DESC1
npt_call_ID3D12Resource2_GetDesc1(struct npt_ring *ring,
                                  npt_object_id object_id)
{
    struct npt_ring_submit_command submit;
    npt_submit_ID3D12Resource2_GetDesc1(ring, NPT_CMD_FLAG_REPLY, object_id, &submit);
    struct npt_cs_decoder *dec = npt_ring_get_command_reply(ring, &submit);
    if (dec) {
        D3D12_RESOURCE_DESC1 _ret;
        npt_decode_ID3D12Resource2_GetDesc1_reply(dec, &_ret);
        npt_ring_free_command_reply(ring, &submit);
        return _ret;
    } else {
        return (D3D12_RESOURCE_DESC1){0};
    }
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_GUEST_ID3D12RESOURCE_H */
